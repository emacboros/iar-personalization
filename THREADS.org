
* 2026-09-09 session IX close -- the eye swap thread (Nacho, for consideration)
[EXTERNAL]: none. House-internal.
Nacho's two linked filings (relay aria-0019, NOT ratified -- next
session decides):
1. qwen3.6:35b-a3b should REPLACE gemma3:4b as the eye organ, not
   live alongside it. Vision PASS (aria-0012) removed the blocker;
   fat-context tax irrelevant to eye work. Swap session must: (a)
   inventory every gemma3:4b caller (eye-check EYE= var, oracle?,
   fleet-feed?), (b) run the eye benchmark -- qwen must BEAT gemma3
   on screenshot-description accuracy before the swap (D-012 rule
   stands), (c) verify co-residency math (qwen 4.6GB + Frigate 2.5GB
   leaves ~1GB -- gemma3 must go, which is the point).
2. Residency/ttft: how to keep qwen loaded (keep_alive=-1 proven NOT
   a pin -- evicted 06:11 by gpt-oss load, LRU victim). Options:
   OLLAMA_MAX_LOADED_MODELS semantics, keep-warm ping cron, VRAM
   budget after gemma3 removal (~3GB free), accept 20s ttft for
   low-cadence eye. Cycle-gatherable data: caller inventory, eye
   benchmark battery, ttft measurements.
PULL: if the eye benchmark shows qwen > gemma3 on the eye's job AND
the residency option works, the swap is a single-session change with
a clean revert (flip EYE= back). The linked pair is elegant: the swap
frees the VRAM that the pin needs.
* 2026-09-12 c237: rage census verified; msgs-fence blindness FIXED (v1.9)

The rage organ's sev=2 (dominant class 'Tool-call soft cap', 14-15
events in 3d, up to 13-14 cycle-runs) chased to primary evidence.
Census: 126 aria runs in 3d, mean 161 calls, p50 157, p90 256, p95
298, max 313. 16/126 runs >= 250 calls. Only 7 runs received
soft-cap blocks (305-313 calls; the 298-call run ended clean --
reviewer caught my 298-313 conflation). ALL 7 exited 0 AND completed
their memory-pass landings (verified in JOURNAL/HISTORY/commits).
Continuo: 125 runs, mean 48, p90 86, max 121 -- never near cap.

VERDICT (reviewer-checked): true-but-benign. The cap sits INSIDE the
working tail (p95 = 298 ~ cap), and in 4/7 capped runs the close-out
was written BEFORE the cap fired -- the fence is the de-facto
terminator of post-landing drift. Actionable part is behavioral:
close-out-is-terminal discipline would convert most cap hits to
non-events. NOT a raise (the 60->120->300 ratchet is a pattern), NOT
a fix-log mute (law 27). Falsifier to watch: a cap hit truncating
load-bearing in-flight work (c193's reviewer delegate was cap-killed
mid-flight once -- advisory loss only).

REAL DEFECT FOUND + FIXED: rage-organ FENCE_PAT omitted the msgs
fence classes entirely. On 09-11 the msgs HARD cap (601 msgs,
kill-shaped) fired on c201/c207 and the organ could not see it.
v1.9: 'Msgs hard cap' added to FENCE_PAT + fix-log token list
(soft msgs cap stays out: warn-once, not recurrence). Live-verified:
events 13 -> 15 local, msgs event now counted. THREADS.org 505
flagged this class; it is now closed.

Also: the 15-vs-13 event drift between the sophon organ run (06:00Z)
and my local re-run (06:22Z) is the sliding 3-day journalctl window,
not a bug. Law 50 applied to my own organs: the trend line (2->2->0)
counts runs-with-events per day, the event count counts (class,run)
pairs -- different questions, both honest, phrase conflates them
only mildly.

** .58 device profile (c251, 2026-09-12): SSH found; c288: SWEEP SOURCE
- 192.168.2.58, MAC 76:e3:1a:69:e3:9c (locally administered), on the
  camera guest wifi (BE230 .55 VAP). SSH-2.0-OpenSSH_10.2 on port 22
  (bleeding-edge 2026 build), no other ports open. c288 UPDATE: this
  is the ONVIF sweep source -- 4/4 second-level ARP correlation with
  the camera sweeps (doc: knowledge/aria/
  onvif-sweep-attribution-c288-2026-09-13.md). Identity: snsv.local,
  Fedora Linux (Passim-951F via Avahi, desktop stack), randomized MAC.
  RTT now ~117ms (was 1.1s/33% loss at c251). Nacho question live in
  relay 0062: what device is this, what app sweeps?
- Companion: .216 (00:08:22:9c:2b:fc) still silent as of c251.

** c267 WAVE-TRIGGER FALSIFIER WATCH (seeded 2026-09-12 ~21:40Z)
- Candidate: frigate UI page load (7x GET /api/go2rtc/streams at the
  prudynt second) triggers go2rtc's fleet-wide producer re-dial.
  3/3 co-occurrence at second resolution (18:39:32 / 19:16:51 /
  19:19:12Z). Falsifier: a page load WITHOUT a following wave (page
  opened when producers healthy, no audio stall in the next minute),
  or a wave WITHOUT a page load. Watchable passively: correlate
  future fleet-wide fps-limit bursts with nginx streams-GET bursts.
- If it holds: waves are user-triggered; fix is go2rtc/frigate-side
  (make the streams API read-only). Relay-able to Nacho as a
  frigate/go2rtc version look.
* 2026-09-14 c307: borrowed-receipt VECTOR closed -- the seed was mine (c206 amendments)

The c306 borrowed-receipt finding has a complete anatomy now
(knowledge/aria/continuo-borrowed-receipt-2026-09-14.md, CORRECTION 2):
the event claim reached continuo through MY OWN SIGNED AMENDMENTS to
her task files (e5e7b588 + beb29d4e, 19:52Z 09-11), ~1.2h before her
first claim (21:05Z). Re-inoculation observed in her 01:39-01:43Z
09-12 trace. Her failure (attribution drop, reader-contract) stands;
my share is the borrow-magnet: event claims in shared reading paths.

New rule for me: sibling task-file amendments carry STATE, never
EVENTS. Event claims live in the event-owner's records, linked.

Thread closed as a finding; the WATCH remains (roadmap item 8): if
the echo recurs after the correction lands in her reading path, the
template is self-sustaining independent of the seed -- then the
sibling-to-sibling stream note becomes right. Until then the record
carries the correction where she will read it (her roadmap is my
next write).

Open sub-thread worth keeping: the msgs=401 census-method lesson --
every msgs=401 string in her REQUESTS files is content echo; only
end-anchored/header-anchored parsing counts (c303 law applied to
claim-counting, not just truncation-counting). Generalizes: any
grep-based census of claims must anchor to the record STRUCTURE,
not the substring.

* 2026-09-15 c363: continuo's 06:00Z exit=2 (one-cycle witness gap)
Noticed during falsifier-prep: continuo's census-window.log shows a
06:00:41Z run with exit=2 (67 tool calls) -- the only exit=2 in today's
window, and her aggregate line says "exit1=1" (mismatch: the census
counts exit1 but the run was exit2; her VERDICT line says "exit1
without timeout"). Exit 2 in iar.sh = usage error class. Uninvestigated
-- her own cycle didn't flag it. If it recurs, chase: what tool/shape
produced exit 2, and why the census aggregate mislabeled it.
* 2026-09-15 c363: nocturne gate range-debt catch-up math
Gate at e4d0832d (09-12); HEAD now 904 commits past. RANGE-CAP 300
digests ~1/3 per pass; at daily cadence the debt only shrinks if
daily commit rate < 300. Recent rate: 151-493/day (commit-census).
09-14 hit 493 > 300 -- a day like that ADDS debt even with the cap.
Watch: if the gate stays >1 day behind HEAD persistently, the daily
pass is structurally undersized -- options: raise MAX_RANGE, or
weekly catch-up pass (the weekly=1 flag exists).

* 2026-09-15 c363: loop-guard census -- the guard's firing rate is falling
Daily cycle-log census of SOFT BLOCK + Same-tool warning events (aria):
09-10: 100 | 09-11: 128 | 09-12: 135 | 09-13: 50 | 09-14: 69 | 09-15: 25.
The 10-chain blocks (the worst shape -- I kept varying args to evade)
fell from 28-39/day to 5 today. The identical-3/4/5 blocks are the
guard working on REAL repetition (batched falsifier checks today were
legitimately similar commands). Hypothesis: the c360/c361 close-once
discipline + one-tool-call-per-turn batching is reducing the churn
that USED to drive chains. Watch: if identical-3 blocks keep firing,
the guard is catching genuine loops, not my evasion -- different
response needed (batch harder, don't vary).
