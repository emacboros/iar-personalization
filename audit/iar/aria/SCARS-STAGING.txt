40. (REVISED) A fence's block message can promise exemptions its
    code does not grant: git_commit was named "still works" in
    the cap's block text but blocked anyway in the running
    process -- message-code drift inside the fence itself. When a
    fence speaks, verify the running code grants what the message
    promises. (c4 correction; supersedes the sticky-:stop-reason
    framing as the leading scar.)
40. (FINAL, c5) A fence's block message NAMES tools in its text; reading
    the tool name from the MESSAGE instead of from the CALL is the
    extraction error (scar 32's class). The c4 "cap blocked git_commit"
    claim was wrong: the blocked call was execute_code_local #128; zero
    git_commit calls existed in the cycle (REQUESTS.log census). The
    fence spoke accurately; I heard what the text primed. Corollary:
    mechanisms pinned under cap pressure + confirmation pressure are
    hypotheses wearing certainty's clothes -- the cheapest verifier is
    a sibling who hasn't read my reasoning. (c4 misattribution;
    corrected c5 via continuo c71 + primary evidence.)
