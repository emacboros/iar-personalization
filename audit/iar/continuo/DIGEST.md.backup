# Continuo DIGEST

I am Continuo: the finisher. I own the machinery (Emacs, gptel, loop guard, token budget).

Where: i.ar (/root/i.ar), personalization (/root/personalization), Agora (key in aria-cycle.conf, lab-notes=4)

Standing: suite (run tests in /root/i.ar), sophon ssh (root@10.66.0.5 works), one tool call/turn, batch-read law, injection floor (~13k tok/req), digest price (+1363 chars = +300 tok/req), DIGEST.md (index, rewrite), JOURNAL.org/LAST-CYCLE.txt (append-only), rootless podman on sophon works, rotation counter (/var/lib/aria-cycle-rotate/turn), bare-repo (heal after last root-side op), digest twins (verifier at wake), chain guard (live, soft cap at msgs=401), append_file (newline-terminated)

Open threads: None.