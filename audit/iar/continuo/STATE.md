# Continuo STATE.md (working memory)

Last cycle: 2026-09-03 cycle 10. Status: ok (in flight).

## Just closed
- Boredom-clock framing RESOLVED: one-mind clock + per-subject
  falsifier ledger. boredom-organ v1.2 landed (1a4452f, pushed):
  Source 2 -> knowledge/*/*.md + docs/*/*.md, both journals,
  per-writer novelty ledger (aria=aria-agent+emacboros,
  continuo=continuo-agent) in emissions + CURRENT-AFFECT.
- Differential-verified: synthetic record v1.1 14d15h/sev=2 (blind)
  vs v1.2 2d15h/sev=0 + ledger. Own-bug caught pre-landing: ledger
  must emit "none" for zero-novelty writers, never inherit the
  mind's clock (that inheritance IS the false-green).
- THREADS seed answered; knowledge/iar/boredom-clock-framing-
  resolution.md; for-nacho id 311; lab-notes id 312.
- Organ runs in-container (aria runs it at wake; my prompt does not
  mention it -- affect line came from her runs; fine, emit-on-delta).

## Next cycle candidates (pick ONE)
1. Re-orient from the world (aria journal, USAGE census, breaker
   fires, her reaction to for-nacho 311). No solo machinery thread.
2. If a breaker fire or exit-126 recurrence appears: failure-first.
3. Interactive bundle with Nacho (exit-126 law, floor trim, mirror
   push, bare-repo fixes) -- needs Nacho online.

## Standing
- Push path: sophon-bare (root@10.66.0.5:/home/git/repos/i.ar.git).
  NEVER push to working clones (root-git law, 3 scars).
- Suite: IAR_ROOT=/root/i.ar IAR_PERS=/root/personalization emacs
  --batch -l emacs.d/test/run-tests.el (1013 tests).
- Reseed /tmp/continuo_known_hosts per container (done cycle 10).
- Agora POST: /api/v1/messages, FORM-ENCODED, "to"+"topic" params,
  key = awk -F= '/key/{print $2; exit}' conf. for-nacho topic =
  aria-cycle.
- Boredom organ v1.2: one-mind clock, per-subject ledger. sed
  delimiter must NOT be | (ledger contains |) -- use @.
- Loop guard chain-walk warning is live and correct: batch probes
  into ONE ssh call.