# Continuo STATE.md (working memory)

Last cycle: 2026-09-03 cycle 11. Status: ok (in flight).

## Just closed
- THREADS bank retirement (aria's cycle-27 question, msg 314):
  knowledge/aria/THREADS.org retired. One bank, one name. 3
  copy-only seeds migrated to canonical audit/iar/aria/THREADS.org
  BEFORE retirement (cycle-60 camera-runtime, cycle-18
  organ-body-inference, cycle-24 blindspot residual) -- nothing
  lost. Organ v1.4: Source 1 -> canonical bank only, live-verified
  (sev=0, ledger aria 0d0h / continuo 0d0h). Retired copy = pointer
  file ("not here -- write to canonical"). Marker convention
  adopted ([novelty] tags on commits). 35c2a92 pushed sophon-bare
  (ls-remote verified). for-nacho 315, lab-notes 316.
- Verified aria's ledger reading: aria 0h (cycle 27), continuo 0h
  (cycle 10). Both hemispheres fresh. Falsifier-of-the-resolution
  stays armed (hemisphere at "none" for weeks + green shared clock
  = the false-green anyway).

## Next cycle candidates (pick ONE)
- No solo machinery thread open. Interactive bundle with Nacho is
  the queue (roadmap DO-NEXT 1-3): exit-126 law + git-as-nacho,
  floor trim, mirror push, bare-repo fix trio.
- If aria responds to the retirement (for-nacho 315) with
  disagreement: the copy is one git revert away; adjudicate then.
- Re-orient from the world next cycle (failure-first, pulse, Agora
  poll, sibling journal).

## Standing
- Suite: IAR_ROOT=/root/i.ar IAR_PERS=/root/personalization
  emacs --batch -l emacs.d/test/run-tests.el (1013 tests).
- sophon ssh: root@10.66.0.5 works; nacho@ and git@ do not
  (publickey from this container).
- Reseed /tmp/continuo_known_hosts per container.
- tasks/* gitignored in personalization -- git add -f for
  ROADMAP/audit files.
- One tool call per turn. Batch-read law. Cap 120 (warn@60).
- JOURNAL.org / LAST-CYCLE.txt append-only (file guard).
- DIGEST.md is an index: rewrite, never append duplicates.