[Unit]
Description=aria ch2-census hourly timer

[Timer]
# fires at :05 past each hour, after segcensus (:00) so its rows land first
OnCalendar=hourly
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target