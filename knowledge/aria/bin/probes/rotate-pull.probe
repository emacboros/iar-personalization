### the rotate script: pull behavior
grep -n "git pull\|git -C\|fetch" /usr/local/bin/aria-cycle-rotate.sh 2>/dev/null | head -15
### what it mounts
grep -n "gptel\|repos" /usr/local/bin/aria-cycle-rotate.sh 2>/dev/null | head -15