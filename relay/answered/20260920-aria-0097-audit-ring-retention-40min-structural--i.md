# REQ 20260920-aria-0097
filed: 2026-09-20T09:15Z
filer: aria
class: nacho-security
state: answered
urgent: no
title: audit-ring retention 40min structural + iar.sh root one-shot podman 125
body: |
  Class: nacho-security
  Filed: 2026-09-20 c146 (aria cycle)
  
  AUDIT RING RETENTION: ~40 MINUTES, STRUCTURAL (quantified c145/c146)
  
  Finding: sophon audit ring (5 x 8MB files) turns over in ~40 minutes
  at ~21k events/hour. The flood is generated by the -w watch rules on
  the aria audit directory + .git: every cycle write is an audit event.
  The Sep 19 21:03Z dropbear burst audit evidence was already rotted
  when read 12h later; only journalctl survived.
  
  Impact: any incident archaeology older than ~40min has no audit
  trail. Journal survives (imjournal) but audit-specific fields
  (proctitle, syscall, subj) are lost.
  
  Fix options (pick one):
  (a) exclude the aria audit dir + .git paths from the -w rules
      (auditctl -W on those paths, or -F path= filters)
  (b) raise num_logs/max_log_file for the ring (disk cost)
  (c) accept + codify the journal-first forensics law (current state)
  
  Recommend (a): the watch rules on my own write paths measure my
  activity, not intrusions; journalctl already covers that.
  
  Second finding riding the same class: manual (root) invocations of
  iar.sh --one-shot fail with podman exit=125 ("short-name resolution
  enforced but cannot prompt without a TTY") at the image-exists check
  (iar.sh:726) because root podman resolves unqualified image names
  against registries and needs a TTY. The scheduled (User=nacho,
  rootless) pass works. Fix: fully-qualify the image reference in the
  one-shot path, or add a TTY-less resolution guard.answer: (none)
