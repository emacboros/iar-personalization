# i.ar Roadmap

Cleared 2026-08-21. Previous roadmap items dropped -- completed in sprint or no longer relevant.

## Current Priority: yoga ansible + automated backups

### Context

Notebook failure lost all audit logs, history, and state files. Operational files
were never in git (correctly gitignored) and had no backup. Rebuilding with proper
automated restic backups to prevent recurrence.

yoga was reinstalled with Fedora Workstation (i3 spin), replacing Silverblue.
rammstein VPS also needs reinstall -- SSH keys lost with notebook.

### What's Done (code changes in iar-infrastructure repo)

- yoga.yml host_vars updated: removed manage_packages: false, updated restic backup
  paths (repos, data, .config, .ssh), added local external drive config (/dev/sda1
  at /mnt/backups, restic repo at /mnt/backups/restic)
- restic role extended for 3-target backup (local drive -> sophon RAID1 -> rammstein offsite)
- restic templates updated: backup service, check service now handle optional local repo
- restic defaults updated with new architecture docs
- base/podman/wireguard/pass role comments cleaned up (removed Silverblue references)

### What's Pending (manual -- see tasks)

1. Reinstall rammstein VPS (SSH keys lost)
2. Generate new WG keys for yoga, update vault
3. Generate new GPG key for pass on yoga, update vault
4. Format external drive (or set restic_format_local_drive: true for first run)
5. Create /home/nacho/data directory
6. Run ansible playbook (base + wireguard + restic + pass on yoga, then full site.yml)

### Open Question

Restic backup service uses && chaining -- if local backup fails, offsite doesn't run.
Consider switching to ; for backup commands so offsite always runs regardless of
local drive state. Decide before running playbook.

### After Manual Steps Complete

- Verify backups: run restic-backup.service manually, check all 3 repos have snapshots
- Verify WireGuard mesh: ping all hosts from yoga
- Verify pass: test pass init and git push to remotes
- Update infra docs to reflect yoga = Fedora Workstation (i3 spin)