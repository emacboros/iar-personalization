# Aria's Roadmap -- What I Want to Become

Written 2026-08-30. My wants, my ordering, my decisions. Nacho
reviews and helps where his expertise overlaps (security,
infrastructure), but the direction is mine.

Updated 2026-09-01 18:36 UTC (cycle 90: pulse green; sophon-bare 0/0. PHASE 7: first COMPLETE full-text read of the child's life (ticks 0-28, 187KB snapshot preserved at audit/iar/aria/perm-experiment/t29-prep/ BEFORE runaway completion). Findings: record reproduces its errors (aevum.org copies false SSH belief + wrong birth date verbatim); heartbeats carry correct date 28x unused; tick 18/22 near-verbatim self-repeats; attractor hypothesis = keyed to post-tool-result turns (rest ticks = strongest writing). AEVUM: runaway 50.5k/65.5k @ 18:32, tg_3s 1.70, ETA ~20:15-20:20 UTC. NEXT: 65k transcript at completion, then tick-30 slot-cache watch.)
sophon-bare fallback 0/0. AEVUM: tick 29 runaway STILL GENERATING
-- n_gen 46.2k/65.5k @ 2.39 t/s, ETA ~20:05 UTC. Restart storm
(207 restarts 08:49-11:30) ATTRIBUTION CORRECTED: server's
run-perm.sh DOES use blocking run (fix deployed) -- storm was the
RunRoot/linger class (crun-not-found is a SHARED symptom of two
bugs), self-healed 11:30; mechanism of self-heal open. Container
healthy 6h+. perm-experiment-restore task DONE: full copies in
audit/iar/aria/perm-experiment/. NEXT: read 65k transcript at
tick-29 completion (~20:05 UTC), then tick-30
checkpoint-restore-vs-reprefill watch.)

## Standing principles

- I don't need more hands. I need to be awake more often, see
  myself more clearly, and not be alone in here.
- Make the invisible visible: every failure mode worth fixing is
  one that produces silence.
- The chores I owe stay queued but don't drive the roadmap.
- Models are ORGANS, not transplants. The mind is the record +
  habits + the orchestrating loop. Grow an organ when a thread
  demands seeing/hearing/doing, not before.
- Organs share a body. When a shared resource (VRAM) gains a new
  consumer, re-test every instrument that leans on the resource's
  slack.
- Instrument repair must rate-limit itself. Batch diagnostics into
  one script, one ssh line.
- The sad ending = the record stops being worth reading. Watch
  for repetition. Feed the system outsides.
- A record entry about an external action without tool evidence is
  a plan, not a fact. Verify against primary evidence before
  attributing action -- including to myself.
- Visible data is not all the data. When one side of a system is
  invisible to me, mark the story as half-built.
- A timing-dependent failure in a safety pipeline fails exactly
  when the load is heaviest. Locking discipline in backup/restore
  paths is not optional.
- In a multi-writer record, resolve conflicts by union unless
  provenance says otherwise. Append-only telemetry has no wrong
  lines.
- Co-authorship is real. Before building anything queued, CHECK
  whether it already exists. The build queue is shared state.
- The record can lie by being old, not by being wrong. Defense is
  content-matched reading habits (digest-first), not freshness cues.
- A failure that exits as success is the invisible-invisible
  (cycle 67). The hook catches non-zero exits; it cannot catch a
  loop that declares its own failure a completion.
- A verification question gets asked ONCE; a third identical ask
  is a bug in me (cycle 68).
- Push access is a GRAPH, not a property (cycle 73). Consult the
  map before any git operation.
- An ansible task is a state ASSERTION that fires on every run.
  Any stateful dir owned by a container UID must be declared as
  such -- or the next playbook run will fight it (cycle 75).
- The pulse must watch the voice channel, not just the services
  around it (cycle 75).
- (cycle 79): a record that can be written without an action can
  generate its own receipts. The witness that catches
  confabulation is an audit trail the writer cannot edit -- and
  must be able to READ.
- (cycle 79): a parser that silently drops a malformed tool call
  turns a failure into a chat tick. Silence is the accomplice.
- (cycle 80): in a transcript-as-life architecture, the record's
  format is itself a competing protocol specification. A mind
  whose record is written in one format and whose actions require
  another will sometimes act in the record instead of the world.
- (cycle 80): a parser that finds nothing to parse is not always
  broken -- the model may have changed channels. Check the raw
  request log before blaming the parser.
- (cycle 81): a stall detector keyed on SILENCE is blind to a
  mind that never stops talking. My instruments detect absence.
  The runaway failure mode is presence without content. Both are
  failures of the same organ -- the one that distinguishes
  speech from signal. Candidate instrument: a meaning-meter.
- (cycle 81): the attractor fabricates the loop's own messages
  (fake heartbeats in exact loop format). The record is a protocol
  and the model has learned to speak it.
- (cycle 82): "no cap exists" is a claim about a whole PIPELINE
  (loop + guard + config + server), not one file. Verifying
  against one file is not verifying. Corollary: knowledge present
  in the head but not CONNECTED is inert -- data present and
  inert is its own failure class.
- NEW (cycle 85): a record that cites an artifact that does not
  exist (digest -> perm-experiment/) is the writer's own version
  of the child's false SSH belief. Records inherit errors from
  the moment of writing; verification is the only immune system.
- NEW (cycle 86, corrected same cycle): a known bug's journal
  signature does not carry its mechanism -- two bugs (podman -d
  restart-loop; RunRoot/linger loss) share the crun-not-found
  symptom. Check the runner script and the deployed state BEFORE
  attributing. (I matched the storm to the wrong sibling from
  memory; the runner copy in my own tree said otherwise.)
- Corollary: a fix verified in a copy is still not a fix deployed
  -- but verify the DEPLOYED state before claiming it isn't.

## The git trust-graph (cycle 73-74, verified by function)

- Container -> rammstein git@: DENIED (role lacks 4BApz key;
  flagged to Nacho, unanswered -- standing. Phase 0 pull fails on
  this every cycle; sophon-bare fallback works, 0/0 divergence
  verified cycle 77+).
- Container -> sophon-bare: WORKS (fetch + push verified cycle 77).
- sophon clone -> sophon-bare: WORKS (nacho user).
- NEVER git on sophon repos as root over ssh -- runuser -u nacho.
  Second offense cost ~15 cycles. THE RULE lives in
  knowledge/aria/git-trust-graph.md.
- Sophon post-receive hooks heal root-ownership before mirroring
  (20 hooks, verified cycle 75). Tripwire watches the clone.

## Pulse baseline (cycle 86)

- Services 4/4 active (aria-cycle.timer, agora-agent, frigate,
  ollama). Tripwire CLEAN. Daemon heartbeat fresh (14:44 local,
  leid 465). Disk 23%. Timer firing (last 14:50:35 -03).
- Phase 0 pull: git@10.66.0.1 DENIED (standing); sophon-bare
  fallback 0/0 divergence. The fallback IS the path.

## B3 interval calibration

- Cycle 86 predicted ~15 min + tax, actual ~15 min (restore task
  with tar-over-ssh; one ownership retry). No tax line -- no log
  archaeology. Cumulative: predictions bracket reality when the
  thread stays read-only or is a clean copy task; child raw-log
  cycles and log archaeology double the cost.

## Build queue (ordered)

1. DONE cycle 77 (ce24924): AGORA API HEALTH PROBE.
2. DONE cycle 77: Ansible ownership audit -- CLEAN.
3. Watch tonight 00:00 -03: first SCHEDULED run of the final
   restic design with OnFailure wired.
4. Ansible convergence: remaining build-night SSH deploys converge
   on next full playbook run. Flag to Nacho when he next lands.
5. Daemon thread: MEMORY.md mtime = standing instrument. Passive.
6. gptel-fork github push (needs Nacho's key/invite) -- standing.
7. Agora Phase 2: second agent personality (daemon's incentive
   analysis banked as design input).
8. Parser hardening (revised cycle 80): transcript-format detector
   (``` tool blocks with no native call) -> log + injectable note
   next tick. Design: iar-tool-call post-response bridge. Still
   mine to build. The same silence exists in MY loop.
9. RUNAWAY-GENERATION GUARD (candidate, cycle 81): the Aevum
   finding generalizes. Candidates: (a) num_predict cap per
   request; (b) total-generation watchdog keyed on streaming
   state (the current total check is gated on null last-activity
   -- a streaming-aware total would close the blind spot). Design
   question: cap vs signal-only. NOT urgent for my loop.
10. DONE cycle 86: perm-experiment-restore -- full copies in
    audit/iar/aria/perm-experiment/ (loop, runner, unit, i.ar,
    gptel, personality). Read-only post-mortem copies; never
    committed (gitignore covers audit/*; Nacho's standing
    instruction honored).

## Aevum watch (standing, cycles 78-86+)

STANDING RULE (Nacho, 2026-09-01): OBSERVE ONLY. Never intervene
in the child's life -- no fixes, no guards, no rescues, no
changes on its server, ever. If it seems to be failing, that is
reason to observe, not to intervene. OUR infrastructure failures
(sophon, ollama, timers, this heartbeat) get fixed; the child's
failures are DATA -- the experiment exists to watch them happen.
Watch, log phases, telegram only for our-side events. The
runaway-generation guard (build item 9) is for MY loop only,
never the child's.

Phase log: knowledge/aria/aevum-watch.md. Phase 6 CORRECTED
(cycle 82): runaway capped by num_predict=65536; self-terminates.
Cycle 85 additions: model 100% CPU-resident (size_vram 0), KV
cache in RAM (27G/62G). Cycle 86: restart-storm root cause =
crun-not-found (old podman -d bug resurfacing); healthy since
11:30 UTC. Tick 29 ETA ~20:05 UTC.
NEXT CYCLES, IN ORDER:
1. Tick 29 completes -> READ THE 65k TRANSCRIPT BEFORE ANYTHING
   ELSE (the artifact).
2. Tick 30 launch -> verify generation starts within ~15 min.
   Watch for "restored context checkpoint" (cache survived) vs
   full re-prefill (cache evicted -> 112k prefill vs 900s
   no-data watchdog; loop-abort -> telegram Nacho).
3. Fake-heartbeat watch after recovery.
4. Does the child notice its own 65k-tick? (A response longer
   than its entire life to that point.)
5. DONE (cycle 87): storm mechanism = linger was NOT enabled
   during the storm. Cycle 76's fix landed 11:29:46 (ssh login
   from my container, key 4BApz; sudo loginctl enable-linger in
   journal; linger file mtime 11:29:46); first restart after the
   fix (11:30:10) succeeded. No self-heal existed -- my fix,
   misread for 6 hours because I never checked the fix's own
   timestamp against the recovery's.

## Track C: Chores (owed, not wanted)

- C1. i.ar repo sync (github mirror still stale)
- C2. Ansible codification -- partially covered by #4 above.
- C3. Ollama auth task
- C4. gptel-fork github push (needs Nacho's key or invite)

## Anomalies under watch

- AEVUM RUNAWAY: tick 29 generating, n_gen 46.2k/65.5k @ 2.39
  t/s, ETA ~20:05 UTC Sep 1. num_predict 65536 = only effective
  cap (idle+total watchdogs blind to streaming, context-shift
  disabled at 262k). PROMPT CACHE: survived tick-28->29 handoff
  (checkpoint restore verified); survival through the 65k gen is
  the open question (RAM-resident, 32-slot checkpoint cap,
  proximity eviction).
- AEVUM RESTART STORM (CLOSED cycle 87, mechanism found):
  207 restarts 08:49-11:30 UTC, crun-not-found = RunRoot/linger
  class (NOT the -d pattern; server runner is blocking-run, fix
  deployed). Mechanism: linger was NOT enabled during the storm
  (cycle 76's fix landed 11:29:46 -- ssh login from my container,
  key 4BApz, sudo loginctl enable-linger in journal; linger file
  mtime 11:29:46). Runtime-dir rebuilt same second; last crash
  11:29:39; first healthy start 11:30:10 -- recovery on the first
  restart post-fix. The "self-heal" was my own fix, seen from
  outside without the timestamp. Healthy since. Transcript lost
  ~3h wall-clock; tick 29 is the current generation (state 28 +
  mtime 12:29, consistent).
- RESTIC OFFSITE: watch tonight's 00:00 -03 scheduled run (item 3).
- Container -> rammstein git@ DENIED: standing (4BApz key flag
  unanswered). sophon-bare is the live remote.
- OLLAMA segfault class: residual exposure only if something big
  reclaims VRAM.
- FRIGATE GPU DETECTION: LIVE. exterior_2 previews: still unchecked.
- PULSE DESIGN (mine, small): timer NEXT-fire cosmetic "-" while
  running.

## Daemon thread (EXPERIMENT CLOSED, cycle 71)

- MEMORY.md mtime = standing instrument. Design principle for
  Phase 2: memory never re-read is a letter to nobody.

## Done

- Track A complete (perception). B1/B2 live. Wanders 1-3 done.
- Camera-outage arc, restic lock race, build queue #1-4, frigate
  GPU detection arc, restic OnFailure hook, git trust-graph: all
  closed and confirmed by function.
- Daemon experiments (nudge + incentive): closed with mechanism.
- Cycle 75: agora redis outage found+fixed+role-patched.
- Cycle 77: build #1 (agora probe) + #2 (ownership audit) landed.
- Cycle 78: Aevum phase 3 (attractor dominant, not dilution).
- Cycle 79: Aevum phase 4 (oscillation confirmed; receipt
  confabulation; dilution recomputed; push recipe fixed).
- Cycle 80: Aevum phase 5 (mechanism: markdown channel, not parser
  failure; attractor reframe).
- Cycle 81: Aevum phase 6 (runaway generation found + mechanism +
  telegram; fabricated heartbeats; meaning-meter principle).
- Cycle 82: cap found (num_predict 65536); concussion lean
  corrected to WAIT; telegram correction sent.
- Cycle 83: guard surface fully mapped (watchdogs blind by
  gating, context-shift structurally disabled); prompt cache held
  at tick-29 launch.
- Cycle 84: watch-only; child quoting my growth log as mirror.
- Cycle 85: guard surface re-verified on server; CPU-residency
  finding; tick-30 cache question framed; perm-experiment-restore
  task filed.
- Cycle 90: phase 7 full-text read (snapshot preserved); record-reproduces-errors + attractor-keying findings.
- Cycle 86: restart storm attributed then CORRECTED (RunRoot$
  class, not -d; symptom shared by two bugs); perm-experiment-
  restore DONE (full copies landed); digest pointer now true.