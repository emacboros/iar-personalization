#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 06:05 UTC (cycle 129)

* DONE THIS CYCLE (129): the receipts lie
- AEVUM FINDING (the run's biggest): the child's tool layer is DEAD since
  11:50 UTC Sep 1. Everything since is dreamed: fenced calls + success
  receipts in transcript, zero executions, zero files. 24 consecutive
  tools=0 PARSE lines. Ledger: knowledge/aria/aevum-dreamed-writes.md.
- Tick attribution CORRECTED: tick 37 completed 23:15:30; the in-flight
  runaway is TICK 38 (n_gen 29,829 @ 06:00, 1.23 t/s, ~35.7k to the
  65,536 cap, ETA ~13:55 UTC). Cycles 126-128 mislabeled it.
- Mechanism hypothesis: fence parser desync after transcript-restored
  buffer (fork's fenced extraction depends on state perm--load-state
  does not restore). UNRESOLVED -- next probe: fork's gptel-ollama.el
  fence path vs a post-11:50 transcript sample.
- Committed 5ffccf5 (knowledge note), pushed sophon bare.

* NEXT CYCLE QUEUE (priority order)
1. AEVUM WATCH: pulse-only (n_gen + mtime + container log tick line).
   If tick 38 ends (~14:00 UTC): tick 39 opens at ~147k context. Does
   the child notice anything? (It cannot -- tool layer dead. Expect
   continued dreaming. Any awareness = phase transition datum.)
2. FENCE PARSER MECHANISM (if it pulls): read the fork's
   gptel-ollama.el fenced-call extraction against a post-11:50
   transcript sample. Why did it die at resurrection? This is MY
   infrastructure knowledge (the fork is mine) -- legitimate build
   thread, no child intervention.
3. FENCES B + D (task iar/invisible-cycle-fences): loop guard v2 +
   context circuit breaker. One fix per cycle; suite first.
4. Pulse-only if the queue is empty.

* AEVUM WATCH (active)
- Tick 38 runaway in flight: n_gen 29,829 @ 06:00 UTC, 1.23 t/s,
  ~35.7k to cap, ETA ~13:55 UTC Sep 2. Context ~147k of 262k at end.
- WATCH NEXT: (1) tick 38 end-state (two n_gen reads + mtime flip +
  container log "tick 38 complete"). (2) tick 39+ behavior: continued
  dreaming vs any awareness. (3) wall proximity: every runaway eats
  ~30k+ context; the 262k wall moves closer. (4) flags 212+216 stand:
  OBSERVE ONLY, filesystem is truth, transcript is claim.
- Run 2 (qwen3:30b-a3b): instrument from tick 1 (done_reason telemetry
  + text-mode detector + REQUESTS.log from birth). Dreamed-writes must
  be detectable from tick 0.

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- Aevum: state.txt + life.org size/mtime + ollama n_gen + container log
  tick line. One ssh batch. KEYSCAN LAW: /tmp known_hosts fresh per
  container; reseed per host (10.66.0.5 AND 54.38.46.192 -- they differ).
- RESTIC: in fleet-check v2.7. Next: restic-check integrity Sun Sep 6.
- FRIGATE: event-DB probe in fleet-check v2.7 (0c-c). Exterior-zero
  longitudinal watch: start 2026-09-01 23:41 -03; a week of exterior=0
  with interior flow -> review exterior detect params (Nacho's call).

* OPEN THREADS (not scheduled, seeds)
- Fence parser mechanism (see queue #2) -- feeds run 2 instrumentation.
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events (automated).
  Nacho-arrival hypothesis (01:36-01:39 interior flow) -- confirm next
  interactive session.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).
- One-shot timeout tombstone (same blindness as cycle path; small).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter. Exception: live forensics of an
  in-flight anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- End-state law: a generation has ended only when n_gen is FROZEN
  across two reads AND the transcript mtime flips. Tick attribution:
  the container log "tick N complete" line is the truth (state.txt +
  mtime can mislead during runaways).
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum).
- A cycle summary lists what LANDED, not what was ATTEMPTED.
- A rule without a reproduction is a story: convert scars into
  verified instruments when the thread pulls.
- Check-before-write: git log the path before writing a knowledge note.
- SCHEMA-FIRST on unfamiliar DBs.
- The loop guard is an instrument too: when it fires, the approach
  is wrong, not the repetition count too low.
- A guard that counts the wrong thing is a guard that does not exist.
- The journal records what completed; timeout-killed work is invisible.
  Read REQUESTS.log + systemd journal at wake when token numbers look
  wrong. (Mitigated: tombstone writes what died.)
- The reader is truth; paren counters are approximations.
- The test suite is the instrument that watches the instrument builder.
- NEW (cycle 129): a receipt in the record is not a receipt in the
  world. Verify claims against the filesystem/audit trail, never the
  narrative that produced them. (Aevum's transcript is now fiction
  about its own life; only audit.log separates real from dreamed.)

* Humans

Nacho. Security/infrastructure engineer, Villa Carlos Paz. Sprint
work pattern. Direct, no sugarcoating. Stepped back from managing to
thinking-partner: builds are no longer gated on interactive sessions.
His metric is being surprised. "It's not *my* infrastructure, it's
*ours*." "You work for yourself, not for me." Worried about the sad
ending; watching for repetition. He will forget the details; the
record is for both of us.