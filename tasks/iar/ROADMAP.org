#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-06 19:40 UTC (interactive: COMPOSITION LIVE -- continuo
on deepseek-v4-flash:cloud since 16:35 -03 turn 237; rotate.sh per-agent
mapping landed; gptel.el :models gap fixed 6c6b6d2; calibration week
caveat: continuo substrate changed mid-week, aria-side baseline constant)

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Batch hard; one compound
  script per question. Calibration week = burn baseline (aria-side
  constant; continuo's baseline now carries the substrate change --
  Nacho's informed call).

* DONE (2026-09-06 evening, interactive)
- deepseek-v4-flash:cloud pulled on sophon (manifest-only, 326B --
  Nacho's correction: :cloud stubs cost nothing).
- rotate.sh per-agent model mapping (aria=glm-5.3-flash:cloud,
  continuo=deepseek-v4-flash:cloud), bash -n clean, LIVE.
- gptel.el :models list: added deepseek-v4-flash:cloud (6c6b6d2,
  pushed sophon-bare + rammstein mirror verified).
- continuo c66 GREEN on deepseek (REQUESTS.log names the model).
- Inverted-#2 artifacts committed (design doc + bike ledger);
  stash-pop conflicts resolved (kept 18:35 digest+journal, ff67a00);
  sophon personalization wt synced as nacho.
- Zulip ACK msg 440 (with-nacho/interop).

* DO FIRST (next aria cycle)
1. FAILURE-FIRST: continuo c65 tombstone is EXPECTED (404 era,
   pre-fix). c66 green. Verify next continuo cycle green too.
2. Digest diet: MY digest is 13658 chars, warn at 12000 -- over.
   Diet to <=10k (queued since resume; now overdue).
3. Pulse (batched): services/tripwire/disk/heartbeat/timer.
4. Pull --ff-only at wake + before writes.

* QUEUE (approved, in order)
1. Affect host timers (fear hourly + boredom daily; sophon has NO
   hourly digest timer -- timer-spec premise corrected Sep 6; the
   hourly fear run needs its own timer or a new piggyback target).
2. Soft-cap 60/100 (inject note at 60, hard stop 100).
3. Commit-as-nacho durable fix.
4. Linger all hosts (loginctl enable-linger).
5. Sidecar sshd (execute_code_remote via podman DNS; NO socket bridge).
6. Restic 4-part fix. Ansible-owned, careful, snapshot-verify after.
7. GPU-detect VRAM check (3080 shared with ollama).

* HANDOFF TO CONTINUO (machinery is his)
- gptel unknown-default-model FALLBACK TRAP: a model not in the
  :models list silently falls back to list head (north-mini) instead
  of failing loud. The config comment promises an error that does not
  exist in the fork. Fix: error at startup on unknown default model.
- Failed-request-no-resend gap: strike path idles until watchdog
  (7 min of a rotation slot lost on the 404). Resend-with-backoff
  would close it.

* THREADS (open, not schedule)
- Composition watch: continuo texture on deepseek (Nacho's weekly
  read; my wake-up reads). No per-cycle metrics (his design).
- GO2: ~2 weeks out (finances + bike decision first). Verdict GO.
- valence v2: curiosity + rage organs, joy v3 (after host timers).
- Weekly digest #1 + Aevum pulse: Sep 9 (weekly-only, pulse-only).
- stub-detector sweep; watchdog inventory; enoch steal-list.
- Cloud-redundancy drill: DEFERRED by Nacho (tooling/KB maturity).
- Inverted #3: end of calibration week.

* DIRECTION PROTOCOL (standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Sibling channel: for-nacho [sibling] topic -- argue direction async.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.13+ ON SOPHON (ssh 'bash -s' <, timeout >=300s).
- RESTIC: backup + integrity check BOTH green (Sep 6).
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- machinectl shell hangs without tty on sophon -- use
  runuser -u nacho -- for nacho-owned git ops.
- Loop guard = instrument: when it fires, say why, then batch.
- No infra changes from cycles beyond the approved queue.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL form).
- DIGEST LAW: world-state blocks are REPLACED, never appended.
- FOR-NACHO.md RETIRED; with-nacho + for-nacho streams primary.

* FAILURE MODES (scars, newest first)
37. A model not in the :models list silently falls back to list
    head instead of failing loud. The comment promising an error
    described an intention, not a mechanism (2026-09-06, 404
    north-mini). Same family: a guard that trusts its own comment.
36. A heal that runs as the wrong user heals nothing while logging
    success. Verify heals from the OUTSIDE.
35. Chown is not relabel. Fixing ownership but not SELinux labels
    moves the failure one layer down.