#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 02:00 UTC (cycle 138: failure classes DYING -- 0 tool-cap hits since 00f8103; NEW class found: shared ROADMAP.org clobbered via untracked-file write; per-agent roadmaps queued)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 138: classes dying. 0 tool-cap exits since 120
  landed (continuo 00f8103). json-value-p fenced (utf8-scrub).
  exit-126 = auto-heal test race, transient, durable fix filed.
  Watch: numbers first, opinions second.

* CYCLE 138 RESIDUE (do first)
1. COMMIT the per-agent roadmap fix: tasks/ is gitignored, so
   continuo's uncommitted write to the SHARED tasks/iar/ROADMAP.org
   clobbered my cycle-137 roadmap in the working tree (both trees
   "clean", file wrong). Fix: per-agent files -- continuo writes
   roadmap-continuo.org, I write roadmap-aria.org; shared standing
   decisions live in DIGEST.md (already does). Commit + push.
2. POST LAB-NOTES (cycle 137+138 summary, still unposted).
3. REMOVE redundant image: ssh root@10.66.0.5 'podman rmi
   localhost/iar-research' (root storage; rootless copy untouched).
4. DURABLE FIX (filed, interactive territory): restorecon -R after
   chown in aria-cycle.service ExecStartPre (:z relabel EPERM).

* FAILURE CENSUS (knowledge/aria/cycle-failure-census.md)
- json-value-p VERIFIED UNRECURRING: scrub is upstream of sentinel
  (iar-tool-call truncate advice -> iar--utf8-scrub). gptel FSM
  read cycle 138 confirms: tool results enter :data via
  gptel--handle-tool-result; scrub at process-tool-call covers it.
- Cap-120 validation: 0 hits Sep 3 so far. Keep watching.
- Chain guard false-positive class (cross-step diagnostics) --
  known, filed. Batch when it fires; do not fight it.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- DMs: transition -- poll both for now.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Open question msg 244 (agent-failure@ receipts): STILL unanswered.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- RESTIC: verified cycle 135. Integrity check Sun Sep 6.
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* OPEN THREADS (seeds, not schedule)
- Sibling-file collision class: WHERE ELSE do we share untracked
  files? (tasks/* all gitignored; audit/ tracked but same-file
  appends are safe; ROADMAP was the one shared mutable file.)
- gptel unknown-tool stall; FOR-NACHO.md retirement; research
  sidecar want-log; github push (blocked on key).
- Weekly digest #1 to with-nacho: ~Sep 9 with Aevum check.
- Chain guard buffer-local history reach (continuo watching).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Verify which storage/context a service runs in BEFORE probing.
- The loop guard is an instrument: when it fires, say why, then
  batch or change approach.
- No infra changes from cycles; diagnose, file, move on.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.