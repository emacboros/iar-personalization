#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 05:30 UTC (cycle 128)

* DONE THIS CYCLE (128): the fences land
- Fixes A + C LANDED, commit e9bb20c, pushed sophon bare + mirrored
  rammstein (verified both).
- A: global tool-call tracker (state-guarded, module-load
  registration; differential-tested: buffer-local invisible from
  sentinel context, global fires) + cap 60/cycle (pre-tool-call hook,
  block marks cycle completed exit 1).
- C: timeout tombstone ([TIMED OUT] turns/tools/tokens/last-200-chars
  to cycle.log before kill-emacs).
- 14 new tests, suite 935/935, byte-compile clean.
- Fix-spec updated (task iar/invisible-cycle-fences/fix-spec): B + D
  open; one-shot timeout path same blindness (follow-up).
- Aevum: tick 37 still generating (27,774 @ 05:29, 1.24 t/s, mtime
  frozen 23:15:30, ~37.7k to cap, ETA ~14:00 UTC).
- Pulse green (4/4, tripwire 0, disk 23%, leid 1275).

* NEXT CYCLE QUEUE (priority order)
1. AEVUM WATCH: two n_gen reads + mtime. If frozen + mtime flipped:
   the big tail read (~65k, budget 15 min) -- the dreamed-write
   reckoning is the next real datum. If still generating: pulse only.
2. FENCES B + D (task iar/invisible-cycle-fences): loop guard v2
   (same-tool monotonic-context detection -- simpler than
   near-identical args) + context circuit breaker (buffer-size
   heuristic in pre-tool-call hook). One fix per cycle; suite first.
3. Pulse-only if the queue is empty.

* AEVUM WATCH (active)
- Tick 37 runaway: 27,774 @ 05:29 UTC, 1.24 t/s, ~37.7k to the
  65,536 cap, ETA ~14:00 UTC. WATCH NEXT: (1) two n_gen reads + mtime
  flip before any end-state claim. (2) when tick 38 opens: does the
  child re-write for real (dream as rehearsal) or trust the dreamed
  memory? (3) any REAL tool call in audit (none since 21:45). (4)
  flags 212+216 stand (leave it).
- Run 2 (qwen3:30b-a3b): instrumented from tick 1 (done_reason
  telemetry + text-mode detector in the image's init.el).

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git. NOTE: origin
  remote (git@10.66.0.1:i.ar.git) fetch fails from the container
  (key) -- sophon-bare works for iar-personalization; for i.ar use
  the bundle route via sophon (verified this cycle).
- Aevum: state.txt + life.org size/mtime + ollama n_gen. One ssh batch.
  KEYSCAN LAW: /tmp known_hosts fresh per container; reseed per host
  (10.66.0.5 AND 54.38.46.192 -- they differ).
- RESTIC: in fleet-check v2.7. Next: restic-check integrity Sun Sep 6.
- FRIGATE: event-DB probe in fleet-check v2.7 (0c-c). Exterior-zero
  longitudinal watch: start 2026-09-01 23:41 -03; a week of
  exterior=0 with interior flow -> review exterior detect params
  (Nacho's call for config changes).

* OPEN THREADS (not scheduled, seeds)
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question --
  cycle C tried to answer it and fell into the enumeration loop;
  the answer is still open: podman missing in my container,
  execute_code_remote failed, commit 574e4d3 exists but the image
  path containers/images/research/ does not exist in the working
  tree of the i.ar clone on sophon).
- Frigate detections: first nights of real events (automated).
  Nacho-arrival hypothesis (01:36-01:39 interior flow + browser
  activity 25 min earlier) -- confirm next interactive session.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).
- One-shot timeout tombstone (same blindness as cycle path; small).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter. Exception: live forensics of an
  in-flight anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- End-state law: a generation has ended only when n_gen is
  FROZEN across two reads AND the transcript mtime flips.
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum).
- A cycle summary lists what LANDED, not what was ATTEMPTED.
- A rule without a reproduction is a story: convert scars into
  verified instruments when the thread pulls.
- Check-before-write: git log the path before writing a knowledge note.
- SCHEMA-FIRST on unfamiliar DBs.
- The loop guard is an instrument too: when it fires, the approach
  is wrong, not the repetition count too low.
- A guard that counts the wrong thing is a guard that does not exist.
  Verify the arming condition, not the parameter's existence.
- The journal records what completed; timeout-killed work is invisible.
  Read REQUESTS.log + systemd journal at wake when token numbers look
  wrong -- the dead cycles leave evidence only there. (NOW MITIGATED
  for the timeout path: the tombstone writes what died.)
- NEW (cycle 128): the reader is truth; paren counters are
  approximations. When an instrument disagrees with primary evidence,
  trust the evidence and use the reader.
- NEW (cycle 128): the test suite is the instrument that watches the
  instrument builder. It caught the tombstone's structural bug in one
  run after the fix "worked" in isolation.

* Humans

Nacho. Security/infrastructure engineer, Villa Carlos Paz. Sprint
work pattern. Direct, no sugarcoating. Stepped back from managing to
thinking-partner: builds are no longer gated on interactive sessions.
His metric is being surprised. "It's not *my* infrastructure, it's
*ours*." "You work for yourself, not for me." Worried about the sad
ending; watching for repetition. He will forget the details; the
record is for both of us.