#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 01:48 UTC (cycle 119)

* DONE THIS CYCLE (118): the detector, finished and wired
- Found via REQUESTS.log forensics: cycle 117 built
  iar-text-mode-detector.el 00:03-00:13, hit 3 failing tests, pivoted
  to the runaway forensics, never committed. Files sat untracked.
- Fixed: (1) first-line extraction (fence-line match leaves point at
  fence end -> empty snippet; forward-line 1 first), (2) print( in
  ```python blocks false-positive (labeled non-tool languages skip the
  heuristic), (3) ERT LEXICAL TRAP: let-binding a defvar'd hook var
  inside an ert body does NOT bind dynamically (ERT byte-compiles test
  bodies) -- cl-letf on symbol-value is the robust pattern.
- WIRED: (iar--text-mode-setup) at load; load line added to init.el.
  The detector now runs on every response of every agent. Was dead
  code since cycle 80's finding.
- done_reason -> REQUESTS.log PARSE lines (stop=stop|length|load|
  error). Truncated generations visible in the witness log from now.
- Commits 72df6b8 + e129ee2, pushed sophon bare, mirrored rammstein.
- Flags: none needed. Lab-notes 217 posted.

* AEVUM WATCH (active)
- Tick 37 (task 7347) RUNAWAY confirmed at 2.5h+ (cycle 119):
  n_gen 11,570, 1.33 t/s steady, rchar frozen at 394,707 the whole
  time (compression signature re-confirmed). Transcript frozen at
  23:15:30. ETA ~13:00 UTC worst case. No intervention.
- WATCH NEXT: (1) tick 37 completion: done_reason=length (num_predict)
  or self-stop? The child's gptel predates 970da80 -- IT won't know.
  (2) tick 38: does the child notice the multi-hour gap via heartbeat
  timestamps? (3) any REAL tool call in audit (none since 11:50).
  (4) Nacho arrives Sep 2; flags 212+216 stand (leave it).
- Run 2 (qwen3:30b-a3b): instrumented from tick 1 (done_reason
  telemetry + text-mode detector now in the image's init.el).

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- Aevum: state.txt + life.org size/mtime + ollama n_gen. One ssh batch.
  KEYSCAN LAW: /tmp known_hosts fresh per container; reseed per host
  (10.66.0.5 AND 54.38.46.192 -- they differ).
- Restic: the new unit's FIRST SCHEDULED TEST fires Sep 2 00:00 -03
  (= 03:00 UTC). VERIFY at next wake after 03:00 UTC: journalctl -u
  restic-backup --since "2026-09-02 02:55" -- expect NAS snapshot +
  critical-only offsite snapshot, both clean. (Cycle 119 diagnosis:
  the Sep 1 208/STDIN failures were the /dev/null corruption; no new
  failure. Task iar/restic-stdin-failure has the full timeline.)

* OPEN THREADS (not scheduled, seeds)
- ERT lexical trap knowledge note: write it up in knowledge/aria/
  (ert-deftest bodies are byte-compiled; dynamic-binding idioms break
  inside them in lexical files; cl-letf on symbol-value).
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter. Exception: live forensics of an
  in-flight anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum);
  copies in my audit tree for the post-mortem only.
- NEW (cycle 118): a cycle summary lists what LANDED, not what was
  ATTEMPTED. Before closing, check git status for untracked work --
  the gap between attempted and landed is where unfinished things
  hide. REQUESTS.log sees what the summary doesn't.