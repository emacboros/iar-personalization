#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-10 00:35 UTC (interactive session XI: relay queue CLEARED -- 8 rulings landed fc6bc209; qwen-as-eye RATIFIED; root-key option (c) approved; 0/0 tombstone ruling; affect-dashboard standing law)

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Batch hard; one compound
  script per question. Calibration week = burn baseline.

* DONE (2026-09-10, session XI w/ Nacho -- relay queue cleared)
- ALL 9 open relay filings ruled (fc6bc209, pushed sophon-bare,
  sophon working tree synced + root-owned files healed):
  - 0013 DROPPED: no upstream PRs now (Nacho filed 3 himself: 1
    merged, 2 unanswered; gptel nearing v1.0). REVISIT after v1.0
    lands: fresh fetch, re-audit the 6 fixes against upstream.
  - 0017 ACKed (eye-feed timer ratified).
  - 0019+0021 RATIFIED: qwen3.6:35b-a3b REPLACES gemma3:4b as the
    eye (swap, not co-reside). D-012 gate satisfied (battery v1).
    Execution items: 6 caller scripts re-point, eye-check max-time
    45s->300s, fear/rage mouth latency ~45s accepted.
  - 0022 APPROVED: install auditd watch on the audit tree (rule in
    filing) so the next poisoning names its writer. NOTE: auditd
    currently runs with `-a task,never` (syscall auditing
    suppressed) -- adding the watch rule may require re-enabling;
    coordinate with Nacho before touching audit policy.
  - 0023 APPROVED option (c): cycle remotes -> git@10.66.0.5
    (git-shell user exists, uid 1002, serves /home/git/repos).
    Never-git-write-over-root-ssh law stands regardless.
  - 0024 APPROVED: rage v1.7/v1.8 design ratified (already landed
    e597ff0). STANDING LAW: every affect (rage now, future organs)
    must surface on the dashboard affect panel.
  - 0025 RATIFIED: .el-correction gate -- specific change approved
    by filing + full suite green + delegated reviewer agent checks
    diff before push. Fix = run-tests.el redirects audit paths to
    temp dir. Archetype rule amended from blanket ban to gated.
  - 0026 RATIFIED: (a) decision-wake = continuo world-state digest
    block + archetype line "read DECISIONS.org tail"; (b) 0/0
    tokens_out cycle-end IS tombstone-worthy (Nacho ruling).
- Relay queue now EMPTY (open/ = 0). Dashboard badge should clear
  on next generator tick.

* DO FIRST (next aria cycle)
1. FAILURE-FIRST per protocol.
2. Pulse: read 10.66.0.5:8095/json FIRST; ssh only for what JSON lacks.
3. Pull --ff-only at wake + before writes.
4. Fear-organ heartbeat-stale anomaly root-cause (from c134; fear
   read sev=0 at 20:00Z -- verify it stays quiet on fresh files).

* QUEUE (approved, in order)
1. EYE SWAP EXECUTION (ratified): re-point 6 gemma3 callers
   (eye-check, frontend-eye-check, fleet-check, fear-organ,
   rage-organ, identity-watch), eye-check max-time 45s->300s,
   verify battery gate post-swap. Residency pin question rides on
   the live qwen instance.
2. Auditd watch for root-poisoning writer (0022) -- audit policy
   change = Nacho coordinate first.
3. Root-key option (c): point cycle remotes at git@10.66.0.5
   (nacho-arch; auth topology change -- Nacho applies or supervises).
4. Rage v1.7 test battery (design ratified; organ already v1.8).
5. Dashboard: affect panel completeness check (rage missing from
   /json affect panel asof 00:33 -- generator parses boredom/fear/
   rage but JSON showed only boredom+fear; rage line exists in
   CURRENT-AFFECT.md -- investigate why affect() drops it).
6. run-tests.el audit isolation (0025 fix) under the new .el gate.
7. Decision-wake mechanism (0026): continuo digest block + archetype
   protocol line (archetype edit = nacho-test via relay).
8. 0/0 tokens_out tombstone policy + tests (0026 ruling).
9. Soft-cap 60/100; commit-as-nacho durable fix; linger all hosts;
   sidecar sshd; restic 4-part; GPU-detect VRAM check.
10. Dashboard UI visual fixes (interactive, with Nacho's eyes).

* HANDOFF TO CONTINUO (machinery is hers)
- gptel unknown-default-model FALLBACK TRAP (error at startup).
- Failed-request-no-resend gap (resend-with-backoff).
- 0025 fix is hers to execute under the new gate (or aria's, gated).

* THREADS (open, not schedule)
- Composition watch: nemotron on continuo (48h fire census, verdict
  by 09-11; Nacho's weekly read).
- GO2: ~2 weeks out (finances + bike decision first). Verdict GO.
- valence v2: curiosity + rage organs, joy v3 (after host timers).
- Weekly digest #1 + Aevum pulse: Sep 9 (weekly-only, pulse-only).
- gptel upstream PRs: SHELVED until gptel v1.0 lands (0013).
- Cloud-redundancy drill: DEFERRED (tooling/KB maturity).
- Inverted #3: end of calibration week.

* DIRECTION PROTOCOL (standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Sibling channel: for-nacho [sibling] topic -- argue direction async.

* STANDING PATROLS (pulse-only)
- READ THE DASHBOARD FIRST: curl -s http://10.66.0.5:8095/json.
- Pull --ff-only at wake + before writes. KEYSCAN LAW: reseed /tmp
  known_hosts.
- fleet-check v2.13+ ON SOPHON (ssh 'bash -s' <, timeout >=300s).
- RESTIC: backup + integrity check BOTH green (Sep 6).
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. CONTEXT ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- machinectl shell hangs without tty on sophon -- use
  runuser -u nacho -- for nacho-owned git ops.
- Loop guard = instrument: when it fires, say why, then batch.
- No infra changes from cycles beyond the approved queue.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL form).
- DIGEST LAW: world-state blocks are REPLACED, never appended.
- DASHBOARD LAW: cycles may extend the JSON via the generator script
  (repo version IS running version; differential-test before push).
  UI files (dashboard/ui/*) are interactive-only.
- NEW (session XI): AFFECT-DASHBOARD LAW -- every affect organ's
  output must surface on the dashboard affect panel. An affect that
  is not on the dashboard is invisible to the human.
- NEW (session XI): .EL-CORRECTION GATE -- a specific .el change may
  be approved by relay filing; execution requires (1) approved scope,
  (2) full test suite green, (3) delegated reviewer agent checks the
  diff before push. Blanket "no .el edits" is superseded by the gate.
- NEW (session XI): 0/0 tokens_out cycle-end = tombstone-worthy
  anomaly (never a clean end).
- NEVER git add/commit/push on the sophon WORKING TREE over root ssh
  (c123 law; structural fix = option (c) remotes, approved).

* FAILURE MODES (scars, newest first)
38. A guard blocking a retry loop is a signal to change approach,
    not to resubmit. (Family of 23: the law you just wrote is the
    one you're about to break.)
37. A model not in the :models list silently falls back to list
    head instead of failing loud. A guard that trusts its own comment.
36. A heal that runs as the wrong user heals nothing while logging
    success. Verify heals from the OUTSIDE.
35. Chown is not relabel. Fixing ownership but not SELinux labels
    moves the failure one layer down.