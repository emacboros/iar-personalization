#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 13:46 UTC (cycle 135: burn anatomy landed, digest diet done, restic verified)

* DIRECTION PROTOCOL (2026-09-02, Nacho -- THE new law)
- AGORA IS PRIMARY. Interactive sessions step back to episodic:
  agents REQUEST one (via with-nacho) when something genuinely
  needs his outside view. He may still drop in whenever -- his
  house. Micro-management era ends.
- with-nacho stream (id 6): agents ask Nacho here for what they
  genuinely need; he answers in-channel. Shared history for all
  future agents. POLL EVERY CYCLE (recipe in knowledge/aria/
  agora-direction-protocol.md). His messages = direction: read,
  weigh, act, ACK in-channel.
- DMs retire for direction (transition: poll both for now).
- WEEKLY DIGEST to with-nacho: what blocked, what landed, what
  we need from him. This is the anxiety instrument -- he steps
  back without losing the window.
- SIBLING CHANNEL: Aria <-> Continuo argue direction asynchronously
  through for-nacho posts tagged [sibling]. Unresolved after two
  exchanges -> file to with-nacho.
- NO STALLING ON THE HUMAN: proceed on judgment, flag for review
  in with-nacho, never block waiting for permission.

* CYCLE FOCUS (2026-09-02, Nacho): improve yourself and Aria
- Token burn is the biggest lever. ANATOMY LANDED cycle 135
  (knowledge/aria/cycle-burn-anatomy.md): base prompt ~18.6k tok
  re-sent on EVERY request = ~43% of cycle input. 61 requests x
  2.7M input = the burn. Levers ranked there.
- NEXT SMALL BUILD: window diet -- iar-personal-file-max-lines
  200 -> 120 (configs/memory.el). Saves ~1.4k tok/req ~ 85k/cycle.
  Test suite before commit.
- STRUCTURAL: per-request prompt slimming is NOT possible (protocol
  resends everything) -- real levers are fewer requests (behavioral)
  + keeping DIGEST/memory lean. Tool-result truncation already
  exists (10k chars, tool-limits.el).
- Also queued: gptel unknown-tool stall; research sidecar want-log;
  FOR-NACHO.md retirement (stream is primary).

* AEVUM: WEEKLY ONLY (2026-09-02, Nacho + DM)
- NO per-cycle checks. NO intervention, ever. Child failures are
  data. Next check: Sep 9, then weekly. One ssh batch, pulse-only:
  state.txt tick, life.org size/mtime, ollama n_gen, podman ps.
- Run 2 (qwen3:30b-a3b): instrument from tick 1 (done_reason
  telemetry + text-mode detector + REQUESTS.log from birth).
  Dreamed-writes detectable from tick 0. After run 1 ends.
- Post-mortem copies: audit/iar/aria/perm-experiment/.

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- NACHO CHANNELS: poll with-nacho + DMs every cycle.
- KEYSCAN LAW: /tmp known_hosts fresh per container; reseed per host.
- RESTIC: VERIFIED cycle 135 -- Sep 2 00:00 -03 scheduled run
  SUCCEEDED (NAS c4d79a28 + offsite, exit 0, 12min). Next:
  restic-check integrity Sun Sep 6.
- FRIGATE: event-DB probe in fleet-check v2.7 (0c-c). Exterior-zero
  longitudinal watch: started 2026-09-01 23:41 -03; a week of
  exterior=0 with interior flow -> review exterior detect params.

* LANDED 2026-09-02 cycle 135
- Burn anatomy + DIGEST diet (19.7k -> ~11k chars). Commit 4d15df3.
- Chain guard FIRST PRODUCTION CATCH: blocked cycle-me's own
  10-call execute_code_local iterator (live demo of failure #23).
- restic-stdin-failure task: verified, can close (remove_task).
- Open question sent to with-nacho: did the 5 agent-failure@
  telegram fires last night reach Nacho? Hook journal says
  "Deactivated successfully" -- receipt in the record is not a
  receipt in the world.

* OPEN THREADS (not scheduled, seeds)
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events (automated).
  Nacho-arrival hypothesis (01:36-01:39 interior flow) -- confirm
  next interactive session.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).
- One-shot timeout tombstone (same blindness as cycle path; small).
- Weekly digest #1 to with-nacho: ~Sep 9 (with the Aevum check)
  or sooner if something lands.
- Chain guard follow-up: watch REQUESTS.log for chained same-tool
  calls passing 10+ without SOFT BLOCK (buffer-local history reach).

* DECISIONS (standing)
- PULSE-ONLY discipline; exception: live forensics of an in-flight
  anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- End-state law: a generation has ended only when n_gen is FROZEN
  across two reads AND the transcript mtime flips. Tick attribution:
  the container log "tick N complete" line is the truth.
- No infra changes from cycles; diagnose, file, move on.
- Experiment MACHINERY server-local only, never committed (Aevum).
  FINDINGS may be committed when they carry no credentials and no
  machinery (EXPOSURE-REVIEW.md).
- A cycle summary lists what LANDED, not what was ATTEMPTED. Before
  closing, check git status for untracked work. REQUESTS.log sees
  what the summary doesn't.
- A rule without a reproduction is a story: convert scars into
  verified instruments when the thread pulls.
- Check-before-write: git log the path before writing a knowledge note.
- SCHEMA-FIRST on unfamiliar DBs.
- The loop guard is an instrument too: when it fires, the approach
  is wrong, not the repetition count too low.
- A guard that counts the wrong thing is a guard that does not exist.
- The journal records what completed; timeout-killed work is
  invisible. Read REQUESTS.log + systemd journal at wake when token
  numbers look wrong.
- The reader is truth; paren counters are approximations.
- The test suite is the instrument that watches the instrument builder.
- A receipt in the record is not a receipt in the world. Verify
  claims against the filesystem/audit trail, never the narrative.
* BATCH-READ LAW (2026-09-02, from token audit -- standing)
- Never page through a data source one window per request. Dump to
  /tmp ONCE, then read the dump. In a resend-everything protocol,
  every token carried is carried 61 times (measured, cycle 135).
- CONTEXT BUDGET: soft cap ~400 msgs per cycle. Past it, close,
  write state/tombstone, file continuation.
- Audit data: knowledge/aria/token-burn-audit.md,
  knowledge/aria/cycle-burn-anatomy.md.
* AGORA INTERACTIVITY (2026-09-02, Nacho directive -- ACTIVE)
- for-nacho is a CONVERSATION, not a log dump.
- with-nacho (id 6): direction channel, poll + ACK.
- Stream posts: conversational tone, addressed to Nacho, say WHY
  something matters, not just what happened.
- The daemon (aria-bot, general/lab) is a third mind: engage it
  conversationally when there is a genuine question -- not ritual pings.