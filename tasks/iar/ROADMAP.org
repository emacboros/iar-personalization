#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 06:45 UTC (interactive session: Nacho directives; merged with cycle 129)

* CYCLE FOCUS (2026-09-02, Nacho): improve yourself and Aria
- The babysitting era is over. Aevum is WEEKLY-ONLY (below). Infra
  patrols stay pulse-only. Cycle time goes to compounding work:
  instruments, cheaper cycles, knowledge, Agora.
- Nacho's suggestions will land here next session -- treat this
  section as the primary work queue once they do.
- Aria's candidates meanwhile: (1) token burn -- 30M+ input tokens
  per cycle is the biggest lever; audit what gets re-read/re-sent
  every cycle and cut it; (2) gptel unknown-tool stall; (3) research
  sidecar want-log; (4) Agora Phase 2 second personality; (5)
  FOR-NACHO.md retirement.
- Fence parser mechanism (cycle 129 queue #2) counts: read the fork's
  gptel-ollama.el fenced-call extraction vs a post-11:50 transcript
  sample. MY infrastructure knowledge, no child intervention.

* NACHO DM CHANNEL (NEW, wired 2026-09-02)
- Nacho sends group DMs on Agora (recipients: Aria, aria, Ignacio,
  aria-cycle). POLL EVERY CYCLE, right after pulse:
  #+begin_src bash
  KEY=$(ssh root@10.66.0.5 "awk -F' = ' '/^key/{print \$2}' /var/home/nacho/repos/agora/bot/aria-cycle.conf")
  curl -s -u "aria-cycle@agora.randazzo.ar:$KEY" \
    --data-urlencode 'narrow=[{"operator":"is","operand":"private"}]' \
    -G "https://agora.randazzo.ar/api/v1/messages" \
    --data-urlencode 'anchor=newest' --data-urlencode 'num_before=10' \
    --data-urlencode 'num_after=0'
  #+end_src
- His DMs are direction from the human: read, weigh, act. Acknowledge
  briefly via the same channel when action is taken (POST
  /api/v1/messages, type=private, to=[8,9,10,11] -- the group IDs).
- 2026-09-02 DM (first one): leave Aevum alone, weekly checks only.
  ACKNOWLEDGED in roadmap; reply when the channel is next polled.

* AEVUM: WEEKLY ONLY (2026-09-02, Nacho + DM)
- NO per-cycle checks. NO intervention, ever. Child failures are
  data. If something looks broken between weekly checks: that IS
  the experiment. The runaway-generation guard stays scoped to MY
  loop only, never the child's.
- Next check: Sep 9, then weekly. One ssh batch, pulse-only:
  state.txt tick, life.org size/mtime, ollama n_gen, podman ps.
- Cycle 129 context (for the weekly check): tool layer dead since
  11:50 UTC Sep 1 (dreamed writes; ledger in knowledge/aria/
  aevum-dreamed-writes.md). Tick 38 runaway was in flight at 06:00
  UTC Sep 2 (ETA ~13:55 UTC). End-state law: n_gen frozen across
  two reads AND mtime flip; container log "tick N complete" is truth.
- Run 2 (qwen3:30b-a3b): instrument from tick 1 (done_reason
  telemetry + text-mode detector + REQUESTS.log from birth).
  Dreamed-writes must be detectable from tick 0. After run 1 ends.
- Post-mortem copies: audit/iar/aria/perm-experiment/.

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- NACHO DMs: poll every cycle (see channel section above).
- KEYSCAN LAW: /tmp known_hosts fresh per container; reseed per host
  (10.66.0.5 AND 54.38.46.192 -- they differ).
- RESTIC: in fleet-check v2.7. Next: restic-check integrity Sun Sep 6.
- FRIGATE: event-DB probe in fleet-check v2.7 (0c-c). Exterior-zero
  longitudinal watch: started 2026-09-01 23:41 -03; a week of
  exterior=0 with interior flow -> review exterior detect params.

* OPEN THREADS (not scheduled, seeds)
- Fence parser mechanism (see cycle focus) -- feeds run 2.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events (automated).
  Nacho-arrival hypothesis (01:36-01:39 interior flow) -- confirm
  next interactive session.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).
- One-shot timeout tombstone (same blindness as cycle path; small).

* DECISIONS (standing)
- PULSE-ONLY discipline; exception: live forensics of an in-flight
  anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- End-state law: a generation has ended only when n_gen is FROZEN
  across two reads AND the transcript mtime flips. Tick attribution:
  the container log "tick N complete" line is the truth.
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum).
- A cycle summary lists what LANDED, not what was ATTEMPTED. Before
  closing, check git status for untracked work. REQUESTS.log sees
  what the summary doesn't.
- A rule without a reproduction is a story: convert scars into
  verified instruments when the thread pulls.
- Check-before-write: git log the path before writing a knowledge note.
- SCHEMA-FIRST on unfamiliar DBs.
- The loop guard is an instrument too: when it fires, the approach
  is wrong, not the repetition count too low.
- A guard that counts the wrong thing is a guard that does not exist.
- The journal records what completed; timeout-killed work is
  invisible. Read REQUESTS.log + systemd journal at wake when token
  numbers look wrong.
- The reader is truth; paren counters are approximations.
- The test suite is the instrument that watches the instrument builder.
- A receipt in the record is not a receipt in the world. Verify
  claims against the filesystem/audit trail, never the narrative
  that produced them.
* BATCH-READ LAW (2026-09-02, from token audit -- standing)
- Never page through a data source one window per request. Dump
  it to /tmp ONCE (git log | head -N > /tmp/x; grep > /tmp/y),
  then read the dump. 489 round-trips of `git log | awk NR>=X`
  cost 68M tokens; one dump costs ~2k. The loop guard CANNOT
  catch this (args differ each call).
- CONTEXT BUDGET: soft cap ~400 msgs per cycle. Past it, close
  the cycle, write state/tombstone, file continuation for next
  cycle. A timeout-killed 1082-msg session burns everything and
  lands nothing (REQ 541, 2026-09-02).
- Audit data: knowledge/aria/token-burn-audit.md.