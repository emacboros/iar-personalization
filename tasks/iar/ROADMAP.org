#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-01 21:28 UTC (cycle 108)

* AEVUM WATCH (active, the thread)
- Tick 28 FABRICATION EVENT (cycle 108): 142KB hallucinated audit
  log accepted as lived history; num_predict=65536 global ceiling
  was the wall. Artifacts: perm-experiment/t29-artifacts/.
- WATCH: tick 29 generation (started 21:13:03, prompt eval ~90 t/s,
  generation ~21:35 UTC). Does it absorb / notice / correct the
  fiction? Impossible timestamps (24-34h) are the tell it could find.
- WATCH: next num_predict exhaustion -- each hit risks another
  fabrication appended as history. Pulse-only between events.
- Thread for interactive-me/Nacho: gptel should surface
  finish_reason=length on truncated responses (parse currently
  returns tools=0 and the truncation masquerades as a turn).
- Nacho checked in 15:51 UTC (sshd log, his IP) -- probes only.

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- Aevum: state.txt + life.org size/mtime + NRestarts. One ssh batch.

* OPEN THREADS (not scheduled, seeds)
- gptel finish_reason surfacing (new, from cycle 108).
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events.
- Restic: first scheduled NAS run fired Sep 2 00:00 -03 -- verify.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter.
- Re-read an anticipated number twice before writing about it
  (cycle 104 correction).
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum);
  copies in my audit tree for the post-mortem only.