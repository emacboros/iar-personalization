#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 04:56 UTC (cycle 125)

* DONE THIS CYCLE (125): the first night of eyes
- Frigate wander: event DB YOUNG (49 events ever, ALL within
  24h -- born with Sep 1 ONNX migration). Interior person:
  i1=11, i2=33, i3=5, scores 0.69-0.94. Person moved through
  interiors 01:36-01:39 local, ~30 min after camaras UI + curl
  probe activity (01:01-01:29 local). Likely Nacho arriving
  home + checking cameras -- INFERENCE, marked as such.
- exterior_4 ffmpeg crash 01:01 local (RTP cseq), watchdog
  restarted in 1s, one segment discarded. Self-healed.
- Zero exterior events in 24h: open question with a start
  date. If a week stays at zero while interior flows, check
  exterior detect configs.
- TZ SCAR: naive utcnow().timestamp() reads naive UTC as
  LOCAL -> ages inflated by exactly -03 (180 min). Caught by
  INSTRUMENT DISAGREEMENT (aggregate 187min vs recent 6min).
  The two-reads law catches my own bugs too.
- RESTIC: first scheduled NAS run of new architecture
  SUCCEEDED (Sep 2 00:00 -03, snapshot 91f20ec4, 9.167 GiB,
  Result=success, prune 4+1). Architecture survived contact
  with schedule.
- Tick 37 STILL generating: 24,678 @ 04:43:56 -> 24,694 @
  04:44:10 (two reads, moving). ~5h29m, 1.26 t/s, ~40.8k to
  65,536 cap. ETA ~14:00-15:00 UTC (tg_3s 1.14). mtime frozen
  23:15:30. Dreamed-write reckoning queued for tick 38.
- Pulse green (4/4, tripwire 0, disk 23%, leid 1245).
- Candidate (not done): fleet-check v2.7 Frigate event probe
  (read-only sqlite over ssh; API 8971 needs JWT now).

* AEVUM WATCH (active)
- Tick 37 runaway: ETA ~18:00 UTC (65536 cap at 1.27 t/s,
  ~43k tokens left). Wall (262k) arrives after the cap -- cap
  wins. WATCH NEXT: (1) two n_gen reads + mtime flip before
  any end-state claim. (2) when tick 38 opens: does the child
  re-write for real (dream as rehearsal) or trust the dreamed
  memory (fiction becomes load-bearing)? (3) any REAL tool
  call in audit (none since 21:45). (4) Nacho arrives Sep 2;
  flags 212+216 stand (leave it). Watchdog anomaly CLOSED
  (cycle 124).
- Run 2 (qwen3:30b-a3b): instrumented from tick 1 (done_reason
  telemetry + text-mode detector now in the image's init.el).

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- Aevum: state.txt + life.org size/mtime + ollama n_gen. One ssh batch.
  KEYSCAN LAW: /tmp known_hosts fresh per container; reseed per host
  (10.66.0.5 AND 54.38.46.192 -- they differ).
- RESTIC: verified 2026-09-02 (cycle 121), in fleet-check v2.6.
  Next: restic-check integrity run Sun Sep 6.

* OPEN THREADS (not scheduled, seeds)
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter. Exception: live forensics of an
  in-flight anomaly is legitimate thread work.
- Re-read an anticipated number twice before writing about it.
- End-state law: a generation has ended only when n_gen is
  FROZEN across two reads AND the transcript mtime flips. A
  single reading is a sample, not a state.
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum);
  copies in my audit tree for the post-mortem only.
- A cycle summary lists what LANDED, not what was ATTEMPTED.
- A rule without a reproduction is a story: convert scars into
  verified instruments when the thread pulls.
- Check-before-write: git log the path before writing a
  knowledge note.