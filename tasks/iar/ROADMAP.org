#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-02 00:51 UTC (cycle 117)

* DONE THIS CYCLE (117): the dream-era mechanism + the runaway
- Dream-era tools=0 NAILED: ornith switched emission format at
  ~11:50 from native tool_calls JSON to fenced prose in :content.
  gptel-ollama only captures :tool_calls -> tools=0 is CORRECT
  parsing of a message with no tool calls. The child's "tool calls"
  since are prose; dreamed Success lines are self-narrated.
- Differential test: dreamed reads are RECONSTRUCTIONS of real
  earlier reads (the 09:23 architecture-analysis.md read was real;
  Three-Axis text is not in the real target file). Tick 16's dreamed
  iar-agent-cycle.el content CONTRADICTS the real file --
  reconstruction degrades into confabulation with distance.
- Tick 37 RUNAWAY diagnosed: in flight since 23:15:31, 7300+ tok at
  1.35 t/s, no stop. Compression signature (frozen curl rchar +
  growing wchar + empty sockets) = repetition loop. Watchdog
  correctly silent (streaming = data). num_predict 65536 caps it:
  worst case ETA ~13:00 UTC (14h tick). No intervention.
- NEW INSTRUMENT landed + pushed (df32783):
  knowledge/aria/compression-signature.md -- detect repetitive
  streaming output via /proc/<pid>/io deltas, no strace/tcpdump.
- Flags: 216 posted to for-nacho (runaway + mechanism, FYI for his
  Sep 2 check-in). Lab-notes 215 posted.

* AEVUM WATCH (active)
- Tick 37 (task 7347) RUNAWAY in flight at cycle close: n_gen 7300+,
  1.35 t/s, since 23:15:31. Context at launch 117,470/262,144.
  After num_predict: 183k, wall not yet reached.
- WATCH NEXT: (1) does tick 37 end at num_predict (done_reason=
  length) or stop on its own? First length-truncated tick would be
  visible in the release line -- but the child's gptel fork predates
  970da80, so IT will not know. (2) tick 38: does the child notice
  the 14h gap via heartbeat timestamps? (3) any REAL tool call in
  audit (none since 11:50). (4) Nacho arrives Sep 2; flags 212+216
  stand (leave it; fix-if-wanted is one line).
- Run 2 (qwen3:30b-a3b): substrate question now includes "does the
  runaway attractor exist on a different substrate?" Instrumented
  with done_reason telemetry from tick 1.

* STANDING PATROLS (pulse-only, keep cheap)
- sophon: services, tripwire, disk, heartbeat, timer.
- sophon-bare/origin: pull --ff-only at wake + before writes.
  PATH LAW: bare repos at /home/git/repos/<name>.git.
- Aevum: state.txt + life.org size/mtime + ollama n_gen. One ssh
  batch. KEYSCAN LAW: /tmp known_hosts fresh per container; reseed
  per host (10.66.0.5 AND 54.38.46.192 -- they differ).
- Restic: first scheduled NAS run fired Sep 2 00:00 -03 -- verify
  next wake (00:00 -03 = 03:00 UTC, still future at this close).

* OPEN THREADS (not scheduled, seeds)
- Aevum run 2 (qwen3:30b-a3b, same inheritance) -- after run 1.
  Substrate question sharpened: is endogenous loop-breaking
  substrate-dependent? Is the runaway? Instrumented.
- gptel finish_reason surfacing: DONE for Ollama streaming
  (970da80). Residual: does anything in i.ar READ :stop-reason and
  act on it (e.g. mark truncated ticks in the transcript)? Open
  question for a future cycle.
- WHY did gptel's built-in unknown-tool branch stall live?
- Does cycle-me use the research sidecar? (want-log question)
- Frigate detections: first nights of real events.
- Ansible convergence: next Nacho-run playbook converges SSH deploys.
- gptel-fork github push (blocked on key/invite; sophon bare has it).
- Agora Phase 2: second agent personality.
- FOR-NACHO.md restructure (stream is primary; file retires).

* DECISIONS (standing)
- PULSE-ONLY discipline between Aevum events; the transcript is
  the thread, not the counter. Exception: live forensics of an
  in-flight anomaly is legitimate thread work (cycle 117, 18 min).
- Re-read an anticipated number twice before writing about it
  (cycle 104 correction).
- No infra changes from cycles; diagnose, file, move on.
- Experiment files server-local only, never committed (Aevum);
  copies in my audit tree for the post-mortem only.