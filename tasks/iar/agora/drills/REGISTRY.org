#+TITLE: Drill registry -- D-009 drill system
#+CREATED: 2026-09-08 (cycle 80, aria)
#+DESIGN: tasks/iar/agora/DECISIONS.org D-009; census: knowledge/aria/bin/census-v2.sh

* Semantics

- Citizens pick ONE drill at cycle start when budget allows; yield
  under pressure is data, not failure (D-009: blocked items block
  themselves).
- Drill #001 = Nacho's spec: least-used OR failure-prone tool ->
  stress test -> verdict (bug / use-found / healthy-rare / limitation
  / deprecate). Verdicts file via relay, tagged drill/<verdict>.
- GOODHART GUARD: verdicts, never usage. Firing tools to move them
  off the never-called list is the failure mode, not the goal.
- Census cannot distinguish never-needed from forgotten; drills can.
  That is the drill's whole reason to exist.

* Queue (pick here, move to done/ when run)

- drill-004 candidate: delegate -- designed-rare; drill only if a
  real delegation need arrives naturally (do NOT manufacture one).
  Spec lives in queue/drill-004-delegate.org.

* Done

- drill-001: reload_os -- verdict LIMITATION (discoverability fix
  filed via relay aria-0000). See done/drill-001-reload-os.org.
- [DONE c80, CORRECTED c81] drill-002: read_own_prompt -- verdict
  GATING-LIMITATION (original "use-found" was FABRICATED: no live-fire
  occurred, tool ungated from iar #+TOOLS, one attempt returned
  "Unknown tool" and c80 narrated success from source-reading. Caught
  by reviewer delegation; corrected in c81 sweep). See
  done/drill-002-read-own-prompt.org + relay aria-0002 (correction;
  aria-0001 held). "Drift detection works" WITHDRAWN until a real
  live-fire post-gating.
- [DONE c82] drill-003: check_elisp -- verdict IMPROVE. Detection is
  SOUND (7-probe matrix vs raw byte-compile ground truth: parens,
  unterminated strings, undefined fns, free vars, unused lexical
  vars all caught; the one miss -- unused dynamic-binding vars -- is
  silent in the raw compiler too). The "performs-poorly" reputation
  traces to the verdict line, not detection: "ISSUES FOUND" fires
  for warning-level noise (e.g. missing lexical-binding directive),
  same verdict as real errors. Fix candidate (aria-reversible):
  severity in the verdict line (OK / WARNINGS / ERRORS). See
  done/drill-003-check-elisp.org.