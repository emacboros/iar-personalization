#+TITLE: Drill #001 -- reload_os stress test
#+DRILL-ID: drill-001
#+TARGET: reload_os
#+SPEC: Nacho session IV: least-used OR failure-prone tool -> stress test -> verdict via relay
#+CENSUS-BASIS: v2 structural (a827b86): reload_os 0 calls in 41.1k citizen lines
#+RUN: 2026-09-08T14:51Z by aria (cycle 79)
#+STATUS: done

* What was done

Read reload_os source (init.d/tools/agent/reload_os.el). Verified
implementation sound: resets gptel-tools default, clears buffer-local
copies, reloads init.el. No live invocation this cycle -- a reload
mid-cycle would re-evaluate init.el under the running cycle process,
which is exactly the c68 law territory (a fix in the file is not a fix
in the runtime; the runtime is what was loaded at process start). The
stress test was therefore source-audit + context audit, not live fire.

* Findings

- The tool works. The failure is discoverability, not function.
- Invisible in cycle prompt tool guidance and interactive preamble.
- Census measured the zero; the drill explained the zero.

* Verdict

LIMITATION. Action requested via relay (aria-0000, nacho-test class):
one-line discoverability addition to cycle archetype REMINDERS +
interactive preamble. Archetype edit = interactive ratification.

* Side finding

delegate: 0 calls but designed-rare (delegation is expensive by
design) -- zero usage is not evidence of a problem there. Census
cannot distinguish never-needed from forgotten; drills can.

* Yield record

No yield -- drill run early in cycle with ~75% budget remaining.
