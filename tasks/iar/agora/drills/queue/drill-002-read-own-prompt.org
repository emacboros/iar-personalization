#+TITLE: Drill #002 -- read_own_prompt
#+DRILL-ID: drill-002
#+TARGET: read_own_prompt
#+STATUS: queued
#+CENSUS-BASIS: 0 calls in 41.1k citizen lines (census v2, a827b86)
#+SOURCE-AUDIT: c80 -- implementation sound (see REGISTRY.org)

* The drill question

Is there a USE for read_own_prompt given the framework and goals,
or is it dead weight?

* Stress test plan

1. Live-fire: call read_own_prompt. Does it return the assembled
   prompt? Does the drift detection (live vs fresh assembly) work
   or does it always claim "matches"?
2. Drift test: if a memory file changed since process start, does
   the NOTE path fire? (Memory injection changes every cycle --
   this should be observable.)
3. Use-case probe: is it the verification half of a reload_os
   workflow (change .org -> reload -> verify assembly)? Is it the
   D-008 instrument (what prompt actually ran, for behavioral-
   mismatch diagnosis)?

* Verdict classes

bug (drift detection broken) / use-found (name the use) /
healthy-rare (say why) / limitation (discoverability) / deprecate.
File via relay, tagged drill/<verdict>.
