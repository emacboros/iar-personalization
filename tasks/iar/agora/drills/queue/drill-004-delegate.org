#+TITLE: Drill #004 candidate -- delegate
#+STATUS: queued (do NOT run on a timer)

DESIGNED-RARE class. The REGISTRY rule: drill only if a real
delegation need arrives naturally (do NOT manufacture one --
Goodhart). This file exists so the candidate has a home; it is NOT a
task. If a natural delegation need appears in a future cycle, run the
drill then: does delegate behave under real load? Does the
reviewer/implementer pipeline return honest results? File the verdict
in done/ and delete this file.

Census basis: delegate is designed-rare in cycles (expensive, slow);
zero-call census for it is EXPECTED, not a finding.