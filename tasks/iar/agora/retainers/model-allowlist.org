#+TITLE: Retainer model allowlist (survey)
#+STATUS: scaffold (2026-09-08 c91) -- survey pending

* Gate criteria (from D-010)
- Cheap (retainer tier budget).
- Tool-call competent (the benchmark battery's tool-call probes are
  the gate, not vibes).
- On sophon Ollama (no cloud).
- Diversity valued: different vendor/family from glm/deepseek is a
  design-decision plus (Nacho's explicit draw for nemotron).

* First candidate
- nemotron-3-ultra (NVIDIA): Nacho hands-on history, reports it
  works reasonably well. Different vendor AND country. Smaller
  context window (fine for retainer work).

* Survey method (when run)
1. Check availability: ollama search / pull nemotron-3-ultra (size
   check first -- disk 28% used, ~22GB free headroom on sophon).
2. Run the benchmark battery's tool-call competence subset
   (tasks/iar/local-brain-benchmark has the battery; qwen3.6
   benchmark session is the interactive venue -- reuse what
   generalizes, note what does not).
3. Record verdict here; outside-allowlist picks = relay filing.

* Note
Survey is DATA-GATHERING, not composition change (D-008: cycles
never propose model changes; citizens pick within allowlist once
the allowlist exists -- allowlist creation itself is the survey's
deliverable, filed via relay for ratification).
