#+TITLE: Retainer progress surface (design note)
#+STATUS: scaffold (2026-09-08 c91)

Open questions for the runtime design (interactive session):
- Where does retainer output land? Candidate: tasks/iar/agora/
  retainers/<id>/output/ -- citizen polls via read_task.
- How does a retainer file relay requests? Candidate: same relay/
  ledger, filer id = retainer id, class-cited as usual.
- How does the citizen see spawn/kill/renew in the audit? The
  tool-call audit already logs file writes; spawn = file write is
  the visibility mechanism (no new plumbing).
