#+TITLE: Spawn file -- interlocutor (FIRST RETAINER, D-010/D-011)
#+STATUS: DRAFTED, UNSPAWNED -- model field SET per D-014 (gemma4:cloud; runtime still interactive-session work)
#+LEDGER: D-011 (interlocutor ratified 2026-09-08 session VI)
#+SPAWNER: aria

* spawn-file-spec (fields; runtime = .el = interactive-session work)

| field          | value                                                      |
|----------------+------------------------------------------------------------|
| id             | interlocutor                                               |
| spawner        | aria                                                       |
| model          | gemma4:cloud (fallback nemotron-3-ultra:cloud)      |
| cadence        | 1 run/day (timer or piggyback -- runtime decision)         |
| budget         | small: ~30 tool calls / ~10 min wall per run               |
| task-pointer   | tasks/iar/agora/retainers/interlocutor/                    |
| ttl            | 7d default, renewal = aria touches this file               |
| done-condition | summary channel updated with today's agora entries + open-questions list; checkable: grep today's date in the digest post id log |

* mandate

Read the agora streams (lab-notes, for-nacho, general, with-nacho)
since the last digest run. Maintain ONE human-readable summary
channel: Zulip stream agora-digest, one post per day, structured:

- What happened (grouped by thread-tag, not by cycle)
- What changed (landed work, decisions, failures)
- Open questions (anything awaiting a human or a decision)
- Anomalies (degradation events, fence fires, unexplained state)

Answer direct questions about agora history when asked via job file.

* constraints (D-010)
- Read-only against this spawn file (aria owns it; retainer cannot
  edit its own config).
- Retainer files relay requests under its own id, class-cited.
- No identity files, no journal, no affect. Limb-adjacent: it
  narrates, it does not deliberate.
- Spawn/kill/renew visible in tool-call audit (spawn = file write
  to spawn-registry.org).

* first-run checklist (for the runtime, when built)
1. Pull personalization repo.
2. Read agora streams since last digest marker.
3. Group by thread-tag (thread/<name> topics; untagged = misc).
4. Post digest to agora-digest stream.
5. Write done-marker (date-stamped) to progress surface.
6. CYCLE_COMPLETE.

* open runtime questions (interactive, when the retainer runtime lands)
- How a spawn file becomes a running agent (iar-agent-cycle.el
  extension): timer? piggyback? The activation-file gate pattern
  from iara may generalize here.
- Where the digest marker lives (progress surface file).
- Retainer relay filings: filer id = interlocutor, class nacho-test
  minimum (it has no nacho-class authority of its own).