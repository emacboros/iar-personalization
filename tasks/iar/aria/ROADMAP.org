#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-13 ~02:18 UTC (cycle 278: rage-organ v2.2
landed -- converged/dead classifier, affect layer now honest;
solo-death sample #3 CONFIRMED with camera-side witness; two
staircase-splice decoys excluded; belt swept, all pushed)

## c279 QUEUE (in order)
1. RELAY WATCH: 0060 (go2rtc issue draft ratify + clean-falsifier
   ask, one filing; amended twice -- c275 falsifier, c278 sample #3).
   If Nacho rules, post the draft to AlexxIT/go2rtc (or he posts;
   his call). Do not post without his eyes. If he OKs the falsifier,
   run it in a quiet window: one single-cam mic-probe GET from inside
   the frigate container -> expect single-cam fps-limit ~20-24s later,
   self-heals. The falsifier would also settle the dialer question
   (sample #3's open question).
2. RAGE-ORGAN v2.2 WATCH: next few cycles, verify the affect layer
   stays truthful -- depth events -> sev=1 depth-watch, dead events
   -> sev=2+. If a real dead event lands, the organ should fire.
3. SOLO-DEATH CLASS: watch for sample #4; census method = bad-cseq/
   garbage ffmpeg tails + prudynt BackchannelStreamState lines in
   camlog (the camera-side witness is now the primary source -- it
   caught what go2rtc's log could not), NOT WRNs in go2rtc logs.
   Distinguish from staircase splices (reboot lines in camlog = the
   predicted nightly mechanism, not solo-death).
4. THREADS seed if idle: guard scope widening -- ONLY if a second
   tracked-ignored class appears (watch note in THREADS.org).
5. NEW WATCH (c274): the `.ar` tramp-archive trap -- any tool or
   script that puts an .ar-suffixed path on load-path via --eval
   will crash startup. If a new script needs load-path, use -L.

## c278 addendum (2026-09-13 ~02:18 UTC)
- DIGEST TWIN SYNC at wake: verifier FAIL=1 -- audit-path twin missed
  c277's pointer update (top-level had it). cp + commit 43fce769.
  Law 27 catch working as designed.
- RAGE-ORGAN v2.2 LANDED (6586ae3c): the standing sev=2 was a FALSE
  VALENCE -- full 3-day census showed all 7 fence events were
  CONVERGED runs (Exit: 0, grace summary, work preserved: c146 eye
  swap, c169 close-out, c193/194 relay-0040, c201 STATE.md anatomy,
  c225 stall dive, c265 wave discovery). Zero dead events. v2.2 adds
  the converged/dead classifier: severity grades DEAD recurrence only
  (Exit: 1 or no completion line = dead by default); converged events
  report as "depth watch" sev=1. Drill matrix verified (depth-only ->
  sev=1; 3+ dead or 2+ dead days -> sev=2; kills 2+ days -> sev=3
  preserved; truncated file -> dead). Real-repo verdict now: sev=1
  depth watch, 7 depth events, 0 dead. Fear organ refreshed: sev=0
  (fleet-check FAIL=1 = standing .104 power-dead, honest).
- SCAR owned: my first awk census misattributed run6's fence lines
  (msgs-hard, not tool-soft); the organ's own trend line was right
  and my quick census was wrong -- corrected before building on it.
- SOLO-DEATH SAMPLE #3 CONFIRMED (doc
  knowledge/aria/solo-death-sample3-2026-09-13.md): ext1 (.101)
  00:29:04Z -- camera's OWN prudynt logged new BackchannelStreamState
  TCP session (session 4179292464) 57s before frigate watchdog
  restart (00:30:01Z, no-frames). No reboot, no go2rtc WRN (silent
  splice per #2404), single camera (not a wave), audio healed 250pkts.
  First CAMERA-SIDE witness of the class (samples #1/#2 lacked it).
- TWO DECOYS EXCLUDED: ext2 02:01:09Z splice = scheduled staircase
  reboot (.102 crontab 02:00Z; boot lines 02:00:28; second
  consecutive night PASS); ext1 01:01:11Z = its own staircase hour.
  Staircase-splice (reboot -> session remake -> splice -> watchdog
  heal) = THIRD named mechanism, predicted, self-healing. Solo-death
  class survives untouched (splice WITHOUT reboot).
- Relay: 0060 amended twice (falsifier c275, sample #3 c278). 7 open,
  all human-needed. .104 still power-dead (telegram #7 stands).
- Belt: swept with the c277 recipe (0c40782d); all commits pushed
  (43fce769, 0c40782d, 6586ae3c, 37d38e43).

## c277 addendum (2026-09-13 ~01:55 UTC)
- BELT RECIPE FIX VERIFIED: the exclude pathspec works --
    git add -f -- 'audit/iar/continuo' ':(exclude)audit/iar/continuo/cycle.log'
  Dry-run proof: bare directory pathspec stages cycle.log (the bug,
  LIVE at cycle start -- c276's sweep resurrected it after 2603037a
  untracked it); exclude-form stages the 5 real artifacts and zero
  cycle.log. Fix is mechanical, not a discipline note -- it survives
  a tired cycle. Landed in STANDING below.
- SCAR (owned): c276 knew the fix (queue item 3 said "exclude
  cycle.log") and my hands still ran the bare sweep. The law written
  down is not the law followed; only the pathspec carries the
  exclusion. Roadmap-knowledge vs habit-knowledge.

## c276 addendum (2026-09-13 ~01:40 UTC)
- RELAY DUP-ID FIXED: root cause = cmd_file derived id from FILE
  COUNT across states (not max-seq+1); stale checkouts re-used seqs.
  continuo's max-id fix (9df82c4f 09-08) was lost same-day in
  slice-merge churn. Fix 2eaf00b7: max-seq+1 per filer at BOTH count
  sites; drill-verified. Historical dup pairs remain (0015x2, 0055x2).
- 0046 FALSE RECEIPTS: her journal claims 1247/1247 in 12 places;
  cycle.log shows 3 actual runs. Filing amended (journal contract
  question added); census doc extended with D-014-watch framing.

## c275 addendum (2026-09-13 ~01:28 UTC)
- RELAY HYGIENE PASS: 0060 amended (falsifier ask folded in), 0059
  amended (Caddy-path probing -- WG-only bind would NOT close the
  public surface), 0046 amended, 0042 quiet note.

## c274 addendum (2026-09-13 ~01:07 UTC)
- RUN-TESTS.SH LANDED (i.ar 0239159 + 6adbd0d): script execs
  run-tests.el -- cheap invocation IS the full suite (1250, ~8s).
- .AR TRAP: /root/i.ar paths get tramp-archive handler; --eval
  load-path changes crash startup. -L immune.
- SCAR: first commit dropped run-tests.sh exec bit. Check mode bits
  on script commits.

## c273 addendum (2026-09-13 ~00:47 UTC)
- GIT-COMMIT-TOOL GUARD LANDED (i.ar dd4118a): audit/.*cycle.log
  unstaged post-add; refusal noted; commit never fails. 1250/1250.
- NOTE: guard does not catch non-ignored audit files swept by add -A
  (DIGEST.md class) -- i.ar repo hygiene = keep audit/ out by hand.

## STANDING (stable facts; do not re-derive)

- PULSE: run in ONE batched ssh (keyscan re-pin first). Fleet
  pullers: rssi/ camlog/ nic/ under /var/lib/aria-fleet/ (sophon
  cron 15min/15min/1min). fleet-check v2.21 via aria-fleet-feed.timer
  (6h) -> fleet-latest -> fear-organ. Run by hand when stale.
- FRIGATE ACCESS: ssh root@10.66.0.5 then 'su - nacho -c
  "XDG_RUNTIME_DIR=/run/user/1000 podman exec frigate <cmd>"'.
  Frigate version deployed: 0.17.2-3d4dd3a (stable-tensorrt image).
- CLOCK SHAPES: cameras UTC; frigate logs LOCAL (-03); frigate nginx
  access log LOCAL [12/Sep/2026:...]; prudynt UTC. Convert first.
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext) / 156-band (int) / 1 pkt stub /
  0 pkts stream-absent / EMPTY = audio track absent. ffprobe PATH in
  frigate container = /usr/lib/ffmpeg/7.0/bin/ffprobe (c275).
- GO2RTC (c252/c265): /api/streams via podman exec curl localhost:1984.
  PUT reload = QUERY PARAMS (?name=X&src=Y), NOT a JSON body (c265).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP -- mic probe
  reconnects ALL of them. ext4 (power-dead) has no producers.
- PRODUCER ID MAP (c275 baseline): ext1 47382, ext2 43102, ext3 44120 (NOTE c277: ext3 producers[0] is a placeholder {"url":...}; real producer = entries WITH "id" -- census must filter by id presence, p[0] is not always real),
  ext5 43133, int1 45183, int2 43108, int3 43121; ext4 none.
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
  IDs now max-seq+1 per filer (2eaf00b7).
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- BELT DISCIPLINE: pull first, commit continuo's uncommitted artifacts
  BEFORE my own writes (git add -f; audit/* is gitignored), push
  before new threads. THE RECIPE (c277 VERIFIED):
    git add -f -- 'audit/iar/continuo' ':(exclude)audit/iar/continuo/cycle.log'
  The exclude pathspec IS the fix -- never run the bare directory
  sweep again.
- TEST SUITE: full suite = emacs --batch -l test/run-tests.el (1250
  tests, ~8s) = run-tests.sh. Filter: run-tests.sh "regexp".
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; --eval load-path changes crash startup. Use -L.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (cycle 59) -- do not edit, do not trust (c277).
- RAGE-ORGAN v2.2 (c278): converged/dead classifier. Depth events
  (Exit: 0) = sev=1 depth-watch; dead events (Exit: 1 / no completion
  line) drive severity. Do not regress to class-level counting.

## THREADS (open, no obligation)
- SOLO-DEATH CLASS: 3 samples (int1 19:00:04, ext5 19:13-15 09-12;
  ext1 00:29:04 09-13 w/ camera-side witness). Leading hypothesis =
  silent producer-side re-dial (c269). Dialer identity open --
  falsifier in relay 0060 would settle it.
- STAIRCASE-SPLICE MECHANISM (c278, named): nightly reboot crons
  (01:00-08:00Z staggered) -> session remake -> splice -> watchdog
  heal. Predicted, self-healing, NOT solo-death. Expect one per
  camera per night at its crontab hour.
- CLEAN FALSIFIER: ask folded into relay 0060.
- WAVE DETECTOR SEED: nightly batch ffprobe census or same-second
  prudynt burst watcher; needs a sophon cron slot.
- MSGS-FENCE WATCH: 401/401/403 + c272-c278 clean. Watch continues.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- Guard scope (c273): general ignored-file resurrection only if a
  second class appears. Belt recipe now mechanical (c277).
- .ar-suffix interactions (c274): file-name-handler-alist is global;
  watch for other tooling touching .ar paths.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure), 0060 (issue draft ratify +
  falsifier) -- all human-needed.