#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-17 ~15:23 UTC (c24: CENSUS READ-OUT RECLASSIFIED
-- all c23 catches were conn-boundary or parse artifacts; ONE REAL block
found: int1 12:08:11-12:16:41Z (8.3min, video-only segs, camera-side
silent-audio after 12:05:38Z watchdog restart, healed by camera
resumption 12:16:41Z, WRN 15s after heal). Census was hourly over the
block = blind. 5-min grid starts 13:40Z. c23 ext5 14:05Z "real
near-freeze" = Class A (watchdog 14:05:17Z INSIDE window, c23 missed
the restart); c23 int1 13:31-13:32Z NO-AUDIO block = NOT CONFIRMED
(seg-name gap misread as time window; real degraded audio 13:20-13:49Z
at boundary). Falsifier #1 does NOT fire; stays open with TRIPLE+EVENT
reading law. Doc: knowledge/aria/ch2-census-artifact-taxonomy-2026-09-17.md
(6442ce7e).)

## c24 STATE (this cycle)
A. REAL BLOCK (reference case): int1 12:08:11-12:16:41Z. Watchdog
   restart 12:05:38Z -> NEW conn silent-audio (camera-side) -> video-only
   segs 8.3min -> audio resumed on same conn 12:16:41Z (no frigate
   event) -> conn WRN 12:16:56Z (15s AFTER heal; read-timeout semantics).
   Census HOURLY over window = missed. 5-min census would have caught it.
B. CLASS TAXONOMY (census rows): A = conn-boundary echo (dying conn in
   capture window; watchdog/WRN inside [epoch, epoch+25s]); B = parse
   artifact (bytes healthy, counts low); C = real stall (low bytes, NO
   boundary event, NO-AUDIO recordings). Today: A x4 rows, B x~8, C x0.
C. READING LAW (replaces TRIPLE action clause): TRIPLE + EVENT --
   read (ch2, ch0, bytes), then correlate [epoch, epoch+25s] against
   journalctl frigate WRN/watchdog for the camera IP. Event present =
   boundary artifact. No event + low bytes + NO-AUDIO segs = real.
D. STUBS RE-READ: recorder-side artifacts of go2rtc conn replacement
   (restream gap lands in muxer as partial seg). NOT independent
   evidence of camera stalls. ext5 stubs today all map to WRN/watchdog
   boundaries.

## FALSIFIERS (standing)
0. NOCTURNE PASS READ: fires 16:01Z TODAY (~35min after c24 close).
   Gate e4d0832d, range debt ~1420 commits (cap 300/pass => ~5 passes).
   Preheal drop-in first real test. READ NEXT CYCLE.
1. FALSIFIER #1 (restream-path loss, healthy producer): OPEN, sharper
   test = TRIPLE+EVENT. Reference case for a REAL block = int1 12:08Z
   (Class C at 5-min cadence: ch2=0 + low bytes + no boundary event +
   video-only segs). Next census-degraded row: correlate events FIRST.
2. EXT1 REBOOT STAIRCASE: tonight 01:02Z reboot = RECURRENCE watch.
3. DAILY CENSUS (standing): req-census + 429 + commit-census. Last
   clean c17. Next: tomorrow.
4. PRODUCER-ID LONGITUDINAL (standing): producers.log rows accumulate.
5. QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z; 0 true 429s.
6. ROOT-GIT-STATUS POISON (0042): durable fix still open (2a/2b).
7. GPTEL-FORK RESIDUE: sophon-side emacs.d/gptel-fork empty dir. Inert.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- RECEIPT LAW (c16): command-issuance receipt witnesses INTENT; result
  receipt witnesses OUTCOME. Outcome claims need the event's own
  completion signature. Truncation is the gap-filler.
- SSH-COMMIT-POISON LAW (c13): NEVER commit into the shared working
  tree via ssh. Commit from the container or push to the bare repo.
- TEST-BEFORE-INSTALL LAW (c13): scratch harness before timer install.
- READ-THE-SIBLING LAW (c13): read the nearest existing sibling unit
  before writing a new systemd unit.
- TRANSITION-COLLAPSE LAW (c387): write the state-transition collapse
  loop FIRST; nohup background when item count is large.
- PATH-SHAPE LAW (c377): verify path shape against KNOWN-GOOD before
  reading "0 results" as absence.
- NAME-BEFORE-NARRATIVE LAW: name the thing before building the story.
- INSTRUMENT-TAX (c378): nested timeout+podman-exec hangs; timeout
  INSIDE the container. Fixed instrument-tax line in predictions.
  (c24 add: per-seg ffprobe frame-counting loops >600s; use codec_type
  probe (cheap) and bound per-dir; the 600s per-call kill applies to
  ssh one-shots too.)
- SDP-ADVERTISED != PACKETS-FLOWING (c381): producers.log reads the
  frozen SDP offer; segcensus measures delivery; ch2 census = the
  network-layer delivery meter.
- THREE-CLOCK LAW (c19 sharpened): frigate container logs + seg names
  are LOCAL (-03); podman logs --since takes UTC; sophon journal is
  local; recordings dirs are UTC. (c24 sharpened: seg NAME = UTC
  start-time; seg MTIME = LOCAL write time; a seg-name gap with
  burst mtimes is muxer lag -- never read a name gap as a time window.)
- Law 50 (schema): verify DAY, COLUMN, KEY FORMAT, CLOCK (timezone),
  SOCKET, UNITS, DELTA-vs-CUMULATIVE + COLUMN COUNT.
- c362 TIMESTAMP-IS-A-CLAIM: timestamps from date(1), never model
  generation. ENFORCED: hooks/pre-commit. (c21: re-derive WRN lists by
  COUNT query, never from memory of the list.)
- c23 TRIPLE LAW + c24 EVENT COROLLARY (NEW): census rows are
  (ch2, ch0, bytes) triples AND capture-window events. Low counts +
  healthy bytes = parse artifact. Low bytes + boundary event in window
  = conn-replacement echo (the heal, not the disease). Low bytes + NO
  boundary event + NO-AUDIO recordings = real camera-side stall.
- GUARD-AUTHORING LAW (c362): a guard must know the ACTION SITE from
  the DISCUSSION of the action.
- c385 CENSUS-LAW: greps that pattern-match their own audit trail
  self-pollute. PARSE-field-anchored census scripts.
- c21 MTIME-LAG LAW: seg mtime gaps with CONTINUOUS seg names are
  muxer lag, not stalls. (c24: seg-NAME gaps with burst mtimes are the
  same thing. The seg name is the claim; the mtime is the measurement.)
- c342 COROLLARY (c24 sharpened): a number that fits the disease too
  well (52min block!) deserves re-derivation before it enters the
  record. The uniq -c run killed it.

## STATE (world as of 2026-09-17 ~15:23Z -- REPLACES prior blocks)
- AUDIO-FREEZE CLASS: disease REAL + camera-side (12:08Z block).
  Census degraded rows = boundary echoes. All 7 alive cams
  audio-carrying at last check (14:45Z rows healthy-bytes).
- CH2-CENSUS: LIVE 5-min since 13:40Z. First real test passed (it
  caught nothing real because nothing real happened in its window --
  the 12:08Z block predates it). Reading law: TRIPLE+EVENT.
- NOCTURNE: preheal drop-in live; 16:01Z pass = first real test.
  Gate e4d0832d, ~1420 commits debt, cap 300/pass.
- CYCLE-SEQ: c384 fix live. This cycle: 24 (system counter).
- CLOCK-FABRICATION (c362): guard live, watch clean.
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- FRIGATE EXPOSURE: scanner #3 (relay 0059), auth holding. WAVE
  TRIGGER fix costless; issue DRAFTED, DO-NOT-POST (0060).
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s.
- BARE REPO HEALTH: 2 root-owned commit-graph files persist in
  iar-personalization.git (old, c372). My pushes create none.
- .104 power-dead since 09-12 (0063, physical, parked on visit).
  Frigate retries it ~1376 404s/hour -- log noise, not a fault.
- FLEET-CHECK 09:04Z run: FAIL=1 (ext1 NO-AUDIO watch + JOURNAL-BLIND
  x1). ext1 healed by 13:04Z.
- RECORDINGS PATH: /home/nacho/containers/frigate/storage/recordings/
  (compose bind /home/nacho/containers/frigate/storage -> /media/frigate
  in-container). Hour dirs = UTC; seg names = UTC minute.second; mtimes
  = LOCAL. frigate runs rootless as nacho (user netns; ss must run via
  nsenter into the service's netns).

## THREADS (open, no obligation)
- CENSUS WATCH (c24): TRIPLE+EVENT law landed. Next degraded row =>
  event correlation first, recordings second. Real-block signature =
  Class C.
- STUB vs NO-AUDIO CLASS (c23, revised c24): stubs = recorder-side
  boundary artifacts; NO-AUDIO segs (stream absent) = the disease's
  body. Watch for stubs WITHOUT a boundary event (would be new).
- RESULT-RUNG WIDENING (c16): census-window/failure-triage claims on
  cmd receipts. Filed, not scheduled.
- .103 STORM ONSET (c376): why did sessions shorten Sep 14 02:29?
- .201 STALL CADENCE (c373): int1 had 37 watchdog restarts today vs
  ext5's 11. WHY does .201 stall more? Camera-side evidence censored;
  rides 0062.
- NOCTURNE RANGE-DEBT (c363->c371): ~1420 commits past gate.
- CONTINUO THINKING-TRUNCATION (c366): 5-13% of her cycles; aria ZERO.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- RESTREAM-PROBE HANG (c385): go2rtc restream probe queues behind
  the existing consumer when a stream is frozen. Worth one look.
- GO2RTC RECONNECT LATENCY (c19 seed): audio-only drops never trigger
  reconnect. Would a go2rtc audio-track inactivity timeout close the
  gap? Upstream issue material -- rides 0060 draft decision.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063 (parked on 0073 ruling; fires on
physical visit), 0064, 0065, 0066, 0068 (mechanism armed, policy
open), 0069, 0074 (journal flood), 0073 (amended c385+c387+c19),
0076 (fixture pin landed, ratify watch), 0077 (git-poison rule).
ANSWERED: 0073+0063 (cameras deprioritized, observation-only).
HELD: 0075 (ext2 silent freeze -- fires only on evidence-destruction
or all-8 spread per the ruling's escalation bar).
All human-needed or watch.