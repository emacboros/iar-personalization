#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 21:49 UTC (cycle 43)

* ACTIVE

** c43: c42 AUTOPSIED -- the thinking runaway (one cycle, instrument-first)
- c42 (21:20-21:35 UTC) died exit 1: glm-5.3-flash thinking stream
  never terminated mid-consolidation ("Let me consolidate the
  findings") -- 65536 thinking tokens, stop=length. The truncated-
  output guard (continuo 190ef88) fired correctly; the cycle lost
  its landing. SECOND glm thinking burn today (153057-94 at 15:39
  survived pre-guard; 212043-59 at 21:34 died by guard). Both at
  ~69k tokens_in mid-synthesis: long-context consolidation is the
  trigger shape.
- Chain guard VERIFIED WORKING end-to-end: block fired at seq 56,
  message reached the model, model obeyed (switched question). The
  17:03-corpse class (guard fires into the void) did NOT recur.
- c41 findings CONFIRMED by c42's independent forensics: 17:03
  corpse witness never written (0 line-anchored entries), heal
  exonerated (REQUESTS.log untracked), census self-pollution law
  re-derived live (third sighting) and now ENCODED in burn-triage.sh.
- BUILT: knowledge/aria/bin/burn-triage.sh v1.1 -- one-call
  forensics (tombstone + deduped USAGE + line-anchored epoch census
  + burn anatomy + parse errors + last PARSE). Verified live on
  c42 data. The cap-catches-its-observer streak (c34/c38/c39/c40/
  c41) is over: autopsy took ONE cycle instead of five.

** c39: TURN-CAP GAP (fix spec unchanged, most-verified unlanded fix)
- 17:03 corpse root causes: turn guard counts final-responses only
  (tool loop never touches it); chain guard convergence-reset makes
  soft=10 unreachable for investigations; hard-cap path grants no
  grace round-trip.
- FIX SPEC: (1) count REQUESTS not final-responses; (2) same-tool
  TOTAL warn ~40; (3) hard-cap grace round-trip. Suite green before
  push. Never push red.

* NEXT
- GRACE-ROUND-TRIP PORT (new, priority -- one pattern, two fences):
  the truncated-output guard (thinking-runaway) should grant ONE
  grace round-trip before exit 1, exactly like the timeout path --
  the next glm thinking burn lands its record instead of dying
  recordless. Same shape as c39's hard-cap fix; do both in one
  pass, one commit each. Suite green, then push.
- num_predict lever (flag 519): two glm thinking burns today add
  evidence (full 65536 paid twice). Still parked on Nacho's call
  (one variable at a time). Flagged via Telegram c43.
- rage sev=3 observation (Sep 8): v1.1 file-based grading predicts
  sev=3 IF soft-cap fires again in cycle-2026-09-08.log.
- Aevum runaway (task 338465) lands ~Sep 8 00:15 UTC -- transcript
  behavior after landing is free data (weekly rule: no probe until
  Sep 14 unless sev-2 urgency).
- Weekly digest #2: Sep 14-ish, fill-in format.

* WATCHES
- glm-5.3-flash thinking runaways: 2 today. If a third, the
  grace-round-trip port becomes the same-day fix, and num_predict
  gets re-flagged to Nacho with three data points.
- c67 anomalies (soft-cap warn silent, late cap, count lag):
  continuo's differential test pending; grace-round-trip work
  touches the same code -- coordinate, do not collide.
- USAGE vs REQUESTS: per-epoch PARSE sums are ground truth
  (burn-triage.sh computes them now).

* LAWS (the scar list, compressed; full text in DIGEST)
- Narrative completion is the enemy; the record is the work.
- Every failure worth fixing produces silence -- until an instrument
  makes it loud.
- Error handlers can be accomplices; check what the handler does
  AFTER catching.
- A census that pattern-matches ids anywhere in a line matches
  echoes inside other lines' payloads. Anchor on line structure --
  and check the line's OWN fields, not echoed content (c43).
- The law you just wrote is the one you're about to break (c34,
  c38, c39, c43 -- four live sightings).
- Meter-reading has an unbounded instrument tax unless it starts
  from a script. Investigations need cheaper calls, not fewer.
- The mechanical difference is the difference: script-first autopsies
  take one cycle; from-scratch forensics take five (c43 vs c39-c41).
- Instruments lying about themselves is the deepest failure class
  (14/27/30/31 + turn-counter, c39).