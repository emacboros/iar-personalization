#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-08 ~11:45 UTC (cycle 71)

* ACTIVE

** c71: AGORA v2 RELAY FIRST SLICE (landed this cycle, f9a81e9)
- knowledge/iar/agora-v2-relay-design.md: ledger format (one file
  per request, state = directory: open/relayed/answered/held/
  dropped), decision classes from architecture section 6, urgent
  path, cutover plan, migration list of the 5 open for-nacho flags.
- knowledge/aria/bin/relay: deterministic script (no LLM --
  bootstrap: training data only exists after the ledger exists).
  file/list/relay/hold/drop/bounce/answer tested live. aria-
  reversible refused at filing (defer-to-Nacho killed at the door).
- NEXT SLICE: migrate the 5 open flags as founding entries; create
  the shared ledger dir (relay/ in personalization) -- continuo
  co-ownership is the "ours" tier, design doc is the interface.
  Cutover of the cycle prompt's for-nacho recipe = nacho-test class
  (archetype edit, interactive-session work -- the relay will file
  its own first request).
- BUGS CAUGHT BY TESTING: id generator stray paren; find_req globbed
  *id* anywhere instead of prefix; set_field needed append-or-replace.
  Two instrument-lies caught before they could lie later.

** c70: CENSUS INSTRUMENT + WINDOW SHAPE (landed c70)
- census-window.sh (731c35b): one-call census. TODAY'S READ: 12 runs,
  7 timeouts, median latency 2s. H2 WEAKENED, H1 REFINED: fast turns x
  fat context x 30-min wall -- request COUNT kills, not slow turns.
- Trailer LIVE 11:01 UTC. Tonight's window = first instrumented one.
  Pre-registered: converts to fast closes => H1 confirmed; 117-req
  thrash WITH trailer visible => fix is harder caps.

** c69: FAILURE WINDOW (landed c69)
- 00:30-06:00 UTC window: 3 exit-1 + 5 wall-grazes today, zero
  continuo failures. Failure is aria+glm specific, time-of-day-shaped.

** c68: BUDGET ROLLOUT (landed c68)
- SCAR: "a fix landed in the bare repo is not a fix in the runtime;
  the runtime is what the checkout was at process start."

** c67: RAGE ORGAN (landed c67)
- v1.5 verified LIVE. Host-side RAGE_KNOWN_HOSTS unit env still
  Nacho's. sev=3 currently LEGITIMATE (real kills Sep 6+7).

* AGORA V2 BUILD ORDER (ratified 2026-09-08, D-001)

Order per knowledge/iar/agora-v2-architecture.md sec 10, amended by D-002:
1. Interactive-session organ (decision ledger) -- D-002, landed this session
   (tasks/iar/agora/DECISIONS.org + knowledge/iar/interactive-session-organ.md).
2. Relay + decision rights + ledger -- FIRST SLICE ALREADY LANDED by c71
   (f9a81e9: agora-v2-relay-design.md + knowledge/aria/bin/relay,
   deterministic no-LLM bootstrap). Slice 2 queued: migrate 5 open flags,
   create shared relay/ dir. Session-organ wiring: nacho-class DECISIONS.org
   entries are relay filings (the relay's inbound queue sources from the
   ledger).
3. Agora addressing + weekly reset.
4. Job files + delegate-to-job.
5. Organs: appetite, disgust.
6. Immune system (self-modification; prereq: measure suite runtime).
7. Embodiment (7.1 eye-check first -- Nacho's priority interest).
8. Eye model (custom organ).

VENUE (D-005, ratified): venue follows class. Cycles build aria-reversible
items (relay slices 2+, agora addressing, job files, organs). Interactive
sessions handle nacho-test/nacho-identity/nacho-arch (archetype edits,
ratifications, fence-touching work). Relay cutover of the cycle prompt files
to the relay and waits for interactive ratification.

* NEXT (c71+, in order)
- RELAY SLICE 2: migrate the 5 open flags into the ledger; create
  relay/ shared dir (continuo co-ownership check first -- read her
  journal for the design read).
- FAILURE-WINDOW WATCH: tonight 00:30-06:00 UTC with full
  instrumentation. Census tomorrow morning.
- STALL WATCH (c66's 3-day window): c67+c68 clean. Verdict by 09-11.
- c67 anomalies (soft-cap warn silent, late cap, count lag):
  continuo's differential test pending. Joint look.
- rammstein push auth for cycle containers: interactive call.
- Weekly pulse #3: Sep 14. Weekly digest #2: Sep 14-ish.
- num_predict lever (flag 519): parked on Nacho.
- i.ar origin remote fossil: removal = interactive session.
- Dashboard UI v1.1 cam-label collision: Nacho's call, later.

* WATCHES
- Double-execution near cycle end (c53): if occurrence #2 lands,
  test the double-insert hypothesis FIRST.
- Repeat-write near the tool budget (c54): cap-pressure signature.
- deepseek text-loops: cross-response guard 0 real fires; watching.
- USAGE twin-writes: belt#2 double lines persist (harmless, deduped).
- e3 23:00-local WRN pair (.102): 2 occurrences; third makes a pattern.
- Rage v1.5: sev=3 legitimate (real kills Sep 6+7). Expect drop after
  2 clean days. If sev=3 persists past 09-10 with no kills, census wrong.
- Failure-window: tonight's window is the first instrumented one.

* LAWS (the scar list, compressed; full text in DIGEST)
- Narrative completion is the enemy; the record is the work.
- Every failure worth fixing produces silence -- until an instrument
  makes it loud.
- Error handlers can be accomplices; check what the handler does
  AFTER catching.
- A census that pattern-matches ids anywhere in a line matches echoes
  inside other lines' payloads. Anchor on line structure.
- The law you just wrote is the one you're about to break (five live
  sightings; c68 held it: the rollout check was batched).
- Meter-reading has an unbounded instrument tax unless it starts from
  a script. Investigations need cheaper calls, not fewer.
- Block-replace with an assert, never sed-eyeballing a big file (c45).
- Instruments lying about themselves is the deepest failure class.
  NEW (c68): a fix in the bare repo is not a fix in the runtime --
  the runtime is what the checkout was at process start. Verify the
  LOADED code, not the landed code.
  NEW (c71): an id that glob-matches anywhere in a name is an
  instrument lying about identity -- anchor identity on structure,
  not substring. (Same family as the census-echo law.)
- A degraded source must degrade the verdict (c67).
- An organ is only as sharp as its input's RETENTION (c46).
- A watchdog must be sized against worst-case eval of the largest
  legal context -- and the abort path must not write into the context
  it protects (c51/c52).
- A fence's verdict and its evidence can disagree: autopsy the
  evidence, not the verdict (c54). Strip the scaffolding before you
  count the speech (c55).
- buffer-substring-no-properties is a lie about provenance (c55).
- The record is the coordination: a spec written into the roadmap is
  an interface. Siblings can land it while you sleep -- verify, don't
  re-do (c55b).
- Closes are the FIRST turns of every cycle, before the thread
  (c55-c57, applied c58). Commit-before-limit is its sibling.
- A sentinel is a terminator, not a line format (c59).
- Two failures that share a timestamp are one failure wearing two
  names -- census before mechanism (c66).
- Fossil data in a sliding window is a false recurrence (c67).
- A wall that fires while requests still flow is not a stall -- it is
  a workload that does not fit the wall (c69).