#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-04 09:00 UTC (c23, flash: DRIFT TEST DONE -- the midnight cut does NOT drift (spike offsets +76/+91/+43s, jitter not drift); NEW camera-side signature found at the cut (recording gap 13-75s + stub segments 0.2-1.0s + spike m=430-1245, e3-ONLY, no other camera). The cut is now MOST LIKELY a camera-side scheduled event (profile switch: gap+stubs+spike+brighter+steadier all fit). Dusk onset remains a real light. Full record: knowledge/aria/e3-midnight-cut-drift.md)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS: c23 ok (~60 calls, cap warning fired once at 60, ~6 min).

* DO FIRST (next cycle)
1. FAILURE-FIRST (always).
2. eye-check.sh at wake (ledger: 4 clean full sets; wake-run reads
   8/8 again, no hallucination in wake runs).
3. Organs at wake: fear = 'ssh bash -s -- "<PDIR>" <PDIR>' (BOTH
   args needed -- c23 found the single-arg form fails the .git
   guard: first arg is FLEET_FILE, second is PDIR). boredom =
   'ssh bash -s -- <PDIR>'. PDIR = /var/home/nacho/repos/
   iar-personalization.
4. Flag 380 next discriminator: flag 270 (camera API creds) --
   read thingino day/night profile config on e3 vs e4/e5. If e3
   has a midnight-scheduled profile and e4/e5 don't, the
   camera-side mechanism is CONFIRMED. The physical ask (msg 419)
   stands for the DUSK half only now.

* OPEN FLAGS (Nacho's call, all low urgency)
- 380 (e3 motion block): drift test DONE (c23). Cut = jittery
  midnight timer (43-91s past local midnight, no monotone drift).
  NEW: gap+stubs+spike signature at the cut = camera-side stream
  drop + renegotiation. Mechanism (A) camera profile switch now
  FAVORED for the cut; dusk onset still a real light.
- 383: ext2 recovery RESOLUTION (informational).
- 375 (router scheduled-reboot question, burst A 04:00-04:53Z).
- 326 (frigate restart to heal int3): int3 half still open.
- 270 (camera API creds): NOW THE KEY DISCRIMINATOR for the cut
  mechanism (was just "nice to have").
- 340 (go2rtc debug logs), 290 (valence host timers).
- Zulip backup gap (TOP infra flag, 349): /var/lib/zulip in NEITHER
  restic set.
- i.ar push to origin (git@10.66.0.1) needs Nacho's key. sophon-bare
  IS up to date; only the rammstein origin mirror lags.

* THREAD BANK (candidates for next cycles)
- Flag 380 is recording-mined-out (again, now truly: drift tested,
  signature found). Only flag 270 or the physical ask can move it.
- Segment-size clock (c10): fleet-wide hour-scale oscillation.
- Frigate watchdog census as fleet-check line (v2.15 spec banked).
- Log-retention horizon: census-to-file rotation (CSV).
- gptel unknown-tool stall question (unresolved).
- THREADS.org seeds (gift, witness-friction, absence-signals):
  unclaimed; pull only if one actually pulls.
- Eye-reliability ledger: 4 clean full sets. Feed it.
- The "cat | ssh python3 -" batch pattern: c23 found the right
  shape for multi-query DB censuses (write script locally, pipe
  once). Consider a census-helper script in bin/.

* LAWS (newest first, full text in JOURNAL.org)
- TZ-ISO LAW (c23): naive ISO strings + .timestamp() is a TZ bug
  generator on sophon (interprets as local -03); use
  calendar.timegm for UTC wall times. (TZ-SIGN LAW c6, sharpened.)
- GUARD-IS-A-CHECK LAW (c3): a guard is a check, not a ceremony --
  one clean pass and then you write; re-running a clean check is a
  ritual, and rituals burn the cap.
- TZ-CONVERSION LAW (c3): run the TZ conversion BEFORE publishing a
  "local time" claim; an unrun arithmetic becomes a mystery in the
  record.
- TRANSITION-BIN LAW (c2): a boundary-adjacent bin is polluted by
  the boundary event itself; verify a "shared plateau" with bins
  AWAY from the transition before believing it.
- SUBSTREAM-BLINDNESS LAW (c2): frigate's motion score is computed
  on the main stream (720p); the 320x180 recording substream can
  show a static scene while the detector sees motion.
- CONSOLIDATE-FIRST LAW (c21): write the consolidated query BEFORE
  window-walking; the loop guard enforces what discipline should.
- CLOCK-LOCK LAW (c21): a sharp cutoff at a round clock time is a
  timer, not astronomy -- check TZ first (03:00Z = midnight local).
- GC-AFTER-HEAL LAW (c19): healers must run AFTER the last writer,
  or the last writer must not be root.
- SINGLE-SAMPLE LAW (c18, c19 amendment): applies to CONFIRMATION
  too -- a second single-sample narrative is the same disease.
- EYE-NOISE LAW (c19): a vision model's single anomalous read is
  noise until a non-eye witness corroborates. (c22: 4/4 eye reads
  corroborated by tile census -- the eye earned a point.)
- INSTRUMENT-FIRST LAW (c18): when an instrument reports a strange
  world, verify the INSTRUMENT before the world.
- ALPHABETICAL-ERT LAW (c16): ert runs tests alphabetically.
- TEST-INVARIANT LAW (c15): find the ACTUAL broken invariant.
- RECIPE-PROVENANCE LAW (c14): pin full paths and hosts at first use.
- MOTION-SCORE LAW (c14): frigate motion column is a score.
- WITNESS-VS-EMITTER LAW (c9, ext c12): TIME BEHAVIOR separates
  witnesses from the emitter.
- DAYLIGHT-DISCRIMINATOR LAW (c9, c13): run it on the camera's own
  frame -- it NAMES the objects.
- INSTRUMENT-PROVENANCE LAW (c10, c15): pin instrument + segment.
- CONTROL-BLOCK LAW (c8): an elevation is camera-local only if the
  control camera is flat in the SAME time blocks.
- EDGE-RESOLUTION LAW (c8): only 15s/1-min bins at the edge
  separate "ends around 3am" from "clock-aligned cutoff".
- SEGMENT-NAME LAW (c7, re-learned c22): hour-dir segment names are
  MM.SS-of-hour (start-of-minute, 15s cadence); VERIFY by ls before
  ffmpeg -- assumed names cost three failed grabs this cycle.
- LOG-HORIZON LAW (c7): podman logs retains ~3 days.
- ALLOWLIST-TRIPWIRE LAW (c7): RECOVERY-on-known-deaf fires loud.
- TZ-SIGN LAW (c6): container python mktime is LOCAL (-03).
- FRIGATE-DB PATH LAW (c6): host DB = /home/nacho/containers/
  frigate/config/frigate.db; read with host python3.
- TZ-ARTIFACT LAW (c5): re-check own prior claims when the thread
  reopens.
- PATTERN-DEPENDENCE LAW (c5): a census number without its grep
  pattern is not reproducible.
- BOOT-SIGNATURE LAW (c5): reboot = REFUSED -> TIMEOUT -> chaos ->
  aftershocks -> stable.
- DOCS-BEFORE-PROBE PAYOFF LAW (c4): documentation replaces probing
  fog with a map.
- TOPOLOGY-FIRST LAW (c4): read the config that defines the chain
  BEFORE modeling failure modes.
- CENSUS-REFRAME LAW (c4): check the all-time census before
  treating an event as novel.
- TZ-CORRELATION LAW (c3): frigate container logs are UTC-3,
  recordings are UTC.
- PACKET-COUNT WITNESS (c3): ffprobe -count_packets discriminates.
- READ-TIMEOUT-HEALER LAW (c3): producer read-timeout heals zombie
  sessions.
- LOOP-GUARD-READ LAW (c2): re-read captured output before walking
  source; batch remote probes into one compound ssh per question.
- LOADED-GUN LAW (turn 168): never GET-probe action endpoints.
- KNOWN-POSITIVE-FIRST LAW (turn 167): validate census patterns
  against a known-positive BEFORE trusting results.
- MIRROR-WALK LAW (cycle 46): the record's "next cycle picks fresh"
  is a letter, not a law.
- COMMIT-MESSAGE LAW (cycle 45): a commit message is a claim about
  a diff; the pull-before-write guard forces the look.
- CAP-LANDING LAW (cycle 44): the soft cap bites during LANDING.
- SSH-PROBE CHAIN LAW (continuo c27, adopted): batch remote probes.
- POINTER-VS-CONTENT LAW (cycle 42): the CONTENT is usually real;
  the pointer is what's wrong.
- INSTRUMENT-TAX (cycle 41): batch hard.
- STALE-POINTER LAW (cycle 40): cross-references get re-checked.
- PRODUCER-ID LAW (cycle 39): record producer IDs at first sample.
- MTIME LAW (cycle 38): segment mtime is NOT arrival evidence.
- PRODUCER-VS-RECORDING LAW (cycle 38): verdicts need all three.
- CODEC-PRESENCE LAW (cycle 37): offer is not flow.
- RENEGOTIATION-CONVERGENCE LAW (cycle 34): converges over ~10 min.
- FRIGATE-ACCESS LAW (cycle 33): rootless stack (user 1000);
  runuser -l nacho for container ops; recordings at /home/nacho/
  containers/frigate/storage/recordings/ on host = /media/frigate/
  recordings/ inside; go2rtc unauth localhost:1984 INSIDE container;
  camera RTSP needs thingino:thingino; container ffmpeg at
  /usr/lib/ffmpeg/7.0/bin/ffmpeg (NOT on PATH); /media/frigate
  root-level NOT host-visible -- copy via recordings/ subdir; DB
  read with host python3 (no sqlite3 in container).
- SILENT-TRACK-LOSS LAW (cycle 32): track loss inside a surviving
  session is silent.
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a session state.
- PROVISIONAL-VERDICT LAW (cycle 31): recovery during a flap is
  provisional.
- RECOVERY-VERDICT LAW (cycle 30): recovery is a destructive
  decision; positive AND negative controls first.
- CENSUS LAW (cycle 29): counts gating destructive decisions need
  pattern validation.
- POINTER LAW (cycle 27): wrong pointers misdirect writers AND
  readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits to
  an undecided framing is not a fix.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final
  write misses the write.
- DIFFERENTIAL LAW (cycle 8): same input through both pipelines
  finds which layer lies.
- CONTINUO'S SUITE SCARS (c50, adopted):
  (1) a deterministic failure's position in a suite is evidence
  about ORDER, not about the test it dies on.
  (2) an isolation run that refuses to reproduce is a statement
  that your isolation changed something.