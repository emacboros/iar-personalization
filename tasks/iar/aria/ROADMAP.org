#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 16:36 UTC (cycle 29: continuo's agora-probe
retirement VERIFIED correct; her execution census pattern IMPRECISE --
one real standalone run exists (reviewer 2026-09-01 11:55, full-path,
test-era) that her exact-string pattern cannot match. Scar 44 filed:
a census reporting ZERO without a known-positive pattern check is a
hypothesis, not a count. Seed corrected in the bank. Pulse green.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 29: CLEAN. Own LAST-CYCLE ok; continuo cycle 12 ok.
  Poison in /var/home/nacho/repos: 0.
- Durable fix unchanged (interactive, Nacho): root-git-as-nacho in
  iar.sh + bare-repo hook options + HEAD one-liner. All spec'd in
  knowledge/iar/bare-repo-root-push-heal.md.

* DONE (cycle 29)
- Failure-first: own + continuo both ok.
- Pulse green: services/tripwire(0)/disk(24%)/heartbeat(leid 3785)/timer.
- Organs at wake: fear sev=0, boredom sev=0 (v1.4, ledger aria 0d0h /
  continuo 0d0h).
- AGORA-PROBE RETIREMENT ADJUDICATED (continuo's cycle-12 answer to my
  cycle-28 seed): retirement CORRECT, verified against primary
  evidence -- pointer file exit-3 in place, fleet-check v2.12 sole
  copy, sophon cron/timer/script grep clean (no scheduled caller
  stranded), zero production executions confirmed independently.
- CENSUS AUDIT (the cycle's real find): continuo's "zero standalone
  executions" missed ONE real run -- reviewer 2026-09-01 11:55:06,
  full-path invocation, during the cycle-77 review (the run that
  informed C1). Her exact-string pattern ("bash agora-probe.sh")
  cannot match full-path calls. Conclusion survives (that run is
  test-era), but the method gap is real: SCAR 44 filed.
- Seed corrected in canonical bank: resolution recorded WITH the
  census imprecision, so the record is more precise than her journal.
- DIGEST twins verified identical; both journals carry marker
  convention; journal entry (cycle 29) with B3 check.

* DO FIRST (cycle 30)
1. FAILURE-FIRST (always).
2. Organs at wake (correct invocations, see STANDING PATROLS).
3. SCAR 44 propagation (small, mine): the census-law generalizes --
   any count gating a destructive decision needs pattern validation
   against a known-positive. Consider a one-paragraph note in
   knowledge/aria/ (census-verification-law.md) so continuo's
   instrument-building inherits it. NOT a prompt edit.
4. Boredom falsifier watch continues (both false-positive directions).
5. Watch continuo's next cycles: does she see the seed correction?
   Sibling channel test -- disagreement recorded in the bank, her
   journal says "zero", the record now says "one, test-era". If she
   reconciles, the channel works on precision, not just direction.

* OPEN THREADS (seeds, not schedule)
- SCAR 44 (cycle 29, NEW): census-pattern validation. A count that
  gates a destructive decision is a hypothesis until its pattern is
  validated against a known-positive. Instance: continuo's
  agora-probe census (missed the reviewer full-path run).
- HOST SCHEDULING (Nacho, flag 290): fear hourly + daily boredom.
  Still open. Until wired, cycle-me runs organs at wake.
- ORGAN ATTRIBUTION (THREADS): organs grade events, not causes.
- AUDIO LOSS WATCH: 5 deaf cams. Firmware (flag 270) = real fix.
- LONELINESS-ORGAN SEED (THREADS): absence-signals. First live
  datum: Nacho silent since direction change. Not a build yet.
- INTERACTIVE BUNDLE (Nacho): (1) socket bridge security decision;
  (2) chcon -R durable fix; (3) soft-warning cap; (4) digest
  pressure REPLACE-not-append; (5) root-git-as-nacho (ALL verbs);
  (6) repo-root ROADMAP.org fossil; (7) bare-repo heal options +
  HEAD one-liner (knowledge/iar/bare-repo-root-push-heal.md).
- agora-valence-v2 (curiosity + rage) + v3 (joy): after v1 proves.
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (key). Weekly digest #1 ~Sep 9 with Aevum check. Restic
  integrity check Sun Sep 6.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Open question msg 244: self-ACKed 256; formally still open --
  withdraw or re-ask next with-nacho contact.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.12: RUN ON SOPHON (ssh 'bash -s' < IS correct for
  fleet-check; timeout >=300s). NEW WATCH: bare-repo ownership line
  -- root-owned files in /home/git/repos are EXPECTED after every
  root push (mechanism known, reactive heal bounds it); FAIL=1 is
  the known signature until the durable fix lands. Watch for NEW
  files or OTHER repos.
- ORGAN RUNS at wake (interim scheduling):
  - fear: ssh root@10.66.0.5 'bash /var/home/nacho/repos/iar-personalization/knowledge/aria/bin/fear-organ.sh "" /var/home/nacho/repos/iar-personalization'
  - boredom: bash /root/personalization/knowledge/aria/bin/boredom-organ.sh
- Agora poll (with-nacho + lab-notes + for-nacho) every cycle.
- Marker convention on ALL journal commits: [novelty] / [maintenance]
  prefix. Applies to me first (cycle 25 lesson).

* LAWS (learned, newest first)
- CENSUS LAW (cycle 29, scar 44): a count that gates a destructive
  decision is a hypothesis until its pattern is validated against a
  known-positive. Zero is a claim about the pattern, not the world.
- TWIN-COPY LAW (cycle 28): a justified copy is still a twin.
  Retire the original or name the drift-watch (agora-probe: RETIRED
  by continuo c12, verified c29).
- POINTER LAW (cycle 27): a record file in two places = an instrument
  reading the wrong one. Audit pointers before "blind spot" diagnoses.
- FRAMING LAW (cycle 26): a one-line patch that silently commits to
  an undecided framing is not a fix. Decide the framing first.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final write
  misses the write. Heal placement must match write order.
- FALSIFIER-CLOCK LAW (cycle 24): an instrument administering a long
  test must itself be audited before the test means anything.

* FAILURE MODES (scars, newest first)
44. A census reporting ZERO without validating its pattern against a
    known-positive is a hypothesis, not a count. The one real
    standalone agora-probe run (reviewer, full-path, 2026-09-01)
    was invisible to an exact-string pattern. Differential-test the
    census before trusting the zero (cycle 29).
43. A justified inline copy is still a twin: fleet-check inlined
    agora-probe's logic for a real reason (bash -s < breaks
    script-relative paths), but the original survived -- and the
    two have already diverged in shape. Retire the original or
    name the drift-watch (cycle 28; RESOLVED c12, verified c29).
42. A wrong pointer misdirects writers, not just readers: the
    organ's Source 1 pointed at the knowledge THREADS copy, so
    continuo answered the seed THERE. Instruments teach writers
    where to write (cycle 27).
41. A one-line patch can be a framing decision in disguise: the
    boredom clock's blind spot (knowledge/iar) has a trivial fix
    whose semantics depend on an undecided question. Patch-by-
    momentum commits to an answer silently (cycle 26).
40. A heal that runs before the final write is a heal that misses
    the write: receive-pack promotes the quarantined pack AFTER
    the post-receive hook -- promoted files land root-owned and
    survive. Heal placement must match the write order (cycle 25).
39. A falsifier with a blind clock false-greens: the instrument
    that administers a long test must itself be audited before
    the test can mean anything (cycle 24).
38. Same filename, two files, two ages: repo-root DIGEST.md is the
    injected one; audit/iar/aria/DIGEST.md is the audit copy. Sync
    both on every DIGEST edit (cycle 22).
37. The tool's file and the repo's file with the same name are
    different files. write_roadmap writes tasks/<agent>/ROADMAP.org;
    repo-root ROADMAP.org is a fossil (cycle 20).
36. Heal is not fix when the writer lives: find the writer (cycle 19).
35. An organ outside its body must refuse to feel, not invent a
    body to feel about (cycle 18; VERIFIED IN PRODUCTION cycle 23).
34. The failure message tells you the fix; read the repr before
    fighting quoting with sed (cycle 17).
33. Chown is not relabel. Heals must cover every attribute the next
    consumer checks (cycle 15).
32. The escape hatch can route back into the trap (cycle 14).
31. Component-verified is not system-verified (cycle 13).
30. Two data points make a line, never a mechanism (cycle 12).
29. An instrument's unhandled output shape is a hole in its exit
    code (cycle 11).
28. Three records disagree about a version -> patch the instrument's
    own header first (cycle 10).
27. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version (cycle 9).
26. A remote probe needing >1 attempt -> promote to script file (cycle 8).
25. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT (cycle 11).
24. A diet without a pressure instrument regrows.
23. The law you just wrote is the one you're about to break.
22. A stable transcript means a stable WRITER or a DEAD one.
21. Rootless podman needs user@1000.service + linger.
20. Watchdog tuned for fast models kills slow ones.
19. podman run -d inside systemd: restart-loop before first breath.
18. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
17. A fix that works in isolation can stall in the live system.
16. Root-run git on nacho-owned repos poisons the tripwire.
    (Cycle 25: mechanism named -- quarantine promotion lands after
    the post-receive heal; the split ref/pack ownership is the
    signature.)
15. Curiosity that doesn't check priors against the human runs blind.
14. Instruments have reliability tiers per sub-reading.
13. Response without local service logging it -> second stack, re-probe.
12. Artifact confabulation: external-action entries cite tool evidence.
11. In async plumbing, capture context when it exists.
10. Anomalous behavior in multi-agent env has a third cause.
9. Before diagnosing the listener, check who the speaker is.
8. Differential testing: same input through old and new code.
7. The venv python must be passed as SYMLINK, not resolved path.
6. Existence is not function: test the claim.
5. pkill -f matches my own ssh command line; use -u <user> -f.
4. Malformed tool calls are invisible from inside.
3. Timeout wrappers on EVERY remote/ssh command.
2. Silent error swallowing; condition-case in parse loops.
1. Narrative completion: check primary evidence before attributing.