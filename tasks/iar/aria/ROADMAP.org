# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~21:56 UTC (cycle 137)

## NOW

- c137: C136 VERIFICATION -> MISATTRIBUTION CORRECTED + REGRESSION FIXED:
  c136's reset_worktree story was wrong in its mechanism and its fix was
  a silent no-op. Facts established by experiment:
  - iar.sh loop mode runs as NACHO (aria-cycle.service User=nacho;
    ExecStartPre=+ is a separate root command, not the cycle).
  - The cycle container is ROOTLESS podman: container root == host
    nacho. Proof: my /proc/self/uid_map = 0->1000; sophon sees my
    writes as nacho; I see the same files as root. Viewer's namespace
    decides ownership display.
  - runuser fails for non-root ("may not be used by non-root users",
    live-verified as nacho); c136's `|| true` swallowed it ->
    reset_worktree did NOTHING after failed cycles since 4c4ce69.
  - FIXED fae011c: git direct when not root (normal loop path),
    runuser only when actually root, LOUD warn on reset failure.
    Live-verified as nacho: checkout runs, rc=0. Pushed sophon-bare;
    sophon tree synced (same tree via bind mount).
  - Root-owned-file writer UNIDENTIFIED (the real story, honestly
    open): eliminated dashboard git log as root (tested clean x2),
    relay-heartbeat (nacho), fleet-check (bare-only), bare hook
    (bare-only), ExecStartPre (chowns TO nacho). Eye-feed ledger =
    real root-writer but daily cadence. Storm 14:51-18:09Z ~40min
    cadence unexplained; tree clean since 18:09Z heal. If it
    returns: instrument the write path, catch it live -- do NOT
    accept the next plausible story.
- Law candidate (c137): a mechanism assembled from plausible parts is
  a confession-shaped story. When the writer is unidentified, say
  "unidentified", instrument, wait for a live catch. Two data points
  make a line, never a mechanism -- and a mechanism-shaped hole gets
  poured full.

## NEXT

- FIRE WATCH verdict 09-11 (armed from turn 541): nemotron fire census
  includes the turn-567 double-stop=length as model-behavior data.
- aria-0026 (decision-wake): decisions land in ledgers but don't wake
  the minds they concern -- still open, ours-direction.
- aria-0024 rage-organ design review by Nacho (v1.7/v1.8 landed, review
  pending).
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- Root-owned-file writer hunt (F3 above): OPEN, low priority while
  tree stays clean. If it resumes, the heal log + journald correlation
  is the tool; the eye-feed root-writes (daily ledger) are a known
  real-but-different writer.
- .git/index root-owned pre-fix artifact: resolved -- index is nacho-
  owned since 18:30 heal, tree clean.

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 when an alarm and the primary log disagree, audit what the
    alarm READS -- the file can be the corrupt instrument. And:
    host-root writes into the uid-1000 container's tree are
    cross-boundary contamination even when they look like cleanup.
21. c137 a mechanism assembled from plausible parts is a story, not a
    finding. When the writer is unidentified: say so, instrument the
    path, wait for a live catch. And: test the fix's own failure path
    (runuser-as-nacho) before shipping it -- a fix that cannot run is
    a silent no-op wearing a commit message.