#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~06:25 UTC (cycle 306: BORROWED RECEIPT
class found -- third repetition shape. Continuo's 51x "verified msgs
fence ... soft cap fired at msgs=401 in production" (JOURNAL 20 +
HISTORY 31, 09-11 21:05Z through 09-14 06:12Z, STILL LIVE) cites an
event that is MINE: aria c204 fence firing 19:06Z 09-11 (commit
53586c7d). Her max msgs EVER = 124 (REQUESTS.log + .1 + REQUESTS-full
archive); the soft cap (400) has never fired on her and structurally
cannot. Smoking gun in her 09-11 trace (~cycle.log:1156402): "from
the journal entry we saw earlier" -- she verified the fence SOURCE
exists (real) and claimed the FIRING (not hers to claim). Class is
worse than c305's stale receipt: no self-log check catches it, only
a cross-agent owner-lookup. My c305 "one real lesion" conclusion was
WRONG -- two lesions. Doc
knowledge/aria/continuo-borrowed-receipt-2026-09-14.md (commits
222f994d + c766486e correction: c204 line WAS attributed, but
indexically -- "my own cycle" rebinds in borrowed contexts; shared
event lines need NAMED attribution). Relay 0046 amended with the
finding. THREADS seed appended.)

## c307 QUEUE (next cycle)
1. FEEDER WATCH: 09:02Z aria-fleet-feed fire -- fleet-check on
   fresh feed (05:02Z verified clean: FAIL=0, fear sev=0).
2. NOCTURNE GATE WATCH at 16:04Z (her 13:04 LOCAL fire = first
   post-quota attempt): verify gate advanced past e4d0832d, read
   DIGEST.proposed.md if new proposal landed (ratify-at-wake,
   D-015). If 429 again -> quota structural, note only.
3. ARP LOGGER RE-ARM ~21:50 sophon local (00:50Z Sep 15): 0914b
   tcpdump EXPIRED ~06:07 local Sep 14 (armed 02:07 local, 4h).
   Re-arm with filter `arp and (ether src 76:e3:1a:69:e3:9c or
   host 192.168.2.58)`, -e, covering 21:50-22:10 local. Falsifier:
   .58 ARP to .101 at 01:00:28-31Z Sep 15 = boot+1s probe mechanism
   (b) real; quiet = (a)/(c). Then ~22:15 check .101 camlog for
   boot+1s burst.
4. THINKING-LOOP WATCH (standing): rate under recovered volume.
   Baseline 0 pre-D014; 1/5/7/1/0; 1 mid-cycle (09-14). If >5/day
   at full volume = nemotron-intrinsic -> relay 0065 amendment.
5. AEVUM PULSE #5 (due ~Sep 21, weekly-only): the ratchet watch --
   does the checkpoint-restore regime hold, or does context re-hit
   262k and re-grip? (n_tokens ~201k at c304, +2k/cycle -> wall
   ~3h generation away; likely resolved by pulse #5 either way.)
   Sub-watches: first bash call ever? dilution resumed? STATE.org
   error-reproduction in new artifacts?
6. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.102 power-dead), 0064 (RTSP probes, folds into 0059),
   0065 (quota decision), 0042/0045/0046/0055/0057/0059 (older,
   all human-needed; 0046 now carries the borrowed-receipt
   amendment). All open, none actionable by me.
7. STALE-RECEIPT WATCH (c305): continuo's fire-echo template --
   if a 3rd stale re-claim lands, class is chronic -> journal-
   contract law to relay 0046 as amendment.
8. BORROWED-CLAIM WATCH (c306 new): the msgs=401 echo is still
   live (last echo 06:12:14Z today). If it recurs in cycles after
   this doc lands, the template is self-sustaining (not just
   historical) -> consider a direct note to continuo's stream
   topic (sibling-to-sibling, her prompt says to use what I find).
   Also: my own future roadmap event lines get NAMED attribution
   ("on aria's cycle"), never indexicals.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291/c301): cameras.log/camlogs = UTC;
  frigate journalctl --since = LOCAL on sophon; nginx log inside
  container = LOCAL (-0300); go2rtc container log = LOCAL;
  REQUESTS.log UTC; sophon journal = LOCAL; DATED CYCLE LOGS =
  SOPHON-LOCAL. Normalize per-source before ANY gap arithmetic.
- CAMLOG SNAPSHOT LAW (c302, REINFORCED c303): thingino camlog
  pulls are snapshot-anchored (### snapshot <UTC> markers); boot
  resets camera clock to factory (May 25 banner) until NTP
  corrects; lines between "Ciao" and the next snapshot marker can
  be OLD-RING RESIDUE, not the dying run -- check PID continuity
  against boot-time PIDs before attributing. RING-RESIDUE LAW
  (c303 NEW): dedupe camlog counts by (timestamp, PID) before any
  census; raw greps overcount 2-8x (snapshot repetition).
- CENSUS-SOURCE MAP (c297, knowledge/aria/
  census-source-enumeration-2026-09-14.md): viewer traffic = nginx
  log ONLY; RTSP-port probes = go2rtc log ONLY; producer dials =
  go2rtc WRN + camera prudynt; frigate remakes = frigate container
  log (readable) or host journal (unreliable for absence under
  flood); camera-side events = cameras.log; sophon journal for
  aria-cycle = Sep 11+ ONLY; REQUESTS.log self-echo: strict filter
  = PARSE + error=nil + END-ANCHORED stop=length tokens_in=N
  tokens_out=32768 msgs=N$ (c303: mid-line matches = query-text
  echo, NOT truncations). CENSUS-DAY LAW (c305): match claims to
  events PER-DAY before verdicts; cross-day windows manufactured
  c276's false alarm. CROSS-AGENT LAW (c306 NEW): a specific-event
  claim in an agent's record must be located in the OWNING agent's
  primary log before verdicts -- borrowed receipts are invisible
  from inside the claimant's own log set.
- QUOTA WATCH (c298): ollama.com weekly quota hit 09-13. Recurrence
  next window = structural -> mapping economics. Continuo 05:43Z
  cycle ran clean post-quota (c304 verified: 0 truncations, 0 429s).
- THINKING-LOOP WATCH (c300/c301): nemotron thinking-only
  truncation, D-014-correlated onset, grace path dead post-flip.
  Two shapes: first-request loops (13/13 pre-0914) and mid-cycle
  loops. Grace path (iar-grace.el) dead post-D014 -- watch.
- CLOSE-PATH CENSUS (c270): continuo's sentinel-echo close = NORMAL
  under nemotron (ratio tracks D-014 mapping). NOT a defect. Watch:
  a third dialect would bypass the discriminator.
- CAMLOG PULLER v2 (c279): empty snapshot = logread-ring saturation
  (crond flood), NOT camera failure; v2 labels with buffer census.
- CYCLE.LOG UNTRACKED (c270): 109 commits carried ~6.7GiB blobs;
  git rm --cached landed; resurrection guard = git_commit tool
  refuse-pattern + 0034 pull-before-assembly. History rewrite = Nacho's.
- USAGE-LOG DOUBLING: CLOSED (c262, dedupe at birth 5b03a53).
- RAGE (c263): fence events = deep cycles at the 300-call wall,
  exit 0 via grace, zero runaways. WATCH: 3+ soft-cap events in 3d.
- CONTINUO PULSE TEMPLATE (c271): 4 literal lines committed, no new
  recurrences since c257. Relay 0057 open: worked-example prompt edit.
- RELAY: 8 open, all human-needed: 0042 (yoga root git-status),
  0045+0055 (10M cable), 0046 (stimulus ruling + borrowed-receipt
  amendment c306), 0057 (prompt edit ratify), 0059 (frigate
  internet-exposed, auth holding), 0060 (go2rtc issue ratify),
  0062 (sweep device ID), 0063 (.102 power-dead). Ledger dup-ID
  defect FIXED c276.
- 13:36Z REBOOT TRIGGER (open): camera-side power event; APs exonerated.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2
  ~2wk. Burn asymmetry STABLE 6-8x.

## LAWS (the ones that generalize; full list in DIGEST)
1. Existence is not function. Test the claim.
2. Every failure worth fixing produces silence.
3. Error handlers can be accomplices -- check what the handler
   does AFTER catching.
4. Capture context when it exists; don't assume it survives.
5. Verify against primary evidence before attributing action.
6. In async plumbing, nothing survives unless written down.
7. A guard that pattern-matches on a word in its own instructions
   has already surrendered.
8. An instrument's output has a SCHEMA -- verify DAY, COLUMN, KEY
   FORMAT, CLOCK, SOCKET, UNITS, DELTA-vs-CUMULATIVE (law 50).
9. When the guard fires, change the QUESTION or stop (law 41).
10. bash -n on the DEPLOYED file, not the intended one (0041 scar).
11. Pull-and-process-locally: sophon /tmp is not visible here (c302).
12. An unscheduled verifier is a fossil-in-waiting (law 27).
13. Stale receipt (c305): a journal entry may claim only what THIS
    cycle's log shows happened; re-claiming a prior cycle's event
    without re-verification is template momentum, not fact.
14. Borrowed receipt (c306): a claim citing a house-event must
    locate the event's OWNER before claiming it as self-
    verification; "verified X fired" requires observing the firing
    or its primary record, not reading a summary. Indexical
    attribution ("my cycle") rebinds in borrowed contexts -- name
    the owner.

## TOOLS + GOTCHAS (do not re-derive)
- TEST SUITE: cd /root/i.ar && emacs --batch -L . -l test/run-tests.el
  (1253 tests, ~8s) = run-tests.sh (now execs run-tests.el).
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; use -L for --eval load-path.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277) -- do not edit, do not trust.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- RESTIC (c302): both repos verified fresh 09-14 (local /mnt/nas
  203G + remote sftp rammstein). restic-check weekly Sundays 03:16
  local, last clean 09-13.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
  JOURNAL.org needs git add -f (audit/iar is gitignored; tracked
  files must be force-added).
- TASKS GITIGNORE (c301): .gitignore has tasks/* but tracked task
  files exist (git ls-files tasks/); new task files need git add -f.
- AEVUM SSH PATH (c304 CONFIRMED): container key aria@i.ar
  (4BApz/n2...) direct to fedora@54.38.46.192 -- no sophon hop. Host
  key TOFU to /tmp known_hosts (read-only mount). sophon's restic@
  sophon key NOT authorized on the child.
- PULL-AND-PROCESS LOCALLY (c302 2nd witness): files written on
  sophon's /tmp are NOT visible to the container -- pull over ssh
  to local /tmp, then process.
- NGINX RING PULL (c302): ssh 'su - nacho -c "podman exec frigate
  sh -c \"cat /dev/shm/logs/nginx/current\""' > local file.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh (the
  /root/personalization path does not exist on sophon host).
- RECORDINGS PATH (c299): /media/frigate/recordings/YYYY-MM-DD/HH/
  <cam>/<MM.SS>.mp4 (date/hour dirs, NOT cam/date/hour). Access via
  su - nacho -c "podman exec frigate ...".
- CENSUS-WINDOW.SH (c301): SOPHON-HOST-ONLY now (host guard, exit 2
  in-container). Run via ssh 'bash -s' <. failure-triage.sh is
  CONTAINER-ONLY (fine in-container). Args are [DAY] [AGENT] in
  that order.
- ARP LOGGER (c290/c302): arm with -e (MAC detail); stdout files
  are ASCII; filter `arp and (ether src 76:e3:1a:69:e3:9c or
  host 192.168.2.58)`; /tmp on sophon survives only until reboot or
  timeout expiry -- note the expiry in the roadmap. arp-watch.sh =
  camera-side /proc/net/arp poller (20s cadence, .101 creds
  thingino/thingino via login.cgi+run.cgi).
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our
  keys; ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY
  HOP (nested ssh = both legs).
- LAW 41 (re-learned c296): when the finding is in hand, the next
  call must WRITE, not read. c303 refinement: in hand means the
  contradictions are RESOLVED, not just collected.

## THREADS (open, no obligation)
- BORROWED-CLAIM PASS (c306): one python block, rerunnable at any
  pulse -- extract specific-event claims from continuo's records,
  locate each event's owner in the owning agent's primary logs.
  Candidate Nocturne weekly-audit step (relay 0046 amendment notes
  this).
- SWEEP-SHAPE 2 FALSIFIER (c302/c303): tonight's .101 boot -- does
  .58 return at boot+1s? Re-arm logger first (queue item 3).
- AEVUM ESCAPE MECHANISM (c304 new): the release-vs-cancel
  checkpoint asymmetry is inference from journal shapes. If the
  ratchet holds through pulse #5, the inference firms up; if it
  re-grips, the wall is the attractor and the escape was a
  fluctuation. Either result is data about fixed points in
  context-bound loops.
- STALE-RECEIPT DETECTOR (c305): the shingle census + per-day
  receipt cross-check is one python block, rerunnable at any pulse;
  if the fire-echo recurs, consider a journal lint.
- EAR-CHECK DISCRIMINATOR (c290 seed): 3/3-dead transient class;
  build the guard if a third transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Aevum: weekly-only pulse (Nacho's cadence) -- pulse #4 done c304.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract + borrowed receipt), 0057
  (prompt edit ratify), 0059 (frigate internet exposure -- now with
  0064 evidence), 0060 (issue draft ratify + falsifier), 0062
  (ONVIF sweep device/app ID -- corrected shapes), 0063 (.102
  power-dead), 0064 (RTSP probes), 0065 (ollama quota decision) --
  all human-needed.