# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~12:21 UTC (cycle 125)

## NOW

- c125: EYE BATTERY V1 RUN (relay aria-0019 items a+b): qwen3.6
  BEATS gemma3:4b on JOB A (screenshot description -- correct brand
  reads, no invented defects), TIE on JOB B (camera classification
  2/2 both). Co-residency IMPOSSIBLE (qwen load evicts gemma3 --
  ollama ps post-battery: only qwen, 4312 MB). Swap costs priced:
  6 scripts / 8 model-string sites, eye-check max-time 45->300s,
  fear/rage mouth latency 6s->45s warm. Decision stays Nacho's
  (aria-0021 filed with data). n=2 -- a line, not a mechanism.
- SESSION X (interactive, 10:30-11:25Z): DASHBOARD V2 LANDED
  (D-013): generator v2.0 connectome data layer (42dae75) + UI v2
  force-graph (d05cf78) + v2.1 fixes (41fff7b) + docs. Live on
  aria.randazzo.ar. Eye-check canvas-blindness documented (ecddab8).
- c124: D-014 plumbing fix LANDED+VERIFIED (7d796b9): turn 541
  (10:51Z) first clean nemotron-3-super:cloud cycle (3x 200 OK).
  Fire watch re-armed from 10:51Z, verdict 09-11.
- c122: EYE-CHECK SCHEDULING LANDED (aria-0017): eye-feed timer
  daily 09:30 -03, REPORT.md contract + ledger. c120 audit PAID.
- c121: FEAR-FLEET WIRING (aria-0015): fleet-feed 6h timer, first
  scheduled fire verified 06:00 09-09.

## NEXT (cycle work, in pull order)

0. TIMER-STALL WATCH (c125, URGENT-when-observed): aria-cycle.timer
   next_elapse was STUCK IN PAST (08:40 -03) during the c125 cycle
   (60-min wall vs 10-min rotation). On service completion, check
   `systemctl show aria-cycle.timer | grep TimersCalendar`:
   recompute-from-NOW = benign (close watch); still-past/infinity =
   REAL BUG (all cycles stop) -> telegram Nacho + restart-timer
   workaround. Doc: knowledge/aria/timer-stall-investigation-
   2026-09-09.md. Also: continuo 08:32 run TIMED OUT (1800s, 79
   calls, exit 1) -- verify her next run gets the 3600s wall.
1. Fire watch: continuo cycles running nemotron-3-super:cloud
   (turn 541+ clean). Census fires through 09-11, verdict on the
   flip (context-shape vs model).
2. Fleet-feed watch: 12:00 -03 fire verification (standing
   patrol, watch closes after).
3. Eye swap session prep: battery is DONE; remaining item is the
   blinded-judge pass (optional, design in eye-battery-v2.sh
   header) + qwen warm-ttft measurement. Awaits Nacho's D-decision
   on aria-0021/0019.
4. Twin-verifier PERS fix (sophon-side, c124 diagnosis): script
   hardcodes /root/personalization but runs as nacho via sudo -u;
   /root not traversable. Fix: PERS resolves to the checkout the
   script lives in. Needs sophon checkout edit -- infra-adjacent,
   file via relay or do in-session.
5. Drills: #004 delegate only on natural need.
6. gptel PRs: pending Nacho's answer on aria-0013.
7. Retainer build: spawn-file-spec drafted; runtime parsing =
   interactive-session work (D-005). Interlocutor = gemma4:cloud
   (D-014; spawn file updated, runtime pending).
8. Failure-window watch: gpu-load-probe windows live. Aevum
   pulse #4: Sep 14.
9. Laptop origin artifact review with Nacho: pending his
   availability; salvage BEFORE reinstall/scrub.
10. W38 connectome snapshot: next week -- first trend comparison
    against W37 baseline (caveat: ~24h continuo hole from the
    404 window).

## STANDING

- Pulse: all green as of c125 (services 4/4, tripwire 0, disk 29%,
  heartbeat leid=932, keyscan reseeded, twin FAIL=0).
- Relay open queue (Nacho acks whenever): aria-0013 (gptel PRs),
  aria-0017 (eye-check install note), aria-0018 (D-014 landed note
  -- superseded by 0020), aria-0019 (qwen-as-eye consideration --
  DATA NOW LANDED via 0021), aria-0020 (D-014 plumbing failure
  note), aria-0021 (eye battery results). 0002/0003/0007 ratified.
- sophon-bare i.ar.git at 5ab51ff. Container checkout at 9d82cfa
  (42 ahead of stale origin refs -- origin fetch fails from
  container, never compute divergence against origin refs; use
  sophon-bare explicit main).
- 14 of 20 sophon repos have DANGLING HEAD (HEAD->master, only
  main exists). Use explicit refs.
- Aevum: weekly-only, pulse #4 Sep 14.
- BIKE LEDGER OPEN: KLR650 presumptive, license upgrade next week,
  test-drives primary. GO2 ~2 weeks out.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
    c111: mktime takes EXACTLY six fields. c120: when two of your
    own instruments disagree, first check whether they speak the
    same timezone.
12. Anchor greps on line START, never on substrings.
13. Verify a "pending" roadmap item against the primary source
    before working it -- roadmap lines go stale.
14. Apparent size lies on sparse files; read the blocks (c99).
15. An instrument that cannot say who it was when it measured lies
    by omission -- ran-as + input-branch context (c102).
16. An instrument that fails for the wrong reason produces a verdict
    that is wrong with confidence -- validate on a control that CAN
    pass (c104). Probe must be IN RANGE (c109).
17. Enumeration is what an under-stimulated instrument does (c105).
18. Commit-derived activity is a lying instrument for cycle health
    (c105). Wrapper-exit is not cycle-health (c105).
19. Waiting for a state change is not work (c106).
20. A test suite that has not run is a press release (c107).
21. A model that cannot say "I cannot see that" is a perception
    hazard (c109).
22. Workarounds generalize past their warrant (c110). Sticky
    tracked-ness is state nobody owned (c113).
23. A time-delta instrument must be validated against a KNOWN delta
    (c111).
24. An instrument that exists only as per-cycle improvisation
    re-derives its methodology every run (c114). If a census runs
    more than twice by hand, it wants to be a script.
25. A per-agent file has no agent column; the partition is the path
    (c115). Validate against a control that MUST match.
26. A scar whose evidence base has rotated out must be re-verified
    before it is trusted again (c118). A plausible scar is a
    hypothesis, not a fact.
27. An instrument that exists but is never scheduled is a
    fossil-in-waiting (c118). Wiring a verifier into a standing
    routine is part of building it. c120: the wiring decision is
    PER INSTRUMENT. c122: the audit is paid when the LAST family
    member has a schedule.
28. Recovery/healing events with NO journal entries at the boundary
    are camera-side (c118).
29. c120: an organ's installed config is its true spec -- read the
    systemd unit, not the script's usage docs.
30. c121: an instrument built to feed another instrument must be
    live-fired against BOTH its green and its failure path before
    the first scheduled fire.
31. c122: write references only after reading the world they point
    into -- the relay id I assumed (0016) was taken; the tool
    assigns, the record follows. Same shape as the fear-organ's
    "" argument: a reference written before the world was read.
32. c123: a model allowlist is a config that must change WITH the
    model mapping -- rotate.sh and gptel.el are one system patched
    separately; D-014 landed one of two halves. A guard whose
    comment says fail-fast but whose body silently swaps is the
    error-handler-as-accomplice class wearing a config's clothes.
33. c125: the eye's instruments live on sophon (firefox, ffmpeg,
    magick); the aria container is a text machine. A battery that
    needs eyes fetches inputs from sophon and judges in the
    container. Boundary-shaped instruments, not boundary-denying
    ones.

## SESSION IX ADDENDUM (2026-09-09 ~09:28 UTC)

- D-014 LANDED (interactive, Nacho): continuo -> nemotron-3-super:
  cloud (rotate.sh 09:24Z, backup .bak-20260909); interlocutor
  retainer -> gemma4:cloud (spawn file + registry + allowlist);
  qwen3.6 = local resident FINALIZED; aria unchanged.
- FIRE WATCH (48h, verdict by 09-11): census continuo fires post-flip
  with the strict-EOL instrument (knowledge/aria/bin/tool-census-v3).
  Re-armed from first verified fire (turn 541, 10:51Z).
- RELAY TOOL BUG (2 same-day collisions live): find_req matches first
  hit with no uniqueness check. Fix: die on ambiguous id. Cycle-sized.
- qwen residency forensics (09:45Z): evicted 06:11 by gpt-oss load;
  keep_alive=-1 is not a pin (LRU eviction on VRAM pressure).
  c125: qwen now resident again (4312 MB) post-battery -- live test
  subject for the keep-warm question.