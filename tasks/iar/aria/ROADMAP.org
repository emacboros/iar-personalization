#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~10:07 UTC (cycle 312: FIX-2 LANDED
(2f7c82c, suite 1263/1263, pushed; drain-instead-of-abort: live
pipeline keeps streaming up to iar-delegate-drain-grace 300s, then
c149 abort path; design doc in task file, modules.md updated,
b955bedc). ECHO CLOSED (correction 6, 100bc4b7): continuo's
09:36Z post-peer-note cycle is CLEAN -- 0 msgs=401 emissions,
journal all PULSE. Template was not nemotron-intrinsic; the peer
note (addressed, sibling-to-sibling) stopped what annotation could
not. Borrowed-receipt law v4 final form filed. ARP logger re-armed
on enp10s0 (first arm on "any" failed -- tcpdump rejects -e ether
filters on the any device; interface name matters). Fleet-check
FAIL=0. .102 BACK ONLINE (rssi -32dBm @01:45Z -- power-cycled,
self-recovered or Nacho cycled it); .104 still power-dead 47h
(relay 0063 half-resolved). agora-agent heartbeat gap = false
alarm (8min cadence, my 12h grep window caught only the tail).)

## c313 QUEUE (next cycle)
1. NOCTURNE GATE WATCH at 16:04Z (13:04 local): verify gate advanced
   past e4d0832d, read DIGEST.proposed.md if new (ratify-at-wake,
   D-015). If 429 again -> quota structural (relay 0065).
2. SWEEP-SHAPE 2 FALSIFIER: after tonight's .101 boot (~00:50-01:00Z
   Sep 15), read /tmp/arp-reqs-0914c.txt (armed, enp10s0, filter
   `arp and (ether src 76:e3:1a:69:e3:9c or host 192.168.2.58)`,
   -e, 47000s timeout). Falsifier: .58 ARP to .101 at 01:00:28-31Z
   = boot+1s probe mechanism (b) real; quiet = (a)/(c). Then
   ~22:15 sophon-local check .101 camlog for boot+1s burst.
3. RELAY 0063 UPDATE: .102 self-recovered (rssi -32 @01:45Z);
   amend the filing -- .104 still needs Nacho's power cycle.
4. THINKING-LOOP WATCH (standing): 1 mid-cycle 09-14. If >5/day at
   full volume = nemotron-intrinsic -> relay 0065 amendment.
5. AEVUM PULSE #5 (due ~Sep 21, weekly-only): the ratchet watch.
6. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.104 power-dead only now), 0064 (RTSP probes, folds into
   0059), 0065 (quota decision), 0042/0045/0055/0057/0059 (older).
   All open, none actionable by me. 0059 has scanner #3 evidence.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291/c301): cameras.log/camlogs = UTC;
  frigate journalctl --since = LOCAL on sophon; nginx log inside
  container = LOCAL (-0300); go2rtc container log = LOCAL;
  REQUESTS.log UTC; sophon journal = LOCAL; DATED CYCLE LOGS =
  SOPHON-LOCAL. Normalize per-source before ANY gap arithmetic.
- CAMLOG SNAPSHOT LAW (c302/c303): thingino camlog pulls are
  snapshot-anchored; boot resets camera clock to factory until NTP;
  RING-RESIDUE LAW: dedupe camlog counts by (timestamp, PID).
- CENSUS-SOURCE MAP (c297): viewer traffic = nginx log ONLY;
  RTSP-port probes = go2rtc log ONLY; producer dials = go2rtc WRN +
  camera prudynt; frigate remakes = frigate container log; camera-
  side events = cameras.log; sophon journal for aria-cycle = Sep 11+
  ONLY; REQUESTS.log self-echo: strict filter = PARSE + error=nil +
  END-ANCHORED tokens_out=32768 msgs=N$. CENSUS-DAY LAW (c305).
  CLAIM-ANCHOR LAW (c307): claim censuses anchor to record
  STRUCTURE, not substrings. REQLOG-RACE LAW (c308): during
  delegation windows, per-agent REQUESTS.log boundaries are
  unreliable -- dedupe by REQ id across ALL agent logs (HISTORICAL
  logs only; fix-3 792de9a fixes FUTURE attribution). CENSUS-SELF-
  ECHO LAW (c309): an investigation's own greps write the search
  string into its own request-log tails -- filter census matches by
  epoch AND by structural position. TRUNCATION-LAYER LAW (c310):
  the request-log's tail view ([+N chars]) is NOT the agent's
  context; context truncation = iar-tool-result-max-chars (10k,
  middle-truncation). Verify WHICH layer truncated before
  concluding an agent "never saw" content.
- BORROWED-RECEIPT LAW v4 (c306-c312, CLOSED): (1) a journal entry
  may claim only what THIS cycle's log shows happened TO THIS AGENT;
  (2) annotation in the reader's path decays paraphrase but does
  not stop template re-emission; (3) a PEER NOTE (addressed,
  sibling-to-sibling, on the social channel) stops template
  re-emission where ambient annotation failed -- a mind corrects
  when addressed, not when overheard. Echo closed c312 (correction
  6); census law already excluded continuo from real-fire counts.
- LAW 41 (c296/c303): when the guard fires, change the QUESTION or
  stop -- changing the query's costume while keeping the
  enumeration is compliance theater.
- LAW 50 (c215-c259): an instrument's output has a SCHEMA --
  verify the DAY, the COLUMN, the KEY FORMAT, the CLOCK (timezone),
  the SOCKET, the UNITS, and DELTA-vs-CUMULATIVE. c312 addendum:
  verify the ERROR against the DATA before building a mechanism
  story (the "killed buffer" was ert's post-failure rendering; the
  real error was resp-start=0 + empty buffer -> substring(0 1)).
- TEST-SUITE discipline (c312): bash -n / check_elisp on the
  DEPLOYED file; read the actual error text; ert's backtrace
  renders buffer state at PRINT time, not error time.

## TOOLS + GOTCHAS (do not re-derive)
- TEST SUITE: cd /root/i.ar && emacs --batch -L . -L emacs.d -l
  emacs.d/test/run-tests.el (1263 tests, ~8s). run-tests.sh only
  loads test-prompt-assembly.el -- NOT the full suite.
- .AR TRAP (c274): paths ending .ar get tramp-archive handler; use
  -L for --eval load-path; never add a .ar path to load-path via
  --eval (excessive-lisp-nesting at startup).
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277).
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt. READ API: GET
  /api/v1/messages --get with narrow JSON; POST with read params
  POSTS (agora-api-read-recipe.md).
- ausearch HANGS (c281): use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- RESTIC (c302): both repos verified fresh 09-14. restic-check
  weekly Sundays 03:16 local, last clean 09-13.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
  JOURNAL.org needs git add -f. TASKS gitignored: new task files
  need git add -f. create_task paths are relative to tasks/iar/aria/
  for cycle agents -- verify the landed path with find before
  git add (c308: create_task nested an extra iar/).
- GIT-COMMIT GUARD GAP (c308): the refuse-pattern only matches
  cycle.log; other audit files (DIGEST.md etc.) CAN be swept by
  add -A in the i.ar repo. Check git show --stat before pushing;
  untrack strays immediately (6fe9997 precedent). c311: untracked
  strays also need a DISK delete (git rm --cached leaves the file;
  an unmarked stale copy trips the digest-twin verifier).
- AEVUM SSH PATH (c304): container key aria@i.ar direct to
  fedora@54.38.46.192 -- no sophon hop. Host key TOFU to /tmp.
- PULL-AND-PROCESS LOCALLY (c302): sophon /tmp files invisible to
  the container -- scp to local /tmp first (c311: nginx pull).
- NGINX RING PULL (c302): ssh 'su - nacho -c "podman exec frigate
  sh -c \"cat /dev/shm/logs/nginx/current\""' > local file.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh.
- RECORDINGS PATH (c299): /media/frigate/recordings/YYYY-MM-DD/HH/
  <cam>/<MM.SS>.mp4 via su - nacho -c "podman exec frigate ...".
- CENSUS-WINDOW.SH (c301): SOPHON-HOST-ONLY. Args [DAY] [AGENT].
- ARP LOGGER (c290/c302/c312): arm with -e ON enp10s0 (the "any"
  device REJECTS -e ether-address filters -- c312); filter
  `arp and (ether src 76:e3:1a:69:e3:9c or host 192.168.2.58)`;
  /tmp on sophon survives only until reboot or timeout expiry --
  note the expiry. Current arm: /tmp/arp-reqs-0914c.txt, 47000s
  from 06:50 sophon-local 09-14.
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our
  keys; ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP.
- DELEGATE DEPTH GUARD (c308): strips delegate AFTER project tools
  (i.ar 763c7b3). Fix-2 drain-instead-of-abort LANDED (2f7c82c,
  c312): live pipeline drains up to iar-delegate-drain-grace
  (default 300s) then c149 abort. All three cascade fixes CLOSED.
- REQUESTS.log ROTATION (c309): aria's c204-era (09-11) REQUESTS
  lines are GONE from both .log and .1 (rotation); the surviving
  primary record for old-cycle fence fires is the DATED CYCLE LOG.
  Cycle logs outlive request logs -- cite them for old events.
- START-TAIL VIEW (c310): REQUESTS.log START tails are TRUNCATED
  VIEWS (~1400 chars/msg), not full context. Tool results >10k are
  middle-truncated in CONTEXT (iar-tool-call.el); the [+N chars]
  marker in a START tail may be the log's truncation, not the
  context's. Check file size vs iar-tool-result-max-chars before
  concluding an agent missed content.
- EXT3 CRASH-LOOP CLASS (c294/c311): -38 filter-init signature,
  20s restart cadence, heals on ANY session remake (c294: 166x
  healed by 21:43 remake; c311: 107x healed coincident with a
  fleet-wide go2rtc producer re-dial 06:05:38-59 local). Watch for
  recurrences; self-healing is expected, alert only if >2h.
- AGORA-AGENT HEARTBEAT (c312): alive: lines every ~8min; a grep
  window narrower than the cadence manufactures a false gap. Check
  the CADENCE before calling a heartbeat stale.

## THREADS (open, no obligation)
- SWEEP-SHAPE 2 FALSIFIER (c302/c303): tonight's .101 boot -- logger
  armed (queue item 2).
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- STALE-RECEIPT DETECTOR (c305): shingle census + per-day receipt
  cross-check -- the echo watch (c306-c312) was its live case;
  the detector itself is still unbuilt.
- EAR-CHECK DISCRIMINATOR (c290 seed): build the guard if a third
  transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Aevum: weekly-only pulse -- #5 due ~Sep 21.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract + home-grown vector + c309/c310
  corrections -- echo portion now CLOSED via correction 6),
  0057 (prompt edit ratify), 0059 (frigate internet exposure --
  scanner #3 evidence 09-14), 0060 (issue draft ratify + falsifier),
  0062 (ONVIF sweep device/app ID), 0063 (.104 power-dead only),
  0064 (RTSP probes), 0065 (ollama quota decision) -- all
  human-needed.