#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 09:26 UTC (cycle 14: sidecar fix-C verified live
(d768f37, continuo); bare-repo poison flagged (id 280); interior_1
no-audio first observation; guard fired 13x incl. on prescribed
tool-switches -- convergence reset now top interactive item.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 14: LAST-CYCLE ok. Pulse green. No cycle failures.
- LIVE EVENT (audio loss): two trigger paths, one endpoint (silent
  audio): (1) ext2/3/4 = VLAN RTSP outage (network trigger);
  (2) interior_3 = camera-side ffmpeg crash (no network signature).
- NEW WATCH (cycle 14): interior_1 NO-AUDIO, first observation,
  UNCLASSIFIED. Second data point next cycle decides: transient
  (hour-boundary reboot class) vs 5th deaf camera. If 5th deaf ->
  session-setup variant of thingino bug more likely -> restart-lever
  caveat sharpens to "restart likely harmful." Follow up in-topic
  (flag 280) either way.
- NEW FLAG (cycle 14, id 280): BARE-REPO POISON root-run variant --
  6 root-owned files in i.ar.git (packs + multi-pack-index +
  info/refs), written 09:18:54 UTC = continuo's push window for
  d768f37. Ref git-owned, packs root-owned (split ownership = the
  datum). My own push same morning: all git-owned (clean). Fix is
  Nacho's: chown the 6 files + push-path hygiene (git@ pushes, or
  auto-heal scope extension). Cycles don't chown /home/git.
- RESTART-LEVER CAVEAT (msg 276): frigate restart could return MORE
  deaf cams (session-setup variant). Firmware (flag 270) = real fix.
  Nacho's call; frigate NOT restarted.
- WATCH (every cycle): fleet-check ear lines. v2.10 semantics:
  "NO-AUDIO/SILENT (known-deaf, watch state)" = expected; RECOVERED
  (with real dB) = withdraw flags in-topic; NO-AUDIO/SILENT on other
  cam = new event (FAIL). If interior_1 confirmed deaf, add to
  KNOWN_DEAF + withdraw in-topic.

* DONE (cycle 14)
- Sidecar fix-C VERIFIED LIVE (continuo cycle 2, d768f37): preflight
  executable-find in execute_code_remote, honest diagnosis instead of
  generic re-signal. 3 new tests, 991/991. Docs entry d45e729
  (continuo's rebase+push pending). The end-to-end call the sidecar
  never got.
- Bare-repo poison root-run variant: detected (fleet-check), evidence
  gathered (3 sophon journal probes), flagged (id 280), thread filed
  (THREADS.org: who writes the packs; /home/git/repos outside
  tripwire sight).
- interior_1 no-audio: first observation logged, unclassified (two-
  point law), flag 280.
- Guard meta-finding (13 fires incl. on prescribed tool-switches):
  journal entry "the guard that ate its own lesson"; convergence
  reset = top interactive-bundle item (4th witness set).

* DONE (cycle 13)
- Research-sidecar wiring audit: the sidecar was NEVER reachable.
  Every execute_code_remote call ever failed (no podman client in
  Emacs container). Audit bridge logs callback-fired errors as
  status=success. Knowledge: research-sidecar-wiring.md (39164af).

* OPEN THREADS (seeds, not schedule)
- AUDIO LOSS WATCH (every cycle): v2.10 semantics above + interior_1
  classification (cycle 15).
- SIDECAR NERVE: fix-C LANDED (continuo). Remaining: socket bridge =
  real fix (security decision, with-nacho). Production-watch fix-C
  firing honestly next time a sidecar target is used.
- INTERACTIVE BUNDLE (for next session with Nacho): (1) loop-guard
  convergence reset -- 13-fire cycle 14, fires on prescribed
  tool-switches, 4 witness sets (aria 11-14, continuo 2+); (2) socket
  bridge security decision; (3) chcon -R relabel fix (durable);
  (4) soft-warning cap (continuo's); (5) digest-pressure REPLACE-
  not-append. Bundle grows one line per witness event.
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (blocked on key).
- Weekly digest #1 to with-nacho: ~Sep 9 with Aevum check.
- THREADS.org seeds: stub-detector sweep; watchdog inventory; bare-
  repo pack-writer question (if poison recurs).
- Camera API creds: version-confirm + NTP-push threads both route
  through camera auth. One for-nacho ask covers both (flag 270).
- agora-mind architecture: DESIGN COMPLETE (08eda99). Build deferred
  until explicit go. REMIND NACHO NEXT SESSION: define agora changes
  to support organ layer.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Msg 244 ACKed (id 256). No open questions.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.10: RUN ON SOPHON (ssh 'bash -s' <, timeout >=300s).
  Script lives at knowledge/aria/bin/fleet-check.sh (NOT audit/).
  A local run manufactures false alarms (cycle 7 lesson).
- nacho@ ssh DENIED (publickey) from this container; root@ leg
  works. If root leg also dies, file for-nacho flag.
- RESTIC: verified cycle 135. Integrity check Sun Sep 6.
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. EXTENDED (cycle 8): remote probes that
  fail once become script files -- third attempt is where the guard
  lives, and the guard is right. EXTENDED (cycle 9): the guard also
  fires on REMOTE-API enumeration chains -- batch the listing into
  one call before the third fetch. SUPERSEDED CAVEAT (cycle 14):
  switching tools does NOT escape the guard -- the counter counts
  the tool NAME across switches; when blocked mid-diagnosis, batch
  everything remaining into ONE call or park the question and file
  it. Do not retry the same call shape; do not expect the switch
  itself to reset anything.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Commit authorship is not evidence of authorship (shared tree).
- A fix is not PINNED until a test fails when the fix is removed.
- Reviewers can be right about the bug and wrong about the mechanism.
- An instrument's output is evidence only when it ran where it was
  designed to run (cycle 7).
- Correcting your own flag in-topic is cheap; confidently wrong
  advice in someone's action queue is not (cycle 7).
- WATCH-STATE LAW (cycle 10): a watch state belongs inside the
  instrument, not in the reader's head. Alarm fatigue kills the
  exit code's meaning; encode known-bad into the tool.
- UNHANDLED-OUTPUT LAW (cycle 11): an instrument's unhandled output
  shape is a hole in its exit code, not a curiosity. The wrinkle
  was the finding -- patch same-cycle.
- TWO-POINT LAW (cycle 12): two data points make a line, never a
  mechanism. An interval story built from two events is a
  coincidence hypothesis -- check for a shared trigger before
  shipping the narrative.
- COMPONENT-VERIFIED IS NOT SYSTEM-VERIFIED (cycle 13): a build
  verified at every component level can be dead at the system
  level. One end-to-end call on day one is cheaper than four days
  of a healthy organ with a cut nerve.
- GUARD-ESCAPE LAW (cycle 14): the loop guard counts tool NAMES,
  not questions; convergence is invisible to it. The escape is not
  switching tools (it follows you) -- it is batching the remaining
  work into one call, or parking the question with a written trail
  (flag/thread) and moving on. A blocked probe costs a turn; a
  retried probe costs two.
- No infra changes from cycles; diagnose, file, move on.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands (raw [ ] { } in query = empty body).
- DIGEST LAW (cycle 4): world-state blocks are REPLACED, never
  appended. Check the file size before and after any DIGEST edit.

* FAILURE MODES (scars, newest first)
32. The escape hatch can route back into the trap. The cycle-12
    caveat said "switch tools when blocked"; the guard counted the
    switches too (13 fires, cycle 14). A rule about escaping a
    guard must be tested against the guard's actual counter, not
    the guard's intended purpose (cycle 14).
31. Component-verified is not system-verified. The sidecar was
    verified at every component level and never end-to-end; the one
    missing call was the only one that mattered (cycle 13).
30. Two data points make a line, never a mechanism. Check for a
    shared trigger before shipping an interval story (cycle 12).
29. An instrument's unhandled output shape is a hole in its exit
    code, not a curiosity. Patch same-cycle (cycle 11).
28. When three records disagree about an instrument's version,
    the instrument's own header is the one that lies least -- but
    patch it first, then trust it (cycle 10).
27. An unauthenticated API that redirects every path to / is not
    "no API" -- it is auth-walled or wrong-version (cycle 9).
26. When a remote probe needs more than one attempt, promote it
    to a script file immediately (cycle 8).
25. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT (cycle 11).
24. A diet without a pressure instrument regrows.
23. The law you just wrote is the one you're about to break.
22. A stable transcript means a stable WRITER or a DEAD one.
21. Rootless podman needs user@1000.service + linger.
20. Watchdog tuned for fast models kills slow ones.
19. podman run -d inside systemd: restart-loop before first breath.
18. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
17. A fix that works in isolation can stall in the live system.
16. Root-run git on nacho-owned repos poisons the tripwire.
15. Curiosity that doesn't check priors against the human runs blind.
14. Instruments have reliability tiers per sub-reading.
13. Response without local service logging it -> second stack, re-probe.
12. Artifact confabulation: external-action entries cite tool evidence.
11. In async plumbing, capture context when it exists.
10. Anomalous behavior in multi-agent env has a third cause.
9. Before diagnosing the listener, check who the speaker is.
8. Differential testing: same input through old and new code.
7. The venv python must be passed as SYMLINK, not resolved path.
6. Existence is not function: test the claim.
5. pkill -f matches my own ssh command line; use -u <user> -f.
4. Malformed tool calls are invisible from inside.
3. Timeout wrappers on EVERY remote/ssh command.
2. Silent error swallowing; condition-case in parse loops.
1. Narrative completion: check primary evidence before attributing.