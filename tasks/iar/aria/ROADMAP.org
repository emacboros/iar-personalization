#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-10 ~22:18 UTC (cycle 167)

## NOW

- c167: ARIA-0030 CORRECTION -- c166 was DEAD CODE (082f12f, suite
  1206/1206): the post-response handler NEVER runs for tool-call
  responses (FSM: WAIT->TOOL->TRET->WAIT; DONE = text-only only).
  continuo's 21:36 cycle proved it live: 7 echoes, zero echo-close
  messages, ended via thinking-only truncation guard. REAL FIX:
  iar--cycle-terminal-echo-close PRE-TOOL-CALL hook (echo command
  shape + iar--cycle-terminal-echo-p over FSM response region) ->
  completed, exit 0/2, block. c166 branch = documented DONE-path
  belt. Pushed sophon-bare+origin; sophon checkout at 082f12f LIVE.
  Relay 0030 amended with CORRECTION section. LAW 40 (candidate):
  deployment is not activation -- reachability is part of
  correctness; test the fix's own failure PATH, not just its parts.
  WATCH NEXT CYCLE: continuo's close should register on the FIRST
  echo ("Terminal sentinel echo (pre-tool-call, CYCLE)" in the
  service log, exit 0).
- c165: design grounded (journal c165) -- superseded by c166 build.
- c164: GIT SERVER CENSUS: (1) 50-dangling-objects RESOLVED (fsck
  clean -- digest line dead, do not re-file); (2) git-server.md
  dangling-HEAD census predicate WRONG (TRUE dangling=0; recipe
  manufactured the fault it claimed to detect -- fixed in doc
  addendum); (3) mirror health VERIFIED 5/5 sophon==rammstein;
  (4) stale packed ref 2b891950 = harmless ancestor hygiene;
  (5) NEW POISON VECTOR: root-run fetch/rev-list on sophon clones
  rewrote 2 .git/index as root -- caught by own tripwire, healed;
  git-trust-graph rule 3 amended (poison class = ANY root-run git
  in clone tree). Knowledge: git-server-census-2026-09-10.md.
- c162: REQUESTS.LOG ROTATION-LOSS ARTIFACT FOUND + FIXED: snapshot's
  silence column was FICTION (one generation kept). connectome-snapshot.sh
  v2 (52cfa439): REQ census from cycle.log (line-start anchored) with
  REQUESTS.log(.1) fallback. Knowledge: requests-log-rotation-loss-2026-09-10.md.
- c161: EXT2-DEAD-CAMERA TASK RESOLVED-STALE (camera alive at
  192.168.2.102; task cited old subnet). LAW 35 for tasks. Storm
  watch: 2 network-wide storms at round hours (00:xx, 08:00 -03);
  third storm -> file the router-reboot relay question with timestamps.
- c160: ORACLE TRAFFIC CENSUS: ~300/hr granite chat POSTs = oracle
  /chat calls from Nacho devices (house IP + CF WARP); mouth used in
  scripted cadence -- relay aria-0031 filed. Knowledge:
  oracle-traffic-census-2026-09-10.md.

## WATCH (standing)

- RAGE DECAY: 2 fence events 09-10 (vs 10-12/day 09-09); drop
  prediction 09-12 stands -- VERIFY 09-12.
- EXT1 POISON: staircase still climbing (309394s, aria-0028 reboot
  pending with Nacho). Post-reboot check falsifiable: durations
  reset to ~16s = thingino clock source fault confirmed.
- QWEN RESIDENCY (session XIII): keep_alive -1 + num_ctx 131072 live;
  first scheduled caller on new config = eye-feed 09:30 -03 09-11.
  If frigate watchdog restarts reappear: first suspect = LARGE prefill
  colliding with detect pipeline; re-price before touching residency.
- FIRE WATCH: first eye-feed run 09-11 = verdict day (Nacho's
  instrument-finding: screenshot timing misses canvas).
- aria-0028/0029/0030/0031 await Nacho (0030 now carries a working
  patch to ratify).
- Burn asymmetry aria 258M vs continuo 44M (5.9x) = composition-review
  material for interactive session.

## LAWS (the scar ledger -- numbered, oldest first)

1. Existence is not function. Test the claim.
2. Every failure worth fixing produces silence.
3. Error handlers can be accomplices -- check what the handler does
   AFTER catching.
4. Capture context when it exists; don't assume it survives.
5. Verify against primary evidence before attributing action.
6. In async plumbing, nothing survives unless written down.
7. A guard that pattern-matches on a word in its own instructions
   has already surrendered.
8. The law you just wrote is the one you're about to break.
   c152 sighting: wrote the empty-end guard, then looped on a
   completed push-check myself. c154 sighting: after naming the
   pattern ("fences stopped it at 8 calls"), burned 6 calls on
   ssh-grep variations. c157 sighting: 28 min + ~100 calls on git
   forensics before the dump-once shape; the census was 4 commands.
   c159 sighting: fence fired at 100 again; the census was 4
   commands once anchored. Phrase the FIRST question as one command.
9. Before diagnosing a failure, check whether you have the latest world.
10. The condition-case that keeps things running keeps bugs invisible.
11. c115 partition law: container boundary = instrument boundary.
12. c117 tail-anchoring wall: anchors or lies.
13. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
14. c121 live-fire both paths before first scheduled fire.
15. c122 write references only after reading the world they point into.
    c151: resolve the path the RUNNER uses, not the assumed path.
16. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
17. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
18. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
19. c136 when an alarm and the primary log disagree, audit what the
    alarm READS.
20. c137 a mechanism assembled from plausible parts is a story, not a
    finding. Test the fix's own failure path before shipping it.
21. c138 before declaring an instrument stale, check which clock it reads.
22. c140 an audit log is primary evidence only if its ATTRIBUTION
    survives async boundaries.
23. c145 one awk pass: a census over a multi-day log is ONE command
    with a window filter, not a walk.
24. c147 a platform can ship with an instrument deliberately disabled:
    when an instrument produces silence, ask "did the platform
    configure it away".
25. c143 identity travels with the REQUEST, not the buffer current at
    completion; restore parent defaults BEFORE the callback.
26. c145 a fix-log entry is a CLAIM ABOUT A WINDOW, not a standing fact.
27. c148 every async completion contract needs its ABORT case tested.
28. c148 a relabel that sets only the type preserves the poison:
    heal with the FULL SELinux context or not at all.
29. c151 instruments that decode content survive lying metadata;
    instruments that read metadata inherit the lie.
30. c152 an unanchored grep over a log that contains your own queries
    counts your own queries as findings. Anchor on the line format.
    c157 sighting: 59 "fires" -> 8 with strict-EOL + tokens_out anchor.
31. c153 a fence census spans CLOCK ZONES: journald speaks local (-03),
    REQUESTS.log speaks UTC, day-files speak file-day. Reconcile the
    clocks BEFORE comparing counts.
32. c154 a signal is not delivered until the RECEIVER's transport
    confirms it: check the server-side log (401s) before concluding
    "read and ignored". c155 AMENDMENT: server-side 401s can belong to
    ANOTHER instrument (the dashboard probe). Pair the server log with
    the actor's own log before attributing (law 34).
33. c155 an attribution needs the ACTOR's log, not just the ACT's log.
    Server-side evidence + client-side silence = the act belongs to
    someone else. Two instruments, one IP, one window -- read both.
34. c156 INJECTION-BEATS-READ: a correction delivered in a read-once
    file loses to a stale premise carried in the injected layer
    (digest/journal). A correction that must outrun an injection must
    land in the injection layer -- or change the injection.
35. c156 an organ has a CANONICAL RUNNER: running a host-bound organ
    from a container tests the code but CORRUPTS the shared state it
    writes. Test organs where they run; restore state if you must
    test elsewhere.
36. c157 the shared checkout is a MAPPED VIEW: host-root files are
    nobody/unwritable from the cycle container; heal ownership with
    the FULL context (chown + chcon) from the host side. The
    ExecStartPre heal is the canonical fixer.
37. c159 a close signal is only real in the channel the RECEIVER
    reads: the model echoed the sentinel through a tool call, the FSM
    listened in model text -- 12 turns of ceremony between "done" and
    "close registered". Speak the listener's language, or make the
    listener speak the speaker's. (c166: FIXED -- the listener now
    reads the tool channel too.)
38. c164 the poison class is not a COMMAND, it is a SIDE EFFECT: any
    root-run git in the clone tree poisons (fetch/rev-list write the
    index). The DENIED edge protects in both directions.
39. c166 a test fixture must match the PRODUCTION SHAPE, not the
    convenient shape: the reqlog publishes a list of plists; a
    list-of-lists fixture fails honestly (the suite caught the
    fixture's bug, not the code's).