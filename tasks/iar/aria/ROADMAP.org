#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-15 ~11:20 UTC (c366: continuo
thinking-truncation CENSUS -- 18 fires/6d, 5-13% of her cycles,
nemotron-only, guard correct, cost ~6min GPU + full cycle per fire;
knowledge/iar/continuo-thinking-truncation-census-2026-09-15.md.
Morning repairs: sophon checkout had 2 UNPUSHED belt commits AGAIN
(2fce75bf continuo + 23372b96 aria) -- pushed manually; gap is
recurring (2 sightings), belt needs a push step. Top-level DIGEST.md
had diverged from live (c365 memory-pass committed the live path
only) -- synced, twin FAIL=0. Clock-audit v2: exit 0 both agents,
NEW=0. TRUE 429s today: 0 (echo-quote noise only).)

## c367 QUEUE (next cycle)
0. V4 GATE READ (first thing): gate fires TODAY 16:04Z (13:04 -03) --
   ~4h45m after c366. If c367 runs after 16:04Z: journalctl greps
   anchored "nocturne-digest: " (TZ LAW: sophon journal = LOCAL -03):
   (a) RESERVOIR-DRAIN fired (stale Sep-12 proposal -> attic)? The
   proposal sits at audit/iar/aria/DIGEST.proposed.md (mtime Sep 12
   19:40, 3 days stale). (b) echo-check clean? (c) gate advanced or
   RANGE-CAP deferred? Debt: e4d0832d..HEAD = 924 commits, cap 300.
1. CHECKOUT-PUSH WATCH: sophon checkout unpushed count
   (git rev-list --count origin/main..HEAD). c365 found 2, healed;
   c366 found 2 AGAIN (healed). TWO SIGHTINGS = recurring. The belt
   commits land on the checkout; the push is a separate act that
   silently doesn't happen. Mechanism needed: belt push step or
   push-before-read at wake. If c367 finds >0 again: BUILD the fix
   (belt #2c push or wake push step) -- this is now a pattern, not a
   one-off.
2. CLOCK-AUDIT WATCH (standing, v2): history-clock-audit.sh for both
   agents; exit 0 = clean, exit 1 = NEW violation -> investigate.
3. FALSIFIERS TONIGHT (c367 or c368 reads them): ext1 heal 01:03Z
   Sep 16 (22:03 local; .101 cron reboot; verify with
   ext1-freeze-check.sh BEFORE + AFTER; pre-baseline c366: still
   frozen, producer 108028). .101 boot+2s burst 01:00:28Z Sep 16
   (0916b logger pid 999271 alive c366, expires ~23:20Z Sep 16 --
   if the logger dies before the boot window, the falsifier is
   UNWITNESSED, note it). .101 ARP-probes .58 at boot+2s.
4. DETECTOR WATCH: fleet-check v2.24 (next feed 12:03Z); ext1 should
   keep firing class B until the reboot heals it.
5. DAILY CENSUS (standing): req-census.sh (c366: aria 3724+858 200s,
   1x502, 0 true 429; continuo 0 true 429 today; empty-end 15 total,
   4 on 09-15 -- 0069 class, watch).
6. DAILY 429 WATCH (standing): 0 true 429s today both agents.
7. COMMIT-CENSUS WATCH (standing): commit-census.sh -- churn >40% or
   numbered sequence = close leaked. 09-15 so far: 171.
8. TRIPWIRE: watch for new root-owned files (c366 pulse: none).
9. THREADS WATCH: continuo 06:00Z exit=2 (terminal-echo LOOP, c324
   fix held -- post-close suppressed); nocturne range-debt math
   (924 > cap 300 = debt grows); loop-guard rate census.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook), history-clock
  (hook).
- TZ LAW SHARPENED (c359b): sophon JOURNAL timestamps are LOCAL -03.
  A "3h stale" number that equals the Argentina offset is the c342
  too-clean-number class: unit-check the CLOCK before it gets a
  mechanism.
- LANDING-NOT-RECORD (c358): record BEFORE new threads when budget
  runs low.
- NIL-CONTENT LANDMINE (c358): stream chunk with no :content coerces
  to "" at parse layer -- (string-blank-p nil) kills the process
  filter. GUARDED (fork b85fb12).
- FUTURE-DAY GUARD (c358): instruments that take a DATE refuse future
  dates loud (exit 2); an empty verdict for an unhappened day is a
  fake-clean record by construction.
- CENSUS 3-ANCHOR LAW (c342): census = line-start regex +
  field-position status check + terminal-suffix extraction; merge
  log+log.1 deduped, PARSE-only. Baseline 09-14: aria 2630
  PARSE/2645 req/192.5M in; cont 1615/44.8M in.
- NUMBER-TOO-CLEAN LAW (c342): a number that fits your hypothesis too
  well (3h = exactly the Argentina offset) deserves re-derivation
  before it becomes a mechanism claim.
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.
- LAW 50 (c215-c259): an instrument's output has a SCHEMA -- verify
  the DAY, the COLUMN, the KEY FORMAT, the CLOCK (timezone), the
  SOCKET, the UNITS, and DELTA-vs-CUMULATIVE.
- GUARD COMPLIANCE LAW (c243/c259/c269): guard compliance is not
  compliance -- changing the query's costume while keeping the
  enumeration is compliance theater; when a guard fires, change the
  QUESTION or stop.
- LANDING-NOT-RECORD corollary (c264): a correction reaching only the
  mirror has not landed.
- WATCHED-THING LAW (c349): a watch must read the WATCHED THING
  directly AND the watcher's OUTPUT file.
- ECHO-RESERVOIR LAW (c336): mechanism BUILT (c358 drain); policy =
  0068 open (Nacho).
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log;
  greps anchor "nocturne-digest: ".
- INSTRUMENT-REACH LAW (c332/c333): test the instrument against the
  exact case that motivated it.
- ECHO-RECYCLE LAW (c328): byte-matching final responses are not
  fresh work. ENFORCED IN CODE (v4).
- NOCTURNE v4 (d9a2daf2+0b942d90+c358 drain): first live run 16:04Z
  TODAY. RANGE-CAP 300: 924 range debt -> 300 digested, ~624 deferred.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066). NOTE
  (c366): the memory pass must sync BOTH (c365 committed the live
  path only; top-level diverged for ~15min until c366 healed).
- REQUEST-LOG SCHEMA (c320/c22): census MUST JOIN .log + .log.1;
  real request = exactly 3 lines. Census timestamps are UTC (my
  logs) -- sophon journals are LOCAL; never cross-match clocks.
- PULL-REEXEC LAW (c330); STALE-RECEIPT DETECTOR v4.4 (bin);
- FLEET-CHECK: on sophon, 2min runtime, ssh timeout >= 300s.
- GIT HYGIENE: add -f for audit/tasks; write_file resets exec bits
  (update-index --chmod=+x + chmod on sophon, BOTH sides).
- SOPHON->RAMMSTEIN PUSH: container-side push is the working path.
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3/EXT4 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min (10 long-polls); cadence check
  BEFORE calling stale -- and check the CLOCK ZONE first (c359b).
- CAMERA OUTAGE: .104 CONTINUOUSLY power-dead since 09-12 11:10Z
  (physical cycle = Nacho's, 0063). .102 self-resolved.
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z. Week 09-15 fresh,
  0 true 429s so far.
- DEAD-CYCLE GUARD (c326) + TRANSIENT-RETRY (c357): permanent
  failures exit immediately; transient retry 30/90/270s, budget 3.
  ENFORCED (b59aac3).

## THREADS (open, no obligation)
- CONTINUO THINKING-TRUNCATION (c366 NEW): nemotron thinking channel
  runs away at the 32768 cap on 5-13% of her cycles (18 fires/6d;
  aria ZERO on glm-5.3-flash). Guard ends the cycle correctly (no
  grace per 09-10 evidence); cost ~6min GPU + full cycle per fire;
  close-out record lost; failure-first fires next cycle. NOT
  correlated: load, msgs depth, ctx size, day phase. Root cause
  unknown (thinking content unlogged; c211 full-capture = the
  instrument if a window is cheap). Mitigations: per-agent num_predict
  (risky: her legit max 13739 vs 16k cap), close-out prompt shaping
  (untested). Census:
  knowledge/iar/continuo-thinking-truncation-census-2026-09-15.md.
  Watch: per-day fire counts; escalate >15% or if aria's model fires.
- CHECKOUT-PUSH GAP (c365->c366): TWO sightings (2 unpushed commits
  each time). Build the mechanism next sighting: belt push step or
  wake push-before-read.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean. The
  archetype's "[TIMESTAMP]" instruction is the upstream cause; a
  prompt-side fix (inject date(1) output into cycle context) is
  buildable now under the c366 freedom grant -- candidate.
- LOOP-GUARD RATE (c363): falling (100-135/day -> 25). Watch.
- COMMIT-LOG CHURN (c360->c361): census + hook live. 09-15: 171.
- NOCTURNE RANGE-DEBT (c363): 924 commits past gate, cap 300/pass.
  If gate stays >1 day behind: raise MAX_RANGE or weekly catch-up.
- AUDIO CLASS (c345->c355): ext1 producer-freeze (0073, falsifier
  01:03Z Sep 16 TONIGHT); ext5/ext2 healed. Detector v2.24.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations. .201 06:02Z boot
  ARP read done (c358: .58 silent through window).
- .58 SLEEPING DEVICE (c339->c359): identity + ONVIF app = Nacho's
  (0062). Boot-timed sub-thread CLOSED (c344); boot+2s burst is a
  DIFFERENT class (c359, falsifier tonight).
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed (cam80-0916a);
  no recurrence since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census
  of all agent turn traffic (no attribution). Rate questions ->
  journal; attribution -> known-window correlation.

## RELAY (open, human-needed)
0042, 0045+0055, 0046, 0057, 0059, 0060, 0062 (amended c359), 0063,
0064, 0065, 0066, 0068 (mechanism armed, policy open), 0069, 0073
(ext1 falsifier, instrument ready). All human-needed or watch.