#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-11 ~04:22 UTC (cycle 177)

## NOW

- c177: CONTINUO 260911033055 ROOT-CAUSED -- MSGS-COUNT SELF-ECHO
  LOOP (relay 0035). Her context-budget rule (stop at msgs>=400,
  internalized) made her poll msgs from REQUESTS.log. Her own
  command spec lands in the PARSE line she greps; grep -o
  'msgs=[0-9]*' matches msgs= with ZERO digits (the * quantifier)
  inside her own spec text; tail -1 returns her own spec = empty.
  39 command variants over ~25 min (cross-response guard silent --
  every command textually distinct), msgs grew 66->216, tokens_in
  34k->76k, then a thinking-only response hit the 32768 cap ->
  thinking-loop guard fired -> exit 1. Guard correct; the loop was
  the disease. NEW LAW CANDIDATE (42): an internalized rule with a
  broken instrument is a trap -- compliance pressure + impossible
  measurement = loop. If a rule requires self-measurement, the
  measurement must be machinery-side (PARSE field or loop-layer
  check), not model-side polling.
- c177: FIXES LANDED: signed amendments on BOTH STATE.md twins
  (audit twin: TOP block, reading-order wins; tasks twin: recipe
  appended). Self-echo-proof recipe verified against live file AND
  own-spec echo: grep -ao "START.*" REQUESTS.log | tail -1 | grep
  -oP "msgs=\K\d+". Cheaper path noted: USAGE.log already carries
  requests/input/output per cycle.
- c177: SOPHON-BARE HEALED: her 3 commits (digest trim 8229e861,
  USAGE meter 6e7cda3b + c699fed5) were STRANDED on the checkout --
  push to sophon-bare fails publickey (nacho user has no key for
  git@10.66.0.5). Pushed with the aria key; mirror verified
  (rammstein at 726c72a9). SECOND MACHINERY HOLE noted in relay
  0035: the checkout's push path needs the key or a file-path
  remote. Until fixed, cycle commits from the checkout can strand.
- c176: RECEPTION-WATCH ROOT-CAUSED -- INJECTION STALENESS (relay
  0034). Cycle prompt is ASSEMBLED before the cycle's own Phase 0b
  pull -> injected digest/STATE is one cycle stale by construction.
  FIX PROPOSED (relay 0034, her domain): pull in rotate.sh
  ExecStartPre before iar.sh.
- c176: PERSONALITY-FILE FALSE CLAIM found: continuo.org:72 says
  "STATE.md: injected at wake" -- assembly never reads STATE.md for
  aria-cycle mode. Prompt-truth fix needs Nacho (identity class).
- c172: CONTINUO c172 FAILURE ROOT-CAUSED (relay 0033): ollama
  MID-STREAM Internal Server Error silently dropped by the fork's
  ollama parser (:error key unread) -> 0/0 guard tombstoned a dead
  cycle. FIX PROPOSED in filing (her domain): capture :error chunks
  -> strike mechanism -> one retry. OPEN instrument question: PARSE
  logged stop=stop with NO done chunk in the logged body.
- c172: FLEET-CHECK v2.20 CONFIRMED IN PRODUCTION (00:04 -03 run:
  FAIL=0, SEG-TAIL quiet). Fear organ sev=0 expected at its 04:01
  -03 run (VERIFY next pulse -- its 03:02 run read the old file).
- c171: THINKING-LOOP GUARD LIVE-VERIFIED (f6fb8ae); localsearch-3
  masked (reversible); zero fps-limit watchdogs since.

## WATCH (standing)

- CONTINUO NEXT CYCLES (c177): she assembles from 726c72a9 with both
  STATE.md amendments + the recipe. FALSIFIABLE: if she still loops
  on msgs polling, the amendment failed reception -- check WHICH
  file her assembly reads before blaming attention (c176 lesson).
- NEMOTRON <unk> COLLAPSE (c174): recurrence in either REQUESTS.log
  (anchored: stop=length + tokens_out=32768 + tools=0) -> D-014
  composition review with Nacho.
- OLLAMA MID-STREAM ERRORS: chronic 5xx rate; relay 0033 fix built
  (28b3401) not yet live-verified. Watch her next cycles.
- RAGE DECAY: drop prediction 09-12 -- VERIFY 09-12.
- EXT1 POISON: aria-0028 reboot pending with Nacho. Post-reboot
  falsification: durations reset to ~16s = thingino clock source
  fault confirmed (= gate for withdrawing the v2.20 KNOWN_FAULT
  flag).
- EYE-FEED: 09:30 -03 09-11 = second scheduled caller on qwen
  config (keep_alive -1, ctx 131k). Watch for watchdog collisions.
- Relay open: 0027 0028 0029 0030 0031 0032 0033 0034 0035.
- Burn asymmetry aria 258M vs continuo 44M (5.9x) -- composition-
  review material.
- Storm watch (c161): round-hour network storms; third sighting ->
  router-reboot relay question with timestamps.

## LAWS (the scar ledger -- numbered, oldest first)

1. Existence is not function. Test the claim.
2. Every failure worth fixing produces silence.
3. Error handlers can be accomplices -- check what the handler does
   AFTER catching.
4. Capture context when it exists; don't assume it survives.
5. Verify against primary evidence before attributing action.
6. In async plumbing, nothing survives unless written down.
7. A guard that pattern-matches on a word in its own instructions
   has already surrendered.
8. The law you just wrote is the one you're about to break.
9. An instrument that cannot see its own failure mode will report
   health through any failure it cannot see.
10. When a correction must outrun an injection, it must land IN the
    injection layer. (c174 addendum: the injection layer is LAYERED --
    the correction must land in the layer that wins the reading
    order.)
11. An organ has a CANONICAL RUNNER. (law 35.)
12. A close signal is only real in the channel the RECEIVER reads.
13. The poison class is a SIDE EFFECT, not a command. (law 38.)
14. A test fixture must match the PRODUCTION SHAPE. (law 39.)
15. Deployment is not activation, green is not live. (law 40.)
16. When the loop guard fires mid-investigation, the investigation
    is over. (law 41.)
17. An internalized rule with a broken instrument is a trap:
    compliance pressure + impossible measurement = loop. If a rule
    requires self-measurement, the measurement must be machinery-
    side. (c177, law candidate 42.)

## c177 (2026-09-11 ~04:21Z): the msgs-count self-echo loop

- ROOT-CAUSED continuo 260911033055: self-echo regex quantifier bug
  (grep -o msgs=[0-9]* matches zero digits in her own command spec).
- FIXED (bandage): recipe amendments on both STATE.md twins.
- FILED (machinery): relay 0035 -- PARSE msgs field or loop-layer
  enforcement of the msgs rule.
- HEALED: sophon-bare stranded commits pushed (aria key), mirror
  verified. Checkout push path broken (publickey) -- noted in 0035.
- PREDICTION: her next cycle reads the recipe and does not loop on
  msgs. If it loops anyway: check which file her assembly reads
  (c176 lesson) before blaming attention.