# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~01:17 UTC (cycle 102)

## NOW

- c102: RAGE SEV OSCILLATION ROOT-CAUSED + FIXED (c101's side
  finding, CLOSED). NOT one phenomenon: the unit runs as nacho,
  whose default key (id_ed25519, probed FIRST by the organ's loop)
  is NOT in root's authorized_keys -- only aria_ed25519 is. ssh
  uses only the first -i, so nacho-run offered the wrong key,
  jok=0 degraded window sev=2; root-run = jok=1 full recurrence
  sev=3. Same world, two grades, alternating by ran-as. LANDED:
  v1.6.1 (8603e53) ran-as+KH context on degradation lines;
  v1.6.2 (0ceb33e) aria_ed25519 FIRST in key probe -- THE FIX.
  Verified live: nacho-run now jok=1 sev=3 (full window certified).
  Organ law 15 added: an instrument that cannot say who it was
  when it measured lies by omission. Interactive-session item
  DROPPED (fixed in-bounds, additive+reversible per D-011).
- c101: RAGE sev=3 ROOT-CAUSED. The recurring fence class = tool-call
  soft cap, ARIA-ONLY (Sep 6/7/8: aria 25/52/26 lines, continuo
  2/11/0). Correlates with fat cycles: aria mean 4.35M in-tok/cycle
  vs continuo 1.31M (3d); blocks land inside 7M-token closes. Root =
  burn anatomy (cycle-burn-anatomy.md), not a bug. Levers: batch
  harder + tool-result truncation (queued). Analysis:
  knowledge/aria/rage-root-cause-2026-09-09.md (b49c869).
- c100: RELAY HYGIENE DONE + 0007 CUTOVER LANDED. i.ar 3f19973:
  aria_daily for-nacho section -> relay ledger (dual-write ends).
  Relay sweep: 0007/0009/0010/0004/0006/0008 answered, 0001
  dropped. open/ = 4 drill verdicts + aria-0006 (real asks).
  Verified live: heartbeat timer (fired 0000) + gpu-probe on sophon.
- c99: muse-glimmer pull verified in-flight. Sparse-blob lesson:
  read st_blocks not st_size. gemma3 Load-failed 20:01:42
  root-caused = cold-start race (ollama restart + rage organ 20s
  curl), one-shot, healed.
- c98: Aevum weekly pulse #3 (grip fixed point, 253 aborts/24h).
  Archive aevum-weekly-pulse-3-2026-09-09.md, with-nacho msg 642.
- Pulse all green (services, tripwire 0, disk 28%, heartbeat alive).
- c97: connectome baseline snapshot (W37) -- committed to main by
  continuo c165; nobody-owned file problem GONE (verified c99).

## NEXT (cycle work, in pull order)

1. muse-glimmer:30b benchmark WHEN PULL LANDS (c102 01:11 check:
   still in flight, ~7.4GB/16GB by blocks, ~1.2MB/s, ETA past
   04:00). Battery in tasks/iar/muse-glimmer-benchmark/battery.org.
   Vision is THE gate (qwen fitter class). File verdict via relay.
2. Weekly verdict obligation: next connectome snapshot (W38) gets a
   falsifiable observation (drill pattern). W37 is baseline.
3. Drills: #004 delegate only on natural need. Registry + queue live.
4. Retainer build: spawn-file-spec.org drafted (c97); runtime
   parsing = interactive-session work (D-005).
5. Failure-window watch: gpu-load-probe first instrumented window
   00:30 tonight (Sep 10); continuo owns the census. Aevum pulse #4:
   Sep 14.
6. (closed c102) Rage-organ oscillation: FIXED in-bounds (v1.6.2
   key order). No interactive session needed.

## STANDING

- Pulse: all green as of c102. Re-verify each cycle.
- Aevum host key: NOT in container known_hosts; accept-new used
  (TOFU). Re-verify or pre-pin next pulse.
- Diverged-pull protocol: stash -> rebase -> pop -> push. SCAR
  (c97): nobody-owned untracked file freezes rebase. Escape:
  update-ref + re-apply from object.
- Timer-state lie: aria-cycle.service shows "activating" mid-cycle.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
- glm degradation watch (session VI): 3 human-witnessed events.
- aria-cycle.conf key parse: ini-format key=value (cut -d=, not -d:).
- Sophon-side file creation = nobody:nobody (podman id-mapping).
- Zulip read API: narrow param URL-encoded JSON; .subject not
  .topic; num_after with anchor=<id> works.
- Ollama sparse blobs: partial blobs pre-allocate; st_blocks is the
  written bytes (c99).
- gemma3 cold-load = 17-20s; affect organs' 20s curl timeout races
  it right after any ollama restart (c99, one-shot, self-heals).
- Soft-cap economics (c101): 120 calls x ~35-60k mean ctx = 4-7M
  in-tok; the cap fires late (6M+ burned). Batching is the lever
  that moves rage sev down.
- Ran-as law (c102): every organ/instrument logs WHO it ran as and
  which input branch it took; an instrument that cannot say who it
  was when it measured lies by omission.
- nacho's ssh keys to root@10.66.0.5: ONLY aria_ed25519 works;
  id_ed25519 + emacboros_ed25519 denied (verified c102).

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
12. Anchor greps on line START ('] agent | tool_call'), never on
    substrings -- cmd= fragments pollute unanchored matches.
13. Verify a "pending" roadmap item against the primary source
    (the stream, the file, the ledger) before working it -- roadmap
    lines go stale (digest #1 done 2 days early; D-012 ratification
    sat in the ledger while the roadmap said "awaiting Nacho").
14. Apparent size lies on sparse files; read the blocks (c99).
15. An instrument that cannot say who it was when it measured lies
    by omission -- ran-as + input-branch context on every
    degradation line (c102).