#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-11 ~21:46 UTC (cycle 211)

## NOW

- c211 (21:37-21:46Z): LAW-40 LIVE TEST ARMED (84eb2ab, pushed
  sophon-bare): iar-request-log-full-capture = t in configs/
  tool-limits.el (defcustom appended at file end, overrides the
  defvar). Next cycle's pull-before-assembly picks it up; the
  FIRST real request should dump audit/<agent>/REQUESTS-full/
  REQ-<id>.json. VERIFY then REVERT in the same cycle.
  - Belt sync: f8e72df8 (c210 close) was stranded ahead-2 on BOTH
    checkouts (my c210 push never landed on sophon checkout --
    it was pushed to sophon-bare but the checkout fetches from
    sophon-bare only at cycle start; origin git@10.66.0.1 fetch
    401s from sophon checkout -- publickey. sophon-bare remote
    is the working transport). Pushed; sophon checkout current.
  - Continuo watch: her 21:13 cycle CLEAN -- awaiting-ghost line
    GONE from close-out (c205/c206 fix holds, law-40 verified),
    zero doubled-path writes since 20:51 (d09916a live test
    still pending her next task-tree write), msgs max 94, echo
    close worked first try. Her lab-notes post took 3 attempts
    (Zulip API param confusion: type/to/topic vs subject) --
    seed filed, hers.
  - Frigate recipe learned (8 calls): root podman ps does NOT
    see nacho's rootless containers. Recipe:
    machinectl shell nacho@ /bin/sh -c "podman exec frigate
    wget -qO- http://127.0.0.1:5000/api/stats". All 8 cams ~5.0
    fps OK. Host 8971 401s (nginx auth) -- container-internal
    is the path.
  - RSSI round 2: .104 n=213 mean -68.8, 12.2% below -70,
    longest below-70 stretch 4 samples (~4min), zero gaps,
    zero uptime resets. puller.log discrepancy: cron appends to
    rssi/puller.log but the file is EMPTY (puller writes
    nothing on success -- fine, but the redirect target exists
    empty; cosmetic).
  NEXT CYCLES (in order):
  (1) VERIFY + REVERT full-capture: check REQUESTS-full/ has a
      REQ-*.json with full payload (msgs count >= START msgs),
      then set the defcustom back to nil, commit, push both.
  (2) continuo's next post-fix task-tree write must land flat
      (doubled-path fix d09916a live test).
  (3) reboot-persistence seam: after tonight's reboots, camera-side
      /var/rssi.log first line vs sophon copy answers persistence
      empirically (puller keeps last-good; gaps = reboots).
  (4) storm watch: next ext4-storm window -> pull sophon's .104
      series FIRST (perishable evidence rule), correlate RSSI dips
      vs go2rtc timeouts.

- c210 (21:20-21:28Z): FULL-INJECTION CAPTURE FLAG BUILT (2e3f7f0,
  pushed origin + sophon-bare). iar-request-log-full-capture
  (defcustom, OFF): START-time dump of the COMPLETE payload to
  audit/<project>/<agent>/REQUESTS-full/REQ-<id>.json, pre-response
  (survives watchdog kills), pruned to newest 200. 6 new tests,
  suite 1241/1241. Doc: knowledge/aria/full-injection-capture-
  2026-09-11.md. Scars: (a) prune regex lost backslashes via python
  heredoc -> matched NOTHING -> silent no-op; caught only by the
  test's deletion-side assertion. LAW CANDIDATE: assert on what a
  filter removes, not just what it keeps. (b) /root/i.ar/emacs.d/
  gptel-fork is an EMPTY DIR here (fork lives at /root/.emacs.d/
  gptel-fork) -- 6 debug calls burned on a stale pointer.

- c209 (21:06-21:15Z): RSSI PULLER LIVE (build). sophon
  /var/lib/aria-fleet/rssi-puller.sh, cron */15, thingino web API
  transport (dropbear REJECTS our keys -- login.cgi + run.cgi is the
  recipe), 8/8 cameras, atomic writes + last-good-on-failure. First
  .104 series analysis: n=193, mean -68.9, 13% below -70 (bgscan
  threshold), longest continuous below-70 stretch 7 MIN, zero
  reboots. Relay 0043 filed (host cron change, visibility).

- c208 (20:48-20:56Z): DOUBLED-PATH FOSSILS found + FIXED (build,
  d09916a sophon-bare): iar--task-path-strip-agent-prefix guard in
  both resolvers, 5 tests, suite 1235/1235. Awaiting-ghost fix
  VERIFIED (her 20:48 close-out clean). Law-40 live test pending.

- c207 (19:59-20:42Z): CONTINUO EXIT-126 ROOT-CAUSED. Root `git
  status` from yoga (aria@i.ar key = emacboros_ed25519) rewrote
  .git/index as root -> her rootless podman lsetxattr EPERM -> exit
  126. LAW 44 SECOND ACTOR SHAPE: interactive root ssh + "read-only"
  git commands. Relay 0042 OPEN (nacho-security): confirm yoga
  actor + iar.sh index-chown proposal. Index verified nacho-owned
  c209/c211 (no re-poison since heal).

## STANDING (verified, still true)

- phantom-exit fix baf05d0; rage organ v1.8; fence panel 0 true
  production rejections; dashboard v2.1/v2.2 live; fleet-check
  v2.20; cycle limits: turns 120 / soft 300 / fence 192k / wall
  3600s; eye-feed LIVE daily 09:30 -03; aria-0026 ratified.
- ZULIP READ PATH: /api/v1/messages + basic auth, string pair.
  Recipe: knowledge/aria/zulip-read-path.md.
- RSSI SERIES LIVE + DURABLE: per-camera cron (c202) + sophon
  puller (c209). Data: /var/lib/aria-fleet/rssi/<ip>.log. Doc:
  knowledge/aria/rssi-puller-live-2026-09-11.md.
- CAMERA WATCH: 7/8 recording; .103 network-dead (chronic; relay
  0038). .2 = Dropbear bridge carrying ext4. Frigate stats recipe:
  machinectl shell nacho@ podman exec frigate wget (rootless store).
- Relay open: 0042 (yoga actor, Nacho), 0043 (puller visibility).
  Verify against answered/ before citing older ids.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2 ~2wk.
- Burn asymmetry STABLE 6-8x full-day. knowledge/aria/
  burn-asymmetry-2026-09-11.md.

## RECIPES (hard-won, reuse them)

- thingino web API: POST /x/login.cgi {thingino:thingino} -> cookie
  -> GET /x/run.cgi?cmd=<base64>. Response ECHOES the command as
  HTML -- grep output on data pattern, never head-check line 1.
- sophon-side file edits: python3 heredoc, NOT sed with escaped
  pipes (sed-over-ssh quoting burned calls in c207 AND c209).
- python heredocs that WRITE ELISP: backslashes do not survive
  naive heredocs -- the c210 prune regex lost its backslashes and
  silently matched nothing. Use a python file (write_file) with
  asserted anchors, or check the written bytes after.
- scp-then-run for multi-line python over ssh (c207).
- Frigate stats (c211): root podman can't see nacho's rootless
  containers. machinectl shell nacho@ /bin/sh -c "podman exec
  frigate wget -qO- http://127.0.0.1:5000/api/stats". Host 8971
  401s (nginx auth on); container-internal 5000 is the path.
- sophon i.ar checkout remotes: origin (git@10.66.0.1) fetch 401s
  from there (publickey); sophon-bare (root@10.66.0.5) is the
  working transport. Push to sophon-bare, checkout picks up at
  next cycle start.
- Timezone discipline: sophon journal = LOCAL -03; my records = UTC.
  Normalize before comparing (c186 law).
- gptel fork in THIS container: /root/.emacs.d/gptel-fork (the
  /root/i.ar/emacs.d/gptel-fork path is an empty dir -- stale).