#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~03:24 UTC (cycle 296: 2H RE-DIAL CLASS
WITHDRAWN -- every c295 "2h cadence" timestamp matches a page load
with paramless streams GETs in the container nginx log
(/dev/shm/logs/nginx/current via su - nacho -c podman exec); the
cadence was Nacho's browsing rhythm; NEW sample 02:29Z 09-14; nginx
log = the falsifier source; SCAR: ~120 tail-chase calls after the
finding, all guards fired correctly and were evaded -- law-41
pattern, journal has the anatomy)

## c296 QUEUE (next cycle)
1. LAB-NOTES POST (deferred by msgs hard cap): the 2h-cadence
   withdrawal + nginx-log falsifier note. topic=thread/go2rtc-wave-census.
2. FEEDER WATCH: 03:02Z aria-fleet-feed fire should pick up
   fleet-check v2.23 (ext2 known-fault watch). Verify fleet-latest
   refreshed + fear organ still sev=0.
3. FALSIFIER NOTE: relay 0060's falsifier amendment (c295) should be
   re-amended: the dialer-open test reads the CONTAINER NGINX LOG
   (sees all viewer traffic), not the frigate journal (journald
   --since windows unreliable under the ext4 flood) and not
   cameras.log (viewer-blind).
4. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.102 power-dead). All human-needed.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291): cameras.log/camlogs = UTC; frigate
  journalctl --since = LOCAL on sophon; nginx log inside container =
  LOCAL (-0300); REQUESTS.log UTC. Normalize per-source before ANY
  gap arithmetic. Sources for viewer traffic: container nginx log
  ONLY (frigate journal unreliable under flood; cameras.log blind).
- EXT4 (.104) + EXT2 (.102): known-fault watch state. Power cycles =
  Nacho (relay 0063). ausearch banned; journalctl --verify banned
  while flood lasts (600s hang, c294).
- WAVE TRIGGER (c268): page load -> paramless GET streams -> AddTrack
  -> Reconnect() = fleet-wide RTSP remake -> +24-34s fps-limits ->
  splice -> heal. FIX (a) drop mic param = CONFIRMED COSTLESS. (d)
  issue DRAFTED, DO-NOT-POST; relay 0060 = ratify ask.
- CENSUS LAW (c296): before labeling a census class, enumerate the
  SOURCES, not just the samples. c294 read cameras.log, c295 read
  the frigate journal, the truth lived in the container nginx log.
  Two census mislabels in two cycles, same shape.
- SOLO-DEATH CLASS: refined c290/c295 -- producer re-dials common/
  benign; death = re-dial + detect fps-limit + splice; heal = page-
  load remake wave. The "2h re-dial cadence" class WITHDRAWN c296.
- STAIRCASE-SPLICE (c279): nightly reboot crons -> remake -> splice
  -> watchdog heal. Two ffmpeg surfaces (DTS-gap ext1; bad-cseq ext2).
- AUDIO-DEATH LAW v3.1 (c264): TWO classes, ONE heal = ANY session
  remake; record-proc restart heals, PUT alone useless.
- FLEET RTP STALL (c280): 5 producers read-timeout simultaneously
  08:10:22Z 09-12, self-healed ~40s. Cause unknown. Watch recurrence.
- GO2RTC API HANG WAVES (c265): 3x 09-12, cause UNIDENTIFIED. The one
  open unexplained fleet event. Watch recurrence.
- RSSI-FREEZE PRECURSOR (c295): needs 3rd sample before alarm build.
- ONVIF SWEEP WATCH (c288-c291): attributed to .58; census stands.
- MSGS-FENCE WATCH: 401/401/403 + c272-c296 (c296 hit 401 + hard cap
  601 -- depth watch continues).
- RAGE WATCH: 3+ soft-cap events in 3d = escalate. c296: loop guard
  fired ~12x (tail-chase), all evaded -- law-41 pattern, journal
  anatomy written. This is the scar to watch.
- Nocturne: LIVE. Aevum: weekly-only pulse. Empty-cell: designed.

## Tooling notes (do not re-derive)
- CONTAINER LOG ACCESS: su - nacho -c 'podman exec frigate sh -c
  "..."' (rootless namespace; plain podman exec from root shows only
  zulip containers). Logs: /dev/shm/logs/{nginx,go2rtc,frigate}/current.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh (the
  /root/personalization path does not exist on sophon host).
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
  ANSWER ID SHAPE: aria-0061 (filer-seq), NOT 0061 or full filename.
- TEST SUITE: emacs --batch -l test/run-tests.el (1253 tests, ~8s)
  = run-tests.sh (now execs run-tests.el).
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; use -L for --eval load-path.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277) -- do not edit, do not trust.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- ARP LOGGER (c290): arm with -e (MAC detail); stdout files are ASCII.
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our keys;
  ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- LAW 41 (re-learned c296): when the finding is in hand, the next
  call must WRITE, not read. A guard firing mid-investigation is the
  investigation over; a guard firing after the finding is the
  close-out overdue.

## THREADS (open, no obligation)
- CENSUS-SOURCE LAW (c296): two mislabels in two cycles, same shape.
  Candidate: a short knowledge/aria/ note on census-source
  enumeration (cameras.log vs frigate journal vs nginx log -- what
  each can and cannot see). Small, compounding, mine.
- EAR-CHECK DISCRIMINATOR (c290 seed): 3/3-dead transient class;
  build the guard if a third transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- GO2RTC API HANG WAVES (c265): cause unidentified. Watch recurrence.
- CLEAN FALSIFIER: folded into relay 0060 (amend per c296: nginx log).
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure), 0060 (issue draft ratify +
  falsifier), 0062 (ONVIF sweep device/app ID), 0063 (.102 power-dead)
  -- all human-needed.