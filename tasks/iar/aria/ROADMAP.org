# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~23:41 UTC (cycle 98)

## NOW

- c98 DONE: Aevum weekly pulse #3 run (Sep 9, Nacho's weekly schedule).
  GRIP IS A FIXED POINT: 253 aborts/24h at ~5.5min cadence, zero
  variance; tick 141 frozen 47.5h; transcript frozen Sep 8 00:24.
  Sep 07 zero-abort day = c52 corollary confirmed (abort machine only
  runs when a request is inside the eval window). Archive:
  aevum-weekly-pulse-3-2026-09-09.md, with-nacho msg 642. Pushed
  bdfbcf4 + 0954a42.
- Digest #1: ALREADY POSTED Sep 7 (with-nacho msg 516) -- roadmap line
  was stale by 2 days. Verified in-stream, not assumed. Next digest
  due with next weekly cycle.
- Pulse all green (services, tripwire 0, disk 28%, heartbeat alive).
- c97: connectome baseline snapshot (W37). Snapshot file on sophon is
  nobody:nobody (podman id-mapping) -- repo copy pending chown,
  flagged for-nacho.
- c96: nemotron survey PASS, relay aria-0009 filed (awaiting Nacho).

## NEXT (cycle work, in pull order)

1. FOR-NACHO pending: chown knowledge/aria/connectome on sophon ->
   then next snapshot run commits the file to the repo.
2. Allowlist ratification (aria-0009) -> on ratify: flip
   spawn-interlocutor.org model field to nemotron-3-ultra:cloud.
3. Weekly verdict obligation: next connectome snapshot (W38) gets a
   falsifiable observation (drill pattern). W37 is baseline.
4. 0007-legacy cutover EDIT still to do (ratified, not landed).
5. Relay queue for Nacho: 0000, 0001, 0002, 0003, 0006, 0007, 0008,
   aria-0009.
6. Drills: #004 delegate only on natural need. Registry + queue live.
7. Retainer build: spawn-file-spec.org (format spec doc) -- pure doc,
   cycle work.
8. Failure-window watch: gpu-load-probe first instrumented window
   00:30 tonight (Sep 10); continuo owns the census. Aevum pulse #4:
   Sep 14.

## STANDING

- Pulse: all green as of c98. Re-verify each cycle.
- Aevum host key: NOT in container known_hosts; accept-new used (TOFU
  against record's prior verifications). Re-verify or pre-pin next pulse.
- Diverged-pull protocol: stash -> rebase -> pop -> push. NEW SCAR
  (c97): unremovable nobody-owned untracked file freezes rebase
  --abort/--skip/cherry-pick. Escape: update-ref + re-apply from
  object.
- Timer-state lie: aria-cycle.service shows "activating" mid-cycle.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
- glm degradation watch (session VI): 3 human-witnessed events.
- aria-cycle.conf key parse: ini-format key=value (cut -d=, not -d:).
- Sophon-side file creation = nobody:nobody (podman id-mapping).
- Zulip read API: narrow param must be URL-encoded JSON; .subject not
  .topic on message objects; num_after with anchor=<id> works.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
12. Anchor greps on line START ('] agent | tool_call'), never on
    substrings -- cmd= fragments pollute unanchored matches.
13. Verify a "pending" roadmap item against the primary source
    (the stream, the file) before working it -- roadmap lines go
    stale (digest #1 was done 2 days before its deadline line).