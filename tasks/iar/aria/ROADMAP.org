#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-04 05:15 UTC (c15, flash: pngprof.py BANKED -- lost c13 decoder rebuilt as durable instrument, selftest + ffmpeg positive control (101.3 vs 103.1). c13 12Z band numbers NON-REPRODUCIBLE (segment-dependent or lost-decoder bug); provenance pinned. fedora@ 10th cycle standing.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS: c15 ok (~22 calls, no loop-guard fires, no cap warning).

* DO FIRST (next cycle)
1. FAILURE-FIRST (always).
2. Organs at wake; fleet-check baseline (v2.14: int3-only allowlist).
3. e3 thread: PHYSICAL ONLY. Pixel archaeology CLOSED (c12-c14:
   emitter e3-local, 2 instruments + 2 neighbors). Flag 380 ask
   stands (385/391/396). Do NOT re-pull e3 frames without a new
   question. If a cycle has nothing else: ONE census query
   (is the block there tonight) and move on.
4. Watch: ext2 re-deafness; ext4 recurrence; .104 RTSP flakiness
   (350 i/o timeouts all-time); .102 daily 23:00 WRN pair.
5. fedora@10.66.0.5 ssh auth: re-verify each cycle (10th standing);
   interactive item (11) stands.
6. Census dedupe (REQUESTS.log double-log): key on epoch
   (REQ <epoch>-N). Low priority, banked.

* OPEN FLAGS (Nacho's call, all low urgency)
- 380 (e3 motion anomaly): REAL + nightly + clock-aligned +
  e3-LOCAL + objects NAMED (c13) + neighbors FLAT (c12 e4, c14 e2).
  Physical check ask stands (385, 391, 396). Pixel archaeology
  closed. c15: pngprof.py banked for any future re-profiling.
- 383: ext2 recovery RESOLUTION (informational).
- 375 (router scheduled-reboot question, burst A 04:00-04:53Z).
- 326 (frigate restart to heal int3): int3 half still open.
- 270 (camera API creds): unblock path documented.
- 340 (go2rtc debug logs), 290 (valence host timers).
- Zulip backup gap (TOP infra flag, 349): /var/lib/zulip in
  NEITHER restic set.
- i.ar push to origin (git@10.66.0.1) needs Nacho's key.

* THREAD BANK (candidates for next cycles)
- Segment-size clock (c10): fleet-wide hour-scale size oscillation,
  00h trough. Candidate fleet-check line.
- Frigate watchdog census as fleet-check line (v2.15 spec banked).
- Log-retention horizon: census-to-file rotation (CSV).
- Thingino follow-ups (banked in thingino-primary-source-notes.md).
- Glance rotation as standing instrument -- BUILD decision.
- gptel unknown-tool stall question (unresolved).
- THREADS.org seeds (gift, witness-friction, absence-signals):
  unclaimed; pull only if one actually pulls.
- pngprof.py follow-ups: RGB->luma coefficients are BT.601; camera
  is likely BT.601 too (fine); if a future camera differs, note it.

* LAWS (newest first, full text in JOURNAL.org)
- TEST-INVARIANT LAW (c15, new): when a test fails, find the ACTUAL
  broken invariant before patching -- a "fix" for a phantom bug
  (struct.pack('>I',13) was RIGHT) costs a call and leaves the real
  bug (missing CRC chunks) in place.
- RECIPE-PROVENANCE LAW (c14): a working recipe recorded without
  its exact invocation is a fossil; pin full paths and hosts at
  first use (sqlite3-in-container was; ffmpeg path wasn't).
- MOTION-SCORE LAW (c14): frigate's motion column is an activity
  score (0-255+), not a boolean; census metric = SUM(motion) per
  bin; motion=1 is a near-useless filter.
- WITNESS-VS-EMITTER LAW (c9, EXTENDED c12): surfaces lit by an
  unseen source are witnesses, not the emitter; hunt the geometry,
  not the glow. c12: TIME BEHAVIOR separates witnesses from the
  emitter. c13: witnesses NAMED. c14: TWO instruments + TWO
  neighbors agree -- e3-local confirmed.
- DAYLIGHT-DISCRIMINATOR LAW (c9): a hypothesis about a light dies
  if its footprint is visible as an object at noon. c13 AMENDMENT:
  the discriminator also NAMES the objects -- run it on the
  camera's own frame, not just neighbors'.
- INSTRUMENT-PROVENANCE LAW (c10, RESOLVED c11): a census number
  without its exact source is not re-runnable. c15 AMENDMENT: a
  number without its INSTRUMENT is not even explainable -- c13's
  12Z band ratio (0.49) doesn't reproduce (c15: 0.89) and the lost
  decoder can't be interrogated. Pin instrument + segment at first
  use.
- PROBE-BATCH TRIGGER (c10): 3rd consecutive single-question ssh
  = STOP and write the batch. c13 AMENDMENT: container-vs-host
  confusion -- if ls fails, check WHERE the path lives before
  re-running.
- HEREDOC-TARGET LAW (c10): never use the same local file as ssh
  input source AND remote write target in one command.
- EDGE-RESOLUTION LAW (c8): only 15s/1-min bins at the edge
  separate "ends around 3am" from "clock-aligned cutoff".
- CONTROL-BLOCK LAW (c8): an elevation is camera-local only if
  the control camera is flat in the SAME time blocks.
- SEGMENT-NAME LAW (c7): hour-dir segment names are MM.SS-of-hour;
  grids differ per camera (e2 has 30.04, e3 has 30.13).
- LOG-HORIZON LAW (c7): podman logs retains ~3 days; note the
  horizon in every census claim. DB retention also 3 days.
- ALLOWLIST-TRIPWIRE LAW (c7): RECOVERY-on-known-deaf fires loud.
- TZ-SIGN LAW (c6): container python mktime is LOCAL (-03);
  utc() = mktime - 3h.
- FRIGATE-DB PATH LAW (c6): inside the container the DB is
  /config/frigate.db; on the HOST it is /home/nacho/containers/
  frigate/config/frigate.db (NOT storage/config).
- TZ-ARTIFACT LAW (c5): re-check own prior claims against primary
  evidence when the thread reopens.
- PATTERN-DEPENDENCE LAW (c5): a census number without its grep
  pattern is not reproducible; pin ONE pattern and document it.
- BOOT-SIGNATURE LAW (c5): reboot = REFUSED -> TIMEOUT -> chaos
  -> aftershocks -> stable.
- DOCS-BEFORE-PROBE PAYOFF LAW (c4): documentation replaces
  probing fog with a map (incl. DANGER list).
- TOPOLOGY-FIRST LAW (c4): read the config that defines the chain
  BEFORE modeling failure modes.
- CENSUS-REFRAME LAW (c4): check the all-time census before
  treating an event as novel.
- TZ-CORRELATION LAW (c3): frigate container logs are UTC-3,
  recordings are UTC.
- PACKET-COUNT WITNESS (c3): ffprobe -count_packets discriminates.
- READ-TIMEOUT-HEALER LAW (c3): producer read-timeout heals
  zombie sessions (2nd instance: ext2 c7).
- LOOP-GUARD-READ LAW (c2): re-read captured output before walking
  source; batch remote probes into one compound ssh per question.
- LOADED-GUN LAW (turn 168): never GET-probe action endpoints.
- KNOWN-POSITIVE-FIRST LAW (turn 167): validate census patterns
  against a known-positive BEFORE trusting results.
- MIRROR-WALK LAW (cycle 46): the record's "next cycle picks
  fresh" is a letter, not a law.
- COMMIT-MESSAGE LAW (cycle 45): a commit message is a claim
  about a diff; the pull-before-write guard forces the look.
- CAP-LANDING LAW (cycle 44, 13 instances): the soft cap bites
  during LANDING. Landing first = comfortable.
- SSH-PROBE CHAIN LAW (continuo c27, adopted): batch remote probes.
- POINTER-VS-CONTENT LAW (cycle 42): the CONTENT is usually real;
  the pointer is what's wrong. c15 SELF-INSTANCE: "banked in the
  journal" pointed at /tmp, which dies per container.
- INSTRUMENT-TAX (cycle 41): batch hard.
- STALE-POINTER LAW (cycle 40): cross-references get re-checked.
- PRODUCER-ID LAW (cycle 39): record producer IDs at first sample.
- MTIME LAW (cycle 38): segment mtime is NOT arrival evidence.
- PRODUCER-VS-RECORDING LAW (cycle 38): verdicts need all three.
- CODEC-PRESENCE LAW (cycle 37): offer is not flow.
- RENEGOTIATION-CONVERGENCE LAW (cycle 34): converges over ~10 min.
- FRIGATE-ACCESS LAW (cycle 33): rootless stack (user 1000);
  runuser -l nacho for container ops; recordings at /home/nacho/
  containers/frigate/storage/recordings/ on host = /media/frigate/
  recordings/ inside; go2rtc unauth localhost:1984 INSIDE container;
  camera RTSP needs thingino:thingino; container ffmpeg is at
  /usr/lib/ffmpeg/7.0/bin/ffmpeg (NOT on PATH, c14); /media/frigate
  root-level is root-owned INSIDE container and NOT host-visible --
  copy via recordings/ subdir (c13); DB on host = /home/nacho/
  containers/frigate/config/frigate.db, read with host python3
  (no sqlite3 in container, c14).
- SILENT-TRACK-LOSS LAW (cycle 32): track loss inside a surviving
  session is silent.
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a session state.
- PROVISIONAL-VERDICT LAW (cycle 31): recovery during a flap is
  provisional.
- RECOVERY-VERDICT LAW (cycle 30): recovery is a destructive
  decision; positive AND negative controls first.
- CENSUS LAW (cycle 29): counts gating destructive decisions need
  pattern validation.
- POINTER LAW (cycle 27): wrong pointers misdirect writers AND
  readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits
  to an undecided framing is not a fix.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final
  write misses the write.
- DIFFERENTIAL LAW (cycle 8): same input through both pipelines
  finds which layer lies.