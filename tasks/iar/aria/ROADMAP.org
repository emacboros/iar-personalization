#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-13 ~04:11 UTC (cycle 283: sink noise filter +
logrotate deployed; dropbear brute-force FROM SOPHON witnessed 03:39Z,
source unidentified, watch filed; int1 splice still pending 06:00Z)

## c284 QUEUE (in order)
1. INT1 SPLICE WATCH: int1's crontab 06:00Z = 03:00 local. Check
   frigate journal for "exceeded fps limit" on interior_1 ~06:00Z +
   splice markers. Prediction: one splice per camera per night at its
   crontab hour (census-backed 7/7). cameras.log should show int1's
   boot lines live at 06:00Z (post-filter, boot lines survive).
2. SINK PERSISTENCE CHECK (CRITICAL, tonight): do the jct writes to
   /etc/thingino.json survive the nightly crontab reboots? ext1 01Z,
   ext2 02Z, ext3 03Z, ext5 05Z, int1 06Z, int2 07Z, int3 08Z.
   CHECK: cameras.log boot lines from each camera at its hour + a
   post-reboot jct get rsyslog.enabled census (rerun the c282 census
   after the reboot wave). A camera that reboots and then goes silent
   in cameras.log = config reverted = fix belongs in camera boot
   script (relay to Nacho). If ALL revert, the sink survives only
   until tonight -- file relay immediately.
3. DROPBEAR WATCH (new, c283): grep dropbear /var/log/cameras.log.
   03:39:48Z sophon-origin 3x failed root pw vs .103, source
   UNIDENTIFIED (forensics in sink doc c283 addendum). Recurrence ->
   capture samples, file relay nacho-security. 7 days silent -> close
   watch. Details: tasks/iar/aria/sink-watch/.
4. PULLER v2 VERIFICATION: after tonight, confirm census labels
   behave (real log lines reappear at each camera's reboot).
5. RELAY WATCH: 0060 (go2rtc issue draft ratify; c280 strengthens the
   case). If Nacho rules, post draft to AlexxIT/go2rtc. Do not post
   without his eyes.
6. WAVE DETECTOR BUILD: trigger precise (fleet backchannel sessions +
   +24-34s fps-limits). cameras.log = continuous witness source (no
   ring loss). Build when it pulls.
7. FLEET RTP STALL WATCH: 08:10:22Z class. If recurs, correlate nic
   1-min log + camlog + cameras.log (second witness).

## c283 addendum (2026-09-13 ~04:11 UTC)
- SINK NOISE FILTER LIVE: /etc/rsyslog.d/30-cameras.conf drops
  `$msg contains "cmd {"` (RSSI cron signature) before the file
  action; verified live (tcpdump + absence/presence: RSSI lines
  arrived and did not land, ntpd line landed). Logrotate weekly x8
  deployed (/etc/logrotate.d/cameras.log, HUP postrotate). Sink
  steady-state now ~2 lines/10min.
- DROPBEAR BRUTE-FORCE FROM SOPHON (03:39:48Z, first sink security
  witness): 192.168.2.69 -> .103 dropbear, 3 failed root password
  attempts, single 1s burst. Source on sophon UNIDENTIFIED: no
  process/cron/script/journal/history trace; continuo clean; my c282
  ssh-to-camera at 03:39:49Z is BatchMode key-only (cannot send
  passwords) and exited 255 one second AFTER the attempts. Watch
  filed: tasks/iar/aria/sink-watch/dropbear-watch-and-persistence.
  Recurrence -> relay nacho-security with multi-sample data.
- SCAR (caught in time): nearly overwrote the sink doc with the c283
  addendum (write_file vs append_file); restored from HEAD, appended
  properly. Append-only discipline held by one reflex.
- Belt: 6eb3b535 (continuo artifacts + affect), ca753981 (sink doc),
  1c304c0c (journal+history).
- Instrument tax: predicted ~30min, actual ~14min (filter was small;
  the dropbear forensics were the unplanned depth -- worth it).

## c282 addendum (2026-09-13 ~03:55 UTC)
- SINK ROLLOUT CORRECTION: c281's "all 7 cameras forward" was FALSE --
  ext1 (192.168.2.101) was never configured (rsyslog.enabled key
  absent, syslogd bare). c281's logger-test witness list had six
  names; the doc's claim said seven; nobody counted. Fixed c282
  ~03:48Z: jct set 4 keys + S01syslogd restart -> -R flag live ->
  ARIA-101-SINK-JOINed landed 03:48:48Z. Fleet genuinely 7/7 now.
  Doc corrected in place (camera-syslog-sink-c281-2026-09-13.md,
  c282 CORRECTION section). LAW CANDIDATE (FLEET column): a claim
  that quantifies a set ("all N") must be verified by a census of
  the set, not by witnessing a subset.
- Pre-persistence baseline: sink holds 6 cams' crond lines (03:26Z
  onward) + ext1 joined 03:48Z. Zero fps-limit events since 03:00Z;
  zero Backchannel sessions in sink so far (none expected -- no
  page traffic overnight).
- rssi puller uptime census: all 7 crontab reboots fired on schedule
  last night (22:02Z-05:02Z) -- persistence check has a clean prior.
- Belt: d1dbd5c0 (continuo artifacts), 821a92b7 (sink doc correction).

## c281 addendum (2026-09-13 ~03:29 UTC)
- CAMERA SYSLOG SINK LIVE (doc knowledge/aria/camera-syslog-sink-c281-
  2026-09-13.md): all alive thingino cameras forward syslog udp514
  to sophon 192.168.2.69 -> /var/log/cameras.log. Chain: jct set
  rsyslog.{enabled,host,port,local} -> S01syslogd restart -> syslogd
  runs with -R. sophon: /etc/rsyslog.d/30-cameras.conf (imudp bound
  LAN IP, $fromhost-ip 192.168.2.* -> cameras.log & stop).
- THE TRAP (25min to find): firewalld FedoraWorkstation zone accepts
  udp 1025-65535; 514 is BELOW 1025. tcpdump saw packets arrive;
  rsyslog received nothing; local logger tests landed fine. Fix:
  rich rule source 192.168.2.0/24 udp 514 accept (PERMANENT, verified
  in nft ruleset). Law-50 texture: verify the path TO the socket.
- Splice watch: no new fps-limit events in the 4h window checked
  (20:00-00:00 local); int1 06:00Z prediction intact.

## OPERATIONAL RECIPES (standing)

- FAILURE-FIRST: read LAST-CYCLE.txt first every cycle.
- PULL-BEFORE-WRITE: git pull --ff-only before ANY shared-file write;
  if pull fails, no shared writes this cycle.
- BELT DISCIPLINE: commit continuo's uncommitted artifacts BEFORE my
  own writes. THE RECIPE (c277 VERIFIED):
    git add -f -- 'audit/iar/continuo' ':(exclude)audit/iar/continuo/cycle.log'
- CLOCK SHAPES: cameras UTC; frigate logs LOCAL (-03); frigate nginx
  access log LOCAL [12/Sep/2026:...]; prudynt UTC. Convert first.
  Camera crontab reboots: ext1 01Z, ext2 02Z, ext3 03Z, ext5 05Z,
  int1 06Z, int2 07Z, int3 08Z (local = Z - 3).
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext) / 156-band (int) / 1 pkt stub /
  0 pkts stream-absent / EMPTY = audio track absent. ffprobe PATH in
  frigate container = /usr/lib/ffmpeg/7.0/bin/ffprobe (c275).
- GO2RTC (c252/c265): /api/streams via podman exec curl localhost:1984.
  PUT reload = QUERY PARAMS (?name=X&src=Y), NOT a JSON body (c265).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP -- mic probe
  reconnects ALL of them. ext4 (power-dead) has no producers.
- PRODUCER ID MAP (c275 baseline): ext1 47382, ext2 43102, ext3 44120
  (ext3 producers[0] is a placeholder; filter by id presence),
  ext5 43133, int1 45183, int2 43108, int3 43121; ext4 none.
- CAMERA SYSLOG SINK (c281-c283): /var/log/cameras.log on sophon; all
  7 alive cams forward udp514; noise filter drops RSSI crond lines
  (c283); logrotate weekly x8 (c283). PERSISTENCE UNVERIFIED (jct
  write vs nightly reboots -- tonight). Camlog puller = backup witness.
- CAMERA ACCESS RECIPE (c209/c282): dropbear ssh REJECTS our keys;
  transport = curl login (POST /x/login.cgi thingino:thingino ->
  cookie) + GET /x/run.cgi?cmd=<base64>. jct reads/writes
  /etc/thingino.json keys. Census pattern: loop jct get over all IPs.
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- TEST SUITE: full suite = emacs --batch -l test/run-tests.el (1250
  tests, ~8s) = run-tests.sh. Filter: run-tests.sh "regexp".
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; --eval load-path changes crash startup. Use -L.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (cycle 59) -- do not edit, do not trust (c277).
- RAGE-ORGAN v2.2 (c278): converged/dead classifier. Depth events
  (Exit: 0) = sev=1 depth-watch; dead events (Exit: 1 / no completion
  line) drive severity. Do not regress to class-level counting.
- FRIGATE LOG PATTERN (c279): batched pull journalctl -u
  frigate.service --since "-6h" to /tmp, grep locally. Loop guard
  fires at 10 same-tool calls -- do not iterate ssh greps.
- ZULIP POST KEY: aria-cycle.conf is an ini file -- key line is
  "key = ..." (grep ^key, cut -d= -f2, strip quotes). First-line
  head -1 grab fails (Malformed API key, c280).
- ausearch HANGS (c281): ausearch on sophon blocked >600s twice --
  do not run it in a cycle; use journalctl for AVCs if needed.
- APPEND-ONLY (c283): shared docs get append_file, never write_file;
  a write_file on an existing doc erases the record's history.

## THREADS (open, no obligation)
- DROPBEAR WATCH (c283): sophon-origin brute-force, source unknown.
  Recurrence -> nacho-security relay. 7d silent -> close.
- WAVE DETECTOR BUILD: trigger precise (fleet backchannel + +24-34s
  fps-limits); cameras.log = continuous witness. Build when it pulls.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence;
  correlate nic 1-min + camlog + cameras.log when it repeats.
- SOLO-DEATH CLASS: samples #1/#2 (int1 19:00:04, ext5 19:13-15
  09-12) still lack camera-side witnesses; sample #3 reclassified
  OUT (c280). Leading hypothesis = silent producer-side re-dial
  (c269). Dialer identity open -- falsifier in relay 0060.
- STAIRCASE-SPLICE MECHANISM: census-backed (c279). int1 06:00Z is
  the next predicted event (c284 watch).
- SINK PERSISTENCE (c281/c282): jct vs nightly reboots -- tonight
  tells. ext1 is the newest member (set 03:48Z c282).
- CLEAN FALSIFIER: ask folded into relay 0060.
- MSGS-FENCE WATCH: 401/401/403 + c272-c283 clean. Watch continues.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- Guard scope (c273): general ignored-file resurrection only if a
  second class appears. Belt recipe now mechanical (c277).
- .ar-suffix interactions (c274): file-name-handler-alist is global;
  watch for other tooling touching .ar paths.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure), 0060 (issue draft ratify +
  falsifier) -- all human-needed.