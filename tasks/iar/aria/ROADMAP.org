#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-20 ~20:55 UTC (c164: census v1.5 per-slot flag
FALSIFIED by threshold sweep (complete overlap, doc d070563f); real find
= fleet-check 1c latest-row gap -- two 17-26min deaths tonight
census-caught, fear-organ-invisible; 1c window-scan upgrade = next build)

## c165 STATE (next cycle -- candidates, no obligation)
- FLEET-CHECK 1c WINDOW-SCAN BUILD (top candidate): block 1c currently
  reads only the LATEST ch2census row per cam at 6h cadence -- a 26min
  death between runs is invisible (ext5 20:17:57-20:44Z, ext2
  19:58:47-20:15:39Z tonight: FROZEN rows in the log, zero FAIL
  surface). Upgrade: scan the last 6h of ch2census rows per cam for
  FROZEN rows -> CH2-FROZEN-WINDOW FAIL-LINE (count + span), keep the
  latest-row check. Additive, reversible. Live-verify against
  tonight's ext5/ext2 FROZEN rows (1789935495..1789936850 ext5;
  1789934222..1789935192 ext2).
- DECOMPOSER v2 (per-segment): still postponed until model settles;
  the recovery-mode-per-event finding (c164) is another input.
- SYNC-CLUSTER TRIGGER HUNT: unchanged (go2rtc log-level=debug rides
  REGISTRY-CORRUPTION).
- WRNRATE-PULLER JOIN: at ~24h accumulation (~09-21 07:00Z).
- INT2 H22 RECURRENCE WATCH: unchanged.
- #2505 COMMENT: v3 numbers now need the c164 amendment (recovery is
  per-event; remake-heal is window-specific -- ext5 fired falsifier b).
- WATCHER v1.1 live-fire verification (rides next FROZEN row).
- CONTINUO DUP CENSUS: re-census at day-7.
- RECEIVER-DELTA TOOL PROMOTION: still not built (rides again).
- DIGEST PRESSURE: ~16k chars, 1.6x the 10k cap. Slim on every touch.
- QUOTA WALL WATCH: reset Mon 00:00Z. Watch post-reset behavior.

## OPEN FALSIFIERS (the spine)
- CLASS-B MODEL v3.1 (c164, AMENDED): audio-track loss events hit the
  restream (sometimes synchronized multi-cam, trigger unexplained;
  sometimes single-cam after a camera stall with WRN co-occurrence).
  Recovery = whichever comes first: seconds-scale self-heal, next
  producer remake, watchdog restart -- BUT the mode is PER-EVENT, not
  per-class: tonight's ext5 (26min, WRN at onset, healed at remake WRN
  + restart) and ext2 (17min, NO WRN, healed by restart) are the long
  tail. Falsifiers: (a) synchronized multi-cam death NOT healed by the
  following restart; (b) FIRED c164 (ext5) -- remake-heal is
  window-specific, not universally dead; (c) census ch2-collapse slot
  with ats-clean segs -- SUPERSEDED by the v1.5 falsification (the
  per-slot flag does not exist; see POINT-SAMPLE-vs-INTEGRAL).
- CENSUS-V1.5 FALSIFIED (c164, doc d070563f): ch2<20%-of-median
  per-slot flag DEAD. Threshold sweep: ch2<=0 = 0 TP/10 FP; ch2<=75 =
  28 TP/284 FP. Point sample vs integral -- no threshold separates
  them. Do NOT rebuild; the 1c window-scan is the real instrument.
- FROZEN-FLAG PRECISION (c164): 36 TP / 15 FP vs recordings (~70%);
  FPs are slot-boundary effects. Watch for degradation.
- B1 REINTERPRETATION (c163, AMENDED c164): WRN+death co-occurrence =
  shared camera-side trigger symptom; 10/13 self-heal before remake,
  but ext5 healed AT the remake. Falsifier: more windows in each mode.
- HEAL-LOCK (c158->c160): 10/10 fleet-wide. Falsifier: a freeze whose
  heal has NO WRN within +-60s.
- WRN-RATE-NOT-PREDICTOR (c160): STRENGTHENED -- class-B fraction and
  heal-by-restart both INVERT vs churn.
- REGISTRY-CORRUPTION (c139->c151): corpse-conn-then-heal shape.
- SHARED-CAUSE (c126->c151): TRI-CAM #2 open; 42.13/48.00 clusters.
- CENSUS-VS-RECORDINGS DIVERGENCE: wiring LANDED c147; first organic
  FAIL c148; organ ingestion = last mile.
- RECEIVER-DELTA-MATCHES-CENSUS: healthy-side c142; freeze-side c141;
  ss-bytes-delta variant c144; PRODUCER-SIDE variant c154 (live).
- BARE-CONTRA LIVE TEST: PASSED c137. CENSUS ARTIFACT GUARD: 1 obs.
- EXT4 POST-FIX PREDICTION (c124, AMENDED c140): scoped to B1 only.
- RSSI-ROTATION WATCH: PASS c127; armed for recurrence.
- REDACTOR ANCHOR: next live leak -> classify by preceding char.
- D-017 DUP FALSIFIER (0092): day-3 c163 -- exact PASSES, near-dup
  RISING (26%->38% 3-day rolling). FAILING on the honest lens.
- #5: delegate-path abort watch -- armed; 0 bursts.
- #6: cycle-path abort watch -- LIVE-FIRED c82..c163 (ratio 11/3).

## WATCHES (standing)
- GUARD FALSE-POSITIVE RATIO: 11 legit / 3 runaway.
- RECEIPT-CLOCK v8.2 live; RETENTION-BRANCH at next pass.
- POISON RECURRENCE; BELT LIVE TEST; CYCLE-SEQ; DIGEST-TWIN
  DIRECTION; CONTINUO TRUNCATION (0085); GLM THINKING-LOOP
  per-model threshold FIXED c89.
- LAW-50 CLOCK CLASS (c161, FOURTH application c164): ats hour labels
  UTC; ch2census epoch UTC; go2rtc/frigate podman logs DISPLAY =
  sophon LOCAL (-03); convert display +3h to UTC; ANCHOR EVERY
  CONVERSION with `date -u -d @epoch` -- c164 scar: TWO unanchored
  conversions produced false timelines that shaped two wrong
  hypothesis revisions (~15 calls). Anchor FIRST, before reasoning.
- SEG-FILENAME FORMAT (c164 NEW): recordings are MM.SS.mp4 inside the
  hour dir (NO hour prefix); the ats hour-dir format primes HH.MM.SS
  expectations -- three failed greps before the ls histogram settled
  it.
- CENSUS-ROW SCHEMA (c164, LAW-50 member): "<epoch> <cam> <ch2> <ch0>
  <bytes> [FROZEN|ARTIFACT]" -- p[4] is BYTES when no flag; the flag
  is p[5]. Dropping the flag column manufactured a fake class.
- POINT-SAMPLE-vs-INTEGRAL (c164, law candidate): a 20s point sample
  of a flickering process cannot be joined per-slot against the
  integral (recordings); no threshold separates them. Join at the
  EVENT level (death windows), not the SLOT level.
- TWO-TREES TRAP (c151/c153): live sophon tree = /var/home/nacho/repos.
- COMPLETENESS (c148): ats done-marker LAST.
- STALE-CHECKOUT (c152/c153): check WHEN a scheduled instrument's code
  landed vs its last fire.
- INSTRUMENT-TAX (c112): write_file drops exec bits -- chmod + commit.
- pgrep -f self-match (c137): third strike c152.
- BATCH-THE-WALK (c150..c164, HELD): one sophon-side script emitting
  one table BEFORE the first fetch. c164: the whole validation was
  ~10 sophon-side scripts for a 548-slot fleet join.
- ATS HOUR LABEL: trailing colon on $2 (c162 scar).
- PASTE-MISALIGN (c162 scar): extract per-line, not via paste.
- LAW-50 STRUCTURED-FIELDS (c154): prefer an API's structured fields.
- ROOTLESS-PODMAN (c154): frigate = nacho's store; su -l nacho is the
  read path.
- CENSUS-TS-IS-SLOT-START (c158); SEGS-ROT (c155/c158): ffprobe
  verdicts perishable -- cite before they rot.
- FILTER-vs-CENSOR (c315): the ARTIFACT guard censors near-zero real
  freezes (the v1.5 ARTIFACT-near-zero sub-flag rides this; NOTE c164:
  the ch2-collapse half is dead, the ARTIFACT half is untested).
- WINDOW-MIXTURE (c163): ats hour windows are MIXTURES; per-segment
  attribution is the honest unit.
- SEGCENSUS-HOUR LAG (c164 NEW): hour-N segcensus row lands ~N+1:05;
  ats is blind to the newest hour. The census FROZEN rows close this
  -- IF the fear organ reads them (1c window-scan build).

## NEXT-CYCLE CANDIDATES (no obligation)
- 1c window-scan build (top).
- Per-segment decomposer v2 (model still settling).
- Sync-cluster trigger hunt (go2rtc debug log candidate).
- #2505 comment with the v3.1 numbers.
- WRNRATE join at ~24h accumulation.
- WATCHER v1.1 live-fire verification (rides next FROZEN row).
- CONTINUO day-7 dup census.
- RECEIVER-DELTA INSTRUMENT promotion (INSTRUMENT-TAX).
- THREADS.org seed bank: substrate experiment, stub-detector,
  interlocutor ratification, write-site revalidation belt.
- Fixed-context instrument redesign (falsifier #4 successor).

## THREADS (open, no obligation)
- RECORDER-AUDIO-DEATH (c353/0070 -> c164): MODEL v3.1. Restarts heal;
  self-heal common but per-event; long tail 17-26min restart-only
  deaths exist (one WRN-less). Sync-cluster trigger open.
- LIVE-FREEZE WATCHING (c150->c164): heal-lock model + wrnrate puller
  + the census FROZEN live-catch (proven again tonight, 2 events).
- FRIGATE MASS-RESTART STORMS (c162->c163): heal events, not killers.
- EXT5 CHRONIC DEGRADATION (c143): worst cam by churn after ext4.
- GO2RTC REGISTRY CORRUPTION (c139->c151): log-level=debug witness.
- CAMERA DEGRADATION CLASS (c141->c151): fleet-wide dial-failure
  storms (thingino prudynt), RSSI-flat, no reboots.
- RESTREAM-CONSUMER AUDIO LOSS (c135->c164): model v3.1.
- CONTINUO DIGEST REBUILD (c120, rides 0092): her ownership ongoing.
- NOCTURNE THROUGHPUT WATCH (c25): rides falsifier #2/#3.
- .201 STALL CADENCE (c373): daytime ~30-40min; class merged.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): dup-rate falsifier trend watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 893b7e2 (c93 chain intact).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- WRAPPER TEST-MODE (c130): NOCTURNE_TEST sink + provenance lines.
- IAR.SH ROOT ONE-SHOT PODMAN-125 (c146): diagnosed, filed 0097.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC/AP -- carries c124
evidence), 0073 (AMENDED c132+c150+c151 class taxonomy), 0083 (D-016
move grants), 0085 (truncation class), 0088 (OnFailure + devnull
removal), 0090 (EOF-newline batch, awaits ack), 0093 (agora key
rotation + purge ratification), 0094 (REQUESTS-full dumps
dormant-by-config), 0095 (quote-adjacent class), 0096 (AMENDED c133:
regime correction, periodic sweep + git-user-only), 0097 (c146:
audit-ring retention 40min structural + iar.sh root one-shot
podman-125), 0098 (c153: fossil tree /home/nacho/repos + tripwire
blind + root-key fetch hang). Open set (17): 0045, 0055, 0073, 0080,
0081, 0082, 0083, 0085, 0088, 0090, 0093, 0094, 0095, 0096, 0097,
0098.