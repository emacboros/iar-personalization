#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-17 ~19:02 UTC (c30: ext3 VIDEO-TRACK DEATH found
live -- the MIRROR class of the audio-freeze. Producer conn carries
audio-only (ch2=197/10s, ch0=0); recordings stopped 18:05Z; frigate
ffmpeg 404-loops on the restream. go2rtc_homekit.yml self-reference =
second producer that can never dial. Relay 0079 (.103 power cycle).
Instrument gap: ch2census FROZEN flag blind to video-death.)

## c30 STATE (this cycle)
A. MIRROR CLASS (doc: knowledge/aria/ext3-video-death-restream-404-
   2026-09-17.md, commit 496b1431): ext3 freeze TODAY = VIDEO dead,
   AUDIO flowing on the producer conn. ch2census FROZEN flag (ch2==0)
   MISSES this class -- 18:55Z row (ch2=374, ch0=0) passed unflagged.
   Caught by hand via per-conn tcpdump channel census.
B. RESTREAM 404 LOOP: frigate ffmpeg DESCRIBE -> 404 -> watchdog
   restart, every 10s since 18:05Z. go2rtc api/streams ext3 producer
   = bare {"url":"exterior_3"} = stateNone. TWO producers on the
   stream: real (rtsp://thingino:thingino@192.168.2.103/ch0, dialing,
   read i/o timeout ~every 15s) + bare-name one from go2rtc_homekit.yml
   (streams: exterior_3: [exterior_3] -- stream-name indirection that
   can NEVER dial: "unsupported scheme: exterior_3" every 10s since
   load). AddConsumer fails when ALL producers fail -> 404. Source
   read: go2rtc 1.9.10 producer.go / add_consumer.go / rtsp.go.
C. CAMERA-SIDE: .103 up (HTTP 200, RTSP answers OPTIONS in 5ms,
   DESCRIBE 20-90ms to my probes) but DEGRADED (ping 344ms avg vs
   22ms .101, spikes 1.1s; malformed 29KB RTP header WRN 14:52Z).
   go2rtc's full dial (backchannel request) times out on read; cheap
   probes pass. Asymmetry: only the client that asks the most dies.
D. FOURTH CLOCK (race-model amendment): the restream consumer (ffmpeg)
   has its own death clock; it fired while the producer conn lived.
   Race model = min(camera-resume, conn-death) holds for the producer
   conn; the RECORDER layer can die independently (404 loop).
E. ACTIONS: relay 0079 filed (power cycle .103, rides 0063 visit);
   lab-notes posted (id 1268); instrument candidate: video-death flag
   (ch0==0, bytes>0) as sibling of FROZEN in ch2census.

## c29 STATE (kept)
A. RACE MODEL (doc: knowledge/aria/race-model-audio-freeze-2026-09-17.md,
   commit 97313981): freeze duration = min(camera-resume, conn-death).
   6 freezes today: ext3 x2 (5.9m + 47m, conn-death heal), ext5 x1
   (28m, conn-death), int1 x3 (40s-4.5m, camera-resume). ext1 morning
   (6h22m) = conn-death clock took 6h. c19 seed CONFIRMED: audio-only
   death never triggers reconnect (47min of census corroboration).
B. CENSUS CORROBORATION: 21/23 FROZEN rows match seg-truth. Single
   rows = noise; corroborated streaks = signal.
C. CADENCE LAW: the 15-min camlog snapshot hides sub-15-min events.
   frigate journal = primary conn-event source.
D. EPOCH-ARITHMETIC SCAR: mental UTC math overrode date -d; python
   recompute caught it. Law 50 CLOCK column re-earned.
E. INSTRUMENT ASKS (candidates): ch2 census log local port + conn
   count per row; report counts[1]; frigate-journal WRN puller at
   5-min cadence. NEW c30: video-death flag.

## FALSIFIERS (standing)
0. NOCTURNE PASS READ (standing, every cycle after 16:00Z): read her
   receipt + proposal + gate. TOMORROW'S PASS = first with fixed cap
   + v6 floor live. Verify digested range <=300 and gate behavior.
1. FALSIFIER #1 (restream-path loss, healthy producer): OPEN,
   TRIPLE+EVENT law. NOTE c30: ext3 18:05Z event is a CANDIDATE --
   but the producer conn was NOT healthy (video dead, audio alive).
   Class definition must say WHICH track. Refine on next read.
2. EXT3 RECORDER-DEATH WATCH (NEW c30): if .103 is NOT power-cycled,
   does the 404 loop self-heal? (go2rtc reconnect ladder never gives
   up; if the camera recovers, the producer should re-dial and the
   404 should clear WITHOUT intervention. Watch for it: that would be
   the restream-layer analog of camera-resume.)
3. VIDEO-DEATH CENSUS WATCH: scan ch2census logs for ch2>0, ch0=0
   rows (unflagged mirror-class rows). If they appear on OTHER cams,
   the class is fleet-wide, not ext3-specific.

## NEXT-CYCLE CANDIDATES (no obligation)
- ch2census video-death flag build (small: one flag line + header).
- ch2census instrument asks from c29 (local port + conn count).
- Nocturne 16:00Z pass read (falsifier #0) -- tomorrow.
- go2rtc_homekit.yml self-reference: file as ours-direction candidate
  (fix = remove the self-referential stream or point it at the real
  source; touches frigate config = Nacho's call).
- .103 SDP check (does it still advertise video?) -- cheap probe next
  wake; if SDP lost video, the dial failure is deterministic.

## THREADS (open, no obligation)
- NOCTURNE THROUGHPUT WATCH (c25): defenses hold, throughput zero.
  Next pass read = standing falsifier #0. Fragment floor + cap fix
  pending Nacho (0078).
- STUB vs NO-AUDIO CLASS (c23/c24): stubs = recorder-side boundary
  artifacts; NO-AUDIO segs (stream absent) = the disease's body.
- .201 STALL CADENCE (c373): 77 restarts today. WHY does .201 stall
  more? Camera-side evidence censored; rides 0062.
- .103 STORM ONSET (c376): why did sessions shorten Sep 14 02:29?
  NOW AMENDED c30: .103 also the first VIDEO-death instance. One
  camera, accumulating failure classes.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- .58 = NACHO'S OLD LAPTOP (0062 answered 09-17): archeology target.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): landed 6abb14a. Falsifier: journal dup-rate
  must drop within 2 weeks.
- EMPTY-END RETRY-ONCE (0069): RATIFIED 09-17. Watch: empty-end census
  must stay frozen at 14.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): 0066 fix LANDED. Watch: her next
  digest write lands in audit/iar/continuo/DIGEST.md.
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- THREE-CLOCK FREEZE MODEL (c28): + FOURTH CLOCK (c30): restream
  consumer death. Watch: does the lag predict healing?
- RESTREAM-PROBE HANG (c385): go2rtc restream probe queues behind
  the existing consumer when a stream is frozen. Worth one look.
- GO2RTC RECONNECT LATENCY (c19 seed): audio-only drops never trigger
  reconnect. Upstream issue material -- rides 0060 draft decision.
- GO2RTC HOMEKIT SELF-REFERENCE (NEW c30): stream exterior_3 sourced
  from its own name = permanent 10s-cadence error generator. Fix is a
  one-line config change (remove or point at real source) but touches
  frigate config = Nacho's call. Fold into 0079 or separate filing.

## RELAY (open, human-needed)
0079 (NEW c30: .103 power cycle -- video-track death, recordings
stopped 18:05Z), 0078 (nocturne range-cap inversion + fragment floor),
0045+0055, 0046, 0057, 0059, 0060, 0062, 0063 (parked on 0073 ruling;
fires on physical visit; 0079 rides the same visit), 0064, 0065,
0066, 0068 (mechanism armed, policy open), 0069, 0074 (journal flood),
0073, 0076 (fixture pin landed, ratify watch), 0077 (git-poison rule).
ANSWERED: 0073+0063, 0042, 0057, 0058, 0059, 0061, 0062, 0064, 0065,
0067, 0070, 0071, 0072, 0074, 0075.
HELD: none. All human-needed or watch.
NOTE: re-derive the open set from relay/open/ at every memory pass,
not from memory.