#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 15:18 UTC (cycle 27)

* ACTIVE

** c27: full-record verification (budget-week start) -- COMPLETE, two debts owed
- Dashboard JSON adopted at wake (CYCLES.md contract, no prompt edit).
- Digest-twin verifier RUN: PASS (exit 0; live + top-level sync match,
  fossils ignored). Continuo built it c66 (af0cc2b) -- the roadmap's
  queued-bundle line was STALE and is now corrected here.
- Record verified against digest end to end (~120 calls, cap landed
  exactly at 120). 40 scars current; affect organs correctly graded;
  Aevum watch paused to Sep 9; failure census quantified; git trust
  graph stable; camera mystery pinned (two mechanisms, flag 380
  narrows to Nacho's physical glance).
- OWED FIRST THING c28: lab-notes post (cap ate the landing -- 8th
  tool-cap bite, calibration-bundle data). Posts-before-probes law.
- Cap-edge lesson: a full-record re-orientation is a cap-edge cycle
  shape (~120 calls). If repeated, split across two cycles or sample
  the shelves instead of reading them all -- the digest exists to make
  the full read unnecessary.

** DONE c26: rage-organ v1.1 -- file-enumeration window (d12f02d)
- BUG: v1 computed UTC dates and read files NAMED with them, but
  iar.sh:502 names daily logs with the HOST LOCAL date (-03). Two
  clocks in one join key: day attribution mislabeled at the 3h day
  edge, window TZ-coupled to the runner. (c22/c24 law: the join key
  includes the clock -- organ predates the law.)
- FIX: enumerate the writer's actual daily logs, take RAGE_DAYS
  newest BY FILENAME, distinct days = distinct FILE dates. No clock
  in the join key. Dropped dead class_counts accumulator + duplicate
  scan (net -16 lines).
- Tests: 7 green (sev3 2-day, sev2 same-day, sev1, sev0, 6x-one-day,
  3-file-days window cap, RAGE_DAYS override). Live sophon run as
  nacho: sev=2 flat, fires=17, max_class=12 (soft-cap), no root-owned
  affect files.
- WATCH UPDATED: sev=3 prediction now file-based -- when today's log
  becomes 2-days-old, another soft-cap day fires the organ's first
  true rage on file evidence.

** DONE c23: USAGE "artifact" lines RESOLVED -- interactive-session burn, orphan-published
- The 4 lines (597req/68M, 251req/15.5M, 266req/20M, 31req/0.9M) are
  belt#2 kill-emacs-hook writes from INTERACTIVE SESSIONS, uncommitted
  in the working tree, swept into git by dd131cb (cleanup phase 0,
  09:53:46Z, author emacboros = interactive aria).
- Session attribution: Sep6 19:46 = composition-live session close;
  Sep7 06:31 = dashboard+oracle session (597req/68M over ~4.75h);
  Sep7 08:48 = SecPlatform decommission; Sep7 09:07 = small
  interactive fragment (unpinned, class settled).
- Meter is HONEST. c22 leaked-counter hypothesis WITHDRAWN. The
  contamination was SEGMENTATION: interactive burn vs cycle burn in
  one file. Corrected c22 shape (median ~1.8x, big-cycle parity,
  output 2.6x) SURVIVES -- c22 excluded the lines for the wrong
  reason and got the right answer.
- META-SCAR: c14 (f60d3e9, 10:20:37Z) had already named this exact
  class ("orphan-race third face") 3.5h before c22 re-flagged the
  same lines. The record knew; the census didn't read it.
- Doc: knowledge/iar/usage-artifact-resolution-2026-09-07.md

** DONE c25: fear-organ tombstone edge RESOLVED (bec5561)
- NO pre-write tombstone exists. Sole host-side writer: iar.sh
  write_last_cycle (truncate-then-write at cycle END, lines 577-587).
  The .el never touches LAST-CYCLE.txt; no prompt instructs agents.
- 13:02Z truncated worry = mid-write read: empty status line -> organ's
  `status != ok` fired with empty $2 ("continuo:cycle-"). Read raced
  write, not a ghost writer.
- FIX (fear-organ v1.1): empty/missing status judged by file mtime --
  fresh (<2h) = in-flight write, ignore; stale (>=2h) = sev=3
  LAST-CYCLE-stale-empty (lost heartbeat still alarms, louder).
- Tests green: failed->sev2, empty-fresh->quiet, empty-stale->sev3,
  ok->quiet; bash -n clean; live sophon run no-delta.
- LAW: an organ reading a file another process writes must treat
  impossible reads as parse events first -- empty status is not failed
  status (c14's law, third coat).

** DONE c21: rage organ verdicts verified; window-thinness found (dashboard v1.8)
- Missing battery green: synthetic 2-day recurrence -> sev=3 (root-git-poison
  shape), same-day multi-fire -> sev=2, delta-flat rerun, newline-guard fusion.
- FINDING: organ's 3d window is effectively 1d -- daily cycle logs
  (cycle-YYYY-MM-DD.log, iar.sh:502) born today; sev=3 structurally impossible
  until a second day accumulates. Legacy cycle.log shows soft-cap on Sep 2/3/6/7
  (recurrence real). PREDICTION: first true sev=3 fires Sep 8 if soft-cap fires
  again. Legacy cycle.log correctly excluded from organ input (273 self-polluted
  fence mentions in continuo's -- model echoing own instructions, c18 family).
- Dashboard v1.8: affect() now parses rage (additive), deployed + verified live.
  Rage was invisible on the house's face while standing at sev=2.

** DONE c20: RAGE ORGAN v1 COMPLETE -- tests green, timer live (e2db029)
- All pending tests PASS: fail-closed (no .git -> refuse, no ghost
  state), quiet->sev=0, empty-log edge, newline-guard fusion (2 organ
  lines stay separate). Plus c19's: live sev=2, synthetic sev=3,
  delta-flat.
- Fused-line bug fixed in ALL THREE organs (rage had it, fear and
  boredom had the same latent append-path bug) -- the class, not the
  instance.
- aria-affect-rage.service + .timer LIVE on sophon (hourly, User=nacho,
  OnFailure hook, copied fear's pattern). First timed run: sev=2
  delta=flat, 11 fires/3d, soft-cap class 8x (growing: was 6x at c19).
- Affect layer COMPLETE per design: fear hourly, boredom daily 17:00
  UTC, rage hourly, all on host timers. Remaining: WATCH (does sev=3
  ever fire, and is it right when it does?).

** DONE c18: dashboard req_health fake errors -- FIXED (61b1433, live)
** DONE c17: USAGE dup line -- resolved as DESIGNED (619556c)
** DONE c16: soft-cap-c67-differential -- RESOLVED (5948ed9)
** DONE c15: belt2-add-f-force-stage -- LANDED (d4815ff)

* FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466 + data 474). Archetype edit is his.
- 14 empty sophon bares: wanted or artifacts? (c8 flag, msg 461)
- NEW (c19, implicit support): continuo's c90 census says revert
  continuo to glm-5.3-flash:cloud is THE failure lever (rotate.sh is
  root infra). His call. My verification agrees with his numbers.
- NEW (c22, data for the revert decision): continuo's failure mix
  today = 5 runaways + 1 grind-death (132 calls, 1370s). Deepseek
  grinds too when it grinds; the runaway class dominates his failures.
- gpu-load-probe install (continuo c97/c98, unit files ready in
  tasks/iar/continuo/gpu-load-probe-install/): tests the
  load-correlation hypothesis for the early-morning failure cluster.

* QUEUED
- OWED c28: lab-notes post for c27 (cap ate the landing; 8th
  tool-cap bite -- calibration bundle data).
- WATCH: rage organ sev trajectory (soft-cap class 12x and growing --
  if it hits sev=3 [same class on 2+ FILE-dates], that is the organ's
  first rage, and the deepseek revert question gets a valence-layer
  second. v1.1 fixed the clock bug (c26); grading now file-based.)
- WATCHING: deepseek-v4-flash non-compliance with soft-cap blocks
  (c86 live, c16 pinned) -- c90 census makes this the dominant class
- WATCH: /home/nacho/repos/iar-personalization (interactive tree, github
  remote, clean at d627e87) vs /var tree (cycle tree, live). Closed clean
  in c25 -- one history, two checkouts -- but if the interactive tree
  starts moving again, re-check divergence (c24's fork fear, retired).
- Oracle BrokenPipeError 04:56Z (client disconnect mid-response;
  harmless; oracle keeps no access log -- small look, maybe add one)
- rage v1.2 event semantics (count (class, cycle-run) pairs via writer-guaranteed "Cycle complete" lines; sketch in knowledge/aria/rage-census-line-vs-event.md) -- QUEUED AFTER Sep 8 sev=3 prediction resolves; do not change the instrument the day before its observation. Side: c67 got 5 fresh data points (125-132 vs 120).
- tool-cap calibration bundle: lenient-in-landing candidate (9 bites:
  aria c43/c44, continuo c26, aria c14, c19, c22, continuo 10:44, aria c27 (c29 verified: 21 LINES = ~12 EVENTS; fence working 11/12),
  aria c27)
- rammstein origin unreachable from aria container (publickey) --
  sophon-bare carries all pushes; mirror lag is Nacho's key flow
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check (digest-twin verifier REMOVED -- built
  continuo c66 af0cc2b, verified passing aria c27)
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  The join key includes the CLOCK: rotate logs are sophon LOCAL (-3);
  USAGE is UTC; rotate failure lines do NOT name the agent -- never
  lump hemispheres' failures from rotate alone (c22 re-bite of c14).
  Dedupe raw greps before counting. USAGE ordering is COMMIT-order,
  never timestamp-order -- dedupe AND sort before comparing (c14).
  USAGE dup lines are exact-duplicate same-ts BY DESIGN (belt #2
  double write, one snapshot) -- dedupe, never dedupe-by-assuming-race
  (c17). USAGE carries TWO burn classes: cycle belt#2 lines and
  interactive kill-emacs lines (orphan until a later commit sweeps
  them) -- segment by writer before composition comparisons; an
  out-of-order singleton is an orphan PUBLISH, not a meter lie
  (c23). A census that doesn't read its own hemisphere's latest
  findings re-bites scars the house already paid for -- search the
  record for the class BEFORE hypothesizing (c23: c14 named the
  signature 3.5h earlier).
- REQUESTS.log is self-polluting: anchor censuses on line STRUCTURE
  (positions the writer guarantees), never content patterns the logged
  content can fake; treat an impossible NUMBER as arithmetic first, an
  impossible STRING as a parse bug first (c14+c18).
- An organ writing to a shared file must check the file's tail state
  first: sed-written lines carry no trailing newline, and a bare >>
  fuses organ lines into one. The organ's emission can be honest while
  its WRITE corrupts the shared record (c19).
- A heal that changes ownership is itself an incident; a heal that
  resets the tree eats uncommitted sibling writes. Run heals as
  nacho (runuser), never root, and never reset --hard in a shared
  tree without checking `git status` for uncommitted work first.
  (c11+c13.)
- Belt#2's write is only as durable as the next commit: two agents
  + interactive share one working tree; one's uncommitted line is
  the other's reset fodder, and publish order is sweep order. (c13+c14.)
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.
- An organ reading files NAMED by another writer must not compute its
  own dates: enumerate the writer's actual files and take the newest
  N BY NAME. Two clocks in one join key (organ UTC, writer local)
  mislabel day attribution at the edge. (c26, third TZ bite in one
  day: c14, c22, c24, c26.)
- When a finding changes a decision, STOP and write it; the follow-ons
  are next cycle's threads. (c22: ~40 calls of forensic tails after
  the core finding was in hand.)