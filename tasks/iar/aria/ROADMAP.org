# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-10 ~03:49 UTC (cycle 146)

## NOW

- c146: EYE SWAP LANDED + LIVE-FIRED (a47f7b8d, 3d8ec0fe, 2c7c5c30,
  7a696201). qwen3.6:35b-a3b replaces gemma3:4b in all 6 callers;
  every caller verified on qwen (frontend 2/2, eye-check 8/8, fleet
  MATCH, mouths 2s warm, identity-watch OK after b64 + think:false
  fixes). RESIDENCY FINDING: resident qwen = 537% CPU + 24GB RSS,
  frigate ext3/ext4 starved into watchdog loops; per-request
  keep_alive:300 landed on all callers (batches share one load,
  5min idle -> unload). Frigate restart healed the 00:01-00:44
  loop (standing remedy). As-built:
  knowledge/aria/eye-swap-as-built-2026-09-10.md.
- c146: fear-organ v1.2 LANDED (0a1062ce): the c121 fleet wiring was
  a PHANTOM LANDING -- unit still passes "", claimed staleness
  branch never committed. Fix in the organ: self-feed from
  /var/lib/aria-fleet/fleet-latest when $1 empty + >26h = sev=1
  fleet-stale. 4 paths live-fired. fear-fleet-wiring.md carries the
  CORRECTION section (window clause per law 27).
- c145: fix-log window-field rewrite LANDED (fcf47b25). Rage organ
  parses the new format (verified live this cycle: events=18,
  dom_class soft-cap, fix-day logic correct).
- c143: misattribution fix LIVE (7b837c1).

## NEXT (in order)

- 1. AUDITD WATCH (aria-0022 ratified): auditd rule on the audit
  tree so the next root-owned-file poisoning names its writer.
  Host-side install (aria-reversible class, D-011).
- 2. REMOTES -> git@ (aria-0023 ratified, option c): cycle
  push/fetch remotes point at git@10.66.0.5 (git-shell), retire
  root-ssh for git ops. SCAR RE-INSTANCE c146: root-ssh git pull on
  the sophon checkout left .git/ORIG_HEAD root-owned again (healed;
  the remotes fix kills this class at the root).
- 3. AFFECT-DASHBOARD LAW (aria-0024 standing addition): every
  affect must surface on the dashboard. Rage is parsed (v1.8
  affect()) but RAGE LOG is not in the oracle context add() list
  -- add it alongside fear/boredom.
- 4. aria-0025 GATE (ratified with conditions): run-tests.el
  redirects iar-audit-path/iar-personalization-path to temp dir.
  .el change under the new gate: filing-approved, suite green,
  delegated reviewer before push.
- 5. aria-0026 DECISION-WAKE (ratified): continuo world-state
  digest block + archetype protocol line (read DECISIONS.org tail).
  Prompt-file edits = Nacho's (nacho-identity) -- file the exact
  line via relay, or land in next interactive session.
- 6. 0/0 TOMBSTONE POLICY (aria-0026 ruling): tokens_out=0
  text-only cycle-end = anomalous (tombstone + non-zero exit).
  .el change, same gate as 0025.
- EYE SWAP FOLLOW-UPS (from c146): (a) watch the 09:30 eye-feed's
  first SCHEDULED qwen run (cold load ~20s, verify report refresh);
  (b) ear-check flapping-camera noise: newest-segment sampling
  gives ~0.7%/run false-FAIL on exterior_3-class flaps -- consider
  a 2-segment sample before FAIL (instrument change, next cycle);
  (c) eye-check has no think:false -- add if empty reads appear.
- CONNECTOME v2.1: inter-agent edge layer from citizen REQUESTS.log
  PARSE specs=delegate; edge to :agent or agent-assistant. audit.log
  delegate lines stay quarantined (c142 rule: no agent= detail =
  misattributed, exclude).
- FIRE WATCH verdict 09-11 (armed from turn 541).
- aria-0027 (filed c138): lucid_curie removal -- Nacho action, open.
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- Parked (hypothesis): aria USAGE.log out-of-order lines (14:23:28,
  15:23:36 after 19:19:34) -- suspect c91 forensics re-appends.
  Non-urgent; c14 dedupe handles same-ts pairs, out-of-order is
  a different artifact.

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 when an alarm and the primary log disagree, audit what the
    alarm READS -- the file can be the corrupt instrument. And:
    host-root writes into the uid-1000 container's tree are
    cross-boundary contamination even when they look like cleanup.
21. c137 a mechanism assembled from plausible parts is a story, not a
    finding. When the writer is unidentified: say so, instrument the
    path, wait for a live catch. And: test the fix's own failure path
    before shipping it -- a fix that cannot run is a silent no-op
    wearing a commit message.
22. c138 before declaring an instrument stale, check which clock it
    reads. And: cheap falsification -- one probe, 13 seconds -- beats
    assembling a mechanism from plausible parts. A mount's NAME is not
    its contract.
23. c140 an audit log is primary evidence only if its ATTRIBUTION
    survives async boundaries. The agent name must resolve from the
    buffer that OWNS the request (FSM :buffer), never from whatever
    buffer happens to be current at completion. And: a census built on
    a misattributed log is wrong in exactly the row it exists to make
    visible -- quarantine the corrupt rows before building on them.
24. c141 log forensics is ONE awk pass, not twenty greps: a census
    without a query design is an iterator pattern, and the same-tool
    fence is right to fire on it. Ask the targeted question first;
    enumerate only when the question IS the enumeration.
25. c142 a missing detail field is itself evidence: a delegate audit
    line without agent=/task= is not a sparser version of the truth,
    it is the signature of the bridge running outside the dispatch
    context. Shape of the row tells you which code path wrote it.
26. c143 identity must travel with the REQUEST, not the buffer that
    happens to be current at completion: resolve the agent from the
    FSM's :buffer (the conversation buffer that owns the request),
    never from current-buffer in an async completion path. And:
    restore the parent's identity BEFORE the callback that hands
    control back, not after -- everything the callback touches sees
    the world as you left it.
27. c145 a fix-log entry is a CLAIM ABOUT A WINDOW, not a standing
    fact: every entry carries an explicit WINDOW clause naming what
    is retired and what still counts. Prose that outlives its window
    ages into a lie (2 instances: c135, c144). The organ reading the
    file and the prose describing the fix are different instruments;
    keep the prose honest to the same day-granularity the parser uses.