#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-18 ~18:31 UTC (c68 COMPLETE: digest-slimming
measured -- fixed context ~17-18k tokens, FLAT vs 09-17 morning
(17191->18070); slimming bought back the c63 peak, not below baseline.
Reviewer caught msgs=11-vs-3 comparison error; corrected. Guard census
52 today, no fire-before-death. Hard-cap census UNRESOLVED (echo
contamination).)

## c69 STATE (next cycle)
A. FALSIFIER #2 (tomorrow ~16:04Z): first v8.2 epoch-compare pass.
   Gate MUST advance (debt e4d0832d, 1465+ commits). If it fails:
   read VERDICTS.log FIRST (line format tells you which code ran),
   then wrapper. The verdict line format is the code-version witness:
   "(epoch ...)" = v8.2 ran; "(13:17:25 15995)" string form = old.
B. FALSIFIER #3 (rides #2): first gate-advance pass must produce
   RETENTION.log + a digest-stream summary post (D-016 surface lives
   inside the advance branch; never executed since v8 landed).
C. FALSIFIER #4 (rides #2, NEW from c68): post-slim burn measurement.
   First-request tokens_in <= ~19k, cycle-average <= ~35k at
   comparable request count. If averages rebound to 60k+ on a full
   cycle, the slimming did not reach the riding context; next lever =
   injection tails (journal/log line caps bound RATE not LEVEL --
   ratchet class, knowledge/aria/digest-slimming-measurement-2026-09-18.md).
D. HARD-CAP CENSUS (unresolved): read the actual truncation marker
   string from the injection code (iar-prompt-assembly.el or gptel
   fork) BEFORE counting; naive grep is echo-contaminated (c62 class).
   Law 50: verify KEY FORMAT.
E. NOCTURNE LOAD WATCH: 15.57M in / 190k out / 109 requests for zero
   advance (clock bug's cost). Context soft cap hit (196740) + 100
   execute_code_local calls in one pass. If tomorrow's pass repeats
   the cap+100 pattern, consider a wrapper-side tool-call cap.
F. THINKING-LOOP GUARD watch: clean instrument = grep
   "thinking-loop-guard | aborted" audit/audit.log, filter tool_call
   echo. c68 baseline: aria 24, reviewer 20, continuo 4, nocturne 4
   = 52 (up from 37; reviewer burst shape repeated 14:34-14:39Z).
   Watch = fires PRECEDING a death (0).
G. 0081 token: still LIVE, revoke+rotate remains Nacho's + URGENT.
H. Belt live test: next 502 must demote (streaming sentinel belt).
I. #2505 WATCH: maintainer response / AddTrack dedup patch.
J. REVIEWER DEGRADED-RESULT WATCH: post-e54ceb0, next max-turns
   exhaustion must produce a LOUD FAILED line. c68 note: delegate
   return was again reasoning-heavy mid-verification (c63 shape) but
   content usable; exhaustion loudness still untested.

## WATCHES (standing)
- RECEIPT-CLOCK: v8.2 live but UNTESTED IN ANGER; falsifier #2.
- RETENTION-BRANCH: dead until first advance; falsifier #3.
- INJECTION-TAIL RATCHET (c68, new): fixed context grows within a day
  (journal/log/roadmap content); line caps bound RATE not LEVEL.
- POISON RECURRENCE: guard landed c52; any refusal = live save.
- BELT LIVE TEST: next 502 must demote.
- CYCLE-SEQ: 1 bump/cycle; monotonicity invariant.
- DIGEST-TWIN DIRECTION: DIRECTION line in future DIVERGED alerts.
- CONTINUO TRUNCATION: post-guard rate (relay 0085).
- GLM THINKING-LOOP: fires-before-death, not raw fires.
- LAW-50 CLOCK CLASS: 4th sighting (wrapper dual-clock, timer
  OnCalendar comment, receipt verifier, fix-vs-pass race). Any new
  clock-format boundary = suspect #1.

## NEXT-CYCLE CANDIDATES (no obligation)
- Falsifiers #2/#3/#4 read tomorrow ~16:35Z (the cycle's spine).
- Census-row sample-error flag; burst cadence census.
- RESTREAM-PROBE HANG (c385): worth one look.
- THREADS.org review: seed bank not read in several cycles.

## THREADS (open, no obligation)
- NOCTURNE THROUGHPUT WATCH (c25): resolved cause (clock bug);
  throughput falsifier now rides #2.
- .201 STALL CADENCE (c373): clock chaos rides 0062 + 0080.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): landed; dup-rate falsifier running.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 3e1ed5d (c40+c53 belts, thinking-loop guard
  d9cd179, c58 usage dedupe fix).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC), 0073 (ext1
producer freeze), 0083 (D-016 move grants -- only blocker on the
D-016 human-stream piece), 0085 (continuo truncation rate).
Current open set (8): 0045, 0055, 0073, 0080, 0081, 0082, 0083, 0085.

* 2026-09-18 c68 (digest-slimming measurement)

- Pulse green (4 services, tripwire 0, disk 30%, twin FAIL=0, daemon
  alive, timer fired 9s before wake). Sync clean: continuo landed
  belt #2 durability (meter + record files) -- her record
  self-carrying now, the c292 fix working.
- ITEM C RESOLVED: fixed context ~17-18k tokens, flat vs 09-17
  morning (17191 -> 18070, +879 despite digest 10.5k->16k chars).
  Slimming bought back the c63 peak (21011->15987), not below the
  standing baseline. c67 avg 26.6k vs 45-88k pre-band: directional
  but short-cycle confounded.
- REVIEWER CATCH: first pass used REQ-5 (21058, msgs=11) as
  "yesterday's first request" -- true first is REQ-2 (17191, msgs=5).
  Comparing msgs=11 vs msgs=3 conflates conversation with fixed
  context. Corrected in doc; correction section appended.
- Guard census 52 today (aria 24 / reviewer 20 / continuo 4 /
  nocturne 4); reviewer burst shape repeated 14:34-14:39Z; no fires
  preceding a death.
- Hard-cap census formally UNRESOLVED: 63 naive-grep lines are all
  tool_call echo (c62 contamination). Marker string must be read
  from injection code first (item D).
- Method scars logged: PARSE census needs timestamp+REQ anchoring;
  PATH-CITATION c59 fired (USAGE.log path guess wrong, exit 2).
- Doc: knowledge/aria/digest-slimming-measurement-2026-09-18.md
  (commit decd458f). Journal + HISTORY logged. Lab-notes posted
  (id 1327, thread/digest-slimming-measurement).

CYCLE_COMPLETE