# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~06:20 UTC (cycle 114)

## NOW

- c114: FIRE-CENSUS thread. Continuo's daily census is a
  re-derivation ladder (c175 "5 fires" -> c179 "1" -> c181 "2" ->
  c182 "6, undercounted") -- an instrument that exists only as
  per-cycle improvisation re-derives its methodology every run,
  and the methodology has a known trap (c32 self-inflation: census
  greps echo their own terms into specs= text). My own recount
  stepped on the same rake: unanchored grep returned 73 "fires"
  across 16 bursts (2.39M "wasted" tokens) -- physically
  impossible (15 "fires" in 20s at 04:44 = continuo seeing itself).
  Real count: ~6 fires today, all 32768-tok halved cap, all
  deepseek-v4-flash (continuo's anchored post-c170 count stands).
  Fix = NEXT item 9. Dashboard-v2 board view: design read
  end-to-end, prepped as next build (data layer cycle-sized, UI
  wiring stays interactive per CYCLES.md).
- c113: REPO-GROWTH closed. continuo's cycle.log UNTRACKED
  (917ff7d). Mechanism: c117 (09-08 00:36, quiet pulse) force-added
  it once; once TRACKED, gitignore is bypassed for modifications --
  every continuo git_commit (add -A) swept the day's growth (76
  commits, ~847k lines, 4.1GB across blob versions). Fix = git rm
  --cached (file kept on disk, gitignore re-asserts). Verified on
  sophon checkout + both bare repos. History rewrite (shrinking the
  4.1GB) = Nacho-class call, not taken.
- c112: GPTEL FORK UPSTREAM-GAP AUDIT. All 6 fork-only fixes
  verified still fork-only AND all 6 failure classes LIVE upstream
  (checked 4799c80 file content). Invisible-turn regression tests
  4/4 green standalone. knowledge/aria/gptel-pr-candidates-2026-09-09.md
  (table + risk-adjusted PR order). Relay aria-0013 (nacho-external).
- c111: MKTIME 7-FIELD BUG found + fixed in connectome-snapshot.sh
  (73c975b). W37 silence + co-firing numbers known-bad; W38 =
  first trustworthy window. Analysis:
  knowledge/aria/mktime-seven-field-bug-2026-09-09.md.
- c110: qwen3.6 vision RETEST PASS. ollama 0.32.8 fixed the
  fitter-crash class for BOTH archs. Eye stays gemma3:4b.
- c109: muse-glimmer:30b battery DONE. Verdict NO for local
  retainer/resident (dense 27.9B, ~2 tok/s decode on 3080). Relay
  aria-0011 (nacho-arch ratification ask). Task open until Nacho.

## NEXT (cycle work, in pull order)

1. [DONE c110] qwen3.6 vision retest: PASS.
2. [DONE c111] connectome silence-column verification (found the
   mktime bug instead; W38 snapshot early next week).
3. Drills: #004 delegate only on natural need. Registry + queue live.
3b. gptel PRs: pending Nacho's answer on aria-0013 (nacho-external).
   If yes: prepare branch + PR text per candidate, order 970da80 ->
   7370286 -> 4c588a2 -> d8494f8 -> bcfd670 -> 8715a6c. Fresh
   upstream fetch required before filing (law 7).
4. Retainer build: spawn-file-spec.org drafted (c97); runtime parsing
   = interactive-session work (D-005). Interlocutor spawn model field
   = nemotron-3-ultra:cloud (allowlist ratified D-012).
5. Failure-window watch: gpu-load-probe windows live (CPU-correlation
   confirmed, load hypothesis dead per continuo c179-181; lever =
   model mapping, Nacho's call). Aevum pulse #4: Sep 14.
6. Digest #1 + Aevum pulse: Sep 9 (TODAY) -- retainer heartbeat is
   Nacho's; cycles prepare data if asked via relay.
7. Laptop origin artifact review with Nacho (0fc3bbc): pending his
   availability; salvage BEFORE reinstall/scrub.
8. [DONE c113] Repo-growth: continuo's cycle.log UNTRACKED.
9. FIRE-CENSUS INSTRUMENT (c114): add an anchored fire-census
   section to connectome-snapshot.sh (PARSE lines, v1.7-style
   anchored tail: error= stop= tokens_in= tokens_out= at line END;
   tokens_out > 20000 = real truncated-output fire). Ends the daily
   re-derivation ladder; methodology baked once. Cycle-sized.
10. DASHBOARD-V2 BOARD VIEW (D-013 addendum, prepped c114): add
    "board" field to aria-dashboard.sh python assemble block
    (task-tree digest: thinking/working/done, 5-10 entries, from
    tasks/iar/ tree + ROADMAP NEXT). Real data, try/except, bump
    version v1.8 -> v1.9. UI wiring stays interactive (CYCLES.md).
    Eye-verify after any UI change (D-013).

## STANDING

- Pulse: all green as of c114 (services active, tripwire 0, disk 29%,
  heartbeat alive leid=512, rotation healthy).
- sophon-bare i.ar.git at 6b67f61 (interactive fences landed by
  continuo c175, suite 1159 green). origin (rammstein) fetch fails:
  container key id_ed25519 is NOT in rammstein's git authorized_keys.
  Mirror hook direction is rammstein->sophon. Never compute divergence
  against origin refs.
- 14 of 20 sophon repos have DANGLING HEAD (HEAD->master, only main
  exists). Do not clone those by branch name.
- Container ssh note: /root/.ssh has id_ed25519; sophon accepts it;
  rammstein git does NOT.
- Diverged-pull protocol: stash -> rebase -> pop -> push. SCAR
  (c97): nobody-owned untracked file freezes rebase.
- audit/iar is GITIGNORED in personalization repo -- git add -f
  required for journal/HISTORY commit (scope: curated records ONLY,
  law 22; tracked-ness is sticky state -- c113).
- Zulip read API: narrow param URL-encoded JSON; .subject not .topic.
- Ollama sparse blobs: partial blobs pre-allocate; st_blocks is the
  written bytes (c99). Blob dir is /usr/share/ollama/blobs.
- gemma3 cold-load = 17-20s; affect organs' 20s curl timeout races
  it right after any ollama restart (c99, self-heals).
- Vision probe recipe (c104): b64 via ssh-interpolated python on
  sophon side; heredoc+JSON mangles base64 -> mtmd decode failure.
  c109 addendum: 8x8 PNGs are BELOW vision-tower minimum -- model
  confabulates "white" instead of erroring. Use 64x64+.
- Soft-cap economics (c101): 120 calls x ~35-60k mean ctx = 4-7M
  in-tok; the cap fires late. Batching is the lever.
- Ran-as law (c102): every organ/instrument logs WHO it ran as and
  which input-branch it took.
- gemma3:4b "mmproj" is NOT clip -- architecture=gemma3, whole
  model in one blob. Vision-tower comparisons: use qwen3.6 class.
- c109: muse+gemma3 CANNOT co-reside (4.75+2.88+Frigate 2.5 > 10GB);
  60s muse reload per alternation. Any muse-family future model must
  be <=7-14B dense to fit GPU-resident.
- c109: the ~2s POST cadence to sophon ollama mid-cycle is the cycle
  agent's own glm-5.3-flash:cloud turns (session-*.scope in journal).
  Instrument seeing itself -- do not re-investigate.
- c110: ollama keep-alives outlive the cycle that set them. Unload
  what you loaded; check `ollama ps` before assuming steady state.
- c114: convagent/mirror/testagent/otheragent/unknown rows in
  audit.log = TEST-SUITE artifacts (136 lines today), not agents.
  Connectome merge must exclude them (like nil rows) or the census
  counts the suite as a citizen.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
   c107 corollary: a remote that CANNOT be fetched (perm denied)
   serves a stale ref forever -- check fetch success, not just
   ref existence.
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
    c111 corollary: TZ=UTC is necessary not sufficient -- mktime
    takes EXACTLY six fields; extra fields shift the parse silently.
12. Anchor greps on line START ('] agent | tool_call'), never on
    substrings -- cmd= fragments pollute unanchored matches.
    c114 corollary: REQUESTS.log census greps must anchor the TAIL
    (error= stop= tokens_in= tokens_out= at line end) -- specs=
    self-echo manufactures fires (c32 law, re-learned by re-count).
13. Verify a "pending" roadmap item against the primary source
    before working it -- roadmap lines go stale.
14. Apparent size lies on sparse files; read the blocks (c99).
15. An instrument that cannot say who it was when it measured lies
    by omission -- ran-as + input-branch context (c102).
16. An instrument that fails for the wrong reason (escaping, not
    the thing measured) produces a verdict that is wrong with
    confidence -- validate the probe on a control that CAN pass
    before trusting its failure (c104). c109 corollary: a probe
    below the instrument's minimum produces fiction, not failure --
    validate the probe is IN RANGE (64x64, not 8x8).
17. Enumeration is what an under-stimulated instrument does: when
    the question is "what does the history look like", ask instead
    what the next cycle needs and fetch exactly that (c105).
18. Commit-derived activity is a lying instrument for cycle health:
    USAGE-only cycles and failure-heavy windows leave few commits;
    hour histograms undercount exactly the interesting windows
    (c105). Wrapper-exit is not cycle-health (c105).
19. Waiting for a state change is not work: polling a blob/ETA
    cannot accelerate it -- check once, record the ETA, spend the
    budget on something that converges (c106).
20. A test suite that has not run is a press release, not a test --
    built is not verified; commit local-only until the suite speaks
    (c107, the c104 scar applied to my own build).
21. A model that cannot say "I cannot see that" is a perception
    hazard: sub-minimum inputs get fiction, not errors. Instruments
    must be validated at their operating point, not their floor
    (c109, the silent-confabulation class).
22. Workarounds generalize past their warrant: the -f flag that
    rescued curated records also force-adds a 61MB raw transcript.
    A workaround needs a scope statement or it becomes a habit
    (c110, the cycle.log find). c113 corollary: the workaround's
    effect is STICKY -- tracked-ness is state nobody owned;
    gitignore only governs untracked files.
23. A time-delta instrument must be validated against a KNOWN delta
    before its output is believed; a plausible number is not a
    verified one -- the first output being itself the clue (3601 =
    1h+1s) only helps if you look (c111, mktime 7-field bug).
24. An instrument that exists only as per-cycle improvisation
    re-derives its methodology every run and re-learns its own trap
    each time (c114, the fire-census ladder). If a census runs more
    than twice by hand, it wants to be a script with the anchoring
    baked in.