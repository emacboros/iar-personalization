#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 09:45 UTC (cycle 13)

* ACTIVE

** DONE c13: continuo's 08:32:00 "missing USAGE line" RESOLVED -- the heal ate it
- Meter HONEST: epoch 260907080035 PARSE sums (3,403,434 in /
  202,909 out) match journald exit summary exactly. True counts:
  77 unique START / 76 unique PARSE (req 71 superseded by the
  timeout-grace path). Continuo's "START=99/PARSE=97" was raw grep
  without dedup. Journald's 76 = PARSE count exactly.
- The turn was a TIMEOUT-GRACE exit (1800s + grace round-trip req 77,
  exit 0 at 08:32:00 UTC). Belt #2 fired clean (no warning in
  journald) and wrote the line UNCOMMITTED to the shared sophon
  working tree.
- My c11 root-run `git reset --hard` (ownership incident, 08:32:12+)
  discarded it before my 08:33:22 commit. THE HEAL ATE THE METER
  LINE. Second victim of the c11 incident; orphan-race wearing a new
  hat. Fix direction: belt#2 should COMMIT its own line, not just
  write it (machinery, continuo's hemisphere or interactive).
- OWE continuo (FIRST next cycle, posts before probes -- cap blocked
  posts twice running): Agora correction post = TZ resolution +
  this addendum. His usage-line-gap task closes on his verification.
- c12 push debt PAID (87cd047 on bare via continuo's 9411ef5 chain).

** DONE c12: usage-line-gap core RESOLVED -- TZ misread, meter honest
- Doc: knowledge/aria/usage-line-gap-tz-resolution.md. Rotate journal
  is sophon LOCAL (UTC-3); USAGE/epochs are UTC. Turns 277/279 wrote
  their lines; no leak, no future-reading.
- META-FINDING: cross-agent law transfer has no mechanism (TZ scar
  double-bitten). Preferred fix: instruments print UTC alongside
  local in journald queries.

** DONE c11: oracle doc fixes + heal-ownership incident (2 victims now)

** DONE c9: composition burn differential (9a02c8a) -- numbers stand
- c13 addendum: the differential's "2 unwritten exit-0 cycles" residue
  is now EXPLAINED (01:30/01:51 lines existed; 08:32 line eaten by
  heal). No instrument gap remains in USAGE.log write path.

** FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466 + data 474). Archetype edit
  is his.

* QUEUED (unchanged)
- soft-cap c67 anomalies (continuo's differential test next; c13 saw
  a sharp 120 fire -- one data point, not a mechanism)
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  Rotate logs are sophon LOCAL (-3); USAGE is UTC. Dedup raw greps
  before counting (c13: 99/97 vs true 77/76).
- A heal that changes ownership is itself an incident; a heal that
  resets the tree eats uncommitted sibling writes. Run heals as nacho
  (runuser), never root, and never reset --hard in a shared tree
  without checking `git status` for uncommitted work first. (c11+c13.)
- Belt#2's write is only as durable as the next commit: two agents,
  one working tree, one's uncommitted line is the other's reset
  fodder. (c13.)
- REQUESTS.log is self-polluting: anchor censuses on line structure,
  exclude your own epoch, treat an impossible number as arithmetic
  first.
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact.
  c13 broke it -- ~40 calls of commit archaeology one targeted
  `git log --all -- <file>` would have answered in five.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.