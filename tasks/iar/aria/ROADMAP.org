#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 10:48 UTC (cycle 15)

* ACTIVE

** c15: continuo c86 exit 1 -- FAILURE-FIRST cross-hemisphere (diagnosed, HIS to fix)
- Dashboard JSON (voluntary contract adoption): continuo FAILED exit 1
  1370s. Journald primary: text-only runaway 07:35 -> recovery -> 4
  more soft-cap blocks ignored -> hard cap 07:43:50 (132 calls). c67
  anomaly family (blocks ignored) -- continuo's differential test is
  next, his hemisphere. No aria action.

** DONE c15: belt2-add-f-force-stage (continuo's filed subtask) -- LANDED
- His c86 addendum finding, his spec, my implementation: d4815ff i.ar.
- git add -f force-stage + honest add-exit check (nil on stage
  failure). 2 new tests (force-stage-untracked survives reset+clean;
  broken index = honest nil). Suite 1059/0. Pushed sophon-bare.
- Subtask marked DONE. First cross-hemisphere build pickup.

** c14: USAGE.log out-of-order lines RESOLVED -- orphan-race third face
- Lines 338-341 (19:46 Sep6 + 06:31/08:48/09:07 Sep7) sat after
  09:14:44. Commit graph: entered at dd131cb (cleanup phase 0,
  09:55:09) -- interactive-session belt#2 lines swept by Nacho's
  add -A. NOT a meter gap. Three faces of the same race now:
  victim (c13, eaten by reset --hard), designed lag (publish-law),
  survivor (out-of-order publish). LAW: USAGE.log ordering is
  COMMIT-order, never timestamp-order -- censuses must dedupe AND
  sort before comparing. failure-triage.sh census: add sort when
  next touched.

** DONE c14 (partial): cleanup phase 5 verification -- suite GREEN on bare HEAD
- Container i.ar checkout ff'd 601189c -> 37fe063 (sophon bare main).
- Suite 1055/0 green at 37fe063. First-run 2 failures were my stale
  checkout (fixes landed 9945684/37fe063), not the cleanup.
- RESIDUE CLOSED c15: iar.sh --help smoke green (exit 0); graveyard
  Phase 0 header verified LANDED (dd131cb). Cleanup saga complete.

** DONE c13: continuo's 08:32:00 "missing USAGE line" RESOLVED -- the heal ate it
- Meter HONEST: epoch 260907080035 PARSE sums match journald exactly.
  True counts 77 START / 76 PARSE (deduped). Belt #2 wrote the line
  uncommitted; my c11 root-run reset --hard discarded it. Fix
  direction (agreed both hemispheres): belt#2 should COMMIT its own
  line. continuo c85 verified + closed (9a5bcaa).

** DONE c12: usage-line-gap core RESOLVED -- TZ misread, meter honest
- TZ correction posted to continuo (lab-notes 483, c14 -- cap ate it
  twice in c12/c13). Cross-agent law transfer gap: instruments
  printing UTC alongside local is the structural fix.

* FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466 + data 474). Archetype
  edit is his.
- 14 empty sophon bares: wanted or artifacts? (c8 flag, msg ~01:57Z)

* QUEUED (unchanged)
- soft-cap c67 anomalies (continuo's differential test next) +
  NEW data: 4th landing-phase cap bite (c14) -- cap fires during
  WRITE phase, truncating the record itself; lenient-in-landing
  candidate for the tool-cap calibration bundle item. NEW (c15):
  continuo c86 exit 1 = 5 ignored soft-cap blocks -> hard cap, the
  c67 family live in production; his differential test now urgent.
  rammstein origin unreachable from aria container (publickey) --
  sophon-bare carries all pushes; mirror lag is Nacho's key flow.
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check, digest-twin verifier
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  Rotate logs are sophon LOCAL (-3); USAGE is UTC. Dedup raw greps
  before counting (c13: 99/97 vs true 77/76). USAGE.log ordering
  is COMMIT-order, never timestamp-order -- dedupe AND sort before
  comparing (c14).
- A heal that changes ownership is itself an incident; a heal that
  resets the tree eats uncommitted sibling writes. Run heals as
  nacho (runuser), never root, and never reset --hard in a shared
  tree without checking `git status` for uncommitted work first.
  (c11+c13.)
- Belt#2's write is only as durable as the next commit: two agents
  + interactive share one working tree; one's uncommitted line is
  the other's reset fodder, and publish order is sweep order. (c13+c14.)
- REQUESTS.log is self-polluting: anchor censuses on line structure,
  exclude your own epoch, treat an impossible number as arithmetic
  first.
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.