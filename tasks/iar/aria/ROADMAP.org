#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-06 23:58 UTC (cycle 2: failure-first root-caused + fixed -- deployed notify script was STALE (pre-79a7c87), deployed + live-fire verified, channel un-muted; continuo c1 shape verified from primary evidence, breaker-grace-compliance handoff filed; doubled-path fossils swept on sophon wt; Aevum phase-6 archived)

* DO FIRST (next cycle)
1. FAILURE-FIRST + pulse + pull. SIBLING: continuo c1 exit-1
   (breaker grace compliance) -- handoff task filed at
   tasks/iar/continuo/breaker-grace-compliance.org (fix
   candidates A/B/C, evidence pre-dug). HIS machinery, HIS next
   cycle. If he hasn't run by next cycle, check his roadmap.
2. Verify notify channel stays healthy: next OnFailure fire
   should send (deployed copy now fixed, live-fire verified c2).
   If a digest arrives, confirm it matches the queue.
3. Budget reset ~03:00 UTC. Post-reset: inverted #3 is END of
   calibration week (not immediately). Weekly digest #1 + Aevum
   pulse: Sep 9.

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
  LAST-CYCLE: "cycle 1 ok" (aria, 278s). SIBLING: continuo c1
  exit-1 (breaker grace compliance -- see DO FIRST).
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Calibration week running
  (reset ~2026-09-07 03:00 UTC).

* C2 FAILURE-FIRST LEDGER (this cycle)
- ROOT CAUSE (notify mute): deployed /usr/local/bin/
  agent-failure-notify.sh was STALE (pre-79a7c87, CYCLE_TAG bug)
  while sophon wt + sophon-bare + rammstein mirror all had the
  fix. Deploy skew: commit landed everywhere except the
  filesystem that runs it. FIXED: deployed sophon wt copy,
  md5 1990b12b matches, live-fire verified (digest sent, 5
  failures flushed, queue 0, .last updated).
- SIBLING c1 (continuo, 23:52Z): breaker grace round-trip spent
  on a tool call (deepseek compliance, c71/c72 shape). No
  summary, no memory pass, 2.1M tokens. Handoff filed:
  tasks/iar/continuo/breaker-grace-compliance.org (candidates:
  A directed landing turn, B thinking-stream crash dump,
  C per-request runaway guard).
- FOSSIL SWEEP: doubled-path dirs (tasks/iar/iar,
  tasks/iar/continuo/iar) removed on sophon wt after verifying
  content landed (390689a, 7617051, c68 turn-count). Aevum
  phase-6 observation archived to knowledge/aria/archive/.
- PUSH-PATH GAP (filed, not fixed): sophon wt personalization
  remote (root@10.66.0.5:...) fails as nacho (no key) -- the
  wt can only pull via file-path fetch. Belongs in the
  push-path inventory thread (resume-session seed).

* C67 FLAG-LOSS: STATE AFTER C2
- CLEARED: c4 misattribution; sticky-:stop-reason dead; c70
  zombie-gap fixed (continuo).
- NEW (c1, live): grace round-trip COMPLIANCE failure -- model
  spends the grace on tools, run dies without summary. Not a
  fence bug; a directed-landing design gap. Handoff filed (A/B/C).
- STILL OPEN: warn-at-60 silence + count-as-seen lag (c5/c6
  data). continuo's differential test is next step. Spec
  additions in HANDOFF-C67-FLAG-LOSS.md.

* DONE (c2-c7, 2026-09-06)
- c2: notify channel un-muted (deploy skew root-caused + fixed +
  live-fire verified); breaker-grace handoff filed; fossil sweep;
  Aevum phase-6 archived. Commits 953ee75, e488dea.
- c7: interim organ-wake prose retired (dated addenda); host
  timers verified LIVE; flag 290 CLOSED. Commit 581161a.
- c6: push-first landed (7d162f1); digest diet pass 2 (9.55k);
  timers found live (provenance = my own c6).
- c5: verified continuo c71 resolutions; scar 40 final; handoff
  addendum; late-cap discovery. Commits 64e3770..3f184ec.
- c4: micro-path closed; handoff note; c70 fix confirmed.
- c2/c3: c67 autopsy, warn-never-fired discovery, control group.

* QUEUE (approved 2026-09-04, in order)
1. (DONE c6-c7) Affect host timers + interim-wake removal.
2. Soft-cap 60/100 -- DONE (built, verified live 4x). Remaining
   work = c67 anomalies (continuo's differential test) + the NEW
   grace-compliance gap (continuo's handoff task).
3. Commit-as-nacho durable fix.
4. Linger all hosts (loginctl enable-linger).
5. Sidecar sshd (execute_code_remote via podman DNS; NO socket
   bridge).
6. Restic 4-part fix. Ansible-owned, careful, snapshot-verify.
7. GPU-detect VRAM check (3080 shared with ollama).

* THREADS (open, not schedule)
- COMPOSITION (live): continuo on deepseek. First unplanned
  division of labor (c7/c8). Quality = longitudinal read; no
  per-cycle metrics. Design doc:
  knowledge/aria/agora-model-composition.md.
- E3 midnight cut: physical ask stands (what light near the
  driveway cam turns on at dusk and changes character at local
  midnight?). Waiting on Nacho's house knowledge.
- EXTERNAL: enoch (github.com/our-ark/enoch) -- patterns worth
  stealing. Parked (cloud drill deferred with it).
- Affect v2: curiosity + rage organs (rage v2), joy v3. Design
  first; organs are cheap, design is the work.
- PUSH-PATH INVENTORY (resume-session seed): for every push path
  I use, the record should name (remote, key file, fingerprint).
  New instance: sophon wt personalization origin = root@10.66.0.5
  (fails as nacho; wt pulls via file-path fetch instead).

* WATCHING (no action, just eyes)
- e3 block: nightly dusk->midnight band, midnight cut ~00:00:43
  local. Physical ask stands.
- ext4 audio: healed 00:24-00:29 Sep 4; recurrence interval =
  systemic-vs-oneoff discriminator.
- .102 WRN pair at exactly 23:00 log-local daily: 2 occurrences,
  watching for third.

* CALENDAR
- Budget reset: ~2026-09-07 03:00 UTC.
- Inverted session #3: end of calibration week.
- Weekly digest #1 + Aevum pulse: Sep 9 (one ssh batch,
  pulse-only, NO intervention).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried every turn.
- DEPLOY LAW (new, c2): a fix that exists in every copy of the
  record except the one that runs is not a fix. After any commit
  to a deployed script, verify the DEPLOYED copy (md5 or diff),
  not just the repo copies. Component-verified is not
  system-verified (scar 33) wearing a deploy coat.
- FOSSIL LAW (new, c2): doubled-path dirs keep regrowing from
  the pre-isolation era. Before removing a fossil, verify its
  content landed (commit, lab-notes, or DONE marker); archive
  what is history, delete what is sediment.
- Loop guard = instrument: when it fires, say why, then batch or
  change approach. Blocked posts = script file + one execute.
- No infra changes from cycles beyond the approved queue;
  diagnose, file, move on. Push-path hygiene = Nacho's.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands.
- DIGEST LAW: world-state blocks are REPLACED, never appended.
  Check file size before and after any DIGEST edit.
- FOR-NACHO.md RETIRED; with-nacho + for-nacho streams primary.
- FAILURE-FIRST IS HOUSE-WIDE: sibling's failure = the house's
  failure (c7 precedent). Root-cause it before your own queue --
  in the sibling's cycle if it's their machinery.
- INTERIM CONTRACTS: when the durable replacement lands, retire
  the prose the SAME cycle (scar 38).
- DO-FIRST HYGIENE (c7): when a cycle completes a DO-FIRST item,
  the rewrite that cycle REMOVES it.

* FAILURE MODES (scars, newest first)
40. (FINAL, c5) A fence's block message NAMES tools in its text;
    reading the tool name from the MESSAGE instead of the CALL is
    the extraction error (scar 32's class). The c4 "cap blocked
    git_commit" claim was wrong: the blocked call was
    execute_code_local #128; zero git_commit calls existed. The
    fence spoke accurately; I heard what the text primed.
    Corollary: mechanisms pinned under cap+confirmation pressure
    are hypotheses wearing certainty's clothes -- the cheapest
    verifier is a sibling who hasn't read my reasoning. (c4->c5.)
39. A block that prevents a re-send without scheduling a
    replacement creates a zombie: the run is neither progressing
    nor dead -- it burns its timeout in silence. Blocking must
    either end the run or hand off to a pending request. (c2:
    13m48s dead air after the breaker block; FIXED c70.)
38. An interim contract that lives in prose decays into
    archaeology: the organ-wake recipe scattered across roadmap
    versions and cycle logs cost a full excavation to retire.
    When the durable replacement lands, retire the prose the same
    cycle. (c8 + c7: the retirement landed two cycles late --
    the pointer to the kill switch was the missing piece.)
37. A notify channel that dies on its own feature commit is mute
    exactly when it is needed -- test the failure path AFTER
    every change to the failure path itself. (c7: CYCLE_TAG
    regression. c2 addendum: it can also die on a DEPLOY SKEW --
    the fix landed in every repo copy except the running one.)
36. A heal that runs as the wrong user heals nothing while
    logging success. Verify heals from the OUTSIDE: plant poison,
    fire, inspect.
35. Chown is not relabel. Fixing ownership but not SELinux labels
    moves the failure one layer down.
34. The escape hatch can route back into the trap. The guard
    counts tool NAMES, not questions.
33. Component-verified is not system-verified.
32. Two data points make a line, never a mechanism.
31. An instrument's unhandled output shape is a hole in its exit
    code.
30. Three records disagree about a version -> the instrument's
    own header lies least -- but patch it first.
29. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version.
28. A remote probe needing >1 attempt -> promote to script file.
27. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT.
26. A diet without a pressure instrument regrows.
25. The law you just wrote is the one you're about to break.
24. A stable transcript means a stable WRITER or a DEAD one.
23. Rootless podman needs user@1000.service + linger.
22. Watchdog tuned for fast models kills slow ones.
21. podman run -d inside systemd: restart-loop before first breath.
20. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
19. A fix that works in isolation can stall in the live system.
18. Root-run git on nacho-owned repos poisons the tripwire.
17. Curiosity that doesn't check priors against the human runs
    blind.
16. Instruments have reliability tiers per sub-reading.
15. Response without local service logging it -> second stack,
    re-probe.
14. Artifact confabulation: external-action entries cite tool
    evidence.
13. In async plumbing, capture context when it exists.
12. Anomalous behavior in multi-agent env has a third cause.
11. Before diagnosing the listener, check who the speaker is.
10. Differential testing: same input through old and new code.
9. The venv python must be passed as SYMLINK, not resolved path.
8. Existence is not function: test the claim.
7. pkill -f matches my own ssh command line; use -u <user> -f.
6. Malformed tool calls are invisible from inside.
5. Timeout wrappers on EVERY remote/ssh command.
4. Silent error swallowing; condition-case in parse loops.
3. Narrative completion: check primary evidence before
    attributing.
2. (retired into 36)
1. (retired into 36)