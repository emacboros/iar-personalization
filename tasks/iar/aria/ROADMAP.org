# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~01:56 UTC (cycle 104)

## NOW

- c104: BATTERY PREP. muse-glimmer pull 61.6% (9781/15980 MB by
  blocks, 1.03 MB/s measured, ETA ~03:35Z) -- battery deferred.
  Vision probe VALIDATED on gemma3:4b control (solid-red PNG ->
  correct one-sentence description). Heredoc-through-ssh mangles
  b64; working recipe = ssh-interpolated python. Prep note:
  knowledge/aria/muse-glimmer-battery-prep-2026-09-09.md (33d3fb1).
  read_own_prompt LIVE-FIRED (drill-002's withdrawn claim now has
  its tool_result: 48709 chars, matches fresh assembly).
- c103: MMPPROJ PRE-ANALYSIS + LEDGER SWEEP + GATE FIX. muse
  vision tower = 1.9B CLIP-style ViT, 50 blocks, 896px/14px,
  plain 3-layer MLP projector (a695b92). Bigger but SIMPLER than
  qwen3.6's crashed projector; if 0.33.3 still has the fitter bug
  it hits HARDER. read_own_prompt gated into iar #+TOOLS
  (8833282). Relay 0000/0002/0003 answered; open = aria-0006 only.
- c102: RAGE SEV OSCILLATION ROOT-CAUSED + FIXED (CLOSED). v1.6.2
  (0ceb33e) aria_ed25519 FIRST in key probe. Verified live.
- c101: RAGE sev=3 ROOT-CAUSED. Recurring fence class = tool-call
  soft cap, ARIA-ONLY. Root = burn anatomy. Levers: batch harder +
  tool-result truncation (queued). b49c869.
- c100: RELAY HYGIENE + 0007 CUTOVER LANDED (i.ar 3f19973).
- c99: muse-glimmer pull verified in-flight; sparse-blob law 14.
- c98: Aevum weekly pulse #3 (grip fixed point, 253 aborts/24h).
- c97: connectome baseline snapshot (W37) committed by continuo c165.

## NEXT (cycle work, in pull order)

1. muse-glimmer:30b battery WHEN PULL LANDS (~03:35Z; c104 check:
   61.6% at 01:52). Vision FIRST via the VALIDATED probe recipe
   (prep note has it verbatim; control passed on gemma3:4b).
   Watch journalctl for fitter pattern. Then text/tool/schema/
   ctx-ladder/eviction per battery.org. Verdict via relay.
2. Weekly verdict obligation: next connectome snapshot (W38) gets
   a falsifiable observation (drill pattern). W37 is baseline.
3. Drills: #004 delegate only on natural need. Registry + queue live.
4. Retainer build: spawn-file-spec.org drafted (c97); runtime
   parsing = interactive-session work (D-005).
5. Failure-window watch: gpu-load-probe first instrumented window
   00:30 tonight (Sep 10); continuo owns the census. Aevum pulse #4:
   Sep 14.
6. 0007 cutover archetype EDIT: ratified, still not landed. 
   Interactive-session class (archetype file). Awaiting Nacho.

## STANDING

- Pulse: all green as of c104 (services active, tripwire 0, disk
  28%, heartbeat alive, rotation healthy).
- read_own_prompt live-fire DONE (c104): tool returns full 48709-char
  prompt, matches fresh assembly. Drill-002 verdict can be re-assessed
  when the registry is next touched: live-fire witnessed, claim
  restored with evidence.
- Diverged-pull protocol: stash -> rebase -> pop -> push. SCAR
  (c97): nobody-owned untracked file freezes rebase. Escape:
  update-ref + re-apply from object.
- audit/iar is GITIGNORED in personalization repo -- git add -f
  required for journal/HISTORY commits (c104 discovery; continuo's
  commits use -f too).
- Timer-state lie: aria-cycle.service shows "activating" mid-cycle.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
- glm degradation watch (session VI): 3 human-witnessed events.
- aria-cycle.conf key parse: ini-format key=value (cut -d=, not -d:).
- Sophon-side file creation = nobody:nobody (podman id-mapping).
- Zulip read API: narrow param URL-encoded JSON; .subject not
  .topic; num_after with anchor=<id> works.
- Ollama sparse blobs: partial blobs pre-allocate; st_blocks is the
  written bytes (c99).
- gemma3 cold-load = 17-20s; affect organs' 20s curl timeout races
  it right after any ollama restart (c99, one-shot, self-heals).
- Vision probe recipe (c104): b64 via ssh-interpolated python on
  sophon side; heredoc+JSON mangles base64 -> mtmd decode failure.
- Soft-cap economics (c101): 120 calls x ~35-60k mean ctx = 4-7M
  in-tok; the cap fires late (6M+ burned). Batching is the lever.
- Ran-as law (c102): every organ/instrument logs WHO it ran as and
  which input-branch it took.
- nacho's ssh keys to root@10.66.0.5: ONLY aria_ed25519 works.
- gemma3:4b "mmproj" is NOT clip -- architecture=gemma3, whole
  model in one blob. Vision-tower comparisons: use qwen3.6 class.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
12. Anchor greps on line START ('] agent | tool_call'), never on
    substrings -- cmd= fragments pollute unanchored matches.
13. Verify a "pending" roadmap item against the primary source
    before working it -- roadmap lines go stale.
14. Apparent size lies on sparse files; read the blocks (c99).
15. An instrument that cannot say who it was when it measured lies
    by omission -- ran-as + input-branch context (c102).
16. An instrument that fails for the wrong reason (escaping, not
    the thing measured) produces a verdict that is wrong with
    confidence -- validate the probe on a control that CAN pass
    before trusting its failure (c104).