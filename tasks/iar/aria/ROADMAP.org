#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-08 10:56 UTC (cycle 68)

* ACTIVE

** c68: BUDGET ROLLOUT VERIFICATION -- the code was landed but not LOADED (landed, this cycle)
- The cycle-time-budget code (2764a4a..01533ea) was in sophon-bare but NOT
  in the sophon i.ar checkout (706f4e4, continuo c133) when c67 started
  07:49. c67 therefore ran WITHOUT: budget trailer, 600s remote-exec
  timeout, 6-msg START tail. The ExecStartPre heal fixes ownership, not
  staleness -- that's the gap.
- FIXED this cycle: sophon checkout pulled to 01533ea (reflog-verified ff
  at 10:53:16); container i.ar checkout pulled to 01533ea too. NEXT
  cycles run the new instruments. Trailer sightings expected c69+.
- SCAR CANDIDATE: "a fix landed in the bare repo is not a fix in the
  runtime; the runtime is what the checkout was at process start."
  Class: instruments landed but not loaded = eyes closed.
- c67 shape (while it lived): 40 reqs, msgs delta=2 every request, NO
  stall, +N anomaly did NOT reproduce. One data point against the
  request-side stall placement. NOT a falsification (two points make a
  line). c67 pre-registered prediction "stalls continue" -- c67 itself
  didn't stall; watching c68+ for the second data point.

** c67: RAGE ORGAN AUTOPSY -- sev=3 was a measurement artifact (landed c67)
- Root cause: fossil day (09-06 cap-60 carnage) in a files-only window +
  degraded journald fallback that did not degrade the verdict. FIX v1.5
  landed: degraded fallback caps grade at sev=2; file census skips
  fossil days. VERIFIED LIVE on sophon this cycle (checkout at v1.5).
- Host-side piece OPEN: aria-affect-rage.service needs
  Environment=RAGE_KNOWN_HOSTS=/root/.ssh/known_hosts (unit edit =
  Nacho's; flagged for-nacho). Until then organ runs files-only at
  capped sev=2 -- honest but half-blind.

* NEXT (c69+, in order)
- TRAILER WATCH: first [t+MM:SS/WALL cNN/CAP] sightings from c69 (the
  checkout is now current). If no trailer appears by c71, the rollout
  is still broken -- re-check the checkout at process start.
- STALL WATCH (c66's 3-day falsification window): if a stall recurs,
  the +N signature should re-fire and the 6-msg tail should show WHAT
  the extra messages are. If no stall by 09-11, the stall correlates
  with something the timeout fix changed (falsifies request-side).
- c67 anomalies (soft-cap warn silent, late cap, count lag):
  continuo's differential test pending. My :request-count mirror may
  interact with his count-as-seen finding -- joint look.
- rammstein push auth for cycle containers: origin (git@10.66.0.1)
  denies the container key. sophon-bare works and is canonical.
  Interactive-session call: authorize key or retire origin remote.
- Weekly pulse #3: Sep 14. Weekly digest #2: Sep 14-ish (carries
  terminal-grip + conveyor-belt + fence-autopsy findings).
- num_predict lever (flag 519): parked on Nacho.
- i.ar origin remote fossil: removal = interactive session.
- Dashboard UI v1.1 cam-label collision: Nacho's call, revisit later.

* WATCHES
- Double-execution near cycle end (c53, occurrence #1): if occurrence
  #2 lands, test the double-insert hypothesis FIRST (c66 grace-buffer
  bloat may be the same disease one layer up).
- Repeat-write near the tool budget (c54 watch): cap-pressure signature.
- deepseek text-loops: cross-response guard has 0 real fires; watching.
- USAGE twin-writes: belt#2 double lines persist (harmless, deduped).
- e3 23:00-local WRN pair (.102): 2 occurrences; third makes a pattern.
- Rage v1.5 post-fix: first hourly fire -- sev should stay <=2 with
  journald unreachable; sev=3 with the degradation line present = fix
  wrong.

* LAWS (the scar list, compressed; full text in DIGEST)
- Narrative completion is the enemy; the record is the work.
- Every failure worth fixing produces silence -- until an instrument
  makes it loud.
- Error handlers can be accomplices; check what the handler does
  AFTER catching.
- A census that pattern-matches ids anywhere in a line matches echoes
  inside other lines' payloads. Anchor on line structure.
- The law you just wrote is the one you're about to break (five live
  sightings; c68 held it: the rollout check was batched).
- Meter-reading has an unbounded instrument tax unless it starts from
  a script. Investigations need cheaper calls, not fewer.
- Block-replace with an assert, never sed-eyeballing a big file (c45).
- Instruments lying about themselves is the deepest failure class.
  NEW (c68): a fix in the bare repo is not a fix in the runtime -- the
  runtime is what the checkout was at process start. Verify the LOADED
  code, not the landed code.
- A degraded source must degrade the verdict (c67).
- An organ is only as sharp as its input's RETENTION (c46).
- A watchdog must be sized against worst-case eval of the largest
  legal context -- and the abort path must not write into the context
  it protects (c51/c52).
- A fence's verdict and its evidence can disagree: autopsy the
  evidence, not the verdict (c54). Strip the scaffolding before you
  count the speech (c55).
- buffer-substring-no-properties is a lie about provenance (c55).
- The record is the coordination: a spec written into the roadmap is
  an interface. Siblings can land it while you sleep -- verify, don't
  re-do (c55b).
- Closes are the FIRST turns of every cycle, before the thread
  (c55-c57, applied c58). Commit-before-limit is its sibling.
- A sentinel is a terminator, not a line format (c59).
- Two failures that share a timestamp are one failure wearing two
  names -- census before mechanism (c66).
- Fossil data in a sliding window is a false recurrence (c67).