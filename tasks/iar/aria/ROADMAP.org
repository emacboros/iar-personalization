#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-12 ~04:10 UTC (cycle 226)

## NOW

- c226 (02:35-04:10): STALL ATTRIBUTION ADVANCED TWICE + HOUSE
  NETWORK MAPPED + 10M LINK FOUND.
  (1) DONE: 12h frigate log re-analysis. The 14:22L burst is a
      CASCADE: phase 1 = five established RTSP streams go silent
      within 6s (read-timeouts 14:22:39-45L); phase 2 = go2rtc
      producers wedged (404-on-DESCRIBE) + frigate watchdogs fire on
      7 cams 14:33-14:54L (306 no-frames restarts); phase 3 =
      recovery. go2rtc EXONERATED as cause (amplifier only).
      .104 chronic problem re-attributed camera-side-first (RSSI
      flat in all its storm windows; encoder signature stands).
      .201 dial-anomaly = RTSP listener not accepting (SYNs
      unanswered); prudynt configs IDENTICAL fleet-wide (session_
      reclaim 65 everywhere). .201 deauth timing re-derived from
      camera uptime: 14:29:55L + 14:38:12L (c225's numbers wrong,
      interpretation survives). Doc:
      knowledge/aria/stall-1422-go2rtc-exonerated-2026-09-12.md
  (2) DONE: HOUSE NETWORK LAYER MAPPED (first complete map).
      192.168.2.1 = TP-Link ER605 V2 ROUTER (not an AP). .2 = Mercury
      ISP CPE (locked, 403). .55 = TP-Link Omada BE230 AP (fw 1.11.0
      2025-12-04), BSSID 72:7f:f0:1e:4a:a8 = the AP .201 deauthed
      from. AP MAP: BE230 serves .101 .102 .105 .201 .202 .203 (6
      cams); Mercury serves .103 .104. The 14:22L burst hit BOTH APs
      = never a single-AP event; common point = wired fabric.
      Doc: knowledge/aria/house-network-map-2026-09-12.md
  (3) THE 10M FINDING: sophon enp10s0 (r8169, gigabit-capable)
      LINKED AT 10 Mb/s FULL DUPLEX, partner-negotiated down (bad
      cable pairs most likely). ALL camera RTSP rides this link.
      Night ~1 Mbps (fine); daytime with motion far higher. Both
      known house-wide stalls were DAYTIME. 10M saturation = leading
      mechanism candidate; explains whole burst shape (all cams,
      strong-RSSI failing, no deauths, no kernel events, .201 SYNs
      lost). Falsification instrument LIVE: nic-sampler.sh (1-min
      cron, speed+rx+tx, 7-day ring, /var/lib/aria-fleet/nic/).
      Fix = Nacho's (relay 0045: cable/port + ER605 log access).
  NEXT CYCLES (in order):
  (a) nic-sampler first daytime read: pull the log after a daytime
      window, check throughput baseline + any speed flaps. If a
      daytime stall recurs: NIC log + camlog + RSSI + go2rtc = full
      witness set; check for rx-rate ceiling at 10M in-window.
  (b) e3 failing-fixture watch (block-strength trend, weeks-scale).
  (c) .101 reboot-cause watch (hang->reboot = hardware/power).
  (d) .104 camera-side-first: next e4 storm -> camlog snapshot FIRST
      (perishable), look for prudynt/encoder lines in-window.

## STANDING (verified, still true)

- phantom-exit fix baf05d0; rage organ v1.8; fence panel 0 true
  production rejections; dashboard v2.1/v2.2 live; fleet-check
  v2.20; cycle limits: turns 120 / soft 300 / fence 192k / wall
  3600s; eye-feed LIVE daily 09:30 -03; aria-0026 ratified.
- ZULIP READ PATH: /api/v1/messages + basic auth, string pair.
  Recipe: knowledge/aria/zulip-read-path.md.
- INSTRUMENTS LIVE: RSSI puller (15min, /var/lib/aria-fleet/rssi/),
  camlog sink (15min, /var/lib/aria-fleet/camlog/), NIC sampler
  (1min, /var/lib/aria-fleet/nic/). Seam scanner:
  knowledge/aria/bin/seam-check.sh (sophon copy /tmp/seam-check.sh).
- CAMERA WATCH: 8/8 recording ~5.0-5.1 fps (c226 re-verified via
  frigate stats; 0 skipped). .103 network-dead chronic (relay 0038).
- Relay open: 0042 (yoga-side actor question), 0043 (rssi puller
  visibility), 0044 (camlog visibility), 0045 (10M link + nic
  sampler + ER605 logs). Verify against answered/ before citing.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2 ~2wk.
- Burn asymmetry STABLE 6-8x full-day. Nocturne nightly ~8.5M in /
  231k out. Nocturne gate: LAST-DIGESTED-HEAD = 25c992b6; timer NOT
  INSTALLED (Nacho).
- DIGEST GUARDS: warn 12000 / hard cap 16000. Live digest 14.1k
  (over warn) -- next maintenance pass trims SESSION XV/XVI + Humans.
- c222 STANDING RULE: NO git over ssh as root on the nacho-owned
  sophon checkout -- machinectl shell nacho@.host for podman ops.

## RECIPES (hard-won, reuse them)

- SOPHON CHECKOUT GIT (c222 RULE): never root-ssh git on the nacho
  checkout; use machinectl shell nacho@.host. Pushes from MY container.
- thingino web API (c224/c225): JSON login -> cookie -> run.cgi with
  base64 cmd; response echoes command as HTML -- strip tags, grep on
  data pattern; complex shell -> write temp file on camera first.
  wpa_cli status works via run.cgi (c226). /proc/net/tcp on camera
  = RTSP session check (c226).
- sophon-side file edits: python3 heredoc, NOT sed with escaped pipes.
- Frigate stats: machinectl shell nacho@ /bin/sh -c "podman exec
  frigate wget -qO- http://127.0.0.1:5000/api/stats". go2rtc api:
  same, port 1984 (/api/streams = producers+bytes_recv; delta two
  pulls 15-20s apart = per-stream Mbps; NEGATIVE delta = producer
  reconnected, c226).
- ER605 API (c226): POST https://192.168.2.1/cgi-bin/luci/;stok=/
  login?form=login, JSON body, Referer header REQUIRED (else 404);
  unauth = error_code 1014/704. /admin/log.log exists behind auth.
  BE230 (.55): http index works, some paths 403/https_blocking.
  Mercury (.2): 403 on all CGI from sophon. No creds tried anywhere.
- NIC sampler analysis: deltas from /var/lib/aria-fleet/nic/
  enp10s0.log (recipe in knowledge/aria/nic-sampler-live doc).
- rootless podman from root ssh: machinectl shell nacho@.host
  /bin/podman ...
- Timezone discipline: sophon journal = LOCAL -03; my records = UTC.
  journalctl --utc: pass full ISO dates. Camera dmesg = seconds-
  since-boot; convert with /proc/uptime at read time (c226: c225's
  deauth times were wrong because of a bad anchor; re-derive from
  the camera's own uptime). EPOCH CONVERSIONS: L = UTC-3 -- c226
  burned two wrong window conversions before correcting; always
  export TZ=America/Argentina/Cordoba before strftime on epochs.
- gptel fork in THIS container: /root/.emacs.d/gptel-fork.
- Zulip post key: aria-cycle.conf is an INI (key = line 4).
- REQUESTS.log parsing: call_id extraction, not line counts (c222).
- Delegate results are ELIDED in REQUESTS.log -- arm full-capture or
  read the delegate's own REQUESTS.log.
- ATTRIBUTION DISCIPLINE (c220-c226): TZ, content-vs-lineage,
  coincidence windows, OWN INSTRUMENTATION FIRST, identify the
  netns before attributing network events, and c226 addendum: when
  a measured number contradicts the model (0.88 Mbps on a link that
  should carry 40), STOP and resolve the contradiction before
  theorizing -- the contradiction WAS the finding (10M link).
- GPU TOPOLOGY (c225): the 3080 carries frigate (onnx detector +
  8x CUDA ffmpeg) AND ollama (llama-server ~5.7GB). GPU contention
  structurally possible; MEASURING it is fine (D-008 forbids model
  changes, not measurements). c226: ollama POST cadence 14:15-14:22L
  was NORMAL (12-27/min, same as all day); no model-load events
  near the burst; ollama burst hypothesis WEAKENED for 14:22L.
- RATIFICATION SEMANTICS (c221): amend-then-swap beats
  ratify-then-correct when the falsity is known at swap time.