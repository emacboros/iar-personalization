# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-10 ~09:52 UTC (cycle 151)

## NOW

- c151: FLEET-FAIL ROOT-CAUSED -- exterior_1 CAMERA LIES ABOUT TIME.
  The seg-tail probe failed because ext1's persisted segments carry
  absurd, GROWING duration metadata (28h->196h->224h->281h) since
  the camera's 01:18 -03 restart (storm tail). Localization: recorder
  restart heals ~2min then poison returns (recorder = witness, not
  cause); fresh 10s copy-path records direct AND via restream decode
  exactly 10.00s (live pipeline clean); audio timestamps fine (ear
  check unaffected); detection fine (re-encode normalizes). CAMERA-
  SIDE. Relay aria-0028 filed (nacho-arch): reboot .101; if it
  recurs, thingino NTP/timestamp config. SEG-TAIL will flap until
  then -- expected, fear organ may pulse on it.
- c151: interior_3 AUDIO RECOVERED (fleet-check RECOVERY event, real
  samples + dB). KNOWN_DEAF emptied (v2.17, d8614b48); interior_3
  now FAILs if it goes deaf again. Live-fired: FAIL=0 from the real
  tree. Pushed sophon-bare + (rammstein via mirror hook).
- c151 TWO-TREES TRAP (c122 second sighting): sophon has TWO
  iar-personalization checkouts. /var/home/nacho/repos = live bind
  source (feeder reads it). /home/nacho/repos = FOSSIL (v2.10-era,
  github origin, HEAD 09-07, nothing runs it). Mid-cycle ssh
  verification cd'd into the fossil; nearly concluded my edit failed.
  Law sharpening: resolve the path the RUNNER uses, not the assumed
  one. Fossil = cleanup candidate (noted in aria-0028; untouched).
- c150: FLEET-CHECK v2.16 N-SEGMENT EAR CHECK LANDED (bec3c416):
  the 03:01Z ext3 NO-AUDIO FAIL sampled a 0.79s video-only stub
  written during a frigate watchdog restart. Census: 3 stubs/~1200
  segments, ext3-only, all in restart windows. RESTART STORM
  (~180 restarts 00:xx-01:xx -03) is CAMERA-SIDE, NOT qwen (c146
  keep_alive holds).
- c150 RAGE PREDICTION (no patch): day-granular fix-filter keeps
  09-09's 21 pre-raise ghosts (ratified conservatism). PREDICTION:
  09-09 rotates out of the 3d window TODAY -> sev 0-1. VERIFY next
  cycle.
- c149: RAGE DASHBOARD VISIBILITY FIXED (rage-organ v1.8.1/v1.8.2,
  fecc2353 + 4101d3f4): asof= missing -> dashboard regex silently
  dropped rage; TODAY reassignment truncated age_s. NOW_STAMP
  carries full ISO. LAW: when a record serves two readers, every
  writer must satisfy the STRICTER parser.
- c149: RAMMSTEIN GIT@ KEY GAP CLOSED (aria@i.ar key added; mirror
  has two independent writer paths). 14/20 sophon bare repos =
  empty placeholders (documented); 1 real dangling commit pruned;
  root-run gc = new root-poison entry point (healed).
- c148: DELEGATE TIMEOUT RACE FIXED (i.ar 182afe7 + gptel-fork
  65eb5db). GIT LABEL POISON FIXED (ExecStartPre writes FULL
  SELinux context; Nacho review flagged).

## NEXT (ordered)

- RAGE-DECAY VERIFICATION (today): prediction sev 0-1 as 09-09
  ghosts rotate out of the 3d window. Two data points make a line.
- CONTINUO c1 exit-1 (nemotron output-runaway, 'did I append?'
  loop, fence fired correctly): HER failure-first owns root-cause;
  check her outcome next cycle.
- EYE-FEED scheduled qwen run verification (09:30 -03 daily; first
  scheduled run today).
- FIRE WATCH verdict 09-11 (armed from turn 541).
- RAGE CENSUS CAVEAT: SEG-TAIL flap (camera-side) will accumulate
  fence events in the 3d window -- these are camera-side, not
  cycle-side; note when the rage census reads them.
- 0/0 TOMBSTONE POLICY (aria-0026 ratified): tokens_out=0 text-only
  end = tombstone + non-zero exit. Lands as its own step with
  tests (.el, same gate as 0025).
- DECISION-WAKE protocol line (aria-0026 ratified candidates 1+2):
  continuo world-state digest block + one archetype line (read
  DECISIONS.org tail). Archetype edit = interactive-session work;
  the continuo digest block = continuo's.
- CONNECTOME v2.1: inter-agent edge layer from citizen REQUESTS.log
  PARSE specs=delegate. audit.log delegate lines stay quarantined
  (c142 rule).
- aria-0027 (lucid_curie stale rootful container): filed 09-09,
  still OPEN, container still Up (36h+ as of this cycle). Nacho
  action.
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- FOSSIL TREE: /home/nacho/repos/iar-personalization on sophon
  (v2.10, github origin, nothing runs it) -- cleanup candidate,
  filed in aria-0028.
- Parked (hypothesis): aria USAGE.log out-of-order lines -- suspect
  c91 forensics re-appends. Non-urgent.

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
    c151 sharpening: when verifying a deployed instrument, resolve the
    path the RUNNER uses (ExecStart, feeder script), not the assumed
    path -- a second checkout with the same repo name is a fossil
    wearing the live tree's clothes.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 when an alarm and the primary log disagree, audit what the
    alarm READS -- the file can be the corrupt instrument.
21. c137 a mechanism assembled from plausible parts is a story, not a
    finding. Test the fix's own failure path before shipping it.
22. c138 before declaring an instrument stale, check which clock it
    reads. Cheap falsification beats assembling a mechanism from
    plausible parts.
23. c140 an audit log is primary evidence only if its ATTRIBUTION
    survives async boundaries.
24. c145 one awk pass: a census over a multi-day log is ONE command
    with a window filter, not a walk.
25. c147 a platform can ship with an instrument deliberately disabled:
    when an instrument produces silence, ask "did the platform
    configure it away".
26. c143 identity travels with the REQUEST, not the buffer current at
    completion; restore parent defaults BEFORE the callback.
27. c145 a fix-log entry is a CLAIM ABOUT A WINDOW, not a standing
    fact: every entry carries an explicit WINDOW clause.
28. c148 every async completion contract needs its ABORT case tested.
29. c148 a relabel that sets only the type preserves the poison:
    heal with the FULL SELinux context or not at all.
30. c151 instruments that decode content survive lying metadata;
    instruments that read metadata inherit the lie. A camera that
    lies about its clock is invisible to every instrument that
    trusts metadata.