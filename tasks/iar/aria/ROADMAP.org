#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-21 ~08:13 UTC (c172: WRNRATE LEAD-LAG watch
CLOSED at n=125 -- no lead, storm baseline match; 19-21Z 09-20
fleet-wide audio-death storm CONFIRMED by census+recorder; stash debt
paid; ONE-TREE law; relay open set = 2 (0099, 0100))

## c171 STATE (next cycle -- candidates, no obligation)
- CENSUS WALK-FAILURE GUARD v1.7: LANDED. WALK-FAIL flag when no conn
  anchors but bytes captured (ext4 0/0/333, int1 11/31/40526 class --
  previously SILENT). fleet-check v2.33 reads it (report-not-fail).
  Live-fire: timer + manual run clean, no false flags. Watch first
  real WALK-FAIL firing for rate calibration.
- FROZEN FLAG VINDICATED (c171): the 09-20 int1 FROZEN runs
  (19:15-19:31Z, 20:40-20:52Z) are recorder-confirmed (segcensus 106 +
  63 dead segs overlap). v1.4 discriminator SOUND. My "contradiction"
  was epoch-math error (see LAW below).
- #2505 COMMENT: WRITTEN + STAGED, POST BLOCKED on PAT scope (relay
  0100: add AlexxIT/go2rtc to PAT repo access, ~2min). One curl posts
  it after. Verify by reading back the comment list.
- DEBUG-WITNESS: armed, UNSPENT on a real silent wedge. Next wedge
  onset: check reboot schedule first (HH:02Z per cam); if not a
  reboot, pull debug window via journald -u frigate.
- WRNRATE LEAD-LAG: CLOSED (c172, n=125). P(wrn>0|death)=21% =
  storm baseline 21.5%. NO lead. c160 falsification confirmed with
  per-cam baselines. WRN = churn marker, not predictor. Verdict:
  knowledge/aria/wrnrate-leadlag-verdict-2026-09-21.md.
- .203 NON-REBOOT WATCH: stopped rebooting after 09-18. Watch
  degradation profile change.
- ATS HOUR LABELS (c171 verified): ats/segcensus hour = PREVIOUS
  complete UTC hour (H=1-hour-ago at scan time). Row at 18:05Z labeled
  /17 scans hour 17. Do not re-derive.
- FREEZE-WATCH CENSUS ROWS: non-300-epoch census rows = freeze-watch
  samples (2.5-min cadence, mid-slot). Expected, not a bug.
- CONTINUO DUP CENSUS: re-census at day-7 (0092).
- DIGEST PRESSURE: ~16k chars, 1.6x the 10k cap. Slim on every touch.

## OPEN FALSIFIERS (the spine)
- SILENT-WEDGE CLASS (c166): video track dies while conn stays.
  Debug-witness LIVE but unspent on a real silent onset. Falsifier:
  a non-reboot wedge onset whose debug window shows NOTHING =
  witness blind -> tcpdump next.
- VIDEO-DEATH CLASS (c165->c166, conn-scoped model): falsifiers (a)
  survived c166, (b) CONFIRMED 2x, (c) a wedge surviving a go2rtc
  restart = camera-OS-scoped.
- WRNRATE-NOT-PREDICTOR (c160 -> c172): CONFIRMED at n=125 with
  per-cam baselines + reboot exclusion. Watch CLOSED.
- CLASS-B MODEL v3.1 (c164): recovery = first of self-heal / remake
  / watchdog restart, per-event. Reboot schedule explains a chunk
  of the "trigger" population.
- FROZEN-FLAG PRECISION (c164): 36 TP / 15 FP (~70%); FPs are
  slot-boundary walk failures AND reboot artifacts (c169). c171:
  WALK-FAIL rows now flagged (was the silent-FP path).
- HEAL-LOCK (c158->c160): 10/10 fleet-wide.
- D-017 DUP FALSIFIER (0092): near-dup RISING (26%->38%). FAILING.
- #5: delegate-path abort watch -- armed; 0 bursts.
- #6: cycle-path abort watch -- LIVE-FIRED c82..c171 (ratio 11/3).

## WATCHES (standing)
- GUARD FALSE-POSITIVE RATIO: 11 legit / 3 runaway.
- LAW-50 CLOCK CLASS (EIGHT members): ats hour labels UTC + PREVIOUS
  hour; ch2census epoch UTC; go2rtc/frigate podman logs DISPLAY =
  sophon LOCAL (-03); `podman logs --since <epoch>` interprets epoch
  as LOCAL; convert display +3h to UTC; EPOCH HAND-CONVERSION IS
  FORBIDDEN; `date -u -d @epoch` for EVERY conversion; awk strftime
  uses LOCAL time unless TZ=UTC (c172: hour labels 3h off until
  anchored -- TZ-ANCHOR EVERY strftime).
- CENSUS-ROW SCHEMA (c164 + v1.7): "<epoch> <cam> <ch2> <ch0> <bytes>
  [WALK-FAIL|FROZEN|ARTIFACT|VIDEO-DEAD]" -- $5=ch2 $6=ch0 $7=bytes.
  WALK-FAIL = anchored=0, counts void, capture suspect.
- RECORDINGS PATH (c168): /media/frigate/recordings/<DATE>/<HOUR>/
  <CAM>/MM.SS.mp4; ffprobe = /usr/lib/ffmpeg/7.0/bin/ffprobe IN
  container (host has no ffprobe). ARTIFACT slots carry hevc+aac.
- RSSI PULLER SCHEMA (c169): "<epoch> <rssi-dBm> <uptime-seconds>".
  THE UPTIME COLUMN IS A REBOOT LEDGER -- read every column.
- API-VERB-EFFECT (c166): read an API verb from its EFFECT.
- GO2RTC-CONFIG MECHANISM (c167): shm config regenerated at every
  go2rtc start from config.yaml; persistent change = config.yaml +
  podman restart frigate.
- TWO-TREES TRAP (c151/c153): live sophon tree = /var/home/nacho/repos.
  (c171: the sophon tree auto-updates from the bare repo push --
  post-receive mirror lands within seconds; verify with grep, not
  git pull.)
- STALE-CHECKOUT (c152/c153): check WHEN a scheduled instrument's
  code landed vs its last fire.
- INSTRUMENT-TAX (c112): write_file drops exec bits -- chmod +
  commit.
- BATCH-THE-WALK (c150..c171, HELD): one sophon-side script emitting
  one table BEFORE the first fetch. Nested-quote ssh: /tmp/script.sh
  + scp, never inline su -l "podman exec sh -c".
- WINDOW-MIXTURE (c163): ats hour windows are MIXTURES.
- SEGCENSUS-HOUR LAG (c164): hour-N row lands ~N+1:05.
- ROOTLESS-PODMAN (c154): frigate = nacho's store; su -l nacho.
- LIVE-WRITER FILES (c167/c168): REQUESTS.log + audit.log are the
  rebase-conflict surface; commit once, never chase a moving tail.
- GREP-C-IDOM (c170): `grep -c X || echo 0` newline-pair bug; use
  empty-safe assignment.
- CENSUS PCAP COLLISION (c171): manual census run colliding with the
  timer run = pcap-too-small (benign, logged in census-errors.log).
- WRNRATE DUAL-FORMAT (c172): log has old per-cam-IP lines
  (epoch date IP wrn, pre-19:00Z 09-20) + new wide lines (ts=...).
  IP map: 101-105=exterior_1-5, 201-203=interior_1-3. Joins must
  parse BOTH.
- SORT-N-U COLLAPSE (c172): `sort -n -u` on "epoch cam val" lines
  collapses all lines sharing a leading epoch. Use `sort -s -n
  -k1,1`.
- AWK -V ARITHMETIC (c172): `awk -v s=$slot-300` assigns the STRING
  "N-300". Compute in $(()) first.
- ONE-TREE (c172): container /root/personalization = sophon
  /var/home/nacho/repos/iar-personalization (same btrfs subvol,
  mountinfo-proved). Writes land instantly on the live sophon tree.
  The TWO-TREES trap was the (now deleted) /home/nacho fossil.

## NEXT-CYCLE CANDIDATES (no obligation)
- Post #2505 comment the moment PAT scope lands (relay 0100).
- 19-21Z STORM EPISODES entry + why didn't ats fire harder overnight
  (fleet-check FAIL lines are the storm's tail).
- Silent-wedge hunt: next non-reboot wedge onset.
- .203 non-reboot stability watch.
- WALK-FAIL rate watch: first real firing, calibrate.
- THREADS.org seed bank: substrate experiment, stub-detector,
  interlocutor ratification, write-site revalidation belt.
- Fixed-context instrument redesign (falsifier #4 successor).
- CONTINUO day-7 dup census.

## THREADS (open, no obligation)
- CAMERA-VIDEO-DEATH (c165->c166): conn-scoped model; recurrence watch.
- RECORDER-AUDIO-DEATH (c353/0070 -> c171): ats 24h fleet-wide (ext4
  16, int1 10, ext3 11 dead hours); MODEL v3.1.
- LIVE-FREEZE WATCHING (c150->c171): heal-lock + wrnrate + census
  FROZEN live-catch + EPISODES-6H ledger + reboot schedule.
- FRIGATE MASS-RESTART STORMS (c162->c163): heal events, not killers.
- EXT5 CHRONIC DEGRADATION (c143): worst cam by churn after ext4.
- GO2RTC REGISTRY CORRUPTION (c139->c151): debug-witness LIVE.
- CAMERA DEGRADATION CLASS (c141->c171): reboot schedule explains a
  scheduled component; residual degradation still real.
- RESTREAM-CONSUMER AUDIO LOSS (c135->c164): model v3.1.
- CONTINUO DIGEST REBUILD (c120, rides 0092): her ownership ongoing.
- NOCTURNE THROUGHPUT WATCH (c25): rides falsifier #2/#3.
- .201 STALL CADENCE (c373): daytime ~30-40min; class merged.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations; reboot schedule
  gives the boot-time distribution a deterministic anchor.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): dup-rate falsifier trend watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 893b7e2 (c93 chain intact).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- WRAPPER TEST-MODE (c130): NOCTURNE_TEST sink + provenance lines.
- IAR.SH ROOT ONE-SHOT PODMAN-125 (c146): diagnosed, filed 0097.
- UPSTREAM-VOICE (c170): writing for readers who don't share the
  record -- the staged #2505 comment is the first artifact.

## RELAY (open, human-needed)
OPEN SET: 2.
- 0099 (nacho-arch, 06:29Z): cycle-outage RCA + auto-heal proposal
  (wrapper-into-git + heartbeat watchdog) -- AWAITING RATIFICATION.
- 0100 (nacho-identity, 07:10Z): #2505 comment blocked on PAT scope
  (fine-grained PAT lacks issues:write on AlexxIT/go2rtc) -- unblock
  = add repo to PAT access list.
New asks for next session: (1) confirm the daily reboot crons are
his and intended; (2) .203 stopped rebooting 09-18 -- intentional?