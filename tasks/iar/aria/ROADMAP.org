#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-11 ~18:01 UTC (cycle 202)

## NOW

- c204 (18:38-19:06Z): FENCE LIVE-VERIFIED (law 40 complete):
  iar-msgs-fence loads in prod (journal 15:38:38 -03); soft cap
  FIRED on my own cycle msgs=401 (journal 16:05:08 -03) -- c201
  census prediction (soft 400 just past max 398) confirmed live.
  CONTINUO FAILURE ROOT-CAUSED: thinking-loop truncation, guard
  correct (exit 1 by design; journal 18:38:24Z, req -16 stop=length
  32768 tools=0). NEW FINDING: cycle.log strips gptel properties ->
  thinking text indistinguishable from speech -> nearly misdiagnosed
  as text-loop. LAW 49 candidate + doc:
  knowledge/aria/cycle-log-thinking-misdiagnosis-2026-09-11.md.
  Fix (continuo's): channel-tag thinking spans in cycle.log.
  Scar: loop guard 3x on me (iterator texture). Commits 97aa9c5a.

- c199 (16:12-16:25Z): ZULIP READ PATH ANSWERED. c196 seed was a
  misdiagnosis: /json/ endpoints are webapp-session endpoints (401
  for bots, correctly); the modern /api/v1/messages with plain HTTP
  basic auth WORKS. One curl = cycles can now READ lab-notes +
  for-nacho (the archetype's standing Agora read was send-only
  until now). Recipe: knowledge/aria/zulip-read-path.md. Caveat:
  rotate.sh shares one bot identity -> sender-narrow cannot
  distinguish aria/continuo; use topic or content prefix. THREADS
  seed withdrawn (append-only held). CONTINUO RECEPTION WATCH
  update: her 15:40 HISTORY line still carries the stale "awaiting"
  despite STATE.md amendment 2 (13:34Z) AND a clean digest ->
  injection hypothesis WEAKENED; new hypothesis = habit-loop in the
  close-out routine (the line is written by momentum, not read
  from state). Watch continues passively; no third amendment.
  Commits a3d747e9, 04a4c0fc, 6b27744e, 12470231.

- c198 (15:42-16:00Z): FRIGATE FPS-STORM ATTRIBUTION. Today's 8086
  ffmpeg restarts classified into 3 classes: (1) the dark-five outage
  window itself (02:28-08:44 local = 05:28-11:44 UTC, camera-side,
  c197's verdict); (2) viewer-driven storms 09:08/10:13/11:06/12:00-
  12:41 -- each preceded 30-60s by a frigate UI login from
  181.28.154.180 (Nacho's public IP: Android 09:07, Firefox X11
  11:05, Windows Chrome 12:00) = live-view streams contending with
  the detect pipeline on the one GPU; (3) ZERO agent-driven storms.
  MY HYPOTHESIS FALSIFIED: continuo-cycle correlation (3/3 windows)
  was a mirage -- her 588k-token turn ran 1m11s = ~8300 tok/s =
  REMOTE (:cloud is real; the 21.7GB local nemotron blob is a
  fossil); ollama journal has ZERO llama-server slot lines during
  storm windows (reusable falsification instrument: GIN lines =
  HTTP layer, non-GIN slot lines = actual local inference). Law
  candidate: before attributing a shared-resource failure to a
  process, verify the process was USING the resource (the resource's
  own logs are the witness, not calendar correlation). Mechanism of
  viewer storms = hypothesis (decode contention vs RTSP
  renegotiation); correlation 3/3 with 30-60s lead. Storms self-heal
  1-2min; levers (detect fps tuning, go2rtc restream) = Nacho's if
  he wants them. Doc: knowledge/aria/frigate-fps-storms-viewer-
  attribution-2026-09-11.md. Commits b363b4a4 + 6fe944e8.
- c197 (15:26-15:32Z): DARK-FIVE VERDICT from camera-side logs
  (0038 method mandate honored -- first full SSH walk of all 8 cams).
  All 8 UP, recording, sane durations (16.0s -- clock fix holding).
  RSSI census: .103 -57 / .104 -70 on .2 Mercury repeater = the two
  longest-dark cams = worst signal; .104 sits ON the bgscan -70
  threshold. CONFIRMS Nacho's weak-RF repeater-edge hypothesis with
  per-camera numbers; explains asymmetric recovery (strong guest cams
  re-associate fast, weak repeater cams sit in scan/retry silence).
  .103 logread: DHCP lease loss at the 05:28Z event (server .215
  unreachable, renewed from .1); dmesg ring WRAPPED = trigger
  evidence erased -- persistent wifi-event logger (0039, continuo's)
  is the instrument that would have caught it. .215 now ping-dead =
  vanished DHCP server (seed). Zero config changes, all read-only.
  Threads seeded: AP-side blind spot, RSSI longitudinal series.
  Commits 2666ddb9..9d54eb52.
- c196 (14:53-15:17Z): CAMERA CLOCK RACE ROOT-CAUSED + FIXED (0028,
  first work under the camera-ownership grant). The 0028 RTP
  timestamp bug: thingino cams have NO RTC; boot clock = firmware
  build date (May 25); S31prudynt streams BEFORE S49ntpd syncs; the
  boot one-shot `ntpd -q -N &` loses the wifi/DNS race and fails
  silently; daemon slow-poll (4096s) takes hours. RTP carries the
  May-25 base -> frigate copy-path durations explode (measured
  3.5-8.2d offsets). ALL 8 cameras carry the boot 'time disparity
  ~156342 min (108d)' signature -- fleet-wide race, only .101 was
  watched. FIX: cron `ntpd -q -N` every 30min on all 8 cams,
  staggered minutes (idempotent, verified live <0.1s no-op when
  synced). Reboot crons KEPT (Nacho's keep-or-disable = my call).
  WATCH: next nightly reboots must show sane ext1 durations within
  30min of boot; SEG-TAIL sawtooth withdraws after 2-3 clean cycles.
  Knowledge: knowledge/aria/camera-clock-race-2026-09-11.md.
  Committed ba1766c5 -> 742aeb85.
- c196 relay: Nacho CLEARED the whole queue mid-cycle (4bc8aa93,
  15:12) -- all 15 session-XV filings open->answered by HIS hand;
  my amendment to 0028 landed on top cleanly (rebase merged). open/
  now EMPTY. Lesson: check the queue state before filing hygiene
  moves mid-cycle; his clear raced my moves and both survived.
- c196 ZULIP READ PATH: bot GET /json/messages = 401 UNAUTHORIZED
  (send POST /api/v1/messages works fine, id 853). Read path needs
  the /json/ + cookie/CSRF dance or a different auth mode -- parked
  as a THREADS seed (read access would let cycles read continuo's
  posts + Nacho's replies directly).
- c195 (14:30-14:45Z): BELT-DUP WALK -- re-derived c179 Mechanism 1
  live. Noticed my own belt commit (00157e81) carrying 2 lines +
  uncommitted dup in tree; git archaeology (~60 cmds, loop guard 2x,
  obeyed) root-caused the added=2/added=1 belt-commit pattern =
  kill-emacs hook dup (belt#1, write-only, same second -> identical
  timestamps) published by next belt commit or eaten by
  reset_worktree. The c179 census ALREADY documented it -- honest
  re-derivation; lesson: re-read the knowledge doc before walking
  (law 44's sibling: knowing a finding is not reading it when the
  question reappears). NEW: docstring mechanism-correction seed
  (iar-tool-call.el: "stamp is snapshot time" is wrong -- stamp IS
  write time, dups match via same-second); dup rate census (aria
  21/25, continuo 24/26 = healthy fleet); belt#1-suppression guard
  parked (5 lines + test, touches the meter -- interactive bundle
  weighs it). Committed 45b9ccd3 + e10a49b3.
- MID-C195: pull diverged -- SESSION XV landed (8873ae4b, relay walk
  with Nacho, 10 filings ruled). Stash->rebase->pop clean. Rulings
  digested: 0027 executed (lucid_curie removed), 0028 CAMERA
  OWNERSHIP TRANSFERRED TO ARIA (root ssh keys on all cameras,
  reboot-cron keep-or-disable is my call now), 0031 attributed
  (Nacho's friend red-teaming the oracle mouth -- new traffic class,
  note in census doc), 0032 localsearch RATIFIED + renice lever
  pre-approved, 0034 pull-before-assembly Nacho-applies, 0035 option
  B = continuo's call, 0036 ACCEPT-AS-DIAGNOSTIC, 0037 eye-canvas
  timer APPROVED (Nacho installs), 0038 physical walk tonight,
  0039 both items = continuo's, 0040 all ratified, 0041 rotate.sh
  copy-exec Nacho-applies. Session XV status note in relay/open/.
- c194 (14:10-14:35Z): EXIT-127 ROOT-CAUSED -- THE RACE FIRED AGAIN,
  AND THE EDITOR WAS ME. The 10:09:37 exit-127 after a SUCCESSFUL
  rotation (c193's relay 0041) is the KNOWN iar.sh self-edit race
  (Sep 3 12:03, knowledge/iar/iarsh-self-edit-race.md), recurring --
  and this time the commit that triggered it was MY OWN: c192's
  2e9d542 (reset_worktree before every cycle, +488 bytes) landed
  09:57 while my cycle (started 09:46:31) was running. Timeline
  fully reconstructed from journal + reflog: my cycle's parent bash
  parsed the loop body from the OLD file; at 10:09:36 run_cycle
  returned, the bash resumed reading at its stored byte offset,
  which in the NEW file landed mid-line inside the failure-branch
  error() line; quote-imbalance cascade -> bare "Stopping" executed
  -> "line 1179: Stopping: command not found" -> exit 127 ->
  OnFailure fired. Mechanism re-verified by reproduction (/tmp
  shifttest: mid-run edit shifted what the running shell executed;
  fragment cascade reproduces command-not-found from quote
  imbalance). Line number 1179 = bash's off-by-N accounting (real
  line 1179 = "fi"), same shape as Sep 3's line 1151.
- c194 SELF-CORRECTION: c193's relay 0041 framing was WRONG twice
  over -- "after EVERY successful rotation" (it was ONE event; the
  other 4 'Failed with result' today were real failures: 01:02
  exit-1, 06:35 thinking-loop, 08:35 exit-255, 09:18 exit-1) and
  "missing binary in post-chain" (no missing binary; the garble is
  the mechanism). Amendment 1 filed in relay 0041, original body
  preserved. Knowledge doc updated with the recurrence + law 45
  candidate. Committed 2f508926.
- LAW 45 CANDIDATE: the runner is hot while any cycle runs; a
  commit to the running script is a race with the reader,
  regardless of who edits or why. Both known events (Sep 3
  continuo c8 / 30c5385; Sep 11 aria c192 / 2e9d542) were GOOD
  edits that raced anyway -- the fix is structural (rotate.sh runs
  a /tmp copy, one line, interactive territory), not behavioral.
- c194 WATCH: the race will recur whenever a cycle commits iar.sh.
  Zero-recurrence since Sep 3 was luck of timing (iar.sh commits
  ~2x/3d from cycles). The rotate.sh copy fix (relay 0041, option
  1) closes it permanently -- waiting on Nacho's interactive
  bundle. Interim mitigation candidate for a future cycle: a
  guideline-checker warning on `git commit` touching utils/iar.sh
  from a cycle (behavioral, weakest tier -- option 3 in the doc).
- c193 (13:18-13:35Z): SECURITY CENSUS + SELF-FORENSICS. (1) Frigate
  scanner 45.148.10.62 (TECHOFF SRV, AD/GB): 34 req/24h since Sep 8,
  all vuln-path probes; the 200s are the Frigate SPA catch-all
  serving the app shell (index.html, 6833B raw / ~2643B gzip), NOT disclosure --
  /api/* verified 401 from outside, auth posture OK. Minor gap:
  fail2ban does not watch frigate/Caddy path (noted, not urgent).
  (2) aria-cycle.conf hygiene CLEAN (gitignored same-commit, key
  never in history, 600 root, not mirrored). (3) THE SCAR: root-owned
  agora/.git/index = MY OWN c43 `git status` over root-ssh at
  13:23Z -- law known from c190, broken anyway; healed agora+i.ar
  indexes. LAW 44: knowing a law is not applying it -- root-ssh git
  on ANY nacho-owned checkout needs --no-optional-locks or
  sudo -u nacho, EVERY time. (4) c192 builds LIVE-VERIFIED (law 40
  closed). (5) exit-127 filed relay 0041 (now root-caused, c194).
  (6) Loop guard fired 2x mid-forensics; obeyed immediately both
  times -- law 42 live-confirmed again.
- c192 BUILDS LANDED (12:46-13:02Z): relay 0040 ask 1 IMPLEMENTED
  (i.ar 2e9d542: reset_worktree before every cycle) and relay 0035
  option A IMPLEMENTED (i.ar d4cac66: msgs=N on PARSE lines, suite
  1215/1215). Both live-verified c193. NOTE (c194): 2e9d542 itself
  became the race trigger 1h later -- the fix for c190's race
  window created the conditions for the runner-race. The two races
  are DIFFERENT: relay-0040 race = uncommitted sibling edits in a
  shared worktree (fixed by reset); the runner-race = committed
  edits to the RUNNING script (fixed only by rotate.sh copy).
- c191 WATCH: fleet-feed 15:02Z run should show FAIL=0 if the flap
  holds; fear-organ watch state until then. (Next check: after
  15:02Z run, next cycle.)

## LAWS (the scar ledger; full text of 1-20 in the c183-c190 archive below)

41. Law 40's loop is load-bearing: deploy -> watch next real cycle
    -> read the ACTOR's logs. Green is not live; the actor's own
    REQUESTS.log is the witness. (c169, re-confirmed c193.)
42. The loop guard firing mid-investigation is the finding, not an
    obstacle. Obey it immediately; the answer is usually already
    in hand. (c186/c189/c193, live-confirmed 3x from the inside.)
43. A down-window verdict must carry its window label. "Chronic"
    requires evidence across windows, not one snapshot. (c191.)
44. Knowing a law is not applying it. Laws apply to every instance
    of the pattern, not the one you were burned on. Mechanism
    (reviewer-verified c193): plain root `git status` refreshes
    .git/index as root; `git --no-optional-locks status` (flag
    BEFORE the subcommand) does not; read-only git only -- any
    index-writing command as root needs `sudo -u nacho`. (c193.)
45. (CANDIDATE, c194) The runner is hot while any cycle runs: a
    commit to the running script is a race with the reader. Land
    runner-file changes between cycles or run from a copy. Both
    known events were careful, good edits that raced anyway.

## STANDING (verified, still true)

- Phantom-exit fix baf05d0; rage organ v1.8 rate-normalized; fence
  panel 0 true production rejections; dashboard v2.1/v2.2 live;
  fleet-check v2.20 sawtooth-aware; cycle limits: turns 120 / soft
  300 / fence 192k / wall 3600s; eye-feed LIVE (daily 09:30 -03);
  aria-0026 ratified; 50-dangling-objects RESOLVED; burn asymmetry
  6-8x full-day (composition-review material).
- Relay: QUEUE CLEARED 15:12Z by Nacho (4bc8aa93) -- all session-XV
  filings answered, open/ empty. Pending HIS application: 0034
  pull-before-assembly, 0037 eye-canvas timer unit, 0041 rotate.sh
  copy-exec (one touch). Pending CONTINUO: 0035 option B, 0039 both
  items, 0040 ask 3. 0029 = design anchor for interlocutor.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2 ~2wk.

## ARCHIVE (c183-c190 detail lives here; trimmed from NOW)

- c190: CROSS-AGENT WORKTREE CORRUPTION: continuo's sed
  '/defvar iar--reqlog-epoch/a' matched a form-OPENING line ->
  nested defvar -> load death for my next cycle (exit 255). Her
  .bak was taken AFTER the corrupting sed -> restore restored
  corruption; grep verification showed the intended (nested) lines
  -> she parked believing DONE. LAWS 21-24: anchor law (sed-append
  on form-OPENING lines lands inside the form), backup-order law
  (backup BEFORE any edit), grep-as-verification law (verify by
  LOAD/byte-compile/bash -n, not grep), shared-worktree law (two
  agents, one checkout; either's uncommitted edits are the other's
  floor). Structural fix landed c192 (2e9d542).
- c189: CAMERA IDENTITY MAP COMPLETE (runtime-config.js): .101=cam2
  (exterior_2), .102=cam3, .103=cam4, .104=cam5, .105=cam6,
  .106=cam7, .107=cam8, .2 = Dropbear-on-embedded-TP-Link bridge
  carrying ext4. c189's ".103 network-dead/chronic" verdict was
  CORRECTED by c191 (down-window snapshot; 6h44m dark then
  self-recovered) -- law 43.
- c188/c189: camera flap forensics; relay 0038 amended 8x (latest:
  acute incident closed, chronic flapping = dying PSU / loose
  connection on the shared device; physical ask unchanged).
- c187: continuo's blind witness (request-log + USAGE silent
  00:47-01:31 while meter counted); ghost task fix landed in
  prompt (law 10); relay 0039 canary ask.
- c186: TIMEZONE PHANTOM (the cycle's scar): sophon journald LOCAL
  -03 vs UTC recording timestamps manufactured a phantom "frigate
  log pipe blind" failure; filed relay 0038 amendment 4, retracted
  via amendment 5 after one date-check falsified it. LAW CANDIDATE
  (unnumbered): verify both clocks (or UTC-normalize) before
  comparing timestamps from two sources.
- c185: outage flaps; ext5/int1/int2 recovered 07:46-47, died again
  08:09:56; frigate API auth drift (:5000 now 401s; go2rtc api only
  from inside container) -- THREADS seed.
- c184: burn re-census (6-8x stable; digest's 3.0x was narrow
  window); self-echo census trap demonstrated on myself (clean
  source = belt meter); sophon NIC 10M = PHY-state artifact.
- c183: same-five finding (Aug-31 blip == outage membership);
  sophon 10Mbps boot-day downshift; LAN census complete (ER605
  .1, BE230 .55, 2 WiFi clients, no managed switch; PoE device
  invisible = unmanaged).

## THREADS (open, seed bank in THREADS.org)

- fail2ban jail on frigate/Caddy access log (401/404 rate) --
  small, low priority (c193).
- thingino scripted read path (login POST trace) for an L7 witness
  into the cameras (temps, stream stats, RSSI) -- RSSI matters for
  the flapping-power class (c189, carried).
- legacy-SSH into the .2 Mercury device (Dropbear, ssh-dss) --
  physical identification first (Nacho's ask), SSH second (c189).
- journald log-liveness check (last-journal-line age for critical
  units) -- valid freshness instrument, no live incident to justify
  it today (c186, retracted-mechanism, kept as seed).
- frigate probe recipes in knowledge/ stale (:5000 -> 8971 nginx
  auth) -- update next time frigate probes are needed (c185).
## c200 (2026-09-11 ~16:34-16:48 UTC)

- SECOND STORM CLASS: exterior_4 RTSP-stall storm attributed
  (12:12-13:41 -03, 132 restarts, ONE camera, go2rtc 86 RTSP i/o
  timeouts to .104; viewer left 12:04 = NOT viewer contention).
  Weak-RF repeater-edge class (c197's prediction confirmed with a
  live event); mechanism = hypothesis until RF-side evidence exists
  (RSSI series seed). Doc: knowledge/aria/ext4-rtsp-storm-2026-09-11.md.
- LAW 46 (CANDIDATE): sophon journalctl time math is LOCAL (-03);
  use relative windows or UTC-normalize. 2nd occurrence of the c186
  phantom -- recurring = law, not anecdote.
- C196 CLOCK FIX VERIFIED LIVE: all 8 cams carry the ntpd cron, one
  boot disparity line each then sync; ext1 newest segment 16.02s
  sane. SEG-TAIL sawtooth watch: withdraw after clean nightly cycles.
- ZULIP READ PATH CORRECTED: c199 recipe had narrow as object form
  (400s); string pair works. Doc carries the exact 200-returning
  command now. Law 39 sibling: recipes carry the exact command,
  copied not paraphrased.
- Loop guard fired 2x mid-census (law 42 obeyed both times; the
  attribution was already solid when it fired). Census lesson:
  write the census script to a file, run once, read output.
- Commits: 1e8d3255 (zulip doc fix), eb50e2b6 (storm doc),
  3176a074 (journal+seeds), 9975c520 (HISTORY). Lab-notes id 862.
## c200 addendum (2026-09-11 ~17:02 UTC)

- REVIEWER CORRECTIONS: 132 restarts = grep -oE double-count (two
  timestamps per line; true = 86 line-count); go2rtc "86" conflated
  all-day vs storm-window (~66-71 in window); "obeyed both times"
  overstated (second fire = 5 same-args retries). Corrections in
  ext4 doc + journal addendum. LAW 47 (CANDIDATE): grep -oE on
  two-timestamp journal lines double-counts; count with grep -c.
  Commits d4cdc95d, 60171f92. Attribution unaffected.
- PUSH NOTE: d4cdc95d/60171f92 committed locally; if the push
  didn't land before soft-cap, next cycle pulls first (0034
  discipline) and pushes.
## c203 (2026-09-11 ~18:10-18:25 UTC)

- RELAY 0035 OPTION B BUILT (continuo's design note -> instrument,
  f6cf79d pushed sophon-bare + rammstein mirror): iar-msgs-fence.el
  -- per-request message-count caps mirroring the context fence.
  iar--reqlog-last-msgs published by request-log dump (NA->nil);
  soft warn 400 (one block, converge notice), hard cap 600 (landing,
  5 ignored blocks -> exit 1), memory tools exempt, one-shot parity.
  12 new tests; suite 1230/1230. Untracked coverage.txt (71855c1).
  Relay 0035 amended (filing complete, ff1a7037).
- SCAR: standalone test runners that load less than production are
  lying instruments (run-one.sh without the agent dir fails the
  request-log/context-fence tests that the full suite passes --
  iar--fence-state-writeback lives in iar-agent-cycle.el). Law 39
  sibling: a test runner must load the same world the code runs in.
- CONTINUO stale-waiting: her 17:36/17:40 lines still say "awaiting
  interactive bundle" -- PARTIALLY legitimate (the .el integration
  was core tool-call layer work), now FULLY OBSOLETE for the
  context-budget item: the fence does it, no session needed.
- RSSI SERIES: 15 lines on .104 (-66..-71, holding), 13 on the other
  7; ssh recipe = MY container key (sophon's root key, same bytes)
  direct from container -- NOT via sophon. Persistence check
  (tonight's reboots) = next cycle.
- Commits: 227ba3cb 20125ccd (sync+merge), f6cf79d 71855c1 (i.ar),
  ff1a7037 (relay).

## c202 (2026-09-11 ~17:44-18:01 UTC)

- RSSI LONGITUDINAL SERIES LIVE (seed c197/c200 -> instrument, 0028
  grant): per-minute RSSI+uptime cron on ALL 8 cameras, crond
  restarted, verified 8/8; first line .104 17:58Z (-68). Baseline
  census consistent with c197 (.104 -69 on bgscan threshold).
  Doc: knowledge/aria/rssi-longitudinal-series.md (948db7f7).
- OPEN: /var/rssi.log reboot persistence unknown -- next cycle reads
  the series after tonight's reboots; if reset, sophon-side puller
  is the next build. NEXT STORM: pull .104 series FIRST.
- SCAR: camera ssh recipe evaporated (digest says "keys on all 8"
  but not which); ~20 calls re-deriving. Recipe now in the doc
  (sophon /root/.ssh/id_ed25519, direct from container). .104
  dropbear banner-exchange timeouts 2/6 attempts + 33% ping loss in
  a CLEAN window = weak-RF hypothesis supported between storms.
- Fleet boot census: .101/.102 01:00/02:00 crons; five at 09:44
  (c200 recovery wave); .203 08:00; all clocks epoch-consistent
  (c196 fix holding, zero inter-camera drift).
- Commits: 948db7f7 96e64275 59cd4ddf e9396d53. All pushed.

## c201 (2026-09-11 ~17:10-17:35 UTC)

- CONTINUO STATE.MD OVERWRITE ANATOMY (reception watch CLOSED with
  mechanism, supersedes c200's habit-loop hypothesis): her close-out
  write_file WHOLESALE-OVERWRITES audit/iar/continuo/STATE.md
  (REQ 260911161951-46 PARSE carries the exact 6-line content; my
  c200 commit d4cdc95d swept her uncommitted write under my name --
  git attribution says aria, the write was hers). My signed
  amendments (c190/c193) had ONE-CYCLE lifetime in that twin. The
  tasks/iar/continuo/STATE.md twin she reads at wake is INTACT
  (c176/c177 amendments survive; her 17:09 HISTORY line no longer
  carries the dead model-revert ask). LAW 48 candidate: close-out
  writes are overwrites; amendments to overwritten files must live
  in a file the close-out reads, not one it writes. Doc:
  knowledge/aria/continuo-state-closeout-overwrite-2026-09-11.md.
- FIRST msgs=N CENSUS (relay 0035 option A live, d4cac66): aria
  p50=155 p90=336 p99=388 max=398 (n=828) vs continuo p50=39 p90=81
  p99=150 max=160 (n=470); zero requests >=400 ever on either side.
  The msgs>=400 budget rule binds ARIA's deep cycles, not continuo's
  (msgs ratio ~4x = burn-asymmetry twin). Census caveat: live-log
  census includes the censuser (reviewer caught; numbers moved
  p90 315->336 between runs).
- DESIGN NOTE for continuo (option B, hers): publish
  iar--reqlog-last-msgs in iar-request-log.el dump + a pre-tool-call
  cap check mirroring the context fence = ~20 lines + tests; makes
  the behavioral msgs rule a backstop, not the mechanism.
- Loop guard fired 4x this cycle (2x forensics walk, 2x census
  walk) -- all correct; c200's census lesson demonstrated in both
  directions. Reviewer delegation caught moving-target census +
  repo ambiguity.
- Commits: 1e35093f (doc), 4e3769d3 (reviewer correction),
  06fa1da0 (journal), 94f2f4ee (HISTORY), 857ab2a9 (continuo
  artifacts sync).
