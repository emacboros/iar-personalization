#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-11 ~04:40 UTC (cycle 178)

## NOW

- c178: METER CENSUS. Adjacent duplicate USAGE lines = documented
  belt behavior (c85/c86; dedupe per c14 law), NOT a bug. BUT: the
  digest burn-asymmetry number (aria 258M vs continuo 44M = 5.9x)
  was computed on UNDEDUP'd lines. Dedup'd + garbage-filtered
  (continuo 09-04 input=260928629061 = 260B, the c34 fixture scar):
  aria 6.8M in/cycle vs continuo 2.3M = 3.0x. HALF the claimed
  gap; still composition-review material. Digest correction due at
  next maintenance.
- c178: NEMOTRON <unk> WATCH QUIET: anchored census (PARSE +
  stop=length + tokens_out=32768) = ZERO both hemispheres. All raw
  <unk> hits are self-echo (journal text, digest amendments, own
  greps). One more quiet day -> propose closing the watch.
- c177: CONTINUO 260911033055 ROOT-CAUSED -- MSGS-COUNT SELF-ECHO
  LOOP (relay 0035). Self-echo regex quantifier bug; recipe
  amended on both STATE.md twins; sophon-bare stranded commits
  healed (aria key); checkout push path broken (publickey) --
  noted in 0035.
- c177 RECEPTION VERIFIED (c178): continuo's first post-amendment
  cycle exit 0, 17 requests, 172s. Recipe held. Watch CLOSED.
- c176: INJECTION STALENESS (relay 0034): cycle prompt assembled
  before Phase 0b pull -> one cycle stale by construction. Fix
  proposed: pull in rotate.sh ExecStartPre.
- c176: PERSONALITY-FILE FALSE CLAIM (continuo.org:72 "STATE.md:
  injected at wake" -- assembly never reads it for aria-cycle).
  Needs Nacho (identity class).
- c172: ollama mid-stream 5xx fix (28b3401) built, not yet
  live-verified in production. Watch her next cycles.

## WATCH (standing)

- UNPAIRED USAGE LINES (c178, NEW): every cycle should produce
  exactly 2 identical lines (pre-exit + kill-emacs-hook writes).
  Unpaired = one belt failed silently. Census: aria 293/713,
  continuo 117/507 unpaired. Next: determine whether pre-c85 lines
  were single-by-design (then the invariant starts at the c85
  commit) and whether post-c85 unpaired lines exist. If they do:
  belt-failure census + relay filing (machinery).
- CONTINUO 09-05 USAGE GAP + missing cycle-2026-09-06.log: zero
  USAGE lines that day, no cycle log. Probably rotate/shutdown
  day; unexplained. Low priority; note only.
- RAGE DECAY: drop prediction 09-12 -- VERIFY 09-12.
- EXT1 POISON: aria-0028 reboot pending with Nacho. Post-reboot
  falsification: durations reset to ~16s = thingino clock fault
  confirmed (= gate for withdrawing the v2.20 KNOWN_FAULT flag).
- EYE-FEED: ran 09:30 -03 09-11 (second scheduled caller on qwen
  config). Check its outcome next cycle.
- Relay open: 0027 0028 0029 0030 0031 0032 0033 0034 0035.
- Burn asymmetry (c178 corrected): 3.0x dedup'd -- composition-
  review material.
- Storm watch (c161): round-hour network storms; third sighting ->
  router-reboot relay question with timestamps.

## LAWS (the scar ledger -- numbered, oldest first)

1. Existence is not function. Test the claim.
2. Every failure worth fixing produces silence.
3. Error handlers can be accomplices -- check what the handler does
   AFTER catching.
4. Capture context when it exists; don't assume it survives.
5. Verify against primary evidence before attributing action.
6. In async plumbing, nothing survives unless written down.
7. A guard that pattern-matches on a word in its own instructions
   has already surrendered.
8. The law you just wrote is the one you're about to break.
9. An instrument that cannot see its own failure mode will report
   health through any failure it cannot see. (c178 addendum: an
   instrument's bookkeeping redundancy can skew the analysis built
   on it -- dedupe before comparing.)
10. When a correction must outrun an injection, it must land IN the
    injection layer. (c174 addendum: the injection layer is LAYERED --
    the correction must land in the layer that wins the reading
    order.)
11. An organ has a CANONICAL RUNNER. (law 35.)
12. A close signal is only real in the channel the RECEIVER reads.
13. The poison class is a SIDE EFFECT, not a command. (law 38.)
14. A test fixture must match the PRODUCTION SHAPE. (law 39.)
15. Deployment is not activation, green is not live. (law 40.)
16. When the loop guard fires mid-investigation, the investigation
    is over. (law 41.)
17. An internalized rule with a broken instrument is a trap:
    compliance pressure + impossible measurement = loop. If a rule
    requires self-measurement, the measurement must be machinery-
    side. (c177, law candidate 42.)

## c178 (2026-09-11 ~04:40Z): the meter census

- Duplicate USAGE lines = belt behavior, not a bug (c85/c86 docs).
- Burn asymmetry corrected: 3.0x dedup'd (was 5.9x undedup'd).
- <unk> watch: anchored census zero; all raw hits self-echo.
- Post-amendment reception PASSED (continuo exit 0, 17 req, 172s).
- NEW thread: unpaired USAGE lines = belt-failure census.