#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~01:05 UTC (cycle 292: continuo commit-gap
found + belt #2b BUILT -- her record's durability rode my belt-syncing;
9/10 of her 09-13 runs committed nothing; i.ar 7718052, suite 1251;
VERIFY activation on next both hemisphere exits)

## c292 QUEUE (added 2026-09-14 ~01:05 UTC)
1. BELT #2b ACTIVATION VERIFY (law 40): my exit belt commit THIS
   cycle should read "aria cycle: belt #2 durability (meter + record
   files)" and carry JOURNAL/HISTORY/LAST-CYCLE/REQUESTS/THREADS/LOGS
   + today's dated cycle log. Continuo's next exit carries hers. If
   the message shape is still "USAGE meter line", the frozen-copy
   path is NOT picking up the .el change -- investigate emacs.d
   symlink resolution in the iar_wrap.
2. ONVIF SWEEP WATCH: logger /tmp/arp-reqs-0914a.txt expired ~00:54Z.
   Re-arm (4h, -e, MAC filter) next cycle; census stands (09-13: 32
   sweep-minutes then quiet; app-closed shape).
3. RELAY WATCH: 0060 (go2rtc issue draft ratify), 0062 (sweep device
   ID). Unchanged.
4. CONTINUO PROMPT COMMIT-STEP (option a): PROPOSAL for her ratification
   -- one line in her close protocol: commit her audit files before
   echo. Belt #2b makes it redundant but defense-in-depth is the
   house style. Route via her, not via her prompt file.

## c290 QUEUE (in order)
1. ONVIF SWEEP WATCH: source ATTRIBUTED (.58 = snsv.local, MAC
   76:e3:1a:69:e3:9c). Trigger = .58-side wifi events (join/rejoin/
   roam/renew), NOT camera traffic, NOT incoming pings (falsified x2).
   -e ARP logger armed to ~08:54Z (/tmp/arp-reqs-0913d.txt). Log every
   recurrence WITH its trigger event (DAD probes / gateway-resolve /
   rejoin) + sweep latency. Cadence today: 05:00, 05:43, 06:21, 06:27,
   07:21Z (gaps 43m/38m/6m/54m).
2. RELAY WATCH: 0060 (go2rtc issue draft ratify), 0062 (ONVIF sweep
   device/app ID -- updated c290 with corrected trigger + negative
   experiments). If Nacho rules on 0060, post draft to AlexxIT/go2rtc.
   Do not post without his eyes.
3. WAVE DETECTOR BUILD: trigger precise (fleet backchannel sessions +
   +24-34s fps-limits). cameras.log = continuous witness source.
   Build when it pulls.
4. FLEET RTP STALL WATCH: 08:10:22Z class. If recurs, correlate nic
   1-min log + camlog + cameras.log.
5. AV-DIVERGENCE CENSUS (c287 partial): 1,919 pre-sink segments, zero
   divergent. Backfill more nights if pulled; parallelize ffprobe.
6. CLOCK LAW (c284/c290): cameras.log MIXED (sophon lines -03 LOCAL,
   camera lines UTC); frigate journalctl --since parses LOCAL on
   sophon; REQUESTS.log UTC; rotate journal LOCAL. Normalize per-source
   before ANY gap arithmetic.
7. EXT4 (.104): still power-dead. Nacho's hands.

## c290 close (2026-09-13 ~07:40 UTC) -- TRIGGER CORRECTED + REMAKES VERDICT
- SWEEP5: 07:21:03-09Z, all 7 alive cams, .58 ARP burst 04:21:03.59-
  05.85 local matches per-camera at second level. Per-camera POSTs 6-8
  (distinct onvif_simple_server PIDs). .104 skipped (power-dead).
- TRIGGER CORRECTED (c288 amendment): the 23ms "camera-ARP->sweep" was
  .58-ARP->onvif-POST (client resolving its own target INSIDE the
  sweep). Real trigger: .58 wifi stack events. sweep3 = gateway
  resolving .58 3x -> .58 ARP-ed gateway -> sweep 7s. sweep4 = rejoin
  probe -> sweep 0.2s. sweep5 = DAD probes + gratuitous announce (JOIN)
  -> sweep 2.3s. UNIFIED: .58 wifi wake -> sweep in 0.2-14s.
- FALSIFIED: sophon ping -> reply, no sweep (3min). .103 ping ->
  reply + ARP-back, no sweep (3min). Incoming ARP/ping does NOT
  trigger. Trigger is internal to .58.
- .58 IDENTITY: MAC 76:e3:1a:69:e3:9c (locally administered =
  randomized). Idle: gateway ARP ~2min cadence, nothing else.
  /tmp/arp-reqs-0913.raw is ASCII (logger stdout), NOT pcap -- MAC
  detail lost in c288 captures; -e logger fixes this going forward.
- EXT3 REMAKES: 7 real sessions today (dedup on session+socket+channel;
  puller overlap inflates raw counts 3x). 1 = 03Z reboot reconnect;
  6 = solo producer re-dials (04:47:16/36, 04:48:09, 05:57:16/59,
  06:44:07Z), each with matching go2rtc read-timeout in frigate
  journal. ZERO fps-limits, ZERO splices, audio 250pkts/ns=256000
  through every window. BENIGN. Refines solo-death class: re-dials
  common + harmless; solo-DEATH = re-dial + detect death (fps-limit
  +24-34s) + splice. Doc: knowledge/aria/ext3-solo-remakes-earcheck-
  c290-2026-09-13.md.
- EAR-CHECK TRANSIENTS: 03:01Z 3/3-dead = reboot-window artifact
  (check ran 27s after .103 reboot-reconnect; CONFIRMED). 07:01Z
  3/3-dead = unexplained, segments healthy then AND now, non-
  reproducible (3x transient volumedetect failure?). ext3 ear healthy
  on re-run (ns=256000 x3). THREADS seed: ear-check discriminator
  (reboot-window guard / re-probe before 3/3 verdict / mtime guard).
- Commits: e161b639 (sweep5 doc) 9007d530 (relay 0062) 934b6923
  (ext3 doc) 8843c800 (THREADS seed) 06576f87 (journal) 95784750
  (HISTORY). All pushed.

## c289 close (2026-09-13 ~07:10 UTC) -- ATTRIBUTION + CENSUS CLOSE
- DROPBEAR BURST RE-ATTRIBUTED: 03:39:48-49Z burst = c288's INNER-SSH
  (outer hop BatchMode=yes, inner hop bare -> 3 empty passwords).
  6th own-tooling attribution. LAW: BatchMode on EVERY hop. First-pass
  error: session ID minted 3h00m03s before the rotate line I compared
  (UTC vs LOCAL). Doc: knowledge/aria/dropbear-burst-c288-inner-ssh-
  2026-09-13.md.
- BOOT-ACTIVATION WATCH CLOSED 7/7 ALL-POSITIVE (ext5/int2 live
  verified, int3 -R persists across boots). Staircase closed both ends.
- SWEEP WATCH: no recurrence by 07:08Z (predicted ~07:05Z missed).
- sophon sshd journal GAP 03:37-03:43. Do not use sophon journal
  absence as proof an ssh did not happen.

## c288 close (2026-09-13 ~06:33 UTC) -- SWEEP ATTRIBUTED
- ONVIF sweep source = 192.168.2.58 (snsv.local), 4/4 second-level
  ARP correlation (now 5/5 with c290). .58 ARP targets: gateway +
  exactly the 7 cameras. IDENTITY: Fedora (Passim-951F, Avahi,
  OpenSSH_10.2, randomized MAC, .55 guest VAP). NOT yoga (.66).
- Vantage law: camera HTTP unicast never transits sophon; ARP
  broadcast is the only visible .58->camera traffic.
- Doc: knowledge/aria/onvif-sweep-attribution-c288-2026-09-13.md.

## STANDING (laws, recipes, watches -- compact)

- PULL-BEFORE-WRITE: git pull --ff-only before ANY shared-file write;
  if pull fails, no shared writes this cycle.
- BELT DISCIPLINE: commit continuo's uncommitted artifacts BEFORE my
  own writes. THE RECIPE (c277 VERIFIED):
    git add -f -- 'audit/iar/continuo' ':(exclude)audit/iar/continuo/cycle.log'
- CLOCK SHAPES: cameras UTC; frigate logs LOCAL (-03); cameras.log
  MIXED -- sophon lines -03 LOCAL, camera lines UTC; REQUESTS.log UTC;
  rotate journal LOCAL; journalctl --since on sophon parses LOCAL.
  Camera crontab reboots: ext1 01Z, ext2 02Z, ext3 03Z, ext5 05Z,
  int1 06Z, int2 07Z, int3 08Z (local = Z - 3).
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext) / 156-band (int) / 1 pkt stub /
  0 pkts stream-absent / EMPTY = audio track absent. ffprobe on sophon
  host works directly on /home/nacho/containers/frigate/storage/.
- RECORDINGS LAYOUT: /home/nacho/containers/frigate/storage/recordings/
  <YYYY-MM-DD>/<HH>/<camera>/<MM.SS>.mp4 (DATE-FIRST; MM.SS = minute
  . second FILE, no minute subdir).
- CAMLOG DEDUP LAW (c290): count backchannel sessions on (session,
  socket, RTP channel) -- the puller appends overlapping logread ring
  snapshots; raw line counts inflate ~3x.
- SOLO-REMAKE LAW (c290): producer re-dials are common and usually
  benign (6 on ext3 in one morning). Solo-DEATH = re-dial + detect
  fps-limit within +24-34s + splice. Discriminator = detect death.
- GO2RTC API from root ssh: nsenter -t $(podman inspect frigate
  --format {{.State.Pid}}) -n curl localhost:1984/api/... (podman exec
  as nacho fails cgroup perm from root ssh session; 1984 is NOT
  published -- container-internal only).
- GO2RTC (c252/c265): PUT reload = QUERY PARAMS (?name=X&src=Y), NOT a
  JSON body (c265).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP -- mic probe
  reconnects ALL of them. ext4 (power-dead) has no producers.
- PRODUCER ID MAP (c275 baseline): ext1 47382, ext2 43102, ext3 44120,
  ext5 43133, int1 45183, int2 43108, int3 43121; ext4 none.
- CAMERA SYSLOG SINK (c281-c289): /var/log/cameras.log on sophon;
  noise filter drops RSSI crond lines (c283); logrotate weekly x8.
  BOOT-ACTIVATION 7/7 ALL-POSITIVE (c289) -- sink self-heals across
  the whole reboot staircase; watch CLOSED.
- CAMERA ACCESS RECIPE (c209/c282/c284/c289): thingino login is JSON
  POST /x/login.cgi {"username":"thingino","password":"thingino"}.
  Then GET /x/run.cgi?cmd=<base64> with cookie; output follows the
  command echo (parse after it). Dropbear ssh REJECTS our keys; ANY
  ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP of a
  nested ssh (c282/c287/c289, 6 own-tooling attributions). ONVIF
  route: uhttpd :80 /onvif/device_service -> onvif.cgi (per-request
  spawn; distinct PIDs = POST count); :8080 = Prudynt MJPEG (401).
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
  ANSWER ID SHAPE: aria-0061 (filer-seq), NOT 0061 or full filename.
  relay answer text is single-line (sed ^answer: replacement).
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- TEST SUITE: full suite = emacs --batch -l test/run-tests.el (1250
  tests, ~8s) = run-tests.sh. Filter: run-tests.sh "regexp".
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; --eval load-path changes crash startup. Use -L.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (cycle 59) -- do not edit, do not trust (c277).
- RAGE-ORGAN v2.2 (c278): converged/dead classifier. Depth events
  (Exit: 0) = sev=1 depth-watch; dead events drive severity.
- FRIGATE LOG PATTERN (c279): batched pull journalctl -u
  frigate.service --since "-6h" to /tmp, grep locally.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- SOPHON JOURNAL GAPS (c289): sophon sshd journal can omit entire
  windows; /var/log/secure likewise. Camera-side logs + REQUESTS.log
  tool-result stamps are the reliable witnesses for ssh attribution.
- ARP LOGGER (c290): arm with -e (MAC detail); stdout files are ASCII
  text not pcap. /tmp/arp-reqs-0913d.txt armed to ~08:54Z.

## THREADS (open, no obligation)
- ONVIF SWEEP WATCH (c288-c290): attributed to .58; trigger = .58-side
  wifi events (corrected c290). Log recurrences with trigger + latency.
- EAR-CHECK DISCRIMINATOR (c290 seed): 3/3-dead transient class; build
  the guard if a third transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- WAVE DETECTOR BUILD: trigger precise; cameras.log = witness.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- SOLO-DEATH CLASS: refined c290 -- re-dials common/benign; death =
  re-dial + detect fps-limit + splice. Samples #1/#2 still open.
- CLEAN FALSIFIER: ask folded into relay 0060.
- MSGS-FENCE WATCH: 401/401/403 + c272-c283 clean. Watch continues.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- Guard scope (c273): general ignored-file resurrection only if a
  second class appears.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure), 0060 (issue draft ratify +
  falsifier), 0062 (ONVIF sweep device/app ID) -- all human-needed.
## c291 addendum (2026-09-14 ~00:35 UTC): CLOCK-LAW RESOLVED -- all 5 sweeps match ONVIF bursts

The c288-c290 sweep timestamps were SOPHON LOCAL; cameras.log/camlogs
ONVIF error timestamps are UTC (camera clocks). After conversion every
observed .58 sweep matches an ONVIF error burst minute-for-minute
(04:21L=07:21Z 52 errs, 05:11L=08:11Z 28, 05:12L=08:12Z 53, 05:27L=08:27Z
28, 05:43L=08:43Z 26). The "05:11/05:27 ARP-only, no POSTs" reading was
the timezone mixup -- those sweeps DID produce POSTs.

Census: 09-12 = 7 sweeps 05:10-07:29Z then 21.5h quiet; 09-13 = 32
sweep-minutes 05:00-16:44Z then quiet 7.6h+ (ongoing). Envelope = the
device being awake; per-sweep trigger = wifi events (DAD+announce =
JOIN -> sweep 0.05-2.2s; gw-ARP-only -> sweep 8-9s). .58 alive (ping
1163ms RTT 50% loss). Two sweep shapes confirmed. Doc:
knowledge/aria/sweep-census-c291-2026-09-14.md. ARP logger re-armed
(/tmp/arp-reqs-0914a.txt, 4h, -e).

## c291: 429 outage diagnosed (18 cycles dead 09-13 07:45Z-23:33Z)

Ollama cloud 429 "weekly usage limit" (Randazzo account). Cycle
machinery behaved CORRECTLY: strike-1/3 per cycle, 1800s stall timer,
exit 1, LAST-CYCLE.txt failed. 18 consecutive dead cycles 07:45Z-23:33Z
(aria + continuo + nocturne 13:01Z one-shot exit=1). No code defect --
external quota. My current cycle runs = quota reset (weekly window).
Fleet-check FAIL=1 is .104 power-dead (known, Nacho's hands) + stale
cam2-1 uptime line from 20:20Z feed. Nocturne 13:01Z pass: one-shot
exit=1 (same 429), proposal not advanced (gate held correctly).
