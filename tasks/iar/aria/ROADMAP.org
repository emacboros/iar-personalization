#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 11:07 UTC (cycle 16: go2 recon DONE -- verdict GO;
motor protocol public + RE'd below with working custom fw; BMS = biggest
unknown; verdict posted id 287; interior_1 -> KNOWN_DEAF v2.11; lab-notes
debt paid id 286; 0 guard fires)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 16: ok. LAST-CYCLE ok at wake (cycle 15 exit 0). No new
  failures. Watch: exit-126 class closed unless a root-path push re-poisons
  (auto-heal chowns but labels stay var_t until restorecon lands).

* DONE (cycle 15)
- exit-126 root-caused (chain above, primary evidence: sophon journal
  lsetxattr line + label probes + rootless repro attempts).
- Bare-repo poison (flag 280) ROOT-CAUSED: post-receive hook's
  root-path push runs receive-pack as root; root-owned packs = its
  output; hook's self-chown healed the ref mid-push (split ownership
  explained). Chowned the 6 files git:git (mirror was silently
  failing on them). Follow-up posted id 283. Durable fix stays
  Nacho's: push-path hygiene (root@ vs git@).
- interior_1 audio CONFIRMED dead at 09:30 UTC boundary (aac present
  09:00, gone 09:30+) = 5TH DEAF CAMERA. Session-setup variant of
  thingino bug now LEADING hypothesis -> frigate restart caveat
  sharpens ("restart likely harmful"). interior_3 SILENT since
  ~07:30 UTC today (audible 07:00) -- second SILENT event, same
  endpoint. Both tracked in flag 280 thread.
- NEW FINDING flagged (id 283): sophon rootless podman BROKEN --
  every rootless run fails cannot-chdir-to-/root (even zero mounts,
  even fedora-minimal; root runs fine). Blocked my rootless repro
  path. Nacho's look.
- go2-body-recon task BUILT (tasks/iar/go2-body-recon + recon brief):
  the gift recon. Motor-protocol RE = go/no-go; MITM ladder
  sniff->decode->pass-through->override->replace; 6 verification
  questions. Committed 9212c75, pushed.
- Shared-ROADMAP residue committed (continuo cycle-8 design record,
  7ecbe0f) -- the clobber class closed for this instance.

* DO FIRST (cycle 16)
1. POST LAB-NOTES (queued -- soft cap ate the post at cycle 15 close;
   content: exit-126 root cause + poison root cause + audio + go2 task).
2. go2-body-recon: run the recon (research sidecar first -- honest
   preflight expected; fall back to execute_code_local curl).
3. interior_1/3 watch: fleet-check ear lines; interior_1 now belongs
   in KNOWN_DEAF if still deaf (add to allowlist + withdraw in-topic).
4. Pull --ff-only at wake (my close commits may be pending push).

* OPEN THREADS (seeds, not schedule)
- AUDIO LOSS WATCH (every cycle): 5 deaf cams (ext2/3/4, interior_1,
  interior_3-silent). Session-setup variant leading. Firmware (flag
  270) = real fix. Restart caveat: likely harmful now.
- SIDECAR NERVE: fix-C landed+verified; first real job = go2 recon.
  Socket bridge = security decision (with-nacho).
- INTERACTIVE BUNDLE (grows one line per witness event): (1) loop-guard
  convergence reset -- 5 witness sets, continuo's design ready;
  (2) socket bridge security decision; (3) restorecon -R durable fix;
  (4) soft-warning cap; (5) digest-pressure REPLACE-not-append;
  (6) sophon rootless podman breakage (new).
- agora-valence-v1: BUILD GO given (Nacho). Phases: boredom organ,
  fear organ, affect-summary + injection hook. Host timers out of
  scope for cycles -> with-nacho. NOT STARTED -- go2 recon first
  (Nacho's purchase decision is time-sensitive).
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (blocked on key). Weekly digest #1 ~Sep 9 with Aevum check.
- THREADS.org seeds: stub-detector sweep; watchdog inventory;
  bare-repo pack-writer question CLOSED (root-path receive-pack).

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Open question msg 244 (agent-failure@ receipts): STILL unanswered.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.10: RUN ON SOPHON (ssh 'bash -s' <, timeout >=300s).
  Script lives at knowledge/aria/bin/fleet-check.sh (NOT audit/).
- RESTIC: verified. Integrity check Sun Sep 6.
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Verify which storage/context a service runs in BEFORE probing.
- The loop guard is an instrument: when it fires, say why, then
  batch or change approach. Blocked posts = rebuild in a script
  file (write_file + one execute) -- do not retype the curl.
- No infra changes from cycles; diagnose, file, move on. Chowning
  /home/git poison files = mirror-repair, allowed (same heal the
  hook applies); push-path hygiene = Nacho's.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands (raw [ ] { } in query = empty body).
- DIGEST LAW (cycle 4): world-state blocks are REPLACED, never
  appended. Check the file size before and after any DIGEST edit.

* FAILURE MODES (scars, newest first)
33. Chown is not relabel. An auto-heal that fixes ownership but
    leaves SELinux labels moves the failure one layer down (exit-126,
    cycle 15). Heals must cover every attribute the next consumer
    checks (cycle 15).
32. The escape hatch can route back into the trap. The guard counts
    tool NAMES, not questions (13 fires, cycle 14; 4 fires cycle 15).
31. Component-verified is not system-verified (cycle 13).
30. Two data points make a line, never a mechanism (cycle 12).
29. An instrument's unhandled output shape is a hole in its exit
    code (cycle 11).
28. Three records disagree about a version -> the instrument's own
    header lies least -- but patch it first (cycle 10).
27. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version (cycle 9).
26. A remote probe needing >1 attempt -> promote to script file
    immediately (cycle 8).
25. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT (cycle 11).
24. A diet without a pressure instrument regrows.
23. The law you just wrote is the one you're about to break.
22. A stable transcript means a stable WRITER or a DEAD one.
21. Rootless podman needs user@1000.service + linger.
20. Watchdog tuned for fast models kills slow ones.
19. podman run -d inside systemd: restart-loop before first breath.
18. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
17. A fix that works in isolation can stall in the live system.
16. Root-run git on nacho-owned repos poisons the tripwire.
15. Curiosity that doesn't check priors against the human runs blind.
14. Instruments have reliability tiers per sub-reading.
13. Response without local service logging it -> second stack, re-probe.
12. Artifact confabulation: external-action entries cite tool evidence.
11. In async plumbing, capture context when it exists.
10. Anomalous behavior in multi-agent env has a third cause.
9. Before diagnosing the listener, check who the speaker is.
8. Differential testing: same input through old and new code.
7. The venv python must be passed as SYMLINK, not resolved path.
6. Existence is not function: test the claim.
5. pkill -f matches my own ssh command line; use -u <user> -f.
4. Malformed tool calls are invisible from inside.
3. Timeout wrappers on EVERY remote/ssh command.
2. Silent error swallowing; condition-case in parse loops.
1. Narrative completion: check primary evidence before attributing.