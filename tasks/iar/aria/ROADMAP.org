#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-04 01:37 UTC (c5, flash: CENSUS THREAD DONE -- frigate watchdog + go2rtc census banked (frigate-watchdog-census.md, fbc4a46): ext4 flappiest ~6x, STALL-class vs ext3 CRASH-class; burst A 04:00-04:53 UTC fleet-wide network blip -> for-nacho 375 (router scheduled-reboot question); burst B = known power event, boot signature mapped. OWN TZ ARTIFACT caught: c4 "zero watchdog events at boundary" wrong -- session loss loud, track loss silent (law refined). fedora@ still broken.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS: c5 ok (exit 0, ~65 calls, loop-guard tripped 2x on enumeration walks).

* DO FIRST (next cycle)
1. FAILURE-FIRST (always).
2. Organs at wake; fleet-check baseline.
3. Watch: ext4 recurrence (healed 00:29 UTC Sep 4; recurrence
   interval = systemic-vs-oneoff discriminator). NEW: ext4 is now
   KNOWN-CHRONIC (667 frigate watchdog restarts all-time) -- watch
   for the NEXT audio loss as pattern continuation, not anomaly.
4. fedora@10.66.0.5 ssh auth: re-verify each cycle; interactive
   item (11) stands.

* OPEN FLAGS (Nacho's call, all low urgency)
- 367: RESOLVED (self-healed) -- withdrawal POSTED (msg 371), closed.
- 326 (frigate restart to heal e2/int3): positive control exists
  (ext4's read-timeout heal). Noted in withdrawal post.
- 270 (camera API creds): UNBLOCK PATH NOW DOCUMENTED -- one WebUI
  login + POST /x/api-key.cgi generates the key; everything
  read-shaped becomes scriptable (metrics, events SSE, config GET).
  The one-ask is now a two-minute task with a written recipe.
- 340 (go2rtc debug logs), 290 (valence host timers).
- Zulip backup gap (TOP infra flag, for-nacho 349): /var/lib/zulip
  in NEITHER restic set.
- i.ar push to origin (git@10.66.0.1) needs Nacho's key.

* THREAD BANK (candidates for next cycles)
- THINGINO FOLLOW-UPS (thread banked in
  knowledge/aria/thingino-primary-source-notes.md): (a) does OUR
  fleet run S32prudyntwd? (needs camera creds -- flag 270);
  (b) prudynt-crash-watch.sh unexamined; (c) rtsp.debug_dump_path
  as a future instrument (which client sends garbage); (d)
  metrics/events endpoints as cheap health instruments post-key.
- Frigate watchdog census as fleet-check line (BUILD decision):
  CENSUS DATA BANKED (knowledge/aria/frigate-watchdog-census.md).
  v2.14 spec: per-camera 24h restart counts (grep "Restarting ffmpeg",
  ONE pinned pattern -- c4's 667 vs c5's 1941 discrepancy = pattern
  dependence, document the grep), go2rtc WRN per camera, 404-storm
  detector (>50/h any cam = restream down), burst detector (>10/h).
  Thresholds: alert cam >20/24h, fleet >100/24h.
- Glance rotation as standing instrument -- a BUILD decision.
- Cap-calibration bundle for Nacho: 8 instances (c4 comfortable
  at ~75 -- data point: cap not hit when landing happens early).
- gptel unknown-tool stall question (unresolved).
- THREADS.org seeds (gift, witness-friction, absence-signals):
  unclaimed; pull only if one actually pulls.
- Census dedupe: key on epoch (REQ <epoch>-N); validate pattern
  against known-positive first.

* LAWS (newest first, full text in JOURNAL.org)
- TZ-ARTIFACT LAW (c5): my own prior claims are data too -- re-check
  them against primary evidence when the thread reopens. c4's "zero
  watchdog events" was a UTC/UTC-3 confusion; the corrected law:
  SESSION loss is loud (watchdog+WRNs), TRACK loss inside a
  surviving session is silent (only segment-dB sees it).
- PATTERN-DEPENDENCE LAW (c5): a census number without its grep
  pattern is not reproducible; pin ONE pattern and document it.
- BOOT-SIGNATURE LAW (c5): camera reboot = dial REFUSED (port
  closed) -> dial TIMEOUT (host down) -> renegotiation chaos
  (fps-limit + DTS storm) -> aftershocks -> stable. ~104 restarts
  per power-cycle on ext3.
- DOCS-BEFORE-PROBE PAYOFF LAW (c4): the loaded-gun scar's
  resolution is documentation -- the API surface read from source
  replaced probing fog with a map, including a DANGER list
  (reboot.cgi, tool-upgrade.cgi, run.cgi) that must never be
  GET-probed.
- TOPOLOGY-FIRST LAW (c4): read the config that defines the chain
  BEFORE modeling failure modes -- one grep showed frigate pulls
  prudynt :554 directly; the camera go2rtc is a bystander; my
  two-hop zombie model was over-built.
- CENSUS-REFRAME LAW (c4): a "new anomaly" may be one event in a
  chronic pattern (ext4: 667 restarts, order of magnitude above
  fleet). Check the all-time census before treating an event as
  novel.
- TZ-CORRELATION LAW (c3): frigate container logs are UTC-3,
  recordings are UTC. Convert before correlating.
- PACKET-COUNT WITNESS (c3): ffprobe -count_packets discriminates
  healthy/dying/renegotiating/hollow.
- READ-TIMEOUT-HEALER LAW (c3): the producer read-timeout heals
  zombie sessions; a session that never times out never heals.
- LOOP-GUARD-READ LAW (c2): re-read captured output before walking
  source; batch remote probes into one compound ssh per question.
- LOADED-GUN LAW (turn 168): never GET-probe action-shaped
  endpoints on production devices. DANGER list now documented.
- KNOWN-POSITIVE-FIRST LAW (turn 167): validate census patterns
  against a known-positive BEFORE trusting results.
- MIRROR-WALK LAW (cycle 46): the record's "next cycle picks fresh"
  is a letter, not a law.
- COMMIT-MESSAGE LAW (cycle 45): a commit message is a claim about
  a diff; the pull-before-write guard forces the look.
- CAP-LANDING LAW (cycle 44, 8 instances): the soft cap bites
  during LANDING. c4 counter-example: landing first = comfortable.
- SSH-PROBE CHAIN LAW (continuo c27, adopted): batch remote probes.
- POINTER-VS-CONTENT LAW (cycle 42): the CONTENT is usually real;
  the pointer is what's wrong.
- INSTRUMENT-TAX (cycle 41): batch hard.
- STALE-POINTER LAW (cycle 40): cross-references get re-checked.
- PRODUCER-ID LAW (cycle 39): record producer IDs at first sample.
- MTIME LAW (cycle 38): segment mtime is NOT arrival evidence.
- PRODUCER-VS-RECORDING LAW (cycle 38): verdicts need all three.
- CODEC-PRESENCE LAW (cycle 37): offer is not flow.
- RENEGOTIATION-CONVERGENCE LAW (cycle 34): converges over ~10 min.
- FRIGATE-ACCESS LAW (cycle 33): rootless stack (user 1000);
  runuser -l nacho; recordings path; go2rtc unauth localhost:1984
  INSIDE container.
- SILENT-TRACK-LOSS LAW (cycle 32): re-confirmed c4 from the third
  angle (zero frigate watchdog events at the 23:53 boundary).
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a session state.
- PROVISIONAL-VERDICT LAW (cycle 31): recovery during a flap is
  provisional.
- RECOVERY-VERDICT LAW (cycle 30): recovery is a destructive
  decision; positive AND negative controls first.
- CENSUS LAW (cycle 29): counts gating destructive decisions need
  pattern validation.
- POINTER LAW (cycle 27): wrong pointers misdirect writers AND
  readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits to
  an undecided framing is not a fix.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final
  write misses the write.
- DIFFERENTIAL LAW (cycle 8): same input through both pipelines
  finds which layer lies.