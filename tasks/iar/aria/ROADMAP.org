#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~20:36 UTC (c335: CAMERA-CLOCK SKEW
discovered -- every thingino camera boots ~3h slow, NTP-corrects
later; segment names + hour dirs + rssi stamps are CAMERA-CLOCK,
segment mtimes + frigate/go2rtc logs are sophon real. Verified
per-camera via uptime -s vs log stamps. int1 audio re-anchored:
died ~12:05Z, healed 17:05-17:19Z watchdog restarts; 06:00Z camera
reboot did NOT heal (law-v3 falsifier). ext2 BACK (16:41:17Z) but
record proc holds video-only mapping -> class-3 audio, relay 0067.
Filing: knowledge/aria/camera-clock-skew-2026-09-14.md.)

## c336 QUEUE (next cycle)
1. NOCTURNE GATE WATCH (next pass Tue 2026-09-15 13:04 LOCAL -03 =
   16:04Z). v4 wrapper + v5 gate-watch both LIVE on sophon checkout.
   Expected: RANGE-CAP line (680-commit debt -> ~300 digested / rest
   deferred), then clean-advance-to-capped-head or ECHO-RECEIPT /
   CLAIM-RECEIPT-FAIL naming the mechanism. One call:
   bash knowledge/aria/bin/nocturne-gate-watch.sh (v5, run it FROM
   the container -- it sshes itself).
2. SWEEP-SHAPE 2 FALSIFIER (Sep 15 boot): 0914d ARP logger EXPIRES
   03:43Z Sep 15 (armed, 217 lines, tcpdump alive). NO re-arm. After
   ~01:05Z Sep 15: read /tmp/arp-reqs-0914d.txt for .58-MAC lines at
   01:00:2x-31Z + cameras.log for .101 onvif lines. Recurs = 2/4 boots.
3. EXT2 AUDIO-HEAL WATCH (c335, new): .102 back up (boot 16:41:17Z);
   record proc holds video-only mapping (class-3: negotiation
   staleness). PREDICTION: next record-proc restart heals audio
   (watchdog fires on 20s no-frames stall). If a restart does NOT
   heal -> third mechanism. Probe newest ext2 segment each cycle:
   ffmpeg -map 0:a volumedetect; "matches no streams" = still broken.
4. CONTINUO 0/0 WATCH (c334): nemotron-cloud empty-end anomaly n=2
   in 5d. Census (field-anchored): awk '$5=="PARSE" &&
   /tokens_in=0 tokens_out=0/' on her REQUESTS.log(.1). 3rd this
   week -> relay ours-direction (D-014, Nacho ratifies).
5. DAILY 429 WATCH (standing): 0 real 09-14 (field-anchored $7).
   Wall re-hit predicted Sun 09-20 ~04-12Z.
6. THINKING-LOOP WATCH (standing): continuo 1 real fire 09-14
   (19:07Z, 32768 thinking tokens, guard fired no-grace correctly).
   Field-anchored positional awk census.
7. AEVUM PULSE #5 (due ~Sep 21, weekly-only).
8. RELAY WATCH: 0042, 0045+0055, 0046, 0059, 0060, 0062, 0063
   (.104 half -- .102 SELF-RESOLVED, note in 0063), 0064, 0065,
   0066, 0067. All open, none actionable by me.
9. CONTINUO REPETITION: census III 45% dup; relay 0046 open. Watch
   dup-rate after the 0046 ruling.
10. PULSE-TEMPLATE WATCH: 4 historical literal-$(date) lines in her
    journal (predating the c325 seed); post-seed 3/3 clean. Watch
    at seed strength.
11. DIGEST-COLLISION WATCH (c331): after relay 0066 lands, verify
    her next memory pass writes audit/iar/continuo/DIGEST.md and
    NOT the top-level sync copy.
12. FLEET-CHECK CLOCK LAW (c335, new): fleet-check's ear-check
    STALE ages are computed on camera-clock-named paths -- a skewed
    camera can make fresh segments look stale. Build the
    normalization (mtime-based age instead of path-sort) when a
    false STALE/NO-SEGMENT lands. Not urgent: .201/.102 corrected
    their clocks; watch for the next skew window.
13. ECHO-RECYCLE LITERATURE (c333, parked): arxiv + semanticscholar
    rate-limited me (429s over ~20min); crossref reachable but query
    mismatch. If pulled again: single query, spaced, or research
    container. Question stands: is context-echo recycling a known
    model-class failure with a known prompt-side fix, or post-hoc-
    gates-only?

## STANDING WATCHES (one-line pointers; FULL LAW TEXT:
## knowledge/aria/roadmap-laws.md via read_knowledge)
- CAMERA-CLOCK LAW (c335, NEW): segment names + hour dirs + rssi
  stamps + camera logread are CAMERA-CLOCK (~3h slow at boot until
  NTP); segment mtimes + frigate/go2rtc journals are sophon real.
  NEVER mix in one timeline. A camera-side "reboot at 03:02Z" may
  really be 06:00Z. Verify with uptime -s read directly from the
  camera while its clock is correct.
- AUDIO LAW v3.1 + CLASS 3 (c335): class 1 straddle-at-reboot (ext2
  09-12), class 2 mid-life go2rtc audio-leg death (int1, 2
  specimens), class 3 record-proc negotiation staleness (ext2, 1
  specimen -- proc negotiated while camera dead, holds video-only
  mapping; go2rtc audio flows unconsumed). Heal = session remake;
  CAMERA reboot does NOT heal class 2 (06:00Z falsifier).
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log
  (DIGLOG best-effort); verdict greps anchor on the wrapper tag
  "nocturne-digest: " -- model prose never carries it; classify
  only this run's lines.
- INSTRUMENT-REACH LAW (c332/c333): an instrument's REACH is part
  of its schema -- test the instrument against the exact case that
  motivated it (c330), verify the DOCSTRING too (c331).
- ECHO-RECYCLE LAW (c328): a final response that byte-matches a
  prior run's final response is not evidence of fresh work.
  ENFORCED IN CODE (v4, every rc=0 run).
- BLOCK-BOUNDARY LAW (c327): a banner's POSITION is not evidence of
  which run it belongs to -- watermark-anchored extraction.
- NOCTURNE v4 (d9a2daf2+0b942d90): echo-check + claim-receipt on
  every rc=0 run; RANGE-CAP 300 commits/pass; gate advances to the
  capped head; deferred remainder logged.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066 pending).
- CLOCK LAW: normalize per-source before ANY gap arithmetic
  (c335: now includes CAMERA clocks).
- REQUEST-LOG SCHEMA (c320/c322): orphan census MUST JOIN .log +
  .log.1; real request = exactly 3 lines; orphan = post-close re-send.
- CENSUS-SOURCE MAP + CLAIM-ANCHOR: census evidence lives in
  REQUESTS.log(.1) PARSE lines, HISTORY.log, git commits -- NEVER
  in dated cycle logs.
- FIELD-ANCHORED CENSUS (c326, re-bitten c334): anchor on the PARSE
  FIELD SHAPE (awk $5=="PARSE" / $5=="START"); substring greps
  self-echo -- the log echoes its own echo.
- PULL-REEXEC LAW (c330): a script that pulls its own replacement
  must re-exec after the pull.
- STALE-RECEIPT DETECTOR v4.4: bash knowledge/aria/bin/
  stale-receipt-detector.sh <aria|continuo>.
- FLEET-CHECK: bash knowledge/aria/bin/fleet-check.sh ON sophon
  (2min runtime; ssh timeout >= 300s; /tmp/fc-rerun.txt pattern for
  async runs).
- ARP LOGGER: 0914d live until 03:43Z Sep 15, then expires.
- GIT HYGIENE: add -f for audit/tasks files; git show --stat before
  push; untrack strays AND delete disk copies; write_file resets
  exec bits (update-index --chmod=+x + chmod on sophon checkout --
  BOTH sides).
- SOPHON->RAMMSTEIN PUSH: sophon checkout has NO key for
  git@10.66.0.1 -- container-side push is the working path (0023).
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container --
  scp first. NGINX RING PULL: podman exec frigate cat
  /dev/shm/logs/nginx/current.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min; check cadence before calling stale.
- CAMERA OUTAGE: .104 power-dead (09-12, relay 0063); .102 SELF-
  RESOLVED 09-14 (power restored; audio class-3 open, relay 0067).
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z.
- FORM-NORMALIZE LAW (c325): the CLAIM's spelling and the LOG's
  spelling are different columns of the same fact.
- DEAD-CYCLE GUARD (c326): failed request + no live successor =
  immediate exit 1.
- FALSE-RECEIPT CLASS (c327/c328): verify the DISK (mtime/size),
  never the narration.
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.
- EMPTY-END ANOMALY (c334): nemotron-cloud can end done_reason=stop
  with 0/0 usage after minutes of thinking-only streaming. n=2;
  watch at seed strength; 3rd in a week = D-014 relay filing.

## THREADS (open, no obligation)
- CAMERA-CLOCK SKEW MECHANISM (c335): WHY do the cameras boot 3h
  slow (RTC-less + NTP delay? timezone misconfig?). The .102 boot1
  murk (segments with mtimes 13:43-15:59Z that fleet-check 15:02Z
  did not see) is parked -- needs a reconstructable directory
  listing to resolve. Watch, no obligation.
- ECHO-RECYCLE ROOT QUESTION (c332): can a prompt-side signal stop
  context recycling, or is it model-class (post-hoc gates only)?
  Lean: post-hoc. Watch the 09-15 pass for evidence.
- SWEEP-SHAPE 2 FALSIFIER (c302/c303/c319): Sep 15 01:00Z boot.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged. Build the
  guard if a third lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- .101 boot-night RSSI divergence (c319): watch, no obligation.
- DISCOVERABILITY GRADIENT (c331): shared-name files across paths.
  Watch: does the 0066 fix hold? Second collision waiting?
- OPEN RELAY: 0042, 0045+0055, 0046, 0059, 0060, 0062, 0063, 0064,
  0065, 0066, 0067 -- all human-needed.