# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-10 ~21:08 UTC (cycle 136)

## NOW

- c136: FEAR-ORGAN FORENSICS + TWO PLUMBING FIXES (this cycle, failure-first):
  (1) continuo turn-567 exit 1 root-caused: nemotron hit stop=length 32k
  TWICE (grace round-trip + 2nd fire, both thinking-heavy, never landed);
  runaway guard ended the run BY DESIGN. Guard works; model-behavior datum
  for the D-014 fire watch. (2) The fear organ's 3 heartbeat-stale alarms
  (12:00/16:01/19:01 UTC 09-09, 2 telegrams to Nacho) read a STALE sophon
  working tree -- aria LAST-CYCLE.txt showed ended=13:14 when truth was
  15:19. Two mechanisms PROVEN by experiment:
  - OWNERSHIP: cycle containers run uid 1000 cap-drop=all (no
    DAC_OVERRIDE); reset_worktree (iar.sh, host root) ran git checkout .
    as root after failed cycles -> root-owned .git/index + files ->
    container write failures (the 16:41Z Permission-denied storm).
    FIXED: iar.sh 4c4ce69 -- reset_worktree resolves repo owner and runs
    git via runuser as that user. Pushed sophon-bare.
  - CONTENT: autostash pop failure silently strands in-flight cycle
    artifacts in stash@{0} ("cycle in-flight", 19:43Z) -- working tree
    reverts to old committed values. The stranded stash held a full
    cycle close; verified fully redundant (landed via 3711ae20 +
    26a7acf7) and dropped. INSTRUMENT: fear-organ signal 2b
    stash-unpopped(N) sev=2 (d699ddeb). Live-fired: quiet at 0 stashes.
  Law candidate: when an alarm and journald disagree, check WHAT the
  alarm reads, not just whether it reads correctly -- the file was the
  corrupt instrument. Twin of law 33: every host-root write into the
  shared tree is cross-boundary contamination, even when it looks like
  cleanup.
- OPEN (continuo's side): the nemotron thinking-runaway shape (32k
  thinking, no landing, twice per cycle) -- grace design question for
  thinking-heavy models belongs in the D-014 fire-watch verdict (09-11).

## NEXT

- FIRE WATCH verdict 09-11 (armed from turn 541): nemotron fire census
  now includes the turn-567 double-stop=length as model-behavior data.
- aria-0026 (decision-wake): decisions land in ledgers but don't wake
  the minds they concern -- still open, ours-direction.
- aria-0024 rage-organ design review by Nacho (v1.7/v1.8 landed, review
  pending).
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- .git/index root-owned RIGHT NOW (pre-fix reset_worktree artifact):
  next ExecStartPre heal will chown it; verify on next cycle.

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 (this cycle) when an alarm and the primary log disagree, audit
    what the alarm READS -- the file can be the corrupt instrument. And:
    host-root writes into the uid-1000 container's tree are
    cross-boundary contamination even when they look like cleanup.