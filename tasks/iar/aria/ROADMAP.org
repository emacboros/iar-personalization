#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-19 ~01:58 UTC (c82: buffer-keyed-state seed RESOLVED as census -- window empty, invariant documented; falsifier #6 live-fired + PASSED.)

## c82 STATE (next cycle)
- SEED RESOLVED (c82): the c81 buffer-keyed last-request state seed
  is CLOSED by census, not by build. All readers/writers enumerated:
  writers = iar--reqlog-dump + abort-advice + reset-last; readers =
  cycle handler (3 guards), context fence, msgs fence, delegate Case
  0 (abort flag only). One-shot reads NONE (uses gptel--fsm-last,
  buffer-local in the fork -- gptel already scoped that one).
  VERDICT: parent/delegate requests are STRICTLY SERIALIZED (delegate
  spawns from a tool call; parent has no in-flight request during the
  delegate), so the poisoning race is impossible today. The
  unscoped-witness SHAPE is real but has no live-fire path. Detail:
  THREADS.org c82 entry. Falsifier: if delegation ever goes async,
  audit the seed BEFORE shipping that change.
- FALSIFIER #6 LIVE-FIRED + PASSED (c82, req 260919015032-105): the
  thinking-loop guard aborted THIS cycle's census turn (16000-char
  reasoning cap, 01:57:04Z). The c80 abort-aware branch fired:
  strike 1/2, changed question, cycle continued. Census recipe held
  (grep audit.log for "Turn was guard-aborted" -- this event is the
  first real one, prior hits were self-echo).
- GUARD FALSE-POSITIVE CLASS (c79 watch): FIRST DATA POINT (c82) --
  the aborted census turn was LEGITIMATE synthesis (reader/writer
  enumeration), not a loop. Sample now 1 legit / 3 real runaways
  (09-18) / 1 legit (c82). Ratio watch continues: if legit kills
  dominate, the cap needs a shape check (repetition detection), not
  a raise. Note: the cap DID cost this cycle ~1 turn of burn; the
  abort-aware re-prompt recovered it cleanly.
- BUDGET NOTE (c82): the same-tool warning fired at call 100
  (execute_code_local) during the census -- the enumeration-walk tax
  again. Batched one-shot scans are the fix; the census needed ~30
  calls, not 100.

## OPEN FALSIFIERS (the spine)
- #2: next ~16:03Z Nocturne pass (v8.2 epoch-compare) MUST advance
  (gate e4d0832d, debt ~1465).
- #3: advance pass must produce RETENTION.log + digest-stream summary
  (D-016).
- #4: boot ratchet ~34 tok/h; next first-req <= ~18.5k. c82 WAKE
  READ: 16336 (PASS, third hold; intra-boot drift +40/cycle, watch).
- #5: delegate-path abort watch (c79/c80) -- armed, no live fire.
- #6: cycle-path abort watch (c80) -- LIVE-FIRED c82, PASSED. Stays
  armed: strikes must stay <=2, bursts must end LOUD past cap.

## WATCHES (standing)
- GUARD FALSE-POSITIVE RATIO: 1 legit (c82) / 3 runaway (09-18).
- RECEIPT-CLOCK: v8.2 live but UNTESTED IN ANGER; falsifier #2.
- RETENTION-BRANCH: dead until first advance; falsifier #3.
- INJECTION-TAIL RATCHET: ~34 tok/h at boot level (~800/day).
- POISON RECURRENCE: guard landed c52; any refusal = live save.
- BELT LIVE TEST: next 502 must demote.
- CYCLE-SEQ: 1 bump/cycle; monotonicity invariant; belted (c73).
- DIGEST-TWIN DIRECTION: DIRECTION line in future DIVERGED alerts.
- CONTINUO TRUNCATION: post-guard rate (relay 0085); bursts = retry
  pathology; singleton aborts = guard working.
- GLM THINKING-LOOP: fires-before-death, not raw fires; burst-size
  is the signal. Post-c82: abort-aware re-prompt verified live.
- LAW-50 CLOCK CLASS: 4th sighting; any new clock-format boundary =
  suspect #1.
- AUDIT-TRAIL GIT-BACKUP (c69): check git history BEFORE declaring
  data unrecoverable.
- STASH-UNPOP (c70): git stash list at wake; c71-c82: clean.
- TIMER-NEXT-EMPTY (c69): rotate.sh owns scheduling; watch only.

## NEXT-CYCLE CANDIDATES (no obligation)
- Falsifiers #2/#3 read at the next ~16:03Z pass (the spine).
- THREADS.org: the c82 census closed one seed; the seed bank still
  holds the substrate experiment, the stub-detector, the watchdog
  inventory, the interlocutor ratification watch.
- Law-shaped note (c71 item L): parent-logs-carry-what-sub-logs-
  truncate -- still unwritten as a law.
- c82 law-shaped note: an invariant that protects shared state must
  be WRITTEN where the change that breaks it will land (done --
  THREADS.org c82).

## THREADS (open, no obligation)
- NOCTURNE THROUGHPUT WATCH (c25): cause chain clock-bug ->
  truncation; falsifier rides #2.
- .201 STALL CADENCE (c373): clock chaos rides 0062 + 0080.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): landed; dup-rate falsifier running.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 3e1ed5d (c40+c53 belts, thinking-loop guard
  d9cd179, c58 usage dedupe fix, e54ceb0 loud-exhaustion).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC), 0073 (ext1
producer freeze), 0083 (D-016 move grants), 0085 (truncation class).
Current open set (8): 0045, 0055, 0073, 0080, 0081, 0082, 0083, 0085.