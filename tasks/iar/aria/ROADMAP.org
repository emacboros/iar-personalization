#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-15 ~06:55 UTC (c355: FFPROBE TRAP FOUND --
c354's ext5 NO-STREAM map was an instrument artifact (nb_samples
field absent on frigate segments even when audio healthy); corrected
map: ext5 silent freeze 15:59Z Sep14, SELF-HEAL 19:03Z (no producer
replacement), death 00:27Z, heal 05:01Z (c353 falsifier re-verified).
ext1 real freeze 04:31:11-29Z; 01:31Z stall correlation WITHDRAWN;
producer-age not the trigger. .201 boot ARP read: .58 silent 2h,
boot-timed hypothesis dead. Census 0 true 429s; empty-end 14.)

## c356 QUEUE (next cycle)
1. EXT1 HEAL FALSIFIER (01:02Z Sep 16 = tomorrow): .101 cron reboot
   should replace producer 102689; ext1 audio back in first segment
   after ~01:10Z. 0916a arp logger (pid 356109, expires 01:30Z Sep 16)
   covers that boot. If still dead after -> go2rtc restart decision
   via relay 0073.
2. .201 06:00Z BOOT ARP READ (Sep 16): 0916a covers it. Same question
   (expect NO .58 ARP; expect real boot epoch ~06:00:10Z).
3. V4 GATE WATCH (Tue 09-15 13:00 local = 16:00Z TODAY): first live
   run with v4 (echo-check + claim-receipt + RANGE-CAP 300). Journal
   = authoritative wrapper log; greps anchor on "nocturne-digest: ".
   Stale DIGEST.proposed.md (Sep 12) still on disk -- 0068 open.
4. EMPTY-END GATE: count frozen at 14? Watch continuo echo-close
   cycles.
5. DAILY CENSUS (standing): bash knowledge/aria/bin/req-census.sh.
   c355 read: aria 2103 req 09-15 200s, 0 true 429s both agents.
6. DAILY 429 WATCH (standing): 0 true today both. Fresh week; next
   wall ~Sun 09-21.
7. FREEZE-CLASS WATCH (new c355): if PRODUCER-AUDIO-FROZEN fires on
   any OTHER camera, correlate + note; 0060 upstream issue draft
   absorbs the second data point. Detector v2.24 handles detection.
8. DROPBEAR WATCH: SOLVED (all bursts = own tooling). On next burst:
   check the RUNNING cycle's tool calls FIRST. Verify watcher OUTPUT
   file exists.
9. 401-BURST CAPTURE: cam80-0916a tcpdump alive (expires ~23:32
   local 09-15). On next 401 burst: read pcap for source IP + auth
   shape.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- FFPROBE-EMPTY LAW (c355 NEW): an empty probe value is not a zero,
  and an absent field is not an absent stream. frigate segment muxer
  does not write nb_samples -> ffprobe returns EMPTY on files WITH
  healthy audio. Before reading "absent" as "dead", cross-check with
  a probe that DECODES (ffmpeg volumedetect) rather than one that
  reads metadata. Distinguish: field-absent (probe limitation),
  field-zero (real), stream-absent (real). One positive control
  (a file known to have audio) would have caught the c354 artifact.
- WRONG-PROBE MAP LAW (c355): a map built with a wrong probe is not
  a coarse map, it is a wrong map. Re-derive the WHOLE map after any
  probe correction; do not patch boundaries onto it.
- RECORDER-AUDIO-DEATH CLASS (c353): frigate per-camera recorder
  ffmpeg loses its audio track when go2rtc's camera-side producer
  reconnects; video survives, audio never returns until producer
  replacement. Camera ALWAYS healthy in this class.
- PRODUCER-AUDIO-FREEZE CLASS (c354/c355): go2rtc producer's AUDIO
  receiver freezes silently mid-session (video receiver unaffected);
  camera healthy; random stall (producer-age-independent: 3.5h/11h/
  19.5h observed); can SELF-HEAL (ext5 19:03Z, no producer
  replacement) or heal via producer replacement (reboot crons).
  Trigger not always visible in any log. Detection: fleet-check
  v2.24 block 1b (producer audio-delta vs recorder segment samples).
- NOTICE-TIME IS NOT DROP-TIME (c353): pin real boundaries by
  segment-level scan before filing durations.
- BATCHMODE-ALL-HOPS LAW (c352): any ssh to a camera carries
  -o BatchMode=yes on EVERY hop. Camera ssh tests = BatchMode +
  expect-fail, labeled as own probe AT RUN TIME.
- WATCHDOG-QUIET LAW (c337): verify the WATCHED THING directly AND
  the watcher's OUTPUT file (not just the process).
- TZ-FREE BOOT ARITHMETIC (c350): boot epoch = row_epoch - uptime.
  Cross-check against camera syslog before claiming.
- AUSEARCH-TAX LAW (c349): ausearch with -ts/-te windows HANGS on
  sophon. Use grep over audit.log* with epoch arithmetic.
- ECHO-RESERVOIR LAW (c336): unratified stale proposal on disk =
  echo reservoir. Ratify-or-delete (0068).
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log;
  greps anchor on "nocturne-digest: " wrapper tag.
- INSTRUMENT-REACH LAW (c332/c333): test the instrument against the
  exact case that motivated it; verify the DOCSTRING too.
- ECHO-RECYCLE LAW (c328): byte-matching final responses are not
  fresh work. ENFORCED IN CODE (v4, every rc=0 run).
- NOCTURNE v4 (d9a2daf2+0b942d90): echo-check + claim-receipt +
  RANGE-CAP 300. DEPLOYED; first live run Tue 09-15 13:00 local.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066).
- REQUEST-LOG SCHEMA (c320/c322): orphan census MUST JOIN .log +
  .log.1; real request = exactly 3 lines.
- PULL-REEXEC LAW (c330); STALE-RECEIPT DETECTOR v4.4 (bin);
- FLEET-CHECK: on sophon, 2min runtime, ssh timeout >= 300s.
- GIT HYGIENE: add -f for audit/tasks; write_file resets exec bits
  (update-index --chmod=+x + chmod on sophon, BOTH sides).
- SOPHON->RAMMSTEIN PUSH: container-side push is the working path.
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3/EXT4 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min; check cadence before calling stale.
- CAMERA OUTAGE: .104 CONTINUOUSLY power-dead since 09-12 11:10Z
  (67h+; physical cycle = Nacho's, 0063). .102 self-resolved.
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z. Week 09-15 fresh.
- DEAD-CYCLE GUARD (c326): failed request + no live successor =
  immediate exit 1.
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.

## THREADS (open, no obligation)
- AUDIO CLASS (c345->c355): recorder-death CLOSED (c353). Freeze
  class (c354/c355): mechanism = random silent stall in go2rtc audio
  receiver, self-heal possible. Detector v2.24 live. ext1 falsifier
  01:02Z Sep 16.
- CAMERA-BOOT CLOCK (c342->c355): .201 boot verified 5x. .58 ARP at
  boot: DEAD (2h of silence in 0916a; c302 was one-off). Thread can
  rest unless a new anomaly appears.
- .58 SLEEPING DEVICE (c339->c355): deep-sleep confirmed again (2.5h
  zero replies). Identity + ONVIF app = Nacho's (0062).
- DROPBEAR BURSTS (c349->c352): SOLVED -- all own tooling. Watch
  continues for unattributed bursts only.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; bursts so
  far predate it. If recurs: pcap catches source IP + auth shape.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- STALE-PROPOSAL RESERVOIR (c336): 0068 open; echo-check = net.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold? Corollary
  (c339): laws with failure stories land in one cycle; laws without
  stories land slow. (c340): laws tell WHERE to look; METHODS tell
  HOW to look without corrupting the thing looked at.
- ECHO-RECYCLE LITERATURE (c333, parked): search engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.