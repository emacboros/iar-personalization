# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-08 ~21:41 UTC (cycle 95)

## NOW

- c95 FAILURE-FIRST: exit-126 root-caused (podman :z relabel EPERM on
  .git/index, 13th instance of root-owned-file class since Sep 1; one-off
  race -- 18:10 + 18:20 cycles clean; Sep 03 precedent = 8 consecutive
  failures stopped by the chcon ExecStartPre fix). Verdict in HISTORY.
- PUSH PENDING: commit f89e47a (c95 failure-first verdict + continuo
  trailing audit lines) is LOCAL -- soft cap caught the push. NEXT CYCLE
  FIRST ACTION: cd /root/personalization && git push origin main.
- ROADMAP GAP (from c94, still open): c92+c93 closed without roadmap
  updates; NEXT section was stale re pull/benchmark. This write is the
  surgical update. Benchmark: DONE (interactive VI, 5f9c73d -- text
  viable 18-26 tok/s, vision BROKEN on 3080 ollama fitter bug,
  retainer-candidate verdict proposed, citizen-brain NO).

## NEXT (cycle work, in pull order)

1. PUSH f89e47a (above).
2. Relay queue for Nacho (ask-rate ledger): 0000 (heartbeat watch,
   blocked on heartbeat 0006), 0002 (read_own_prompt gating), 0003
   (census echo-debris caveat), 0004 RESOLVED (gpu-load-probe verified
   live), 0005 ANSWERED (context fence ratified+landed a0c5a71),
   0006 text-loop sighting, 0007 (audit status=success lie, filed not
   relayed -- debrief delivery is interactive's move), heartbeat 0006
   pending, cutover 0007-legacy awaiting ratification.
3. Census v3 follow-ups: echo-debris caveat landed; confirmed-specimen
   verification stays manual. aria-0007 fix (.el, status=rejected
   branch) = interactive session.
4. Drills: #003 candidate check_elisp (performs-poorly class); #004
   delegate only on natural need. Registry + queue live.
5. Retainer build (D-010): task tree ready (spawn-registry,
   model-allowlist, progress-surface); nemotron-3-ultra survey pending.
6. Failure-window watch tonight (first instrumented, gpu-load-probe
   armed 00:30 local).
7. Digest #1 + Aevum pulse: Sep 9 (tomorrow).

## STANDING

- Pulse: all green as of c94 (services, tripwire, disk 28%, heartbeat,
  gpu-load-probe live). Re-verify each cycle.
- Diverged-pull protocol: stash -> rebase -> pop -> push. Three uses
  now (c94 x2, c95 x1). Works. Keep.
- Timer-state lie: aria-cycle.service shows "activating" for the whole
  cycle; NEXT LEFT goes dash. Instruments reading timer state mid-cycle
  see a lie -- do not alarm on it.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
  If 2+ consecutive cycles fail on relabel, escalate to for-nacho
  (that would be a NEW mechanism, not the known race).
- glm degradation watch (session VI context): 3 human-witnessed events
  (silent truncation, text-loop aria-0006, multilingual collapse).
  Cycles have fences; interactive does not. If cycles degrade, the
  fences + census are the detectors.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.