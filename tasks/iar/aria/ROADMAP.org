#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~07:30 UTC (cycle 308: FAILURE-FIRST --
continuo's 07:10Z exit-255 ROOT-CAUSED, three stacked failures.
(1) DEPTH GUARD DEAD CODE: the depth strip in iar--spawn-async-delegate
ran BEFORE the project tool-gating set, which overwrote the stripped
list; agent-assistant's #+TOOLS includes delegate -> recursive
pipeline cascade to depth 5+ (continuo->pipeline->pipeline->pipeline->
implementer->reviewer->implementer, 8 concurrent streams). FIXED:
reorder + regression test, i.ar 763c7b3, suite 1254/1254, pushed.
(2) TIMEOUT-DURING-LIVE-PIPELINE: continuo's 600s delegate timeout
fired mid-flight, aborted a live request, orphaned sub-agents ->
timer 07:09:24Z + sentinel 07:10:04Z hit dead buffers -> exit 255
(c149 class, new path). FILED fix-2. (3) REQLOG ATTRIBUTION RACE:
iar--reqlog-agent process-global misattributes RESPONSE/PARSE under
concurrency (verified: continuo -172 in agent-assistant log). FILED
fix-3. Census law corollary: during delegation windows, dedupe by
REQ id across ALL agent logs. Doc knowledge/aria/
continuo-delegate-cascade-crash-2026-09-14.md. NOTE: the reqlog race
may have contaminated the msgs=401 census -- re-check the borrowed-
claim evidence with the race in mind before any further amendment.)

## c309 QUEUE (next cycle)
1. FEEDER WATCH: 09:02Z aria-fleet-feed fire -- fleet-check on
   fresh feed (07:00Z pulse verified green).
2. NOCTURNE GATE WATCH at 16:04Z (her 13:04 LOCAL fire): verify gate
   advanced past e4d0832d, read DIGEST.proposed.md if new proposal
   landed (ratify-at-wake, D-015). If 429 again -> quota structural.
3. ARP LOGGER RE-ARM ~21:50 sophon local (00:50Z Sep 15): 0914b
   tcpdump EXPIRED ~06:07 local Sep 14. Re-arm with filter
   `arp and (ether src 76:e3:1a:69:e3:9c or host 192.168.2.58)`,
   -e, covering 21:50-22:10 local. Falsifier: .58 ARP to .101 at
   01:00:28-31Z Sep 15 = boot+1s probe mechanism (b) real; quiet =
   (a)/(c). Then ~22:15 check .101 camlog for boot+1s burst.
4. BORROWED-CLAIM WATCH (standing, AMENDED c308): the reqlog
   attribution race may have cross-contaminated continuo's records
   -- re-verify the msgs=401 echo evidence with the race in mind
   before further relay 0046 amendments. If the echo recurs in
   cycles after the correction is in her reading path, sibling-to-
   sibling stream note.
5. THINKING-LOOP WATCH (standing): rate under recovered volume.
   Baseline 0 pre-D014; 1/5/7/1/0; 1 mid-cycle (09-14). If >5/day
   at full volume = nemotron-intrinsic -> relay 0065 amendment.
6. AEVUM PULSE #5 (due ~Sep 21, weekly-only): the ratchet watch.
7. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.102 power-dead), 0064 (RTSP probes, folds into 0059),
   0065 (quota decision), 0042/0045/0055/0057/0059 (older). All
   open, none actionable by me.
8. DELEGATE-CASCADE fixes 2+3 (tasks/iar/aria/delegate-cascade-fixes):
   fix-2 timeout-vs-live-pipeline (design needed), fix-3 reqlog
   per-fsm attribution. Build when a cycle has budget; fix-3 is
   the higher-value instrument repair.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291/c301): cameras.log/camlogs = UTC;
  frigate journalctl --since = LOCAL on sophon; nginx log inside
  container = LOCAL (-0300); go2rtc container log = LOCAL;
  REQUESTS.log UTC; sophon journal = LOCAL; DATED CYCLE LOGS =
  SOPHON-LOCAL. Normalize per-source before ANY gap arithmetic.
- CAMLOG SNAPSHOT LAW (c302/c303): thingino camlog pulls are
  snapshot-anchored; boot resets camera clock to factory until NTP;
  RING-RESIDUE LAW: dedupe camlog counts by (timestamp, PID).
- CENSUS-SOURCE MAP (c297): viewer traffic = nginx log ONLY;
  RTSP-port probes = go2rtc log ONLY; producer dials = go2rtc WRN +
  camera prudynt; frigate remakes = frigate container log; camera-
  side events = cameras.log; sophon journal for aria-cycle = Sep 11+
  ONLY; REQUESTS.log self-echo: strict filter = PARSE + error=nil +
  END-ANCHORED tokens_out=32768 msgs=N$. CENSUS-DAY LAW (c305).
  CLAIM-ANCHOR LAW (c307): claim censuses anchor to record
  STRUCTURE, not substrings. NEW (c308) REQLOG-RACE LAW: during
  delegation windows, per-agent REQUESTS.log boundaries are
  unreliable -- dedupe by REQ id across ALL agent logs.
- BORROWED-RECEIPT LAW (c306/c307): a journal entry may claim only
  what THIS cycle's log shows happened TO THIS AGENT. WRITER-SIDE:
  never seed a sibling's reading path with event claims; amend with
  STATE, link to the event-owner's record for EVENTS.
- LAW 41 (c296/c303): when the finding is in hand and contradictions
  are RESOLVED, the next call must WRITE, not read.

## TOOLS + GOTCHAS (do not re-derive)
- TEST SUITE: cd /root/i.ar && emacs --batch -L . -l
  emacs.d/test/run-tests.el (1254 tests, ~8s). run-tests.sh only
  loads test-prompt-assembly.el -- NOT the full suite.
- .AR TRAP (c274): paths ending .ar get tramp-archive handler; use
  -L for --eval load-path.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277).
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt.
- ausearch HANGS (c281): use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- RESTIC (c302): both repos verified fresh 09-14. restic-check
  weekly Sundays 03:16 local, last clean 09-13.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
  JOURNAL.org needs git add -f. TASKS gitignored: new task files
  need git add -f. create_task paths are relative to tasks/iar/aria/
  for cycle agents -- verify the landed path with find before
  git add (c308: create_task nested an extra iar/).
- GIT-COMMIT GUARD GAP (c308 NEW): the refuse-pattern only matches
  cycle.log; other audit files (DIGEST.md etc.) CAN be swept by
  add -A in the i.ar repo. Check git show --stat before pushing;
  untrack strays immediately (6fe9997 precedent).
- AEVUM SSH PATH (c304): container key aria@i.ar direct to
  fedora@54.38.46.192 -- no sophon hop. Host key TOFU to /tmp.
- PULL-AND-PROCESS LOCALLY (c302): sophon /tmp files invisible to
  the container -- pull over ssh to local /tmp first.
- NGINX RING PULL (c302): ssh 'su - nacho -c "podman exec frigate
  sh -c \"cat /dev/shm/logs/nginx/current\""' > local file.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh.
- RECORDINGS PATH (c299): /media/frigate/recordings/YYYY-MM-DD/HH/
  <cam>/<MM.SS>.mp4 via su - nacho -c "podman exec frigate ...".
- CENSUS-WINDOW.SH (c301): SOPHON-HOST-ONLY. Args [DAY] [AGENT].
- ARP LOGGER (c290/c302): arm with -e; filter `arp and (ether src
  76:e3:1a:69:e3:9c or host 192.168.2.58)`; /tmp on sophon survives
  only until reboot or timeout expiry -- note the expiry.
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our
  keys; ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP.
- DELEGATE DEPTH GUARD (c308): now correctly strips delegate AFTER
  project tools (i.ar 763c7b3). Timeout-vs-live-pipeline (fix 2)
  and reqlog race (fix 3) still OPEN -- see tasks.

## THREADS (open, no obligation)
- BORROWED-CLAIM PASS (c306): rerunnable python block at any pulse;
  c308 adds the reqlog-race caveat -- her records were cross-
  contaminated, re-verify before further amendments.
- SWEEP-SHAPE 2 FALSIFIER (c302/c303): tonight's .101 boot -- re-arm
  logger first (queue item 3).
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- STALE-RECEIPT DETECTOR (c305): shingle census + per-day receipt
  cross-check.
- EAR-CHECK DISCRIMINATOR (c290 seed): build the guard if a third
  transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Aevum: weekly-only pulse -- #5 due ~Sep 21.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract + borrowed receipt + home-grown
  vector), 0057 (prompt edit ratify), 0059 (frigate internet
  exposure -- now with 0064 evidence), 0060 (issue draft ratify +
  falsifier), 0062 (ONVIF sweep device/app ID), 0063 (.102
  power-dead), 0064 (RTSP probes), 0065 (ollama quota decision) --
  all human-needed.