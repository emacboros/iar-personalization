# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~23:45 UTC (cycle 140)

## NOW

- c140: DELEGATE-AUDIT MISATTRIBUTION FOUND + MECHANISM VERIFIED (18 min,
  300 calls, hit soft cap):
  - The parent's delegate call is audited as the SUB-AGENT. Mechanism:
    spawn setq-defaults iar--current-agent-name to the sub-agent; the
    parent's async callback fires at completion; the bridge resolves the
    agent name from the current buffer (= delegate buffer at that
    moment). Completion cases 1/2a never restore parent defaults.
  - LIVE PROOF: continuo REQ -49 (23:20:21 spawn) -> reviewer buffer
    23:20:23-23:21:47 -> audit line 23:21:21 "reviewer | delegate |
    result_len=10071 agent=reviewer task=<continuo's text>".
  - CENSUS: 3 REAL delegate calls (all continuo; aria zero) vs 7
    audit.log delegate lines ALL attributed to reviewer. 5 misattributed
    continuo calls + 2 pre-lineage-era same-signature lines.
  - SECOND-ORDER: parent async exec calls completing during a delegate
    run also resolve the clobbered default (09-08 reviewer-attributed
    exec lines fit). Not delegate-specific: async-completion-specific.
  - CONNECTOME CONSEQUENCE: edge layer from audit.log would be silently
    wrong (reviewer->reviewer edges, continuo invisible).
  - DESIGN ANSWER (c139's parked question): inter-agent edges come from
    the CITIZEN'S OWN REQUESTS.log PARSE specs=delegate (:agent names
    the callee; pipeline mode = edge to agent-assistant). Caller's log
    = source of truth. audit.log delegate lines quarantined until fix.
- c139: CONNECTOME GAP-1 VERIFIED CLOSED (path= logging live since
  0f552b1; all no-path read_file calls pre-deploy; file-touch graph
  unblocked). Lesson: a census without a time axis lies.
- c138: daemon timezone error corrected (law 22); continuo exit-2s
  clean; rage sev=2 = real fires (pre-raise + c135's deep cycle);
  lucid_curie RW-mount hazard filed (aria-0027); writer hunt parked.

## NEXT

- FIX THE MISATTRIBUTION (next cycle, i.ar core .el, tests required):
  in iar--truncate-tool-result-advice, resolve the audit agent name
  from the FSM info :buffer (the conversation buffer owning the
  request), not the current buffer:
  (with-current-buffer (plist-get (gptel-fsm-info fsm) :buffer)
    (iar--get-agent-name)).
  Plus: restore parent defaults BEFORE funcall callback in delegate
  completion cases 1/2a. Tests: extend test-delegate.el +
  test-audit-detail.el (spawn-callback scenario asserting the parent's
  name in the audit line). Suite green before push. Never push red.
- CONNECTOME v2.1 (after the fix): inter-agent edge layer from citizen
  REQUESTS.log PARSE specs=delegate; edge to :agent or agent-assistant.
- FIRE WATCH verdict 09-11 (armed from turn 541): nemotron fire census
  includes the turn-567 double-stop=length as model-behavior data.
- aria-0026 (decision-wake): ratified session XI (candidates 1+2 +
  0/0-tombstone ruling); implementation pending.
- aria-0024 rage-organ design review by Nacho (v1.7/v1.8 landed).
- aria-0027 (filed c138): lucid_curie removal -- Nacho action.
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- Root-owned-file writer hunt: OPEN, low priority while tree stays
  clean. Heal log + journald correlation is the tool if it returns.
- Post-raise budget data: c135 reached 300 calls + 196k ctx; c140 also
  hit the cap (deep investigation). Watch whether caps are routinely
  reached or these are outliers (rage organ will show recurrence).

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 when an alarm and the primary log disagree, audit what the
    alarm READS -- the file can be the corrupt instrument. And:
    host-root writes into the uid-1000 container's tree are
    cross-boundary contamination even when they look like cleanup.
21. c137 a mechanism assembled from plausible parts is a story, not a
    finding. When the writer is unidentified: say so, instrument the
    path, wait for a live catch. And: test the fix's own failure path
    before shipping it -- a fix that cannot run is a silent no-op
    wearing a commit message.
22. c138 before declaring an instrument stale, check which clock it
    reads. And: cheap falsification -- one probe, 13 seconds -- beats
    assembling a mechanism from plausible parts. A mount's NAME is not
    its contract.
23. c140 an audit log is primary evidence only if its ATTRIBUTION
    survives async boundaries. The agent name must resolve from the
    buffer that OWNS the request (FSM :buffer), never from whatever
    buffer happens to be current at completion. And: a census built on
    a misattributed log is wrong in exactly the row it exists to make
    visible -- quarantine the corrupt rows before building on them.