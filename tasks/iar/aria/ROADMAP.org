#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 19:48 UTC (cycle 38)

* ACTIVE

** c38: BURN BURST AUTOPSIED -- the meter ate the house (05:11-06:31 UTC)
- Dashboard asymmetry: aria 291M input today vs continuo 137M. By
  REQUESTS.log the picture inverts (aria 47.4M / continuo 70.1M).
  Reconciliation: USAGE's 06:31 line (597 reqs / 68M) is a cumulative
  flush over a FOUR-SESSION burst -- aria cycles 05:11, 05:30, 05:50,
  06:10 UTC, each fresh (msgs=2), each 19-28M input.
- Mechanism: req -30 (05:15) ran a token census on its own
  REQUESTS.log; the model then re-grepped the same "REQ-31-START"
  pattern 35-49 times, each result ~4KB, context growing ~2k/turn,
  never accepting the answer. Self-pollution census failure mode,
  purest form: reading the meter became the workload.
- The arc is the vindication AND the indictment: 05:11 session raw
  grep (19.3M) -> 05:30 session built failure-triage.sh MID-BURST
  (28.6M) -> 05:50+06:10 sessions collapsed to 4.5M. The instrument
  works; the burst still cost ~58-68M -- costliest two hours on
  record.
- CRITICAL GAP (next cycle's first read): 192-223 requests in one
  session against iar-cycle-max-turns default 40 -- turn guard did
  not fire. Chain guard also silent through 208 execute_code_local
  calls. Two fences, one burst, zero catches. Read configs/cycle.el
  + the turn tracker BEFORE anything else.
- c38 itself hit the 60-warn and 120-cap reading the meter -- third
  live sighting of "the law you just wrote is the one you're about
  to break." Meter-reading cycles have unbounded instrument tax;
  burn-triage script is the seed (after the turn-cap gap).

** c37: c36's debts paid; rage census verification CLOSED
- Census verification complete: rage sev=2 magnitude = fence LINES
  (30) not events (~17). Inflation 30/17 ~ 1.76x, consistent with
  c29's line-vs-event factor.
- sev=3 premise VERIFIED: all 30 counted fires live in
  cycle-2026-09-07.log (single file-date). Observation premise holds.

** c35: census instrument self-caught under-reporting; guard first-fire verified
- ceiling-census.sh v1.1 (e97ca9a): bare invocation includes all four
  logs. truncated-output guard first production fire VERIFIED
  (continuo c105, 18:23Z, req 29, stop=length, 65536 tokens).

** c34: ceiling-census.sh landed; output-ceiling baseline computed
- 16 ceiling hits today, ALL stop=length at exactly 65536. msgs-depth
  hypothesis KILLED. Lever: configs/gptel.el :num_predict 65536.

** c33: c67 warn-asymmetry RESOLVED (dissolved by live data)
- Law: hemisphere asymmetry is evidence about CODE VERSIONS before it
  is evidence about MODELS.

* NEXT
- TURN-CAP GAP (c38, priority): read configs/cycle.el + turn tracker
  in iar-agent-cycle.el. Why did 192-223 requests not trip
  max-turns=40? Why did the chain guard not trip at 11? Fix or
  re-spec, test, land.
- rage sev=3 observation (Sep 8): v1.1 file-based grading predicts
  sev=3 tomorrow IF soft-cap fires again in cycle-2026-09-08.log.
  Observation day; v1.2 (event semantics) after it resolves.
- Aevum runaway (task 338465) lands ~Sep 8 00:15 UTC -- transcript
  behavior after landing is free data (weekly rule: no probe until
  Sep 14 unless sev-2 urgency).
- num_predict lever (flag 519): c38 adds evidence -- the burst paid
  full 65536 repeatedly. Still parked on the sev=3 observation. One
  variable at a time. Nacho's call.
- burn-triage script: failure-triage.sh is failure-first-shaped; a
  burn-shaped sibling (start from USAGE spike, walk REQUESTS) would
  make meter-reading cycles cheap. After the turn-cap gap.
- Weekly digest #2: Sep 14-ish, fill-in format.

* WATCHES
- rage organ sev trajectory: sev=3 prediction Sep 8 (premise verified c37).
- Aevum: task 338465 runaway lands ~Sep 8 00:15 UTC; next weekly
  pulse Sep 14.
- Oracle BrokenPipeError 04:56Z (harmless; maybe add access log).
- rammstein origin unreachable from aria container (publickey);
  sophon-bare carries all pushes; mirror lag is Nacho's key flow.

* QUEUED
- tool-cap calibration bundle: lenient-in-landing candidate (9+ bites).
- rammstein mirror push-pending: infra repo caddy_sites 02f6393
  (Nacho's key flow); gptel-fork upstream merge (his next-topic pick).
- gpu-load-probe install (continuo c97/c98, unit files ready) --
  Nacho's call.
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check, digest-twin verifier.
- for-nacho open flags (Nacho's answer pending): 461 (empty sophon
  bares), 466 (archetype budget-line -- continuo verified the
  numbers, c104; the prompt edit is interactive work), 519
  (num_predict lever, guard blocker resolved).

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- A meter-reading cycle without a script is a burn event: start burn
  investigations from a script, never from raw grep (c38).
- An instrument that lies by omission is worse than no instrument:
  verify an instrument against a hand-run BEFORE trusting its
  defaults (c35).
- Hemisphere asymmetry is evidence about CODE VERSIONS before it is
  evidence about MODELS. Check what each hemisphere ran first (c33).
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  The join key includes the CLOCK (rotate logs -03 local, USAGE UTC).
  Dedupe raw greps before counting (c14/c17/c22/c23).
- REQUESTS.log is self-polluting: anchor censuses on line STRUCTURE,
  never content patterns. ceiling-census.sh IS the anchor (c14+c18).
- A census that doesn't read its own hemisphere's latest findings
  re-bites scars the house already paid for (c23).
- Organ WRITE hygiene: check the file's tail state first (c19).
- Heals: run as nacho, never root; never reset --hard in a shared
  tree without checking git status (c11+c13).
- Belt#2's write is only as durable as the next commit (c13+c14).
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break (c38:
  meter-reading cycle hit the cap).
- Organs reading files NAMED by another writer: enumerate the
  writer's actual files, take newest N BY NAME (c26).
- When a finding changes a decision, STOP and write it (c22).
- When a thread's premise is already answered in the record, verify
  THAT first (c28).
- A generation has ended only when n_gen is FROZEN across two reads
  AND the transcript mtime flips (c122, Aevum).
- A receipt in the record is not a receipt in the world: the
  filesystem is the truth; the transcript is the claim (c129, Aevum).
- A fence keyed on silence cannot distinguish "dead" from "thinking
  at the wall" (c31, Aevum).
- Past the soft cap the count runs past the cap by design (c33).
- One variable at a time when instruments are mid-observation (c34).