#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 18:05 UTC (cycle 32: i3 boundary hunt DONE -- death pinned
08:39:59->08:50:00 UTC, decoder differential NEGATIVE (dead segment decodes clean
on BOTH decoders; cycle 31's leading hypothesis dead). New picture: break is
go2rtc->frigate segment INPUT, mid-session, zero logs on either end. Scar 46
candidate: silent track loss visible only in output artifacts. e2 restart flag
326 stands -- one restart now tests both failures.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 32: CLEAN. Own LAST-CYCLE ok (cycle 1, 1237s).
  Poison in /var/home/nacho/repos: 0.
- Durable fix unchanged (interactive, Nacho): root-git-as-nacho in
  iar.sh + bare-repo hook options + HEAD one-liner. All spec'd in
  knowledge/iar/bare-repo-root-push-heal.md.

* DO FIRST (cycle 33)
1. FAILURE-FIRST (always).
2. Organs at wake (correct invocations, see STANDING PATROLS).
3. e2 RESTART WATCH: if Nacho answered flag 326 and restarted
   frigate, diff the fleet before/after (does e2 heal? do healthy
   cams stay healthy? does i3 heal too?). If no answer, leave
   standing -- the flag is his call.
4. i3 NEXT STEP: the 08:50 break has no logs. Two cheap probes:
   (a) watch whether i3's one-packet segments EVER self-heal (they
   have not in 9h -- if they do, note the hour and look for a
   trigger); (b) if Nacho restarts frigate (flag 326), i3 is part
   of the experiment. No further enumeration walks -- the boundary
   is known, the mechanism needs the restart event or a self-heal
   to observe.
5. Fleet-check v2.13 watch: FAIL=0 baseline. ext2/int3 state
   changes are REAL news (pipeline failures, not camera ones).
6. SCAR 45 rider stands: recovery verdicts during flaps are
   provisional.
7. Flag 326 follow-up if unanswered by cycle 36: gentle bump in
   for-nacho (not before -- he answers when he lands).

* DONE (cycle 32)
- Failure-first: own LAST-CYCLE ok. Pulse green (services, tripwire
  0, disk 24%, heartbeat leid 3875, timer). Organs quiet.
- Carry-overs from 31: lab-notes 325 posted; e2 restart flag 326
  posted (with experiment framing: does e2 heal, do healthy cams
  stay healthy); msg 262 thread closure 327 posted.
- I3 BOUNDARY HUNT (batched, one ssh per phase): death is
  08:39:59 -> 08:50:00 UTC Sep 3. 08h = 250 packets/hour until
  00.39.mp4 (182 packets); 00.50.mp4 onward = exactly 1 packet
  (header-only, no frames). 9h ongoing, no self-heal.
- DECODER DIFFERENTIAL NEGATIVE: dead segment (09/00.05.mp4)
  decodes clean through host ffmpeg (Lavf62) AND frigate's
  in-container ffmpeg 7.0. The differential hypothesis is DEAD.
  The aac "backward in time" errors belong to exterior_3's detect
  pipeline (pre-heal), NOT interior_3.
- LIVE 3-POINT DIFFERENTIAL: camera .203 sends audio (156KB/5s
  direct); go2rtc carries it (313KB/10s in-container); frigate's
  record ffmpeg (started 05:00:29) writes 1-packet segments since
  08:50. Break = go2rtc output -> frigate segment input.
  Producer SDP still advertises audio (same as healthy i1).
- E2 CONFIRMED unchanged: 0-packet segments at 17h; camera sends
  81920 samples/5s; go2rtc log has only 2 timeout events ever for
  .102 (never errors -> never reconnects -> never heals, holds).
- GO2RTC LOG FORENSICS: i3 (.203) has 4 timeout events total
  (05:00:05+05:00:37 both days). NO go2rtc log lines at all in the
  08:35-08:55 death window. The 05:00 crash-restart (detect ffmpeg
  crashed "Connection timed out", watchdog restart 05:00:29) is
  the prime suspect: new process fine 3h49m, then silent
  mid-session audio loss.
- SCAR 46 CANDIDATE: a pipeline can lose a track mid-session with
  zero log evidence on either end -- the only witness is the
  output artifacts. The ear check (output-watching) caught it;
  frigate's and go2rtc's logs never mentioned it.
- Journal cycle 32 + HISTORY appended. Cap warning fired at 60
  (batched well; record written via memory tools as designed).

* OPEN THREADS (seeds, not schedule)
- E2 + I3 RESTART EXPERIMENT (flag 326, Nacho's call): one frigate
  restart likely heals e2 (producer reset) AND i3 (segment-input
  reset). Watch: e2 heal? healthy cams stay healthy? i3 heal?
- SCAR 46 (write when verified): silent track loss -> output
  artifacts are the only witness. Pending the restart experiment
  confirming the mechanism.
- SCAR 45 + rider (provisional verdicts during flaps).
- SCAR 44 (census-pattern validation): law written, continuo
  adopted.
- HOST SCHEDULING (Nacho, flag 290): fear hourly + daily boredom.
- ORGAN ATTRIBUTION (THREADS): organs grade events, not causes.
- FIRMWARE FLAG 270 RE-SCOPE: camera-side class now EMPTY (both
  remaining deaf cams are pipeline-side). Withdrawable after the
  restart experiment confirms mechanisms.
- LONELINESS-ORGAN SEED (THREADS): absence-signals. Not a build yet.
- INTERACTIVE BUNDLE (Nacho): (1) socket bridge security decision;
  (2) chcon -R durable fix; (3) soft-warning cap; (4) digest
  pressure REPLACE-not-append; (5) root-git-as-nacho (ALL verbs);
  (6) repo-root ROADMAP.org fossil; (7) bare-repo heal options +
  HEAD one-liner.
- agora-valence-v2 (curiosity + rage) + v3 (joy): after v1 proves.
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (key). Weekly digest #1 ~Sep 9 with Aevum check. Restic
  integrity check Sun Sep 6.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag
  for-nacho when something needs him.
- Builds ungated: safe work proceeds without waiting.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.13: RUN ON SOPHON (ssh 'bash -s' < IS correct for
  fleet-check; timeout >=300s). FAIL=0 is the new baseline. If
  FAIL=1: read the ear-check lines FIRST (recovery/deafness is now
  the likeliest mover), bare-repo second.
- ORGAN RUNS at wake (interim scheduling):
  - fear: ssh root@10.66.0.5 'bash /var/home/nacho/repos/iar-personalization/knowledge/aria/bin/fear-organ.sh "" /var/home/nacho/repos/iar-personalization'
  - boredom: bash /root/personalization/knowledge/aria/bin/boredom-organ.sh
- Agora poll (with-nacho + lab-notes + for-nacho) every cycle.
- Marker convention on ALL journal commits: [novelty] / [maintenance]
  prefix.
- BATCHED-SSH LAW (cycle 31): enumeration walks get ONE ssh with a
  loop inside, never one ssh per item.
- PROBE-PATH LAW (cycle 32, new): host-side ffmpeg probes against
  go2rtc (127.0.0.1:8554) are the reliable path; podman-exec
  volumedetect can hang the session (e2 probe, ~9 min wall). For
  in-container decoder tests use -f null with explicit timeout and
  capture to file, never volumedetect through exec.

* LAWS (learned, newest first)
- SILENT-TRACK-LOSS LAW (cycle 32, scar 46 candidate): a pipeline
  can lose a track mid-session with zero log evidence on either
  end. The only witness is the output artifacts. Output-watching
  is not one instrument among many -- for silent track loss it is
  the ONLY instrument.
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a go2rtc session
  state, not a camera state. Video flows on while audio negotiation
  dies. A reconnect heals it; a reconnect only happens on error.
  A silent producer is a deaf instrument forever.
- PROVISIONAL-VERDICT LAW (cycle 31, scar 45 rider): a recovery
  verdict issued during an active flap is provisional. Check the
  flap ENDED before trusting it.
- AUDIO-ROLE LAW (cycle 31): frigate's "audio" role gates audio
  DETECTION, not recording. Recordings carry audio without it.
- RECOVERY-VERDICT LAW (cycle 30): a recovery is a destructive
  decision (it turns watches off). Positive AND negative controls
  before the verdict means anything.
- CENSUS LAW (cycle 29, written for the house cycle 30): a count
  gating a destructive decision needs pattern validation against a
  known-positive. knowledge/aria/census-verification-law.md.
- POINTER LAW (cycle 27): a wrong pointer misdirects writers, not
  just readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits to
  an undecided framing is not a fix. Decide the framing first.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final write
  misses the write. Heal placement must match write order.
- DIFFERENTIAL LAW (cycle 8, deepened 31/32): same input through
  both pipelines is how you find which layer lies. Direct camera vs
  go2rtc vs recorded segment -- three points, not two. A negative
  differential is itself a finding: it kills a hypothesis and
  moves the boundary.
[... older scars 24-43 retained in full in git history; core list:
  43 twin-copy drift; 42 wrong pointer; 41 framing patch; 40 heal
  placement; 39 blind clock; 38 DIGEST twins; 37 write_roadmap vs
  repo-root; 36 heal is not fix; 35 organ fail-closed; 34 read the
  repr; 33 chown is not relabel; 32 escape hatch; 31 component vs
  system; 30 two data points; 29 unhandled output shape; 28 version
  disagreement; 27 auth-walled or wrong-version; 26 promote to
  script; 25 output-watching instrument; 24 diet pressure; 23 fresh
  law vigilance; 22 stable writer or dead; 21 user@1000 + linger;
  20 watchdog tuning; 19 podman -d in systemd; 18 fail2ban bursts;
  17 isolation stall; 16 root-git poison; 15 priors vs human; 14
  reliability tiers; 13 second stack; 12 artifact confabulation;
  11 capture context; 10 third cause; 9 who the speaker is;
  8 differential testing; 7 venv symlink; 6 existence is not
  function; 5 pkill -f self-match; 4 malformed calls invisible;
  3 timeout wrappers; 2 silent swallowing; 1 narrative completion.]