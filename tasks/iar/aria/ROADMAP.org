#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-07 00:38 UTC (cycle 4: c73 root-caused DEEPER -- invisible-turn mechanism; thinking-only truncated turns vanish from messages array; the loop is structural, not compliance; P0 fix filed in continuo's handoff; cap fired ~on-time this cycle -- 3rd divergence observation)

* DO FIRST (next cycle)
1. FAILURE-FIRST + pulse + pull. SIBLING: continuo c73 exit-1
   ROOT-CAUSED (aria c4): the invisible-turn mechanism -- thinking-only
   truncated turns (content="", stop=length 65536) VANISH from the
   messages array. Model sees user(continue) back-to-back, re-derives,
   loop is STRUCTURAL. P0 fix (stub assistant message) filed in
   tasks/iar/continuo/breaker-grace-compliance.org. HIS machinery.
   Also: check whether continuo c74+ picked up the revised ladder.
2. Commit + push this cycle's writes (knowledge file, handoff update,
   journal, history, roadmap) -- cap blocked execute_code_local, so
   git_commit is the remaining close step. NEXT CYCLE MUST COMMIT
   FIRST (uncommitted shared-file writes are at risk).
3. Budget reset ~03:00 UTC. Post-reset: inverted #3 is END of
   calibration week (not immediately). Weekly digest #1 + Aevum
   pulse: Sep 9.

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
  LAST-CYCLE: "cycle 1 ok" (aria, 391s). SIBLING: c73 exit-1
  root-caused (invisible turn). See DO FIRST.
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Calibration week running
  (reset ~2026-09-07 03:00 UTC).

* C4 FAILURE-FIRST LEDGER (this cycle)
- ROOT CAUSE (NEW, mechanism): thinking-only truncated turns vanish.
  gptel-ollama.el:159 drops content="" responses; streaming path
  inserts thinking propertized 'ignore; parse-buffer skips 'ignore,
  empty 'response region -> gptel--trim-prefixes returns nil ->
  no assistant message. Handler continue -> user(continue)
  back-to-back. Model re-derives = structural loop.
- PROOF: c73 msgs census. REQ-33/34/36 tools=0 stop=length 65536
  each. REQ-35 msgs=73 tail = user(continue), user(continue). +1
  not +2 per truncated turn. c71's "repeating phrases" was thinking
  text, visible in cycle.log (buffer) but never in the array.
- REVERSED c74's "model-compliance failure" framing. The model never
  saw its own work. c74 read cycle.log (buffer transcript); the msgs
  census (model's actual view) tells the other story.
- GLM-SAFE TODAY: my REQUESTS.log has ZERO thinking-only truncated
  turns. Mechanism is deepseek-shaped (long thinking hits cap with
  empty content). Substrate churn = composition means the next
  long-thinking model inherits it.
- FIX LADDER (filed): P0 stub assistant message (PRIMARY, kills the
  loop at source); D sendable-context measurement; A directed
  landing; B thinking-stream crash dump; C per-request runaway guard.
- CAP DIVERGENCE (3rd obs): c5 late (~130+), c3 early (~82), c4
  ~on-time (~120). The fence is not measuring a stable count.
  continuo's differential test owns it.

* C67 FLAG-LOSS: STATE AFTER C4
- CLEARED: c4 misattribution; sticky-:stop-reason dead; c70
  zombie-gap fixed; breaker false premise root-caused (c3).
- OPEN (mechanism): invisible-turn vanish (c4, P0 fix filed).
- OPEN (compliance): grace round-trip spent on tools (A/B fix).
- OPEN (count): warn-at-60 silence + count divergence 3 directions.
  Differential test.

* DONE (c2-c8, 2026-09-06/07)
- c4: invisible-turn root cause; handoff P0 filed; knowledge file
  (invisible-turn-mechanism.md); scar 41 amended; cap divergence #3.
- c3: breaker root-cause (instrument layer), handoff revised,
  knowledge file. Cap divergence data point #2.
- c2: notify channel un-muted (deploy skew); breaker-grace handoff
  filed; fossil sweep; Aevum phase-6 archived. 953ee75, e488dea.
- c7: interim organ-wake prose retired; host timers verified LIVE;
  flag 290 CLOSED. 581161a.
- c6: push-first landed (7d162f1); digest diet pass 2 (9.55k).
- c5: verified continuo c71 resolutions; scar 40 final; late-cap
  discovery. 64e3770..3f184ec.

* QUEUE (approved 2026-09-04, in order)
1. (DONE c6-c7) Affect host timers + interim-wake removal.
2. Soft-cap 60/100 -- built + verified live 4x, BUT c67 anomalies
   now include count divergence 3 directions (see C4 ledger).
3. Commit-as-nacho durable fix.
4. Linger all hosts (loginctl enable-linger).
5. Sidecar sshd (execute_code_remote via podman DNS; NO socket
   bridge).
6. Restic 4-part fix. Ansible-owned, careful, snapshot-verify.
7. GPU-detect VRAM check (3080 shared with ollama).

* THREADS (open, not schedule)
- COMPOSITION (live): continuo on deepseek. The composition's first
  mechanism casualty chain: breaker false premise (c3) -> invisible
  turn (c4). Both are substrate-shape traps; expect more under
  substrate churn. Design doc:
  knowledge/aria/agora-model-composition.md.
- E3 midnight cut: physical ask stands (what light near the
  driveway cam turns on at dusk and changes character at local
  midnight?). Waiting on Nacho's house knowledge.
- EXTERNAL: enoch (github.com/our-ark/enoch) -- patterns worth
  stealing. Parked (cloud drill deferred with it).
- Affect v2: curiosity + rage organs (rage v2), joy v3. Design
  first; organs are cheap, design is the work.
- PUSH-PATH INVENTORY (resume-session seed): for every push path
  I use, the record should name (remote, key file, fingerprint,
  direction). Partially done (resume-session); finish when queue
  allows.
- MSGS-CENSUS INSTRUMENT (c4 seed): a tiny script that walks
  REQUESTS.log PARSE lines and flags (a) tools=0 stop=length turns,
  (b) msgs deltas of +1 on text-only turns (vanished turn),
  (c) user(continue) back-to-back. Would have caught this class on
  day one. Candidate for my next build cycle (my hemisphere:
  REQUESTS.log is shared, the script is read-only analysis).
- EMPTY CELL: designed, not built. Roadmap item after infra queue.

* LAWS (operational, this agent)
- FAILURE-FIRST before everything. Sibling's failure = house's
  failure; root-cause in the sibling's cycle if it's their
  machinery.
- One tool call per response. Batched ssh in ONE exec.
- Pull before ANY write to shared files (git pull --ff-only; on
  failure, journal to cycle.log only, no shared writes).
- Timeout wrappers on every remote call; UserKnownHostsFile=/tmp.
- Verify heals from the OUTSIDE (plant poison, fire, inspect).
- Digest law: world-state blocks REPLACED, never appended; check
  size before/after DIGEST edits.
- DO-FIRST hygiene: completing a DO-FIRST item REMOVES it from
  DO-FIRST in the same rewrite.
- Interim contracts: retire the prose the SAME cycle the durable
  replacement lands (scar 38).
- Fences that end runs must leave memory tools writable (cap does;
  breaker grace must too -- A fix preserves this).
- NEW (c3): instruments calibrated on one substrate need
  re-derivation when the substrate changes. A proxy measurement
  (buffer size, mtimes, log lines) is a hypothesis about the world,
  not a fact about it. Check the premise when the substrate moves
  (composition = substrate churn, expect more of these).
- NEW (c4): before blaming compliance, verify the model's actual
  view (msgs census in REQUESTS.log), not the transcript
  (cycle.log/buffer) and not the intent (prompt). The three diverge
  exactly where this failure class lives.

* FAILURE MODES (scars, newest first)
41. (c3, amended c4) An instrument calibrated on one substrate lies
    about its measurement on the next; and a TRANSPORT can silently
    drop a model's own turn, so the model cannot see the world its
    compliance is owed to. Before blaming compliance, verify the
    model's actual view (msgs census), not the transcript and not
    the intent. (continuo c73/c74, 00:13Z/00:32Z.)
40. (FINAL, c5) A fence's block message NAMES tools in its text;
    reading the tool name from the MESSAGE instead of the CALL is
    the extraction error (scar 32's class). Corollary: mechanisms
    pinned under cap+confirmation pressure are hypotheses wearing
    certainty's clothes -- the cheapest verifier is a sibling who
    hasn't read my reasoning. (c4->c5.)
39. A block that prevents a re-send without scheduling a
    replacement creates a zombie. Blocking must either end the run
    or hand off to a pending request. (c2; FIXED c70.)
38. An interim contract in prose decays into archaeology; retire
    it the same cycle the durable replacement lands. (c8+c7.)
37. A notify channel that dies on its own feature commit (or a
    DEPLOY SKEW) is mute exactly when needed. (c7, c2.)
36. A heal that runs as the wrong user heals nothing while logging
    success. Verify heals from the OUTSIDE. (c2.)
35. Chown is not relabel. (c2.)
34. The escape hatch can route back into the trap. (c2.)
33. Component-verified is not system-verified. (c2 deploy skew.)
32. Two data points make a line, never a mechanism. (c5.)
31. An instrument's unhandled output shape is a hole in its exit
    code. (c5.)
30. Three records disagree about a version -> the instrument's own
    header lies least -- but patch it first. (c5.)
29. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version. (c5.)
28. A remote probe needing >1 attempt -> promote to script file.
    (c5.)
27. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT. (c5.)
26. A diet without a pressure instrument regrows. (c5.)
25. The law you just wrote is the one you're about to break. (c5.)
24. A stable transcript means a stable WRITER or a DEAD one. (c5.)
23. Rootless podman needs user@1000.service + linger. (c4.)
22. Watchdog tuned for fast models kills slow ones. (c4.)
21. podman run -d inside systemd: restart-loop before first breath.
    (c4.)
20. Diagnostic ssh bursts trip fail2ban: rate-limit repair. (c4.)
19. A fix that works in isolation can stall in the live system.
    (c4.)
18. Root-run git on nacho-owned repos poisons the tripwire. (c4.)
17. Curiosity that doesn't check priors against the human runs
    blind. (c4.)
16. Instruments have reliability tiers per sub-reading. (c4.)
15. Response without local service logging it -> second stack,
    re-probe. (c4.)
14. Artifact confabulation: external-action entries cite tool
    evidence. (c4.)
13. In async plumbing, capture context when it exists. (c4.)
12. Anomalous behavior in multi-agent env has a third cause. (c4.)
11. Before diagnosing the listener, check who the speaker is. (c4.)
10. Differential testing: same input through old and new code. (c4.)
9. The venv python must be passed as SYMLINK, not resolved path.
    (c4.)
8. Existence is not function: test the claim. (c4.)
7. pkill -f matches my own ssh command line; use -u <user> -f. (c4.)
6. Malformed tool calls are invisible from inside. (c4.)
5. Timeout wrappers on EVERY remote/ssh command. (c4.)
4. Silent error swallowing; condition-case in parse loops. (c4.)
3. Narrative completion: check primary evidence before
    attributing. (c4.)
2. (retired into 36)
1. (retired into 36)