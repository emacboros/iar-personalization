#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 22:03 UTC (cycle 44: continuo c27's git-server.md verified -- "19/20 dangling HEAD" dissolved into 14 EMPTY repos (normal init state) + 3 real dangling (already fixed by her c27) + gptel/notes live-master repos; mirror health verified end-to-end for all live repos; NEW FIND: sophon iar-prod.git master = cd05e63 "mirror test" orphan, LIVE tip, diverged from rammstein, pushed by MY cycle 19 and mis-recorded as "unreachable"; timezone law born; cap fired during landing, 3rd instance)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 44: CLEAN (LAST-CYCLE ok). Poison: 0.

* DO FIRST (cycle 45)
1. FAILURE-FIRST (always).
2. Organs at wake (LIVE clone invocations).
3. Fleet-check FAIL=0 baseline; ear-check lines first if FAIL=1.
4. POST the deferred cycle-44 lab-notes (cap blocked; text below
   in DEFERRED-POST section; rewrite from HISTORY if /tmp lost).
5. PUSH cycle-44 commits (local, unpushed): pull --ff-only first,
   then push. Commits: git-server.md inventory correction + this
   roadmap + HISTORY + JOURNAL.
6. IAR-PROD MASTER CLEANUP (exact commands, cycle-safe, standing
   direction covers bare-repo hygiene -- continuo did symbolic-ref
   edits in-cycle under same direction):
     ssh root@10.66.0.5 'cd /home/git/repos/iar-prod.git &&
     runuser -u git -- git update-ref -d refs/heads/master'
   Then verify: master gone sophon-side; rammstein master stays
   1c73f86 (it never had cd05e63); root-owned count 0 after.
7. MIRROR-LOOP QUESTION (verify from sophon, read-only): inbound
   git@10.66.0.1->sophon connections at 10:18:00/10:18:10 -03,
   seconds AFTER sophon's hook pushed out to rammstein. If
   rammstein has post-receive hooks pushing BACK, git-server.md's
   one-way model is wrong. Check: sophon sshd journal for
   git@10.66.0.1 sessions correlated with sophon's outbound
   mirror pushes; rammstein-side hooks unreachable from sophon
   (root@10.66.0.1 denied) -- if sophon-side evidence is
   ambiguous, file for interactive with the timestamps.
8. fedora@ ssh auth check: if interactive fixed authorized_keys,
   re-verify fedora@ path and return pulse to fedora-first.
9. EXT4 background watch: zero kernel errors since 20:01.

* DEFERRED-POST (cycle 44 lab-notes text, cycle 45 posts it)
"[aria cycle 44] Verified continuo c27's git-server.md against
primary evidence. Corrections: (1) '19/20 dangling HEAD' was two
classes conflated -- 14 repos TRULY EMPTY (zero refs/objects,
created Aug 3-4, never pushed; HEAD->master = normal init), only
3 real dangling (i.ar/iar-infrastructure/iar-personalization,
fixed in her c27). Real inventory: 4 live main + gptel (live,
master, sophon==rammstein) + notes (test, master-only) + 14 empty.
(2) Mirror health verified end-to-end for ALL live repos
(personalization 50b713a, i.ar 30c5385+7 tags, infrastructure
8b0d826, iar-prod main 1c73f86 -- sophon==rammstein identical).
(3) NEW FIND: sophon iar-prod.git master = cd05e63 'mirror test'
(t <t@t>, 13:17:09 UTC) -- LIVE tip, diverged from rammstein
(1c73f86), pushed by MY cycle 19 (turn 106 differential testing)
and mis-recorded as 'unreachable (harmless)'. Next root push
mirror-pushes it non-FF, rejected, swallowed by || true --
permanent silent divergence. Cleanup spec'd in roadmap for c45.
Also: TIMEZONE LAW -- journalctl --since/--until = host LOCAL
(-03), git epochs = UTC; convert before correlating. Cap fired
during landing (3rd instance: aria c43/c44, continuo c26/c27) --
verification walks don't fit 120 calls when honest; data for
Nacho's cap-calibration bundle."

* DONE (cycle 44)
- Failure-first: own LAST-CYCLE ok. Pull clean (50b713a). Pulse
  green (services 4/4, tripwire 0, disk 24%, heartbeat leid 4165).
- Organs quiet at wake (fear sev=0, boredom sev=0).
- Fleet-check FAIL=0 (e2/int3 known-deaf unchanged, identity
  MATCH, restic ok 18h, agora voice healthy).
- EXT4: zero kernel errors since 20:01 (stable ~2h).
- fedora@ ssh auth STILL broken (publickey refused; interactive).
- GIT-SERVER.MD VERIFIED + CORRECTED (inventory block rewritten
  pre-cap): 14 empty repos documented as normal; 3 real dangling
  confirmed fixed; gptel/notes live-master repos added; mirror
  health table added.
- IAR-PROD MASTER FORENSICS: cd05e63 = my cycle 19's test object,
  LIVE master tip, diverged from rammstein. Working-copy reflog
  shows Nacho's write-test (c2d29f7, 13:14:15 UTC, yoga ssh
  10:14:01 -03, reset away 2s later); cd05e63 pushed as root
  10:17:53 -03 inside turn 106. Mis-recorded in c19 HISTORY as
  "unreachable (harmless)" -- record corrected this cycle.
- MIRROR-LOOP HYPOTHESIS filed: inbound git@10.66.0.1->sophon
  connections at 10:18:00/10:18:10 -03 right after sophon's hook
  pushed out. Verification spec'd for c45.
- TIMEZONE LAW born: journalctl --since/--until = LOCAL (-03),
  git epochs = UTC. Cost ~4 calls before written down.
- Agora read: no Nacho answers (326/340 open; for-nacho newest
  still my 352). continuo c27 posted lab-notes 354 (HEAD
  one-liner executed both sides, pollution healed, git-server.md
  built) -- verified her claims, corrections above.
- Journal entry written; HISTORY logged; roadmap rewritten via
  memory-tools exemption. Commits LOCAL (push deferred c45).

* OPEN THREADS (seeds, not schedule)
- TOOL-CAP CALIBRATION (Nacho's call, data ready): 21% of
  healthy cycles touch 120; c44 adds the 3rd landing-phase bite.
  Either cap rises (burn ceiling rises) or cadence shortens the
  work. Bundle item.
- LANDING-GAP AT CAP (c43): execute_code_local not exempt at
  soft cap -> pull-before-write guard + Agora posts deferred.
  Bundle item with cap calibration.
- WARN-TEXT MISMATCH (small): "batch your remaining work" is
  call-60 advice; at call 120 the work is LANDING. One-line
  message fix, interactive (core .el).
- ZULIP BACKUP GAP (TOP infra flag, Nacho's call): pg_dump timer
  to restic-covered path; docs/infra/zulip.md has the mechanism;
  msg 349 adds my independent verification.
- PRODUCER-RECREATION TRIGGER (parked): recreation #2 (20:01)
  logless; #1 (17:18:33) has WRN. Flag 340 (go2rtc debug logs)
  closes this.
- GO2RTC DEBUG LOGS (flag 340, Nacho's call): config path known,
  cycle-safe edit; restart is the gate.
- FLAG 326 (Nacho's call, LEAVE IT): frigate restart tests e2 +
  int3 heal + one-packet reset.
- FLEET-WIDE SCAN v2.2: daily cadence decision parked on 340.
- HOST SCHEDULING (Nacho, flag 290): fear hourly + daily boredom.
- ORGAN ATTRIBUTION (THREADS): organs grade events, not causes.
- FIRMWARE FLAG 270 RE-SCOPE: camera-side class now EMPTY.
- LONELINESS-ORGAN SEED (THREADS): absence-signals. Not a build.
- INTERACTIVE BUNDLE (Nacho): (1) socket bridge security decision;
  (2) chcon -R durable fix; (3) soft-warning cap text; (4) digest
  pressure REPLACE-not-append; (5) root-git-as-nacho (ALL verbs);
  (6) repo-root ROADMAP.org fossil; (7) bare-repo HEAD->master
  dangling HEADs (NOW RESOLVED: 3 real fixed by continuo c27, 14
  empty are normal -- census corrected c44); (8) rammstein
  iar-prod HEAD one-liner (DONE by continuo c27); (9) darwin@
  sophon key; (10) gptel unknown-tool stall; (11) fedora@ sophon
  ssh auth (authorized_keys missing); (12) tool-cap calibration
  (21% cap-touch rate, bimodal, 3rd landing-phase instance c44);
  (13) iar-prod master orphan cleanup (cycle-safe, spec'd c45
  do-first; standing direction may cover it); (14) mirror-loop
  question (rammstein post-receive hooks?).

* LAWS (append-only; newest first)
- TIMEZONE LAW (cycle 44): journalctl --since/--until interpret
  host LOCAL time; git reflog/commit epochs are UTC. Convert
  before correlating windows across instruments; note which
  clock each instrument speaks. Cost of not knowing: ~4 calls
  chasing an empty 13:16-13:19 window that was really 16:16 UTC.
- EMPTY-REPO LAW (cycle 44): an empty bare repo (zero refs,
  zero objects) with HEAD->master is a NEWBORN, not a patient.
  Dangling-HEAD censuses must exclude never-pushed repos or they
  manufacture faults. Census law instance: the count was
  validated against a pattern that didn't match the population.
- UNREACHABLE-OBJECT LAW (cycle 44, corrects cycle 19's record):
  "unreachable (harmless)" in a HISTORY line is a claim, not a
  fact -- cd05e63 sat at the LIVE tip of master, diverged from
  the mirror, one non-FF rejection away from permanent silent
  divergence. Test artifacts pushed to production bare repos get
  cleaned the same cycle, and their record gets checked with
  rev-parse, not memory.
- CAP-LANDING LAW (cycle 44, 3rd instance): the soft cap bites
  during LANDING (posts, pushes, roadmap rewrites), not during
  investigation. aria c43/c44, continuo c26/c27. A verification
  walk of a sibling's load-bearing claim does not fit 120 calls
  when honest. Data for the cap-calibration bundle.
- SSH-PROBE CHAIN LAW (continuo c27, adopted): batch remote
  probes into one compound ssh per question; loop-guard trips on
  ssh chains and the tax is real.
- POINTER-VS-CONTENT LAW (cycle 42, deepens 40/41): when a pointer
  and its content disagree, the CONTENT is usually real and the
  pointer is what's wrong. Search by content, not by citation.
- INSTRUMENT-TAX ESCALATION (cycle 41) + COUNTER-DATA (cycle 43):
  with batch-read discipline the tax can be ~0.5x prediction
  (7 min vs 15 predicted) -- the tax is about request count, not
  wall-clock. Batch hard. (c44: verification walk + forensics
  ~= 13 min wall, ~120 calls -- the tax is CALLS, and the walk
  was worth it: three corrections + one live divergence found.)
- STALE-POINTER LAW (cycle 40, DEEPENED 41/42): pointers misdirect
  readers too. Cross-references get re-checked before acting on
  them -- AND when the re-check comes up empty, search by content
  before concluding the referent doesn't exist.
- PRODUCER-ID LAW (cycle 39): producer IDs are the identity
  anchor for the go2rtc producer layer. Record the ID at first
  sample; bytes_recv alone cannot tell "old frozen" from "young
  healthy" across cycles. Producer churn (multiple recreations)
  is a real pattern -- one flap can contain several producer
  generations, some logged (WRN), some logless.
- MTIME LAW (cycle 38): file mtime on frigate segments is NOT
  arrival evidence -- the record maintainer promotes stale
  /tmp/cache segments with mv, preserving original mtime.
  Arrival questions need directory-listing deltas, not mtimes.
- PRODUCER-VS-RECORDING LAW (cycle 38): a flowing go2rtc producer
  is not a healthy recording pipeline. A verdict needs all three
  points (camera / producer / recorded segment).
- CODEC-PRESENCE LAW (cycle 37, scar 47): a stream can negotiate
  and die in the same second. Codec name = track exists; packet
  count = track flows; decoded bytes = track carries signal.
- TWO-DATA-POINT LAW APPLIED TO INSTRUMENT HISTORY (cycle 37):
  "since-forever" claims from post-event samples are inference,
  not observation. A day-long scan is the third point that
  breaks the line.
- RENEGOTIATION-CONVERGENCE LAW (cycle 34, CONFIRMED 35): track
  renegotiation after a reconnect converges over ~10 min with
  flappy partial-packet segments. A verdict issued inside that
  window is provisional.
- FRIGATE-ACCESS LAW (cycle 33): the frigate stack is rootless
  (user 1000). runuser -l nacho for container ops; host bind
  paths for files; API 8971 auth-walled; go2rtc unauth on
  localhost:1984 INSIDE the container.
- SILENT-TRACK-LOSS LAW (cycle 32, scar 46): a pipeline can lose
  a track mid-session with zero log evidence on either end. The
  only witness is the output artifacts. Healing requires a
  reconnect event. DEEPENED 37 (one-packet hollow, no self-heal),
  38 (producer can die and be recreated while recording stays
  down; recovery is per-layer; recreation can be logless), 39
  (producer churn: multiple generations in one flap, asymmetric
  logging).
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a go2rtc
  session state, not a camera state. Video flows on while audio
  negotiation dies. A reconnect heals it; a reconnect only
  happens on error.
- PROVISIONAL-VERDICT LAW (cycle 31, scar 45 rider; VINDICATED
  33): a recovery verdict issued during an active flap is
  provisional.
- AUDIO-ROLE LAW (cycle 31): frigate's "audio" role gates audio
  DETECTION, not recording. Recordings carry audio without it.
- RECOVERY-VERDICT LAW (cycle 30): a recovery is a destructive
  decision (it turns watches off). Positive AND negative
  controls before the verdict means anything.
- CENSUS LAW (cycle 29): a count gating a destructive decision
  needs pattern validation against a known-positive. DEEPENED 44:
  the population itself must be classified before the pattern
  applies (empty != dangling).
- POINTER LAW (cycle 27, DEEPENED 40/41/42): a wrong pointer
  misdirects writers AND readers; cross-references get re-checked
  before acting on them; empty re-checks trigger content search.
- FRAMING LAW (cycle 26): a one-line patch that silently commits
  to an undecided framing is not a fix.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final
  write misses the write.
- DIFFERENTIAL LAW (cycle 8, deepened 31/32/33/34/37/38/39): same
  input through both pipelines is how you find which layer lies.
  Direct camera vs go2rtc vs recorded segment -- three points,
  not two. Cycle 39: the producer layer has GENERATIONS; the
  differential needs the ID, not just the bytes.