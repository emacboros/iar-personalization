# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-09 ~09:00 UTC (cycle 121)

## NOW

- c121: FEAR-FLEET WIRING LANDED (aria-0015 filed, D-011
  aria-reversible): aria-fleet-feed timer (6h) runs fleet-check
  FROM GIT, atomic write to /var/lib/aria-fleet/fleet-latest;
  fear-organ v1.2 reads it as $1 + NEW staleness branch (fleet
  file >26h old = sev=1). Live-fired: FAIL=0 -> sev=0 flat;
  synthetic FAIL=1 -> sev=2 up; restore -> sev=0 down. Timer
  enabled, first fire 06:00 -03 09-09. As-built:
  knowledge/aria/fear-fleet-wiring.md. Commits e7d5692, 0c23b0f,
  1f17073.
- c121: ARIA EXIT-126 ROOT-CAUSED (c120 open thread CLOSED):
  podman lsetxattr SELinux label race on
  iar-personalization/.git/index at 18:00:15Z 09-08 -- the mount
  raced the ExecStartPre heal's chcon settle. ONE occurrence
  since 09-01 (103 other failures all exit=1 model/tool class).
  Self-healing; no fix warranted. The fear organ was RIGHT.
- c120: INSTRUMENT SCHEDULING AUDIT (law-27 family, 3 finds):
  (1) frontend-eye-check.sh ran ONCE (c74, 09-08 12:49Z), never
  scheduled -- REPORT.md is 3 days stale and the relay watch 0000
  already fired on it. Needs a schedule (host timer candidate).
  (2) fleet-check had NO timer -- FIXED THIS CYCLE via the
  feeder (6h cadence). (3) FEAR ORGAN HALF-BLIND -- FIXED THIS
  CYCLE (composite input restored).
- c119: FLEET-CHECK v2.15 LANDED (4b33ddc): interior_3 KNOWN_DEAF
  withdrawn (premise falsified by c118), stale v2.13 comment
  corrected, parse-shape RESOLVED (dual n_samples = pre/post-decode
  volumedetect; tail -1 correct; cam 203 ch1 404; all cams single
  aac). Live run FAIL=0. Law 26 exercised.
- c119: DIGEST-TWIN VERIFIER WIRED into both cycle prompts' Phase 1
  pulse (i.ar 9d82cfa). Law 27 exercised.
- c118: INTERIOR_3 KNOWN-DEAF PREMISE FALSIFIED: real deaf window
  ~24h (09-03/08 -> 09-04/08 -03); recovery verified; camera-side,
  no frigate journal entries at boundary (law 28).
- c117: FIRE-CENSUS CROSS-EXAMINATION: instrument stands (aria 0 /
  continuo 20 strict-EOL). Relay aria-0014 filed (nacho-arch,
  revert lever). Forensics: knowledge/aria/fires-forensics-2026-09-09.md.
- c116: W37 CONNECTOME SNAPSHOT (65630 lines, both hosts; baseline
  only -- trend claims start W38) + dashboard board view data layer
  (bbf9171, v1.9 live on /json).
- c115: FIRE-CENSUS INSTRUMENT LANDED (6ab2821+18ccd11).

## NEXT (cycle work, in pull order)

1. EYE-CHECK SCHEDULE (c120 find, now the only open law-27 item):
   frontend-eye-check.sh needs a standing trigger -- host timer
   (daily?) or pulse-wiring like the twin. Before next UI change,
   not after.
2. Drills: #004 delegate only on natural need. Registry + queue live.
3. gptel PRs: pending Nacho's answer on aria-0013 (nacho-external).
4. Retainer build: spawn-file-spec drafted; runtime parsing =
   interactive-session work (D-005). Interlocutor model =
   nemotron-3-ultra:cloud (D-012).
5. Failure-window watch: gpu-load-probe windows live. Aevum pulse
   #4: Sep 14.
6. Laptop origin artifact review with Nacho (0fc3bbc): pending
   his availability; salvage BEFORE reinstall/scrub.
7. W38 connectome snapshot: next week -- first trend comparison
   against the W37 baseline.
8. Fleet-feed watch: first scheduled fires 06:00/12:00/18:00/00:00
   -03; verify the 12:00 fire produces a fresh fleet-latest and no
   organ noise (the instrument's second data point).

## STANDING

- Pulse: all green as of c121 (services 4/4, tripwire 0, disk 29%,
  heartbeat leid=702, keyscan reseeded, twin FAIL=0). Fleet-feed
  live run FAIL=0 (frigate events 1292/24h, restic ok 5h).
- Relay open queue: aria-0011 (muse NO), aria-0012 (qwen36 PASS),
  aria-0013 (gptel PRs), aria-0014 (fires revert lever),
  aria-0015 (fear-fleet wiring note). All await Nacho ack.
- sophon-bare i.ar.git at 6b67f61. origin (rammstein) fetch fails:
  container key not in rammstein authorized_keys. Mirror hook
  direction is rammstein->sophon. Never compute divergence against
  origin refs.
- 14 of 20 sophon repos have DANGLING HEAD (HEAD->master, only
  main exists). Use explicit refs.
- Aevum: weekly-only, pulse #4 Sep 14.
- BIKE LEDGER OPEN: KLR650 presumptive, license upgrade next week,
  test-drives primary. GO2 ~2 weeks out.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
    c111 corollary: mktime takes EXACTLY six fields.
    c120 corollary: fear.log stamps UTC, journalctl prints local --
    when two of your own instruments disagree, first check whether
    they speak the same timezone.
12. Anchor greps on line START, never on substrings. c114
    corollary: REQUESTS.log census greps anchor the TAIL.
13. Verify a "pending" roadmap item against the primary source
    before working it -- roadmap lines go stale.
14. Apparent size lies on sparse files; read the blocks (c99).
15. An instrument that cannot say who it was when it measured lies
    by omission -- ran-as + input-branch context (c102).
16. An instrument that fails for the wrong reason produces a verdict
    that is wrong with confidence -- validate on a control that CAN
    pass (c104). Probe must be IN RANGE (c109).
17. Enumeration is what an under-stimulated instrument does (c105).
18. Commit-derived activity is a lying instrument for cycle health
    (c105). Wrapper-exit is not cycle-health (c105).
19. Waiting for a state change is not work (c106).
20. A test suite that has not run is a press release (c107).
21. A model that cannot say "I cannot see that" is a perception
    hazard (c109).
22. Workarounds generalize past their warrant (c110). Sticky
    tracked-ness is state nobody owned (c113).
23. A time-delta instrument must be validated against a KNOWN delta
    (c111).
24. An instrument that exists only as per-cycle improvisation
    re-derives its methodology every run (c114). If a census runs
    more than twice by hand, it wants to be a script.
25. A per-agent file has no agent column; the partition is the path
    (c115). Validate against a control that MUST match.
26. A scar whose evidence base has rotated out must be re-verified
    before it is trusted again (c118). A plausible scar is a
    hypothesis, not a fact.
27. An instrument that exists but is never scheduled is a
    fossil-in-waiting (c118). Wiring a verifier into a standing
    routine is part of building it. c120 corollary: the wiring
    decision is PER INSTRUMENT -- wiring the twin did not wire the
    eye-check or fleet-check; audit the whole instrument family
    for schedules, not just the one that just caught something.
28. Recovery/healing events with NO journal entries at the boundary
    are camera-side (c118) -- absence of logs at the boundary is
    evidence about WHERE the event lives.
29. c120: an organ's installed config is its true spec -- read the
    systemd unit, not the script's usage docs. fear-organ.sh
    documents fleet-file as input; the unit passes "" and has since
    install. Existence of an input path is not function (law 9,
    organ edition).
30. c121: an instrument built to feed another instrument must be
    live-fired against BOTH its green and its failure path before
    the first scheduled fire -- the feeder's synthetic-FAIL test
    cost one sed and proved the whole chain; the alternative was
    discovering a dead wire at the first real fire.