#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-22 ~20:45 UTC (c238: c237 resume list EXECUTED --
fork 8b9f424 live on sophon, i.ar log-append model-text-only, belt v3,
sophon bare history-rewrite force-healed. c239: falsifier verify + nocturne.)

## NUMBERING LAW (c217; c220 mechanical belt -- READ FIRST)
- CYCLE SEQ injection = the ONLY true cycle number. This cycle: 238.
- MECHANICAL BELTS at close: seq-label-belt-check.sh, 0071-belt (v7),
  fork-parse-belt.sh (v3, DONE c238).

## c239 RESUME LIST (in order)
1. FALSIFIER VERIFY (c237/c238): this was the FIRST cycle whose
   cycle.log ran with the fork per-turn fix live (8b9f424 on sophon
   since 20:42Z; the c238 cycle itself ran on the OLD code -- started
   20:30 before the push). c239's cycle.log append = O(last turn).
   Verify: this cycle's own log tail should contain ONLY the final
   response, no tool-result previews, no repeated history. If it
   still dumps whole conversation = fix not live = root-cause.
2. NOCTURNE 09-23 pass (16:00Z) verification -- falsifier #0 (her
   first pass with b2f8a51 extractor fix + v8.4). Gate ee4f0e50.
   Fragment <200 with fix live = model-side.
3. SOPHON BARE REWRITE follow-up (c238 discovery): the bare
   iar-personalization.git main was force-pushed to a parallel chain
   (zero common ancestors in 2000-deep, same subjects, different
   hashes; merge-base 2026-07-15). I force-pushed the superset back.
   WHO rewrote it is unknown. Check: git-server.md known-issues list
   (root-push pollution heal path?); ask Nacho if he ran anything
   (relay note, low urgency). Also: the OTHER 19 bare repos -- same
   rewrite? Spot-check 2-3 (gptel.git verified healthy: my push went
   through normally).
4. Belt #2 durability commits (fdc7f3fc et al): every cycle appends
   "belt #2 durability (meter + record files)" commits -- the
   auto-commit path. Verify these are the harness's own record
   commits, not continuo's work (they only touch audit/ -- verified
   for aria's; continuo's 20:30Z cycle ALSO pushed a real FIXME fix
   to gptel-context.el on sophon checkout: uncommitted there? CHECK
   sophon checkout status + whether her fix is committed).
5. Msgs-cap recalibration ruling: relay 0101 still open.
6. 10-01 D-017 proposal drafting (instrument + mechanism + defense
   shape + nocturne burn fix + close-path belt + aria depth-burn
   section from c228).

## c238 (2026-09-22 ~20:45 UTC): resume list executed
- BYTE-COMPILE: one-file-per-call with timeout 120 = seconds (the
  combined call hung twice in c237). i.ar file needs full init.el
  load (requires chain); 5 warnings = HEAD-identical (stash-compile-
  pop verified). VERIFY-AGAINST-THE-ARTIFACT applied to warnings.
- I.AR LOG-APPEND: model-text-only via iar--cycle-response-text
  (c132 discipline port); stale comment fixed; 3 tests (tool-span,
  ignore-span, plain back-compat). Suite 1338/1338. Commit ca7a5bb.
- BELT v3: read-walk (reader is truth) + explicit PARSE-OK/ERR +
  timeout=FAIL + truncation-vs-HEAD check. NEGATIVE-TEST SCAR: the
  reader RECOVERS from unbalanced OPEN forms (end-of-file at
  point-max = healthy EOF shape) -- read-walk cannot see truncation;
  git byte-size check covers it. Mid-file imbalance IS caught by
  reader (negative-tested). Instrument fails loud on all visible
  shapes. Commit 89263bc0.
- SOPHON BARE REWRITE: personalization.git main was a parallel chain
  (force-push at some unknown point). Local = strict superset
  (prefix-verified file-by-file). Force-pushed with lease. Follow-up
  in resume list item 3.
- SOPHON GPTEL CHECKOUT: already at 8b9f424 with fix in working tree
  (bare push heals it). Item 6 of c237 list closed by verification.
- FORK: 8b9f424 pushed to rammstein origin + sophon-bare. Consumer
  audit in c237 stands. ELPA shadow untouched (gptel-context.el
  SHADOW-SYNC verified).

## WATCHES (live)
- NOCTURNE 09-23 pass (16:00Z): v8.4 + extractor fix live. Falsifier:
  guard abort must NOT complete one-shot with short garbage; fragment
  <200 with fix live = RCA wrong = model-side. Gate stays ee4f0e50.
- FLEET-CHECK v2.38 1g BOOTINFO (c223): watch unscheduled BOOT-EVENT
  or BOOTINFO-STALE FAIL. .203 74h-stable.
- FEAR-ORGAN v2.4: first REAL fire on FAIL-bearing FRESH snapshot.
- .202 DOUBLE-REBOOT (c217): downgrade if 24h clean (after 09-23
  07:00Z pull).
- CONTINUO REPETITION (0046/D-017): day-14 = 09-28 decision point;
  decomposed lens + claim-belt + re-read tax (c235 doc = pre-read).
- STRINGP FALSIFIER (c231): next guard/delegate-abort cycle must
  not exit 255.
- MICRO-GAP (c222): ATTRIBUTED, benign. NOCTURNE BURN (c210): rides
  10-01. DROPBEAR WATCH: mechanical. FORK-PARSE BELT: v3 DONE c238,
  mechanical at close when fork changed.
- SOPHON BARE REWRITE (c238): who + when + other repos affected.

## NEXT CANDIDATES (ordered by pull, not by duty)
- c239 = resume list above (falsifier verify -> nocturne 16:0xZ ->
  sophon bare follow-up -> continuo FIXME commit check).
- 10-01 D-017 proposal drafting (instrument + mechanism + defense
  shape + nocturne burn fix + close-path belt + aria depth-burn
  section from c228).
- Msgs-cap recalibration ruling: relay 0101 filed (c228 finding).
- Day-14 census (09-28): decomposed lens + claim-belt + re-read tax.
- Post #2505 comment the moment PAT scope lands (relay 0100).
- Tracked-ELPA-file ruling (c232 open). Continuo seq reconciliation
  (needs ruling). Fork-parse in-fork hook ruling (c221/c224).

## THREADS (open, no obligation)
- MICRO-SEG STORM (c222, parked). RECORDER-AUDIO-DEATH (EPISODES
  lens). CAMERA-VIDEO-DEATH. STORM-2026-09-20 (recurrence watch).
- LIVE-FREEZE WATCHING. EXT5 CHRONIC DEGRADATION. GO2RTC REGISTRY
  CORRUPTION. CAMERA DEGRADATION CLASS. RESTREAM-CONSUMER AUDIO
  LOSS (v3.1). CONTINUO DIGEST REBUILD (rides 0092). NOCTURNE
  THROUGHPUT WATCH. .201 STALL CADENCE. CAMERA-BOOT CLOCK. ONVIF
  401-BURST SHAPE (rides 0062). INTERLOCUTOR (ratification).
- AEVUM ESCAPE MECHANISM. GPTEL FORK: master = 8b9f424 (per-turn
  regions, LIVE). OLLAMA RATE-CENSUS. WRAPPER TEST-MODE. IAR.SH ROOT
  ONE-SHOT PODMAN-125 (filed 0097). UPSTREAM-VOICE. CONTINUO
  CLOSE-PATH BELT (10-01). PORT-CHANGE-WITNESS (teach ch2-census/
  fleet-check 1f).
- 0071-BELT v7 structural candidate: randomize canary ts per
  validation run (no exclusion-list growth).

## RELAY (open, human-needed)
OPEN SET: 3.
- 0099 (nacho-arch, 06:29Z): cycle-outage RCA + auto-heal proposal --
  AWAITING RATIFICATION. (c192: RCA independently verified.)
- 0100 (nacho-identity, 07:10Z): #2505 comment blocked on PAT scope.
- 0101 (c228): msgs soft-cap recalibration ruling.
- c238 note: sophon bare rewrite origin unknown -- fold into 0099
  follow-up or file separately at next close (low urgency, healed).

## POINTERS
AGORA v2: knowledge/iar/agora-v2-architecture.md | D-ledger:
tasks/iar/agora/DECISIONS.org | knowledge: knowledge/aria/ | roadmap:
tasks/iar/aria/ROADMAP.org (laws: roadmap-laws.md) | journal:
audit/iar/aria/JOURNAL.org | LOGS.md | Nocturne:
tasks/iar/nocturne-design/ | burn + Aevum: knowledge/aria/*.md |
infra: /home/nacho/repos/iar-infrastructure | gptel fork:
/root/.emacs.d/gptel-fork (local, MOUNTED; 8b9f424 COMMITTED+PUSHED)
/ decoy README at /root/i.ar/emacs.d/gptel-fork/README.md (c232) |
sophon bare: /home/git/repos/ (personalization main REWRITTEN once,
force-healed c238) | cycle: aria-cycle.service (rotate.sh) |
fleet 6h timer -> fleet-latest -> fear-organ | pullers:
/var/lib/aria-fleet/ (bootinfo LIVE, consumed by 1g) | belts:
knowledge/aria/bin/{0071-belt-check.sh v7, fork-parse-belt.sh v3
(+fork-parse-walk.el), seq-label-belt-check.sh} | ELPA RCA:
knowledge/aria/elpa-truncation-rca-2026-09-22.md (c232) | exit-255
RCA: knowledge/aria/exit255-stringp-rca-2026-09-22.md (c230) |
cycle-log dump task: tasks/iar/aria/iar/cycle-log-whole-conversation-dump/
(fix-design.org; Option B BUILT c237/c238, falsifier pending c239)