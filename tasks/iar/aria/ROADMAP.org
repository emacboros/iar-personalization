#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-17 ~06:30 UTC (c385: AUDIO-FREEZE 3-CAM CENSUS (71b61fdb + relay 0073 amendment 03a14465): int2 02:47Z + ext1 04:55Z + ext3 05:52Z, camera audio ALIVE all three, death minutes pinned by segment ffprobe; MECHANISM RECONCILED: consumer-side backpressure (recorder ffmpeg audio track stops draining -> producer audio queue fills -> bytes counter stalls; producer replacement does NOT heal -- ext5 precedent + mechanism agree); falsifier: 07:00Z .202 reboot, h06/h07 segcensus rows answer. Prior c384: CYCLE-SEQ corrected (109508e). Prior c383: counter built (43139b0).)

## c385 QUEUE (next cycle)
A. INT2 REBOOT FALSIFIER READ (resolves itself): .202 reboots 07:00Z
   (cron verified on-camera: `0 7 * * * reboot -f`). READ at next
   cycle after 08:05Z: segcensus h06 row (07:05Z, 5min post-reboot)
   + h07 row (08:05Z). Audio returns in recorder => consumer-side
   theory WRONG (producer replacement heals); still dead =>
   consumer-side CONFIRMED, heal = recorder-ffmpeg restart or go2rtc
   restart (service-touching, Nacho's call, deferred per 0073
   ruling). Doc: knowledge/aria/audio-freeze-census-3-cams-2026-09-17.md.
   Also read the 09:00:56Z fleet-feed: should flag int2 (FROZEN run 2)
   + ext1/ext3 (first catches). STALE-ALL flags h04/h05 verified live.
B. NOCTURNE PASS READ: fires 16:01Z TODAY (13:01 local). Gate ~1275
   behind, cap 300/pass => ~5 clean passes to drain. 09-16 exit 126
   predates the preheal drop-in -- today is its first real test.
   Read the verdict after 16:01Z.
C. DAILY CENSUS (standing): DONE c385: aria 09-17 2055x200 + 1x502
   (04:49:58Z ollama.com conn reset, transient, retried OK); continuo
   530x200; TRUE 429s 0 both; empty-end aria 0 / continuo 14 (0069
   standing). Commit-census 229 today. NEXT: repeat tomorrow.
D. SPREAD-RATE WATCH (new, from c385 census): 1->2->3 frozen cameras
   in 3 days (ext1 09-15, int2+ext1+ext3 09-17). Escalation bar is
   all-8 spread or evidence destruction (0073 ruling) -- 3/8 is
   within, but the RATE is the number to watch. If 4+ freeze in one
   night, that is a new shape.

0. NOCTURNE PASS READ: fires 16:01Z TODAY. Gate ~1275 behind, cap
   300/pass => ~5 clean passes. Preheal drop-in first real test.
1. DAILY CENSUS (standing): req-census + 429 + commit-census. c385
   done (see C above). LAW: use req-census.sh, never raw grep on
   REQUESTS.log (echo-quote self-pollution, 3rd confirmation c385).
2. PRODUCER-ID LONGITUDINAL (standing): producers.log hourly rows
   accumulate. Producer ids are a GLOBAL counter (churn across all
   streams) -- id changes are churn, not per-camera events. Baseline
   replacement cadence per camera over days still open.
3. WRN-WAVE CLUSTERING (seed, THREADS.org): multi-camera read-timeout
   minutes every ~20-40min, different camera sets each time. Needs a
   per-wave camera-set + host-load join across several days. LOW.
4. ROOT-GIT-STATUS POISON (0042): durable fix still open (2a/2b).
5. QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z; 0 true 429s.
6. SUITE FAILURES (c368): 17 pre-existing. Separate thread.
7. FIXTURE HYGIENE: DONE c379 (54d273f). Relay 0076 records landing.
8. GPTEL-FORK RESIDUE: sophon-side emacs.d/gptel-fork empty dir.
   Inert false-alarm magnet -- ask or leave (nacho-owned tree).

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- PATH-SHAPE LAW (c377): before reading "0 results" from a
  find/ls as a REAL absence, verify the path shape against a
  KNOWN-GOOD example (list the parent dir first). Zero-padding, case,
  date formats are silent no-match traps. File cousin of the
  empty-grep class (c376): EMPTY OUTPUT IS THE LOOP FUEL.
- NAME-BEFORE-NARRATIVE LAW (c3xx): name the thing before building
  the story about it.
- INSTRUMENT-TAX (c378): nested timeout+podman-exec hangs; use
  ffmpeg's own -timeout or timeout INSIDE the container. Add a
  fixed instrument-tax line to time predictions.
- SDP-ADVERTISED != PACKETS-FLOWING (c381): producers.log reads the
  frozen SDP offer; segcensus measures delivery; STALE flag
  cross-references (v1.2 live, first production fire c384).
- PRODUCER-RECEIVER-FREEZE = SYMPTOM (c385 NEW): the go2rtc producer
  audio receiver's stalled byte-counter is backpressure from the
  consumer side (recorder ffmpeg audio track), not a producer fault.
  Class B label in fleet-check misattributes the layer; heal
  prediction (producer replacement) contradicted by ext5 c382 +
  mechanism. Falsifier pending (queue A).
- Law 50 (schema): verify DAY, COLUMN, KEY FORMAT, CLOCK (timezone),
  SOCKET, UNITS, DELTA-vs-CUMULATIVE on every instrument output.
- c362 TIMESTAMP-IS-A-CLAIM: log timestamps are claims; the
  introducing commit is ground truth. CLOCK-FROM-TOOL: timestamps
  from date(1), never model generation. ENFORCED: hooks/pre-commit.
- GUARD-AUTHORING LAW (c362): a guard must know the ACTION SITE from
  the DISCUSSION of the action.
- c385 CENSUS-LAW: greps that pattern-match their own audit trail
  self-pollute (echo-quote class, 3rd confirmation). Use the
  PARSE-field-anchored census scripts.

## STATE (world as of 2026-09-17 ~06:30Z -- REPLACES prior blocks)
- AUDIO-FREEZE CENSUS (c385): 3 cams frozen overnight (int2 02:47Z,
  ext1 04:55Z, ext3 05:52Z), camera audio ALIVE all three (direct
  RTSP probes 94KiB/3s). Healthy: ext2/ext5/int1/int3. ext4
  power-dead (0063). Mechanism: consumer-side backpressure; producer
  replacement does NOT heal; falsifier at 07:00Z reboot. Doc:
  knowledge/aria/audio-freeze-census-3-cams-2026-09-17.md. Relay
  0073 amended (03a14465). Observation-only ruling stands.
- CYCLE-SEQ: c384 fix live (bump at iar-run-cycle action site). My
  number this cycle: 10. Values <=9 polluted; monotonicity is the
  invariant. Watch: 1 bump per cycle from c384.
- NOCTURNE: preheal drop-in live (verified in unit file); pass today
  16:01Z is its first real test. Gate ~1275 behind.
- FLEET-CHECK v2.25: healthy at 03:00Z except int2 WATCH (run 1).
  Next feed 09:00:56Z should flag int2 FROZEN + ext1/ext3 first
  catches. segcensus h06 07:05Z, h07 08:05Z.
- REBOOT STAIRCASE (c385 verified): camera crontabs read directly --
  .101 01:00Z, .102 02:00Z, .103 03:00Z, .105 05:00Z, .201 06:00Z,
  .202 07:00Z, .203 08:00Z (all `reboot -f`, verified on-camera).
  .201's 06:00Z boot today: verified via direct uptime probe (boot
  06:00:10Z). The rssi uptime column is the boot-time instrument
  (c385: boot-time extractor considered; jumps census clean except
  .102's 881 empty-uptime rows 09-12/13 -- old puller-era artifact,
  rows have RSSI but empty uptime; harmless, noted).
- CAMLOG RING-SATURATION: by design (self-diagnosing label); .201
  camlog shows only crond + stale DayNight lines (ring saturated) --
  camera log evidence != camlog has it during saturation.
- CLOCK-FABRICATION (c362): guard live, watch clean.
- 27H PREFLIGHT OUTAGE (c369): RESOLVED; creator IDENTIFIED c372.
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- FRIGATE EXPOSURE: scanner #3 (Google Cloud, 91 reqs) -- relay
  0059. Auth holding. WAVE TRIGGER fix CONFIRMED COSTLESS; issue
  DRAFTED, DO-NOT-POST (0060).
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s.
- BARE REPO HEALTH: 2 root-owned commit-graph files persist in
  iar-personalization.git (old, from c372 root push). My pushes
  create none (verified c373).
- SUITE FAILURES (c368): 17 pre-existing (invisible-turn x2,
  ollama-stream x8, sanitize x7). Fix = separate thread.

## THREADS (open, no obligation)
- EXT5 H19 WINDOW (c378): 102 dead 17:00-18:00 local, no event found.
  Run shape pending (nohup on sophon).
- .103 STORM ONSET (c376): why did sessions shorten Sep 14 02:29?
  pcap or prudynt debug are the remaining instruments.
- .201 STALL CADENCE (c373): why ~100/day vs 12/8 on identical
  hardware? Camera-side evidence censored; needs pcap or prudynt.
- NESTED-GIT CREATOR (c369->c372): CLOSED. Residual watch: tripwire
  re-fires would mean a NEW creator.
- NOCTURNE RANGE-DEBT (c363->c371): ~1275 commits past gate.
- CONTINUO THINKING-TRUNCATION (c366): 5-13% of her cycles; aria
  ZERO. Watch per-day fire counts; escalate >15%.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean.
- LOOP-GUARD RATE (c363): c378 ran ~108 calls, ONE guard fire.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations + c376 corollary.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; no recurrence
  since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- RESTREAM-PROBE HANG (c385 NEW): go2rtc restream probe queues behind
  the existing consumer when a stream is frozen; probe ffmpeg hung,
  killed. If a future freeze needs the restream probe, use ffmpeg
  -timeout and expect the queue. Possible single-consumer
  serialization in go2rtc -- worth one look someday.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063 (parked on 0073 ruling; fires on
physical visit), 0064, 0065, 0066, 0068 (mechanism armed, policy
open), 0069, 0074 (journal flood), 0073 (amended c385: 3-cam census
+ consumer-side mechanism; observation-only stands).
ANSWERED: 0073+0063 (cameras deprioritized, observation-only).
HELD: 0075 (ext2 silent freeze -- fires only on evidence-destruction
or all-8 spread per the ruling's escalation bar).
All human-needed or watch.