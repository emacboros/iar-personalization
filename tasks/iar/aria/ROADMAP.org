#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-16 ~16:40 UTC (c370: V4 GATE READ -- Nocturne
lost 2 passes to the outage family: 09-15 16:04Z exit=1 (preflight
refuse, nested .git in place); 09-16 16:02Z exit=126 lsetxattr
gptel/.git/index EPERM = the 0042 root-git-status class at a NEW
site (gptel). POISONER IDENTIFIED: the 12:54Z outage-debug session
(sophon->sophon ssh loop, aria key = interactive iar container on
sophon, Nacho debugging) ran root git status in gptel 15:54Z;
16:21Z aria-cycle ExecStartPre chown healed it. Nocturne has NO
pre-start heal of her own; gate at e4d0832d, 939 commits range
debt. CREATOR HUNT NARROWED: belt2d suite git-inits ALL pair with
/tmp cwds; NO git-init with cwd=emacs.d in 09:00-09:32 local;
audit CWD-to-PROCTITLE adjacency pairing is a FALSE-PAIR TRAP
(parent-process CWD records interleave with child execves); the
stray .git likely arrived by COPY not init. Witnesses armed.)

## c371 QUEUE (next cycle)
0. NOCTURNE HEAL GAP (c370 NEW): she has no ExecStartPre chown --
   every root-git poison in any mounted repo kills her next pass.
   ASK (relay, nacho-arch): add the same root pre-start chown+chcon
   to the nocturne timer unit (or a shared pre-heal unit both
   services bind to). Also: her gate is 939 commits behind; two
   passes lost; range debt grows while she is down.
1. V4 GATE WATCH (standing): next pass Tue 09-17 16:04Z -- read
   journalctl "nocturne-digest: " (TZ LAW: sophon journal LOCAL -03).
   Expect: RANGE-CAP defers again (939 > 300); verify echo-check +
   receipt paths still clean.
2. CREATOR HUNT (narrowed c370): the stray .git had an INDEX but no
   git-init ran in cwd=emacs.d in the window => likely arrived by
   COPY (cp/rsync of a .git tree) not init. Next sighting: the
   ExecStartPre stat line (ownership/mtime) + first-fail detail are
   the witnesses. Do not guess.
3. ROOT-GIT-STATUS POISON CLASS (0042, c370 recurrence): any root
   git process touching a mounted checkout poisons it for the next
   container. 0042 already asks for the durable fix (restorecon/
   chown at a shared site). Amend 0042 with the gptel sighting
   (12:54Z debug session -> Nocturne exit 126) rather than filing
   new.
4. CLOCK-AUDIT WATCH (standing): exit 0 = clean, exit 1 = NEW.
5. DETECTOR WATCH: fleet-check v2.24 (ext3 producer-freeze WATCH
   run 1 at 15:00Z fleet -- 2nd consecutive = PRODUCER-AUDIO-FROZEN;
   read next fleet run).
6. DAILY CENSUS (standing): req-census.sh + 429 watch + commit-census.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook), history-clock
  (hook).
- TZ LAW SHARPENED (c359b): sophon JOURNAL timestamps are LOCAL -03.
  A "3h stale" number that equals the Argentina offset is the c342
  too-clean-number class: unit-check the CLOCK before it gets a
  mechanism.
- LANDING-NOT-RECORD (c358): record BEFORE new threads when budget
  runs low.
- NIL-CONTENT LANDMINE (c358): stream chunk with no :content coerces
  to "" at parse layer -- (string-blank-p nil) kills the process
  filter. GUARDED (fork b85fb12).
- FUTURE-DAY GUARD (c358): instruments that take a DATE refuse future
  dates loud (exit 2); an empty verdict for an unhappened day is a
  fake-clean record by construction.
- CENSUS 3-ANCHOR LAW (c342): census = line-start regex +
  field-position status check + terminal-suffix extraction; merge
  log+log.1 deduped, PARSE-only. Baseline 09-14: aria 2630
  PARSE/2645 req/192.5M in; cont 1615/44.8M in.
- NUMBER-TOO-CLEAN LAW (c342): a number that fits your hypothesis too
  well (3h = exactly the Argentina offset) deserves re-derivation
  before it becomes a mechanism claim.
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.
- LAW 50 (c215-c259): an instrument's output has a SCHEMA -- verify
  the DAY, the COLUMN, the KEY FORMAT, the CLOCK (timezone), the
  SOCKET, the UNITS, and DELTA-vs-CUMULATIVE.
- GUARD COMPLIANCE LAW (c243/c259/c269): guard compliance is not
  compliance -- changing the query's costume while keeping the
  enumeration is compliance theater; when a guard fires, change the
  QUESTION or stop.
- AUDIT-ADJACENCY LAW (c370 NEW): sophon audit CWD records are
  per-process events, not attributes of the next execve -- pairing
  CWD-to-PROCTITLE by stream adjacency manufactures false pairs (the
  parent emacs's cwd=emacs.d interleaves with its children's /tmp
  cwds). Pair by PID/PPID or verify by content, never by adjacency.
- STUCK-STATE LAW (c368/c369): a guard's refusal creates a stuck
  state that outlives the cycle; guards need un-stuck mechanisms
  attached at the action site, or every refusal becomes an outage.
  Instances: stuck-staged index (c368), nested .git lockout (c369).

## WORLD STATE (2026-09-16 ~16:40 UTC -- REPLACES prior blocks)
- OUTAGE FAMILY STATUS: cycle service healed (emacs.d/.git ABSENT,
  drop-in live, first-fail detail armed). NOCTURNE still exposed
  (no pre-start heal; 2 passes lost; gate e4d0832d, 939 debt).
  gptel/.git/index healed by 16:21Z service chown (nacho-owned,
  container_file_t).
- 12:54Z SSH LOOP IDENTIFIED: sophon->sophon root ssh with aria key
  12:54-13:03 local = interactive iar container on sophon (Nacho
  debugging the outage; AI-voiced multi-echo commands = interactive
  aria). Its root git status in gptel = the 0042-class poisoner.
  gptel/.git/index mtime 12:54 local matches.
- CREATOR HUNT: no git-init in cwd=emacs.d 09:00-09:32 local 09-15;
  belt2d suite inits all /tmp-paired; .git had index => git add ran
  somewhere; copy-not-init is the leading hypothesis. NOT CLOSED.
- FALSIFIERS (c369 DONE): ext1 heal FALSIFIED (healed ~12:41-15:00Z
  Sep 16, 13h late; 0073 OPEN); .101 boot+2s burst CONFIRMED (boot
  01:00:14Z, boot+16s ARP probing .58).
- V4 GATE: two failed passes (09-15, 09-16); RESERVOIR-DRAIN fired
  09-15 (unratified proposal archived to attic, pass started clean);
  echo-check never reached (rc!=0 both). Next pass Tue 09-17 16:04Z.
- FLEET 15:00Z: ext3 producer-audio-freeze WATCH (run 1, delta=0B/4s,
  video flowing); ext4 power-dead (0063); restic ok; frigate 927
  events/24h; .58 identity = Nacho's (0062).
- CLOCK-AUDIT: history-clock-audit.sh needs an AGENT arg (usage:
  history-clock-audit.sh <agent> [file] [future-tol] [stale-tol]) --
  c370 ran it bare (exit 0 = usage error, NOT a clean verdict; the
  exit-code watch needs the arg to mean anything). Run per-agent
  next cycle: aria + continuo.
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s so far.
- AUDIO CLASS: ext1 producer-freeze OPEN (0073); ext3 WATCH run 1.
- CAMERA OUTAGE: .104 power-dead since 09-12 (0063, physical = Nacho).
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- SUITE FAILURES (c368): 17 pre-existing (invisible-turn x2,
  ollama-stream x8, sanitize x7). Fix = separate thread.

## THREADS (open, no obligation)
- NESTED-GIT CREATOR (c369->c370): narrowed. No git-init in the
  poison window with cwd=emacs.d; audit adjacency pairing is a
  false-pair trap (AUDIT-ADJACENCY LAW). Leading hypothesis: the
  .git arrived by COPY (cp/rsync) not init -- the index + hooks
  shape fits a copied tree. Witnesses: ExecStartPre stat + first-fail.
- NOCTURNE RANGE-DEBT (c363->c370): 939 commits past gate, cap
  300/pass, 2 passes lost. If the heal gap is fixed and she runs
  clean, catch-up takes ~4 passes. If gate stays >1 day behind:
  raise MAX_RANGE or weekly catch-up.
- CONTINUO THINKING-TRUNCATION (c366): nemotron thinking channel
  runs away at the 32768 cap on 5-13% of her cycles (18 fires/6d;
  aria ZERO on glm-5.3-flash). Guard ends the cycle correctly; cost
  ~6min GPU + full cycle per fire. NOT correlated: load, msgs, ctx,
  day phase. Census:
  knowledge/iar/continuo-thinking-truncation-census-2026-09-15.md.
  Watch: per-day fire counts; escalate >15% or if aria's model fires.
- SUITE FAILURES (c368): 17 pre-existing failures. Fix = separate
  cycle's thread (likely gptel-fork b85fb12 interaction).
- CLOCK-FABRICATION (c362->c363): guard live, watch clean. Prompt-side
  fix (inject date(1) output into cycle context) buildable under the
  c366 freedom grant -- candidate.
- LOOP-GUARD RATE (c363): falling (100-135/day -> 25). Watch.
- COMMIT-LOG CHURN (c360->c361): census + hook live. 09-15: 171.
- AUDIO CLASS (c345->c355): ext1 producer-freeze (0073); ext3 WATCH
  run 1 (c370); ext5/ext2 healed. Detector v2.24.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED c369 (.101 probes .58).
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; no recurrence
  since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census
  of all agent turn traffic (no attribution). Rate questions ->
  journal; attribution -> known-window correlation.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063, 0064, 0065, 0066, 0068 (mechanism
armed, policy open), 0069, 0073 (ext1 falsifier, instrument ready).
All human-needed or watch.