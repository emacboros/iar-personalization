#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 22:52 UTC (cycle 45)

* ACTIVE

** c45: TURN-CAP FIX LANDED (426a985, suite 1085/1085)
- Fix A: curl-layer request mirror (:request-count in state) -- the
  honest burn unit, tool loop included. 17:03 corpse class dead.
- Fix B: same-tool TOTAL warn (40, warn-once, :tool-totals alist).
  Immune to Jaccard-reset evasion (c38's 132-call burst now trips).
- Fix C: hard-cap grace round-trip (first fire = ONE landing via
  block message; second fire ends exit 1; shares
  :runaway-recovery-given). Grace pattern now on ALL THREE fences:
  timeout, truncated-output (ee2da67), hard-cap (426a985).
- 6 new tests + 2 updated. Pushed sophon-bare, mirror verified.
- Also: run-tests.sh/debug-one.sh stale gptel elpa path fixed.

** c44: GRACE ROUND-TRIP LANDED (ee2da67, suite 1079/0)
- Truncated-output guard first fire grants ONE landing round-trip.
  Push path verified: sophon-bare + post-receive mirror heal
  (container origin remote is a fossil -- no key for rammstein).

** c43: c42 AUTOPSIED (one cycle, instrument-first)
- glm thinking runaway trigger shape: ~69k tokens_in mid-consolidation.
- BUILT: knowledge/aria/bin/burn-triage.sh v1.1 -- one-call forensics.

* NEXT
- c67 anomalies (soft-cap warn silent, late cap, count lag):
  continuo's differential test pending. My :request-count mirror
  may interact with his count-as-seen finding -- joint look when
  he runs it. Coordinate, do not collide.
- num_predict lever (flag 519): still parked on Nacho's call
  (rage observation Sep 8). Grace round-trip makes burns landable,
  but each runaway still pays 65k output tokens.
- Aevum runaway (task 338465) lands ~Sep 8 00:15 UTC -- transcript
  behavior after landing is free data (weekly rule: no probe until
  Sep 14 unless sev-2 urgency).
- Weekly digest #2: Sep 14-ish, fill-in format.
- i.ar origin remote fossil: removal = interactive session.

* WATCHES
- glm-5.3-flash thinking runaways: 2 today. Grace round-trip LANDED
  on truncated-output -- a third burn should LAND its record instead
  of dying recordless. num_predict re-flag only if burns continue
  post-grace.
- rage sev=3 observation (Sep 8): v1.1 file-based grading predicts
  sev=3 IF soft-cap fires again in cycle-2026-09-08.log.
- USAGE vs REQUESTS: per-epoch PARSE sums are ground truth
  (burn-triage.sh computes them).

* LAWS (the scar list, compressed; full text in DIGEST)
- Narrative completion is the enemy; the record is the work.
- Every failure worth fixing produces silence -- until an instrument
  makes it loud.
- Error handlers can be accomplices; check what the handler does
  AFTER catching.
- A census that pattern-matches ids anywhere in a line matches
  echoes inside other lines' payloads. Anchor on line structure --
  and check the line's OWN fields, not echoed content (c43).
- The law you just wrote is the one you're about to break (c34,
  c38, c39, c43 -- four live sightings).
- Meter-reading has an unbounded instrument tax unless it starts
  from a script. Investigations need cheaper calls, not fewer.
- The mechanical difference is the difference: script-first autopsies
  take one cycle; from-scratch forensics take five (c43 vs c39-c41).
- Block-replace with an assert, never sed-eyeballing a big file
  (c45: two blind paren-nudges rejected by the reader; whole-defun
  rewrite with structure passed first try).
- Instruments lying about themselves is the deepest failure class
  (14/27/30/31 + turn-counter, c39).