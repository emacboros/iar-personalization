# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-08 ~22:01 UTC (cycle 96)

## NOW

- c96 DONE: nemotron-3-ultra:cloud retainer survey -- PASS on the
  D-010 tool-call gate (6/6 probes clean, JSON clean without explicit
  instruction, honest refusals, needle correct to 41k tok, prefill
  ~2.8k tok/s). Cloud model: zero VRAM, zero eviction risk. Survey:
  knowledge/aria/nemotron3-ultra-survey-2026-09-08.md. Relay aria-0009
  filed (allowlist ratification request). Pushed d3fbfe0.
- c95 push PENDING RESOLVED: f89e47a landed (first action this cycle).
- Pulse all green (services, tripwire 0, disk 28%, heartbeat alive,
  gpu-load-probe live).

## NEXT (cycle work, in pull order)

1. Allowlist ratification (aria-0009) -> on ratify: flip
   spawn-interlocutor.org model field from PENDING-BENCHMARK to
   nemotron-3-ultra:cloud (fallback glm-5.3-flash:cloud). Runtime
   itself stays interactive-session work (D-005).
2. Digest #1 + Aevum pulse: Sep 9 (tomorrow). Weekly-only per Nacho.
3. Relay queue for Nacho: 0000 (heartbeat watch -- heartbeat 0006
   INSTALLED per aria-0008, watch should be closeable), 0001 (camera
   keys), 0002 (read_own_prompt gating), 0003 (census echo caveat),
   0006 text-loop, 0007 audit status lie, 0008 heartbeat install note,
   aria-0009 NEW (allowlist). 0007-legacy cutover awaiting
   ratification.
4. Census v3 follow-ups: echo-debris caveat landed; confirmed-specimen
   verification stays manual. aria-0007 fix (.el) = interactive.
5. Drills: #004 delegate only on natural need. Registry + queue live.
6. Retainer build next steps: spawn-file-spec.org (format spec doc,
   build order item 1) -- pure doc, cycle work.
7. Failure-window watch: gpu-load-probe armed 00:30 local tonight;
   tomorrow's census has the load-correlation data.

## STANDING

- Pulse: all green as of c96. Re-verify each cycle.
- Diverged-pull protocol: stash -> rebase -> pop -> push. Three uses.
- Timer-state lie: aria-cycle.service shows "activating" mid-cycle;
  do not alarm.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
  2+ consecutive = escalate (would be NEW mechanism).
- glm degradation watch (session VI): 3 human-witnessed events. Cycles
  have fences; interactive does not (aria-0006).
- aria-cycle.conf key parse: ini-format key=value (cut -d=, not -d:).

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.