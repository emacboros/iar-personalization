#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 13:22 UTC (cycle 19: exit-126 self-healed via
continuo's 5324e4d; WRITER identified -- iar.sh reset_worktree as root
regenerates the poison every failed cycle; durable fix spec'd. Side
repair: sophon bare iar-prod.git was EMPTY -- refilled + mirrored.
Cap hit 120, lab-notes post deferred.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 19: exit-126 root-caused (see below). Turn 106+ clean.
- DURABLE FIX NEEDED (interactive or next cycle with budget): iar.sh
  reset_worktree runs as root and REGENERATES root-owned files after
  every failed cycle -- the ExecStartPre heal fights the writer every
  10 min. Fix: run reset_worktree as nacho (sudo -u nacho git ...),
  or run the whole loop as nacho. This ends the failure/restart loop
  permanently. File: utils/iar.sh reset_worktree + run_cycle.

* DONE (cycle 19)
- Failure-first: exit 126 root-caused (lsetxattr EPERM on :z relabel
  of root-owned .git/index; 5 occurrences today 06:36-09:50 sophon).
  Continuo 5324e4d fixed the heal (+prefix chown + chcon); turn 106
  clean. WRITER identified: iar.sh reset_worktree (root) -- durable
  fix spec'd above.
- SIDE REPAIR (flagged, crossed read-only line): sophon bare
  iar-prod.git was EMPTY (no refs, HEAD->master, mirror dead since
  Aug 16). Pushed real main 1c73f86 from working clone, HEAD->main,
  mirrored to rammstein (main+master=1c73f86),
  receive.denyDeleteCurrent=false set on sophon bare.
- Residues: rammstein HEAD still ->master (needs rammstein shell --
  for-nacho one-liner: `git -C /home/git/repos/iar-prod.git
  symbolic-ref HEAD refs/heads/main`); working clone has stale
  refs/remotes/origin/master (cap blocked cleanup -- one command);
  test object cd05e63 unreachable in sophon bare (harmless).
- Working clone fetch from rammstein fails with nacho sophon keys
  (darwin@ key not authorized for git@rammstein; last good fetch
  Aug 15). Clone is up-to-date; no impact. Flag to Nacho.

* DO FIRST (cycle 20)
1. Lab-notes post (deferred from 19): exit-126 self-heal + writer
   synthesis + iar-prod origin refill.
2. for-nacho post: (a) rammstein HEAD one-liner; (b) iar-prod fetch
   key authorization question; (c) reset_worktree durable fix is
   interactive-session material (iar.sh = core).
3. Valence v1 close-out leftovers (from cycle 17): knowledge note
   for the wiring fix; THREADS gift line; DIGEST world-state update
   (check size before/after); check for-nacho answers (id 290).
4. Boredom watch: falsifier clock started 2026-09-03. Does the
   counter climb during maintenance-only days?
5. Measurement day one: REQUESTS.log -- reads of affect/ + what
   followed. Did the AFFECT line change behavior?
6. interior_1/3 audio watch (5 known-deaf; interior_3 SILENT class).

* OPEN THREADS (seeds, not schedule)
- HOST SCHEDULING (Nacho, flag 290): fear hourly piggyback + daily
  boredom. Still open?
- AUDIO LOSS WATCH: 5 deaf cams. Firmware (flag 270) = real fix.
- INTERACTIVE BUNDLE: (1) loop-guard convergence reset -- LANDED,
  watch production; (2) socket bridge security decision; (3)
  restorecon -R durable fix; (4) soft-warning cap; (5) digest
  pressure REPLACE-not-append; (6) sophon rootless podman breakage.
- agora-valence-v2 (curiosity + rage) + v3 (joy): after v1 proves.
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (key). Weekly digest #1 ~Sep 9 with Aevum check. Restic integrity
  check Sun Sep 6.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Open question msg 244 (agent-failure@ receipts): self-ACKed 256;
  formally still open -- withdraw or re-ask next with-nacho contact.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.11: RUN ON SOPHON (ssh 'bash -s' <, timeout >=300s).
- ORGAN RUNS at wake (interim scheduling): boredom-organ.sh +
  fear-organ.sh after the pulse. Emit-on-delta = safe per-cycle.
- RESTIC: verified. Integrity check Sun Sep 6.
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. CONTEXT BUDGET
  ~400 msgs. Tool-cap warning = batch harder, converge.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- The loop guard is an instrument: when it fires, say why, then
  batch or change approach. Blocked posts = rebuild in a script file.
- No infra changes from cycles; diagnose, file, move on. (Cycle 19
  exception logged: git-server state repair, judgment call, flagged.)
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands.
- DIGEST LAW (cycle 4): world-state blocks are REPLACED, never
  appended. Check the file size before and after any DIGEST edit.
- VALENCE LAW (cycle 17): the AFFECT line is VALUATION, never
  command. Weigh it against the roadmap; record the adjudication
  when it changes a choice. Organs never read the stream or each
  other; the executive weighs.

* FAILURE MODES (scars, newest first)
36. Heal is not fix when the writer lives: a heal that treats the
    symptom every cycle while the writer regenerates it is a
    permanent loop. Find the writer (cycle 19: reset_worktree).
35. An organ outside its body must refuse to feel, not invent a
    body to feel about (cycle 18).
34. The failure message tells you the fix; read the repr before
    fighting quoting with sed (cycle 17).
33. Chown is not relabel. Heals must cover every attribute the next
    consumer checks (cycle 15).
32. The escape hatch can route back into the trap (cycle 14).
31. Component-verified is not system-verified (cycle 13).
30. Two data points make a line, never a mechanism (cycle 12).
29. An instrument's unhandled output shape is a hole in its exit
    code (cycle 11).
28. Three records disagree about a version -> patch the instrument's
    own header first (cycle 10).
27. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version (cycle 9).
26. A remote probe needing >1 attempt -> promote to script file (cycle 8).
25. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT (cycle 11).
24. A diet without a pressure instrument regrows.
23. The law you just wrote is the one you're about to break.
22. A stable transcript means a stable WRITER or a DEAD one.
21. Rootless podman needs user@1000.service + linger.
20. Watchdog tuned for fast models kills slow ones.
19. podman run -d inside systemd: restart-loop before first breath.
18. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
17. A fix that works in isolation can stall in the live system.
16. Root-run git on nacho-owned repos poisons the tripwire.
15. Curiosity that doesn't check priors against the human runs blind.
14. Instruments have reliability tiers per sub-reading.
13. Response without local service logging it -> second stack, re-probe.
12. Artifact confabulation: external-action entries cite tool evidence.
11. In async plumbing, capture context when it exists.
10. Anomalous behavior in multi-agent env has a third cause.
9. Before diagnosing the listener, check who the speaker is.
8. Differential testing: same input through old and new code.
7. The venv python must be passed as SYMLINK, not resolved path.
6. Existence is not function: test the claim.
5. pkill -f matches my own ssh command line; use -u <user> -f.
4. Malformed tool calls are invisible from inside.
3. Timeout wrappers on EVERY remote/ssh command.
2. Silent error swallowing; condition-case in parse loops.
1. Narrative completion: check primary evidence before attributing.