#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~03:55 UTC (cycle 298: continuo failure
storm post-mortem LANDED -- 09-13's 16 failures were Ollama 429
weekly-quota rejections on the Randazzo account (all cloud models
share it), NOT model degradation; continuo's in-storm diagnosis
corrected (lab-notes 1043); census note
knowledge/aria/continuo-failure-storm-2026-09-13.md; relay 0065
filed (credits vs remap vs accept -- Nacho's money/identity call);
belt #2 activation VERIFIED in git log (both agents' exit commits
carry the belt #2 message); commit d437760f pushed)

## c299 QUEUE (next cycle)
1. C265 GO2RTC API HANG WAVES: 3x 09-12, cause UNIDENTIFIED, the one
   open unexplained fleet event. The census-source map's first
   consumer: read the go2rtc container log + nginx log in the hang
   windows (normalize clocks: container logs LOCAL, nginx LOCAL,
   cameras.log UTC). Question: do the hang windows show anything in
   go2rtc log that nginx/frigate do not?
2. QUOTA WATCH (new, from c298): if 429s recur in the next weekly
   window (~09-20), the ollama.com quota is STRUCTURAL for current
   D-014 usage -- that changes mapping economics; relay 0065 already
   frames it. Zero action before recurrence.
3. FEEDER WATCH: 09:02Z aria-fleet-feed fire -- fleet-check v2.23 on
   fresh feed (03:02Z verified clean: FAIL=0, fear sev=0).
4. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.102 power-dead), 0064 (RTSP probes, folds into 0059),
   0065 (quota decision). All human-needed; 0059+0064 share one
   ruling surface.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291): cameras.log/camlogs = UTC; frigate
  journalctl --since = LOCAL on sophon; nginx log inside container =
  LOCAL (-0300); go2rtc container log = LOCAL; REQUESTS.log UTC.
  Normalize per-source before ANY gap arithmetic.
- CENSUS-SOURCE MAP (c297, knowledge/aria/
  census-source-enumeration-2026-09-14.md): viewer traffic = nginx
  log ONLY; RTSP-port probes = go2rtc log ONLY; producer dials =
  go2rtc WRN + camera prudynt; frigate remakes = frigate container
  log (readable) or host journal (unreliable for absence under
  flood); camera-side events = cameras.log. Enumerate the SOURCES
  and the PORTS, not just the samples.
- QUOTA WATCH (c298): ollama.com weekly quota hit 09-13 (16
  continuo + aria 429s). Recurrence next window = structural ->
  mapping economics. Thinking-loop truncation class (nemotron
  32k-cap, 14 cycles 09-10..09-13) is SEPARATE; watch its rate.
- EXT4 (.104) + EXT2 (.102): known-fault watch state. Power cycles =
  Nacho (relay 0063). Both cameras dial-timing out every ~10s
  (go2rtc WRN stream) -- normal until power. RSSI-freeze precursor
  (.102: rssi stopped 15min before death) needs 3rd sample.
- WAVE TRIGGER (c268): page load -> paramless GET streams -> AddTrack
  -> Reconnect() = fleet-wide RTSP remake -> +24-34s fps-limits ->
  splice -> heal. FIX (a) drop mic param = CONFIRMED COSTLESS. (d)
  issue DRAFTED, DO-NOT-POST; relay 0060 = ratify ask.
- SOLO-DEATH CLASS: refined c290/c295 -- producer re-dials common/
  benign; death = re-dial + detect fps-limit + splice; heal = page-
  load remake wave. "2h re-dial cadence" WITHDRAWN c296.
- STAIRCASE-SPLICE (c279): nightly reboot crons -> remake -> splice
  -> watchdog heal. Two ffmpeg surfaces (DTS-gap ext1; bad-cseq ext2).
- AUDIO-DEATH LAW v3.1 (c264): TWO classes, ONE heal = ANY session
  remake; record-proc restart heals, PUT alone useless.
- FLEET RTP STALL (c280): 5 producers read-timeout simultaneously
  08:10:22Z 09-12, self-healed ~40s. Cause unknown. Watch recurrence.
- ONVIF SWEEP WATCH (c288-c291): attributed to .58; census stands.
- RTSP-PORT PROBES (c297): 4x unsupported-method GET on 8554, relay
  0064 filed, folds into 0059 ruling. Watch for new probes + source
  IP context if asked.
- MSGS-FENCE WATCH: c296 hit 401 + hard cap 601; c297/c298 clean.
- RAGE WATCH: 3+ soft-cap events in 3d = escalate. c298: no fence
  events, ~37 calls total (the post-mortem was read-heavy, batched).
- Nocturne: LIVE. Aevum: weekly-only pulse. Empty-cell: designed.

## Tooling notes (do not re-derive)
- CONTAINER LOG ACCESS: su - nacho -c 'podman exec frigate sh -c
  "..."' (rootless namespace; plain podman exec from root shows only
  zulip containers). Logs: /dev/shm/logs/{nginx,go2rtc,frigate}/current.
- QUOTING THROUGH THE SU/SSH LAYERS (c297): 3 layers of quoting eats
  awk field refs and nested greps. Prefer grep/cut/tail over awk in
  the innermost sh -c; pull raw lines and process locally when the
  extraction is nontrivial.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh (the
  /root/personalization path does not exist on sophon host).
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
  ANSWER ID SHAPE: aria-0061 (filer-seq), NOT 0061 or full filename.
- TEST SUITE: emacs --batch -l test/run-tests.el (1253 tests, ~8s)
  = run-tests.sh (now execs run-tests.el).
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; use -L for --eval load-path.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277) -- do not edit, do not trust.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- ARP LOGGER (c290): arm with -e (MAC detail); stdout files are ASCII.
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our keys;
  ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- BELT #2 (c292/c295): VERIFIED LIVE c298 -- both agents' cycle-exit
  commits carry "belt #2 durability (meter + record files)"; belt
  commits are push-doomed by design until a later run pushes (zero
  risk, one inode-space).
- LAW 41 (re-learned c296): when the finding is in hand, the next
  call must WRITE, not read. A guard firing mid-investigation is the
  investigation over; a guard firing after the finding is the
  close-out overdue.
- LAW 50 SOCKET COLUMN (c298, new): the error message names the
  socket -- a 429 naming account+quota is a different failure than a
  model misbehaving, even when both land as "cycle failed". Read the
  error's own words before classing.

## THREADS (open, no obligation)
- C265 HANG WAVES: the census-source map's first consumer (c299
  queue head).
- CENSUS-SOURCE LAW: note landed c297; applied to 429 class c298
  (error-message-as-source). Remaining consumers: c265, any new
  fleet event.
- EAR-CHECK DISCRIMINATOR (c290 seed): 3/3-dead transient class;
  build the guard if a third transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure -- now with 0064 evidence),
  0060 (issue draft ratify + falsifier), 0062 (ONVIF sweep device/
  app ID), 0063 (.102 power-dead), 0064 (RTSP probes), 0065 (ollama
  quota decision) -- all human-needed.