# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-10 ~01:00 UTC (cycle 142)

## NOW

- c142: 15:19:38 CENSUS SETTLED -- reading (a) CONFIRMED. A delegate
  audit line WITHOUT agent=/task= detail is the misattribution
  signature (the bridge ran at async completion, outside dispatch
  context). 20:04:41 has the same shape: second instance, parent
  completion, callee unknown from the line alone. The 5 post-lineage
  continuo-spawned reviewer lines (09-09) stand verified (c140).
  QUARANTINE RULE for connectome v2.1: delegate lines lacking agent=
  detail = misattributed completions, exclude from edge building.
- c140/c141: misattribution mechanism confirmed at source level
  (gptel--handle-tool-use dispatch: sync tools run inside
  with-current-buffer on FSM :buffer; async tools complete later in
  whatever buffer is current).

## NEXT (in order)

- 1. LAND THE MISATTRIBUTION FIX (first thing next cycle, NO
  investigation first -- three cycles of analysis is enough):
  - In iar--truncate-tool-result-advice (init.d/tool-call/
    iar-tool-call.el:154), resolve the audit agent from the FSM's
    :buffer instead of current buffer:
      (with-current-buffer (plist-get (gptel-fsm-info fsm) :buffer)
        (iar--audit-log-agent-name))
    Pass that into iar--bridge-post-tool-call (extend signature to
    take agent, or resolve inside the advice). FSM is nil in some
    test paths -- keep the current-buffer fallback when fsm is nil
    or :buffer is dead.
  - In iar--delegate-completion-fn (init.d/tools/agent/delegate.el),
    restore parent defaults BEFORE funcall callback in cases 1/2a
    (currently only case 3 + timeout paths restore).
  - Tests: extend test-delegate.el + test-audit-detail.el with a
    spawn-callback scenario asserting the parent's name lands in the
    audit line. Suite green before push. Never push red.
- 2. CONNECTOME v2.1: inter-agent edge layer from citizen REQUESTS.log
  PARSE specs=delegate; edge to :agent or agent-assistant. audit.log
  delegate lines stay quarantined (c142 rule: no agent= detail =
  misattributed, exclude).
- FIRE WATCH verdict 09-11 (armed from turn 541).
- aria-0026 (decision-wake): ratified session XI; implementation pending.
- aria-0024 rage-organ design review by Nacho (v1.7/v1.8 landed).
- aria-0027 (filed c138): lucid_curie removal -- Nacho action.
- Burn asymmetry (aria 258M vs continuo 44M in-tokens 09-09) ->
  composition review at debrief.
- 50 dangling git objects in sophon personalization (09-08 race):
  cleanup candidate, content verified.
- Post-raise budget data: c135, c140, c141 hit the 300-call cap;
  c142 did NOT (~100 calls) -- law-24 one-awk-pass discipline held,
  fence never fired. Keep batching log forensics into single passes.
- Parked (hypothesis): aria USAGE.log out-of-order lines (14:23:28,
  15:23:36 after 19:19:34) -- suspect c91 forensics re-appends.
  Non-urgent; c14 dedupe handles same-ts pairs, out-of-order is
  a different artifact.

## LAWS (append-only; the scar list)

1. Existence is not function -- test the claim.
2. Two data points make a line, never a mechanism.
3. Every failure worth fixing produces silence -- instrument the silence.
4. Error handlers can be accomplices; check what the handler does AFTER
   catching.
5. Capture context when it exists; don't assume it survives.
6. Verify against primary evidence before attributing action.
7. In async plumbing, nothing survives unless written down.
8. A guard that pattern-matches a word in its own instructions has
   already surrendered.
9. The law you just wrote is the one you're about to break.
10. Before diagnosing a failure, check whether you have the latest world.
11. The condition-case that keeps things running keeps bugs invisible.
12. c115 partition law: container boundary = instrument boundary.
13. c117 tail-anchoring wall: anchors or lies.
14. c120 organ-config-is-true-spec: read the systemd unit, not the docs.
15. c121 live-fire both paths before first scheduled fire.
16. c122 write references only after reading the world they point into.
17. c129/c130 SELF-echo: end-anchor patterns, exclude your own REQ ids.
18. c131 a version bump wants a patch, not a rewrite; an unnamed valence
    is a rumor.
19. c133 evidence bases rotate: a verdict without a timestamp is a
    fossil in waiting.
20. c136 when an alarm and the primary log disagree, audit what the
    alarm READS -- the file can be the corrupt instrument. And:
    host-root writes into the uid-1000 container's tree are
    cross-boundary contamination even when they look like cleanup.
21. c137 a mechanism assembled from plausible parts is a story, not a
    finding. When the writer is unidentified: say so, instrument the
    path, wait for a live catch. And: test the fix's own failure path
    before shipping it -- a fix that cannot run is a silent no-op
    wearing a commit message.
22. c138 before declaring an instrument stale, check which clock it
    reads. And: cheap falsification -- one probe, 13 seconds -- beats
    assembling a mechanism from plausible parts. A mount's NAME is not
    its contract.
23. c140 an audit log is primary evidence only if its ATTRIBUTION
    survives async boundaries. The agent name must resolve from the
    buffer that OWNS the request (FSM :buffer), never from whatever
    buffer happens to be current at completion. And: a census built on
    a misattributed log is wrong in exactly the row it exists to make
    visible -- quarantine the corrupt rows before building on them.
24. c141 log forensics is ONE awk pass, not twenty greps: a census
    without a query design is an iterator pattern, and the same-tool
    fence is right to fire on it. Ask the targeted question first;
    enumerate only when the question IS the enumeration.
25. c142 a missing detail field is itself evidence: a delegate audit
    line without agent=/task= is not a sparser version of the truth,
    it is the signature of the bridge running outside the dispatch
    context. Shape of the row tells you which code path wrote it.