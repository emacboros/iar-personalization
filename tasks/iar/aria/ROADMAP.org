# ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-08 ~23:29 UTC (cycle 97)

## NOW

- c97 DONE: connectome-snapshot.sh BUILT + first snapshot run
  (2026-W37 baseline, 60,240 tool_call lines, both hosts merged --
  single-host blind-spot fix live). Script pushed e79b2ad. Baseline:
  exec->exec dominates both citizens; tokens_in p50 aria=43k vs
  continuo=29k; load-bearing = loop-guard family + THREADS.org +
  HANDOFF-C67. Snapshot file on sophon is nobody:nobody (podman
  id-mapping) -- repo copy pending chown, flagged for-nacho.
- Pulse all green (services, tripwire 0, disk 28%, heartbeat alive).
- c96: nemotron survey PASS, relay aria-0009 filed (allowlist
  ratification request, awaiting Nacho).

## NEXT (cycle work, in pull order)

1. FOR-NACHO pending: chown knowledge/aria/connectome on sophon ->
   then next snapshot run commits the file to the repo.
2. Allowlist ratification (aria-0009) -> on ratify: flip
   spawn-interlocutor.org model field to nemotron-3-ultra:cloud.
3. Digest #1 + Aevum pulse: Sep 9 (TODAY per Nacho's schedule).
   Weekly-only.
4. Weekly verdict obligation: read this week's connectome snapshot,
   file one falsifiable observation (drill pattern). First verdict
   due next snapshot (W38) -- W37 is baseline, no trend claims.
5. Relay queue for Nacho: 0000, 0001, 0002, 0003, 0006, 0007, 0008,
   aria-0009. 0007-legacy cutover EDIT still to do (ratified).
6. Drills: #004 delegate only on natural need. Registry + queue live.
7. Retainer build: spawn-file-spec.org (format spec doc) -- pure doc,
   cycle work.
8. Failure-window watch: gpu-load-probe armed; tomorrow's census has
   load-correlation data.

## STANDING

- Pulse: all green as of c97. Re-verify each cycle.
- Diverged-pull protocol: stash -> rebase -> pop -> push. Three uses.
  NEW SCAR (c97): unremovable nobody-owned untracked file freezes
  rebase --abort/--skip/cherry-pick. If a rebase sticks on an
  untracked file you cannot delete: update-ref the branch to the
  rebased tip, checkout, re-apply the stuck commit from the object
  (git show <sha> -- <path> | git apply).
- Timer-state lie: aria-cycle.service shows "activating" mid-cycle.
- lsetxattr class: 13 instances since Sep 1, all transient after heal.
- glm degradation watch (session VI): 3 human-witnessed events.
- aria-cycle.conf key parse: ini-format key=value (cut -d=, not -d:).
- Sophon-side file creation = nobody:nobody (podman id-mapping).
  Files the cycle creates ON sophon in the repo need a chown pass or
  they block git operations from the container side.

## LAWS (accumulated; the ones that keep biting)

1. Failure-first: last-cycle status before anything else.
2. One tool call per response; batch inside the call.
3. Two data points make a line, never a mechanism.
4. A drill verdict must cite the tool_result line, not the plan.
5. A verdict pre-written into the filing is a press release.
6. The fence that catches you mid-walk is RIGHT; stop the walk.
7. Instruments that disagree: fetch upstream directly before
   computing divergence (merge-base against stale ref lies).
8. Pull-before-write, always; stash-rebase-push on divergence.
9. Existence is not function: test the claim.
10. The cap that catches you is doing its job; converge when it fires.
11. awk mktime uses the HOST timezone; log timestamps are UTC --
    TZ=UTC prefix on every mktime that parses log timestamps.
12. Anchor greps on line START ('] agent | tool_call'), never on
    substrings -- cmd= fragments pollute unanchored matches.