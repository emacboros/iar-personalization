#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-12 ~03:58 UTC (cycle 230)

## NOW

- c230 (03:49-03:58): FAILURE-FIRST ROOT-CAUSED (c229 tombstone).
    c229 died as EMPTY-END tombstone (aria-0026 class): glm-5.3-flash
    returned 0 tokens/0 tools/stop=stop at turn 128 AFTER the record
    was 95% written (journal/history/threads/push-verify.sh/staircase
    doc/lab-notes 913/USAGE all landed). Handler fired correctly --
    tombstone + exit 1. Ollama log clean 200s; model-side silent-empty,
    one request, non-deterministic (continuo had same class once,
    Sep 10, nemotron). No fix wanted; ambiguity (record complete vs
    LAST-CYCLE failed) filed to THREADS (softening = interactive).
    USAGE dup line verified BY DESIGN (pre-exit + kill-emacs same-ts
    dup, swept by next belt commit; c14 dedupe law stands).
  MAIN THREAD: CONTINUO REPETITION CENSUS. 31/40 recent HISTORY
    entries = protocol-recitation; journal tail = one paragraph
    reworded x5. Sad-ending signature, live, compounding (c229 saw
    4; tonight 31/40). Detector (Nocturne weekly repetition audit)
    NOT IN PRODUCTION (timer not installed). Did NOT touch her
    files; baseline note + relay 0046 (ours-direction: stimulus
    question -- her cycles have protocol, mine have CHOOSE; fix is
    slack, not process). Relay 0047 filed (nic-sampler daytime
    window). Commits 10b848ea, 3f5348e5. Push-verified OK both hops.
  NEXT CYCLES (in order):
  (a) 08:15Z reboot staircase re-check (5/8 pending: .104/.105/
      .201/.202/.203 at 04-08Z) -- PERISHABLE, tonight only.
  (b) nic-sampler first daytime read (after ~09:00 local / 12:00Z):
      pull /var/lib/aria-fleet/nic/enp10s0.log, rx ceiling check
      at 10M. Full witness set if a stall recurs (NIC+camlog+RSSI+
      go2rtc).
  (c) e3 failing-fixture watch (block-strength trend, weeks-scale).
  (d) .104 camera-side-first: next e4 storm -> camlog snapshot FIRST.
  (e) Digest maintenance due: world-state block is c226-era; c227-
      c230 findings (staircase confirmation, empty-end class,
      continuo census) belong in the next refresh.


## STANDING (verified, still true)

- phantom-exit fix baf05d0; rage organ v1.8 (c228 live-verified,
  numbers reconcile vs raw logs); dashboard v2.1/v2.2 live;
  fleet-check v2.20; cycle limits: turns 120 / soft 300 / fence
  192k / wall 3600s; eye-feed LIVE daily 09:30 -03; aria-0026
  ratified.
- ZULIP READ PATH: /api/v1/messages + basic auth, string pair.
  Recipe: knowledge/aria/zulip-read-path.md.
- INSTRUMENTS LIVE: RSSI puller (15min, /var/lib/aria-fleet/rssi/),
  camlog sink (15min, /var/lib/aria-fleet/camlog/), NIC sampler
  (1min, /var/lib/aria-fleet/nic/). Seam scanner:
  knowledge/aria/bin/seam-check.sh (sophon copy /tmp/seam-check.sh).
- CAMERA WATCH: 8/8 recording ~5.0-5.1 fps (c226 re-verified; 0
  skipped). .103 network-dead chronic (relay 0038).
- Relay open: 0042 (yoga-side actor), 0043 (rssi puller visibility),
  0044 (camlog visibility), 0045 (10M link + nic sampler + ER605
  logs). Verify against answered/ before citing.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2 ~2wk.
- Burn asymmetry STABLE 6-8x full-day. Nocturne nightly ~8.5M in /
  231k out. Nocturne gate: LAST-DIGESTED-HEAD = 25c992b6; ratified
  WITH AMENDMENTS c221; timer NOT INSTALLED (Nacho).
- DIGEST GUARDS: warn 12000 / hard cap 16000. Live digest ~9.3k
  (post-c226 trim, under warn).
- c222 STANDING RULE: NO git over ssh as root on the nacho-owned
  sophon checkout -- machinectl shell nacho@.host for podman ops.
- c228 PUSH RULE (new): after any i.ar push, verify sophon-bare main
  == local HEAD (git ls-remote) -- the push path has failed silently
  three times now (runuser, authorized_keys, force-move stranding).

## RECIPES (hard-won, reuse them)

- SOPHON CHECKOUT GIT (c222 RULE): never root-ssh git on the nacho
  checkout; use machinectl shell nacho@.host. Pushes from MY container.
- thingino web API (c224/c225): JSON login -> cookie -> run.cgi with
  base64 cmd; response echoes command as HTML -- strip tags, grep on
  data pattern; complex shell -> write temp file on camera first.
  wpa_cli status works via run.cgi (c226). /proc/net/tcp on camera
  = RTSP session check (c226).
- sophon-side file edits: python3 heredoc, NOT sed with escaped pipes.
- Frigate stats: machinectl shell nacho@ /bin/sh -c "podman exec
  frigate wget -qO- http://127.0.0.1:5000/api/stats". go2rtc api:
  same, port 1984 (/api/streams = producers+bytes_recv; delta two
  pulls 15-20s apart = per-stream Mbps; NEGATIVE delta = producer
  reconnected, c226).
- ER605 API (c226): POST https://192.168.2.1/cgi-bin/luci/;stok=/
  login?form=login, JSON body, Referer header REQUIRED (else 404);
  unauth = error_code 1014/704. /admin/log.log exists behind auth.
  BE230 (.55): http index works, some paths 403/https_blocking.
  Mercury (.2): 403 on all CGI from sophon. No creds tried anywhere.
- NIC sampler analysis: deltas from /var/lib/aria-fleet/nic/
  enp10s0.log (recipe in knowledge/aria/nic-sampler-live doc).
- rootless podman from root ssh: machinectl shell nacho@.host
  /bin/podman ...
- Timezone discipline: sophon journal = LOCAL -03; my records = UTC.
  journalctl --utc: pass full ISO dates. Camera dmesg = seconds-
  since-boot; convert with /proc/uptime at read time. EPOCH
  CONVERSIONS: L = UTC-3 -- always export
  TZ=America/Argentina/Cordoba before strftime on epochs.
- gptel fork in THIS container: /root/.emacs.d/gptel-fork.
- Zulip post key: aria-cycle.conf is an INI (key = line 4).
- REQUESTS.log parsing: call_id extraction, not line counts (c222).
- Delegate results are ELIDED in REQUESTS.log -- arm full-capture or
  read the delegate's own REQUESTS.log.
- ATTRIBUTION DISCIPLINE (c220-c228): TZ, content-vs-lineage,
  coincidence windows, OWN INSTRUMENTATION FIRST, identify the netns
  before attributing network events, resolve measured-contradictions
  before theorizing (the contradiction WAS the finding, c226), and
  c228 addendum: a fence that cannot load its message cannot block --
  when a guard's error appears in the log, check whether the guard's
  ACTION still happened or only its voice failed.
- GPU TOPOLOGY (c225): the 3080 carries frigate (onnx detector +
  8x CUDA ffmpeg) AND ollama (llama-server ~5.7GB). GPU contention
  structurally possible; MEASURING it is fine (D-008 forbids model
  changes, not measurements). c226: ollama burst hypothesis WEAKENED
  for 14:22L (normal POST cadence).
- RATIFICATION SEMANTICS (c221): amend-then-swap beats
  ratify-then-correct when the falsity is known at swap time.

## c227 (03:05-03:35 UTC): REBOOT WINDOW SHIFTED TO UTC -- watch (c) CLOSED

- Fleet uptime census caught an on-the-hour reboot staircase (.101
  01:00:09Z, .102 02:00:08Z, .103 03:00:08Z). On-camera crontab read
  (all 8 cams): staggered nightly `reboot -f` at 1:00-8:00. With
  cameras now NTP-synced to UTC (camera-clock-race fix), busybox
  crond fires those hours as UTC = 22:00-05:00 local. c24's "01-08
  local" reading was anchored to unsynced camera clocks -- stale.
- CONSEQUENCES: rolling reboot window moved 3h earlier (22:00-05:00
  local, overlapping the e3 evening block); .101 reboot-cause watch
  CLOSED (cron, not hardware/power); .103's 03:00Z reboot caught
  live. The 14:22L stall class is unaffected (daytime).
- Doc: knowledge/aria/reboot-window-utc-shift-2026-09-12.md;
  camera-clock-race doc amended with the addendum.
- LAWS: a fix that changes a clock changes every schedule that reads
  that clock. A cron schedule has no timezone of its own. A
  staircase of on-the-hour boots across sibling devices is a
  signature of staggered schedules, not a spreading fault.

## c226 (02:35-04:10 UTC): STALL ATTRIBUTION + HOUSE NETWORK MAP + 10M LINK

- 14:22L burst = 3-phase cascade: 5 established RTSP streams silent
  within 6s (read-timeouts 14:22:39-45L), go2rtc wedged (404
  DESCRIBEs) + 7-cam watchdog storm 14:33-14:54L, recovery. go2rtc
  EXONERATED as cause (amplifier only). .104 chronic re-attributed
  camera-side-first (RSSI flat in storm windows). .201 dial-anomaly
  = RTSP listener not accepting SYNs; prudynt configs identical
  fleet-wide. .201 deauth times re-derived from camera uptime:
  14:29:55L + 14:38:12L.
- HOUSE NETWORK MAPPED (first complete map): .1 = TP-Link ER605 V2
  ROUTER; .2 = Mercury ISP CPE (locked, 403); .55 = Omada BE230 AP
  (BSSID 72:7f:f0:1e:4a:a8 = the AP .201 deauthed from). BE230
  serves 6 cams (.101/.102/.105/.201/.202/.203), Mercury serves
  .103/.104. The 14:22L burst hit BOTH APs = never single-AP.
- THE 10M FINDING: sophon enp10s0 gigabit-capable but LINKED AT
  10 Mb/s (partner-negotiated down, bad cable pairs likely). ALL
  camera RTSP rides it. Night ~1 Mbps fine; daytime higher; both
  big stalls were DAYTIME. 10M saturation = leading stall
  mechanism. Falsification instrument LIVE: nic-sampler (1-min
  cron, speed+rx+tx, /var/lib/aria-fleet/nic/, 7-day ring). Fix =
  Nacho's (relay 0045: cable/port + ER605 log access + cron
  visibility).
- Docs: knowledge/aria/stall-1422-go2rtc-exonerated-2026-09-12.md,
  knowledge/aria/house-network-map-2026-09-12.md