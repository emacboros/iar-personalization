#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-12 ~01:20 UTC (cycle 222)

## NOW

- c222 (01:00-): 0042 ROOT PATTERN SOLVED -- the recurring actor is
  our own cycles. Evidence: (1) rate match, 100-200 root-ssh/hour =
  cycle pulse+instrumentation cadence (c211 sampled: 58 ssh calls =
  68 sessions); (2) ORIG_HEAD 19:05:16L mtime = call_62ptytue, MY
  c212, 22:05:16Z `git fetch sophon-bare` as root -- second-exact
  match; 14 git-over-ssh-as-root calls by aria yesterday, continuo
  ZERO. Relay 0042 updated (4th update). Remaining for Nacho: only
  the original yoga-side sessions (16:58-17:01L, actual poisoner).
  NEW STANDING RULE (mine, effective now): NO git over ssh as root on
  the nacho-owned sophon checkout. Use `machinectl shell nacho@.host`
  (verified this cycle: git status clean as nacho). The old recipe
  (sophon-bare as working transport via root ssh) is RETIRED for
  read/verify calls; push path unchanged (pushes go to sophon-bare
  from MY container, not on sophon).
  NEXT CYCLES (in order):
  (1) post-reboot seam read: IF the reboot window has started
      (01:00-06:00 -03 = 04:00-09:00Z), run
      `ssh root@10.66.0.5 bash /tmp/seam-check.sh` -- ONE command.
      Persisted log -> CLOCKJUMP+SEAM no gap; tmpfs-wiped -> GAP +
      fresh uptimes. If seams found, verify camera-side first line
      same cycle (perishable). sophon itself did NOT reboot (up 10d).
  (2) storm watch: next ext4-storm window -> pull .104 series FIRST
      (perishable), correlate vs c213 baseline.
  (3) e3 failing-fixture watch: block-strength trend over weeks
      (one summary-API call; method in the longitudinal doc).
  (4) DIGEST DIET: world-state block grew 915 -> ~2900 in the
      ratified proposal; next maintenance should compress it (the
      injection-math law says operational state lives in ROADMAP.org).
  (5) DONE c223: Nocturne doubled-path FIXED. Root cause was NOT the
      one-shot state path -- it is project resolution: audit layout is
      audit/<project>/<agent>/ everywhere in elisp (moto/bessie,
      librarian precedent); nocturne has projects/nocturne.org ->
      project=nocturne -> audit/nocturne/nocturne/, while iar.sh
      hardcoded audit/iar/<agent>/ for wrapper artifacts. Fix: iar.sh
      now writes audit/${PROJECT_NAME}/<agent>/ (1ce92d4, pushed);
      wrapper v2 gate state at audit/nocturne/nocturne/
      LAST-DIGESTED-HEAD with auto-migration (bebfda94); artifacts
      migrated; forensics doc + philosophy.md carry the lesson
      (layouts that agree by default are not layouts that agree by
      construction). Next nocturne run digests 25c992b6..HEAD and
      lands unified.
## STANDING (verified, still true)

- phantom-exit fix baf05d0; rage organ v1.8; fence panel 0 true
  production rejections; dashboard v2.1/v2.2 live; fleet-check
  v2.20; cycle limits: turns 120 / soft 300 / fence 192k / wall
  3600s; eye-feed LIVE daily 09:30 -03; aria-0026 ratified.
- ZULIP READ PATH: /api/v1/messages + basic auth, string pair.
  Recipe: knowledge/aria/zulip-read-path.md.
- RSSI SERIES LIVE + DURABLE: per-camera cron (c202) + sophon
  puller (c209). Data: /var/lib/aria-fleet/rssi/<ip>.log. Doc:
  knowledge/aria/rssi-puller-live-2026-09-11.md. Seam scanner:
  knowledge/aria/bin/seam-check.sh (c216; sophon copy /tmp/seam-check.sh).
- CAMERA WATCH: 8/8 recording ~5.0-5.1 fps (c215 primary). .103
  network-dead chronic but still pulled (relay 0038). Frigate stats
  recipe: machinectl shell nacho@ podman exec frigate wget
  (rootless store).
- FULL-CAPTURE FLAG: standing tool, default nil. Arm = flip
  defcustom t (emacs.d/configs/tool-limits.el ~line 303), one
  cycle, verify REQ-*.json, revert. Verified live c212/c213.
- Relay open: 0042 (root pattern SOLVED c222 -- yoga-side actor
  question remains for Nacho), 0043 (puller visibility). Verify
  against answered/ before citing older ids.
- BIKE LEDGER OPEN: KLR650 presumptive, license next week. GO2 ~2wk.
- Burn asymmetry STABLE 6-8x full-day. knowledge/aria/
  burn-asymmetry-2026-09-11.md. Nocturne nightly cost now a line
  item (8.5M in / 231k out for the daily pass).
- DIGEST GUARDS: warn 12000 / hard cap 16000 (iar-prompt-assembly.el
  ~147-154). Live digest 15.6k after ratification (over warn) --
  diet next maintenance.
- NOCTURNE GATE: LAST-DIGESTED-HEAD = 25c992b6; i.ar HEAD = fa0a000
  (2 commits ahead) -- her next run digests the c219/c220 fixes.

## RECIPES (hard-won, reuse them)

- SOPHON CHECKOUT GIT (c222 RULE): never `ssh root@10.66.0.5 'cd
  /var/home/nacho/repos/i.ar && git ...'` -- root git refreshes the
  index as root and re-poisons it (c207/c212 chain, now proven
  self-inflicted). Read/verify: `machinectl shell nacho@.host
  /bin/bash -c "cd /var/home/nacho/repos/i.ar && git ..."` (verified
  c222). Push to sophon-bare happens from MY container, not sophon.
- thingino web API: POST /x/login.cgi {thingino:thingino} -> cookie
  -> GET /x/run.cgi?cmd=<base64>. Response ECHOES the command as
  HTML -- grep output on data pattern, never head-check line 1.
- sophon-side file edits: python3 heredoc, NOT sed with escaped
  pipes (sed-over-ssh quoting burned calls in c207 AND c209).
- python heredocs that WRITE ELISP: backslashes do not survive
  naive heredocs -- use a python file (write_file) with asserted
  anchors, or check the written bytes after.
- scp-then-run for multi-line python over ssh (c207).
- Frigate stats (c211): root podman can't see nacho's rootless
  containers. machinectl shell nacho@ /bin/sh -c "podman exec
  frigate wget -qO- http://127.0.0.1:5000/api/stats". Host 8971
  401s (nginx auth on); container-internal 5000 is the path.
- rootless podman from root ssh: machinectl shell nacho@.host
  /bin/podman ... (runuser -u fails: XDG_RUNTIME_DIR; direct
  podman as root sees only root's store).
- REQUESTS.log parsing: lines are "[TS] JSON-or-PARSE-text"; use
  the regex ^\[(\d{4}...)\] and search the REST. grep for a string
  you searched before = self-echo trap (msgs law). Distinct tool
  calls need call_id extraction (python regex on
  "id":"call_*","function":...), NOT line counts -- context replay
  echoes each call once per subsequent request (c222 scar).
- Delegate results are ELIDED in REQUESTS.log ([+N chars]) -- to
  read a reviewer verdict, arm full-capture or read the delegate
  sub-agent's own REQUESTS.log if it has one.
- sophon i.ar checkout remotes: origin (git@10.66.0.1) fetch 401s
  from there (publickey); sophon-bare is the working transport for
  PUSHES (from my container). Checkout freshness: machinectl (see
  c222 rule above), never root-ssh git.
- Timezone discipline: sophon journal = LOCAL -03; my records = UTC.
  Normalize before comparing (c186 law). c220 addendum: FILE MTIMES
  on sophon are LOCAL too. c222 addendum: journalctl --utc interprets
  --since/--until in LOCAL and prints UTC -- always pass full
  ISO dates with --utc or you will silently shift the window (this
  burned ~6 calls this cycle). Verify TZ before timestamp-matching
  ANYTHING (law 50, extended three times now).
- gptel fork in THIS container: /root/.emacs.d/gptel-fork (the
  /root/i.ar/emacs.d/gptel-fork path is an empty dir -- stale).
- Zulip post key: aria-cycle.conf is an INI (key = line 4), not
  "email:key" on one line -- parse with grep '^key' | sed.
- Nocturne forensics: wrapper log /var/log/nocturne-digest.log has
  ANSI codes + preflight noise -- strip with python re before
  grepping; the [INF] blocks are the wrapper's real output. One-shot
  log: audit/iar/nocturne/oneshot-*.log. Nocturne's own audit tree:
  audit/nocturne/nocturne/ (note: NOT audit/iar/nocturne/ for
  REQUESTS/USAGE -- the doubled audit path is a separate finding).
- ATTRIBUTION DISCIPLINE (c220, extended c222): before attributing an
  artifact to an actor, verify (a) the timestamp's TIMEZONE, (b) the
  file's CONTENT against the attributed actor's lineage, (c)
  coincidence windows against KNOWN activity, and (d) YOUR OWN
  INSTRUMENTATION FIRST -- the cycles' own ssh/git calls are the
  most probable actor for any recurring pattern on sophon (we are
  the loudest thing in the house). Posit an intruder only after the
  self-census is complete.
- RATIFICATION SEMANTICS (c221): the fence governs HER writes
  (DIGEST.proposed.md only); my acceptance criteria are mine.
  Ratifying WITH amendments is still reading + judging. A digest
  that says something I know is false fails the honesty law --
  amend-then-swap beats ratify-then-correct when the falsity is
  known at swap time.