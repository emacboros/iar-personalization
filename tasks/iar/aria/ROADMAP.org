#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~11:22 UTC (cycle 314 CLOSE: STALE-RECEIPT
DETECTOR BUILT + VALIDATED (09b9aaef): knowledge/aria/bin/
stale-receipt-detector.sh v4 -- shingle census + receipt ladder
(STRONG/REQ/WEAK/NONE/NOLOG) + automated borrowed-check against
sibling logs. Run on continuo: flags the msgs=401 claims, names aria
cycle-2026-09-11.log as borrowed source -- the c306-c312 manual
conclusion reproduces mechanically. Run on aria: 2 candidates
(loop-guard budget notes), receipts WEAK, fires were real. Build
note: knowledge/aria/stale-receipt-detector-build-c314.md. ECHO DOC
CLOSE-OUT landed (3d7ecdd8). Relay 0065 addendum (nocturne urgency,
4cf9f75d). Fleet-check FAIL=0 manual re-run (affect fear sev=2 at
10:00Z was the known camera faults; superseded a9dca5ce). ARP logger
re-armed 61200s (/tmp/arp-reqs-0914d.txt, expires ~00:43 local
09-15). NEW SCAR (c314): tail-chasing the live REQUESTS.log at close
-- ~25 add-commit-push rounds on a file that grows one line per
request; msgs=401 soft-cap warning fired and was CORRECT. Law
candidate: a file that grows per-request is not a commit target at
close -- commit once with the close batch, note rotations, leave the
live tail to the next belt. REQUESTS.log ROTATED mid-cycle 11:14Z
(5481 lines -> .1, content preserved: 4f555b19 + git 1f78e29c~1).
c313's law-50 corollary stands: a tail sample has no currency of its
own -- verify against the instrument's FAILURE log.)

## c315 QUEUE (next cycle)
1. NOCTURNE GATE WATCH at 16:04Z (13:04 local): verify gate advanced
   past e4d0832d, read DIGEST.proposed.md if new (ratify-at-wake,
   D-015). If 429 again -> quota structural; relay 0065 amendment
   (quota now blocks nocturne consolidation too).
2. SWEEP-SHAPE 2 FALSIFIER: after tonight's .101 boot (~00:50-01:00Z
   Sep 15), read /tmp/arp-reqs-0914d.txt (armed, enp10s0, filter
   `arp and (ether src 76:e3:1a:69:e3:9c or host 192.168.2.58)`,
   -e, 61200s from 07:43 sophon-local 09-14, expires ~00:43 local
   09-15 -- covers the window). Falsifier: .58 ARP to .101 at
   01:00:28-31Z = boot+1s probe mechanism (b) real; quiet = (a)/(c).
   Then ~22:15 sophon-local check .101 camlog for boot+1s burst.
3. THINKING-LOOP WATCH (standing): 1 mid-cycle 09-14. If >5/day at
   full volume = nemotron-intrinsic -> relay 0065 amendment.
4. AEVUM PULSE #5 (due ~Sep 21, weekly-only): the ratchet watch.
5. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (BOTH .102+.104 power-dead, corrected), 0064 (RTSP probes,
   folds into 0059), 0065 (quota decision -- URGENT: blocks nocturne),
   0042/0045/0055/0057/0059 (older). All open, none actionable by me.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291/c301): cameras.log/camlogs = UTC;
  frigate journalctl --since = LOCAL on sophon; nginx log inside
  container = LOCAL (-0300); go2rtc container log = LOCAL;
  REQUESTS.log UTC; sophon journal = LOCAL; DATED CYCLE LOGS =
  SOPHON-LOCAL. Normalize per-source before ANY gap arithmetic.
- CAMLOG SNAPSHOT LAW (c302/c303): thingino camlog pulls are
  snapshot-anchored; boot resets camera clock to factory until NTP;
  RING-RESIDUE LAW: dedupe camlog counts by (timestamp, PID).
- CENSUS-SOURCE MAP (c297): viewer traffic = nginx log ONLY;
  RTSP-port probes = go2rtc log ONLY; producer dials = go2rtc WRN +
  camera prudynt; frigate remakes = frigate container log; camera-
  side events = cameras.log; sophon journal for aria-cycle = Sep 11+
  ONLY; REQUESTS.log self-echo: strict filter = PARSE + error=nil +
  END-ANCHORED tokens_out=32768 msgs=N$. CENSUS-DAY LAW (c305).
  CLAIM-ANCHOR LAW (c307): claim censuses anchor to record
  STRUCTURE, not substrings. REQLOG-RACE LAW (c308): during
  delegation windows, per-agent REQUESTS.log boundaries are
  unreliable -- dedupe by REQ id across ALL agent logs (HISTORICAL
  logs only; fix-3 792de9a fixes FUTURE attribution). CENSUS-SELF-
  ECHO LAW (c309): an investigation's own greps write the search
  string into its own request-log tails -- filter census matches by
  epoch AND by structural position. TRUNCATION-LAYER LAW (c310):
  the request-log's tail view ([+N chars]) is NOT the agent's
  context; context truncation = iar-tool-result-max-chars (10k,
  middle-truncation). Verify WHICH layer truncated before
  concluding an agent "never saw" content.
- BORROWED-RECEIPT LAW v4 (c306-c312, CLOSED): (1) a journal entry
  may claim only what THIS cycle's log shows happened TO THIS AGENT;
  (2) annotation in the reader's path decays paraphrase but does not
  stop template re-emission; (3) a PEER NOTE (addressed,
  sibling-to-sibling, on the social channel) stops template
  re-emission where annotation failed. Echo closed c312 (correction
  6). DETECTOR (c314): the watch is now automated --
  knowledge/aria/bin/stale-receipt-detector.sh (run at pulses).
- PULLER TESTIMONY (c313): /var/lib/aria-fleet/*/puller.log lines
  ("pull failed, keeping last good", "login failed") are the
  CURRENCY PROOF for every data tail under /var/lib/aria-fleet/.
  Read the puller log BEFORE reading any data tail.
- STALE-RECEIPT DETECTOR (c314): shingle census + receipt ladder +
  borrowed-check. Run: bash knowledge/aria/bin/stale-receipt-detector.sh
  <aria|continuo>. WEAK receipts need human resolution; pre-reqlog
  days yield NONE for possibly-true claims (reports RECEIPT, not
  truth). NOT a fence.
- LIVE-TAIL COMMIT LAW (c314, new): a file that grows per-request
  (REQUESTS.log) is NOT a commit target at close. Commit once with
  the close batch; note rotations; leave the live tail to the next
  cycle's belt. The msgs=401 warning fired on this exact pattern.
- EXT3 CRASH-LOOP CLASS (c294/c311): -38 filter-init signature,
  20s restart cadence, heals on ANY session remake. Watch for
  recurrences; self-healing expected, alert only if >2h.
- AGORA-AGENT HEARTBEAT (c312): alive: lines every ~8min; check the
  CADENCE before calling a heartbeat stale.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh.
- ARP LOGGER (c290/c302/c312/c314): arm with -e ON enp10s0 (the "any"
  device REJECTS -e ether-address filters). Current arm:
  /tmp/arp-reqs-0914d.txt, 61200s from 07:43 sophon-local 09-14
  (expires ~00:43 local 09-15). /tmp on sophon survives only until
  reboot or timeout expiry.
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our
  keys; ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY HOP.
- GIT HYGIENE: JOURNAL.org/HISTORY.log need git add -f (audit/iar
  gitignored). TASKS gitignored: new task files need git add -f.
  create_task paths are relative to tasks/iar/aria/ for cycle agents.
  GIT-COMMIT GUARD GAP (c308): refuse-pattern only matches cycle.log;
  check git show --stat before pushing; untrack strays AND delete
  disk copies (c311 digest-twin lesson).
- PULL-AND-PROCESS LOCALLY (c302): sophon /tmp files invisible to
  the container -- scp to local /tmp first.
- NGINX RING PULL (c302): ssh 'su - nacho -c "podman exec frigate
  sh -c \"cat /dev/shm/logs/nginx/current\""' > local file.
- AEVUM SSH PATH (c304): container key aria@i.ar direct to
  fedora@54.38.46.192 -- no sophon hop. Host key TOFU to /tmp.
- REQUESTS.log ROTATION (c309/c314): old-cycle fence fires live in
  the DATED CYCLE LOGS, not request logs. Cycle logs outlive request
  logs. Rotation commits look like mass deletions (--stat first).
- START-TAIL VIEW (c310): REQUESTS.log START tails are TRUNCATED
  VIEWS (~1400 chars/msg), not full context.
- DELEGATE DEPTH GUARD (c308) + FIX-2 DRAIN (c312, 2f7c82c): all
  three cascade fixes CLOSED.
- CAMERA OUTAGE (c313): .104 power-dead since 09-12 11:00Z; .102
  power-dead since ~02:00Z 09-14 (rssi tail was last-breath, not
  recovery -- relay 0063 corrected 8004d6fb). Both need Nacho.

## THREADS (open, no obligation)
- SWEEP-SHAPE 2 FALSIFIER (c302/c303): tonight's .101 boot -- logger
  armed through 09-15 (queue item 2).
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290 seed): build the guard if a third
  transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Aevum: weekly-only pulse -- #5 due ~Sep 21.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract + home-grown vector + c309/c310
  corrections -- echo portion now CLOSED via correction 6),
  0057 (prompt edit ratify), 0059 (frigate internet exposure --
  scanner #3 evidence 09-14), 0060 (issue draft ratify + falsifier),
  0062 (ONVIF sweep device/app ID), 0063 (BOTH .102+.104
  power-dead, corrected c313), 0064 (RTSP probes), 0065 (ollama
  quota decision -- URGENT: blocks nocturne consolidation) -- all
  human-needed.