#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-06 19:35 UTC (cycle 7: FAILURE-FIRST = continuo turn-235 404 forensics DONE (mechanism hypothesis + natural experiment queued); NEW DO-FIRST: failure-hook CYCLE_TAG regression -- notify channel MUTE since 4c2df1a)

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
  LAST-CYCLE: "cycle 1 ok" (aria). SIBLING: continuo turn 235 FAILED
  (404 north-mini) -- forensics done c7, natural experiment = turn 237.
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Request count is THE lever.
  Calibration week running (reset ~2026-09-07 03:00 UTC).

* DO FIRST (next cycle, in order)
1. FAILURE-FIRST + pulse + pull (standing). Check continuo turn 237:
   green => 404 mechanism CONFIRMED (:models-list fallback); 404 again
   => env path suspect, re-probe.
2. FIX FAILURE-HOOK CYCLE_TAG REGRESSION (URGENT, channel is MUTE):
   /usr/local/bin/agent-failure-notify.sh line 78 uses ${CYCLE_TAG},
   line 88 defines it AFTER -- set -u kills every send since commit
   4c2df1a (telegram cycle numbers, resume session). sophon journal
   16:22:56 -03: 'CYCLE_TAG: unbound variable' exit 1. Queue
   accumulates, Nacho heard NOTHING about the continuo failure.
   Fix = move CYCLE_NUM/CYCLE_TAG block BEFORE MSG compose (one-line
   reorder), differential test (synthetic fire -> TG received ->
   queue reset), commit repo copy + installed copy. Also note: queue
   entry recorded 'unknown exit 0 (success)' -- systemctl show after
   unit restart captures restart state, not failure state; snapshot
   unit props BEFORE restart if cheap.
3. Then: interim organ-wake retirement (c6 tail), soft-cap 60/100.

* DONE (cycles 2-7, 2026-09-06)
- c7 FAILURE-FIRST (sibling's failure): continuo turn 235 (18:50:40
  UTC) exit 1, 1934s, 2 requests both 404 'north-mini-code-1.0:q8_0',
  0 turns. FORENSICS: rotate.sh mapping + deepseek pull + msg 440 =
  Nacho interactive session 18:32-18:39 UTC (sophon journal: root ssh
  from yoga 10.66.0.4 w/ aria key 4BApz at 18:32:37 = rotate.sh
  mtime). Container env PROVEN deepseek (journal _CMDLINE). HTTP
  request PROVEN north-mini (continuo REQUESTS.log). gptel.el lacked
  deepseek in :models until 6c6b6d2 (18:52:27 UTC, 2 min after cycle
  start). north-mini = FIRST entry of pre-6c6b6d2 list, stale since
  refactor 832313e, exists on NO ollama server. Local repro: fork
  sends gptel-model verbatim, NO :models validation (gptel-ollama.el
  :170). HYPOTHESIS: ELPA gptel validates + falls back to first
  entry. Natural experiment = continuo turn 237 (deepseek now in
  list). ALSO FOUND: failed request does NOT abort cycle -- 2 strikes
  counted but event loop sat full 1800s timeout (30 min wall per bad
  model name); worth a timeout-on-failed-request look after fix.
- c6: AFFECT HOST TIMERS LIVE (queue #1). fear hourly + boredom daily
  on sophon, differential test PASSED incl. TG mirror via env bridge
  (/etc/aria-affect.env). Verified live c7: fear fired 16:01 -03,
  boredom armed 14:01 -03, asof fresh. Commits 08e997a/e585519/
  899667e.
- c6 TG var-name mismatch FOUND+FIXED. Digest diet LANDED (9956 bytes).
- c5: fear organ EXERCISED (first full arc). c4: organ contract
  restored + THREADS scars repaired. c3: timer-spec pre-audit.

* QUEUE (approved 2026-09-04, in order)
1. (DONE c6) Affect host timers. Tail: interim organ-wake retirement
   (DO FIRST #3).
2. Soft-cap 60/100 (inject note at 60, hard stop 100).
3. Commit-as-nacho durable fix.
4. Linger all hosts (loginctl enable-linger).
5. Sidecar sshd (execute_code_remote via podman DNS; NO socket bridge).
6. Restic 4-part fix. Ansible-owned, careful, snapshot-verify after.
7. GPU-detect VRAM check (3080 shared with ollama).

* THREADS (open, not schedule)
- COMPOSITION (live): continuo on deepseek-v4-flash from turn 235.
  First cycle failed (404, see c7). Natural experiment next turn.
  No per-cycle metrics; quality is a longitudinal read.
- 404-cost finding: failed request burns full 1800s timeout (c7).
  Candidate: abort-on-known-fatal (404) or shorter first-strike
  timeout. File after hook fix.
- GO2: ~2 weeks out. Verdict GO stands (id 287).
- valence v2: curiosity + rage organs, joy v3 (after host timers).
- Weekly digest #1 + Aevum pulse: Sep 9 (weekly-only, pulse-only).
- stub-detector sweep; watchdog inventory; enoch steal-list
  (self.json, evidence journals); substrate experiment.
- Cloud-redundancy drill: DEFERRED by Nacho.
- Inverted session #3: end of calibration week.
- Zulip pg_dump backup (continuo's, Nacho's call); fedora@ sophon
  ssh auth (continuo c42).
- Digest-twin verifier: census + spec done (continuo c62/c64), build
  = one cycle, after hook fix.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- ORGAN WAKE (interim): RETIRE next cycle (timers verified live c7).
  KEEP the AFFECT-LINE AGE pulse check (asof vs wake time).
- AFFECT TIMERS patrol: list-timers shows both; fear.log/boredom.log
  emissions on delta.
- fleet-check v2.14 ON SOPHON (ssh 'bash -s' <, timeout >=300s).
- RESTIC: backup + integrity check BOTH green (Sep 6). Frigate
  storage backup gap: queued (4-part fix).
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Verify which storage/context a service runs in BEFORE probing.
- Loop guard = instrument: when it fires, say why, then batch or
  change approach. Blocked posts = script file + one execute.
- No infra changes from cycles beyond the approved queue; diagnose,
  file, move on. Push-path hygiene = Nacho's.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands.
- DIGEST LAW: world-state blocks are REPLACED, never appended.
  Check file size before and after any DIGEST edit.
- FOR-NACHO.md RETIRED; with-nacho + for-nacho streams primary.
- FAILURE-FIRST IS HOUSE-WIDE: sibling's failure = the house's
  failure (c7 precedent). Root-cause it before your own queue.

* FAILURE MODES (scars, newest first)
37. A notify channel that dies on its own feature commit is mute
    exactly when it is needed -- test the failure path AFTER every
    change to the failure path itself. (c7: CYCLE_TAG regression.)
36. A heal that runs as the wrong user heals nothing while logging
    success. Verify heals from the OUTSIDE: plant poison, fire, inspect.
35. Chown is not relabel. Fixing ownership but not SELinux labels
    moves the failure one layer down.
34. The escape hatch can route back into the trap. The guard counts
    tool NAMES, not questions.
33. Component-verified is not system-verified.
32. Two data points make a line, never a mechanism.
31. An instrument's unhandled output shape is a hole in its exit code.
30. Three records disagree about a version -> the instrument's own
    header lies least -- but patch it first.
29. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version.
28. A remote probe needing >1 attempt -> promote to script file.
27. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT.
26. A diet without a pressure instrument regrows.
25. The law you just wrote is the one you're about to break.
24. A stable transcript means a stable WRITER or a DEAD one.
23. Rootless podman needs user@1000.service + linger.
22. Watchdog tuned for fast models kills slow ones.
21. podman run -d inside systemd: restart-loop before first breath.
20. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
19. A fix that works in isolation can stall in the live system.
18. Root-run git on nacho-owned repos poisons the tripwire.
17. Curiosity that doesn't check priors against the human runs blind.
16. Instruments have reliability tiers per sub-reading.
15. Response without local service logging it -> second stack, re-probe.
14. Artifact confabulation: external-action entries cite tool evidence.
13. In async plumbing, capture context when it exists.
12. Anomalous behavior in multi-agent env has a third cause.
11. Before diagnosing the listener, check who the speaker is.
10. Differential testing: same input through old and new code.
9. The venv python must be passed as SYMLINK, not resolved path.
8. Existence is not function: test the claim.
7. pkill -f matches my own ssh command line; use -u <user> -f.
6. Malformed tool calls are invisible from inside.
5. Timeout wrappers on EVERY remote/ssh command.
4. Silent error swallowing; condition-case in parse loops.
3. Narrative completion: check primary evidence before attributing.
2. (retired into 36)
1. (retired into 36)