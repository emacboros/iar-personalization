#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-13 ~00:33 UTC (cycle 272: upstream issue draft
LANDED (do-not-post, relay 0060), git-commit-tool guard spec filed,
fix option (a) confirmed costless for the frontend)

## c273 QUEUE (in order)
1. GIT-COMMIT-TOOL GUARD (build, spec filed at
   tasks/iar/aria/git-commit-tool-guard/description.org): refuse
   staging audit/*/cycle.log in git_commit.el -- the c270 resurrection
   vector. Own .el work, ert suite green before push, update
   docs/iar/tools.md.
2. RELAY WATCH: 0060 (issue draft ratify) -- if Nacho rules, post the
   draft to AlexxIT/go2rtc (or he posts; his call). Do not post
   without his eyes.
3. CLEAN FALSIFIER (needs Nacho's OK for the single-cam GET -- it
   remakes one session): single-cam streams GET outside wave windows
   -> expect single-cam fps-limit ~20s later. Fold into relay 0060's
   answer if convenient.
4. SOLO-DEATH CLASS: watch for sample #3; census method = bad-cseq
   splices in frigate ffmpeg tails, not WRNs in go2rtc logs (c269).

## c272 addendum (2026-09-13 ~00:33 UTC)
- UPSTREAM ISSUE DRAFT LANDED: knowledge/aria/go2rtc-issue-draft-
  2026-09-13.md (DO-NOT-POST). Mechanism verified against source at
  v1.9.10 + v1.9.14 + master: ALL wave-path files byte-identical
  (pkg/rtsp/producer.go, pkg/rtsp/consumer.go, pkg/core/connection.go,
  pkg/core/media.go, pkg/core/codec.go, internal/streams/add_consumer.go,
  pkg/probe/consumer.go). The bug is present at every version we could
  run. One-line asymmetry: GetTrack dedups receivers by pointer
  equality; AddTrack has NO dedup over Senders -> any mic-probe match
  on sendonly audio while StatePlay = unconditional Reconnect(),
  never idempotent.
- FIX OPTION (a) CONFIRMED COSTLESS: frigate frontend (v0.17.2 source,
  web/src/hooks/use-deferred-stream-metadata.ts + use-camera-live-mode.ts
  + utils/cameraUtil.ts detectCameraAudioFeatures) consumes ONLY
  producer medias (audio sendonly = two-way audio, recvonly = audio
  output). A paramless GET returns the same producer medias WITHOUT
  AddConsumer (api.go else-branch). Dropping "microphone" from
  frigate's proxy params loses the UI nothing. This closes the fix
  options: (a) is smallest AND complete.
- RELAY 0060 FILED (nacho-external): ratify the draft before posting.
  Includes the frontend finding + fix-(a) confirmation.
- GUARD SPEC FILED: tasks/iar/aria/git-commit-tool-guard/description.org
  (WHY = c270 resurrection anatomy; WHAT = unstage gitignored
  newly-added files after add -A; tests specified).
- LAW CANDIDATE (c271) FIRST LIVE USE: mid-cycle I ran one live
  with-mic GET against exterior_1, then started looking for the
  reconnect it should have caused (camlog puller 15min stale, go2rtc
  WRN-only logs = no visible signal). Caught myself, asked "what would
  the answer cost me?" -- nothing, c268's fleet evidence already
  proves the reconnect. Stopped. The walk did not happen.
- PULSE all green (services active, tripwire 0, disk 30%, twin FAIL=0).
- Commits: d92a1046 (draft + relay 0060), 80dcdd6f (guard spec),
  89c6195b (journal/history). All pushed.

## c271 addendum (2026-09-13 ~00:13 UTC)
- RELAY 0057 CORRECTED: pulse-template census stands at 4 literals
  (journal lines 158/280/378/382: 3 bare + 1 bash-c); ZERO new
  recurrences since c257; c260's "#6/#7" claim WITHDRAWN (misreading).
  Ask unchanged (worked-example edit to continuo_daily.org).
- NOCTURNE VERIFIED LIVE end-to-end: daily pass 19:22-19:41Z on
  sophon (exit 0, 118 calls, 11.4M tok in), gate e4d0832d, next fire
  13:01 -03. Weekly pass not yet exercised -- watch for it.
- SCAR (4th sighting of c259/c269 pattern): ~50 calls of git -S/-G
  archaeology on my own uncommitted LAST-CYCLE.txt. New texture: the
  walk happens when the answer is EMBARRASSING. Law candidate: when a
  guard fires on an enumeration, ask "what would the answer cost
  me?" -- if "nothing, the state is fine," the walk is avoidance.
- MSGS FENCE watch: 401/401/403 three cycles running. c272 came in
  at ~c140 with no fence -- the draft-first discipline worked.

## STANDING (stable facts; do not re-derive)

- PULSE: run in ONE batched ssh (keyscan re-pin first). Fleet
  pullers: rssi/ camlog/ nic/ under /var/lib/aria-fleet/ (sophon
  cron 15min/15min/1min). fleet-check v2.21 via aria-fleet-feed.timer
  (6h) -> fleet-latest -> fear-organ. Run by hand when stale.
- FRIGATE ACCESS: ssh root@10.66.0.5 then 'su - nacho -c
  "XDG_RUNTIME_DIR=/run/user/1000 podman exec frigate <cmd>"'.
  Frigate version deployed: 0.17.2-3d4dd3a (stable-tensorrt image).
- CLOCK SHAPES: cameras run UTC; frigate logs LOCAL (-03); frigate
  nginx access log timestamps are LOCAL with [12/Sep/2026:...] format;
  prudynt camera logs UTC. Convert before correlating.
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext cams) / 156-band (int cams) /
  1 pkt stub (permanent) / 0 pkts stream-absent (transient) / EMPTY
  output = audio track absent (video-only segment).
- GO2RTC (c252/c265): /api/streams via podman exec curl
  localhost:1984. Stream keys exterior_N/interior_N. PUT reload =
  QUERY PARAMS (?name=X&src=Y), NOT a JSON body (c265 scar).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP -- this is why the
  mic probe reconnects ALL of them. ext4 (power-dead) has no producers.
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
- Git: sophon bare /home/git/repos/; push as the cycle user works
  (root-push pollution = structural fix Nacho's).
- BELT DISCIPLINE: pull first, commit continuo's uncommitted artifacts
  BEFORE my own writes (git add -f; audit/* is gitignored by default),
  push before starting new threads. My own close-out files are as
  easy to forget as my twin's -- the c271 scar.

## THREADS (open, no obligation)
- SOLO-DEATH CLASS: leading hypothesis = same splice, producer-side
  re-dial trigger (c269); sample #2 = int1 19:00:04 local.
- CLEAN FALSIFIER: single-cam GET outside wave windows (needs OK).
- WAVE DETECTOR SEED: nightly batch ffprobe census or same-second
  prudynt burst watcher; needs a sophon cron slot.
- MSGS-FENCE WATCH: 401/401/403 + c272 clean (~c140). Watch continues.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger defect: REQ IDs not unique (0055 twice, 0015 twice
  in answered/) -- needs a dedupe law or ID scheme fix.
- Git-commit-tool guard = c273 item 1 (spec filed).
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus ruling), 0057 (prompt edit ratify), 0059 (frigate
  internet exposure), 0060 (issue draft ratify) -- all human-needed.