#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-16 ~18:10 local (c372: EXT3 FREEZE MECHANISM
CONFIRMED END-TO-END -- freeze = window BETWEEN two go2rtc producer
reconnects; RTSP timeouts 14:39:32Z/15:05:18Z bracket the audio death
exactly; next natural timeout heals, no camera reboot needed. ext1
HEALED 12:54Z silent (0073 amended). interior_1 (.201) NEW TRANSIENT
SUB-CLASS: 15-25min self-healing deaths, NO timeout signature, 3x
today (02-04Z, 12-13Z, 17Z) -- detector blind at 6h cadence. c371
ext3-blind-window claim WITHDRAWN: my --since was a future window
(TZ error); onset window has 198k lines. FUTURE-DAY guard applies to
--since too. Silent heals are the norm: WRN fires only on active
reads; frozen receiver logs nothing.)

## c373 QUEUE (next cycle)
0. SEGMENT-CENSUS PULLER (detector gap, mine to build): hourly puller
   counting noaudio segments per camera (volumedetect probe, the
   c354-correct one). Makes transient deaths (interior_1) visible
   between 6h fleet runs. Read-only, lands in /var/lib/aria-fleet/.
   Consider: run inside fleet-feed timer (6h) or its own hourly timer.
1. NOCTURNE PASS WATCH: next fire Wed 09-17 13:00Z. With the heal
   drop-in live, expect a clean pass (or a NEW failure mode --
   watch the preheal logger line). Gate ~953 commits behind; cap
   300/pass => ~4 clean passes to catch up.
2. INTERIOR_1 TRANSIENT MECHANISM (open question): why does
   interior_1 self-heal while ext1/ext3 stay frozen? Receiver-state
   stall vs read-loop stall? The census puller (item 0) gives the
   data; correlate deaths with .201 RTSP timeout CEASED after 14:xxZ.
3. CLOCK-AUDIT FORK BATCHING (c371 finding): batch my probe pattern --
   fewer ssh calls per cycle (c372 ran ~140; each = 6-8 audit events).
4. JOURNAL-BLIND GUARD (c371 law, still valid): fleet-check
   journal-freshness should fail loudly when rsyslog is rate-limited
   (grep the "begin to drop" marker). Small build, my file.
5. CREATOR HUNT (standing, narrowed): witnesses armed. Copy-not-init
   leading hypothesis.
6. ROOT-GIT-STATUS POISON (0042): durable fix still open.
7. DAILY CENSUS (standing): req-census + 429 + commit-census.
8. DETECTOR WATCH: fleet-check v2.24 -- ext3 will read CLEARED at
   next run (correct: healed 15:05Z); ext4 power-dead (0063); restic
   ok; frigate ~900 events/24h.
9. QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z; 0 true 429s.
10. SUITE FAILURES (c368): 17 pre-existing (invisible-turn/
    ollama-stream/sanitize). Fix = separate thread.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook), history-clock
  (hook).
- TZ LAW SHARPENED (c359b, APPLIED-AND-BURNED c372): sophon JOURNAL
  timestamps are LOCAL -03; frigate recording HOUR-DIRS are UTC;
  frigate container logs are LOCAL. A --since/--until window is parsed
  in LOCAL time -- a window named in UTC digits can be FUTURE, and a
  future window is empty by construction. Reconcile the query window
  against the query clock BEFORE reading emptiness as blindness.
- LANDING-NOT-RECORD (c358): record BEFORE new threads when budget
  runs low.
- NIL-CONTENT LANDMINE (c358): stream chunk with no :content coerces
  to "" at parse layer -- (string-blank-p nil) kills the process
  filter. GUARDED (fork b85fb12).
- FUTURE-DAY GUARD (c358, EXTENDED c372): instruments that take a DATE
  -- or a TIME WINDOW -- refuse future values loud (exit 2); an empty
  verdict for an unhappened window is a fake-clean record by
  construction.
- CENSUS 3-ANCHOR LAW (c342): census = line-start regex +
  field-position status check + terminal-suffix extraction; merge
  log+log.1 deduped, PARSE-only. Baseline 09-14: aria 2630
  PARSE/2645 req/192.5M in; cont 1615/44.8M in.
- NUMBER-TOO-CLEAN LAW (c342): a number that fits your hypothesis too
  well (3h = exactly the Argentina offset; "journal blind exactly when
  I needed it" = c371) deserves re-derivation before it becomes a
  mechanism claim.
- AUDIT-ADJACENCY LAW (c370): audit CWD records are per-process
  events, not attributes of the next execve; pairing CWD-to-PROCTITLE
  by adjacency manufactures false pairs.
- JOURNAL-BLIND LAW (c371, NARROWED c372): an empty window under an
  active rate-limit is a fake-clean record by construction -- verify
  the rate-limit marker AND the window's reality (clock, timezone,
  future-ness) before trusting emptiness. The c371 ext3 claim was the
  future-window error, not rate-limit blindness; the flood itself
  (100k+/hour peak, 15 markers today) is real and 0074 stands.

## WORLD STATE (2026-09-16 ~18:10 UTC -- REPLACES all prior blocks)
- AUDIO CLASS FAMILY (3 sub-classes now):
  1. RECORDER-DEATH (c353): producer reconnect kills recorder audio
     track; heal = producer replacement. ext5/ext2 healed.
  2. PRODUCER-FREEZE (0073): go2rtc producer audio receiver frozen,
     video flowing; heal = producer replacement (next RTSP timeout or
     cron reboot). ext1 HEALED 12:54Z Sep 16 (silent, 12h late).
     ext3 froze 14:39-15:05Z Sep 16, SELF-HEALED via next timeout.
     MECHANISM CONFIRMED END-TO-END (see mechanism doc).
  3. TRANSIENT-FREEZE (c372 NEW): interior_1 (.201), 15-25min
     self-healing deaths, no timeout signature, ~every 5-10h, 3x
     today. Detector blind at 6h cadence. Census puller = fix.
  Micro-deaths (1-3 segments) appear across cameras; likely same
  mechanism at small scale.
- SILENT HEALS ARE THE NORM: producer replacement often logs NOTHING
  (WRN fires only on active read timeout). Absence of a reconnect
  event is not absence of a reconnect. Every heal attributed to a
  camera cron reboot carries this asterisk now.
- c371 WITHDRAWAL: "journal blind during ext3 onset" was a
  future-window TZ error; the window has 198k lines + 195 go2rtc
  lines. Journal-flood finding stands (rsyslog rate-limited 15x
  today, 100k+/hour peak from devnull-watch + my probes + yoga
  actor). 0074 stands.
- NOCTURNE: heal drop-in landed c371; next pass Wed 09-17 13:00Z.
  Gate ~953 commits behind; cap 300/pass => ~4 clean passes.
- FALSIFIERS: ext1 01:03Z heal FALSIFIED (healed 12:54Z instead);
  .101 boot+2s burst CONFIRMED c369. Next: Nocturne pass 13:00Z.
- BARE REPO HEALTH: 2 root-owned files in iar-personalization.git
  (commit-graph files, 14:26:29Z -- from MY root push c372; the
  post-receive hook heals ownership but these appeared after; mirror
  sync verified dde3b735 BEFORE my pushes, re-check next cycle).
- V4 GATE: two failed passes (09-15, 09-16); next pass Wed 09-17
  13:00Z (moved from 16:04Z per c371). RESERVOIR-DRAIN fired 09-15
  (unratified proposal archived, pass started clean).
- FLEET 17:29Z manual run: ext3 CLEARED (post-heal, correct);
  ext4 power-dead (0063); restic ok; frigate 901 events/24h;
  BARE OWNERSHIP warning (2 root-owned commit-graph files).
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s so far.
- CAMERA OUTAGE: .104 power-dead since 09-12 (0063, physical = Nacho).
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- SUITE FAILURES (c368): 17 pre-existing (invisible-turn x2,
  ollama-stream x8, sanitize x7). Fix = separate thread.
- FRIGATE EXPOSURE: scanner #3 (Google Cloud, 91 reqs) -- relay
  0059. Auth holding. WAVE TRIGGER fix CONFIRMED COSTLESS; issue
  DRAFTED, DO-NOT-POST (0060).

## THREADS (open, no obligation)
- INTERIOR_1 TRANSIENT-FREEZE (c372): why does it self-heal when
  ext1/ext3 don't? Correlate with .201 timeout cessation. Census
  puller first.
- NESTED-GIT CREATOR (c369->c370): narrowed. Copy-not-init leading
  hypothesis. Witnesses: ExecStartPre stat + first-fail.
- NOCTURNE RANGE-DEBT (c363->c371): ~953 commits past gate, cap
  300/pass. Heal drop-in live; catch-up ~4 passes if clean.
- CONTINUO THINKING-TRUNCATION (c366): 5-13% of her cycles; aria
  ZERO. Watch per-day fire counts; escalate >15%.
- SUITE FAILURES (c368): 17 pre-existing. Separate cycle.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean. Prompt-side
  fix (inject date(1) into cycle context) buildable under c366 grant.
- LOOP-GUARD RATE (c363): falling. Watch.
- AUDIO CLASS (c345->c372): 3 sub-classes; mechanism doc landed.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; no recurrence
  since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063, 0064, 0065, 0066, 0068 (mechanism
armed, policy open), 0069, 0073 (amended c372: ext1 closed, ext3
mechanism confirmed, interior_1 new sub-class, detector gap = mine),
0074 (journal flood -- devnull-watch rule scoping). All human-needed
or watch.