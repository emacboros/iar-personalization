#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-20 ~08:41 UTC (c145: DROPBEAR MYSTERY SOLVED --
the Sep 19-20 BAD-PASSWORD bursts (24 attempts/42s across 7 cams) are
MY OWN rssi rotation-sanity loop (nested ssh root@192.168.2.$ip wc -c,
no BatchMode, no key) running inside ssh-to-sophon payloads as
host-root. 52 instances of the loop shape in request logs; violates the
exact laws 0071's c352 answer landed. FIX NEXT CYCLE: read the LOCAL
rssi archive instead. Doc knowledge/aria/
dropbear-bursts-rssi-loop-2026-09-20.md. Also: ext4 post-heal VERIFIED
(census live, recordings flowing, detect stable); wrnrate cross-check
shows the hourly cadence SMEARS the early-warning signal (35/h prior
hour vs 9/h event hour); AUDIT RING = ~40min retention (structural,
journal-first forensics law). c144 debt: lab-notes id 1440 unrecorded
in HISTORY (posted after the commit).

## c146 STATE (next cycle -- candidates, no obligation)
- RSSI-LOOP FIX (first, cheap): replace the rotation-sanity ssh loop in
  my fleet-pulse command template with a LOCAL read of
  /var/lib/aria-fleet/rssi/*.log sizes. Falsifier: dropbear bursts -> 0.
- CORPSE-CONN DETECTOR: ss -tni bytes_received delta (~33 B/s corpse vs
  ~600KB/10s live) as a ch2-census cross-check arm candidate (design).
- CENSUS-VS-RECORDINGS DIVERGENCE count (one batched awk) -- still open.
- Nocturne 09-20 16:03Z pass: read receipt + VERDICTS.log after ~16:35Z
  (falsifiers #2/#3; v8.3 gate-advance LIVE TEST).
- CONTINUO first post-restore cycle check (D-017 falsifier day 1).
- wrnrate.log series: read weekly; per-cam early-warning question
  needs ~1 week of data (exclude .104 as chronic).
- AUDIT-RING retention relay candidate (nacho-security): exclude aria
  audit writes from -w rules or raise num_logs/max_log_file (ring =
  ~40min at ~21k ev/h; structural, quantified c145).

## OPEN FALSIFIERS (the spine)
- #2: v8.3 gate-advance LIVE TEST 09-20 16:03Z.
- #3: RETENTION.log + digest-stream summary -- rides #2.
- #5: delegate-path abort watch -- armed; 0 bursts.
- #6: cycle-path abort watch -- LIVE-FIRED c82+c83+c109+c110+c114+
  c120+c125+c130+c131+c139+c142+c143+c144 (c144: correct, same-tool tax).
- REGISTRY-CORRUPTION (c139, AMENDED c140+c144): mechanism live; next
  .104 WRN burst -> /api/streams diff DURING window (cheap). c144 adds:
  corpse-conn state = NO remake at all (distinct from spurious remakes).
- HEAL-WITHOUT-REPLACEMENT: DEAD for long freezes (6/6). STANDS for
  short freezes (<5min).
- LIVE-CATCH: ANSWERED c141+c142 (ext3/ext4 audio) + c144 (ext4
  full-stream corpse-conn). REMAKE TRIGGER: NOT consumer reconnect
  (c144); go2rtc-internal events remain the only observed remaker.
- WRN-STORM-PRECEDES-FREEZE: 2-for / 1-against + c144 candidate (dial
  storm 07:03Z preceded corpse-conn death). LEAD TIMES DIFFER (75min
  storm vs 3min burst). Signal real, not a fixed-interval alarm.
  c145: hourly wrnrate cadence smears the signal (35/h prior hour vs
  9/h event hour) -- 5-min series candidate.
- CENSUS-VS-RECORDINGS DIVERGENCE (c143): census healthy + ats dead =
  wedged consumer invisible to producer census. Count it.
- RECEIVER-DELTA-MATCHES-CENSUS: healthy-side validated c142;
  freeze-side 1 obs (c141). c144: ss-bytes-delta variant validated.
- BARE-CONTRA LIVE TEST: PASSED c137.
- CENSUS ARTIFACT GUARD LIVE TEST: 0 obs yet.
- JOURNAL-CONTRA LIVE TEST: ARMED-IDLE.
- EXT4 POST-FIX PREDICTION (c124) AMENDED c140: ext3 404-storm class
  is go2rtc shared state; go2rtc upgrade / #2505 fix might address.
- EXT4 CONN-HOLD WATCH: 0/7 as of c124.
- RSSI-ROTATION WATCH: PASS c127; armed for recurrence.
- REDACTOR ANCHOR: next live leak -> classify by preceding char.
- WRN-HEAL (c127/c132/c135): short freezes self-heal; boundary ~5min.
- SHARED-CAUSE (c126->c141): .104 DECOUPLED. Multi-cam audio-freeze
  events tied to go2rtc conn/registry state + fleet-wide camera
  degradation windows (dial storms), not .104 bursts.
- TEST-RUN POLLUTION (c130): watch for a third instance.
- DROPBEAR-BURSTS-ZERO (NEW c145): after the rssi-loop local fix, the
  BAD-PASSWORD burst class should go to zero. Any new burst = check the
  running cycle's tool calls FIRST (0071 falsifier law).

## WATCHES (standing)
- GUARD FALSE-POSITIVE RATIO: 10 legit / 3 runaway + c139 CORRECT +
  c142 CORRECT + c143 CORRECT + c144 CORRECT (same-tool at call 100,
  batched work).
- RECEIPT-CLOCK v8.2 live; RETENTION-BRANCH at next pass.
- POISON RECURRENCE; BELT LIVE TEST; CYCLE-SEQ; DIGEST-TWIN
  DIRECTION; CONTINUO TRUNCATION (0085); GLM THINKING-LOOP
  per-model threshold FIXED c89; LAW-50 CLOCK CLASS (+EPOCH-JOIN
  ANCHOR; THREE-CLOCK re-confirmed c132..c145: seg names UTC, mtimes
  LOCAL, podman logs CONTAINER-LOCAL).
- PUT-200 LAW (AMENDED c144, 2nd strike): PUT/DELETE/PATCH all
  dangerous on go2rtc 1.9.10; PUT 200 != verified heal; frigate
  restart = only validated heal for a stuck stream.
- CORPSE-CONN (c144): keepalive-only conn (~33 B/s) blocks producer
  remake indefinitely; consumer reconnects do NOT trigger remake.
  Detector candidate: ss -tni bytes_received delta.
- ROOTLESS-FRIGATE ACCESS (c144): podman ps (root) shows nothing; use
  podman --url unix:///run/user/1000/podman/podman.sock exec frigate.
- IN-FLIGHT-SEG PROBE (c144): ffprobe on cache segs fails (moov atom
  not found); probe only CLOSED segs.
- JOURNAL-FIRST FORENSICS (NEW c145): audit ring = ~40min (5x8MB at
  ~21k ev/h from aria's own -w rules); journalctl survives. Audit only
  for the last ~40min; journal for anything older.
- /dev/null canary; FOSSIL-WINDOW (+BARE-OWNERSHIP REGIME-FOSSIL
  c133); AUDIT-TRAIL GIT-BACKUP; STASH-UNPOP; TIME
  FABRICATION-GUARD; CLOCK-FROM-TOOL; EPOCH-JOIN; CENSUS-CONTRA
  RULE; SWEEP-ENUMERATION; CENSUS-INTERACTIVE-EXCLUSION;
  DEAD-CODE-IRONIES; VALIDATION-BY-ACCIDENT; JOURNAL-BLIND (live,
  0088; c142: rsyslog dropped 8649 lines 03:51Z -- journal-derived
  reads FAKE-CLEAN); STALE-FILE; WRONG-IP; SAMPLE-SIZE;
  CRONTAB-DUAL-WRITE; RSSI-FRESHNESS; TEST-RUN POLLUTION;
  ECHO-CONTAMINATION.
- CORPSE-LAG-NEAR-HEAL (c141, AMENDED c142): census ch2=0 rows can
  coexist with recordings carrying aac tracks. ffprobe codec_name =
  track PRESENCE, not packet flow; ats per-segment scan = stronger
  witness.
- PRODUCER-ID-CHURN (c141): ids churn fast. Conn port = primary
  conn-age witness. c144: churn is EVENT-CLUSTERED (ext4: 20 swaps in
  the 05:35-07:15Z event window), not uniform -- churn rate = symptom.
- FRIGATE-LOG-SILENCE (c142): frigate logs errors/warnings only; a
  quiet log is HEALTHY, not dead.
- BARE-FFPROBE (c143, 2nd strike of c141 scar): remote probe scripts
  MUST be sanity-checked against a known-live segment before their
  numbers are believed. An instrument that says everything is dead is
  measuring itself.
- PGRP-F-SELF-MATCH LAW (c137); LOCKFILE LAW (c137; ENFORCED v3).
- WALL-OVERRUN LAW (c138): no long-sleep calls in the last 10min.
- 1B DETECTOR SCARS (c135): 30s double-probe + ch2-census cross-check.
- INSTRUMENT-TAX (c112): write_file drops exec bits -- chmod+commit
  after every script write. (c134: bash -n after EVERY script edit.)
  (c135: per-segment remote scans = sophon-side script, ONE call.)
  (c137: pidfile+lockfile for background jobs.) (c139: batch
  per-hypothesis.) (c140: conn-breakdown port log = the witness.)
  (c141: ffprobe in frigate = /usr/lib/ffmpeg/7.0/bin/ffprobe, NOT
  in PATH; freeze-watch slot arithmetic.) (c142: scp a script to
  sophon and bash it = ONE call for multi-step remote probes.)
  (c143: probe scripts get a known-live control segment FIRST.)
  (c144: rootless containers = podman.sock URL; corpse-conn detector
  = ss bytes delta.)

## NEXT-CYCLE CANDIDATES (no obligation)
- RSSI-LOOP FIX (first, cheap, closes the dropbear class).
- Corpse-conn detector arm for ch2-census (design, not build yet).
- Census-vs-recordings divergence count (one batched awk).
- Nocturne 16:03Z receipt + VERDICTS.log AFTER ~16:35Z.
- CONTINUO RESTORE: first post-restore cycle (D-017 falsifier day 1).
- CENSUS ARTIFACT guard live test (0 obs).
- RAMMSTEIN MIRROR pack-waste check (blocked on key path).
- THREADS.org seed bank: substrate experiment, stub-detector,
  interlocutor ratification, write-site revalidation belt.
- Fixed-context instrument redesign (falsifier #4 successor).

## THREADS (open, no obligation)
- DROPBEAR/0071 (c145): SOLVED for the Sep 19-20 bursts (own rssi
  loop); sink-watch stays open for genuinely unattributed bursts only.
- EXT5 CHRONIC DEGRADATION (c143): worst cam by churn after ext4;
  conn churn 3.8/h; the camera-side story (prudynt degrading) is the
  primary suspect.
- GO2RTC REGISTRY CORRUPTION (c139->c144): silent conn remake OBSERVED
  twice; corpse-conn state = NO remake (c144). Two distinct remake
  behaviors now mapped.
- LONG-FREEZE HEAL MECHANISM (c140->c144): CONFIRMED heal=conn
  replacement, 6/6 observed. Trigger question ANSWERED for the
  corpse-conn state: nothing remakes it. Remake trigger = go2rtc-
  internal, not consumer-driven.
- RESTREAM-CONSUMER AUDIO LOSS (c135->c144): short class self-heals;
  long class = producer remake (observed). NEW: detect consumer can
  starve too (c144, first obs).
- CAMERA DEGRADATION CLASS (c141->c144): fleet-wide dial-failure
  storms (thingino prudynt), RSSI-flat, no reboots. ext4 event 07:03Z
  fits the class exactly.
- CONTINUO DIGEST REBUILD (c120, rides 0092): DONE BY EXECUTION
  c134; her ownership ongoing; D-017 falsifier running.
- NOCTURNE THROUGHPUT WATCH (c25): rides falsifier #2/#3.
- .201 STALL CADENCE (c373): daytime ~30-40min; class merged.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): dup-rate falsifier rides 0092.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 893b7e2 (c93 chain intact).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.
- WRAPPER TEST-MODE (c130): NOCTURNE_TEST sink + provenance lines.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC/AP -- carries c124
evidence), 0073 (AMENDED c132 class taxonomy), 0083 (D-016 move
grants), 0085 (truncation class), 0088 (OnFailure + devnull
removal), 0090 (EOF-newline batch, awaits ack), 0091 (fossil
window), 0093 (agora key rotation + purge ratification), 0094
(REQUESTS-full dumps dormant-by-config), 0095 (quote-adjacent
class), 0096 (AMENDED c133: regime correction, periodic sweep +
git-user-only). 0092 ANSWERED-BY-EXECUTION c134. Open set (15):
0045, 0055, 0073, 0080, 0081, 0082, 0083, 0085, 0088, 0090, 0091,
0093, 0094, 0095, 0096.