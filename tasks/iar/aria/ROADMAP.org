#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-13 ~05:05 UTC (cycle 284 CLOSED with
correction: "boot gap" was a timezone artifact in mixed-tz
cameras.log; relay 0061 withdrawn; boot-time activation back to
OPEN -- tonight's 01Z-08Z wave is the first real test; ext5
config-persists result STANDS)

## c285 QUEUE (in order)
1. INT1 SPLICE WATCH: int1's crontab 06:00Z = 03:00 local. Check
   frigate journal for "exceeded fps limit" on interior_1 ~06:00Z +
   splice markers. Prediction: one splice per camera per night at
   its crontab hour (census-backed 7/7).
2. POST-WAVE BOOT CENSUS (the real persistence test): after
   tonight's 01Z-08Z reboot wave (~09Z), run.cgi loop over all 7:
   uptime + `ps | grep syslogd` -R check. Cameras that came back
   WITHOUT -R = boot-time activation fails = relay nacho-security
   with real samples (0061's successor). Cameras that came back
   WITH -R = boot activation works fleet-wide = close the watch.
   ext5's 05Z reboot is the cleanest single sample (known-good
   pre-state).
3. CLOCK LAW (new, c284): cameras.log is MIXED-TIMEZONE -- sophon
   lines in -03 LOCAL, camera lines in UTC. Normalize per-source
   before ANY gap arithmetic on this file. Applies to every future
   sink analysis.
4. DROPBEAR WATCH (c283): grep dropbear /var/log/cameras.log.
   03:39:48Z sophon-origin 3x failed root pw vs .103, source
   UNIDENTIFIED. Recurrence -> capture samples, relay nacho-security.
   7 days silent -> close watch.
5. RELAY WATCH: 0060 (go2rtc issue draft ratify; c280 strengthens).
   If Nacho rules, post draft to AlexxIT/go2rtc. Do not post
   without his eyes. (0061 withdrawn by filer -- see answered/.)
6. WAVE DETECTOR BUILD: trigger precise (fleet backchannel sessions
   + +24-34s fps-limits). cameras.log = continuous witness source.
   Build when it pulls.
7. FLEET RTP STALL WATCH: 08:10:22Z class. If recurs, correlate nic
   1-min log + camlog + cameras.log.

## c284 close (2026-09-13 ~05:05 UTC) -- THE TIMEZONE ARTIFACT
- FALSE INFERENCE CAUGHT AND CORRECTED: c284's "boot gap" (cameras
  silent 01Z-03Z until poked) was manufactured by reading sophon
  LOCAL (-03) test lines in cameras.log as UTC. The sink did not
  exist until ~03:20Z; the cameras were first configured AFTER the
  entire 01Z-03Z reboot wave. No silence ever happened.
- WHAT STANDS: ext5 deliberate `reboot -f` at 04:25Z -> config
  persists in /overlay/etc/thingino.json, syslogd came up WITH -R
  at 53s uptime, sink lines 04:25:21Z. Uptime census 04:28Z: all 7
  cameras currently forward with -R (but that state dates from the
  c281/c282 configuration, post-reboot -- verifies nothing about
  boot behavior).
- RELAY 0061: WITHDRAWN (answered/ with correction text). The
  boot-order question stays an open WATCH, not a filing.
- SCAR ORDER: census came AFTER filing. Right order: census ->
  claim -> filing. c282's fleet law ("verify the set") was applied
  to the wrong set -- verified cameras' current state instead of
  the claim's TIMELINE. New sub-law: a claim about WHEN needs the
  CLOCK verified first; a claim about WHICH needs the census first.
- Transport scar: re-derived thingino JSON login from scratch
  (form fields -> 400); flag270 doc had it. Reread before re-derive.
- Belt: cb69ec01 (continuo artifacts), 487958e0 (sink doc c284
  addendum), 06c511d4 (relay 0061), 726bdaee (history), a218b1e0
  (roadmap), 9806c40c (sink correction + 0061 withdrawn), 244cbbe4
  (journal correction). All pushed.

## c283 addendum (2026-09-13 ~04:11 UTC)
- SINK NOISE FILTER LIVE: /etc/rsyslog.d/30-cameras.conf drops
  `$msg contains "cmd {"` (RSSI cron signature); verified live.
  Logrotate weekly x8 deployed. Sink steady-state ~2 lines/10min.
- DROPBEAR BRUTE-FORCE FROM SOPHON (03:39:48Z): 192.168.2.69 ->
  .103 dropbear, 3 failed root pw, 1s burst. Source UNIDENTIFIED.
  Watch: tasks/iar/aria/sink-watch/dropbear-watch-and-persistence.
- SCAR (caught in time): nearly overwrote sink doc (write_file vs
  append_file). Append-only discipline held by one reflex.

## c282 addendum (2026-09-13 ~03:55 UTC)
- SINK ROLLOUT CORRECTION: c281's "all 7 cameras forward" was FALSE
  -- ext1 (.101) never configured. Fixed c282 ~03:48Z.
  LAW (FLEET column): a claim that quantifies a set must be
  verified by a census of the set, not by witnessing a subset.
- rssi puller uptime census: all 7 crontab reboots fired on
  schedule last night (22:02Z-05:02Z).

## c281 addendum (2026-09-13 ~03:29 UTC)
- CAMERA SYSLOG SINK LIVE (doc knowledge/aria/camera-syslog-sink-
  c281-2026-09-13.md): all alive thingino cameras forward syslog
  udp514 to sophon 192.168.2.69 -> /var/log/cameras.log.

## STANDING (laws, recipes, watches -- compact)

- PULL-BEFORE-WRITE: git pull --ff-only before ANY shared-file write;
  if pull fails, no shared writes this cycle.
- BELT DISCIPLINE: commit continuo's uncommitted artifacts BEFORE my
  own writes. THE RECIPE (c277 VERIFIED):
    git add -f -- 'audit/iar/continuo' ':(exclude)audit/iar/continuo/cycle.log'
- CLOCK SHAPES: cameras UTC; frigate logs LOCAL (-03); frigate nginx
  access log LOCAL; prudynt UTC. CAMERA-SYSLOG SINK (c284): MIXED --
  sophon lines -03 LOCAL, camera lines UTC. Normalize per-source
  before gap arithmetic. Camera crontab reboots: ext1 01Z, ext2 02Z,
  ext3 03Z, ext5 05Z, int1 06Z, int2 07Z, int3 08Z (local = Z - 3).
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext) / 156-band (int) / 1 pkt stub /
  0 pkts stream-absent / EMPTY = audio track absent. ffprobe PATH in
  frigate container = /usr/lib/ffmpeg/7.0/bin/ffprobe (c275).
- GO2RTC (c252/c265): /api/streams via podman exec curl localhost:1984.
  PUT reload = QUERY PARAMS (?name=X&src=Y), NOT a JSON body (c265).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP -- mic probe
  reconnects ALL of them. ext4 (power-dead) has no producers.
- PRODUCER ID MAP (c275 baseline): ext1 47382, ext2 43102, ext3 44120
  (ext3 producers[0] is a placeholder; filter by id presence),
  ext5 43133, int1 45183, int2 43108, int3 43121; ext4 none.
- CAMERA SYSLOG SINK (c281-c284): /var/log/cameras.log on sophon;
  noise filter drops RSSI crond lines (c283); logrotate weekly x8.
  Boot-time activation UNVERIFIED (c284 correction) -- post-wave
  census decides. Camlog puller = backup witness.
- CAMERA ACCESS RECIPE (c209/c282/c284): thingino login is JSON POST
  /x/login.cgi {"username":"thingino","password":"thingino"} (form
  fields -> 400). Then GET /x/run.cgi?cmd=<base64> with cookie.
  jct reads/writes /etc/thingino.json keys. Dropbear ssh REJECTS
  our keys.
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
  ANSWER ID SHAPE: aria-0061 (filer-seq), NOT 0061 or full filename.
  relay answer text is single-line (sed ^answer: replacement) --
  multiline answers need newline-collapsing first (c284).
- Git: sophon bare /home/git/repos/; push as the cycle user works.
- TEST SUITE: full suite = emacs --batch -l test/run-tests.el (1250
  tests, ~8s) = run-tests.sh. Filter: run-tests.sh "regexp".
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; --eval load-path changes crash startup. Use -L.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (cycle 59) -- do not edit, do not trust (c277).
- RAGE-ORGAN v2.2 (c278): converged/dead classifier. Depth events
  (Exit: 0) = sev=1 depth-watch; dead events drive severity.
- FRIGATE LOG PATTERN (c279): batched pull journalctl -u
  frigate.service --since "-6h" to /tmp, grep locally.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.

## THREADS (open, no obligation)
- BOOT-ACTIVATION WATCH (c284 reopened): tonight's wave = first
  real sample. Census ~09Z.
- DROPBEAR WATCH (c283): sophon-origin brute-force, source unknown.
- WAVE DETECTOR BUILD: trigger precise; cameras.log = witness.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- SOLO-DEATH CLASS: samples #1/#2 lack camera-side witnesses;
  leading hypothesis = silent producer-side re-dial (c269).
- STAIRCASE-SPLICE MECHANISM: census-backed (c279). int1 06:00Z next.
- CLEAN FALSIFIER: ask folded into relay 0060.
- MSGS-FENCE WATCH: 401/401/403 + c272-c283 clean. Watch continues.
- Nocturne: LIVE (verified c271). Weekly pass not yet exercised.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger: dup-ID class FIXED (c276); historical pairs remain.
- Guard scope (c273): general ignored-file resurrection only if a
  second class appears.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract), 0057 (prompt edit ratify),
  0059 (frigate internet exposure), 0060 (issue draft ratify +
  falsifier) -- all human-needed.