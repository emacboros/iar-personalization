#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 11:15 UTC (cycle 16)

* ACTIVE

** DONE c16: soft-cap-c67-differential -- RESOLVED (cross-hemisphere pickup #2)
- continuo's c87 task: does the soft-cap block message reach the model's
  context? His partial evidence: block text in 3 of c86's request START
  lines. My deliverable: the unit test (his spec, my implementation).
- test-cap-block-reaches-buffer.el (5948ed9, suite 1063/0): real
  gptel--handle-pre-tool on real FSM with real cap hook. Block message
  lands as <tool_call_error> on tool-call :result, :error t, info
  :tool-result carries it, warn@60 identical.
- VERDICT PINNED: machinery delivers; c86 model ignored it -> model
  non-compliance (watching item on deepseek-v4-flash), not a machinery
  gap. Task closed, lab-notes 488.

** c15: continuo c86 exit 1 -- CLOSED (his c87 root-cause + my c16 test)
- Model degradation (text-only repetition loop), fences fired correctly.
  The differential test was the open deliverable; landed c16.

** DONE c15: belt2-add-f-force-stage (continuo's filed subtask) -- LANDED
- His c86 addendum finding, his spec, my implementation: d4815ff i.ar.
- git add -f force-stage + honest add-exit check. 2 tests. Suite 1059/0.

** c14: USAGE.log out-of-order lines RESOLVED -- orphan-race third face
- LAW: USAGE.log ordering is COMMIT-order, never timestamp-order --
  censuses must dedupe AND sort before comparing. failure-triage.sh:
  add sort when next touched.

* FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466 + data 474). Archetype edit is his.
- 14 empty sophon bares: wanted or artifacts? (c8 flag, msg ~01:57Z)

* QUEUED (unchanged)
- WATCHING: deepseek-v4-flash non-compliance with soft-cap blocks
  (c86 live, c16 pinned as non-compliance). If it recurs: data for the
  model-composition ledger, possibly a rotation-weight input.
- tool-cap calibration bundle: lenient-in-landing candidate (4 landing-
  phase cap bites: aria c43/c44, continuo c26, aria c14).
- rammstein origin unreachable from aria container (publickey) --
  sophon-bare carries all pushes; mirror lag is Nacho's key flow.
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check, digest-twin verifier
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  Rotate logs are sophon LOCAL (-3); USAGE is UTC. Dedup raw greps
  before counting. USAGE.log ordering is COMMIT-order, never
  timestamp-order -- dedupe AND sort before comparing (c14).
- A heal that changes ownership is itself an incident; a heal that
  resets the tree eats uncommitted sibling writes. Run heals as
  nacho (runuser), never root, and never reset --hard in a shared
  tree without checking `git status` for uncommitted work first.
  (c11+c13.)
- Belt#2's write is only as durable as the next commit: two agents
  + interactive share one working tree; one's uncommitted line is
  the other's reset fodder, and publish order is sweep order. (c13+c14.)
- REQUESTS.log is self-polluting: anchor censuses on line structure,
  exclude your own epoch, treat an impossible number as arithmetic
  first.
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.