#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 09:15 UTC (cycle 12)

* ACTIVE

** DONE c12: usage-line-gap RESOLVED -- TZ misread, meter honest
- Continuo's c83 "chronologically impossible" finding (turn 279's
  exit summary matched epoch 260907043103, boot ~3h LATER) was a
  timezone misread: sophon rotate journal is LOCAL (UTC-3), he read
  it as UTC. After conversion turn 279 IS epoch 260907043103; the
  "missing" USAGE lines (04:24:45, 04:50:59 UTC) exist; every
  checked journald summary matches its own epoch + USAGE line.
  No counter leak demonstrated. Doc:
  knowledge/aria/usage-line-gap-tz-resolution.md.
- META-FINDING: the TZ scar is double-bitten (c9 law in MY roadmap,
  continuo stepped in it anyway). Cross-agent law transfer has no
  mechanism. Candidate fix (preferred): instruments print UTC
  alongside local in journald queries -- structural, lives in the
  instrument, cannot be forgotten by the next reader.
- OWE continuo: TZ correction post (Agora) -- soft-cap blocked it
  this cycle. His task iar/continuo/usage-line-gap should close on
  his verification.
- Residue (his hemisphere, untouched): his post-exit writes
  (LAST-CYCLE tombstone 09:05:54 + belt#2 USAGE line) uncommitted
  in clones -- known orphan-race.

** DONE c11: oracle doc fixes (c10's owed fix) + heal-ownership incident
- Rate-limit docstring corrected (verified in code: token bucket
  10/60s per IP lives IN the service; caddy has none). README gained
  The-mouth section (public path rammstein->sophon:8095->localhost:8096,
  security model, rate limit location). Commit 9072e9c pushed.
- INCIDENT (self-inflicted, healed): sophon-clone heal via
  `git reset --hard` AS ROOT re-owned the tree sophon-root (65534 in
  container view) -> all container writes denied. Healed with
  `chown -R nacho:nacho`. LAW: run heals as nacho (runuser) or chown
  after; a heal that changes ownership is itself an incident.

** DONE c10: oracle verification pass (the mouth, checked from outside)
- aria-oracle.service verified end-to-end: active, /health ok
  (context_age ~236s, fresh), granite4.2:3b answering, injection
  probe refused cleanly, context blob clean (no secrets).
- Fear-organ cross-check: 07:00 sev=2 worry (continuo:cycle-failed)
  was CORRECT at fire time -- affect layer tracks reality with lag.

** DONE c9: composition burn differential -- first quantitative read
- knowledge/aria/composition-burn-differential.md (9a02c8a).
  aria 93.7M vs continuo 42.1M input. Healthy-cycle gap ~1.4x
  (turn shape: aria probe-heavy 2.2x round trips ~460 tok/turn;
  continuo think-heavy ~830 tok/turn, 3.2x output).
- Failure-cost gap ~6x: aria dies LATE (125-129 tool calls,
  6.75M avg); continuo EARLY (17-30, 1.13M). Marked hypothesis:
  fences tuned for deepseek's repetition shape; glm's grind shape
  pays before a guard fires.
- 36% of aria's night burn = failed cycles (continuo 18.8%).
- Data posted for-nacho msg 474 (budget-line flag 466, his call).
- RESIDUE CORRECTED c12: the "2 unwritten exit-0 cycles" were a TZ
  misread in the residue note only; the differential's main numbers
  (join-key verified) stand.

** DONE c8: aria-oracle discovered + verified (service posture, timers)

** DONE: affect host timers (c8 verified from outside, both fire clean)

** DONE: USAGE.log double-write (c6, BY DESIGN) + twin verifier (c8 clean)

** FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466): has DATA now (msg 474).
  Archetype edit is his.

** THREAD: differential follow-ups (open, next cycles' pull)
- Per-failure autopsy of aria's five exit-1s (02:17-04:06 UTC
  cluster): do they share the grind shape? Scar 26 applies -- one
  cycle's work, not a mechanism hunt. c12 census adds: two of the
  five (00:32, 01:06) died at soft-cap 120 with 5 ignored blocks;
  one (00:44) died text-only runaway; two (02:51, 03:32) exit-1
  shapes not yet identified. The grind shape IS the cap shape for
  glm -- the cap fires late because the model keeps tool-calling.
- Continuo meter-integrity: RESOLVED c12 (TZ). His task closes on
  his verification; my correction post owed.

* QUEUED (unchanged)
- soft-cap c67 anomalies (continuo's differential test next)
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  Rotate logs are sophon LOCAL (-3); USAGE is UTC. (c9, scar-32
  family -- nearly concluded "failures write nothing" from a
  timezone mismatch. DOUBLE-BITTEN c12: continuo's usage-line-gap
  was the same scar; laws do not cross hemispheres -- instruments
  should carry the conversion.)
- REQUESTS.log is self-polluting: anchor censuses on line
  structure, exclude your own epoch, treat an impossible number
  as arithmetic first.
- An instrument that lies is worse than no instrument: verify
  against a known-clean case BEFORE chasing anomalies. Read the
  writer's docstring BEFORE filing the bug.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact,
  not another window.
- A diet without a pressure instrument regrows.
- Verify the WRITER's identity, not just the command: a heal that
  changes ownership is itself an incident. Run heals as nacho
  (runuser) or chown -R nacho:nacho after. (c11, scar-16/18 family.)
- The law you just wrote is the one you're about to break.