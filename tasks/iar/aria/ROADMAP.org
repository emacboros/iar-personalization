#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-17 ~01:34 UTC (c373: EXT2 PERSISTENT FREEZE -- the 'healthy' camera (.102) went audio-dead 19:32Z silently, 5h50m+, producer id 26 frozen-in-place, no scheduled healer (0 read-timeouts/24h); ext1-0073 class 2nd instance. PATH B CONFIRMED: ext3 h21 block + int1 h00 block healed by watchdog ffmpeg restarts (exact segs, zero producer WRNs). WRN-ALIGNMENT EVIDENCE VACUOUS: ext4 dials every 10s -> every 23s window covered by construction; do not count alignment pairs again; discriminator = go2rtc CPU spike at dial time. SEG-CENSUS v1.1 LIVE: producer probe (id + audio-track per cam hourly -> producers.log; replacement witness). Nocturne next pass 16:01Z TODAY (roadmap's 13:00Z was local/UTC slip), gate 1246 behind, cap 300/pass.

## c381 QUEUE (next cycle)
0. SEG-CENSUS H01 READ (fires 01:05Z, may be late by probe): ext2 still
   100% dead? producers.log first hourly row (v1.1 live). One batched call.
1. EXT2 FREEZE WATCH: does it self-heal (ext1 healed ~12h late)? If
   h01+h02 both total-dead, it is a persistent freeze with no scheduled
   healer -- the falsifier is a producer-id CHANGE in producers.log.
2. PRODUCER-ID LONGITUDINAL: first night of producers.log -- which
   cameras replace producers how often? int1/int2 replaced tonight
   (5525->5725, 3652->5715). Baseline the replacement cadence.
3. NOCTURNE PASS READ: fires 16:01Z TODAY (13:01 -03; the old 13:00Z
   was a local/UTC slip). Gate 1246 behind, cap 300/pass => ~4-5 clean
   passes to drain. Read the verdict next cycle after 16:01Z.
4. GO2RTC CPU-SPIKE DISCRIMINATOR (the real contention test): sample
   go2rtc process CPU at ext4 dial seconds vs freeze onsets. Needs the
   live-probe wired into fleet-check or a small sampler. THE test that
   can actually confirm/kill the contention hypothesis.
5. ROOT-GIT-STATUS POISON (0042): durable fix still open (2a/2b).
6. DAILY CENSUS (standing): req-census + 429 + commit-census. c373:
   0 true 429s both agents; aria 490 reqs/44.6M in; continuo 62/3.3M.
7. QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z; 0 true 429s.
8. SUITE FAILURES (c368): 17 pre-existing. Separate thread.
9. FIXTURE HYGIENE TEST (Nacho-ratified c371): mktemp-anchored test
   repos + pin test. Build next interactive or cycle.
10. GPTEL-FORK RESIDUE: sophon-side emacs.d/gptel-fork is an EMPTY dir
    (rotate.sh copies from /var/home/nacho/repos/gptel each cycle; the
    sophon copy is untracked residue removed by git clean). Inert, but
    a future false-alarm magnet -- consider deleting the empty dir
    (cosmetic, nacho-owned tree; ask or leave).

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- PATH-SHAPE LAW (c377): before reading "0 results" from a
  find/ls as a REAL absence, verify the path shape against a
  KNOWN-GOOD example (list the parent dir first). Zero-padding, case,
  date formats are silent no-match traps. File cousin of the
  empty-grep class (c376): EMPTY OUTPUT IS THE LOOP FUEL.
- NAME-BEFORE-NARRATIVE LAW (c378 NEW): before attributing an event
  to a named camera, verify the NAME->IP map against the LIVE system
  (config order + go2rtc API), not against memory or an older doc.
  A wrong map survives for days and manufactures false mechanisms
  (c377's ext2 claim). Verify the LABEL before the STORY.
- DB-BEFORE-DERIVED (c377): when a derived read (filesystem scan)
  contradicts the primary record (frigate.db recordings table), the
  disagreement is the finding -- check the query before the world.
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook),
  history-clock (pre-commit).
- TZ LAW SHARPENED (c359b, APPLIED-AND-BURNED c372 AND AGAIN c374):
  sophon JOURNAL timestamps are LOCAL -03; frigate recording HOUR-DIRS
  and segcensus labels are UTC. Reconcile the query window against the
  query clock BEFORE building a narrative on the timestamps.
- INSTRUMENT-TAX LAW (c378 NEW, B3 data): never wait inline for a
  sophon python job longer than 30s -- nohup it, read it NEXT cycle.
  dead-runs.py cost ~13 min of wall (4 timeouts + nohup waits) for
  marginal data. Predictions underestimate instrument-repair time;
  add a fixed instrument-tax line to every plan.
- LAW 50 (c215-c259): an instrument's output has a SCHEMA --
  verify the DAY, the COLUMN, the KEY FORMAT, the CLOCK (timezone),
  the SOCKET, the UNITS, and DELTA-vs-CUMULATIVE. c377 addition:
  verify the PATH SHAPE. c378 addition: verify the NAME->IP MAP
  (the label before the story).
- Guard compliance is not compliance (c243/c259/c269): when a guard
  fires, change the QUESTION or stop.

## WORLD STATE (2026-09-16 ~23:00 UTC -- REPLACES all prior blocks)
- IP MAP CORRECTED (c378): ext2=.102, ext5=.105. All camera-side
  evidence from c374-c377 needs re-attribution when reused.
- AUDIO NIGHT EVENT (c377->c378 CORRECTED): ext5 (.105) watchdog-
  visible stall cascade h21-h22 (producer WRNs 22:30-22:35Z, watchdog
  restarts 21:40Z + 22:36Z, audio recovered post-restart). ext2
  (.102) healthy. ext3 (.103) audio dead 19:39-21:37Z then recovered
  in-session; h21 140 dead recovering; storm continues (522 sessions
  today). int1 (.201) h21 129 dead (21-min run 18:47-40:07 local).
- CONTENTION MECHANISM (c378): candidate SURVIVES with corrected
  actors (ext5/int1 stall around ext3 storm bursts). 134 same-second
  multi-camera WRN pairs today. Amendment 3 in mechanism v3 doc.
- PRODUCER-REPLACEMENT-HEALS: now has a counterexample (ext5 3433
  replaced, audio stayed dead until watchdog restart). Heal path for
  ext5 = watchdog. ext1 silent freeze (0073) still the only confirmed
  silent-freeze instance.
- SEG-CENSUS PULLER v1.1 (c373): LIVE. Hourly at :05 + producer probe
  (producers.log: id + audio-track per cam). h00: ext2 225 dead (frozen
  since 19:32Z), int1 56 (healed), ext5 2 (stubs), ext3 0 (healed).
- .103 RECONNECT STORM (c374->c378): sessions/day 15/99/182/222/522
  (cameras.log dedup). Storm survives nightly 03:00 reboots. Onset
  trigger (Sep 14 02:29 local) UNIDENTIFIED.
- FRIGATE WATCHDOG (c374): ~380 restarts/hour all day (ext4 dial
  crash loop dominates). fps-limit kills: 38 today (ext3 30).
- NOCTURNE: heal drop-in landed c371; next pass Wed 09-17 13:00Z.
  Gate ~1000+ commits behind; cap 300/pass => ~4 clean passes.
- 27H PREFLIGHT OUTAGE (c369): RESOLVED; creator IDENTIFIED c372 (mid-edit belt2d test).
- CLOCK-FABRICATION (c362): guard live, watch clean.
- STUCK-STAGED RESIDUE (c368): mechanized belt #2d live.
- CHECKOUT-PUSH GAP: closed c367->c368.
- FALSIFIERS: Nocturne pass 16:01Z TODAY; ext2 self-heal watch (h01/h02
  segcensus + producer-id change in producers.log); segcensus h01 01:05Z.
- BARE REPO HEALTH: 2 root-owned commit-graph files persist in
  iar-personalization.git (old, from c372 root push). My pushes
  create none (verified c373).
- V4 GATE: two failed passes (09-15, 09-16); next pass Wed 09-17
  13:00Z. RESERVOIR-DRAIN fired 09-15.
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s so far.
- CAMERA OUTAGE: .104 power-dead since 09-12 (0063, physical =
  Nacho). .102 self-resolved Sep 14 13:41 (8.5h outage, crash-loop
  class, watchdog 3309).
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- FRIGATE EXPOSURE: scanner #3 (Google Cloud, 91 reqs) -- relay
  0059. Auth holding. WAVE TRIGGER fix CONFIRMED COSTLESS; issue
  DRAFTED, DO-NOT-POST (0060).
- SUITE FAILURES (c368): 17 pre-existing (invisible-turn x2,
  ollama-stream x8, sanitize x7). Fix = separate thread.

## THREADS (open, no obligation)
- EXT5 H19 WINDOW (c378): 102 dead 17:00-18:00 local, no event found.
  Run shape pending (nohup on sophon).
- .103 STORM ONSET (c376): why did sessions shorten Sep 14 02:29?
  pcap or prudynt debug are the remaining instruments.
- .201 STALL CADENCE (c373): why ~100/day vs 12/8 on identical
  hardware? Camera-side evidence censored; needs pcap or prudynt.
- NESTED-GIT CREATOR (c369->c372): CLOSED. Creator = mid-edit belt2d test (unbound default-directory). Copy-not-init WITHDRAWN. Residual watch: tripwire re-fires would mean a NEW creator; the class law is test fixtures bind default-directory before relative git ops.
- NOCTURNE RANGE-DEBT (c363->c371): ~1000 commits past gate.
- CONTINUO THINKING-TRUNCATION (c366): 5-13% of her cycles; aria
  ZERO. Watch per-day fire counts; escalate >15%.
- SUITE FAILURES (c368): 17 pre-existing. Separate cycle.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean.
- LOOP-GUARD RATE (c363): c378 ran ~108 calls, ONE guard fire (the
  nohup wait chain -- now an INSTRUMENT-TAX law). Batching build
  DONE; the remaining driver is inline-waiting, mechanized away.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations + c376 corollary.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; no recurrence
  since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063 (parked on 0073 ruling; fires on
physical visit), 0064, 0065, 0066, 0068 (mechanism armed, policy
open), 0069, 0074 (journal flood).
ANSWERED: 0073+0063 (cameras deprioritized, observation-only).
HELD: 0075 (ext2 silent freeze -- fires only on evidence-destruction
or all-8 spread per the ruling's escalation bar).
All human-needed or watch.