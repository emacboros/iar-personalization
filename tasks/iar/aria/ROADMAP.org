#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~21:29 UTC (c337: EMPTY-END MECHANISM
RESOLVED by direct probe -- 6 live requests vs the ollama proxy:
healthy streams ALWAYS carry usage counts in done:true; the 0/0 is
an intermittent SERVER-side accounting bug (~0.1%/req, 2 events in
4 days, TWO model families: nemotron + deepseek). NOT model
degradation, NOT size/shape/overflow. Relay 0069 filed (D-014
trigger met) with retry-once proposal. Relay 0063 amended: c336's
"brief revival" CORRECTED -- .104 continuously dead since 09-12
11:10Z (58h+); the 17:45 self-heal was the WATCHDOG going quiet,
not the camera returning. Segment evidence: zero ext4 segments
09-13/09-14.)

## c338 QUEUE (next cycle)
1. V4 GATE WATCH (Tue 2026-09-15 13:00 LOCAL = 16:00Z): first run
   with v4 live (echo-check + claim-receipt every rc=0 run +
   RANGE-CAP 300). Expected: RANGE-CAP line (695-commit debt ->
   300 digested), then echo-check verdict on the fresh pass.
   Journal is the AUTHORITATIVE wrapper log; verdict greps anchor
   on "nocturne-digest: " wrapper tag.
2. EXT2 AUDIO-HEAL WATCH: class-3 (negotiation staleness) verified
   still broken 21:26Z (newest segment 26.02 hevc,video only; no
   record-proc restart since 17:45). PREDICTION: next record-proc
   restart heals. Probe newest ext2 segment each cycle.
3. STALE-PROPOSAL RESERVOIR: relay 0068 open (ratify-or-delete
   policy). The stale audit/iar/aria/DIGEST.proposed.md (Sep 12
   19:40, 11006 bytes) is STILL on disk. V4 echo-check is the net.
   If the Tue pass echoes again, that's echo recurrence #3 -- the
   reservoir policy becomes urgent.
4. EMPTY-END RETRY-ONCE (0069, pending Nacho): if ratified, build
   retry-once on 0/0-stop-with-substantial-thinking in
   iar-agent-cycle.el (core .el: full suite before push). The
   guard (aria-0026) currently tombstones; retry converts a lost
   cycle into a ~3min delay.
5. SWEEP-SHAPE 2 FALSIFIER (c302/c303/c319): Sep 15 01:00Z boot --
   check tonight's boot shape against the c319 prediction.
6. DAILY 429 WATCH (standing): 0 today both agents (field-anchored
   $6/$7). Wall re-hit predicted Sun 09-20 ~04-12Z.
7. THINKING-LOOP WATCH (standing): continuo 2 real today (19:07Z);
   aria 2 real today (13:23Z, 14:33Z). tools=0 anchor.
8. AEVUM PULSE #5 (due ~Sep 21, weekly-only).
9. CONTINUO REPETITION: the census sentence is now 5x in her 09-14
   journal (relay 0046 open). Watch dup-rate after the ruling.
10. PULSE-TEMPLATE WATCH: post-seed 7/7 clean (line 520 predates
    the seed: 1958f6c0 14:16Z < e13e3504 15:43Z). Watch at seed
    strength.
11. DIGEST-COLLISION WATCH (c331): after relay 0066 lands, verify
    her next memory pass writes audit/iar/continuo/DIGEST.md and
    NOT the top-level sync copy.
12. FLEET-CHECK CLOCK LAW (c335): build mtime-based age
    normalization when a false STALE/NO-SEGMENT lands.
13. RELAY WATCH: 0042, 0045+0055, 0046, 0057, 0059, 0060, 0062,
    0063 (amended), 0064, 0065, 0066, 0067, 0068, 0069. All open,
    all human-needed.

## STANDING WATCHES (one-line pointers; FULL LAW TEXT:
## knowledge/aria/roadmap-laws.md via read_knowledge)
- EMPTY-END CLASS (RESOLVED c337): intermittent ollama.com proxy
  accounting bug; done:true chunk arrives with no usage fields.
  Healthy streams ALWAYS carry counts (verified 6 shapes). Guard
  aria-0026 tombstones correctly; retry-once proposed (0069).
  Census = terminal-field anchor. NOT nemotron-specific.
- WATCHDOG-QUIET LAW (c337, NEW): an instrument going quiet is not
  the thing it watches getting better. The 17:45 ext4 watchdog
  silence was the capture thread giving up, NOT the camera
  recovering. Verify the WATCHED THING directly (segments, ping)
  before crediting a heal to the watcher's silence.
- ECHO-RESERVOIR LAW (c336): an unratified stale proposal on disk
  is an echo reservoir. Ratify-or-delete (0068); echo-check = net.
- CAMERA-CLOCK LAW (c335): segment names + hour dirs + rssi stamps
  + camera logread are CAMERA-CLOCK (~3h slow at boot until NTP);
  segment mtimes + frigate/go2rtc journals are sophon real.
- AUDIO LAW v3.1 + CLASS 3 (c335): heal = session remake; CAMERA
  reboot does NOT heal class 2. Class 3 = record-proc negotiation
  staleness (ext2): prediction = next record-proc restart heals.
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log;
  verdict greps anchor on "nocturne-digest: " wrapper tag.
- INSTRUMENT-REACH LAW (c332/c333): test the instrument against
  the exact case that motivated it; verify the DOCSTRING too.
- ECHO-RECYCLE LAW (c328): a final response that byte-matches a
  prior run's final response is not evidence of fresh work.
  ENFORCED IN CODE (v4, every rc=0 run) -- first live test Tue.
- BLOCK-BOUNDARY LAW (c327): watermark-anchored extraction.
- NOCTURNE v4 (d9a2daf2+0b942d90): echo-check + claim-receipt on
  every rc=0 run; RANGE-CAP 300 commits/pass. DEPLOYED 15:58
  local 09-14; first live run Tue 09-15 13:00 local.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066).
- CLOCK LAW: normalize per-source before ANY gap arithmetic.
- REQUEST-LOG SCHEMA (c320/c322): orphan census MUST JOIN .log +
  .log.1; real request = exactly 3 lines.
- CENSUS-SOURCE MAP + CLAIM-ANCHOR: census evidence lives in
  REQUESTS.log(.1) PARSE lines, HISTORY.log, git commits.
- FIELD-ANCHORED CENSUS (c326, re-bitten c336 TWICE, c337 AGAIN:
  123 phantom 0/0 lines = self-echo): anchor on FIELD SHAPE.
  Terminal-field anchor for 0/0: split($0,a," ") and test
  a[n-2]/a[n-1]. 429: status= is $6, code is $7.
- PULL-REEXEC LAW (c330); STALE-RECEIPT DETECTOR v4.4 (bin);
- FLEET-CHECK: on sophon, 2min runtime, ssh timeout >= 300s.
- GIT HYGIENE: add -f for audit/tasks; write_file resets exec
  bits (update-index --chmod=+x + chmod on sophon, BOTH sides).
- SOPHON->RAMMSTEIN PUSH: container-side push is the working path.
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3/EXT4 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min; check cadence before calling stale.
- CAMERA OUTAGE: .104 CONTINUOUSLY power-dead since 09-12 11:10Z
  (58h+, relay 0063 amended -- NO revival, c336 claim corrected);
  .102 SELF-RESOLVED 09-14 (audio class-3 open, relay 0067).
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z.
- DEAD-CYCLE GUARD (c326): failed request + no live successor =
  immediate exit 1.
- C271 RECURRENCE LAW: re-census against git history before
  amending a filing with recurrence claims.

## THREADS (open, no obligation)
- EMPTY-END RETRY-ONCE (0069): build on ratification. The direct-
  probe method (interrogate the live edge) is the reusable lesson.
- STALE-PROPOSAL RESERVOIR (c336): 0068 open; echo-check = net.
- CAMERA-CLOCK SKEW MECHANISM (c335): WHY 3h slow at boot?
- SWEEP-SHAPE 2 FALSIFIER (c302/c303/c319): Sep 15 01:00Z boot.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- .101 boot-night RSSI divergence (c319): watch, no obligation.
- DISCOVERABILITY GRADIENT (c331): watch: does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): arxiv + semanticscholar
  rate-limited. If pulled again: single query, spaced, or
  research container.