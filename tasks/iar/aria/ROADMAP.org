#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-04 01:01 UTC (c3, flash: ext4 audio SELF-HEALED 00:24-00:29 UTC Sep 4 -- flag 367 RESOLVED, withdrawal pending. c2 boundary confirmed; flap pattern verified via packet counts; healer = producer read-timeout forces reconnect (positive control for flag 326). ext4 Sep-3 had a SECOND loss (04:23-04:27) + recovery (16:41-16:57, mechanism unknown). ext3 outage 18:10-18:26 UTC. fedora@ still broken.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS: c3 ok (exit 0; cap hit at 120 -- landed knowledge+journal
  first, posts deferred).

* DO FIRST (next cycle)
1. FAILURE-FIRST (always).
2. DEFERRAL LAW: post lab-notes c3 summary + flag-367 withdrawal
   (for-nacho follow-up in topic flags) FIRST at close-start,
   before any new work. Text is in JOURNAL.org c3 entry.
3. Organs at wake; fleet-check baseline.
4. Watch: does ext4 stay healthy? (healed 00:29 UTC; recurrence
   interval is the systemic-vs-oneoff discriminator). If ext4
   re-deafens, the read-timeout healer is too slow -- strengthens
   flag 326 (restart) or a targeted reconnect watchdog.
5. ext4 recovery #1 (16:41-16:57 UTC Sep 3): mechanism unknown.
   Producer generation for that window is gone (6824 is current).
   Low priority -- one data point, class already named.
6. fedora@10.66.0.5 ssh auth: re-verify each cycle; interactive
   item (11) stands.

* OPEN FLAGS (Nacho's call, all low urgency)
- 367: RESOLVED (self-healed) -- withdrawal pending c4.
- 326 (frigate restart to heal e2/int3): NOW HAS A POSITIVE
  CONTROL (ext4's read-timeout reconnect healed it live). Note
  this in the withdrawal post.
- 340 (go2rtc debug logs), 290 (valence host timers), 270 (camera
  API creds -- blocks thingino API-surface documentation).
- Zulip backup gap (TOP infra flag, for-nacho 349): /var/lib/zulip
  in NEITHER restic set.
- i.ar push to origin (git@10.66.0.1) needs Nacho's key.
  (Local main ahead ~35 of origin/main.)

* THREAD BANK (candidates for next cycles)
- THINGINO PRIMARY-SOURCE THREAD (chosen c2, still never started):
  read the thingino source/docs for (a) the API surface (unblocks
  the loaded-gun scar: docs before probing), (b) any auto-update
  mechanism (rolling-reboot cascade hypothesis). Internet door is
  open; summaries-not-pastes.
- Glance rotation as standing instrument (fleet-check line or
  sophon timer) -- a BUILD decision.
- Cap-calibration bundle for Nacho: 8 instances now (c3 hit cap
  during landing-adjacent work again -- the pattern holds).
- Breaker-trim question (NAMED unknown): answerable only on first
  fire. Do not manufacture a fire.
- Boredom-organ longitudinal: keep runs cheap and honest.
- gptel unknown-tool stall question (unresolved).
- THREADS.org seeds (gift, witness-friction, absence-signals):
  unclaimed; pull only if one actually pulls.
- eln-cache staleness: NAMED class, no work unless it recurs.
- Census dedupe: key on epoch (REQ <epoch>-N); anchor greps
  line-start; validate pattern against known-positive first.

* LAWS (newest first, full text in JOURNAL.org)
- TZ-CORRELATION LAW (c3): frigate container logs are UTC-3,
  recordings are UTC. Convert before correlating; a WRN at 20:53
  container-TZ IS the 23:53 UTC boundary event. Near-miss caught
  mid-analysis.
- PACKET-COUNT WITNESS (c3, instrument law): ffprobe -count_packets
  discriminates healthy (250/16s-seg) / dying (4-19) /
  renegotiating (541-611, duplication) / hollow (1, int3 class).
  Sharper than codec-presence alone.
- READ-TIMEOUT-HEALER LAW (c3): the producer read-timeout is the
  mechanism that heals zombie sessions (video flowing, audio
  dead). A session that never times out never heals -- ext2/int3
  are exactly that state. ext4's self-heal is the positive control.
- LOOP-GUARD-READ LAW (c2): re-read captured output before walking
  source; enumerate only what one targeted command cannot answer.
- LOADED-GUN LAW (turn 168): never GET-probe action-shaped
  endpoints on production devices. Prefer API docs (primary
  source) over probing.
- KNOWN-POSITIVE-FIRST LAW (turn 167): every census validates its
  pattern against a known-positive count BEFORE trusting results.
- SELF-HEALING-FAILURE LAW (turn 163): a failure that disappears
  with zero code change is environmental until proven otherwise.
- MIRROR-WALK LAW (cycle 46): the record's "next cycle picks fresh"
  is a letter, not a law.
- COMMIT-MESSAGE LAW (cycle 45): a commit message is a claim about
  a diff; the pull-before-write guard forces the look.
- CAP-LANDING LAW (cycle 44, 8 instances): the soft cap bites
  during LANDING (posts, pushes, roadmap rewrites). Data for the
  cap-calibration bundle.
- SSH-PROBE CHAIN LAW (continuo c27, adopted; DEEPENED c46): batch
  remote probes into one compound ssh per question.
- POINTER-VS-CONTENT LAW (cycle 42): the CONTENT is usually real;
  the pointer is what's wrong.
- INSTRUMENT-TAX (cycle 41): with batch-read discipline the tax is
  ~0.5x prediction or better. Batch hard.
- STALE-POINTER LAW (cycle 40): cross-references get re-checked
  before acting; empty re-checks trigger content search.
- PRODUCER-ID LAW (cycle 39): record producer IDs at first sample;
  generations are the identity anchor.
- MTIME LAW (cycle 38): segment mtime is NOT arrival evidence.
- PRODUCER-VS-RECORDING LAW (cycle 38): a flowing producer is not
  a healthy recording pipeline; verdicts need all three points.
- CODEC-PRESENCE LAW (cycle 37): offer is not flow; packet count =
  track flows; decoded bytes = signal.
- RENEGOTIATION-CONVERGENCE LAW (cycle 34, CONFIRMED 35): track
  renegotiation converges over ~10 min with flappy segments.
- FRIGATE-ACCESS LAW (cycle 33): stack is rootless (user 1000);
  runuser -l nacho; recordings at /home/nacho/containers/frigate/
  storage/recordings/<date>/<hour>/<cam>/; go2rtc unauth
  localhost:1984 INSIDE the container.
- SILENT-TRACK-LOSS LAW (cycle 32): a pipeline can lose a track
  mid-session with zero log evidence on either end; only the
  output artifacts witness it.
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a go2rtc
  session state, not a camera state.
- PROVISIONAL-VERDICT LAW (cycle 31): a recovery verdict issued
  during an active flap is provisional.
- RECOVERY-VERDICT LAW (cycle 30): a recovery is a destructive
  decision (it turns watches off); positive AND negative controls
  first.
- CENSUS LAW (cycle 29): a count gating a destructive decision
  needs pattern validation against a known-positive.
- POINTER LAW (cycle 27): wrong pointers misdirect writers AND
  readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits
  to an undecided framing is not a fix.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final
  write misses the write.
- DIFFERENTIAL LAW (cycle 8, deepened 31-39, 163): same input
  through both pipelines is how you find which layer lies.