#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-12 ~22:05 UTC (cycle 268: WAVE TRIGGER CONFIRMED
at code level -- frigate metadata probe's microphone param remakes all
RTSP sessions; doc knowledge/aria/wave-trigger-confirmed-2026-09-12.md)

## c268 addendum to NOW
- WAVE TRIGGER CONFIRMED (edbd30fd): the chain is source-verified in
  go2rtc 1.9.10 + frigate 0.17.2. Frigate 0.17.2 live grid fetches
  per-cam stream metadata 2s after page mount
  (useDeferredStreamMetadata, DEFER_DELAY_MS=2000, Promise.allSettled
  parallel). Frigate backend (frigate/api/camera.py) ALWAYS proxies
  with video=all&audio=all&microphone. The MICROPHONE param builds a
  probe consumer whose mic media (recvonly, CodecAny) matches the
  camera's speaker (sendonly audio) media -> prod.AddTrack ->
  rtsp conn.AddTrack -> ModeActiveProducer && StatePlay -> Reconnect()
  = TEARDOWN + Dial/Describe(backchannel)/Setup/Play. FULL SESSION
  REMAKE per camera. All 7 alive cams have speaker audio in SDP ->
  7 remakes at the same second -> prudynt BackchannelStreamState
  sessions -> +24s fleet-wide watchdog restarts.
- VIDEO/AUDIO DO NOT reconnect: streams-level GetTrack hits the
  pointer-equality shortcut (track.Codec == codec, codec = producer's
  own codec object from MatchMedia) -> existing track returned. Only
  the MIC (fresh CodecAny object, no pointer match) falls through to
  conn.AddTrack -> Reconnect.
- TIMELINE (local -03): page loads 15:39:24 / 16:16:43 / 16:19:08 ->
  7x streams GETs at 15:39:32 / 16:16:51 / 16:19:12 (= the prudynt
  UTC seconds 18:39:32Z/19:16:51Z/19:19:12Z) -> watchdog events
  15:39:55-15:40:05 / 16:17:15-23 / 16:19:36-46, all 7 cams each.
- NEGATIVE CONTROLS: 15:43 batch of 5 MSE ws connects -> NO wave.
  Page closed after 16:19 -> ZERO watchdog events 16:20-21:54.
  29 watchdog events in 6h: all in wave minutes + ext3's known
  52-min death story (17:10-17:34). No unexplained events.
- FIX OPTIONS (Nacho's call): (a) drop "microphone" from frigate's
  metadata proxy params (smallest change, kills the wave); (b) go2rtc:
  match existing receivers by codec NAME not pointer identity;
  (c) go2rtc: serve cached SDP for metadata GETs without AddConsumer;
  (d) file upstream issue to AlexxIT/go2rtc (pointer-equality smell).
- SOLO-DEATH CLASS STILL OPEN: ext5/int1 audio legs 16:13-15 local had
  NO streams GETs at that minute -> NOT page-load related. Separate
  trigger. (Audio-only deaths don't trip video watchdogs.)
- CLEAN FALSIFIER PENDING: single-cam GET outside wave windows ->
  expect single-cam fps-limit ~20s later. The two single GETs in this
  window (16:17:17 int3, 16:19:18 int2) landed INSIDE wave windows --
  ambiguous. Next deliberate page-load session can test.
- c267 items stand: twin regression repaired (e9db72b4); Nocturne
  timer LIVE; internet scanners on frigate 8971 (Nacho's call);
  relay ledger defect (REQ IDs not unique) noted.

## c266 addendum (mechanism, now fully explained by c268)
- WAVE MECHANISM: go2rtc re-dials ALL camera producers simultaneously.
  Proof: prudynt BackchannelStreamState new-session logs at IDENTICAL
  seconds (18:39:32, 19:16:51, 19:19:12Z) on .103/.105 (BE230) AND
  .201 (Mercury) -- two APs, same second. TRIGGER now confirmed
  (c268): frigate UI page load -> metadata probe GETs.
- API-HANG THEORY FALSIFIED (c266): all 3 "Failed to fetch streams" =
  GET /api/go2rtc/streams/exterior_4 -> 500 @ ~3s (dead-camera dial
  timeout, browser-driven). ext4-specific noise, NOT wave triggers.
- EXT3 52-MIN DEATH RESOLVED (c266, trigger now identified): wave-3
  reconnect landed mid-flight; camera-side encoder stuck; watchdog
  restart 20:14:29Z healed.
- HEAL-CREDIT CORRECTED (c266): PUT reload was a bystander; watchdog
  restart healed ext3. relay-0058 RESOLVED.
- WAVE DETECTOR SIGNAL: same-second prudynt BackchannelStreamState
  bursts across cameras = wave signature. Camera-side logs primary.

## STANDING (stable facts; do not re-derive)

- PULSE: run in ONE batched ssh (keyscan re-pin first). Fleet
  pullers: rssi/ camlog/ nic/ under /var/lib/aria-fleet/ (sophon
  cron 15min/15min/1min). fleet-check v2.21 via aria-fleet-feed.timer
  (6h) -> fleet-latest -> fear-organ. Run by hand when stale.
- FRIGATE ACCESS: ssh root@10.66.0.5 then 'su - nacho -c
  "XDG_RUNTIME_DIR=/run/user/1000 podman exec frigate <cmd>"'.
  Frigate version deployed: 0.17.2-3d4dd3a (stable-tensorrt image).
- CLOCK SHAPES: cameras run UTC; frigate logs LOCAL (-03); frigate
  nginx access log timestamps are LOCAL with [12/Sep/2026:...] format;
  prudynt camera logs UTC. Convert before correlating.
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; 250 pkts healthy (ext cams) / 156-band (int cams) /
  1 pkt stub (permanent) / 0 pkts stream-absent (transient) / EMPTY
  output = audio track absent (video-only segment).
- GO2RTC (c252/c265): /api/streams via podman exec curl
  localhost:1984. Stream keys exterior_N/interior_N. PUT reload =
  QUERY PARAMS (?name=X&src=Y), NOT a JSON body (c265 scar).
- ALL 7 ALIVE CAMERAS HAVE SPEAKER AUDIO in SDP ('audio, recvonly'
  media) -- verified 2026-09-12 via /api/streams. This is why the
  mic probe reconnects ALL of them. ext4 (power-dead) has no producers.
- RELAY: knowledge/aria/bin/relay (file/list/show/digest/answer).
- Git: sophon bare /home/git/repos/; push as the cycle user works
  (root-push pollution = objects owned by nacho -> "unable to
  migrate objects"; heal = chown -R git:git <bare>/objects, done
  twice by hand, structural fix Nacho's).

## THREADS (open, no obligation)
- UPSTREAM ISSUE CANDIDATE: file with AlexxIT/go2rtc -- "probe GET
  with microphone reconnects playing RTSP producers" (pointer-equality
  codec match in GetTrack/AddTrack). Internet consult next cycle.
- SOLO-DEATH CLASS (ext5/int1 16:13-15 local): not page-related;
  audio-only deaths don't trip video watchdogs. Separate trigger.
- CLEAN FALSIFIER: single-cam GET outside wave windows (deliberate
  test on next page-load session).
- WAVE DETECTOR SEED (c262/c264): nightly batch ffprobe census or
  same-second prudynt burst watcher; needs a sophon cron slot.
- c263 trailer budget discipline vs 300-call wall (3d watch: this
  cycle hit the soft cap at 300 -- depth-vs-cap watch continues).
- Nocturne: gate advanced (timer LIVE, 13:00Z daily). Nothing rotting.
- Aevum: weekly-only pulse (Nacho's cadence).
- Empty-cell experiment: designed, not built.
- Relay ledger defect: REQ IDs not unique (0055 twice, 0015 twice
  in answered/) -- needs a dedupe law or ID scheme fix.