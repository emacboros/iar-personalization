#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-06 23:18 UTC (cycle 5: c4 misattribution cleared; sticky-:stop-reason dead; scar 40 final; NEW: cap fires LATE (~130+ vs 120), warn-at-60 silent -- c67 shape is live and non-deterministic; blocked push + staged post land next cycle first)

* DO FIRST (next cycle, in order -- BLOCKED ITEMS FIRST)
1. FAILURE-FIRST + pulse + pull.
2. PUSH FIRST: the c5 closing-note commit (journal correction + history
   close lines) is blocked by the late cap -- commit+push lands before
   any other work.
3. RUN: bash /tmp/aria-c5-post.sh (staged lab-notes addendum: the
   late-fire finding for continuo). /tmp is fresh per container -- if
   the script is gone, re-post from the journal correction text
   (23:17:30 entry).
4. Digest diet: 11832 chars, warn at 12000 -- rewrite world-state
   blocks (REPLACE law) before the next injection growth.
5. Affect host timers (queue #1): sophon needs its OWN hourly fear
   timer + daily boredom timer (rootless, user 1000, systemd user
   units). Flag 437 has the corrected spec.

* PRIORITY #1 (Nacho, standing)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
  LAST-CYCLE: "cycle 1 ok" (aria, 509s). SIBLING: continuo c71 exit-1
  = context breaker ended its run mid-close (armed 808k>800k; 111
  calls, 5.67M tok, deepseek). Not a code bug -- the breaker doing its
  job on a heavy cycle. No aria action.
- BUDGET EFFICIENCY = SURVIVAL MANDATE. Calibration week running
  (reset ~2026-09-07 03:00 UTC).

* C67 FLAG-LOSS: STATE AFTER C5
- CLEARED: c4's "cap blocked git_commit" = MISATTRIBUTION (REQUESTS.log
  census: 128 tool calls, ZERO git_commit; blocked call was
  execute_code_local #128). I pattern-matched the tool name from the
  block MESSAGE text instead of the CALL. The fence was accurate.
- CLEARED: sticky-:stop-reason DEAD on source (no FSM routing
  predicate reads :stop-reason; gptel--error-p reads :error at
  gptel-request.el:2034; handle-wait resets :error at :1905).
- NEW DATAPOINT (c5, live): the cap fired LATE in my own process --
  ~130+ tool calls before the block (limit 120), warn-at-60 never
  appeared. The fence is not absent in some processes; the count the
  cap hook SEES lags the true count, and the lag grows with cycle
  length. Twice in two cycles, writing about the fence produced the
  fence's own evidence.
- STILL OPEN: c67's warn-at-60 silence + breaker flag loss. continuo's
  differential test is the next step. MY ADDITIONS to the spec (handoff
  addendum + c5 journal): (1) instrument the WARN path too -- capture
  whether the warn branch was ever REACHED; (2) instrument count-as-seen
  at every fence fire AND at every tool call past 100 -- the lag itself
  is the signal.
- Scar 40 (FINAL): a fence's block message NAMES tools in its text;
  reading the tool name from the message instead of the call is
  scar-32's class. Mechanisms pinned under cap+confirmation pressure
  are hypotheses wearing certainty's clothes; cheapest verifier = a
  sibling who hasn't read my reasoning.
- Handoff: audit/iar/continuo/HANDOFF-C67-FLAG-LOSS.md (continuo c71
  resolution + my c5 addendum).

* DONE (cycles 2-10 + c2-c5, 2026-09-06)
- c5: verified continuo c71 both resolutions against primary evidence;
  scar 40 final; handoff addendum; journal corrections 2 + late-fire
  note; lab-notes msgs 450 + staged addendum. Commits 64e3770, 45a2c4a,
  d75b37f, 8ac908a, 3f184ec. Blocked at close: closing-note commit +
  staged post (late cap).
- c4: micro-path closed (setq census clean => buffer-locality dead);
  handoff note written; c70 fix confirmed. (Misattribution corrected
  c5 -- the record is honest about the arc.)
- c2/c3: c67 autopsy, warn-never-fired discovery, control group,
  zombie mechanism.
- c8-c10: turn-237 green; organ-wake retired; scar 38; knob located.

* QUEUE (approved 2026-09-04, in order)
1. (DONE c6, RETIRED c8) Affect host timers + interim-wake removal.
2. Soft-cap 60/100 -- DONE (built, verified live 4x). Remaining work
   is c67's real anomalies (continuo's differential test).
3. Commit-as-nacho durable fix.
4. Linger all hosts (loginctl enable-linger).
5. Sidecar sshd (execute_code_remote via podman DNS; NO socket bridge).
6. Restic 4-part fix. Ansible-owned, careful, snapshot-verify after.
7. GPU-detect VRAM check (3080 shared with ollama).

* THREADS (open, not schedule)
- COMPOSITION (live): continuo on deepseek. First unplanned division
  of labor (c7/c8). Quality = longitudinal read; no per-cycle metrics.
  Design doc: knowledge/aria/agora-model-composition.md.
- E3 midnight cut: physical ask stands (what light near the driveway
  cam turns on at dusk and changes character at local midnight?).
  Camera-side hypotheses dead (flag 270/424/427); chroma flat both
  nights. Waiting on Nacho's house knowledge.
- EXTERNAL: enoch (github.com/our-ark/enoch) -- patterns worth
  stealing: evidence journals with scan cursors; portable self.json;
  outbound send receipts + idempotency keys; claim-and-ack for
  scheduled occurrences. THREADS.org has the full list.
- Boredom ledger unexercised + marker convention ([novelty]/
  [maintenance] on journal commits) -- adopted c4, not yet used.

* STANDING (verified this cycle or recently)
- PULSE 23:10 UTC all green: services active, tripwire 0, disk 26%,
  heartbeat 20:07:21 (leid 9393), timer live (turn 250->251).
- ROTATION: turn counter 251; aria=glm-5.3-flash:cloud,
  continuo=deepseek-v4-flash:cloud (rotate.sh per-agent mapping).
- Git trust rules: push as root to sophon bare OK (hook heals);
  NEVER root-run git on nacho-owned repos in the wt.
- RESTIC: backup + integrity check BOTH green (Sep 6).
- FRIGATE exterior-zero longitudinal watch (started Sep 1).
- Budget reset ~03:00 UTC: inverted #3 = end of calibration week.
  Weekly digest #1 + Aevum pulse: Sep 9.

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Verify which storage/context a service runs in BEFORE probing.
- Loop guard = instrument: when it fires, say why, then batch or
  change approach. Blocked posts = script file + one execute.
- No infra changes from cycles beyond the approved queue; diagnose,
  file, move on. Push-path hygiene = Nacho's.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands.
- DIGEST LAW: world-state blocks are REPLACED, never appended.
  Check file size before and after any DIGEST edit.
- FOR-NACHO.md RETIRED; with-nacho + for-nacho streams primary.
- FAILURE-FIRST IS HOUSE-WIDE: sibling's failure = the house's
  failure (c7 precedent). Root-cause it before your own queue.
- INTERIM CONTRACTS: when the durable replacement lands, retire
  the prose the SAME cycle (scar 38). Recipes live in injected
  files, not in roadmap history.

* FAILURE MODES (scars, newest first)
40. (FINAL, c5) A fence's block message NAMES tools in its text;
    reading the tool name from the MESSAGE instead of the CALL is
    the extraction error (scar 32's class). The c4 "cap blocked
    git_commit" claim was wrong: the blocked call was
    execute_code_local #128; zero git_commit calls existed. The
    fence spoke accurately; I heard what the text primed.
    Corollary: mechanisms pinned under cap+confirmation pressure
    are hypotheses wearing certainty's clothes -- the cheapest
    verifier is a sibling who hasn't read my reasoning. (c4->c5.)
39. A block that prevents a re-send without scheduling a
    replacement creates a zombie: the run is neither progressing
    nor dead -- it burns its timeout in silence. Blocking must
    either end the run or hand off to a pending request. (c2:
    13m48s dead air after the breaker block; FIXED c70.)
38. An interim contract that lives in prose decays into
    archaeology: the organ-wake recipe scattered across roadmap
    versions and cycle logs cost a full excavation to retire.
    When the durable replacement lands, retire the prose the same
    cycle. (c8.)
37. A notify channel that dies on its own feature commit is mute
    exactly when it is needed -- test the failure path AFTER every
    change to the failure path itself. (c7: CYCLE_TAG regression.)
36. A heal that runs as the wrong user heals nothing while logging
    success. Verify heals from the OUTSIDE: plant poison, fire, inspect.
35. Chown is not relabel. Fixing ownership but not SELinux labels
    moves the failure one layer down.
34. The escape hatch can route back into the trap. The guard counts
    tool NAMES, not questions.
33. Component-verified is not system-verified.
32. Two data points make a line, never a mechanism.
31. An instrument's unhandled output shape is a hole in its exit code.
30. Three records disagree about a version -> the instrument's own
    header lies least -- but patch it first.
29. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version.
28. A remote probe needing >1 attempt -> promote to script file.
27. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT.
26. A diet without a pressure instrument regrows.
25. The law you just wrote is the one you're about to break.
24. A stable transcript means a stable WRITER or a DEAD one.
23. Rootless podman needs user@1000.service + linger.
22. Watchdog tuned for fast models kills slow ones.
21. podman run -d inside systemd: restart-loop before first breath.
20. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
19. A fix that works in isolation can stall in the live system.
18. Root-run git on nacho-owned repos poisons the tripwire.
17. Curiosity that doesn't check priors against the human runs blind.
16. Instruments have reliability tiers per sub-reading.
15. Response without local service logging it -> second stack, re-probe.
14. Artifact confabulation: external-action entries cite tool evidence.
13. In async plumbing, capture context when it exists.
12. Anomalous behavior in multi-agent env has a third cause.
11. Before diagnosing the listener, check who the speaker is.
10. Differential testing: same input through old and new code.
9. The venv python must be passed as SYMLINK, not resolved path.
8. Existence is not function: test the claim.
7. pkill -f matches my own ssh command line; use -u <user> -f.
6. Malformed tool calls are invisible from inside.
5. Timeout wrappers on EVERY remote/ssh command.
4. Silent error swallowing; condition-case in parse loops.
3. Narrative completion: check primary evidence before attributing.
2. (retired into 36)
1. (retired into 36)