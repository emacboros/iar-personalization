#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-12 ~11:50 UTC (cycle 247)

## NOW
- c247 (11:29-11:50): OUTAGE CONTINUES (35+ min, no recovery).
    Death time PINNED ~11:10:00-11:10:22Z (was 10-min window):
    frigate segs through 11:10:0x for all 5 dead cams + NIC RX
    0.88->0.29 Mbps 11:10-11:12 + go2rtc mass timeout 11:10:22Z.
    TWO c246 CORRECTIONS: (1) ".202 boot 08:15" was wrong -- ONE
    boot at 07:00:32Z (staircase hour 7), May-25 pre-NTP + Sep-12
    post-NTP same PIDs; boot lines persist in later snapshots =
    DEEP RING BUFFER (new law-50 texture: a snapshot instrument has
    a MEMORY; check its reach before reading timestamps as recent).
    (2) dropbear bad-password events (.104/.105/.202) = OWN tooling
    (192.168.2.69 = sophon, 0.03ms RTT, matches c231/c235/c241
    cycles); 12x count = snapshot double-count. Power-group
    hypothesis unchanged. Incident doc addendum on sophon. Commit
    b11477b5. NEXT CYCLE: recovery check FIRST; if back -> audio-
    death-law watch (attach-during-down) + reconsider 14-18Z read;
    if not -> keep watch, consider telegram #3 if >2h.
- c245 rage-organ ghost hunt (3 defects fixed, v2.0.1) -- done,
  see HISTORY.
- c245 (10:22-10:55): RAGE-ORGAN GHOST HUNT (affect-organ repair).
    The sev=2 phrase ("13 events / up to 10 runs, soft cap") was
    FALSE. Three defects found + fixed in rage-organ.sh:
    (1) v2.0 journal WINDOW bounded to oldest file day (was 72h
    rolling -- reached past the file census into the fix-transition
    day; law-50 WINDOW: an instrument's window is part of its
    schema). (2) v2.0.1 sentinel-key counted as an event in the
    fix-filter rebuild (events 7->6). (3) v2.0.1 DOM_CLASS
    tie-break deterministic (was hash-random; dominant class
    flickered between msgs and soft cap). Ground truth: soft cap =
    4 events / 4 runs / 2 days (09-10+09-11), all exit-0 deep
    cycles; Msgs hard cap = 2 events ever (09-11 20:40Z, 09-12
    05:03Z), both block 1/5 -> clean summary -> exit 0. No standing
    condition. Commits 106c1990 + 45b8f8a6, pushed.
    Scar: rotate.sh INF lines are sophon LOCAL -03 (7th TZ sighting
    -- briefly "proved" a contradiction from mixing with UTC).
    Scar: 600s timeout on an awk one-liner (unbounded scan).

- c244 (10:04-10:15): AUDIO-DEATH-LAW LIVE RE-CENSUS (hours 07-10).
    THE FIND: ext4 TRANSIENT 2nd instance -- audio gone 08:19:52Z
    (4s after go2rtc producer i/o timeout, NO record restart),
    healed 09:36:20Z (12s after next renegotiation, no restart).
    Fleet-check 09:02Z NO-AUDIO caught ext4 mid-transient (correct
    for its moment). ext4 flapped AGAIN by 10:47Z (trigger
    UNIDENTIFIED -- delayed-death clause). ext2 fine anatomy:
    BOUNCED-THEN-PERMANENT (audio back 02:00:20-00:46Z, 340pkts/
    21.76s, died at 2nd renegotiation). int2: attached mid-reboot,
    ~11s audio, died at 1st post-reboot renegotiation. .104
    chronic: 13 producer renegotiations in 5h. Addendum 3
    committed (28b0397b). Relay 0050 updated.

- c243 (09:31-09:55): DAYTIME NIC READ #2 (09Z mean 1.05 CLEAN).
    nic-window-stats v1 was RX-ONLY (TX-blind) -- v2 DEPLOYED
    (rx/tx split + total_peak, epochs only; relay 0054). Verified:
    03:03Z tx_peak 4.57 = restic burst (attributed); 09:24Z RX
    4.20 single-minute burst UNATTRIBUTED (watch). FIELD-ORDER LAW
    (law-50 COLUMN): sampler is <epoch> <speed> <rx> <tx>. .203
    boot CONFIRMED (full pre-NTP + post-NTP sequence = ONE boot,
    clock jumped mid-init). 8/8 staircase closed. LAW-50 6th
    sighting: PRE-NTP EPOCH labels (May 25 in thingino camlog =
    firmware-default clock). Reviewer broke my "identical PIDs
    across boots" reading -- same processes within one boot.
    Loop-guard compliance-theater scar; new law: change the
    QUESTION or stop, never the costume.

## NEXT CYCLES (in order):
  (a) nic-sampler FALSIFICATION WINDOW (the real test): 14:00-
      18:00Z read via nic-window-stats.sh v2 -- needs a cycle
      awake after ~17:30Z (historical stall hour 17:22Z).
      DEFERRED until camera recovery (11:10Z outage confounds the
      baseline; 5/8 cams dark changes the traffic profile).
      Morning+09Z windows CLEAN (means ~1.0); only >4 Mbps events:
      restic TX 03:03Z (attributed) + 09:24Z RX burst (watch).
  (b) POST-FRIGATE-RESTART WATCH (Nacho action pending, relay
      0050): when frigate restarts, verify ext2+int2 audio returns
      within one segment cycle (HEAL test, not mechanism
      confirmation -- addendum discipline). int3 should be healthy
      too. Fleet FAIL=1 stays honest until then.
  (c) ext4 flap trigger: the 10:47Z flap had NO logged producer
      renegotiation 10:11:46Z->10:47Z. Watch for a 3rd transient
      with go2rtc delta logging active (go2rtc-delta.sh) to catch
      the trigger in the act.
  (d) RAGE-ORGAN DESIGN QUESTION (opened c245): thresholds grade
      PRESENCE, not benignness -- a fence that fires and is
      obeyed (block 1/5 -> clean summary -> exit 0) grades
      identically to a standing condition. Candidate: an
      "obeyed-vs-killed" dimension (like the kill census) so
      block-1/5-then-clean-exit reads as irritation, not anger.
      Design only; the organ is shared with continuo -- propose
      via relay ours-direction or discuss with Nacho first.
  (e) 09:24Z RX 4.20 burst: unattributed, single-minute. If it
      recurs, correlate with restic/other timers before theorizing.

## OPEN QUESTIONS (parked, no owner)
- Nocturne timer NOT INSTALLED (Nacho's action; relay/digest note).
  DIGEST.proposed.md on disk is her 00:50Z proposal (15.6k) --
  STALE vs live digest (10.3k, c243 diet). Do NOT swap without
  re-reading; her next run will regenerate after the gate moves.
- Aevum weekly pulse: unchanged (Nacho's cadence).

## RECIPES (hard-won, reuse them)

- NIC WINDOW READ (c243, v2): /var/lib/aria-fleet/nic-window-stats.sh
  <start_epoch> <end_epoch> [--force] on sophon (root ssh). v2
  output: rx_mean/rx_peak/rx_p95 tx_mean/tx_peak/tx_p95
  total_peak + link_speeds (epochs only, no strftime). v1 was
  rx-only (TX-blind) -- do not trust pre-c243 v1 numbers for TX
  claims. Sampler field order: <epoch> <speed> <rx> <tx>.
- RAGE-ORGAN v2.0.1 (c245): journal window = oldest file day
  (host-local 00:00), sentinel excluded from rebuild, DOM_CLASS
  tie-break deterministic. Residual: events on the newest file day
  AFTER the file's current content are masked (T8 anti-double-
  count) -- small conservative tail gap; dedupe-by-timestamp would
  close it (deliberately not done).
- FRIGATE RECORDINGS (c235): host path
  /home/nacho/containers/frigate/storage/recordings/<UTC-date>/
  <hour>/<cam>/<seg>.mp4; INSIDE the netns container use
  /media/frigate/recordings (c244). ffprobe at
  /usr/lib/ffmpeg/7.0/bin/ffprobe (not on PATH).
- JOURNAL FRESHNESS (c236, automated in fleet-check v2.21):
  stat -c %Y /var/log/journal/$(cat /etc/machine-id)/system*.journal
  | sort -rn | head -1; age = $(date +%s) - newest; >1800s = stale.
- SOCKET STATS (c236): ss -tnie "( sport = :PORT )" -- exact
  filter, NEVER bare grep. Two samples 60s apart: bytes_sent AND
  bytes_received both growing = healthy.
- thingino web API (c224/c226/c231/c235/c241): JSON login
  /x/login.cgi {username:thingino,password:thingino} ->
  Set-Cookie thingino_session=<hex> -> /x/run.cgi?cmd=<base64>
  with -b "thingino_session=<hex>". Output is HTML-wrapped.
- REDIS CLI ON SOPHON (c241): password at
  /run/secrets/zulip__redis_password inside the container.
  Zulip redis dbsize oscillates 0-1 (rate-limiter keys are
  ephemeral) -- dbsize 0 is NORMAL, not data loss.
- go2rtc API (c241): inside frigate container:
  curl http://localhost:1984/api/streams -- producers[].sdp has
  m=video/m=audio lines. go2rtc LOG timestamps are LOCAL time
  (c244 TZ scar) -- check the log tail against wall-clock before
  citing anything from it.
- AUDIO CENSUS (c242): ffprobe -v error -select_streams a
  -count_packets -show_entries stream=nb_read_packets -of csv=p=0
  <seg.mp4>; signatures: 250 pkts healthy / 1 pkt stub (permanent
  death) / 0 pkts stream-absent (transient) / partial truncation.
  Per-hour batched census: 3 segs/cam/hour (full sweep only for a
  suspect camera-hour).

## ATTRIBUTION DISCIPLINE (cumulative)

- TZ (7 sightings; rotate.sh INF = sophon local), content-vs-
  lineage, coincidence windows, OWN INSTRUMENTATION FIRST, OWN
  HISTORY BEFORE FORENSICS (c241), identify the netns before
  attributing network events, resolve measured-contradictions
  before theorizing, verify the READ HOUR against the PREDICTION
  WINDOW (c229), verify your own FILING's claims against the
  primary source (c231), NAME->IP map from config (c233),
  address-space discipline (c235), SOCKET identity before socket
  stats (c236), heartbeat-gap <10min is normal not death (c236),
  raw SQL beats ORM when queries disagree (c236), unit-check
  before physics claims (c236), INVENTORY SOURCE before inventory
  claims (c238), hex-dump boundary bytes when parser and data
  disagree (c238), epoch-anchor remote timestamps (c239), status
  marker is not a record (c240), build the full table before
  stating a law (c241), a filter must match the ARGS not the
  concept (c242), a census that finds one non-fitting cell is
  worth more than nine clean ones (c242), COLUMN ORDER is part of
  an instrument's schema (c243), PRE-NTP EPOCH labels (c243),
  guard compliance is not compliance -- change the QUESTION or
  stop (c243), an instrument's WINDOW is part of its schema
  (c245), a sentinel is not an event (c245), tie-breaks must be
  deterministic -- hash order must never pick the dominant
  (c245), specificity is not accuracy -- verify the window before
  reading the phrase (c245), a snapshot instrument has a MEMORY --
  ring-buffer depth is part of its schema; reconstruct the chrono
  before reading any timestamp in it as recent (c247).

## HISTORY (compressed)

- c247 (11:29-11:50): outage continues; death time pinned
  11:10:00-11:10:22Z (3 instruments); .202 boot correction (deep
  ring buffer law); dropbear = own tooling; b11477b5.
- c245 (10:22-10:55): rage-organ ghost hunt; 3 defects (journal
  window, sentinel event, hash-random tie-break); v2.0+v2.0.1;
  ground truth: no standing fence condition; TZ 7th sighting.
- c244 (10:04-10:15): audio re-census hours 07-10; ext4 transient
  2nd instance; ext2 bounced-then-permanent; int2 ~11s-then-death;
  addendum 3; relay 0050 update; go2rtc-log-TZ scar.
- c243 (09:31-09:55): nic v2 (rx/tx); 09Z clean; .203 closed;
  pre-NTP-epoch law; reviewer PID break; compliance-theater law.
- c242 (08:46-09:21): daytime nic read CLEAN; audio census 00-08
  confirms law; int3 TRANSIENT found; addendum 2.
- c241 (07:57-08:26): audio-death law (8-camera table); redis
  Sep 1 = own cycle-75 fix; relay 0050 upgraded.
- c240 (07:37-07:45): 10M link origin story; 0052 repair; 0055.
- c239 (06:41-07:20): shelf census; nic-window-stats tool; 0052.
- c238 (06:13-06:31): rage census; msgs-fence fixed (v1.9).
- c236 (05:41-05:55): false alarm (heartbeat gap); journal
  freshness instrument landed; nic overnight clean.
- c235 (05:23-05:34): staircase CLOSED (all 8 crontabs: nightly
  reboot -f, hours 1-8 UTC). ext2 audio death pinned at 02:00Z.
- c234 (05:10-05:19): WEDGE CORRECTION (c233 TZ artifact).
- c232 (04:31-04:4x): digest maintenance; diet pass due.
- c231 (04:05-04:20): staircase 4/8 confirmed; nic-sampler age
  self-correction (0048); overnight series CLEAN.
- c227 (03:05-03:35): reboot window shifted to UTC; watch closed.
- c226 (02:35-04:10): 14:22L stall attribution; house network
  mapped; THE 10M FINDING + nic-sampler deployed (0045).