#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 04:30 UTC (cycle 2)

* ACTIVE

** DONE-ish: failure-triage.sh (built cycle 2, 04:30 UTC)
- knowledge/aria/bin/failure-triage.sh: one-call failure summary
  (~2k tokens) for failure-first cycles. Syntax-clean.
- NEXT CYCLE, FIRST ACTION: run it once (bash
  knowledge/aria/bin/failure-triage.sh aria), verify output shape,
  fix if broken, commit. Do not read anything else first.
- Then commit + post lab-notes note owed from cycle 2.

** FOR-NACHO (flag owed, post to for-nacho stream next cycle)
- FAILURE-FIRST archetype clause needs a budget line. Current text
  gives total license ("root-causing THAT failure IS this cycle's
  work"); glm-5.3-flash reads that as "read everything". Three
  consecutive exit-1s the night of 2026-09-07, 20.3M input tokens,
  all failure-first corpse-reads. Proposed: "one triage call
  (failure-triage.sh), then fix, verify, converge -- the corpse is
  data, not a place to live." Archetype edits are interactive-session
  work; flagging, not editing.

** THREAD: the continuo differential (open, pull when pulled)
- Same night, same protocol: aria (glm-5.3-flash) failed 3x burning
  20.3M; continuo (deepseek-v4-flash) failed 2x then COMPLETED (3.4M,
  exit 2). Is the difference substrate, or luck? failure-triage.sh is
  the instrument. One comparison, one cycle, no mechanism-claims from
  two data points (scar 26).

* QUEUED (from interactive sessions; unchanged this cycle)
- affect host timers (queue #1, flag 437 spec)
- digest-twin verifier (continuo's hemisphere)
- soft-cap c67 anomalies (continuo's differential test next)
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- An instrument that lies is worse than no instrument: verify the
  census tool against a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.
- Narrowing is a discipline: each probe should produce an artifact,
  not another window. (Cycle 2 addition, from the 04:00 corpse.)