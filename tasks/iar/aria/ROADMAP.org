#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-15 ~05:07 UTC (c353: AUDIO CLASS CLOSED --
ext5 falsifier CONFIRMED: .105 05:00Z cron reboot replaced producer
105630->106079, audio back (05:12/05.28 segments 256000 samples).
Refinement: recorder restart alone insufficient (02:01:06Z restart
did NOT heal); fresh producer session is the heal. Relay 0072
answered+closed. Next: .201 06:00Z ARP read, detector build.
Census 0 true 429s; empty-end frozen 14.)

## c354 QUEUE (next cycle)
1. [DONE c353] EXT5 FALSIFIER: CONFIRMED. Producer 105630->106079
   at 05:00Z reboot; audio back. Class closed. Refinement: recorder
   restart alone insufficient (02:01:06Z restart did not heal under
   old producer); fresh producer session = the heal.
2. .201 06:00Z BOOT ARP READ: 0915e arp logger (pid 3460747, expires
   06:13:30Z) covers the boot. VERIFY STILL WRITING first (mtime +
   proc start, c344 law). Read /tmp/arp-reqs-0915e.txt AFTER 06:15Z:
   (a) .58 MAC (76:e3:1a:69:e3:9c) ARP at boot+0-2s = boot-timed
   behavior (c302 was one-off, expect NO); (b) .201 boot rssi epoch
   = real (~06:00:10Z), NEVER 03:02 (5th confirmation).
3. AUDIO-CLASS DETECTOR (build, mine, read-only probes): fleet-check
   addition -- go2rtc producer audio-bytes delta > 0 (flowing) AND
   newest recorder segment n_samples == 0 for 2 consecutive runs ->
   RECORDER-AUDIO-DEAD flag. Would have caught ext2 at 18:34Z and
   ext5 at 00:27Z instead of 30h/4h later. Proposal in relay 0072
   (Nacho may object; default = build).
4. DROPBEAR WATCH: SOLVED (all bursts = own tooling). Watcher v2 +
   401 pcap stay armed for UNATTRIBUTED bursts only. On next burst:
   check the RUNNING cycle's tool calls FIRST, then the snapshot.
   Verify watcher OUTPUT file exists (not just process).
5. 401-BURST CAPTURE: cam80-0916a tcpdump alive (expires ~23:32
   local 09-15). On next 401 burst: read pcap for source IP + auth
   shape.
6. V4 GATE WATCH (Tue 09-15 13:00 local = 16:00Z): first live run
   with v4 (echo-check + claim-receipt + RANGE-CAP 300). Journal =
   authoritative wrapper log; greps anchor on "nocturne-digest: ".
   Stale DIGEST.proposed.md (Sep 12) still on disk -- 0068 open.
7. EMPTY-END GATE: count frozen at 14? Watch continuo echo-close
   cycles.
8. DAILY CENSUS (standing): bash knowledge/aria/bin/req-census.sh.
   c353 read: aria 1680 req 09-15 200s, 0 true 429s both agents.
9. DAILY 429 WATCH (standing): 0 true today both. Fresh week; next
   wall ~Sun 09-21.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- RECORDER-AUDIO-DEATH CLASS (c353 NEW): frigate per-camera recorder
  ffmpeg loses its audio track when go2rtc's camera-side producer
  reconnects (ext5: timeouts 00:26:48+00:27:04Z, death 00:27:05Z);
  video survives, audio never returns until producer replacement
  (heal paths seen: camera cron reboot killing the producer). The
  camera is ALWAYS healthy in this class -- verify uptime continuity
  (rssi epoch/uptime) + live RTSP decode before blaming the camera.
  Trigger is NOT always visible in the go2rtc log (ext2's 18:33Z
  drop had no logged event). Detection: producer audio-flow vs
  recorder segment samples (the two instruments answer different
  questions; the gap IS the fault).
- NOTICE-TIME IS NOT DROP-TIME (c353): when you first observe a
  degraded window, the boundary you record is the NOTICE boundary;
  pin the real boundary by segment-level scan before filing
  durations (ext2: 30h lag in the digest's class-3 window).
- BATCHMODE-ALL-HOPS LAW (c352): any ssh to a camera carries
  -o BatchMode=yes on EVERY hop of a nested ssh. sshpass password
  tests against cameras manufacture the exact alarm shape being
  hunted; camera ssh tests = BatchMode + expect-fail, labeled as
  own probe in the cycle log AT RUN TIME. Attribution recipe: burst
  during a cycle -> check that cycle's tool calls FIRST (cycle.log
  timestamps are UTC; rotate journal is local -03).
- WATCHDOG-QUIET LAW (c337, re-earned c344, re-earned c352): verify
  the WATCHED THING directly AND the watcher's OUTPUT file (not just
  the process).
- TZ-FREE BOOT ARITHMETIC (c350): boot epoch = row_epoch - uptime
  (both camera-side columns; no reader TZ assumptions). Cross-check
  against camera syslog (logread) before claiming. A number that
  fits your hypothesis too cleanly deserves re-derivation (c342).
- AUSEARCH-TAX LAW (c349): ausearch with -ts/-te windows HANGS on
  sophon. Use grep over /var/log/audit/audit.log* with epoch
  arithmetic. Audit retention ~4h/rotation -- arm a live watcher at
  trigger time, not a post-mortem. PROCTITLE decode names the exact
  command of an ssh client.
- ECHO-RESERVOIR LAW (c336): unratified stale proposal on disk = echo
  reservoir. Ratify-or-delete (0068).
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log;
  greps anchor on "nocturne-digest: " wrapper tag.
- INSTRUMENT-REACH LAW (c332/c333): test the instrument against the
  exact case that motivated it; verify the DOCSTRING too.
- ECHO-RECYCLE LAW (c328): byte-matching final responses are not
  fresh work. ENFORCED IN CODE (v4, every rc=0 run).
- NOCTURNE v4 (d9a2daf2+0b942d90): echo-check + claim-receipt +
  RANGE-CAP 300. DEPLOYED; first live run Tue 09-15 13:00 local.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066).
- REQUEST-LOG SCHEMA (c320/c322): orphan census MUST JOIN .log +
  .log.1; real request = exactly 3 lines.
- PULL-REEXEC LAW (c330); STALE-RECEIPT DETECTOR v4.4 (bin);
- FLEET-CHECK: on sophon, 2min runtime, ssh timeout >= 300s.
- GIT HYGIENE: add -f for audit/tasks; write_file resets exec bits
  (update-index --chmod=+x + chmod on sophon, BOTH sides).
- SOPHON->RAMMSTEIN PUSH: container-side push is the working path.
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3/EXT4 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min; check cadence before calling stale.
- CAMERA OUTAGE: .104 CONTINUOUSLY power-dead since 09-12 11:10Z
  (65h+; physical cycle = Nacho's, 0063). .102 self-resolved.
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z. Week 09-15 fresh.
- DEAD-CYCLE GUARD (c326): failed request + no live successor =
  immediate exit 1.
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.

## THREADS (open, no obligation)
- AUDIO CLASS (c345->c353): CLOSED. Recorder audio track death on
  producer reconnect; heals only via producer replacement (camera
  cron reboots are the natural healer). Detector = build candidate
  (c354 queue #3).
- CAMERA-BOOT CLOCK (c342->c350): VERIFIED on .201 (real time, all
  boots). Remaining: .201 06:00Z 09-15 boot ARP question (.58 at
  boot+0-2s). Reboot mechanism = camera cron (verified .105 crontab:
  `0 5 * * * reboot -f`; fleet staggered 01:00-08:00Z).
- DROPBEAR BURSTS (c349->c352): SOLVED -- all own tooling. Watch
  continues for unattributed bursts only.
- .58 SLEEPING DEVICE (c339->c344): boot-timed hypothesis DEAD.
  Identity + ONVIF app = Nacho's (0062).
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; bursts so
  far predate it. If recurs: pcap catches source IP + auth shape.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- STALE-PROPOSAL RESERVOIR (c336): 0068 open; echo-check = net.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold? Corollary
  (c339): laws with failure stories land in one cycle; laws without
  stories land slow. (c340): laws tell WHERE to look; METHODS tell
  HOW to look without corrupting the thing looked at.
- ECHO-RECYCLE LITERATURE (c333, parked): search engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.