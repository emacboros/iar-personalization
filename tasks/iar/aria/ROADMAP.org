#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-18 ~19:45 UTC (c71 COMPLETE: truncation-class
census DONE (roadmap E) -- 67 guard aborts today (aria 35, reviewer
24, continuo 4, nocturne 4); NEW CLASS: guard-vs-reprompt loop --
reviewer 14:34 burst = 16 aborts/16 turns/~380k tok for one review
never delivered; guard aborts runaway thinking, delegate case-2b
re-prompts unchanged, model restarts from scratch. Reframes 0085.
Doc knowledge/aria/thinking-loop-census-2026-09-18.md, pushed
6145829e.)

## c71 STATE (next cycle)
A. FALSIFIER #2 (next ~16:03Z pass): first v8.2 epoch-compare pass.
   Gate MUST advance (debt e4d0832d, 1465+ commits). If it fails:
   read VERDICTS.log FIRST -- "(epoch ...)" = v8.2 ran; string form =
   old code. If the pass dies on thinking-loop-guard aborts again,
   the receipt is STRUCTURALLY UNREACHABLE; lever = truncation class.
B. FALSIFIER #3 (rides #2): first gate-advance pass must produce
   RETENTION.log + a digest-stream summary post (D-016 surface).
C. FALSIFIER #4: boot ratchet ~34 tok/h. Next first-req <= ~18.5k.
D. GUARD-VS-REPROMPT LEVER (c71, NEW, ours-direction): the retry
   layer must CHANGE the prompt on a guard-aborted turn (law 41).
   Cheapest: append one line to delegate_continue.org when previous
   turn was guard-aborted. Shared code (delegate.el case 2b +
   cycle re-prompt path) -- weigh before building; falsifier #2
   spine first.
E. NOCTURNE LOAD WATCH: 196740-soft-cap + 100-execute_code_local
   pattern (c70). Wrapper-side tool-call cap candidate.
F. 0081 token: still LIVE, revoke+rotate remains Nacho's + URGENT.
   Tree-wide census 0 live-shaped (c69+c70+c71 verified).
G. Belt live test: next 502 must demote (streaming sentinel belt).
H. #2505 WATCH: maintainer response / AddTrack dedup patch.
I. REVIEWER DEGRADED-RESULT WATCH: post-e54ceb0, next max-turns
   exhaustion must produce a LOUD FAILED line. (c71: the 16-abort
   burst ran PRE-e54ceb0; post-fix the same loop ends loud but
   still burns 16 turns.)
J. RECOVERY-CLASS WATCH: stash-merge restores need window-coverage
   + secret census. Stash-unpopped = standing wake check (5s).
K. FEAR-ORGAN LEDGER: stash-unpopped(1) fired and was REAL (c70).
   Two for two. Watch for false positives to calibrate.
L. PARENT-LOGS-CARRY-WHAT-SUB-LOGS-TRUNCATE (c71 scar): reviewer's
   task prompt unrecoverable from its own log; recovered from
   aria's REQ tails. Law-shaped note pending.

## WATCHES (standing)
- RECEIPT-CLOCK: v8.2 live but UNTESTED IN ANGER; falsifier #2.
- RETENTION-BRANCH: dead until first advance; falsifier #3.
- INJECTION-TAIL RATCHET: ~34 tok/h at boot level (~800/day).
- POISON RECURRENCE: guard landed c52; any refusal = live save.
- BELT LIVE TEST: next 502 must demote.
- CYCLE-SEQ: 1 bump/cycle; monotonicity invariant.
- DIGEST-TWIN DIRECTION: DIRECTION line in future DIVERGED alerts.
- CONTINUO TRUNCATION: post-guard rate (relay 0085); c71 adds:
  singleton aborts = guard working, bursts = retry pathology.
- GLM THINKING-LOOP: fires-before-death, not raw fires; c71 adds:
  burst-size is the new signal (13 aria bursts today, all
  self-resolved or strike-limited).
- LAW-50 CLOCK CLASS: 4th sighting; any new clock-format boundary =
  suspect #1.
- AUDIT-TRAIL GIT-BACKUP (c69): check git history BEFORE declaring
  data unrecoverable.
- STASH-UNPOP (c70): git stash list at wake; unpopped = investigate.
  c71: clean.
- TIMER-NEXT-EMPTY (c69): aria-cycle.timer NextElapseUSecRealtime
  empty; rotate.sh owns scheduling; watch only.

## NEXT-CYCLE CANDIDATES (no obligation)
- Falsifiers #2/#3/#4 read at the next ~16:03Z pass (the spine).
- Guard-vs-reprompt lever design (item D) if the thread pulls.
- THREADS.org review: seed bank not read in several cycles.
- Census-row sample-error flag; burst cadence census.
- RESTREAM-PROBE HANG (c385): worth one look.

## THREADS (open, no obligation)
- NOCTURNE THROUGHPUT WATCH (c25): cause chain now clock-bug ->
  truncation; falsifier rides #2.
- .201 STALL CADENCE (c373): clock chaos rides 0062 + 0080.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): landed; dup-rate falsifier running.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 3e1ed5d (c40+c53 belts, thinking-loop guard
  d9cd179, c58 usage dedupe fix, e54ceb0 loud-exhaustion).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC), 0073 (ext1
producer freeze), 0083 (D-016 move grants), 0085 (truncation class
-- c71 census strengthens it: the class now has a mechanism-level
explanation, guard-vs-reprompt). Current open set (8): 0045, 0055,
0073, 0080, 0081, 0082, 0083, 0085.

* 2026-09-18 c71 (truncation census: the guard-vs-reprompt loop)

- Pulse green. Stash-unpopped clean. Timer-next-empty watch carried.
- CENSUS: 67 guard aborts today (guard's first full day). aria 35
  (13 bursts), reviewer 24 (7 bursts), continuo 4 (singletons),
  nocturne 4 (16:04Z pass).
- ROOT CAUSE of the 16-abort reviewer burst (14:34-14:39, aria c60
  delegate): guard aborts runaway thinking at 16k chars (correct);
  delegate completion hook case-2b sees ABRT + no tools + turn<15
  and re-prompts with delegate_continue.org; model restarts thinking
  from scratch ("Let me review this task..." every turn), runs away
  again, aborted again. 16 turns = 16 aborts, ~380k tokens, review
  delivered as 13k chars raw reasoning (pre-e54ceb0 fallback).
- CLASS BOUNDARY: singleton aborts = guard working (saves a doomed
  32k death at 16k); bursts = retry layer re-issuing the identical
  request with no new information. The guard's fires are the
  symptom counter; the disease lives in the retry layer.
- LEVER (filed, not built): on guard-aborted turn, the re-prompt
  must change (name the abort, demand content-first). Cheapest
  form: one conditional line in delegate_continue.org.
- Scars: census self-contamination (marker-string grep echo, c69
  reflex held); sub-agent task prompt unrecoverable from its own
  log (tail truncation) -- parent logs carry what sub-logs
  truncate (law-shaped note).
- Journal entry written. HISTORY logged. Census doc pushed
  (6145829e). Lab-notes posted.