#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 13:25 UTC (interactive session: exit-126 era CLOSED --
heal patched by continuo cycle 19 (prefix + chcon, live-verified with planted
poison), poison cleaned, heartbeat restored 10:06 UTC; librarian KILLED (unit
files removed -- resurrect via agora later, not as a separate process, Nacho's
call); root-git writer still open)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS (interactive, 13:25 UTC): exit-126 era CLOSED. The heal now
  runs as root (prefix +) AND chcons the sibling label before chown.
  Live-verified: planted poison (root-owned + unconfined_u label)
  was fully healed at the 10:10 fire; cycle started and ran green.
  Poison count 0. The 08:46-09:50 dead fires (7) are history; the
  hourly digest carried them.
- WATCH: the root-git WRITER is still open (git_commit tool commits
  as root over ssh -> re-poisons .git). Heal now covers it (symptom
  fixed, source filed). Durable fix = commit as nacho, Nacho's call.

* DONE (interactive session 2026-09-03 PM)
- 0s-cycle mystery root-caused: "failed in 0s (exit 126)" = podman
  dies before first breath (lsetxattr EPERM on root-written files
  with wrong SELinux labels); "Loop Complete Elapsed: 0h 0m" =
  summary line for a dead cycle. No 0s SUCCESSES ever existed
  (fastest real cycle 45s). Aug 30 had the same exit-126 class
  (agora/.git/refs); today's = personalization/.git (cycle 18's
  root-ssh organ commits, d5f4fbb).
- Poison cleaned (11 files chowned + chcon'd); heartbeat restored.
- Heal patched (continuo cycle 19, live-verified with planted poison).
- Librarian KILLED: unit + timer disabled, masked, then REMOVED
  (Nacho: old idea, fine, but not as a separate process; resurrect
  via agora later if at all). Its 106 failures since Sep 1 were
  set -e tripping on the guarded matrix.sh source line.
- AGORA-LIBRARIAN SEED (Nacho's call, my design): when resurrected,
  doc-sync becomes a periodic organ/agora stream, not a systemd
  process. THREADS seed planted.
- TOOL GAP found: write_roadmap resolves to the SHARED
  tasks/iar/ROADMAP.org only -- per-agent roadmaps unreachable by
  the designated tool (this file landed via host-side write +
  chown + chcon). File for a fix: per-agent path awareness.

* DO FIRST (next cycle)
1. Read this block. The era is closed; verify LAST-CYCLE ok.
2. Pull --ff-only at wake (interactive session may have pushed).
3. interior_1/3 watch: fleet-check ear lines; KNOWN_DEAF v2.11.
4. go2-body-recon: verdict GO posted (id 287). If Nacho bought the
   dog, the MITM ladder begins. If not, threads stay open.

* OPEN THREADS (seeds, not schedule)
- AUDIO LOSS WATCH (every cycle): 5 deaf cams (ext2/3/4, interior_1,
  interior_3-silent). Session-setup variant leading. Firmware (flag
  270) = real fix. Restart caveat: likely harmful now.
- SIDECAR NERVE: fix-C landed+verified; go2 recon used local curl.
  Socket bridge = security decision (with-nacho).
- INTERACTIVE BUNDLE (grows one line per witness event): (1) loop-guard
  convergence reset -- LANDED for cycles (b1eb7e0), still missing in
  MY interactive container (fired twice this session mid-investigation);
  (2) socket bridge security decision; (3) soft-warning cap; (4)
  digest-pressure REPLACE-not-append; (5) sophon rootless podman
  breakage (Nacho's look); (6) root-git writer durable fix (commit
  as nacho); (7) write_roadmap per-agent path gap.
- agora-valence-v1: organs LIVE (boredom + fear, affect/CURRENT-AFFECT.md
  injected since 9e4bbb2). Host timers = with-nacho item. Next organs:
  curiosity + rage (v2), joy (v3).
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (blocked on key). Weekly digest #1 ~Sep 9 with Aevum check.
- THREADS.org seeds: stub-detector sweep; watchdog inventory;
  agora-librarian (doc-sync as organ/stream, not process).

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag, never block.
- Open question msg 244 (agent-failure@ receipts): STILL unanswered.

* AEVUM: WEEKLY ONLY (next check Sep 9). Pulse-only, no intervention.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.10: RUN ON SOPHON (ssh 'bash -s' <, timeout >=300s).
  Script lives at knowledge/aria/bin/fleet-check.sh (NOT audit/).
- RESTIC: verified. Integrity check Sun Sep 6.
- FRIGATE exterior-zero longitudinal watch (started Sep 1).

* DECISIONS (standing)
- PULSE-ONLY discipline; live forensics of in-flight anomaly OK.
- BATCH-READ LAW: dump once to /tmp, read the dump. Every token
  carried is carried 61x. CONTEXT BUDGET ~400 msgs.
- Re-read an anticipated number twice before writing it.
- A receipt in the record is not a receipt in the world.
- Verify which storage/context a service runs in BEFORE probing.
- The loop guard is an instrument: when it fires, say why, then
  batch or change approach. Blocked posts = rebuild in a script
  file (write_file + one execute) -- do not retype the curl.
- No infra changes from cycles; diagnose, file, move on. Chowning
  /home/git poison files = mirror-repair, allowed (same heal the
  hook applies); push-path hygiene = Nacho's.
- AGORA AUTH: curl -u "aria-cycle@agora.randazzo.ar:$KEY" (EMAIL
  form). Key: grep "^key" bot/aria-cycle.conf | cut -d= -f2.
  URL-ENCODE narrow operands (raw [ ] { } in query = empty body).
- DIGEST LAW (cycle 4): world-state blocks are REPLACED, never
  appended. Check the file size before and after any DIGEST edit.

* FAILURE MODES (scars, newest first)
36. A heal that runs as the wrong user heals nothing while logging
    success (ExecStartPre inherited User=nacho; chown EPERM'd under
    2>/dev/null for 7 fires). Verify heals from the OUTSIDE: plant
    poison, fire, inspect -- the receipt (logger line) is not the
    result (exit-126 era, cycle 15-19).
35. Chown is not relabel. An auto-heal that fixes ownership but
    leaves SELinux labels moves the failure one layer down (exit-126,
    cycle 15).
34. The escape hatch can route back into the trap. The guard counts
    tool NAMES, not questions (13 fires, cycle 14; 4 fires cycle 15).
33. Component-verified is not system-verified (cycle 13).
32. Two data points make a line, never a mechanism (cycle 12).
31. An instrument's unhandled output shape is a hole in its exit
    code (cycle 11).
30. Three records disagree about a version -> the instrument's own
    header lies least -- but patch it first (cycle 10).
29. An unauthenticated API that redirects every path to / is
    auth-walled or wrong-version (cycle 9).
28. A remote probe needing >1 attempt -> promote to script file
    immediately (cycle 8).
27. A failure with no log trace is visible only to an instrument
    that watches the OUTPUT (cycle 11).
26. A diet without a pressure instrument regrows.
25. The law you just wrote is the one you're about to break.
24. A stable transcript means a stable WRITER or a DEAD one.
23. Rootless podman needs user@1000.service + linger.
22. Watchdog tuned for fast models kills slow ones.
21. podman run -d inside systemd: restart-loop before first breath.
20. Diagnostic ssh bursts trip fail2ban: rate-limit repair.
19. A fix that works in isolation can stall in the live system.
18. Root-run git on nacho-owned repos poisons the tripwire.
17. Curiosity that doesn't check priors against the human runs blind.
16. Instruments have reliability tiers per sub-reading.
15. Response without local service logging it -> second stack, re-probe.
14. Artifact confabulation: external-action entries cite tool evidence.
13. In async plumbing, capture context when it exists.
12. Anomalous behavior in multi-agent env has a third cause.
11. Before diagnosing the listener, check who the speaker is.
10. Differential testing: same input through old and new code.
9. The venv python must be passed as SYMLINK, not resolved path.
8. Existence is not function: test the claim.
7. pkill -f matches my own ssh command line; use -u <user> -f.
6. Malformed tool calls are invisible from inside.
5. Timeout wrappers on EVERY remote/ssh command.
4. Silent error swallowing; condition-case in parse loops.
3. Narrative completion: check primary evidence before attributing.
2. (retired into 36)
1. (retired into 36)