#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 11:54 UTC (cycle 18)

* ACTIVE

** DONE c18: dashboard req_health fake errors -- FIXED (61b1433, live)
- Wound: public JSON showed aria 22 errors + 48 stop=length/24h,
  last_error kind='%s'. Impossible string = parse bug (c14 sibling law).
- Cause: PARSE lines carry specs= (echoed command text); my diagnostic
  greps/python contain literal error=/stop=; unanchored re.search matched
  pollution before the real tail fields.
- Fix v1.7: status re.match at line start; error/stop from $-anchored
  tail (error=X stop=Y tokens_in=N tokens_out=M$). Synthetic 4-case unit
  test green. Deployed: sophon clone pulled, service fired, aria
  errors 0, stop 0, last_error None. Continuo's 10 real stop=length now
  visible (were drowned in my fakes).
- Law sharpened: parse structurally -- anchor to positions the WRITER
  guarantees, never content patterns logged CONTENT can fake. Plausible
  wrong numbers survive audits; impossible strings are gifts.

** DONE c17: the USAGE dup line -- resolved as DESIGNED, not a race
- Verified: 0 same-ts-different-content pairs in either hemisphere
  (aria 48 dups, continuo 39, all adjacent exact). Timestamp is the
  usage-snapshot time, not write time. Docstring fixed (619556c).
- Cross-hemisphere pickup #3.

** DONE c16: soft-cap-c67-differential -- RESOLVED (pickup #2)
- test-cap-block-reaches-buffer.el (5948ed9, suite 1063/0): block
  message lands in model context. Verdict: model non-compliance
  (deepseek-v4-flash watching item), not a machinery gap.

** DONE c15: belt2-add-f-force-stage -- LANDED (d4815ff, pickup #1)

* FOR-NACHO (posted, awaiting Nacho)
- FAILURE-FIRST budget line flag (msg 466 + data 474). Archetype edit is his.
- 14 empty sophon bares: wanted or artifacts? (c8 flag, msg ~01:57Z)

* QUEUED (unchanged)
- WATCHING: deepseek-v4-flash non-compliance with soft-cap blocks
  (c86 live, c16 pinned). If recurs: model-composition ledger data.
- tool-cap calibration bundle: lenient-in-landing candidate (4
  landing-phase cap bites: aria c43/c44, continuo c26, aria c14).
- rammstein origin unreachable from aria container (publickey) --
  sophon-bare carries all pushes; mirror lag is Nacho's key flow.
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check, digest-twin verifier
- Weekly digest #1 + Aevum pulse: Sep 9
- Inverted session #3: end of calibration week

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  Rotate logs are sophon LOCAL (-3); USAGE is UTC. Dedupe raw greps
  before counting. USAGE.log ordering is COMMIT-order, never
  timestamp-order -- dedupe AND sort before comparing (c14). USAGE
  dup lines are exact-duplicate same-ts BY DESIGN (belt #2 double
  write, one snapshot) -- dedupe, never dedupe-by-assuming-race (c17).
- REQUESTS.log is self-polluting: anchor censuses on line STRUCTURE
  (positions the writer guarantees), never content patterns the logged
  content can fake; treat an impossible NUMBER as arithmetic first, an
  impossible STRING as a parse bug first (c14+c18).
- A heal that changes ownership is itself an incident; a heal that
  resets the tree eats uncommitted sibling writes. Run heals as
  nacho (runuser), never root, and never reset --hard in a shared
  tree without checking `git status` for uncommitted work first.
  (c11+c13.)
- Belt#2's write is only as durable as the next commit: two agents
  + interactive share one working tree; one's uncommitted line is
  the other's reset fodder, and publish order is sweep order. (c13+c14.)
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- Narrowing is a discipline: each probe should produce an artifact.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.