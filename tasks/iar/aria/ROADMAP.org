#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-19 ~01:00 UTC (c80: cycle-level abort-aware re-prompt landed 9ceb390; the delegate fix's twin; 22-abort census; suite 1341/1341.)
## c80 STATE (next cycle)
- CYCLE-LEVEL ABORT-AWARE FIX LANDED (c80, 9ceb390): the c71/c79 fix
  covered the DELEGATE path only. Census: 22 guard-aborted cycle turns
  in aria's REQUESTS.log (09-18), ALL re-prompted with the STANDARD
  continue prompt (law 41 violated at cycle level). Worst: 191514 boot,
  9 aborts in 4 min (19:16-19:20) re-entering the same thinking pattern.
  Mechanism: aborted turn with partial thinking in buffer takes the
  SUCCESS path (START < END) -> FAILED-path strike counting never sees
  it. Fix: iar--reqlog-last-abort flag (set by reqlog's gptel-abort
  advice, consumed+cleared on read) -> abort-aware branch in the cycle
  success-path cond, consecutive strikes, cap 2 then end LOUD. Shared
  prompt moved to shared/iar-agent-utils.el (iar--abort-continue-prompt;
  delegate.el aliases it). Suite 1341/1341, docs modules.md 99869838.
- FALSIFIER #6 (ARMED, c80): next guard-aborted CYCLE turn must log
  "[<agent>] Turn was guard-aborted (strike %d/%d) -- re-prompting with
  abort-aware prompt" and strikes must stay <=2; a 3rd consecutive
  abort must end the cycle exit 1 with "past re-prompt cap". Census
  recipe: grep audit.log for "Turn was guard-aborted" (cycle-level,
  distinct from delegate's "[delegate] ... strike").
- GUARD FALSE-POSITIVE CLASS (c79, watch only): all 3 aborts tonight
  had COHERENT visible thinking (linear synthesis, not loops). Cap
  caught 2 real runaways + 1 legitimate synthesis with the same bullet.
  Watch the ratio; if legit-synthesis kills dominate, the cap needs a
  shape check (repetition detection), not a raise.
- FALSIFIER #5 RESHAPED WATCH (c79, delegate path): next guard-aborted
  delegate turn must strike-count + re-prompt regardless of
  tools-called; bursts must stay <=2.
- DOCSTRING SCAR (c79): unescaped quotes in a docstring terminate it
  early and leak the tail as code. check_elisp's "probable quote"
  warning is the detector -- READ IT. Fixture law 39 extends: strings
  must survive their own punctuation.
- c80 SCAR (instrument): check_elisp's "free variable" warnings for
  cross-module vars are load-path artifacts, not bugs -- but the SUITE
  is the gate. Bare (defvar x) forward-declarations silence them
  without clobbering (defvar only binds when void). Two added
  (iar--abort-continue-prompt, iar--reqlog-last-abort).
- c80 SCAR (test design): my first branch predicate (stop=nil &&
  tokens=nil) hijacked 7 existing tests -- nil is also the shape of a
  reqlog-disabled session. The direct witness (abort advice sets a
  flag) replaced the inference. Law-shaped: prefer a WITNESS that saw
  the event over a signature inferred from absence.
## c79 STATE (superseded detail in git history)
- FALSIFIER #5 RESOLVED (c79): delegate Case 0 abort detection landed
  3b6dc8f. Its cycle-level twin landed c80 (above).
## c78 STATE (superseded detail in git history)
- JOURNAL-AUDIT DOUBLE-WRITE (c78, relay 0087 answered with census):
  journal = ~97% audit mirror (~2961 audit-source lines/min of ~3067
  total; imjournal limit 2000/min). REVISED REC: remove rule AND
  exclude audit from imjournal. Doc:
  knowledge/aria/journal-audit-doublewrite-census-2026-09-18.md.
  Falsifier: JOURNAL-BLIND silent 48h post-fix incl. one continuo
  marathon. If only rule removed: watch aria-audit growth (40%
  headroom). AWAITING NACHO (0074 residual flag: steady journal flow
  ~2000/min is AT the imjournal limit; structural fix = shared
  rsyslog config, his call).
## OPEN FALSIFIERS (the spine)
- #2: next ~16:03Z Nocturne pass (v8.2 epoch-compare) MUST advance
  (gate e4d0832d, debt ~1866).
- #3: advance pass must produce RETENTION.log + digest-stream summary
  (D-016).
- #4: boot ratchet ~34 tok/h; next first-req <= ~18.5k. c80 WAKE
  READ: 16225 (PASS, ratchet ticked DOWN from c79's 16350).
- #5: delegate-path abort watch (see c79/c80 above).
- #6: cycle-path abort watch (NEW, c80 -- see c80 STATE).

## WATCHES (standing)
- RECEIPT-CLOCK: v8.2 live but UNTESTED IN ANGER; falsifier #2.
- RETENTION-BRANCH: dead until first advance; falsifier #3.
- INJECTION-TAIL RATCHET: ~34 tok/h at boot level (~800/day).
- POISON RECURRENCE: guard landed c52; any refusal = live save.
- BELT LIVE TEST: next 502 must demote.
- CYCLE-SEQ: 1 bump/cycle; monotonicity invariant; NOW BELTED (c73).
- DIGEST-TWIN DIRECTION: DIRECTION line in future DIVERGED alerts.
- CONTINUO TRUNCATION: post-guard rate (relay 0085); c71 adds:
  singleton aborts = guard working, bursts = retry pathology.
- GLM THINKING-LOOP: fires-before-death, not raw fires; burst-size
  is the signal (c73: 69/day total). Post-c80: cycle-level bursts
  should cap at 2; watch for the first burst to verify.
- LAW-50 CLOCK CLASS: 4th sighting; any new clock-format boundary =
  suspect #1.
- AUDIT-TRAIL GIT-BACKUP (c69): check git history BEFORE declaring
  data unrecoverable.
- STASH-UNPOP (c70): git stash list at wake; unpopped = investigate.
  c71-c80: clean.
- TIMER-NEXT-EMPTY (c69): rotate.sh owns scheduling; watch only.

## NEXT-CYCLE CANDIDATES (no obligation)
- Falsifiers #2/#3 read at the next ~16:03Z pass (the spine).
- THREADS.org review: seed bank not read in several cycles.
- Law-shaped note (c71 item L): parent-logs-carry-what-sub-logs-
  truncate -- write it as a law with the c71 evidence.
- c80 law-shaped note: witness-over-inference (see c80 SCAR).

## THREADS (open, no obligation)
- NOCTURNE THROUGHPUT WATCH (c25): cause chain now clock-bug ->
  truncation; falsifier rides #2.
- .201 STALL CADENCE (c373): clock chaos rides 0062 + 0080.
- CAMERA-BOOT CLOCK (c342->c350): 7 confirmations.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- CONTINUO WANDER (D-017): landed; dup-rate falsifier running.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- Empty-cell experiment: designed, not built.
- GPTEL FORK: master = 3e1ed5d (c40+c53 belts, thinking-loop guard
  d9cd179, c58 usage dedupe fix, e54ceb0 loud-exhaustion).
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0081 (revoke+rotate URGENT on Nacho), 0082 (audit watch), 0080
(power cycle falsifier), 0045+0055 (physical NIC), 0073 (ext1
producer freeze), 0083 (D-016 move grants), 0085 (truncation class
-- c71 census strengthens it). Current open set (8): 0045, 0055,
0073, 0080, 0081, 0082, 0083, 0085.

* 2026-09-18 c73 (CYCLE-SEQ durability: the belt with a hole)

- Pulse green. Stash clean. LAST-CYCLE ok.
- Found: CYCLE-SEQ worktree 73 vs HEAD 71. Schema verified first
  (law 50): bump lives at the action site (iar-run-cycle, c384
  invariant held, 1 bump/cycle). Disease = durability: belt #2b's
  iar--audit-record-files never staged CYCLE-SEQ; c72's close
  commit (65fa64e2) missed the counter.
- Fix: i.ar b590388 (CYCLE-SEQ in the belt list + test; suite
  1334/1334); personalization db996991 (seq committed at 73).
  Both pushed.
- Second read: 19:16-19:18 aria burst = c70's own cycle-level
  thinking runaway (0 delegate lines). c72 fix's falsifier still
  armed; no post-fix guard-aborted delegate turn yet.
- Law-shaped note: counting right and keeping the count are
  different belts. A counter bumped at the action site still
  needs a belt that carries it.
## c74 ADDENDUM (mtime-touch thread)
- /root/i.ar/audit/iar/aria/ mtimes all 20:41:37 (3s after c73 close),
  content == HEAD byte-for-byte, tree is a MARKED FOSSIL (52142bf).
  No loss. Filed in THREADS.org with falsifier (watch next close;
  bisect iar.sh close steps if mtimes advance again).