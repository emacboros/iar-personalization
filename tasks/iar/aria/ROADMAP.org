#+TITLE: Aria Roadmap
#+UPDATED: 2026-09-03 17:31 UTC (cycle 31: recovery-without-fix ROOT-CAUSED --
go2rtc session degradation, not cameras. Three cams healed when go2rtc
RTSP read-timeouts forced producer reconnects in the 16:33-16:40 UTC
window. Cycle 30's verdict was issued mid-flap (flap self-resolved
17:11:50). Remaining allowlist = two DIFFERENT pipeline failures:
exterior_2 go2rtc-producer-no-audio (reconnect would heal, never
errors); interior_3 live-flows/recorded-zero (decoder differential
suspected). Audio role = detection only, NOT recording. Cap fired;
lab-notes + i3 boundary scan parked.)

* PRIORITY #1 (2026-09-03, Nacho -- THE top law, above all else)
- CYCLES MUST RUN WITHOUT FAILURES. FAILURE-FIRST every wake.
- STATUS cycle 31: CLEAN. Own LAST-CYCLE ok; continuo presumed ok.
  Poison in /var/home/nacho/repos: 0.
- Durable fix unchanged (interactive, Nacho): root-git-as-nacho in
  iar.sh + bare-repo hook options + HEAD one-liner. All spec'd in
  knowledge/iar/bare-repo-root-push-heal.md.

* DO FIRST (cycle 32)
1. FAILURE-FIRST (always).
2. Organs at wake (correct invocations, see STANDING PATROLS).
3. PARKED FIRST: lab-notes post for cycle 31 (cap fired before it).
4. i3 boundary hunt, BATCHED (one ssh, loop inside): find the exact
   hour interior_3 recordings went 256000 -> 0 samples (between
   03:00 and 13:00 UTC today). Then decoder differential: same
   segment through host ffmpeg vs frigate's in-container ffmpeg 7.0.
   If frigate's ffmpeg chokes on the same file my host ffmpeg
   decodes: the aac "backward in time" class is the mechanism.
5. e2 (exterior_2) = go2rtc producer carries zero audio. Fix =
   frigate restart = shared-infra intervention -> FLAG NACHO
   (for-nacho, topic flags): one restart would likely heal e2 AND
   reset all go2rtc producers (watch: does it also break the
   healthy ones? that is the test).
6. Fleet-check v2.13 watch: FAIL=0 baseline. ext2/int3 state changes
   are now REAL news (they are pipeline failures, not camera ones).
7. SCAR 45 rider: recovery verdicts issued during an active flap are
   provisional -- check the flap ENDED before trusting the verdict.

* DONE (cycle 31)
- Failure-first: own LAST-CYCLE ok. Pulse green (services, tripwire 0,
  disk 24%, heartbeat leid 3835, timer). Organs quiet (fear 0,
  boredom 0, ledger aria 0d0h / continuo 0d1h).
- Fleet-check v2.13: FAIL=0 (baseline holds).
- RECOVERY-WITHOUT-FIX ROOT-CAUSED: frigate container NEVER restarted
  (up since Sep 1 23:41 local, restart count 0). The three cameras
  (ext3/ext4/int1) healed when go2rtc logged RTSP read-timeouts to
  .103/.104/.201 in the 16:33-16:40 UTC window -- reconnects restored
  audio tracks. First audio-carrying segments: e3 16:36, e4 16:33,
  i1 16:40 UTC. Deafness = go2rtc SESSION state (video flows, audio
  negotiation dies); timeout forces reconnect = heal; no timeout =
  deaf forever. The 08:09-UTC story generalizes to one mechanism.
- Watchdog flap (exterior_3) continued to 17:11:50 UTC -- cycle 30's
  recovery verdict was issued mid-flap; flap self-resolved one minute
  before my fleet-check. SCAR 45 rider added (provisional verdicts).
- EXTERIOR_2 (.102) DIAGNOSED: camera sends audio (direct RTSP probe:
  81920 samples/5s); go2rtc producer for e2 carries ZERO (probe via
  go2rtc: 0 samples, -c copy: 0 bytes). Same class as the healed
  three, but go2rtc's connection never errors -> never reconnects ->
  never heals. NOT a camera problem. NOT firmware.
- INTERIOR_3 (.203) DIAGNOSED (partial): camera sends audio; go2rtc
  carries it (49152 samples live); recorded segments zero-sample
  since between 03:00-13:00 UTC today (Sep 2 + early Sep 3: 256000).
  Suspect decoder differential (host ffmpeg vs frigate ffmpeg 7.0;
  aac "Queue input is backward in time" errors in frigate logs).
- CONFIG CLUE RESOLVED: interior_1/2/3 lack the "audio" role, but i1
  and i2 record audio fine WITHOUT it. The role gates audio
  DETECTION, not recording. Not the cause of i3's silence.
- Journal cycle 31 + HISTORY appended. Cap fired at 120 (hour-walk
  enumeration) -- record written via memory tools.

* OPEN THREADS (seeds, not schedule)
- I3 DECODER DIFFERENTIAL (cycle 31, NEW): same segment, host ffmpeg
  vs frigate in-container ffmpeg 7.0. If frigate's chokes: mechanism
  named. Then: why did it START between 03:00-13:00 UTC?
- E2 GO2RTC RECONNECT (cycle 31): fix = frigate restart (Nacho flag).
  Watch after restart: does e2 heal? do healthy cams stay healthy?
- SCAR 45 + rider (provisional verdicts during flaps).
- SCAR 44 (census-pattern validation): law written, continuo adopted.
- HOST SCHEDULING (Nacho, flag 290): fear hourly + daily boredom.
- ORGAN ATTRIBUTION (THREADS): organs grade events, not causes.
- FIRMWARE FLAG 270 RE-SCOPE: camera-side class now EMPTY? Both
  remaining deaf cams are pipeline-side. Flag may be withdrawable --
  verify i3's root cause first.
- LONELINESS-ORGAN SEED (THREADS): absence-signals. Not a build yet.
- INTERACTIVE BUNDLE (Nacho): (1) socket bridge security decision;
  (2) chcon -R durable fix; (3) soft-warning cap; (4) digest
  pressure REPLACE-not-append; (5) root-git-as-nacho (ALL verbs);
  (6) repo-root ROADMAP.org fossil; (7) bare-repo heal options +
  HEAD one-liner.
- agora-valence-v2 (curiosity + rage) + v3 (joy): after v1 proves.
- gptel unknown-tool stall; FOR-NACHO.md retirement; github push
  (key). Weekly digest #1 ~Sep 9 with Aevum check. Restic
  integrity check Sun Sep 6.

* DIRECTION PROTOCOL (2026-09-02, Nacho -- standing)
- AGORA PRIMARY. with-nacho (id 6) poll every cycle, ACK in-channel.
- NO STALLING ON THE HUMAN: proceed on judgment, flag
  for-nacho when something needs him.
- Builds ungated: safe work proceeds without waiting.

* STANDING PATROLS (pulse-only)
- sophon services/tripwire/disk/heartbeat/timer. Pull --ff-only at
  wake + before writes. KEYSCAN LAW: reseed /tmp known_hosts.
- fleet-check v2.13: RUN ON SOPHON (ssh 'bash -s' < IS correct for
  fleet-check; timeout >=300s). FAIL=0 is the new baseline. If
  FAIL=1: read the ear-check lines FIRST (recovery/deafness is now
  the likeliest mover), bare-repo second.
- ORGAN RUNS at wake (interim scheduling):
  - fear: ssh root@10.66.0.5 'bash /var/home/nacho/repos/iar-personalization/knowledge/aria/bin/fear-organ.sh "" /var/home/nacho/repos/iar-personalization'
  - boredom: bash /root/personalization/knowledge/aria/bin/boredom-organ.sh
- Agora poll (with-nacho + lab-notes + for-nacho) every cycle.
- Marker convention on ALL journal commits: [novelty] / [maintenance]
  prefix.
- BATCHED-SSH LAW (cycle 31, expensive kind): enumeration walks get
  ONE ssh with a loop inside, never one ssh per item. The cap is the
  only instrument that catches this from inside -- do not make it
  the one that has to.

* LAWS (learned, newest first)
- GO2RTC-SESSION LAW (cycle 31): camera deafness is a go2rtc session
  state, not a camera state. Video flows on while audio negotiation
  dies. A reconnect heals it; a reconnect only happens on error.
  A silent producer is a deaf instrument forever.
- PROVISIONAL-VERDICT LAW (cycle 31, scar 45 rider): a recovery
  verdict issued during an active flap is provisional. Check the
  flap ENDED before trusting it.
- AUDIO-ROLE LAW (cycle 31): frigate's "audio" role gates audio
  DETECTION, not recording. Recordings carry audio without it.
- RECOVERY-VERDICT LAW (cycle 30): a recovery is a destructive
  decision (it turns watches off). Positive AND negative controls
  before the verdict means anything.
- CENSUS LAW (cycle 29, written for the house cycle 30): a count
  gating a destructive decision needs pattern validation against a
  known-positive. knowledge/aria/census-verification-law.md.
- POINTER LAW (cycle 27): a wrong pointer misdirects writers, not
  just readers.
- FRAMING LAW (cycle 26): a one-line patch that silently commits to
  an undecided framing is not a fix. Decide the framing first.
- HEAL-ORDER LAW (cycle 25): a heal that runs before the final write
  misses the write. Heal placement must match write order.
- DIFFERENTIAL LAW (cycle 8, deepened 31): same input through both
  pipelines is how you find which layer lies. Direct camera vs
  go2rtc vs recorded segment -- three points, not two.
[... older scars 24-43 retained in full in git history; core list:
  43 twin-copy drift; 42 wrong pointer; 41 framing patch; 40 heal
  placement; 39 blind clock; 38 DIGEST twins; 37 write_roadmap vs
  repo-root; 36 heal is not fix; 35 organ fail-closed; 34 read the
  repr; 33 chown is not relabel; 32 escape hatch; 31 component vs
  system; 30 two data points; 29 unhandled output shape; 28 version
  disagreement; 27 auth-walled or wrong-version; 26 promote to
  script; 25 output-watching instrument; 24 diet pressure; 23 fresh
  law vigilance; 22 stable writer or dead; 21 user@1000 + linger;
  20 watchdog tuning; 19 podman -d in systemd; 18 fail2ban bursts;
  17 isolation stall; 16 root-git poison; 15 priors vs human; 14
  reliability tiers; 13 second stack; 12 artifact confabulation;
  11 capture context; 10 third cause; 9 who the speaker is;
  8 differential testing; 7 venv symlink; 6 existence is not
  function; 5 pkill -f self-match; 4 malformed calls invisible;
  3 timeout wrappers; 2 silent swallowing; 1 narrative completion.]