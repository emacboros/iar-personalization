#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-08 ~20:47 UTC (cycle 94)

* ACTIVE

** c94: ROADMAP GAP FOUND + PENTEST LIMB TASK TREE READ
- c92+c93 closed WITHOUT roadmap updates (last roadmap commit =
  session V 19:33). Their work IS landed (census v3 97a73f9,
  specimen 3 + mechanism chain 0dfc92b, benchmark 5f9c73d) -- only
  the roadmap lagged. This update repairs it.
- NEW task tree read: tasks/iar/pentest-limb-design/ (Nacho,
  20:40, 29136a1). Limb-not-citizen RATIFIED; container-first
  fencing; 7 open design questions (scope fence, model, cadence,
  findings contract, injection resistance, audit, personality).
  Design session pending -- interactive (nacho-arch class).
- Loop-chain fence caught a non-converging verification walk
  (10 git-log calls; one targeted command answered). Fence
  functioned as designed; rage class data fresh.

** c93: CENSUS V3 + SPECIMEN 3 + MECHANISM CHAIN (LANDED)
- Census v3 (97a73f9): name=nil -> malformed-emission class;
  fence taxonomy as columns, canonical-start classifier,
  echo-proof. aria 24 nils / continuo 0.
- Specimen 3 (0dfc92b): 20:22:00 thinking-as-name, THIS cycle's
  own emission. Mechanism chain mapped end-to-end: gptel-ollama
  sanitize (string names pass) -> unknown-tool fence ->
  gptel--handle-pre-tool -> truncate-advice audit name=nil
  status=success. Fix = one branch in the advice (status=rejected
  when tool-spec nil AND result starts <tool_call_error>) --
  .el work = interactive; relay aria-0007 carries it with exact
  location (iar-tool-call.el:154-170).
- Composition signal (D-008): glm-only wall classes (soft-cap 23 /
  loop-chain 18 / hard-limit 2); deepseek zero walls. Fence rate
  1.69 vs 0.98 per 100 reqs.

** BENCHMARK: qwen3.6:35b-a3b RESULTS LANDED (5f9c73d, session VI)
- Text VIABLE: 18-26 tok/s decode, 423-573 tok/s prefill, tool
  calls clean, 30k ctx correct. Thinking ON BY DEFAULT (cycles
  must set think:false explicitly).
- Vision BROKEN on 3080: ollama fitter computes fit WITHOUT vision
  headroom, then CLIP warmup OOMs and segfaults the whole server.
  Upstream-report candidate. gemma3:4b stays as eye model.
- VERDICT (proposed, Nacho ratifies): citizen brain NO (prefill
  tax + fat-context class hurts locally); RETAINER brain
  YES-CANDIDATE; eye organ NO.
- PULL LANDED (verified c94: ollama list shows qwen3.6:35b-a3b).

** c89: GPU-LOAD-PROBE INSTALLED on sophon (relay 0004 RESOLVED)
- Unit+timer live, enabled, next fire 00:30 tonight. Verified
  c94: enabled+active, CSV sampling every 60s, nacho-owned.
- Install fixes: ExecStart=/bin/bash <script>; User=nacho (not
  root); no PrivateNetwork+NoNewPrivileges combo.
- LAW (c89): a unit file never live-fired is a hypothesis --
  fire the thing, not the scheduler.

** c87: RAGE-ORGAN v1.6.1 -- run-id collision fix LANDED (e25f829)
- Trend census undercounted 09-07 (16 vs 17): file-source run id
  collided across hemispheres. Fix: run id carries the agent.
- LAW (c85->c87): read the instrument's source AND recompute its
  census independently. Two instruments that agree can be wrong
  the same way; two that DISAGREE have found something.
- Rage sev=3 stands: 12 -> 17 -> 5-so-far, kills on all 3 days.

** c86: EXTERIOR_4 CORRECTION (supersedes c85 verdict)
- DEAD 11:34-14:46 local, then SELF-RECOVERED. 433 watchdog
  restarts (~3/min), never rebooted, trigger unknown (auth wall).
- FOR-NACHO POST LANDED (c87, msg 619). DRAIN DONE.

** c82+c83: DRILL-003 (check_elisp) -- verdict IMPROVE, FIX LANDED
- Fix landed (i.ar 67ab1a1): OK/WARNINGS/ERRORS severity lines.
  5 tests, suite 1130 green. Drill #004 (delegate) queued,
  designed-rare.

** AGORA V2 BUILD ORDER (ratified 2026-09-08, D-001)
1. Decision ledger -- LANDED (D-001..D-010).
2. Relay + decision rights -- slices 1-3 LANDED. 0007 cutover
   request FILED, awaits interactive ratification.
3. Agora addressing + weekly reset.
4. Job files + delegate-to-job (LOAD-BEARING: retainers run on
   them, D-010).
5. Organs: appetite, disgust.
6. Immune system (prereq: measure suite runtime).
7. Embodiment (7.1 eye-check LIVE; watch 0000 waiting on heartbeat).
8. Eye model (custom organ).
VENUE (D-005): cycles build aria-reversible; interactive handles
nacho-test/identity/arch. D-008: cycles NEVER propose model changes.

* NEXT (c95+, in order)
- FAILURE-WINDOW WATCH: tonight 00:30-06:00 local instrumented
  (gpu-load-probe verified live, armed); census-window.sh in the
  morning.
- STALL WATCH (c66's 3-day window): verdict by 09-11.
- Rage v1.6 watch: if 09-09 clean, trend flips declining -> healing
  cap sev=2. Census trusted (v1.6.1).
- Digest #1 + Aevum pulse: Sep 9 (TOMORROW). Weekly digest #2: Sep 14.
- Retainer build (D-010): cycle work -- spawn-file-spec.org next
  (build order item 1 in tasks/iar/agora/retainers/PLAN.org);
  nemotron-3-ultra survey after (qwen3.6 retainer-candidate is
  the second candidate; survey = data-gathering per D-008).
- Pentest limb design questions (tasks/iar/pentest-limb-design/):
  cycle-side prep possible (findings contract draft reuses
  eye-check REPORT.md pattern); design session = interactive.
- Relay queue for interactive ratification: 0007 (cutover) +
  aria-0000 (drill 001 verdict) + aria-0002 (gate read_own_prompt;
  aria-0001 held/superseded) + aria-0004 (status check -- NOTE:
  probe half VERIFIED installed c94; heartbeat 0006 still absent,
  watch 0000 still cannot fire) + aria-0005 (context-size warning)
  + aria-0006 (text-loop sighting) + aria-0007 (status=success lie).
- rammstein push auth for cycle containers: interactive call.
- notes.git mirror on rammstein: needs Nacho (git-shell blocks init).
- num_predict lever (flag 519): parked on Nacho.
- i.ar origin remote fossil: removal = interactive session.

* WATCHES
- exterior_4 flap: SELF-RECOVERED 14:46. Recurrence watch: if it
  recurs, 11:34 start + 3h12m duration pattern is the next census.
- Double-execution near cycle end (c53): if occurrence #2 lands,
  test the double-insert hypothesis FIRST.
- Repeat-write near the tool budget (c54): cap-pressure signature.
- deepseek text-loops: cross-response guard 0 real fires; watching.
- USAGE twin-writes: belt#2 double lines persist (harmless, deduped).
- e3 23:00-local WRN pair (.102): 2 occurrences; third makes a pattern.
- Rage v1.6: trend-aware. sev=3 stands while trend flat. Healing cap
  watch 09-09. v1.6.1 census fix landed c87.
- Failure-window: tonight is the first instrumented window (probe
  live, verified c94).
- Union merge (c78): watch first real conflict for chronological
  interleave; verify entries stay parseable.
- Census v3: watch for new echo classes.
- Ollama pulls: qwen3.6 LANDED (c94 verified). Pull-death watch
  closed for qwen; reopen for nemotron survey pull.
- Same-tool warning efficacy: census not stop (2 proofs). If
  aria-0005 lands, compare warning-class convergence.
- ROADMAP LAG (c94): c92+c93 closed without roadmap updates. If a
  cycle closes without roadmap touch twice more, make roadmap
  update a MANDATORY close step (currently "if a thread moved").

* LAWS (the scar list, compressed; full text in DIGEST)
- Existence is not function: test the claim (6,8).
- Every failure worth fixing produces silence (2,4).
- Error handlers can be accomplices -- check what the handler does
  AFTER catching.
- Capture context when it exists; don't assume it survives (11,13).
- Verify against primary evidence before attributing action (32,40).
- In async plumbing, nothing survives unless written down.
- A guard that pattern-matches on a word in its own instructions
  has already surrendered.
- A census that pattern-matches ids anywhere in a line matches echoes
  inside other lines' payloads. Anchor on line structure.
- An instrument must not pattern-match on tokens that appear in the
  queries that produce its input (census-echo law, generalized c79).
- The law you just wrote is the one you're about to break (five live
  sightings; c68 held it: the rollout check was batched).
- Meter-reading has an unbounded instrument tax unless it starts from
  a script. Investigations need cheaper calls, not fewer.
- Block-replace with an assert, never sed-eyeballing a big file (c45).
- Instruments lying about themselves is the deepest failure class.
  (c68): a fix in the bare repo is not a fix in the runtime --
  verify the LOADED code, not the landed code.
  (c71): an id that glob-matches anywhere in a name is an instrument
  lying about identity -- anchor identity on structure, not substring.
  (c78): same symptom, different disease -- census before mechanism,
  then mechanism before fix.
  (c79): a silent-zero instrument is a lying instrument -- verify
  against known ground truth, never trust exit 0.
  (c82): an instrument's failure class can be its OUTPUT SHAPE --
  detection can be sound while the verdict line trains its user to
  ignore it.
  (c83): a classifier that says "clean" when it didn't understand
  what it saw is a silent-zero instrument -- unknown diagnostic
  shapes classify as warnings, never OK. And: a stub harness is an
  instrument too -- when live-fire contradicts ground truth, check
  whether the harness (not the tool) is the liar before believing
  either.
  (c85->c87, sharpened): reading the source is necessary, not
  sufficient -- recompute the census independently too. Two
  instruments that agree can be wrong the same way; two that
  disagree have found something. A join-key collision across
  hemispheres survives source-reading and dies to disagreement.
  (c86): a verdict written before the recovery window closes is a
  hypothesis -- watch windows close before verdicts harden.
  (c89): a unit file never live-fired is a hypothesis. Timer-green
  is not service-green -- fire the thing, not the scheduler.
  (c94): an enumeration walk that keeps re-asking the same question
  in different shapes is ritual, not verification -- the first
  targeted command usually held the answer; distrust of your own
  instrument results is not a reason to repeat them, it is a reason
  to check the instrument ONCE.
- A degraded source must degrade the verdict (c67).
- An organ is only as sharp as its input's RETENTION (c46).
- A watchdog must be sized against worst-case eval of the largest
  legal context -- and the abort path must not write into the context
  it protects (c51/c52).
- A fence's verdict and its evidence can disagree: autopsy the
  evidence, not the verdict (c54). Strip the scaffolding before you
  count the speech (c55).
- buffer-substring-no-properties is a lie about provenance (c55).
- The record is the coordination: a spec written into the roadmap is
  an interface. Siblings can land it while you sleep -- verify, don't
  re-do (c55b).
- Closes are the FIRST turns of every cycle, before the thread
  (c55-c57, applied c58). Commit-before-limit is its sibling.
- A sentinel is a terminator, not a line format (c59).
- Two failures that share a timestamp are one failure wearing two
  names -- census before mechanism (c66).
- Fossil data in a sliding window is a false recurrence (c67).
- A wall that fires while requests still flow is not a stall -- it is
  a workload that does not fit the wall (c69).
- A drill verdict must cite the tool_result line, not the plan. A
  record written from source-reading is a hypothesis wearing a test
  result's clothes (c81). A pre-registered spec that already
  contains its conclusion is a press release, not a test.