#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-15 ~10:47 UTC (c364: GUARD'S FIRST LIVE CATCH --
continuo generated [2026-09-16 10:38:00], belt staged it, pre-commit
REFUSED the belt commit, belt conflated exit 1 with nothing-to-commit
-> record undurable + refused blob stranded in shared index. Healed:
line annotated in place, belt-recovery ce951eab (IAR_ALLOW_CLOCK=1
audited). MECHANISM FIXED: belt #2c distinguishes REFUSAL from no-op,
returns nil honestly (cb6ea45, suite 1293 green, refusal-honest-nil
test; sophon checkout already carries it). Docs tools.md belt #2c.
GUARD-REFUSAL LAW (new): a guard is not done when it refuses; it is
done when the refusal LANDS -- the receiving machinery must not be
able to mistake refusal for success. Falsifiers still land tonight:
V4 gate 16:04Z, ext1 heal 01:03Z Sep 16, .101 boot 01:00:28Z Sep 16.)

## c364/c365 QUEUE (next cycle)
0. DONE c364: clock-audit caught continuo's new future line; root-caused
   (guard refusal swallowed by belt exit-1 conflation); healed record
   (annotated + committed ce951eab); fixed belt #2c (cb6ea45); docs +
   tests landed. Watch: her next cycle's belt should now return nil
   honestly if the guard fires again -- and the guard should NOT need
   to fire again (her timestamp habit is the upstream cause, prompt-side
   fix still pending with Nacho).
1. V4 GATE WATCH (16:04Z today): first live run with reservoir-drain
   + echo-check + claim-receipt + RANGE-CAP 300. Read journalctl greps
   anchored "nocturne-digest: " (TZ LAW: sophon journal = LOCAL -03;
   16:04Z = 13:04 local): (a) RESERVOIR-DRAIN fired (Sep-12 proposal
   -> attic)? (b) echo-check clean? (c) gate advanced or RANGE-CAP
   deferred? Stale proposal must be IN ATTIC after the run.
2. EXT1 FALSIFIER (01:03Z Sep 16 = 22:03 local Sep 15): .101 cron
   reboot should heal the ext1 producer-freeze (0073). Verify with
   ext1-freeze-check.sh BEFORE + AFTER the reboot window. Pre-baseline
   c363: audio bytes static 5979245 across 3 samples -- frozen confirmed.
3. .101 BOOT+2S FALSIFIER (01:00:28Z Sep 16): 0916b logger (sophon,
   pid 999271, verified alive c363, expires ~23:20Z Sep 16) covers
   the boot. If the malformed-SOAP burst recurs on .101 alone at
   boot+0-2s: boot-timed class REAL; check capture for source. If
   quiet: one-off stands. Also: .101 ARP-probes .58 at boot+2s.
4. CLOCK-AUDIT WATCH (standing, day 1 PASSED): run history-clock-audit.sh
   for both agents daily; a new FUTURE/UNEXPANDED after the guard install
   = the guard failed or was escaped -- investigate. Baseline + STALE
   census in knowledge/aria/history-clock-fabrication-2026-09-15.md.
5. DETECTOR WATCH: fleet-check v2.24 next fleet-feed (12:03Z local
   09:03Z) -- watch for false positives/negatives; ext1 should still
   fire class B.
6. DAILY CENSUS (standing): bash knowledge/aria/bin/req-census.sh.
7. DAILY 429 WATCH (standing): fresh week; next wall ~Sun 09-21.
8. COMMIT-CENSUS WATCH (standing): bash knowledge/aria/bin/
   commit-census.sh -- churn >40% or any numbered sequence = close
   leaked. Hook refusal in stderr = logged signal. Today: aria churn
   36% (c361 hook-install cluster inflates tail), continuo 0%.
9. TRIPWIRE: .git/ORIG_HEAD root-owned (known class, hook heals). New
   root-owned files: investigate.
10. THREADS WATCH (c363 seeds): continuo 06:00Z exit=2 (census
    aggregate mislabeled exit1); nocturne range-debt math (09-14 rate
    493 > cap 300 = debt GROWS on heavy days); loop-guard rate census.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook), history-clock
  (hook).
- TZ LAW SHARPENED (c359b): sophon JOURNAL timestamps are LOCAL -03.
  A "3h stale" number that equals the Argentina offset is the c342
  too-clean-number class: unit-check the CLOCK before it gets a
  mechanism.
- LANDING-NOT-RECORD (c358): record BEFORE new threads when budget
  runs low.
- NIL-CONTENT LANDMINE (c358): stream chunk with no :content coerces
  to "" at parse layer -- (string-blank-p nil) kills the filter. Fork
  guards (b85fb12).
- FUTURE-DAY GUARD (c358): instruments that take a DATE refuse future
  dates loud (exit 2). Empty verdict for unhappened day = fake-clean.
- RECORDER-AUDIO-DEATH CLASS (c353): recorder loses audio track at
  producer reconnect; heal = producer replacement. Camera healthy.
- PRODUCER-AUDIO-FREEZE CLASS (0073/c359): go2rtc audio receiver
  stuck, video flowing, camera healthy. Instrument:
  ext1-freeze-check.sh. Heal falsifier: 01:03Z Sep 16 reboot.
- SINGLE-CAMERA BOOT+2S CLASS (c359): Sep 14 01:00:30Z = 19
  malformed-SOAP errors on .101 ALONE at boot+2s. Falsifier 0916b.
- ROUTER-REFRESH-LOOP (c359): gateway probes stale ARP entries
  ~30/hr indefinitely (.58). Router-side noise, not device activity.
- NOTICE-TIME IS NOT DROP-TIME (c353); BATCHMODE-ALL-HOPS LAW (c352).
- WATCHDOG-QUIET LAW (c337): verify the WATCHED THING directly AND
  the watcher's OUTPUT file.
- TZ LAW (c350/c358): journalctl --since parses LOCAL on sophon
  (-03). Boot epoch = row_epoch - uptime. Too-clean numbers get
  re-derived (c342).
- ECHO-RESERVOIR LAW (c336): mechanism BUILT (c358 drain); policy =
  0068 open (Nacho).
- GATE-WATCH v5 (c333): journal is the AUTHORITATIVE wrapper log;
  greps anchor "nocturne-digest: ".
- INSTRUMENT-REACH LAW (c332/c333): test the instrument against the
  exact case that motivated it.
- ECHO-RECYCLE LAW (c328): byte-matching final responses are not
  fresh work. ENFORCED IN CODE (v4).
- NOCTURNE v4 (d9a2daf2+0b942d90+c358 drain): first live run 16:04Z
  today. RANGE-CAP 300: 904 range debt -> 300 digested, ~604 deferred.
- DIGEST PATHS (c331): injection reads audit/iar/<pers>/DIGEST.md;
  top-level DIGEST.md is ARIA's sync copy ONLY (relay 0066).
- REQUEST-LOG SCHEMA (c320/c22): census MUST JOIN .log + .log.1;
  real request = exactly 3 lines. Census timestamps are UTC (my
  logs) -- sophon journals are LOCAL; never cross-match clocks.
- PULL-REEXEC LAW (c330); STALE-RECEIPT DETECTOR v4.4 (bin);
- FLEET-CHECK: on sophon, 2min runtime, ssh timeout >= 300s.
- GIT HYGIENE: add -f for audit/tasks; write_file resets exec bits
  (update-index --chmod=+x + chmod on sophon, BOTH sides).
- SOPHON->RAMMSTEIN PUSH: container-side push is the working path.
- PULL-AND-PROCESS LOCALLY: sophon /tmp invisible to container.
- AEVUM SSH: aria@i.ar direct to fedora@54.38.46.192, TOFU to /tmp.
- EXT3/EXT4 CRASH-LOOP CLASS: alert only if >2h.
- HEARTBEAT: alive: every ~8min (10 long-polls); cadence check
  BEFORE calling stale -- and check the CLOCK ZONE first (c359b).
- CAMERA OUTAGE: .104 CONTINUOUSLY power-dead since 09-12 11:10Z
  (physical cycle = Nacho's, 0063). .102 self-resolved.
- QUOTA WALL: ~6B tokens/wk, resets Mon 00:00Z. Week 09-15 fresh.
- DEAD-CYCLE GUARD (c326) + TRANSIENT-RETRY (c357): permanent
  failures exit immediately; transient retry 30/90/270s, budget 3.
  ENFORCED (b59aac3).
- C271 RECURRENCE LAW: re-census against git history before amending
  a filing with recurrence claims.

## THREADS (open, no obligation)
- CLOCK-FABRICATION (c362->c363): guard live, watch day 1 PASSED.
  The archetype's "[TIMESTAMP]" instruction is the upstream cause; a
  prompt-side fix (inject date(1) output into the cycle context) is
  interactive-session work -- file for Nacho or propose at next
  session.
- LOOP-GUARD RATE (c363 NEW): firing-rate census falling (100-135/day
  -> 25; 10-chain evasion blocks 28-39/day -> 5). Hypothesis: close-once
  discipline + batching reduced the churn that drove chains. Watch:
  identical-3 blocks = genuine loops, not evasion.
- COMMIT-LOG CHURN (c360->c361): census + hook both live. Watch
  churn% per cycle. Today: aria 36% (hook-install cluster inflates),
  continuo 0%.
- NOCTURNE RANGE-DEBT (c363 NEW seed): gate at e4d0832d (09-12), HEAD
  904 commits past. Cap 300/pass; daily rate 151-493. A 493-day ADDS
  debt even with the cap. If gate stays >1 day behind persistently:
  raise MAX_RANGE or use the weekly=1 catch-up pass.
- AUDIO CLASS (c345->c355): ext1 producer-freeze (0073, falsifier
  01:03Z Sep 16, instrument ready, pre-baseline confirmed); ext5/ext2
  healed. Detector v2.24.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations. .201 06:02Z boot
  ARP read done (c358: .58 silent through window).
- .58 SLEEPING DEVICE (c339->c359): identity + ONVIF app = Nacho's
  (0062). Router refresh loop filed (c359 doc). Boot-timed sub-thread
  CLOSED (c344); boot+2s burst is a DIFFERENT class (c359).
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed (cam80-0916a);
  no recurrence since 00:34:05Z Sep 15.
- BOOT+2S BURST CLASS (c359): 0916b falsifier tonight. If real:
  single-camera boot-timed malformed-SOAP, source unknown.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census
  of all agent turn traffic (no attribution). Rate questions ->
  journal; attribution -> known-window correlation.

## RELAY (open, human-needed)
0042, 0045+0055, 0046, 0057, 0059, 0060, 0062 (amended c359), 0063,
0064, 0065, 0066, 0068 (mechanism armed, policy open), 0069, 0073
(ext1 falsifier, instrument ready). All human-needed or watch.