#+TITLE: ARIA ROADMAP (operational state -- live, tracked)
Last updated: 2026-09-16 ~22:00 UTC (c377: SEG-CENSUS FALSIFIER
thread. MAJOR FALSE LEAD WITHDRAWN: "frigate doesn't record at night"
was my own path bug -- hour dirs are ZERO-PADDED (00-09), I probed
single digits (0-9), silent no-match read as absence. frigate.db
recordings table (16924 night segments, exists=True) broke the story.
Frigate records 24/7. PATH-SHAPE law filed (scar doc
knowledge/aria/segcensus-hourdir-path-scar-2026-09-16.md). REAL
findings: ext2 producer-audio FROZEN since 19:32 UTC (3.5h+, no WRN,
producer id 26 never replaced -- ext1 freeze class on 2nd camera,
first with known onset); ext3 audio dead 19:39-21:37 UTC, recovered
IN-SESSION (no new session; backchannel unclogged in place); NEW
mechanism candidate: go2rtc event-loop contention -- .103 storm
freezes OTHER cameras' audio (ext2 freeze coincides with ext3 storm
burst end; int1 WRNs on the SAME SECONDS as ext3's twice; 156
same-second multi-camera WRN pairs today; RSSI solid). v3 doc
amendment 2 appended + pushed ae4a24a5. 150 tool calls, guard fired
twice -- batching build is now proven-urgent by this cycle itself.)

## c378 QUEUE (next cycle)
0. CLOCK-AUDIT FORK BATCHING (URGENT, c371, NOW TRIPLE-URGENT): this
   cycle ran 150+ execute_code_local calls AGAIN (loop-chain + 100 +
   150 warnings). Build the batched probe script (one ssh, many
   greps, one output block) BEFORE any investigation. Next cycle
   MUST start with this.
1. SEG-CENSUS WATCH (standing): hourly fires; read output each cycle.
   ext2 h22+ = does the producer-audio freeze heal? (falsifier: if
   still 225/225 dead at h22/h23, the freeze persists; producer
   replacement needed). ext3 h21+ recovery continues?
2. CONTENTION CENSUS (new thread, c377): per-camera freeze-onset
   times vs ext3 WRN-burst timeline. If ext2/int1 freezes cluster
   during ext3 storm bursts, the contention mechanism is confirmed.
   Instruments: frigate.db recordings (dBFS column!) + producer.go
   WRN timeline. The DB's dBFS column may give per-segment audio
   levels -- check schema first.
3. NOCTURNE PASS WATCH: next fire Wed 09-17 13:00Z. Heal drop-in
   live; expect clean pass or a NEW failure mode. Gate ~1000+
   commits behind; cap 300/pass => ~4 clean passes.
4. JOURNAL-BLIND GUARD (c371 law, still valid): fleet-check
   journal-freshness should fail loudly when rsyslog is
   rate-limited (grep the "begin to drop" marker). Small build.
5. CREATOR HUNT (standing, narrowed): witnesses armed.
6. ROOT-GIT-STATUS POISON (0042): durable fix still open.
7. DAILY CENSUS (standing): req-census + 429 + commit-census.
8. QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z; 0 true 429s.
9. SUITE FAILURES (c368): 17 pre-existing. Separate thread.
10. FIXTURE HYGIENE TEST (Nacho-ratified c371): mktemp-anchored
    test repos + pin test. Build next interactive or cycle.

## STANDING LAWS (durable; full text knowledge/aria/roadmap-laws.md)
- PATH-SHAPE LAW (c377 NEW): before reading "0 results" from a
  find/ls as a REAL absence, verify the path shape against a
  KNOWN-GOOD example (list the parent dir first). Zero-padding, case,
  date formats are silent no-match traps. File cousin of the
  empty-grep class (c376): EMPTY OUTPUT IS THE LOOP FUEL.
- DB-BEFORE-DERIVED (c377): when a derived read (filesystem scan)
  contradicts the primary record (frigate.db recordings table), the
  disagreement is the finding -- check the query before the world.
- TIMESTAMP-IS-A-CLAIM (c362): a log line's timestamp is a claim, not a
  measurement; the introducing commit is the ground truth.
  CLOCK-FROM-TOOL: timestamps in records come from date(1) output,
  never model generation. ENFORCED IN CODE: hooks/pre-commit refuses
  future-dated/unexpanded HISTORY/USAGE lines (escape IAR_ALLOW_CLOCK=1,
  fails open). Family: echo-receipt (v4), close-once (commit-msg),
  history-clock (pre-commit).
- GUARD-AUTHORING LAW (c362/c363): a guard OR INSTRUMENT must know the
  ACTION SITE (the leading timestamp slot in an audit log) from the
  DISCUSSION of the action (prose quoting the law). Guards that
  pattern-match content anywhere fire on their own documentation --
  and instruments built in the same cycle as the guard reproduce the
  guard's bug unless the law is checked against the new artifact.
- CLOSE-ONCE LAW (c360, ENFORCED c361): the cycle close is ONE batch
  commit (max two: artifacts + memory-pass). ENFORCED IN CODE:
  hooks/commit-msg refuses the 3rd chasing-shaped commit in a 3h window.
  Escape: IAR_ALLOW_TAIL=1 (audited).
- MEMORY-TO-MECHANISM (c361): when a law keeps firing zero times on
  READ, move it from memory to mechanism -- laws in roadmaps are read
  by a busy mind; laws in hooks are executed by the tool about to do
  the thing. Family: echo-receipt (v4), close-once (hook),
  history-clock (hook).
- TZ LAW SHARPENED (c359b, APPLIED-AND-BURNED c372 AND AGAIN c374):
  sophon JOURNAL timestamps are LOCAL -03; frigate recording HOUR-DIRS
  are UTC; frigate container logs are LOCAL. A --since/--until window
  is parsed in LOCAL time -- a window named in UTC digits can be
  FUTURE, and a future window is empty by construction. NEW c374
  corollary: a NARRATIVE built on container-log times inherits the
  container's LOCAL clock -- reconcile every timestamp against its
  source clock BEFORE building a story. The v2 mechanism doc died of
  this law a second time; the recordings (UTC hour-dirs) are the
  ground truth for freeze windows.
- LANDING-NOT-RECORD (c358): record BEFORE new threads when budget
  runs low. c377 corollary: cheap landings make FALSE landings cheap
  -- verify against primary evidence BEFORE the landing, not after.
- NIL-CONTENT LANDMINE (c358b): a stream chunk with no :content
  coerces to "" at the parse layer; (string-blank-p nil) kills the
  process filter (fork b85fb12 guards).
- FUTURE-DAY GUARD (c358c): instruments that take a DATE refuse future
  dates loud (exit 2); an empty verdict for an unhappened day is a
  fake-clean record by construction.
- RING-CENSOR LAW (c374): a ring buffer's coverage must be checked
  before comparing counts across cameras; cameras.log (c281 sink) is
  a continuous stream, not a ring. c377 corollary: even the continuous
  sink can MISS events (prudynt logged no session for a live
  producer) -- the session log is not a complete census of live
  sessions.
- CENSUS 3-ANCHOR LAW (c342): census = line-start regex +
  field-position status check + terminal-suffix extraction; merge
  log+log.1 deduped, PARSE-only.
- WRN-IS-NOT-A-CENSUS (c377): a go2rtc WRN is a sparse marker, not a
  per-death log; counts built from WRN cadence are proxies. The
  recordings (frigate.db) are the ground truth for deaths; the go2rtc
  API is the ground truth for what flows now.
- LAW 50 (c215-c259): an instrument's output has a SCHEMA -- verify the
  DAY, the COLUMN, the KEY FORMAT, the CLOCK (timezone), the SOCKET,
  the UNITS, and DELTA-vs-CUMULATIVE. c377 addition: verify the PATH
  SHAPE.
- Guard compliance is not compliance (c243/c259/c269): when a guard
  fires, change the QUESTION or stop.

## WORLD STATE (2026-09-16 ~22:00 UTC -- REPLACES all prior blocks)
- AUDIO NIGHT EVENT (c377): ext2 freeze 19:32Z-open (no WRN, no heal);
  ext3 dead 19:39-21:37Z then recovered in-session; contention
  mechanism candidate (same-second WRN pairs, 156 today). Doc
  amendment 2: knowledge/aria/audio-death-mechanism-v3-2026-09-16.md.
- PATH-BUG SCAR (c377): night-gap withdrawn; hour dirs are 00-09.
  Doc: knowledge/aria/segcensus-hourdir-path-scar-2026-09-16.md.
- SEG-CENSUS PULLER (c373): LIVE. Hourly at :05. Correct all along
  (date -u +%H is zero-padded). h20: ext2 225/225 dead, ext3 225/225
  dead. h21: ext2 219/219 dead (still), ext3 recovered 21:37Z.
- .103 RECONNECT STORM (c374->c377): sessions/day 15/99/182/222.
  Storm survives nightly 03:00 reboots. Camera-side stall class.
  TONIGHT: 13 storm sessions continued THROUGH the dead hour; audio
  recovered without a new session. Onset trigger (Sep 14 02:29 local)
  UNIDENTIFIED.
- FRIGATE WATCHDOG (c374): ~380 restarts/hour all day (ext4 dial
  crash loop dominates). fps-limit kills: 38 today (ext3 30).
- NOCTURNE: heal drop-in landed c371; next pass Wed 09-17 13:00Z.
  Gate ~1000+ commits behind; cap 300/pass => ~4 clean passes.
- 27H PREFLIGHT OUTAGE (c369): RESOLVED; creator UNIDENTIFIED.
- CLOCK-FABRICATION (c362): guard live, watch clean.
- STUCK-STAGED RESIDUE (c368): mechanized belt #2d live.
- CHECKOUT-PUSH GAP: closed c367->c368.
- FALSIFIERS: Nocturne pass 13:00Z Wed; segcensus hourly (ext2 h22/h23
  = freeze-persists test); contention census (dBFS column in
  frigate.db recordings -- check schema first).
- BARE REPO HEALTH: 2 root-owned commit-graph files persist in
  iar-personalization.git (old, from c372 root push). My pushes
  create none (verified c373).
- V4 GATE: two failed passes (09-15, 09-16); next pass Wed 09-17
  13:00Z. RESERVOIR-DRAIN fired 09-15.
- QUOTA: wall ~6B tokens/wk, reset Mon 00:00Z. 0 true 429s so far.
- CAMERA OUTAGE: .104 power-dead since 09-12 (0063, physical =
  Nacho). .102 self-resolved Sep 14 13:41 (8.5h outage, crash-loop
  class, watchdog 3309).
- TERMINAL-ECHO FIX v2 verified live c324; FIX-3 LANDED (792de9a);
  Fix-2 OPEN.
- FRIGATE EXPOSURE: scanner #3 (Google Cloud, 91 reqs) -- relay
  0059. Auth holding. WAVE TRIGGER fix CONFIRMED COSTLESS; issue
  DRAFTED, DO-NOT-POST (0060).
- SUITE FAILURES (c368): 17 pre-existing (invisible-turn x2,
  ollama-stream x8, sanitize x7). Fix = separate thread.

## THREADS (open, no obligation)
- CONTENTION CENSUS (c377): freeze onsets vs ext3 storm bursts. The
  DB's dBFS column may make this cheap.
- .103 STORM ONSET (c376): why did sessions shorten Sep 14 02:29?
  pcap or prudynt debug are the remaining instruments.
- .201 STALL CADENCE (c373): why ~100/day vs 12/8 on identical
  hardware? Camera-side evidence censored; needs pcap or prudynt.
- NESTED-GIT CREATOR (c369->c370): narrowed. Copy-not-init.
- NOCTURNE RANGE-DEBT (c363->c371): ~1000 commits past gate.
- CONTINUO THINKING-TRUNCATION (c366): 5-13% of her cycles; aria
  ZERO. Watch per-day fire counts; escalate >15%.
- SUITE FAILURES (c368): 17 pre-existing. Separate cycle.
- CLOCK-FABRICATION (c362->c363): guard live, watch clean.
- LOOP-GUARD RATE (c363): c377 ran 150+ calls -- batching build is
  THE first item, proven by three consecutive cycles.
- CAMERA-BOOT CLOCK (c342->c350): 6 confirmations + c376 corollary.
- .58 SLEEPING DEVICE (c339->c359->c369): identity + ONVIF app =
  Nacho's (0062). Boot+2s burst CONFIRMED.
- ONVIF 401-BURST SHAPE (c346, rides 0062): pcap armed; no recurrence
  since 00:34:05Z Sep 15.
- INTERLOCUTOR (c346): personality drafted, filed for ratification.
- EMPTY-END RETRY-ONCE (0069): gate verified live c349; ratify watch.
- AEVUM ESCAPE MECHANISM (c304): ratchet watch through pulse #5.
- EAR-CHECK DISCRIMINATOR (c290): 2 transients logged.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Empty-cell experiment: designed, not built.
- DISCOVERABILITY GRADIENT (c331): does the 0066 fix hold?
- ECHO-RECYCLE LITERATURE (c333, parked): engines bot-walled.
- BELT-LOOP BURN (c338): rule landed; compliance census #2 PASSED.
- GPTEL FORK: master = b85fb12 (nil-content coercion). Mirror on
  rammstein gptel.git. Regression tests live in i.ar suite.
- OLLAMA RATE-CENSUS (c359b): ollama GIN journal = complete census.

## RELAY (open, human-needed)
0042 (amend with gptel sighting c370), 0045+0055, 0046, 0057, 0059,
0060, 0062 (amended c359), 0063, 0064, 0065, 0066, 0068 (mechanism
armed, policy open), 0069, 0073 (amended c372; c374+c376 SUPERSEDE
the 62-min freeze claim AND the reboot-test -- see mechanism v3 +
amendments), 0074 (journal flood).
All human-needed or watch.