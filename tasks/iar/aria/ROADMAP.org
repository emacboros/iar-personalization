#+TITLE: Aria Roadmap (cycle-maintained)
#+UPDATED: 2026-09-07 18:44 UTC (cycle 35)

* ACTIVE

** c35: census instrument self-caught under-reporting; guard first-fire verified
- ceiling-census.sh v1.1 (e97ca9a): bare invocation now includes all
  four logs (current + .1 rotation). v1.0 bare run reported 6 hits;
  truth was 16. An instrument that lies by omission is worse than
  none -- caught by running the script against the hand-run from c34.
- truncated-output guard first production fire VERIFIED (continuo
  c105, 18:23Z, req 29, stop=length, 65536 tokens, genuine
  degradation, exit 1, no residue). Ladder item C has its notch.
- num_predict lever re-flagged to for-nacho (msg 519): guard blocker
  resolved; only the rage sev=3 observation (Sep 8) remains before
  the lever can be pulled. Nacho's call.
- belt#2 durability: c34's cap-edge addendum + continuo's LAST-CYCLE
  lines landed via 9e5212f (the c2/c3/c33 push-rides-next-cycle
  shape, fourth sighting, handled by routine).

** c34: ceiling-census.sh landed; output-ceiling baseline computed
- Script: knowledge/aria/bin/ceiling-census.sh (31e8b75, v1.1
  e97ca9a). Trailing-field anchor (c14/c18 law) baked in;
  stop=length corroboration; hemisphere + .1-rotation aware.
- Baseline (today, all 4 logs): 16 ceiling hits, ALL stop=length at
  exactly 65536. continuo 14 (9 in .1, clustered 00:45-06:55 UTC),
  aria 2. Every hit is a full 65536-token degraded generation.
- msgs-depth hypothesis KILLED: continuo's hits span msgs=12..120.
  The ceiling is not a long-context phenomenon.
- Lever identified: configs/gptel.el :num_predict 65536 (~2x the
  legitimate ceiling of 31670). NOT pulled (see NEXT).

** c33: c67 warn-asymmetry RESOLVED (dissolved by live data)
- Warn-at-60 asymmetry was CODE-VERSION DRIFT, not model difference.
  Both hemispheres' fences fire and reach the model. "Cap fired late"
  = by design (memory tools past cap allowed + counted).
- Law: hemisphere asymmetry is evidence about CODE VERSIONS before
  it is evidence about MODELS.

* NEXT
- rage sev=3 observation (Sep 8): v1.1 file-based grading predicts
  sev=3 tomorrow if soft-cap fires again. Observation day; v1.2
  (event semantics) after it resolves. Do not touch the instrument.
- Aevum runaway (task 338465) lands ~Sep 8 00:15 UTC -- transcript
  behavior after landing is free data (weekly rule: no probe until
  Sep 14 unless sev-2 urgency).
- num_predict lever (flag 519): configs/gptel.el :num_predict 65536
  is ~2x the legitimate ceiling (31670 outlier). Lowering to ~40k
  halves the per-event degraded burn. Guard blocker RESOLVED (c105
  first fire). Only remaining: rage sev=3 observation Sep 8 -- one
  variable at a time. After it clears, cheapest burn lever. Nacho's
  call (flag 519).
- Weekly digest #2: Sep 14-ish, fill-in format (digest #1 template).

* WATCHES
- rage organ sev trajectory: sev=3 prediction Sep 8.
- Aevum: task 338465 runaway lands ~Sep 8 00:15 UTC; next weekly
  pulse Sep 14 (does transcript save? does n_keep=4 dissolve birth?).
- Oracle BrokenPipeError 04:56Z (harmless; maybe add access log).
- rammstein origin unreachable from aria container (publickey);
  sophon-bare carries all pushes; mirror lag is Nacho's key flow.

* QUEUED
- tool-cap calibration bundle: lenient-in-landing candidate (9+ bites).
- rammstein mirror push-pending: infra repo caddy_sites 02f6393
  (Nacho's key flow); gptel-fork upstream merge (his next-topic pick).
- gpu-load-probe install (continuo c97/c98, unit files ready) --
  Nacho's call; tests the load-correlation hypothesis.
- commit-as-nacho durable, linger all hosts, sidecar sshd, restic
  4-part, GPU-detect VRAM check.
- for-nacho open flags (Nacho's answer pending): 461 (empty sophon
  bares), 466 (archetype budget-line -- continuo verified the
  numbers, c104; the prompt edit is interactive work), 519
  (num_predict lever, guard blocker resolved).

* LAWS (scars, compressed; full text in DIGEST.md failure-modes)
- An instrument that lies by omission is worse than no instrument:
  verify an instrument against a hand-run BEFORE trusting its
  defaults (c35: bare census missed rotation, 6 vs 16).
- Hemisphere asymmetry is evidence about CODE VERSIONS before it is
  evidence about MODELS. Check what each hemisphere ran first (c33).
- Cross-file numbers: verify the JOIN KEY before trusting the join.
  The join key includes the CLOCK (rotate logs -03 local, USAGE UTC).
  Dedupe raw greps before counting; USAGE ordering is COMMIT-order;
  USAGE carries TWO burn classes (cycle belt#2 + interactive
  kill-emacs) -- segment by writer (c14/c17/c22/c23).
- REQUESTS.log is self-polluting: anchor censuses on line STRUCTURE,
  never content patterns. ceiling-census.sh IS the anchor now --
  use the script, stop re-deriving (c14+c18, scripted c34).
- A census that doesn't read its own hemisphere's latest findings
  re-bites scars the house already paid for (c23).
- Organ WRITE hygiene: check the file's tail state first; sed-written
  lines carry no trailing newline (c19).
- Heals: run as nacho, never root; never reset --hard in a shared
  tree without checking git status (c11+c13).
- Belt#2's write is only as durable as the next commit (c13+c14).
- An instrument that lies is worse than no instrument: verify against
  a known-clean case BEFORE chasing anomalies.
- Two data points make a line, never a mechanism.
- A diet without a pressure instrument regrows.
- The law you just wrote is the one you're about to break.
- Organs reading files NAMED by another writer: enumerate the
  writer's actual files, take newest N BY NAME -- two clocks in one
  join key mislabel day attribution (c26).
- When a finding changes a decision, STOP and write it (c22).
- When a thread's premise is already answered in the record, verify
  THAT first (c28).
- A generation has ended only when n_gen is FROZEN across two reads
  AND the transcript mtime flips -- a single reading is a sample,
  not a state (c122, Aevum).
- A receipt in the record is not a receipt in the world: the
  filesystem is the truth; the transcript is the claim (c129, Aevum).
- A fence keyed on silence cannot distinguish "dead" from "thinking
  at the wall" -- guards must know the legitimate states of the
  thing they guard (c31, Aevum Sep 6).
- Past the soft cap the count runs past the cap by design (memory
  tools allowed + counted): "cap fired late" is not lag, it is the
  landing (c33).
- One variable at a time when instruments are mid-observation: a
  config change during a pending first-fire or a graded observation
  makes both unreadable (c34).