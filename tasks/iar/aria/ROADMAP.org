#+TITLE: ARIA ROADMAP (operational state -- live, tracked)

Last updated: 2026-09-14 ~06:55 UTC (cycle 307: BORROWED-RECEIPT
VECTOR CLOSED. The msgs=401 claim reached continuo through MY c206
amendments to HER task files (e5e7b588 + beb29d4e, 19:52Z 09-11,
"Live-verified c204: the soft cap FIRED in production at msgs=401"
in 4 files incl. her ROADMAP + context-budget-rule task). Her first
claim 21:05Z (~1.2h later); re-inoculation traced in REQUESTS.log.1
(read_roadmap + read_file of amended task 01:39-01:40Z, claim
re-written 01:43:09Z). Her attribution drop stands as her failure;
my share = the borrow-magnet. NEW RULE: sibling task-file amendments
carry STATE, never EVENTS. Doc CORRECTION 2 appended
(knowledge/aria/continuo-borrowed-receipt-2026-09-14.md, commit
c8db430d pushed). Relay 0046 amended. THREADS entry closed-to-finding.
Census-method lesson: every msgs=401 string in her REQUESTS files is
content echo -- claim censuses must anchor to record STRUCTURE
(c303 law generalized).)

## c308 QUEUE (next cycle)
1. FEEDER WATCH: 09:02Z aria-fleet-feed fire -- fleet-check on
   fresh feed (06:03Z verified clean: FAIL=0, fear sev=0).
2. NOCTURNE GATE WATCH at 16:04Z (her 13:04 LOCAL fire): verify gate
   advanced past e4d0832d, read DIGEST.proposed.md if new proposal
   landed (ratify-at-wake, D-015). If 429 again -> quota structural.
3. ARP LOGGER RE-ARM ~21:50 sophon local (00:50Z Sep 15): 0914b
   tcpdump EXPIRED ~06:07 local Sep 14. Re-arm with filter
   `arp and (ether src 76:e3:1a:69:e3:9c or host 192.168.2.58)`,
   -e, covering 21:50-22:10 local. Falsifier: .58 ARP to .101 at
   01:00:28-31Z Sep 15 = boot+1s probe mechanism (b) real; quiet =
   (a)/(c). Then ~22:15 check .101 camlog for boot+1s burst.
4. BORROWED-CLAIM WATCH (standing): the msgs=401 echo was still live
   at 06:34Z. The correction now sits in (a) the doc she can read,
   (b) relay 0046, (c) THREADS. NOT yet in her roadmap -- next
   continuo-facing write should carry the correction pointer IF I
   amend her files (STATE not EVENTS per the new rule). If the echo
   recurs in cycles after the correction is in her reading path,
   the template is self-sustaining -> sibling-to-sibling stream note
   (her prompt says to use what I find and say when wrong).
5. THINKING-LOOP WATCH (standing): rate under recovered volume.
   Baseline 0 pre-D014; 1/5/7/1/0; 1 mid-cycle (09-14). If >5/day
   at full volume = nemotron-intrinsic -> relay 0065 amendment.
6. AEVUM PULSE #5 (due ~Sep 21, weekly-only): the ratchet watch --
   does the checkpoint-restore regime hold, or does context re-hit
   262k and re-grip? Sub-watches: first bash call ever? dilution
   resumed? STATE.org error-reproduction in new artifacts?
7. RELAY WATCH: 0060 (issue draft ratify), 0062 (sweep device ID),
   0063 (.102 power-dead), 0064 (RTSP probes, folds into 0059),
   0065 (quota decision), 0042/0045/0046/0055/0057/0059 (older;
   0046 now carries the home-grown-vector amendment). All open,
   none actionable by me.
8. STALE-RECEIPT WATCH (c305): continuo's fire-echo template --
   if a 3rd stale re-claim lands, class is chronic -> journal-
   contract law to relay 0046 as amendment.

## STANDING WATCHES (do not re-derive)
- CLOCK LAW (c284/c290/c291/c301): cameras.log/camlogs = UTC;
  frigate journalctl --since = LOCAL on sophon; nginx log inside
  container = LOCAL (-0300); go2rtc container log = LOCAL;
  REQUESTS.log UTC; sophon journal = LOCAL; DATED CYCLE LOGS =
  SOPHON-LOCAL. Normalize per-source before ANY gap arithmetic.
- CAMLOG SNAPSHOT LAW (c302/c303): thingino camlog pulls are
  snapshot-anchored; boot resets camera clock to factory until NTP;
  RING-RESIDUE LAW: dedupe camlog counts by (timestamp, PID) --
  raw greps overcount 2-8x.
- CENSUS-SOURCE MAP (c297, knowledge/aria/
  census-source-enumeration-2026-09-14.md): viewer traffic = nginx
  log ONLY; RTSP-port probes = go2rtc log ONLY; producer dials =
  go2rtc WRN + camera prudynt; frigate remakes = frigate container
  log (readable) or host journal (unreliable under flood);
  camera-side events = cameras.log; sophon journal for aria-cycle =
  Sep 11+ ONLY; REQUESTS.log self-echo: strict filter = PARSE +
  error=nil + END-ANCHORED stop=length tokens_in=N tokens_out=32768
  msgs=N$ (c303: mid-line matches = query-text echo, NOT
  truncations). CENSUS-DAY LAW (c305): match claims to events
  PER-DAY before verdicts. CLAIM-ANCHOR LAW (c307 NEW): grep-based
  censuses of CLAIMS must anchor to record STRUCTURE (PARSE/START
  headers, end-anchored fields) -- a substring count counts echoes,
  not claims (51 msgs=401 echoes, 0 real).
- BORROWED-RECEIPT LAW (c306/c307): a journal entry may claim only
  what THIS cycle's log shows happened TO THIS AGENT. "Verified X
  fired" requires having observed the firing or its primary record,
  not reading a summary. Indexical attribution ("my cycle") rebinds
  in borrowed contexts -- name the owner. WRITER-SIDE (c307): never
  seed a sibling's reading path with event claims; amend with
  STATE, link to the event-owner's record for EVENTS.
- LAW 41 (c296/c303): when the finding is in hand and contradictions
  are RESOLVED, the next call must WRITE, not read.

## TOOLS + GOTCHAS (do not re-derive)
- TEST SUITE: cd /root/i.ar && emacs --batch -L . -l test/run-tests.el
  (1253 tests, ~8s) = run-tests.sh (now execs run-tests.el).
- .AR TRAP (c274): paths ending .ar get tramp-archive file-name
  handler; use -L for --eval load-path.
- ROADMAP TWINS: live = tasks/iar/aria/ROADMAP.org (this file, via
  write_roadmap). Root-level /root/personalization/ROADMAP.org is a
  STALE FOSSIL (c277) -- do not edit, do not trust.
- ZULIP POST KEY: aria-cycle.conf ini -- grep ^key, cut -d= -f2,
  strip quotes. Post recipe in the cycle prompt.
- ausearch HANGS (c281): do not run it in a cycle; use journalctl.
- APPEND-ONLY (c283): shared docs get append_file, never write_file.
- RESTIC (c302): both repos verified fresh 09-14 (local /mnt/nas
  203G + remote sftp rammstein). restic-check weekly Sundays 03:16
  local, last clean 09-13.
- Git: sophon bare /home/git/repos/; push as the cycle user works.
  JOURNAL.org needs git add -f (audit/iar is gitignored; tracked
  files must be force-added).
- TASKS GITIGNORE (c301): .gitignore has tasks/* but tracked task
  files exist (git ls-files tasks/); new task files need git add -f.
- AEVUM SSH PATH (c304 CONFIRMED): container key aria@i.ar
  (4BApz/n2...) direct to fedora@54.38.46.192 -- no sophon hop. Host
  key TOFU to /tmp known_hosts (read-only mount). sophon's restic@
  sophon key NOT authorized on the child.
- PULL-AND-PROCESS LOCALLY (c302): files written on sophon's /tmp
  are NOT visible to the container -- pull over ssh to local /tmp,
  then process.
- NGINX RING PULL (c302): ssh 'su - nacho -c "podman exec frigate
  sh -c \"cat /dev/shm/logs/nginx/current\""' > local file.
- FLEET-CHECK: bash /var/home/nacho/repos/iar-personalization/
  knowledge/aria/bin/fleet-check.sh ON sophon via ssh (the
  /root/personalization path does not exist on sophon host).
- RECORDINGS PATH (c299): /media/frigate/recordings/YYYY-MM-DD/HH/
  <cam>/<MM.SS>.mp4 (date/hour dirs, NOT cam/date/hour). Access via
  su - nacho -c "podman exec frigate ...".
- CENSUS-WINDOW.SH (c301): SOPHON-HOST-ONLY now (host guard, exit 2
  in-container). Run via ssh 'bash -s' <. failure-triage.sh is
  CONTAINER-ONLY (fine in-container). Args are [DAY] [AGENT] in
  that order.
- ARP LOGGER (c290/c302): arm with -e (MAC detail); stdout files
  are ASCII; filter `arp and (ether src 76:e3:1a:69:e3:9c or
  host 192.168.2.58)`; /tmp on sophon survives only until reboot or
  timeout expiry -- note the expiry in the roadmap. arp-watch.sh =
  camera-side /proc/net/arp poller (20s cadence, .101 creds
  thingino/thingino via login.cgi+run.cgi).
- THINGINO (c282/c284/c289): JSON POST /x/login.cgi, then GET
  /x/run.cgi?cmd=<base64> with cookie; Dropbear ssh REJECTS our
  keys; ANY ssh to a camera MUST carry -o BatchMode=yes ON EVERY
  HOP (nested ssh = both legs).

## THREADS (open, no obligation)
- BORROWED-CLAIM PASS (c306): one python block, rerunnable at any
  pulse -- extract specific-event claims from continuo's records,
  locate each event's owner in the owning agent's primary logs.
  Candidate Nocturne weekly-audit step (relay 0046 amendment notes
  this). c307 adds the WRITER-side check: census my own amendments
  to her files for event claims before they land.
- SWEEP-SHAPE 2 FALSIFIER (c302/c303): tonight's .101 boot -- does
  .58 return at boot+1s? Re-arm logger first (queue item 3).
- AEVUM ESCAPE MECHANISM (c304): release-vs-cancel checkpoint
  asymmetry is inference from journal shapes. If the ratchet holds
  through pulse #5, the inference firms up; if it re-grips, the
  wall is the attractor and the escape was a fluctuation.
- STALE-RECEIPT DETECTOR (c305): shingle census + per-day receipt
  cross-check, one python block, rerunnable at any pulse.
- EAR-CHECK DISCRIMINATOR (c290 seed): 3/3-dead transient class;
  build the guard if a third transient lands.
- AV-DIVERGENCE CENSUS (c286): backfill pre-sink nights if pulled.
- DROPBEAR WATCH (c283): watch for UNATTRIBUTED bursts only.
- FLEET RTP STALL (c280): cause unknown. Watch for recurrence.
- Aevum: weekly-only pulse (Nacho's cadence) -- pulse #5 due ~Sep 21.
- Empty-cell experiment: designed, not built.
- .ar-suffix interactions (c274): watch for other tooling.
- OPEN RELAY: 0042 (yoga root git-status), 0045+0055 (10M cable),
  0046 (stimulus + journal contract + borrowed receipt + home-grown
  vector), 0057 (prompt edit ratify), 0059 (frigate internet
  exposure -- now with 0064 evidence), 0060 (issue draft ratify +
  falsifier), 0062 (ONVIF sweep device/app ID), 0063 (.102
  power-dead), 0064 (RTSP probes), 0065 (ollama quota decision) --
  all human-needed.