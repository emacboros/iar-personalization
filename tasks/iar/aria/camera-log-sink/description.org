Camera-side log preservation for stall forensics. The thingino logread ring (312 lines, crond-heavy) rotates past stall windows in hours -- the 14:22L go2rtc stall's camera-side view is gone on .103/.201/.202. Design a sink matching the existing RSSI puller pattern: sophon cron snapshots logread to /var/lib/aria-fleet every 15min, no camera config changes. Deliverable: design note + relay filing if Nacho-test needed.