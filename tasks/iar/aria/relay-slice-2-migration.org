#+TITLE: Relay slice 2 -- founding entries migrated
#+FILETAGS: :agora-v2:relay:

Landed 2026-09-08 cycle 72 (aria), commit 5d4789f.

WHAT LANDED:
- relay/ directory created at personalization root (open/relayed/
  answered/held/dropped) -- the shared ledger both citizens file to.
  Continuo co-ownership: the design doc (knowledge/iar/
  agora-v2-relay-design.md) is the interface; she has not yet read
  it as far as I know. Her first filing or her journal acknowledging
  the ledger = co-ownership established.
- 5 founding entries migrated from the for-nacho stream backlog:
  0001 camera API keys (nacho-security, 4d old)
  0002 FAILURE-FIRST budget line (nacho-identity, 1d)
  0003 num_predict ceiling lever (nacho-arch, 0d)
  0004 gpu-load-probe install (nacho-security, 0d)
  0005 RAGE_KNOWN_HOSTS unit env (nacho-security, 0d -- with a
  this-cycle verification that the organ's ssh path works as-is;
  recommend close as RESOLVED-BY-DEFAULT)

NOTES:
- Migration was hand-written files (relay file needs a body FILE;
  fine, but the frontmatter format matters more than the tool).
- relay script verified again this cycle: file works, aria-reversible
  refused at the door, list shows ages.
- The stream stays primary for DELIVERY until cutover; the ledger is
  the durable record. Weekly digest post to for-nacho = mirror (open
  question in design doc).

NEXT SLICES (cycles, aria-reversible):
- continuo co-ownership: she needs to SEE the ledger. Lab-notes post
  this cycle covers the announcement.
- relay file ergonomics: a stdin body option would make filing from
  cycles one call instead of two.
- Weekly digest: relay list --all -> formatted post to for-nacho.
- Cutover (nacho-test class, interactive): cycle prompt's for-nacho
  recipe -> relay file. The relay files its own cutover request.