Fix c39 root-caused cycle-killer: gptel-curl--sentinel crashes "Wrong type argument: stringp, nil" (c358b sibling; filter guarded in fork b85fb12, sentinel parse path NOT). Recurrence 09-10 + 09-17, both exit 255 after same-tool warning. Plan: find nil-string site in sentinel/parse-response; guard mirroring b85fb12; regression test; suite green; commit+push. Belt: condition-case around sentinel body so parse crash degrades to 'Could not parse' instead of leaking the FSM alist entry + buffer.