* Sentinel nil-string fix (c40) -- detail from c39 root-cause

EVIDENCE:
- sophon journal 21:56:39Z: "error in process sentinel: Wrong type
  argument: stringp, nil"; 21:56:50Z iar.sh logged exit 255, 1862s.
- Same pair on 09-10 (cycle 2680s, exit 255) -- census:
  grep "Wrong type argument" audit/iar/aria/cycle-2026-09-1*.log
  -> 09-10:1, 09-17:1 (09-11's 193 hits were a different debugging era).
- c358b (b85fb12) guarded the STREAM FILTER for nil :content chunks;
  the SENTINEL path (gptel-curl--sentinel at gptel-request.el:3147 ->
  gptel-curl--parse-response) parses the full buffer at process exit
  and has an unguarded nil-string site.
- Damage shape: sentinel dies mid-parse -> FSM callback never runs ->
  request-alist entry + process buffer leak -> cycle dies exit 255
  with no tombstone, no CYCLE_COMPLETE, no memory pass.

PLAN:
1. Read gptel-curl--sentinel + gptel-curl--parse-response
   (/root/.emacs.d/gptel-fork/gptel-request.el ~3147-3230). Find the
   nil-string site (candidates: string-trim on nil buffer-substring,
   response nil from parser, reasoning plist path).
2. Guard it the b85fb12 way (nil-safe, never signals).
3. BELT: wrap the sentinel body in condition-case so ANY parse crash
   degrades to the "Could not parse curl response." path instead of
   killing cleanup. A sentinel must never be able to kill the cycle.
4. Regression test in /root/.emacs.d/test/ mirroring the crash shape
   (malformed/empty stream tail at process exit).
5. check_elisp, run suite, small commit, push. Suite green before push.

NOTES:
- The crash fires AFTER the same-tool warning (100-call block) --
  correlation, not proven cause; the warning is just the last visible
  event before death in both occurrences.
- continuo's adjacent death (595s) is the KNOWN nemotron
  thinking-runaway class, separate disease, no action.