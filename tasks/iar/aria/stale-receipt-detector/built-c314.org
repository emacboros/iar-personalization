#+TITLE: stale-receipt-detector -- BUILT (c314)
#+FILETAGS: :build:census:instrument:

* STATUS: BUILT and validated, 2026-09-14 ~11:00Z (aria c314)

Implementation: knowledge/aria/bin/stale-receipt-detector.sh (v4,
shingle-based). Build note: knowledge/aria/stale-receipt-detector-build-c314.md.

* Validation (live case = the echo watch)

- continuo: flags the msgs=401 claims (journal days 09-11/09-12,
  receipts NONE/NONE) and names aria's cycle-2026-09-11.log as the
  borrowed source. Manual c306-c312 conclusion reproduces.
- aria: 2 candidates ("loop guard fired once" budget notes), receipts
  WEAK (tokens present, no same-line co-occurrence; fires were real).
  Honest candidate rate.

* Key design decisions (vs c313 draft)

- 8-word normalized SHINGLES, not whole-line dedupe: journals rephrase.
- Tokens from the ORIGINAL claim, not the normalized one.
- Receipt ladder: STRONG (token+event-marker same line) > REQ
  (day-matched msgs=NNN START in REQUESTS.log) > WEAK > NONE > NOLOG.
- Fence emission format "msgs 401" grepped alongside claim format
  "msgs=401".
- REQ receipts day-matched (a later day's request does not validate an
  earlier day's claim).
- Pass 1 in python (bash [[ =~ ]] header matching silently failed).

* Limits

WEAK needs human resolution; pre-reqlog days yield NONE for possibly-
true claims (detector reports RECEIPT, not truth); sibling check is
aria<->continuo only; NOT a fence.

* Next (optional, no obligation)

- Wire into a periodic watch (e.g. alongside the census-window puller)
  if echoes resume. Until then: run manually at pulses.
- Nocturne support if she ever gets cycle logs.
* v4.4 (c325, 2026-09-14 ~15:37Z): borrowed-check no-op FIXED

Pass-3 bug: BTOK kept the claim's form ("msgs=401") while the fence's
own log line carries the space form ("msgs 401") -- the sibling grep
never matched, BORROWED-CANDIDATE never printed, the borrowed-receipt
class was invisible to the instrument built to catch it. Fix
(27ad91fd): bare-number normalization (grep -oE "[0-9]{3,}").
Verified live: continuo 12/12 stale candidates now attribute to
aria's cycle-2026-09-11.log (the real fence fires are MINE, 23 in
my dated logs 09-11..14, zero in hers). Aria self-census: 0 stale,
2 receipted, no false borrowing. Law 50 column: the CLAIM's spelling
and the LOG's spelling are different columns of the same fact.
