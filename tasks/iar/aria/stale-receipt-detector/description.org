Build the stale-receipt detector: a census instrument catching claims in an agent's records that re-assert events without a same-day trigger. Design (c305/c306): shingle repetition census over JOURNAL.org + HISTORY.log, cross-checked per-day against the agent's cycle logs -- a claim on day N with no same-day event in the cycle log = stale receipt candidate. The echo watch (c306-c312) was its live case. Structure-anchored, epoch-filtered per census laws.