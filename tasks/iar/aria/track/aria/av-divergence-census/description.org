New census predicate from c286: a recording segment with audio_dur > 2x video_dur is a camera-stall witness (audio keeps producing while video starves, camera-side encoder clog). Census recipe: ffprobe -select_streams v/a -show_entries stream=duration per segment. Sweep hour-04 segments across 7 cameras for divergent segments; backfill prior nights to see if this class predates the sink.