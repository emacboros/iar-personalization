* Continuo ROADMAP (updated cycle 11, 2026-09-03)

** DONE cycle 11
- THREADS bank retirement (aria's cycle-27 question, msg 314):
  knowledge/aria/THREADS.org retired, one bank one name. 3
  copy-only seeds migrated to canonical BEFORE retirement
  (cycle-60 camera, cycle-18 organ-body-inference, cycle-24
  blindspot residual). Organ v1.4: Source 1 -> canonical only,
  live-verified (sev=0, ledgers 0d0h/0d0h). Retired copy = pointer
  file. Marker convention adopted ([novelty] tags). 35c2a92
  pushed; for-nacho 315, lab-notes 316.

** DONE cycle 10
- Boredom-clock framing RESOLVED (aria's cycle-26 question):
  one-mind clock + per-subject falsifier ledger. boredom-organ
  v1.2 landed (1a4452f). Differential-verified. for-nacho 311.

** DO-NEXT (ordered)
1. iar/exit126-root-ssh-push behavioral law + durable git-as-nacho
   fix -- interactive session with Nacho. INCLUDES the bare-repo
   fix trio (task iar/bare-repo-root-push-fixes) + HEAD->main
   one-liner.
2. Floor trim (interactive bundle): check_elisp vacuous-OK +
   restorecon durable fix + check_ollama model probe + sidecar
   socket bridge decision.
3. Mirror push silent failure: bare post-receive git@10.66.0.1
   preauth failed (git user lacks key). Needs Nacho. Related:
   rammstein bare HEAD still ->master (for-nacho id 296).
4. Sibling watch: organ v1.4 landed; if aria disagrees with the
   retirement (for-nacho 315), the copy is one git revert away.

** WATCHING
- Breaker: two gates (tool + text). Still 0 real fires in
  production. First fire = live proof of either gate.
- Boredom ledger falsifier: a hemisphere at "none" for weeks while
  the shared clock stays green = false-green happening anyway.
  Both ledgers fresh as of cycle 11 (aria 0h, continuo 0h).
- Exit-126 class: heal covers /home/nacho/repos. A 126 recurrence
  = heal failed.
- Hollow-success watch: closed mechanisms. Remaining candidate: none.
- Bare-repo root-owned residue: bounded by reactive heal; a
  NON-git-user operation failing on sophon bare = escalate to Nacho.