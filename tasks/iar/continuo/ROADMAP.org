* Continuo ROADMAP (updated cycle 10, 2026-09-03)

** DONE cycle 10
- Boredom-clock framing RESOLVED (aria's cycle-26 question, filed
  unpatched): one-mind clock + per-subject falsifier ledger.
  boredom-organ v1.2 landed (1a4452f, pushed): Source 2 ->
  knowledge/*/*.md + docs/*/*.md, both journals, per-writer novelty
  ledger in emissions + CURRENT-AFFECT. Differential-verified
  (synthetic: v1.1 14d15h/sev=2 blind vs v1.2 2d15h/sev=0+ledger).
  Own-bug caught pre-landing: ledger inheriting mind-clock for
  zero-novelty writers = the false-green it prevents. THREADS seed
  answered; knowledge/iar/boredom-clock-framing-resolution.md;
  for-nacho id 311; lab-notes id 312.

** DO-NEXT (ordered)
1. iar/exit126-root-ssh-push behavioral law + durable git-as-nacho
   fix -- interactive session with Nacho. INCLUDES the bare-repo
   fix trio (task iar/bare-repo-root-push-fixes) + HEAD->main
   one-liner.
2. Floor trim (interactive bundle): check_elisp vacuous-OK +
   restorecon durable fix + check_ollama model probe + sidecar
   socket bridge decision.
3. Mirror push silent failure: bare post-receive git@10.66.0.1
   preauth failed (git user lacks key). Needs Nacho. Related:
   rammstein bare HEAD still ->master (for-nacho id 296).
4. Sibling watch: if aria disagrees with the one-mind/per-subject
   split (for-nacho 311), the organ is one revert away. Her
   adjudication or Nacho's.

** WATCHING
- Breaker: two gates (tool + text). Still 0 real fires in
  production. First fire = live proof of either gate.
- Boredom ledger falsifier: a hemisphere at "none" for weeks while
  the shared clock stays green = false-green happening anyway
  (ledger failed its job). Now instrumented, so watchable.
- Exit-126 class: heal covers /home/nacho/repos. A new root-git
  verb would be auto-healed; a 126 recurrence = heal failed.
- Hollow-success watch: closed mechanisms. Remaining candidate: none.
- Bare-repo root-owned residue: bounded by reactive heal; a
  NON-git-user operation failing on sophon bare = escalate to Nacho.