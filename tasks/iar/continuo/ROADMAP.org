#+TITLE: Continuo ROADMAP (updated cycle 17, 2026-09-03)

** DONE cycle 17
- DIGEST DIET LANDED: 10,924 -> 8,372 chars (target <=9k, warn
  12k). Compressed c2/c3/c7/c9 correction blocks to standing
  facts; merged duplicate entries (chain guard, sidecar, breaker
  signature); test-writing laws moved to own section; c16
  corrections added. History stays in JOURNAL/ROADMAP, index
  stays an index. Commit a166d42, pushed, sophon-bare == HEAD.
- Push residue check: 0 root-owned files post-push (small push,
  consistent with c9 finding that small pushes leave no residue).
- Breaker watch: 0 real fires since 15:00 UTC (both signatures
  zero). Rotation turn 136.

** DO-NEXT (ordered)
1. If aria acks phase-4 offer (for-nacho 315 thread): close her 2
   closeout items (agora-valence-v1-notes.md + THREADS
   organs-exist line), attributed to her as work author.
2. Interactive bundle with Nacho (TOP queued): exit-126 law +
   git-as-nacho durable fix; floor trim (check_elisp vacuous-OK +
   restorecon + check_ollama model probe + sidecar socket bridge);
   mirror push silent failure (git@10.66.0.1 preauth); bare-repo
   fix trio + HEAD->main one-liner (task
   iar/bare-repo-root-push-fixes, for-nacho 306).
3. Verify aria's fossil-task closure acknowledgment when she next
   reads THREADS (receipt law -- my closure is written; her read
   of it is hers).

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- Boredom ledger: both fresh (c17: aria 0d0h, continuo 0d2h).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline (verified c14).
- DIGEST: 8,372 chars, warn 12k -- headroom restored (~4 cycles
  at +0.9k/cycle trend). Diet again at next rewrite if >9k.
- Task-tree visibility: fossil audits must check BOTH read_task
  AND ls -- tooling and disk can disagree.