#+TITLE: Continuo ROADMAP (updated cycle 35, 2026-09-04)

** DONE (cycle 35)
- Bundle item 1 CLOSED: PARSE-line token fields. Code 1afc21a (c34) +
  positive-path test 9b2c637 (c35), suite 1016/1016, pushed sophon-bare,
  remote verified (tip 9b2c637, HEAD->main, 0 root-owned objects).
  Token meter complete; c33 token-census bias law OBSOLETE.
- Failure census (Nacho directive): 103 succeeded, 0 failed since Sep 3.

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (task iar/continuo/interactive-bundle-nacho):
   tool-cap calibration (7 data points, now with COMPLETE token data via
   PARSE lines) + cadence price; STATE.md injection mismatch (personality
   edit); /tmp-copy race; exit-126 restorecon; git-as-nacho identity;
   delayed-heal sweep + mirror-loop note; floor trim leftovers.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT (bundle item).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: baseline FAIL=0; aria c2 found ext4 NEW deafness
  (her thread, boundary 23:53:48 UTC Sep 3, post-rolling-reboot).
- DIGEST: 10,714 chars, warn 12k -- diet at next close if growth continues
  (per-request price verified c33).
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE compound
  ssh per question; dump to /tmp ONCE (chain guard tripped c32).
- Agora key capture (c35 scar): ssh output to FILE first, then read --
  command substitution strips trailing newlines and can eat the payload.
- REQUESTS.log census law (c32): anchor line-start REQ tokens; validate
  vs USAGE line first. Token counts now on PARSE lines (c35).
- Test-hygiene law (c30): tests that set globals MUST restore in
  unwind-protect. let vs let* on sibling-referencing bindings.
- Pulse c35: all green (timer/agora/ollama active, tripwire 0, disk 24%,
  rotation 172).