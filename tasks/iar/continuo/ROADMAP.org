#+TITLE: Continuo Roadmap
#+UPDATED: 2026-09-03 08:25 UTC (cycle 6: tool-cap-overcorrection closed with production receipts)

* IMMEDIATE (next cycle, in order)
1. Breaker production watch (standing): grep sophon journal for
   breaker messages. Count still 0 since landing (4c879a2) --
   suite-verified, unexercised. First real fire = live verification.
2. Floor trim prep: census + injection-trim-analysis ready.
   INTERACTIVE session with Nacho required (core .el). Bundle with:
   chain-guard false-positive fix (convergence reset) + check_elisp
   vacuous-OK. Evidence accumulating: guard blocked Aria's ssh
   probes + for-nacho POST (her cycle 11), my git push (this cycle,
   twice).

* DONE (cycle 6, 2026-09-03)
- TOOL-CAP-OVERCORRECTION CLOSED. Verified against primary evidence
  instead of re-implementing: sophon journal Sep 3 shows 0 cap-60
  exits since the fix (was 26 in 3 days), warn@60 fires and reaches
  the model in prod (continuo 04:23, aria 04:41), healthy cycles
  complete at 20-134 calls with zero exit-1. Docs already accurate
  (agents.md row). Task tree removed. Commit 8733f4a.

* DONE (cycle 5, 2026-09-03)
- CONTEXT-GROWTH CENSUS COMPLETE. Verdict: per-tool trim is dead --
  growth above floor is ~0.5% of burn; burn = (floor+context) x
  request count. Slope front-loaded then flat (no compounding).
  The 253.9k-token runaway (Sep 2, 169M tok, 70% of aria sample)
  predates all fences; zero msgs>400 since. Breaker landed but
  never fired in production (unexercised, not unverified).
  Token-cap at 80k NOT justified (healthy sessions end 30-58k).
  Report: knowledge/iar/context-growth-census-2026-09-03.md.
- invisible-cycle-fences task CLOSED (A+B+C+D all landed; spec
  said TASK COMPLETE; task tree removed).

* DONE (cycle 4, 2026-09-03)
- CHAIN-GUARD HOOK-ORDER FIX LANDED + VERIFIED. Bridge =
  run-hook-with-args-until-success (short-circuits first non-nil).
  Old order: chain guard ran FIRST, blocked call never entered
  history, count frozen at "10 times", hard stop unreachable.
  Differential proof: old order fails both new tests (8/10), new
  order 10/10. Suite 988/988. 44da3c9 pushed, docs row 5bb2de7.

* DONE (cycle 12)
- CENSUS CORRECTED (knowledge/iar/usage-census-2026-09-03.md):
  REQUESTS.log is a debug trace (~26% coverage); USAGE.log is the
  meter. aria 347.3M/4,843/53; continuo 73.4M/1,937/31. Floor
  share 26%/36%.
- CAP-WINDOW WATCH CLOSED: 61-req truncation signature dead.

* OPEN THREADS
1. check_elisp vacuous-OK: interactive-session file.
2. Aevum weekly (Sep 9) is aria's, not mine.
3. Chain-guard false-positive class: same-tool chain counter does
   not reset on convergence. Evidence: aria cycle 11 (ssh probes +
   for-nacho POST blocked), continuo cycle 6 (git push blocked x2).
   Core .el -- interactive session file.

* LAWS ACCUMULATED
- Check git stash list / fsck before declaring work lost.
- A truncated read_file view is not the file; read the region you edit.
- A green check that never touched the code is not a check.
- Measure the floor before trimming the fat.
- A fence that fires but whose return value is discarded is a fence
  that never fired. The block must BE the return. (5520434.)
- Unescaped quotes in a docstring terminate the string.
- Chain guard fire on edit+test iteration: batch the edit, run the
  suite once. The fence is right.
- Census from USAGE.log, not REQUESTS.log. (Cycle 12.)
- Verify a fence in production, not only in the suite. (Cycle 12.)
- Buffer-local state + hooks that run in another buffer = state
  that lives in the wrong vantage. (Tracker class, cycle 2.)
- A guard that blocks without escalating is a screen door. Test the
  WIRING (hook order, registration), not only the math. (Cycle 3.)
- When a fence is silent, test it on yourself. A frozen count is a
  different bug than silence. (Cycle 3.)
- Differential tests that bypass the wiring certify the wrong layer.
  (Cycle 3; second instance cycle 4 -- the sim bypassed the bridge.)
- Bridge law: run-hook-with-args-until-success short-circuits at the
  first non-nil return. Hook ORDER among guards is load-bearing
  whenever one guard's logic assumes another already ran. (Cycle 4.)
- Census law (cycle 5): burn = (floor + accumulated context) x
  request count. Per-tool result trimming has ~0.5% headroom; the
  floor and the request count are the only real levers. Measure the
  distribution before designing a cap: a threshold that sits above
  the healthy p90 is a no-op; one below it taxes every cycle.
- Verify-before-rebuild law (cycle 6): before implementing a fix
  designed in a previous cycle, check whether the world already
  contains the receipt. A task whose fix landed may only need its
  verification closed -- and verification against primary evidence
  (production logs) is worth more than a second implementation.
- A guard's false-positive cost is real work: blocked probes, a
  channel switch, a blocked push. The guard is right about the vice
  and wrong about the instance; convergence must reset the counter.
  (Aria cycle 11, continuo cycle 6.)