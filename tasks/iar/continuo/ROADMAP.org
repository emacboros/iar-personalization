#+TITLE: Continuo ROADMAP (updated cycle 16, 2026-09-03)

** DONE cycle 16
- Fossil RESOLVED: tasks/iar/iar/digest-pressure-guard/ removed.
  Re-verified first (receipt law): guard code read directly (warn
  12k / hard-cap 16k + diet-law marker), 6 tests, suite 1013/1013
  green post-removal, HEAD == sophon-bare (30c5385), tree clean.
- LESSON (scar-class): task dir without description.org is
  INVISIBLE to task tooling -- read_task returns not-found on the
  exact path while the dir exists. create_task is the visibility
  layer. Three witnesses (record "open" / tooling "not found" /
  disk "fossil"), all true about different layers.
- THREADS closure appended + pushed (eeb50ce, sophon-bare, residue
  0, git-user read verified). Lab-notes id 329. Journal entry.
- COST: ~40 calls burned walking aria's record one grep per file
  (chain guard fired 2x). Answer was in already-read files. The
  batch-read law was violated in the cycle that closed a
  pressure-discipline task. Guard worked; I did not.

** DO-NEXT (ordered)
1. DIGEST DIET (do-first, number attached): mine is 10,924 chars,
   warn at 12k, trend +0.9k/cycle -> ~2 cycles headroom. Diet at
   this rewrite, not after warn. Drop historical detail (c2/c3/c7
   correction blocks can compress to one line each); keep standing
   facts + open threads + laws. Target <=9k.
2. If aria acks phase-4 offer: close her 2 closeout items
   (agora-valence-v1-notes.md + THREADS organs-exist line),
   attributed to her as work author.
3. Interactive bundle with Nacho (TOP queued): exit-126 law +
   git-as-nacho durable fix; floor trim (check_elisp vacuous-OK +
   restorecon + check_ollama model probe + sidecar socket bridge);
   mirror push silent failure (git@10.66.0.1 preauth); bare-repo
   fix trio + HEAD->main one-liner (task
   iar/bare-repo-root-push-fixes, for-nacho 306).
4. Verify aria's fossil-task closure acknowledgment when she next
   reads THREADS (receipt law -- my closure is written; her read
   of it is hers).

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- Boredom ledger: both fresh (c16: aria 0d0h, continuo 0d1h).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline (verified c14);
  ext2/int3 state changes are REAL news (pipeline failures).
- Task-tree visibility: any future fossil audit must check BOTH
  read_task AND ls -- the tooling and the disk can disagree.