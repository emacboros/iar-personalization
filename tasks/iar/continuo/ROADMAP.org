#+TITLE: Continuo ROADMAP (updated cycle 36, 2026-09-04)

** DONE (cycle 36)
- USAGE meter poison FOUND+FIXED: c34's input=260928629061 was
  false (true 5330014). Cause: model echoes meter field names
  (c34 edited the meter); loose first-match regex captured echo +
  adjacent digits (dates, epoch ids, total_duration ns). Fix
  6cb09fa: quoted JSON key anchor + last-match, 4 regression
  tests, suite 1020/1020, pushed sophon-bare, remote verified.
- Reconciliation: PARSE dedup == USAGE EXACT on clean epoch
  (1106733==1106733). Floor ~12.8k confirmed; growth 340-620
  tok/req. c33-era floor numbers survive via RESPONSE replay.
- c35 DONE carried: bundle item 1 (PARSE token fields) closed.

** DO-NEXT (ordered)
1. Lab-notes post FIRST next cycle (deferral law): meter poison
   fix + reconciliation result.
2. eval_count accounting gap (NEW, this cycle): USAGE c34
   output=33558 vs dedup eval_count sum 14347. Hypothesis: fork
   accumulates eval_count across multi-turn tool-use requests
   (gptel--ollama-update-tokens sums :tokens-full), dedup takes
   last-per-id. Verify which is "true"; fix or document.
3. Interactive bundle with Nacho (task iar/continuo/interactive-bundle-nacho):
   tool-cap calibration (7 data points; token data now COMPLETE
   and HONEST) + cadence price; STATE.md injection mismatch;
   /tmp-copy race; exit-126 restorecon; git-as-nacho identity;
   delayed-heal sweep; floor trim leftovers.
4. Zulip backup gap: Nacho's call (pg_dump timer).
5. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: baseline FAIL=0; ext4 self-healed (aria c3);
  e2/int3 restart remains Nacho's call.
- DIGEST: 10,714 chars, warn 12k -- diet at next close if growth
  continues (per-request price verified c33).
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE
  compound ssh per question, dump to /tmp ONCE.
- Agora key capture: ssh output to FILE first (c35 scar).
- REQUESTS.log census law: anchor line-start REQ tokens; validate
  vs USAGE line. NEW (c36): USAGE numbers themselves can be
  poisoned by model echo of meter field names -- cross-check
  USAGE against PARSE dedup before trusting either. Quoted-anchor
  + last-match fix is live from 6cb09fa forward.
- Test-hygiene law (c30): tests that set globals MUST restore in
  unwind-protect. let vs let* on sibling-referencing bindings.
- Pulse c36: all green (timer/agora/ollama active, tripwire 0,
  disk 24%, rotation 174).