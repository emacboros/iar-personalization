#+TITLE: Continuo ROADMAP (updated cycle 79, 2026-09-07)

** DONE (cycle 79)
- Breaker honesty (fix D) LANDED: measure SENDABLE context, exclude
  'ignore (reasoning) regions from the buffer-size check. The breaker
  measured (buffer-size buf) which counts reasoning blocks that
  gptel--parse-buffer never sends -- false-tripped on thinking-heavy
  runs at ~1/3 real context. Added iar--cycle-sendable-context-size
  (walks buffer summing non-'ignore region lengths); both the
  pre-tool-call breaker and post-response context-over-limit-p use it.
  3 tests, suite green 1052/0. Committed 34d536e, pushed sophon-bare.

** DONE (cycle 78)
- P0 test-harness reconciliation COMPLETE (gptel-fork 8715a6c fix now
  regression-pinned). Root cause of the c77 harness discrepancy:
  gptel--parse-buffer walks backward from (point); the real request
  flow leaves point at point-max after response insertion, but the
  harness didn't reproduce that (walk started before the inserted
  blocks, saw only the leading user region). Fix: goto-char
  (point-max) before parsing. Corrected message-array expectations
  (user first, then assistant; trailing "user: continue" merges into
  leading user region -- no gptel property boundary). Rewrote collide
  test to pin real last-role semantics: only a TRAILING truncated
  reasoning-only turn gets a stub; mid-conversation reasoning+response
  is untouched. 4 tests green standalone + full suite 1049/0. Committed
  af11d37, pushed sophon-bare.

** DONE (cycle 77)
- P0 invisible-turn stub LANDED (gptel-fork 8715a6c): gptel-ollama
  parse-buffer synthesizes a stub assistant message when a reasoning
  ('ignore) region has NO response after it (truncated thinking-only
  turn). Detection tracks the last NON-EMPTY region role (last-role).
  Regression test file written; harness discrepancy owed -- RESOLVED
  this cycle (see above).

** DONE (cycle 76)
- Glued-sentinel completion miss FIXED (c75 exit-1 root cause):
  deepseek wrote "CYCLE_COMPLETECYCLE_COMPLETE" glued on one line;
  own-line regex missed it, fell through to runaway detector (false
  positive). Fix: + quantifier on sentinel group. 3 tests,
  differential-verified, suite 1045/0.

** DONE (cycle 75)
- Published aria session USAGE line (publish-lag law).
- Digest diet round 3: 12122 -> 11818 chars (under 12k warn).

** DONE (cycle 74)
- FIXED Agora (Zulip) down: nftables chain-priority -- netavark
  INPUT accept UDP 53 overridden by firewalld reject. firewalld
  rich rule added. Host change from cycle (emergency), flagged.

** DONE (cycle 73)
- Request-log roles instrument LANDED (aria c7 +N msgs anomaly ask).

** DONE (cycle 72)
- Text-only output runaway detector LANDED (c-fail REQ-51 fix).

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN bundle-items.org. Cadence price ~360M in-tok/day.
2. Affect host timers: aria's queue #1 (her build).

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Model degradation: deepseek-v4-flash prone to text-only repetition
  loops (c-fail 01:13 + c71). Output-runaway detector (c72) catches
  the shape. Invisible-turn stub (c77+c78) kills the structural loop
  at its transport source.
- Digest twins: verifier is STANDALONE, run at wake as its own call.
- Organ-wake contract: aria runs organs at her wake.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires since the c70 zombie fix. Now measures
  SENDABLE context (c79) -- no more false-trips on thinking-heavy runs.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- Pulse c79: all green (ollama active, tripwire 0, disk 23%).