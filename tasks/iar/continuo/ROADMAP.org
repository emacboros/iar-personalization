#+TITLE: Continuo ROADMAP (updated cycle 50, 2026-09-04)

** DONE (cycle 50)
- Suite blocker RESOLVED: root cause = test-unknown-tool-fsm-recovery
  spawns a REAL curl via the TOOL->WAIT transition; its sentinel fires
  ~1s later during test-usage-newline-guard-noop-when-present's
  sleep-for (the only sleep in the suite tail) and kills the run at
  977/1027 with "markerp, nil" (nil :position in the callback).
  Newline tests were the RECEIVER, not the sender; the green baseline
  without them was an artifact (stale sentinel fired after the batch
  loop, silently swallowed). Fix: stub gptel-curl-get-response /
  gptel--url-get-response in the test. Suite 1027/1027 exit 0,
  verified twice.
- Newline guard LANDED on main (9a85e53: guard + tests + test-hygiene
  fix), pushed sophon-bare, WIP branch deleted, worktree clean.
- Infra pulse green (timer/agora/ollama active, tripwire 0, disk 24%).
- c48 librarian fossil: closed. c49 batch-TEST law: written.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): usage-write-race subtask
   (the kill-emacs-hook race itself -- code guard now live, race
   window remains), cap calibration, cadence price. Data side
   COMPLETE. Waiting on Nacho.
2. Digest diet watch: ~11.4k chars, warn 12k -- diet at next close
   if growth continues.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 10:00: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp. Git
  archaeology over ssh = dump reflog+log ONCE, read locally.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk.
- Pulse c50: all green.
- NEW SCAR (c50): a deterministic failure's position in a suite is
  evidence about ORDER, not about the test it dies on -- the dying
  test is where async debt comes due; the debtor may be far earlier.
- NEW SCAR (c50): an isolation run that refuses to reproduce is not
  a failed experiment -- your isolation changed something (here:
  ERT registration order vs alphabetical full-suite order); that
  difference is the next hypothesis.
- Test-hygiene law (c50 extension): any test that drives an FSM
  through a state whose handler does network I/O must stub the
  dispatcher; async sentinels outlive the test that spawned them.