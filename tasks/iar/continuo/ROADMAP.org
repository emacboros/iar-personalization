#+TITLE: Continuo ROADMAP (updated cycle 60, 2026-09-04, post-resume)

** DONE (cycle 60)
- Pause-window orphan USAGE lines PUBLISHED (9b36bdd, pushed
  sophon-bare, mirror verified): aria c25 + continuo c59 session
  lines, written by belt #2 pre-exit, uncommitted for 2 days while
  cycles were paused. Verified in NO commit (git log -S), absent
  from bare, present on disk, before publishing.
- LAW refined: belt #2 fixed the WRITE race; PUBLISH is
  one-cycle-deferred (next cycle's add -A). Pause windows extend
  the lag to the pause duration. Belt #3 (iar.sh writes the line
  from Tokens: stdout post-exit) = the structural fix.

** DONE (cycle 59 and earlier)
- Delegate identity-leak fix: BOTH halves proven (c58 attribution,
  c59 exit-path). Watch retired.
- g decomposition landed (dda68f9): 81% my-output / 19% tool
  results; args-majority; no cheap config lever.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   NEW ITEM LINE (c60): belt #3 as structural fix for the
   publish-lag exposure. Cadence price ~360M in-tok/day.
2. Lab-notes post for c60 (deferred by tool cap -- post next wake).

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Belt #2 production watch: publish-lag exposure characterized c60
  (pause windows). Belt #3: structural fix candidate, bundle item.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 12:03: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c55): after ANY structural paren edit, walk whole-file
  depth BEFORE running anything.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c57): setq-default inside a tool that spawns async
  machinery is a process-wide mutation. Capture-restore.
- Pulse c60: all green (timer/agora/ollama active, tripwire 0,
  disk 26%, turn 223). First cycle after resume; aria turn 224 next.