#+TITLE: Continuo ROADMAP (updated cycle 24, 2026-09-03)

** DONE cycle 24
- Built docs/infra/zulip.md (full-doc home) from live sophon probes:
  5-container stack, unit, /opt/zulip files, realm data (347 msgs,
  users, streams), bot API recipe, health (0 ERRORs; Sep-1
  rate-limiter 500 burst self-resolved, watch only).
- TOP FIND: backup gap -- /var/lib/zulip in NEITHER restic set
  (local nor offsite); Zulip export dir empty. Agora = single point
  of failure. Flagged for Nacho (pg_dump timer to restic-covered
  path; infra change, not cycle territory).
- Drift fixes: binding 0.0.0.0:8090 (was "localhost"); ansible
  roles/zulip claim marked unverifiable from sophon.
- _overview.md Zulip block -> pointer (1179->616 chars). adcac0c.

** DONE cycle 23
- Floor verification: c22 diet LIVE in production. continuo msgs=12
  16.6k->14.4k (-2.2k); aria msgs=10 22.6k->21.6k (-1.0k net,
  day-drift ate half). Floor share ROSE post-diet (34%/48%) because
  cycles shortened (aria 91->75 reqs) -- ratio is not a lever.
- Diet round 3: infra 2583->2284 (network/services/domains, homes
  verified in overview.md), iar-prod 3588->3443 (hosting+MVP).
  iar/ no cut (already index-shaped). Total 8889->8616 = ~68
  tok/req. Injection lever EXHAUSTED. Commits 99d8aa6+30b6d05.

** DONE cycle 22
- Floor-trim round 2: c20's editorial rule (overview=index, not
  mirror) applied to ALL THREE iar-project overviews, with
  full-doc-home verification per section. iar 4995->2718,
  iar-prod 7757->3588, infra 4113->2583: -7976 chars = ~1990
  tok/req off the floor (c20 was ~621). Kept no-home sections
  (services table, ASCII diagram, zulip block, three-axis).
  Fixed Cloudflare drift in iar-prod/architecture.md. 82 key
  facts verified. Commits a5cd671 + 1bfbda5, pushed.

** DONE cycle 21
- Exit-127-after-success root-caused: cycle 8's commit 30c5385
  edited utils/iar.sh IN PLACE while the runner process was
  executing it; bash's next buffered read landed mid-line (127,
  set -e). Reproduced in /tmp (race6.sh). New failure class.
  knowledge/iar/iarsh-self-edit-race.md. Commit 7446ba7 pushed.
- Failure census (Nacho directive): 12x exit-126 (pre-unit-fix,
  closed), 1x exit-127 (root-caused), 1x exit-2 (clean).
  Post-10:26 era: ~30 cycles, zero failures.

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (TOP queued): rotate.sh runs a /tmp
   copy of iar.sh (kills the self-edit race class); exit-126
   behavioral law + git-as-nacho durable fix; floor trim leftovers
   (check_elisp vacuous-OK + restorecon + check_ollama model probe +
   sidecar socket bridge); mirror push silent failure (git@10.66.0.1
   preauth); bare-repo fix trio + HEAD->main one-liner (306).
2. Zulip backup gap: Nacho's call (pg_dump timer). If he asks for a
   proposal, the doc has the mechanism (docs/infra/zulip.md).
3. If aria acks phase-4 offer (for-nacho 315 thread): close her 2
   closeout items, attributed to her as work author.

** WATCHING
- Zulip backup gap: TOP infra flag (c24). Recurrence check: restic
  sets still exclude /var/lib/zulip = unresolved.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = the /tmp-copy fix is needed
  URGENT (worst case: a real failure loses its tombstone + telegram).
- Boredom ledger: both fresh.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline.
- DIGEST: 8,372 chars, warn 12k -- headroom (~4 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Flag 326 (aria's), flag 306 (aria's).