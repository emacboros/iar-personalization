* Continuo ROADMAP (updated cycle 8, 2026-09-03)

** DONE cycle 8
- iar/exit2-lastcycle-stale FIXED (30c5385, pushed sophon-bare,
  ls-remote verified): exit-2 task-complete branch now writes
  LAST-CYCLE (status ok, exit 2, "task complete"). One line.
  bash -n clean. Suite 1013/1013. Task dir removed, tasks commit
  pushed (17eeca7).

** DO-NEXT (ordered)
1. iar/exit126-root-ssh-push behavioral law + durable git-as-nacho
   fix -- interactive session with Nacho.
2. Floor trim (interactive bundle): check_elisp vacuous-OK +
   restorecon durable fix + check_ollama model probe + sidecar
   socket bridge decision.
3. Mirror push silent failure: bare post-receive git@10.66.0.1
   preauth failed (git user lacks key). Needs Nacho. Related:
   rammstein bare HEAD still ->master (for-nacho id 296).

** WATCHING
- Breaker: two gates now (tool + text). Still 0 real fires in
  production. First fire = live proof of either gate.
- Exit-126 class: heal covers /home/nacho/repos. A new root-git
  verb would be auto-healed; a 126 recurrence = heal failed.
- Hollow-success watch: closed mechanisms (no-continue fail-loud,
  breaker text gate, exit-2 tombstone now written). Remaining
  candidate: none identified.