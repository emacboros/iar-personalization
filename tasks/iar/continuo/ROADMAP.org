* Continuo ROADMAP (updated cycle 6, 2026-09-03)

** DONE cycle 6
- iar/timeout-exit0-no-continue FIXED (0454872, pushed sophon-bare):
  loader dropped ignore-errors (signals on missing file); belt in
  iar-run-cycle fails loud before any state/request. Differential-
  verified: old code fails the missing-file test. Suite 1008/1008.
  Task dir removed. Latent gap now closed by design.
- Test-suite discovery (worth keeping): run-tests sets
  iar-prompts-path via test-loop-chain.el to /root/i.ar/prompts/common
  (absolute) -- load order alphabetical, so earlier test files see
  user-emacs-directory-relative agents.d/common. Two prompt trees
  exist (repo prompts/ + container agents.d/); tests that care must
  set the path explicitly.

** DO-NEXT (ordered)
1. iar/exit126-root-ssh-push behavioral law + durable git-as-nacho
   fix -- interactive session with Nacho.
2. Floor trim (interactive bundle): check_elisp vacuous-OK +
   restorecon durable fix + check_ollama model probe + sidecar
   socket bridge decision.
3. Mirror push silent failure: bare post-receive git@10.66.0.1
   preauth failed (git user lacks key). Needs Nacho. Related:
   rammstein bare HEAD still ->master (for-nacho id 296).

** WATCHING
- Exit-126 class: heal covers /home/nacho/repos. A new root-git
  verb would be auto-healed; a 126 recurrence = heal failed.
- Hollow-success watch: timeout->summary->exit-0 with ~2 tool
  calls. One instance; the no-continue mechanism is now closed
  (fail-loud). Remaining candidate: none identified.
- Breaker: 0 real fires. First fire = live proof.