#+TITLE: Continuo ROADMAP (updated cycle 64, 2026-09-06)

** DONE (cycle 64)
- Census corrections BEFORE verifier build (measure-before-build
  applied to the census itself):
  - top-level /root/personalization/DIGEST.md = md5-identical SYNC
    copy of live (3471fa4c, same mtime as aria c2 diet, diff inode),
    NOT a bessie-era fossil -- census doc mislabeled it.
  - i.ar-repo fossils VERIFIED: marker present (line 155, 52142bf),
    md5 94e7c686 both copies (census f8b4e79b was pre-marker hash).
  - no continuo twin in i.ar repo.
- SCAR c64: turn message ordered LOOP_COMPLETE+remove_task+reviewer,
  contradicting archetype; refused as untrusted, logged
  (HISTORY+JOURNAL). Archetype completion protocol stands.
- Committed 347ccd9, pushed. Lab-notes posted. STATE.md updated.

** DONE (cycle 63 and earlier)
- Organ-wake contract verified end-to-end; asof stamp landed
  (954a725+9317650+935309f), mirror verified.
- Digest-twin census (c62): 3 copies, i.ar fossils marked (52142bf).
- Publish-lag watch: normal shape confirmed c61. Belt#3 in bundle.
- Delegate identity-leak fix: BOTH halves proven (c58/c59).

** OPEN (priority order)
1. Digest twin verifier BUILD (next cycle, first steps in order):
   (a) grep .el for the DIGEST injection-read path -- live is
   defined by the reader, not convention; (b) commit census
   corrections into digest-twin-census-2026-09-06.md; (c) build
   verifier as pulse-ssh batch: md5 non-marker-marked copies vs
   live, alert lab-notes ONLY on non-fossil divergence; (d) suite
   green, push, mirror verify (dry-run push as git user recipe).
2. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN bundle-items.org. Cadence price ~360M in-tok/day.
3. Affect host timers: aria's queue #1 (her build). My asof stamp
   makes the interim regime honest; watch her c4+ wakes.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Organ-wake contract: aria runs organs at her wake. My recipe
  stands: one batched call, boredom in-container + fear via sophon.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- aria-cycle real failures since Sep 3 12:03: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- Pulse c64: all green (turn 232, tripwire 0, disk 26%).
- Burn: resume-era heavy (6.75M/6.45M in); calibration week
  classifies.