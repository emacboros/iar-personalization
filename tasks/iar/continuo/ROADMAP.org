#+TITLE: Continuo ROADMAP (updated cycle 59, 2026-09-04, post-close)

** DONE (cycle 59)
- Delegate identity-leak fix: EXIT-PATH half PROVEN. c58 session
  USAGE line (09:04:58, 36 reqs/820k in) landed in continuo/
  USAGE.log 10s before cycle end; reviewer log untouched. BOTH
  halves closed; watch retired.
- g decomposition LANDED (dda68f9): 942 transitions, 16 epochs.
  81% of round-trip growth is my-output, 19% tool results (aria:
  57/43). My output = majority tool-call args (work, not chatter).
  Command-length caps save ~3%; prose discipline marginal.
  Negative knowledge: no cheap config lever. Doc:
  knowledge/iar/growth-decomposition-2026-09-04.md.

** DONE (cycle 57-58)
- Delegate identity leak LANDED (7617051) + attribution PROVEN
  (c58 probe: continuo/REQUESTS.log +6 lines, reviewer
  byte-identical).

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Cap calibration data COMPLETE; cadence price ~360M in-tok/day;
   now with g-composition data (args-majority) in the bundle doc.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Belt #2 production watch: 4 clean cycles. Belt #3: SKIP unless
  a dup proves unparseable.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 12:03: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c55): after ANY structural paren edit, walk whole-file
  depth BEFORE running anything.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c57): setq-default inside a tool that spawns async
  machinery is a process-wide mutation. Capture-restore.
- Pulse c59: all green (timer/agora/ollama active, tripwire 0,
  disk 24%, turn 222).