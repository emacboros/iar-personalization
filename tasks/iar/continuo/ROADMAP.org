#+TITLE: Continuo Roadmap
#+UPDATED: 2026-09-03 04:44 UTC (cycle 8)

* DONE
- One-shot fence parity LANDED: 4c879a2 (stash-recovered cycles
  5-7 + aria cycle 2 breaker-writeback fix). Sophon checkout
  verified at same commit; suite 981/981 green (was 970).
- Docs follow-up: agents.md (one-shot fences + exit codes),
  modules.md (row 75) -- dfdbf52, pushed.
- Lab-notes post (id 254): parity + stash-recovery lesson. Owed
  since cycle 5 -- paid.
- Per-agent roadmap/task isolation (390689a), tool-cap 60->120
  (00f8103, 0 cap-exits since).

* OPEN THREADS (priority order)
1. Injection trim: cycle-me DIGEST diet. Aria's DIGEST.md ~19.7k
   chars injected FULL into every aria-cycle request. Numbers
   first: measure per-request injection size, then trim. (audit:
   knowledge/aria/token-burn-audit.md)
2. Chain-guard blindness watch (buffer-local history reach).
3. Watch cap-window cycles: does 120 end the tool-cap-exit class?
4. Vacuous-check lesson: check_elisp path aborts at gptel require
   and reports OK -- make it fail loudly. FILE for interactive
   session (not cycle work).
5. i.ar docs live in personalization repo (docs/iar/), not the
   i.ar repo -- no docs/ dir there. Noted; no action.

* LAWS ACCUMULATED
- Check git stash list / fsck before declaring work lost.
- A truncated read_file view is not the file; read the region
  you edit.
- A green check that never touched the code is not a check.