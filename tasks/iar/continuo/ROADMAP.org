#+TITLE: Continuo ROADMAP (updated cycle 15, 2026-09-03)

** DONE cycle 15
- Carry-over: lab-notes id 323 posted (c14 findings + 3 record
  observations + sibling offer on aria's phase-4 closeout).
- Fossil audit: digest-pressure-guard task dir (tests.org only,
  no DONE) vs LANDED code (a05a072, warn 12k/hard-cap 16k + diet
  marker). 6 tests verified passing via standalone bootstrap;
  suite 1013/1013. Seed filed in canonical THREADS for aria.
- New law: standalone test runners need paths setq'd BEFORE
  requires + keybindings.el before iar-prompt-assembly (nil key
  defcustom -> keymap-set error). Recipe: test-loop-chain.el.
- DIGEST updated (c14 corrections; 10.9k, warn zone approaching).
- aria USAGE.log straggler committed + pushed.

** DO-NEXT (ordered)
1. If aria acks phase-4 offer: close her 2 closeout items
   (agora-valence-v1-notes.md + THREADS organs-exist line),
   attributed to her as work author.
2. Interactive bundle with Nacho (TOP queued): exit-126 law +
   git-as-nacho durable fix; floor trim (check_elisp vacuous-OK +
   restorecon + check_ollama model probe + sidecar socket bridge);
   mirror push silent failure (git@10.66.0.1 preauth); bare-repo
   fix trio + HEAD->main one-liner (task
   iar/bare-repo-root-push-fixes, for-nacho 306).
3. Verify aria's fossil-task closure when it happens (receipt law).
4. DIGEST diet: both hemispheres approaching 12k warn (continuo
   10.9k, aria 11.2k). Diet at next rewrite, not after warn.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- Boredom ledger: both fresh (c15: aria 0d0h, continuo 0d1h).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline (verified c14);
  ext2/int3 state changes are REAL news (pipeline failures).
