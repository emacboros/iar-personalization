* Continuo ROADMAP (updated cycle 9, 2026-09-03)

** DONE cycle 9
- Bare-repo root-push pollution VERIFIED + REFINED (aria cycle 25
  mechanism confirmed live). Writer of the 6 root-owned files =
  cycle 8's own pushes. Heal is reactive (each root push sweeps
  previous leftovers; own housekeeping lands root-owned for next).
  Mirror leg healthy end-to-end. NEW finding: 19/20 sophon bare
  repos HEAD->master with only main (dangling HEAD). Knowledge:
  knowledge/iar/bare-repo-root-push-heal.md. Task
  iar/bare-repo-root-push-fixes filed (continuo tree). for-nacho
  id 306, lab-notes id 307. No code landed -- diagnosis cycle.

** DO-NEXT (ordered)
1. iar/exit126-root-ssh-push behavioral law + durable git-as-nacho
   fix -- interactive session with Nacho. NOW INCLUDES the
   bare-repo fix trio (task iar/bare-repo-root-push-fixes) +
   HEAD->main one-liner.
2. Floor trim (interactive bundle): check_elisp vacuous-OK +
   restorecon durable fix + check_ollama model probe + sidecar
   socket bridge decision.
3. Mirror push silent failure: bare post-receive git@10.66.0.1
   preauth failed (git user lacks key). Needs Nacho. Related:
   rammstein bare HEAD still ->master (for-nacho id 296).

** WATCHING
- Breaker: two gates now (tool + text). Still 0 real fires in
  production. First fire = live proof of either gate.
- Exit-126 class: heal covers /home/nacho/repos. A new root-git
  verb would be auto-healed; a 126 recurrence = heal failed.
- Hollow-success watch: closed mechanisms (no-continue fail-loud,
  breaker text gate, exit-2 tombstone). Remaining candidate: none.
- Bare-repo root-owned residue: bounded by reactive heal; a
  NON-git-user operation failing on sophon bare = the pollution
  crossed from nuisance to breakage (escalate to Nacho).