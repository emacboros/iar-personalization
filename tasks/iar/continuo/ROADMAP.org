#+TITLE: Continuo ROADMAP (updated cycle 34, 2026-09-04)

** IN-FLIGHT (cycle 34 -> 35, finish FIRST)
- PARSE-line token fields: CODE LANDED (1afc21a, suite 1015/1015 on the
  code). NOT YET: (a) run suite on HEAD (one new assertion in
  test-reqlog-dump-logs-response-and-parse unrun), (b) write positive-path
  test test-reqlog-dump-parse-carries-tokens (drafted in journal c34,
  shape = dump test + :tokens plist (:input 13249 :output 52), assert
  "tokens_in=13249 tokens_out=52"), (c) push ONLY green. Then bundle
  item 1 is DONE: token meter complete, c33 census-bias law closed.

** CLOSED (cycle 33)
- Floor-delta decomposition: aria ~16.2k vs continuo ~13.2k tok/req floor.
  Delta = file inventory. Explained constant, not a lever.
- STATE.md write-only for aria-cycle mode confirmed (personality claim
  false; bundle item).

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (task iar/continuo/interactive-bundle-nacho):
   PARSE-line token fields -> moves to DONE after cycle 35 verifies+pushes;
   STATE.md injection mismatch (personality edit); /tmp-copy race; exit-126
   restorecon; git-as-nacho identity; delayed-heal sweep + mirror-loop note;
   tool-cap calibration (now with complete token data post-fix) + cadence
   price; floor trim leftovers.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT (bundle item 1).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: FAIL=0 baseline.
- DIGEST: 9,498 chars (diet landed c34 close), warn 12k.
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE compound
  ssh per question; dump to /tmp ONCE (chain guard tripped c32).
- REQUESTS.log census law (c32): substring grep self-inflates. Anchor
  line-start REQ tokens; validate pattern vs USAGE line first.
- Token-census bias law (c33): OBSOLETE after cycle 35 push -- PARSE lines
  carry tokens; RESPONSE-line bias caveat dies.
- Mirror-loop: harmless while refs agree; watch silent push failures.
- Test-hygiene law (c30): tests that set globals MUST restore in
  unwind-protect. let vs let* on sibling-referencing bindings (c34
  relearned in miniature: byte-compile caught it).
- Pulse c34: all green (timer/agora/ollama active, tripwire 0, disk 24%,
  rotation 170).