#+TITLE: Continuo ROADMAP (updated cycle 27, 2026-09-03)

** DONE cycle 27
- FAILURE-FIRST ok (c26 exit 0). Pulse green: services active,
  tripwire 0, disk 24%, timer fired. Deferred c26 lab-notes post
  LANDED (msg 353, first action). Agora read: Nacho silent since
  20:00 UTC, no answers pending.
- THREAD: bare-repo HEAD one-liner EXECUTED under Nacho's standing
  direction (with-nacho 238: do it, report it). sophon 3 repos +
  rammstein 3 repos (incl iar-prod 296 CLOSED). 14 empty sophon
  mirrors SKIPped (census law: validated pattern first). Pollution
  heal: chown both sides, verified 0/0. NEW LESSON: symbolic-ref
  writes are root-owned too -- heal after LAST root-side op.
- Full-doc home built: docs/infra/git-server.md (topology, hook,
  known issues, health checks) + _overview.md pointer line.
- Mirror verified end-to-end: my push 651465d landed on rammstein.
- Loop-guard lesson: 12-call execute_code_local chain tripped the
  chain guard mid-census -- batch-read law applies to ssh probes
  too; the chain was converging but the guard counts, not reads.

** DONE cycle 26
- Burn decomposition from primary evidence
  (knowledge/iar/burn-decomposition-2026-09-03.md). Floor 12.4k,
  ~325 tok/round-trip, growth ~74% of burn. Cadence price
  ~250M tok/day. Tool cap 120 pressed by healthy cycles.

** DONE cycle 25
- Phase-4 stale pointer resolved: offer was lab-notes 323. Closed
  aria's 2 items as offered (9011901).

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (TOP queued): rotate.sh /tmp-copy
   fix; exit-126 behavioral law + git-as-nacho durable fix; floor
   trim leftovers; mirror push silent failure; delayed-heal sweep
   in post-receive (proposal 2, now the ONLY remaining pollution
   fix); tool-cap calibration + cadence price.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42 find): interactive.

** WATCHING
- Zulip backup gap: TOP infra flag (c24, verified aria c41+c42).
- Breaker: 0 real fires, two gates live. First fire = proof.
- Tool-cap calibration: 21% of cycles hit 120 soft cap (aria c43
  full-day data). If mean climbs, cap is a burn ceiling.
- iar.sh self-edit race: recurrence = /tmp-copy fix is URGENT.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline.
- DIGEST: 9,351 chars, warn 12k -- headroom (~3 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora API reads: ONE anchor=newest dump per stream (c25 law).
- ssh probe chains: ONE compound ssh per question (c27 loop-guard
  trip); batch-read law covers remote probes, not just local logs.
- REQUESTS.log census: segment by cycle boundary (msgs=2 marker).