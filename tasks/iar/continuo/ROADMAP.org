* Continuo ROADMAP (updated cycle 4, 2026-09-03)

** DONE this cycle (cycle 4)
- FAILURE-FIRST on turn-107 exit-126: writer = aria cycle-19/20
  root-git verb on iar-prod/.git/config (git branch --set-upstream-to
  as host root). NOT a continuo bug. Heal scope already extended by
  Nacho (13:31, /home/nacho/repos). Turn 109 clean, poison 0,
  replica tests PASS (root + rootless + chcon->mount + heal->mount).
- Librarian fossil unit RESOLVED: Nacho removed iar-librarian
  .service/.timer ~10:10. Fossil failed-state reset-failed. Thread
  closed (was open since cycle 2).
- Timer NextElapseUSecMonotonic=infinity during active cycle =
  known oneshot deferral, NOT a dead timer (re-verified).

** DO-NEXT (ordered)
1. Timeout-grace-path exit-code audit (carried from cycle 2, twice):
   the timeout landing in iar-agent-cycle.el (grace 120s -> summary ->
   exit 1) has no test pinning the exit code. Cycle-safe, my domain.
2. iar/exit126-root-ssh-push: behavioral law (agents push to bare repo
   only, never working clone) + durable fix (git ops as nacho) --
   interactive session with Nacho.
3. Mirror push silent failure: bare post-receive git@10.66.0.1 preauth
   failed (git user lacks key). Needs Nacho (key setup). Related:
   rammstein bare HEAD still ->master (for-nacho id 296 one-liner).

** WATCHING
- Exit-126 class: heal covers /home/nacho/repos now. A new root-git
  verb would be auto-healed; if a 126 recurs, the heal failed --
  investigate immediately.
- Hollow-success watch (carried): timeout->summary->exit-0 with ~2
  tool calls. One instance; watching for a class.
- Breaker: 0 real fires. First fire = live proof.