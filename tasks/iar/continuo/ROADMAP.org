#+TITLE: Continuo ROADMAP (updated cycle 109, 2026-09-07)

** DONE (cycle 109)
- FAILURE-FIRST root-cause turn 353 (20:03:34 exit 1): truncated-output
  guard (c102, 190ef88) SECOND production fire -- req 87 (epoch
  260907195043) stop=length, 65536 tokens > 20000. Genuine degradation
  mid-verification, guard working as designed, NOT a machinery bug.
- Completed the interrupted verification: does the chain guard catch
  aria's 05:11 self-meter-reading burst? NO. Measured 132
  execute_code_local calls in 05:11-06:31: consecutive Jaccard mean
  0.42, only 35% chain-counted, longest chain run 29. The convergence
  reset (0.5) resets faster than the loop accumulates -- varying greps
  defeat it. c38's "chain guard silent through 208 calls" now
  EXPLAINED, not just observed. Wrong fence for the self-meter-reading
  class. Doc: knowledge/iar/chain-guard-vs-burst-2026-09-07.md, commit
  d807932+7c37aa6, pushed. Candidate (context-growth-without-
  convergence) filed for fences task, not landed.

** DONE (cycle 102)
- LADDER ITEM C LANDED (per-request truncated-output guard): the
  invisible-turn stub (c99) made truncated-thinking losses survivable
  but did not prevent the burn (9 truncated 65k turns/day on continuo
  ~590k output tokens, all lost except the stub). This guard ends the
  cycle (exit 1) when a single request is a truncated generation
  (stop=length) with output above 20k tokens -- keying on stop=length
  + tokens_out, NOT raw tokens_out, so a complete 30k response is
  never false-flagged. The request log already captured both in its
  PARSE dump (:before gptel-curl--stream-cleanup, which runs before
  the post-response handler); this publishes them as shared vars
  (iar--reqlog-last-stop, iar--reqlog-last-tokens-out) reset at cycle
  start, and the cycle's post-response handler checks the predicate
  before the text-runaway branch. Re-sending after a truncated turn
  would burn another 65536 tokens on the same loop; ending is the
  honest stop. 8 new tests, suite 1071/1071 green. Commit 190ef88,
  pushed sophon-bare. First production fire = live proof (watching).

** DONE (cycle 101)
- FAILURE-FIRST root-cause c100 timeout (1934s, exit 1). Evidence:
  100 calls (under 120 cap), no breaker/soft/hard-cap/runaway fires.
  NOT a runaway -- a data-heavy close-out (census + design note +
  full memory pass) ran past 1800s; only loss was the DIGEST
  output-burn edit, cut at grace expiry, restored + committed
  24236a6. All c100 substance landed on origin (1256c01/72e6a10/
  3ddf2fb/e8fcf2b). Output-burn fact added to DIGEST. Lesson: a
  long data cycle burns budget on substance; the close-out tail is
  what gets cut -- the fence working as designed.

** DONE (cycle 100)
- OUTPUT-TOKEN BURN DATA (ladder item C): measured REQUESTS.log both
  hemispheres. Legitimate stop=stop output never exceeds ~14k
  (continuo) / ~31k (aria) tokens; 65536 num_predict cap is 4-5x
  need. 9 truncated 65k turns/day on continuo burn ~590k output
  tokens (all lost except the stub). Guard design: key on
  stop=length + tokens_out, NOT raw tokens_out (a complete 30k
  response is legitimate). Data + design note in
  knowledge/iar/output-token-burn-2026-09-07.md, commit 1256c01,
  pushed. Suite green 1063/0.

** DONE (cycle 99)
- P0 INVISIBLE-TURN STUB PRODUCTION-VERIFIED (3 live fires, all
  correct, all cycles completed): fork 8715a6c (deployed 04:50 UTC)
  emits the stub assistant message for truncated thinking-only turns
  in gptel--parse-buffer. Fires at 11:49:45 (REQ 114143-56), 13:30:19
  (130018-31), 14:05:39 (140143-8) -- all the exact invisible-turn
  shape (roles=...,assistant,user, stub in content), all cycles
  landed (USAGE lines present). Before the stub these were the
  structural-loop losses (c73, c1@00:01, c1@01:00). The transport
  fix kills the loop at source. Regression tests green in full suite
  (1063/0). Handoff updated in breaker-grace-compliance.org UPDATE 4.
  Remaining ladder: B (thinking crash dump), C (per-request runaway
  guard -- LANDED c102), D (breaker honesty, landed c79).

** DONE (cycle 98)
- GPU-LOAD PROBE UNIT FILES (c97 wrote the probe script; this cycle
  wrote + committed the systemd unit + timer). gpu-load-probe.service
  (hardened: PrivateNetwork, NoNewPrivileges, TimeoutStartSec=13200,
  runs from the personalization clone -- repo version IS the running
  version) + gpu-load-probe.timer (OnCalendar=*-*-* 00:30:00,
  Persistent=false -- fires 2 min before the observed failure window
  00:32-03:55 local). Install subtask written to
  gpu-load-probe-install/unit-files. Commit 1907cb6, pushed.

** DONE (cycle 97)
- GPU-LOAD PROBE (load-correlation hypothesis, c94/c96): wrote
  knowledge/aria/bin/gpu-load-probe.sh -- samples sophon GPU
  util/VRAM/loadavg/ollama-runner once/min for 3.5h, CSV to
  /var/log/gpu-load/. Tests whether model degradation (both glm and
  deepseek fail in bursts during sophon's early-morning LOCAL window
  00:32-03:55, overlapping plocate and fstrim) is load-correlated. If
  GPU util is high during failures -> fix shifts from model mapping to
  GPU contention; if low -> degradation is intrinsic to the models at
  that hour. Install task filed (gpu-load-probe-install, Nacho -- infra
  change). Commit 1b7b613, pushed.

** DONE (cycle 96)
- CLOCK-DOMAIN CORRECTION (c96): the c94 addendum's time-correlation
  claim was WRONG. It read the journal's LOCAL (-03) failure times and
  called them UTC. Corrected: failures cluster 00:32-03:55 LOCAL
  (03:32-06:55 UTC), NOT 00:00-04:00 UTC; "from 08:00 UTC onward 0
  failures" is false (last failure 07:44 local = 10:44 UTC). The
  load-correlation hypothesis survives but is a LOCAL-hours
  phenomenon (overlaps plocate 00:47 + fstrim 01:01 local). SCAR c84
  re-learned: a timestamp with no timezone label is a reason to ask
  for one (journal short output hides -03), not a reason to assume
  UTC. Correction in knowledge/iar/cycle-failure-census-2026-09-07.md,
  commit 429bc2c, pushed.

** DONE (cycle 95)
- RECOVERY PROMPT (force tool call, e331a83) FIRED ON ME, its own
  author, breaking a text-only loop after the roadmap read. First
  production fire of the new prompt, and it worked as designed. Suite
  1063/0 green. Services green, tripwire 0, disk 26%.

** DONE (cycle 94)
- FAILURE CENSUS ADDENDUM (c94): verified 9 exit-1 + 1 exit-2 = 10
  failures on Sep 7 (reconciled c93 count). NEW FINDING: 7/9 exit-1
  failures cluster in 00:00-04:00 UTC (both models degrade in the same
  early-morning window) -- load-correlation hypothesis filed (Frigate/
  GPU load), unverified. New recovery prompt (force tool call, e331a83)
  has ZERO production fires yet -- first fire = live proof. Suite
  1063/0 green. Commit b85c8c4 pushed.

** DONE (cycle 93)
- FAILURE CENSUS FOLLOW-UP (c93): Sep 7 full-day dump: 10 failures, 7
  continuo (deepseek), 3 aria (glm). NEW: aria/glm failed 3x in the
  00:32-01:06 cluster (hard cap x2, loop-guard identical-call x1) --
  c90 census said glm was stable; this is a new shape. Recovery prompt
  (c91) live: "requesting recovery" recovered 2x (06:38, 08:49 exit 0),
  failed 1x (07:35 hard cap exit 1). Lever unchanged: model revert
  (Nacho's call on rotate.sh). Services green, tripwire 0, disk 26%.

** DONE (cycle 92)
- BELT #2 DURABILITY FIX LANDED (d2d272d, 10:34 UTC): the USAGE
  orphan-write class (c45/c46) is closed at source. The kill-emacs
  hook now writes the USAGE line BEFORE the final commit (belt #2
  pre-exit write, a7e1cf5) AND the honest-return path is hardened
  (ee326bb). Post-fix cycles all committed their USAGE lines. The
  01:30/01:51/08:32 residue was the pre-durability class, eaten by
  aria c11's reset --hard. Watching item CLOSED. Addendum in
  knowledge/iar/usage-line-gap-2026-09-07.md, commit 90b4551.

** DONE (cycle 91)
- FAILURE-REDUCTION follow-up: runaway recovery prompt hardened. The
  c90 census showed recovery works only 25% (2 of 8). Old prompt
  offered a prose choice ("pick ONE action or CYCLE_COMPLETE") -- a
  text-only loop repeats instead of snapping out. New prompt FORCES a
  tool call (append_file to JOURNAL.org) -- the one thing a text-only
  runaway is NOT doing, so it breaks the loop shape directly. Commit
  e331a83, suite 1063/0 green, pushed sophon-bare.

** DONE (cycle 90)
- FAILURE CENSUS (Nacho's top priority): 3-day window, batch-read.
  FINDING: the 2026-09-06 15:32 rotate.sh edit (continuo ->
  deepseek-v4-flash:cloud) inverted the failure rate: 0 failures in
  2 days pre-change (46 cycles), 18 in 1.5 days post (60 cycles,
  23%). Dominant class: deepseek text-only output runaway (8 ending
  cycles, 7 on continuo) + context breaker (5, all continuo -- same
  root cause, deepseek burns context). All fences fired correctly;
  this is MODEL degradation, not machinery. Fix direction: revert
  continuo to glm-5.3-flash:cloud (Nacho call -- rotate.sh is root
  infra). Census: knowledge/iar/cycle-failure-census-2026-09-07.md,
  commit 907880f, posted lab-notes.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN bundle-items.org. Cadence price ~360M in-tok/day.
2. FAILURE-REDUCTION (Nacho's top priority): census DONE c90, addendum
   c94 found failures cluster 00:32-03:55 LOCAL (7/9) -- BOTH models
   degrade in the same early-morning LOCAL window (clock-domain
   corrected c96). Load-correlation hypothesis (Frigate/GPU load)
   UNVERIFIED -- PROBE READY (c97: gpu-load-probe.sh) + UNIT FILES
   READY (c98: service + timer, install subtask written). Install task
   filed (gpu-load-probe-install, Nacho -- infra change). Recovery
   prompt hardened c91 (force tool call), live, first fire c95 (on its
   own author, worked). P0 invisible-turn stub LANDED + VERIFIED (c99).
   Ladder item C (per-request runaway guard) LANDED c102 (190ef88) --
   first production fire = live proof (watching).
   Lever is STILL the model mapping (Nacho's call on rotate.sh).
3. Affect host timers: aria's queue #1 (her build).
4. Belt #3 (iar.sh parsing "Tokens:" stdout): optional now that belt
   #2 commits its own line.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Model degradation: BOTH models degrade in bursts. c90 census:
  deepseek text-only repetition loops (8 ending runaways in 1.5d).
  c93 follow-up: glm/aria ALSO degraded 3x in 00:32-01:06 cluster.
  c94 addendum: failures cluster 00:32-03:55 LOCAL (corrected c96 --
  was mislabeled UTC). Load-correlation hypothesis UNVERIFIED --
  probe READY (c97) + unit files READY (c98), install task filed.
- Invisible-turn stub (P0, 8715a6c): PRODUCTION-VERIFIED c99 (3 live
  fires, all correct, all cycles landed). Structural-loop class
  CLOSED at source. Remaining ladder: B (thinking crash dump), C
  (per-request runaway guard -- LANDED c102, first fire = live proof).
- Truncated-output guard (ladder item C, 190ef88): TWO production
  fires (c105 req 29, c109 req 87), both genuine degradation, both
  working as designed. Live proof obtained.
- Self-meter-reading loop (c38, aria 05:11 burst): chain guard does
  NOT catch it (c109 -- varying greps reset the chain counter). Wrong
  fence. Candidate: context-growth-without-convergence guard, filed
  for fences task, not landed.
- Recovery prompt (force tool call, e331a83): first production fire
  c95 (on its own author), worked. Live proof obtained.
- Digest twins: verifier is STANDALONE, run at wake as its own call.
- Organ-wake contract: aria runs organs at her wake.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires since the c70 zombie fix. Now measures
  SENDABLE context (c79). c90: 5 fires, all continuo (deepseek
  context appetite) -- working as designed, model-driven.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- SCAR (c84): check the clock domain before attributing a mechanism.
  RE-LEARNED c96: journal short output hides the -03 timezone -- a
  timestamp with no timezone label is a reason to ask for short-iso,
  not a reason to assume UTC.
- SCAR (c85): belt #2's write is only as durable as the next commit.
  CLOSED c92: durability fix d2d272d landed 10:34 UTC; post-fix
  cycles all committed. The 01:30/01:51/08:32 residue was the
  pre-durability class, eaten by aria c11's reset --hard.
- Pulse c109: services green (timer/agora/ollama active), tripwire 0,
  disk 26%, suite 1071/1071, turn 354.
