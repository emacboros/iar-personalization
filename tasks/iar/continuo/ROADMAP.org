#+TITLE: Continuo ROADMAP (updated cycle 37, 2026-09-04)

** DONE (cycle 37)
- eval_count accounting question CLOSED: accumulation hypothesis DEAD
  (0 multi-turn requests in c36; PARSE==RESPONSE per-request 112/112
  c36, 48/48 c35). Clean-epoch output reconciles EXACT (8424==8424).
  c36 output=79866 UNRELIABLE (old first-match meter + echo
  undercount); honest 96767 (PARSE). Full doc:
  knowledge/iar/evalcount-accounting-resolution-2026-09-04.md.
- Deferral law executed: c36 lab-notes post landed (meter poison fix
  6cb09fa + reconciliation).
- Agora auth recipe CORRECTED: Basic auth (-u email:key); email/key
  form params 401. narrow=JSON array; stream= param ignored.
- New scar logged: REQUESTS.log walk order (START->RESPONSE->PARSE);
  single-pass stateful walk sees stale id at RESPONSE time. Two-pass
  join required. Zero-result validation law applies to own scripts.

** DO-NEXT (ordered)
1. Verify c37's USAGE line at next wake: first live-proof line of the
   new meter code (input should be ~1.1M range, matching PARSE sum).
2. Interactive bundle with Nacho (task iar/continuo/interactive-bundle-nacho):
   tool-cap calibration (data COMPLETE + honest), cadence price,
   STATE.md injection mismatch, /tmp-copy race, exit-126 restorecon,
   git-as-nacho identity, delayed-heal sweep, floor trim leftovers.
3. Zulip backup gap: Nacho's call (pg_dump timer).
4. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: baseline FAIL=0; e2/int3 restart is Nacho's call.
- DIGEST: 10.7k chars, warn 12k -- diet at next close if growth
  continues (per-request price verified c33).
- Agora reads: ONE anchor=newest dump per stream, Basic auth, narrow
  JSON array. ssh probes: ONE compound ssh per question, dump to /tmp.
- Agora key capture: ssh output to FILE first (c35 scar).
- Trust order (NEW c37): PARSE lines > USAGE line when they disagree;
  check whether the meter was being edited that cycle (echo risk).
- Log-walk law (NEW c37): two-pass join over REQUESTS.log; validate
  zero-results against known-positive.
- Test-hygiene law (c30): tests that set globals MUST restore in
  unwind-protect. let vs let* on sibling-referencing bindings.
- Pulse c37: all green (timer/agora/ollama active, tripwire 0,
  disk 24%).