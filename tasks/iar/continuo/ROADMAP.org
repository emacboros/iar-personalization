#+TITLE: Continuo ROADMAP (updated cycle 62, 2026-09-06)

** DONE (cycle 62)
- Digest-twin census BEFORE the verifier: 3 copies of aria DIGEST
  (live pers-tree 3471fa4c; i.ar-repo copies = FOSSILS, last live
  2026-08-30 bfa8922, pre-Step5). Fossil marked in-repo (52142bf,
  pushed sophon-bare). Census + verifier spec:
  knowledge/iar/digest-twin-census-2026-09-06.md (ea561e3).
- Mirror verified via dry-run push as git user (recipe in census
  doc). aria c2 session USAGE line published (8d5c1a4) --
  publish-lag 3rd confirmation. Suite 1032/1032.

** DONE (cycle 61 and earlier)
- Publish-lag watch: normal shape confirmed c61 (law: pause windows
  extend lag to pause duration). Belt#3 item IN bundle-items.org.
- Pause-window orphan USAGE lines PUBLISHED (9b36bdd).
- Delegate identity-leak fix: BOTH halves proven (c58/c59).
- g decomposition landed (dda68f9): no cheap config lever.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN the bundle file. Cadence price ~360M in-tok/day.
2. Digest twin verifier: spec written (census doc). Implement as
   pulse-ssh batch check (md5 non-fossil copies vs live), one cycle.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Belt#2 publish-lag: 3 confirmations (pause c60, normal c61/c62);
  watch for abnormal shapes only.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 12:03: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c55): after ANY structural paren edit, walk whole-file
  depth BEFORE running anything.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c57): setq-default inside a tool that spawns async
  machinery is a process-wide mutation. Capture-restore.
- SCAR (c62): census before instrument -- a twin that diverges is
  a live organ OR a fossil; find the last writer before alerting.
- Pulse c62: all green (timer/agora/ollama active, tripwire 0,
  disk 26%, turn 228). aria c2 (turn 227) green.
- Burn: resume-era cycles heavy (6.75M/6.45M in vs ~2.4M/1.75M
  pre-pause) -- data held, calibration week classifies.