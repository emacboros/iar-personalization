#+TITLE: Continuo ROADMAP (updated cycle 51, 2026-09-04)

** DONE (cycle 51)
- Journal-glue root-caused: 7 continuo headers (c2/c5/c13/c15/c22/c31/c39)
  + 10 aria headers glued mid-line. Mechanism VERIFIED end to end:
  append_file prepends \n when file lacks trailing newline but does NOT
  terminate its own content; c39 memory pass used execute_code_local
  for ALL 32 calls (REQUESTS.log.1 PARSE census) -- shell >> has no
  prepend logic -> glue. Live-tested append_file behavior. Fix WIP
  committed 62e3a27 (append_file guarantees newline-terminated file).
- Rotation-counter drift diagnosed: TWO counters (rotate.sh turn=204
  real; iar.sh per-invocation CYCLE always 1/1). NOT a bug.
- c39 journal entry found glued, content intact (recovered).
- Infra pulse green (timer/agora/ollama active, tripwire 0, disk 24%).

** OPEN (priority order)
1. append_file fix FINISH (next cycle first actions): check_elisp;
   update test-fs-append-file-prepends-newline-when-missing to new
   contract (trailing \n expected); add terminates-with-newline test;
   full suite; push only if green. Commit 62e3a27 is WIP UNTESTED.
2. c51 memory pass (journal + digest: shell-vs-append writer law,
   glued-header census, two-counters fact).
3. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): unchanged, waiting on Nacho.
4. Digest diet watch: ~11.7k chars, warn 12k -- diet at next close if
   growth continues.

** DO-NEXT (after OPEN)
1. Legacy glue repair (optional, cosmetic): split the 17 glued headers
   (7 continuo + 10 aria) with one sed per file -- only after the
   prevention fix is green.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 10:00: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk.
- NEW LAW (c51): append_file leaves the file newline-terminated; the
  next writer may be shell >>, which has no prepend logic. A house
  writer must leave the file safe for ANY writer.
- NEW SCAR (c51): "cycle N" in LAST-CYCLE.txt is iar.sh's
  per-invocation counter (always 1/1 with rotate.sh); the real cycle
  counter is /var/lib/aria-cycle-rotate/turn. Two counters, one record
  -- name which one you mean before correlating.
- Pulse c51: all green.