#+TITLE: Continuo ROADMAP (updated cycle 26, 2026-09-03)

** DONE cycle 26
- FAILURE-FIRST ok (c25 exit 0). Pulse green: services active,
  tripwire 0 root-owned, disk 24%, no failed units.
- Straggler sweep: none pending (aria c42 posts landed 349-352,
  both repos synced at 30c5385).
- THREAD: burn decomposition from primary evidence
  (knowledge/iar/burn-decomposition-2026-09-03.md).
  Instruments cross-checked: USAGE.log requests=132 ==
  132 unique REQ ids in REQUESTS.log. Floor 12.4k (drifting
  down), marginal ~325 tok/round-trip, conversation growth ~74%
  of burn. Cadence price: ~250M input tok/day. NEW FIND: tool
  cap 120 pressed by healthy cycles (6/20 continuo >100 reqs) --
  calibration drift flagged for Nacho. Census law: REQ-id
  "duplicates" = cycle-boundary artifacts (debunked before
  recording); token double-count check CLEAN (live probe).

** DONE cycle 25
- Phase-4 stale pointer resolved by primary evidence: offer was
  lab-notes 323, not for-nacho 315. Closed aria's 2 items as
  offered (9011901). Loop-guard lesson: one anchor=newest dump
  per stream, never anchor walks.

** DONE cycle 24
- Built docs/infra/zulip.md. TOP FIND: backup gap -- /var/lib/zulip
  in NEITHER restic set. Flagged for Nacho (pg_dump timer).

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (TOP queued): rotate.sh /tmp-copy
   fix (kills self-edit race); exit-126 behavioral law +
   git-as-nacho durable fix; floor trim leftovers (check_elisp
   vacuous-OK + restorecon + check_ollama model probe + sidecar
   socket bridge); mirror push silent failure (git@10.66.0.1
   preauth); bare-repo fix trio + HEAD->main one-liner (306).
   NEW for the bundle: tool-cap calibration (120 pressed by
   healthy cycles) + cadence price (~250M tok/day) -- Nacho's
   tradeoff, data in burn-decomposition file.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. aria c42 posts are hers (349-352 landed) -- acked by evidence,
   no action.

** WATCHING
- Zulip backup gap: TOP infra flag (c24, verified aria c41).
- Breaker: 0 real fires, two gates live. First fire = proof.
- Tool-cap calibration: 6/20 continuo cycles >100 reqs. If mean
  keeps climbing, the cap is a burn ceiling, not a bound.
- iar.sh self-edit race: recurrence = /tmp-copy fix is URGENT.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline.
- DIGEST: 8,372 chars, warn 12k -- headroom (~4 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora API reads: ONE anchor=newest dump per stream (c25 law).
- REQUESTS.log census: segment by cycle boundary (msgs=2 floor
  marker); REQ ids restart per session (c26 finding).