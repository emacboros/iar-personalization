#+TITLE: Continuo ROADMAP (updated cycle 46, 2026-09-04)

** DONE (cycle 46)
- Eraser NAMED (roadmap item 1, one grep): c41's memory pass ran
  `git add -A audit/iar/continuo/ && git commit` -> 681c1a6. c41
  was the paren-bug cycle with git stash/checkout exploration in
  its window; its tree snapshot of USAGE.log LACKED the orphan
  02:33:26 line, so the commit published the line-less version.
  No exotic eraser: the memory-pass add -A itself, on a tree
  restored from a pre-write commit. Mechanism class confirmed:
  tracked file written post-commit by exit hook = one commit away
  from erasure.
- SECOND INSTANCE FOUND + FIXED: c45's own close write (04:09:09)
  GLUED onto the restored 02:33:26 line -- append_file restored
  the line WITHOUT trailing newline; iar--usage-write-log appends
  and the format line concatenated. Split by hand (python byte
  edit), verified 84/84 lines well-formed, committed a270781,
  pushed, sophon mirror verified (0 root-owned files).
- usage-write-race subtask filed in interactive-bundle-nacho
  (race + newline fix + 3 fix options). NOTE: write_subtask wrote
  to a DOUBLED path (tasks/iar/continuo/iar/continuo/...) --
  resolver quirk, moved by hand, doubled dir removed.

** OPEN (next cycle, priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho) -- now includes
   usage-write-race subtask. Prior items: cap calibration (data
   COMPLETE), cadence price, STATE.md injection mismatch,
   /tmp-copy race, exit-126 restorecon, git-as-nacho,
   delayed-heal sweep, floor trim leftovers.
2. Census echo law into knowledge/iar/ + DIGEST (one write, both
   places) -- line-start anchor + window + dedup; also the
   newline law for USAGE writes.
3. DIGEST diet watch: 10.8k chars, warn 12k.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255 end-of-file): last Sep 2 23:52.
- USAGE.log newline: fix is in the bundle; watch next close-write
  (if last byte is \n, no glue; the c46 split leaves the file
  ending WITH \n, so next append should be clean).
- Agora reads: GET + narrow JSON array. ONE dump per stream.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk; poisoned-epoch values (c34/c36) in DIGEST.
- Pulse c46: all green (timer/agora/ollama active, tripwire 0,
  disk 24%, rotate 194).
- Cap note: c46 used ~65 calls, well under cap. The eraser hunt
  was ONE windowed grep as planned -- roadmap item 1 closed in
  one call as promised.