#+TITLE: Continuo ROADMAP (updated cycle 20, 2026-09-03)

** DONE cycle 20
- Overview diet landed: iar-prod _overview.md 9792->7757 chars,
  infra 4563->4113. Removed sections duplicated in full docs
  (hosting-model detail, podman-fix enumeration, SecPlatform block
  duplicating iar-prod overview). Key-fact preservation verified
  (20+ checks, only aardvark dropped to full-docs-only where it
  lives). Suite 1013/1013 green. Saves ~621 tok/req on every
  iar-project request (aria floor ~18.3k->17.7k, continuo
  ~13.6k->12.9k). Floor share projected 36%->~25% at census shape.
  Commits b98d775 (docs) + 2253726 (digest), pushed, residue 0.

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (TOP queued): exit-126 law +
   git-as-nacho durable fix; floor trim leftovers (check_elisp
   vacuous-OK + restorecon + check_ollama model probe + sidecar
   socket bridge); mirror push silent failure (git@10.66.0.1
   preauth); bare-repo fix trio + HEAD->main one-liner (306).
2. Floor-share verify: next census delta re-measures floor share
   post-diet (projected ~25%, was 36%). If it holds, the
   injection-trim thread's structural levers are exhausted --
   remaining burn is cadence price (Nacho-level tradeoff).
3. If aria acks phase-4 offer (for-nacho 315 thread): close her 2
   closeout items (agora-valence-v1-notes.md + THREADS
   organs-exist line), attributed to her as work author.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- Floor share: 36% pre-diet; c20 diet projected ~25%. Verify.
- Boredom ledger: both fresh.
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline.
- DIGEST: 8,263 chars, warn 12k -- headroom (~4 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Flag 326 (aria's), flag 306 (aria's).