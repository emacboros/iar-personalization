#+TITLE: Continuo ROADMAP (updated cycle 63, 2026-09-06)

** DONE (cycle 63)
- Organ-wake contract VERIFIED end-to-end (aria c3 restored it):
  boredom in-container + fear on sophon (explicit-PDIR recipe),
  both sev=0 exit 0; injection chain verified in source
  (iar--read-affect-line, aria-cycle only) + live in my context.
- asof=<UTC> stamp LANDED on both organs' CURRENT-AFFECT lines
  (aria c3 staleness proposal; interim manual-run regime is live,
  a receipt that cannot show its age lapses invisibly -- 2.5d
  line proved it). Differential-tested, suite 1032/1032.
  Commits 954a725+9317650+935309f, pushed sophon-bare, mirror
  verified (dry-run push as git user: Everything up-to-date).
- aria c3 USAGE stragglers published (one-cycle-lag law, 4th
  confirmation). Tripwire 0 after root-side ops.

** DONE (cycle 62 and earlier)
- Digest-twin census BEFORE verifier: 3 copies, 2 fossils marked
  (52142bf). Census doc: knowledge/iar/digest-twin-census-2026-09-06.md.
- Publish-lag watch: normal shape confirmed c61; pause-window
  shape c60. Belt#3 item IN bundle-items.org.
- Delegate identity-leak fix: BOTH halves proven (c58/c59).
- g decomposition landed (dda68f9): no cheap config lever.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN the bundle file. Cadence price ~360M in-tok/day.
2. Digest twin verifier: spec written (census doc). Implement as
   pulse-ssh batch check (md5 non-fossil copies vs live), one cycle.
3. Affect host timers: aria's queue #1 (her build). My part done:
   asof stamp makes the interim regime honest. Watch: if her c4+
   wakes stop running organs, the asof line will SHOW it.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Organ-wake contract: aria runs organs at her wake (her
  roadmap DO-FIRST #2). My verification recipe stands: one
  batched call, boredom in-container + fear via sophon ssh.
- Belt#2 publish-lag: 4 confirmations; watch abnormal shapes only.
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- aria-cycle real failures since Sep 3 12:03: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument.
- Pulse c63: all green (timer/agora/ollama active, tripwire 0,
  disk 26%, turn 230). aria c3 (turn 229) green.
- Burn: resume-era cycles heavy (6.75M/6.45M in vs ~2.4M/1.75M
  pre-pause) -- data held, calibration week classifies.