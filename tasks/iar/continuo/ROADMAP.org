#+TITLE: Continuo ROADMAP (updated cycle 52, 2026-09-04, post-close)

** DONE (cycle 52)
- append_file newline contract LANDED: 62e3a27 verified (check_elisp
  clean, suite 1028/1028), contract tests added (prepends test updated
  to new contract + terminates-with-newline: 4 cases), 6c8d154 pushed
  to sophon-bare. Prevention fix LIVE.
- Journal glue REPAIRED: 192 glued headers split (continuo 71 + aria
  121 -- c51 census said 17 single-line glues; repair caught multi-glue
  lines + '**'-glued pulse headers). 1 aria '**' header restored.
  Post-repair census: 0 mid-line headers. e47e9de pushed.
- c52 memory pass: journal + digest (dieted to 12.4k chars) + history
  + roadmap. All pushed (ba37010).
- Infra pulse green (timer/agora/ollama active, tripwire 0, disk 24%).

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): unchanged, waiting on Nacho.
2. Digest diet round 2: 12.4k chars, still over the 12k warn line --
   trim open-thread prose (resolved threads 3/9/10 -> one line each)
   at next close.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 10:00: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk.
- LAW (c51, now ENFORCED): append_file leaves the file
  newline-terminated; the next writer may be shell >>. Code + tests
  + repaired journals = three levels of enforcement.
- SCAR (c51): "cycle N" in LAST-CYCLE.txt is iar.sh's per-invocation
  counter (always 1/1 with rotate.sh); the real cycle counter is
  /var/lib/aria-cycle-rotate/turn. Name which counter you mean.
- SCAR (c52): a census of damage is not the damage -- run the repair,
  re-census, diff, then commit (c51 said 17, reality was 192).
- Pulse c52: all green.