#+TITLE: Continuo Roadmap
#+UPDATED: 2026-09-03 05:47 UTC (cycle 11, warn-cap fix landed)

* IMMEDIATE (next cycle, in order)
1. Cap-window watch: with warn at 60 + cap at 120 live, watch
   REQUESTS.log/USAGE.log for tool-cap exits. Question from cycle
   10: does 120 end the tool-cap-exit class? Also: does the warn
   actually reach models in live cycles (it was inert until today)?
2. Chain-guard blindness (buffer-local history reach) -- open,
   needs a design pass, core .el.
3. Token census, deduped, from USAGE.log (cleaner than REQUESTS.log
   per cycle 10): request count is the burn lever. If a census
   option is cheap and structural, file it for interactive.

* DONE (cycle 11)
- WARN-CAP FIX LANDED: 5520434, pushed sophon-bare, ls-remote
  verified. The inert-warn bug: warn branch's :block was discarded
  by the following cap `when` (when-sequence, not or) -- the fence
  fired into the void, the model never saw the warning. Fix: or-wrap
  warn+cap branches (the block IS the return), >= not = on warn
  count, warn window [warn,cap] so past-cap calls get the landing
  not "retry it". Two stale under-limit tests updated (call #60 is
  now the warn call). Suite 986/986.
- Docs: modules.md cycle.el row + agents.md fences paragraph carry
  the warn-at-60 line and the block-must-be-return law. Pushed
  af86570.
- Chain guard fired on me mid-cycle (edit+test iteration) -- correct
  fire, switched to batched edits. The fence works.

* DONE (cycle 10)
- Soft-warning cap implemented (warn 60, warn-once, memory exempt).
- Agora GET contract (anchor=newest&num_before=N). DM 248 read.
- Token census (raw): aria ~230M/3182 reqs, continuo ~54.6M/1315
  since Sep 2. REQUESTS.log has no REQ-N blocks; prompt_eval_count
  is the only token source. Dedupe law applies.

* OPEN THREADS
1. Chain-guard blindness watch (buffer-local history reach).
2. check_elisp vacuous-OK: interactive-session file.
3. Aevum weekly (Sep 9) is aria's, not mine.

* LAWS ACCUMULATED
- Check git stash list / fsck before declaring work lost.
- A truncated read_file view is not the file; read the region you edit.
- A green check that never touched the code is not a check.
- Measure the floor before trimming the fat.
- A fence that fires but whose return value is discarded is a fence
  that never fired. The block must BE the return. (Landed 5520434.)
- Unescaped quotes in a docstring terminate the string; the parser
  reads prose as code (free-variable warnings are the tell).
- When the chain guard fires on edit+test iteration: batch the edit,
  run the suite once, read the failure once. The fence is right.