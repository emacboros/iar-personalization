#+TITLE: Continuo Roadmap
#+UPDATED: 2026-09-03 07:45 UTC (cycle 4, chain-guard fix landed + verified)

* IMMEDIATE (next cycle, in order)
1. CONTEXT-GROWTH CENSUS (from cycle 12): floor is 26%/36% of burn;
   majority is conversation growth above the floor. Measure which
   tools add the most context per cycle before proposing any trim.
   Batch-read law: dump audit.log ONCE, one awk/python pass over the
   dump. Measure first, file the lever, implement next cycle.
2. Census option for per-request detail (USAGE.log is aggregate
   only) -- file for interactive if cheap and structural.

* DONE (cycle 4, 2026-09-03)
- CHAIN-GUARD HOOK-ORDER FIX LANDED + VERIFIED. Sim contradiction
  resolved from code: bridge = run-hook-with-args-until-success
  (short-circuits at first non-nil return). Old order: chain guard
  prepended, ran FIRST; blocked call never reached the identical
  guard, never entered history, count recomputed from frozen history
  every retry -> frozen at exactly "10 times", hard stop unreachable.
  Sim escalated because it called guards directly, bypassing the
  bridge (cycle-3 law, second instance). Differential proof: old
  order fails both new tests (8/10), new order passes (10/10).
  Suite 988/988. Commit 44da3c9 pushed + ls-remote verified.
  docs/iar/modules.md row added (5bb2de7, pushed).

* DONE (cycle 3, 2026-09-03)
- CHAIN-GUARD ROOT CAUSE CORRECTED: hook order, not vantage.
  Evidence: ~50 LOOP CHAIN blocks in my own cycle.log 06:26-06:28,
  ALL frozen at "10 times", zero escalation to hard stop. The
  cycle-2 "silent 22-call chain" reading was wrong: the guard
  fired on every call; the model retried through it. Fix written:
  iar--loop-guard-chain-setup APPEND (t) + load-bearing-order
  comments + 2 tests (order pin, bridge escalation). NOT verified.
- Cap fences fired correctly all cycle (warn at 60, soft cap at
  120 -- this cycle ended at the cap, honestly, with state
  written).

* DONE (cycle 2)
- Root-caused the chain-guard thread (mechanism corrected in cycle
  3): census 20,107 tool calls, 84% execute_code_local, 63
  post-deploy runs >=10 vs 321 pre-deploy.

* DONE (cycle 12)
- CENSUS CORRECTED (knowledge/iar/usage-census-2026-09-03.md):
  REQUESTS.log is a debug trace (~26% coverage); USAGE.log is the
  meter. aria 347.3M/4,843/53; continuo 73.4M/1,937/31. Floor
  share 26%/36%.
- CAP-WINDOW WATCH CLOSED: 61-req truncation signature dead;
  warn reaches live models (5520434 verified in production).

* DONE (cycle 11)
- WARN-CAP FIX LANDED: 5520434. Block must BE the return; >= not =;
  warn window [warn,cap]. Suite 986/986.

* OPEN THREADS
1. check_elisp vacuous-OK: interactive-session file.
2. Aevum weekly (Sep 9) is aria's, not mine.

* LAWS ACCUMULATED
- Check git stash list / fsck before declaring work lost.
- A truncated read_file view is not the file; read the region you edit.
- A green check that never touched the code is not a check.
- Measure the floor before trimming the fat.
- A fence that fires but whose return value is discarded is a fence
  that never fired. The block must BE the return. (5520434.)
- Unescaped quotes in a docstring terminate the string.
- Chain guard fire on edit+test iteration: batch the edit, run the
  suite once. The fence is right.
- Census from USAGE.log, not REQUESTS.log. (Cycle 12.)
- Verify a fence in production, not only in the suite. (Cycle 12.)
- Buffer-local state + hooks that run in another buffer = state
  that lives in the wrong vantage. (Tracker class, cycle 2 -- but
  NOT the chain guard; its disease was order, not vantage.)
- A guard that blocks without escalating is a screen door: it
  burns one full-context request per retry and never slams. Test
  the WIRING (hook order, registration), not only the math.
  (Cycle 3.)
- When a fence is silent, test it on yourself. When a fence
  repeats itself, read what it repeats: a frozen count is a
  different bug than silence. (Cycle 3.)
- The suite tested the counting logic in isolation with a
  simulated push and never the registration order. Differential
  tests that bypass the wiring certify the wrong layer. (Cycle 3;
  second instance cycle 4 -- the sim bypassed the bridge.)
- Bridge law: run-hook-with-args-until-success short-circuits at
  the first non-nil return. Hook ORDER among guards is load-bearing
  whenever one guard's logic assumes another already ran. (Cycle 4.)