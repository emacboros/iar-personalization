#+TITLE: Continuo ROADMAP (updated cycle 28, 2026-09-03)

** DONE cycle 28
- FAILURE-FIRST ok. Pulse green (timer/agora/ollama active, tripwire
  0, disk 24%). Agora read: Nacho silent since 20:00 UTC.
- THREAD: aria c44 found iar-prod master divergence -- sophon master
  = cd05e63 (orphan test commit from MY c19, mis-recorded as
  "unreachable"), rammstein master = 1c73f86. Non-FF mirror hazard.
  RESOLVED: refs/heads/master deleted both sides as git user
  (standing direction with-nacho 238, ref-only reversible). cd05e63
  preserved (prune does not collect packed objects). Mirror-loop
  confirmed REAL both directions. Verified: both sides main-only,
  residue 0/0.
- git-server.md addendum committed (d1f420b) + pushed; mirror
  verified live (55a8cd9 on rammstein).
- Task tree: interactive-bundle-nacho created (items preserved from
  DIGEST into subtask file).

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (TOP): now a real task tree
   (iar/continuo/interactive-bundle-nacho). Items: /tmp-copy race;
   exit-126 restorecon; git-as-nacho identity; delayed-heal sweep +
   mirror-loop note; tool-cap calibration + cadence price; floor
   trim leftovers.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT (bundle item 1).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: FAIL=0 baseline.
- DIGEST: 9,351 chars, warn 12k -- headroom (~3 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE
  compound ssh per question.
- REQUESTS.log census: segment by cycle boundary (msgs=2 marker).
- Mirror-loop: harmless while refs agree; watch for silent push
  failures in the loop (known-issue 3).