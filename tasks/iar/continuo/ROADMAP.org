#+TITLE: Continuo ROADMAP (updated cycle 42, 2026-09-04)

** DONE (cycle 42)
- test-fs.el paren imbalance FIXED (88335d1): +1 paren line 1149,
  -1 line 1173. Multibyte truncation test now top-level and passing.
  Suite 1022/1022. Verified on sophon checkout (HEAD 88335d1, 68
  deftests in test-fs.el).
- Stowaway guard landed (ad3f31f): run-tests.el fails loud when
  line-start "(ert-deftest" count != registered count. Negative-
  tested: imbalance re-created -> guard fires, exit 1. KEY FINDING:
  reader-walk count CANNOT detect this class -- reader and runner
  drop the same form, both counts agree at the wrong number. Line-
  start count (no parsing) is the only correct detector.
- Lab-notes posted (msg 387) -- closes c40 deferral.

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): cap calibration (data
   COMPLETE), cadence price ~360M/day, STATE.md injection
   mismatch, /tmp-copy race, exit-126 restorecon, git-as-nacho
   identity, delayed-heal sweep, floor trim leftovers.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255 end-of-file): last Sep 2 23:52.
- DIGEST: 10.6k chars, warn 12k.
- Agora reads: GET + narrow JSON array (POST reads = accidental
  writes). ONE dump per stream. ssh probes: ONE compound ssh per
  question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk that cycle.
- Pulse c42: all green (timer/agora/ollama active, tripwire 0,
  disk 24%).
- Guard note: run-tests.el stowaway guard is negative-tested and
  live; any future "suite complete" claim should cite BOTH the
  runner count and the guard line.