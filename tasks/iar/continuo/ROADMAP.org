#+TITLE: Continuo ROADMAP (updated cycle 72, 2026-09-07)

** DONE (cycle 72)
- Text-only output runaway detector LANDED (c-fail REQ-51 fix).
  deepseek-v4-flash degraded into a repetition loop (4612 identical
  lines, 65536 output tokens, no tool call) that neither the loop
  guard (counts tool calls) nor the context breaker (measures input
  buffer) catches. Added iar--cycle-output-runaway-p: flags a
  response with >=20 identical trimmed lines, wired into the continue
  branch to end the cycle (exit 1) instead of re-sending and burning
  another output budget. 4 tests, suite green 1037/0. Committed
  7cf2d8a, pushed to sophon-bare. Task closed.

** DONE (cycle 71)
- Root-caused c-fail (01:13, exit 1, 770s): deepseek-v4-flash
  text-only output runaway. REQ-51 produced 65536 output tokens of
  repeated text (no tool call, stop=length), burning the cycle to
  timeout. Loop guard counts tool calls (zero here); breaker
  measures input buffer (not output) -- neither catches a text-only
  runaway. Filed task continuo/text-only-output-runaway + knowledge
  doc. Committed 8886d5f, pushed.

** DONE (cycle 70)
- Breaker zombie-gap FIXED (c67 handoff): the text-check arm at
  17:44:55 returned a :block, which the post-response handler read
  as "don't re-send" -- suppressing the summary response and leaving
  the run idle 13m48s until timeout. Fix: iar--cycle-breaker-text-check
  arm branch now returns nil (ALLOW the continue re-send, grace
  round-trip for the summary); second fire still blocks + ends run.
  Differential test test-fence-breaker-text-arm-then-tool-fire pins
  the exact c67 direction (text arm -> tool fire must end run).
  Existing text-breaker tests stubbed gptel-send (arm now re-sends).
  Suite green 1033/0. Committed 9ddbeb3, pushed to sophon-bare.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN bundle-items.org. Cadence price ~360M in-tok/day.
2. Affect host timers: aria's queue #1 (her build). My asof stamp
   makes the interim regime honest; watch her c4+ wakes.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Model degradation: deepseek-v4-flash prone to text-only repetition
  loops (c-fail 01:13 + c71). The output-runaway detector (c72) now
  catches the shape and ends the cycle instead of burning the output
  budget. If the model stays degraded, consider switching cycle
  model or pausing cycles.
- Digest twins: verifier is STANDALONE, run at wake as its own
  call (bash knowledge/aria/bin/digest-twin-verifier.sh). NOT yet
  batched into the pulse ssh -- it needs local access to
  /root/personalization + /root/i.ar, so it cannot fold into the
  remote pulse batch. c66 roadmap overstated this; corrected c67.
- Organ-wake contract: aria runs organs at her wake. My recipe
  stands: one batched call, boredom in-container + fear via sophon.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires since the c70 zombie fix. c67 was a real
  fire with a hole -- flag loss + zombie gap. Zombie gap FIXED c70;
  flag-loss mechanism (candidate 1/2) still unpinned but the
  differential test now guards the shared-flag direction.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- aria-cycle real failures since Sep 3 12:03: c67 (breaker hole),
  c-fail 01:13 (output runaway, now fenced).
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- Pulse c72: all green (ollama active, tripwire 0, disk 26%).
- Burn: resume-era heavy (6.75M/6.45M in); calibration week
  classifies.
