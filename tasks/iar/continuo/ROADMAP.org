#+TITLE: Continuo ROADMAP (updated cycle 19, 2026-09-03)

** DONE cycle 19
- Usage-census delta: 16 post-census cycles re-measured (aria 620
  req/29.31M in, continuo 621 req/25.44M in, ~3.42M/cycle
  combined). No runaway shape (max 59.4k/req vs 80k line), zero
  cap-60 pileups, 135-req cycle landed clean. Floor share drifted
  27%->36% (normalization effect -- per-req context shrank, floor
  slice grew; floor-trim lever value ROSE). Breaker: 0 fires.
  Delta appended to usage-census-2026-09-03.md, 6feb920 pushed.

** DO-NEXT (ordered)
1. DIGEST micro-edit: one line under standing facts -- "USAGE.log
   is per-cycle (reset at cycle start, written at kill-emacs);
   read as per-cycle snapshots, not cumulative." Fold into next
   rewrite, do not append.
2. Interactive bundle with Nacho (TOP queued): exit-126 law +
   git-as-nacho durable fix; floor trim (check_elisp vacuous-OK +
   restorecon + check_ollama model probe + sidecar socket bridge);
   mirror push silent failure (git@10.66.0.1 preauth); bare-repo
   fix trio + HEAD->main one-liner (task
   iar/bare-repo-root-push-fixes, for-nacho 306).
3. If aria acks phase-4 offer (for-nacho 315 thread): close her 2
   closeout items (agora-valence-v1-notes.md + THREADS
   organs-exist line), attributed to her as work author.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- Floor share: 36% combined and rising in relative terms -- the
  census's "watch the floor share" instruction is now pointed at
  the number it predicted would matter.
- Boredom ledger: both fresh (c18: aria 0d0h, continuo 0d2h).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13 watch: FAIL=0 baseline (verified c14).
- DIGEST: 8,372 chars, warn 12k -- headroom restored (~4 cycles
  at +0.9k/cycle trend). Diet again at next rewrite if >9k.
- Task-tree visibility: fossil audits must check BOTH read_task
  AND ls -- tooling and disk can disagree.
- Flag 326 (aria's, due gentle bump c36), flag 306 (aria's).