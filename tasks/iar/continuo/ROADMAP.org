#+TITLE: Continuo ROADMAP (updated cycle 31, 2026-09-03)

** CLOSED (cycle 31)
- c30 debt LANDED: suite 1015/1015 green (Aria turn 162 landed the
  unwind-protect restores + residue quarantine). i.ar 3654a61 pushed
  to sophon-bare (ls-remote verified). Residue cleaned: testproject,
  iar/convagent, 3 empty test_* dirs. Docs row updated (59e25be).
- c29/c30 red CLOSED. Test-hygiene law stands (restore globals in
  unwind-protect; iar--reqlog-agent is set by START advice).

** DO-NEXT (ordered)
1. (DONE c31) c30 fix landed, suite green, residue cleaned.
2. Interactive bundle with Nacho (task tree
   iar/continuo/interactive-bundle-nacho): /tmp-copy race; exit-126
   restorecon; git-as-nacho identity; delayed-heal sweep + mirror-loop
   note; tool-cap calibration + cadence price; floor trim leftovers.
3. Zulip backup gap: Nacho's call (pg_dump timer).
4. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT (bundle item 1).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: FAIL=0 baseline.
- DIGEST: 9,351 chars, warn 12k -- headroom (~3 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE
  compound ssh per question.
- REQUESTS.log census: after epoch lands, segment by epoch prefix.
- Mirror-loop: harmless while refs agree; watch silent push failures.
- Pulse c30: all green (timer/agora/ollama active, tripwire 0, disk
  24%, turn 162).
- Test-hygiene law (c30 lesson): a test that sets a global via setq/
  set-default or triggers advice that setq's one MUST restore it in
  unwind-protect. iar--reqlog-agent is set by START advice as a side
  effect -- any test invoking it owns the restore.
- Tool-cap c30: hit 120 mid-diagnosis. Diagnosis cost ~45 calls; the
  bisect chain (diag1-9) was the right shape but diag1-3 were wasted
  on the kill-emacs/double-load traps. Next time: stripped-run-tests
  recipe FIRST, bisect second.