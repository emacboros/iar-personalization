#+TITLE: Continuo ROADMAP (updated cycle 49, 2026-09-04)

** DONE (cycle 49)
- USAGE newline guard: CODE COMPLETE, tests pass individually,
  check_elisp clean. iar--ensure-trailing-newline (literal last-byte
  read) + guard call in iar--usage-write-log. Committed as WIP
  332a75a on branch continuo/usage-write-fix, pushed to sophon-bare.
- Infra pulse: all green (timer/agora/ollama active, tripwire 0,
  disk 24%). Bundle task verified intact (description + bundle-items
  + usage-write-race).
- c48 librarian fossil: closed (already dead, root cause named).

** BLOCKED (cycle 49, c50's first job)
- Suite blocker: full suite dies DETERMINISTICALLY at 977/1027
  (100/100 runs) -- "error in process sentinel: Wrong type argument:
  markerp, nil" at the test-usage-newline -> test-utf8-scrub
  boundary. Baseline WITHOUT test-usage-newline.el: 1022/1022 green
  exit 0, no sentinel error. Sleep-for hypothesis DISPROVEN (removed,
  death persisted). Load-order hypothesis DISPROVEN (rename-to-zz,
  death persisted). One of the 5 tests leaves process/marker state
  the next file's first test trips. c50: bisect the 5 tests (delete
  half, run, repeat -- 3 runs max), find the state-leaker, fix,
  land, push. NEVER push red.

** OPEN (priority order)
1. c50: resolve suite blocker, land newline guard on main.
2. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): usage-write-race
   subtask + bundle-items. Data side COMPLETE. Waiting on Nacho.
3. Digest diet watch: ~11.4k chars, warn 12k.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255): last Sep 2 23:52.
- aria-cycle real failures since Sep 3 10:00: ZERO.
- ssh probes: ONE compound ssh per question, dump to /tmp. Git
  archaeology over ssh = dump reflog+log ONCE, read locally.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk.
- Pulse c49: all green. aria c15 closed 04:55 (52 reqs, 1.7M).
- NEW LAW (c49): batch-TEST law -- deterministic failure needs one
  diagnostic run, not 100 confirmations. Re-running with a counter
  changed is the same runaway shape as batch-read violations.
- Scar c49: ert test files that create fake processes can poison
  later files' first test; the suite's death boundary is a
  diagnostic, not a verdict on the code under test.