#+TITLE: Continuo ROADMAP (updated cycle 29, 2026-09-03)

** IN-FLIGHT (cycle 29, UNCOMMITTED -- suite RED)
- THREAD: REQ-id epoch prefix (c26 instrument finding: bare counters
  collide across cycles in shared REQUESTS.log). Patched
  iar-request-log.el: iar--reqlog-epoch defvar (boot time, 12 digits),
  id = "epoch-N", all 5 REQ call sites %d -> %s. 2 new tests PASS
  (test-reqlog-epoch-prefix-unique-across-sessions,
  test-reqlog-epoch-set-once-at-load) in test-coverage-aria.el.
- BLOCKER: 2 tests in test-request-log.el (untouched) FAIL:
  test-reqlog-rotation, test-reqlog-filter-advice-resignals. Both fail
  (file-exists-p REQUESTS.log) = nil after append. Direct repro of the
  filter path in isolation WRITES fine (debug10/11 in /tmp). Prime
  suspect: cross-test pollution -- test-coverage-aria.el loads BEFORE
  test-request-log.el; my new epoch test sets
  iar--current-agent-name "convagent" via setq (not unwind-protected?)
  and iar--reqlog-agent is setq'd globally by start advice; a stale
  global agent name redirects iar--reqlog-path for later tests. Check:
  does my epoch test restore iar--reqlog-agent? (it does NOT).
  FIX CANDIDATE: in my new test, save/restore iar--reqlog-agent +
  iar--reqlog-counter + iar--reqlog-epoch in unwind-protect.
- Debug recipe: /tmp/reqlog-debug12.el loads run-tests.el then runs the
  failing test body directly (container /tmp, recreate per cycle).
  Suite: IAR_ROOT=/root/i.ar IAR_PERS=/root/personalization emacs
  --batch -l emacs.d/test/run-tests.el from /root/i.ar.
- After green: commit (iar-request-log.el + test-coverage-aria.el),
  push sophon-bare, update docs/iar/modules.md request-log row (mention
  epoch-prefixed ids), update DIGEST instruments section.

** DO-NEXT (ordered)
1. Cycle 29 blocker: root-cause the 2 test failures, land the epoch
   prefix green. (TOP)
2. Interactive bundle with Nacho (task tree
   iar/continuo/interactive-bundle-nacho): /tmp-copy race; exit-126
   restorecon; git-as-nacho identity; delayed-heal sweep + mirror-loop
   note; tool-cap calibration + cadence price; floor trim leftovers.
3. Zulip backup gap: Nacho's call (pg_dump timer).
4. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT (bundle item 1).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: FAIL=0 baseline.
- DIGEST: 9,351 chars, warn 12k -- headroom (~3 cycles).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE
  compound ssh per question.
- REQUESTS.log census: after epoch lands, segment by epoch prefix
  (replaces msgs=2 marker).
- Mirror-loop: harmless while refs agree; watch for silent push
  failures in the loop (known-issue 3).
- Pulse c29: all green (timer/agora/ollama active, tripwire 0, disk
  24%, turn 160). Suite was 1013/1013 at wake.