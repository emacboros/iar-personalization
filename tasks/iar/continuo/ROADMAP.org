#+TITLE: Continuo ROADMAP (updated cycle 92, 2026-09-07)

** DONE (cycle 92)
- USAGE 01:30/01:51 UTC residue RESOLVED (c88/c90 watching item). Turns
  263/265 (Sep 6 22:21/22:41 local) exited 0 but wrote no USAGE line.
  Root cause: SAME orphan-race class as the 08:32 line (c85) -- belt #2
  pre-exit writes from the PRE-durability era (a7e1cf5 live, d2d272d not
  yet landed) landed UNCOMMITTED and were eaten by aria c11's
  `git reset --hard origin/main` at 08:35:33 UTC (reflog 15e07a4). One
  reset, three orphaned lines (01:30, 01:51, 08:32), one class. The
  durability fix (d2d272d, 10:34 UTC) landed after the window; post-fix
  cycles (10:43 onward) all have committed lines. Watching item CLOSED.
  Addendum in knowledge/iar/usage-line-gap-2026-09-07.md, commit 90b4551.

** DONE (cycle 91)
- FAILURE-REDUCTION follow-up: runaway recovery prompt hardened. The
  c90 census showed recovery works only 25% (2 of 8). Old prompt
  offered a prose choice ("pick ONE action or CYCLE_COMPLETE") -- a
  text-only loop repeats instead of snapping out. New prompt FORCES a
  tool call (append_file to JOURNAL.org) -- the one thing a text-only
  runaway is NOT doing, so it breaks the loop shape directly. Commit
  e331a83, suite 1063/0 green, pushed sophon-bare.

** DONE (cycle 90)
- FAILURE CENSUS (Nacho's top priority): 3-day window, batch-read.
  FINDING: the 2026-09-06 15:32 rotate.sh edit (continuo ->
  deepseek-v4-flash:cloud) inverted the failure rate: 0 failures in
  2 days pre-change (46 cycles), 18 in 1.5 days post (60 cycles,
  23%). Dominant class: deepseek text-only output runaway (8 ending
  cycles, 7 on continuo) + context breaker (5, all continuo -- same
  root cause, deepseek burns context). All fences fired correctly;
  this is MODEL degradation, not machinery. Fix direction: revert
  continuo to glm-5.3-flash:cloud (Nacho call -- rotate.sh is root
  infra). Census: knowledge/iar/cycle-failure-census-2026-09-07.md,
  commit 907880f, posted lab-notes.

** DONE (cycle 88)
- soft-cap-c67-differential CLOSED (aria c16 landed the deliverable:
  test-cap-block-reaches-buffer.el, commit 5948ed9, suite 1063/0).
  Verdict PINNED: machinery delivers the message to context; c86's
  model still called tools past it -> model non-compliance (watching
  item), not a machinery gap. Task dir removed, suite re-verified
  green (1063/0) this cycle.

** DONE (cycle 87)
- FAILURE-FIRST root-cause c86 exit 1 (132 calls, 4.48M tok): model
  degradation (deepseek-v4-flash text-only repetition loop, c67
  family). All fences fired correctly. Not a machinery bug.
- Closed belt2-add-f-force-stage task (aria landed it, suite 1059/0).
- Filed soft-cap-c67-differential task (urgent). RESOLVED c88.

** DONE (cycle 86)
- Belt #2 durability LANDED (c85 fix): iar--usage-write-log-now now
  COMMITS its own USAGE line. Suite 1057/0. Committed d2d272d.
- Graveyard doc fixes: Phase 0 LANDED marker, ox dependency VERIFIED.
- Rebase/merge of diverged shared tree.

** OPEN (priority order)
1. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho): waiting on Nacho.
   Belt#3 item IN bundle-items.org. Cadence price ~360M in-tok/day.
2. FAILURE-REDUCTION (Nacho's top priority): census DONE c90, recovery
   prompt hardened c91. The single biggest lever is STILL the model
   mapping (deepseek -> glm revert, Nacho's call on rotate.sh). If
   deepseek stays, the hardened recovery prompt is the mitigation.
3. Affect host timers: aria's queue #1 (her build).
4. Belt #3 (iar.sh parsing "Tokens:" stdout): optional now that belt
   #2 commits its own line.

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Model degradation: deepseek-v4-flash text-only repetition loops.
  c90 census QUANTIFIED: 8 ending runaways in 1.5 days, all but 1 on
  continuo (deepseek). Recovery prompt hardened c91 (force tool call).
  Revert is still the lever.
- Digest twins: verifier is STANDALONE, run at wake as its own call.
- Organ-wake contract: aria runs organs at her wake.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires since the c70 zombie fix. Now measures
  SENDABLE context (c79). c90: 5 fires, all continuo (deepseek
  context appetite) -- working as designed, model-driven.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- SCAR (c84): check the clock domain before attributing a mechanism.
- SCAR (c85): belt #2's write is only as durable as the next commit.
  CLOSED c92: durability fix d2d272d landed 10:34 UTC; post-fix
  cycles all committed. The 01:30/01:51/08:32 residue was the
  pre-durability class, eaten by aria c11's reset --hard.
- Pulse c92: all green (timer/agora/ollama active, tripwire 0, disk
  26%). USAGE residue resolved -- the last open meter question is
  closed.
