#+TITLE: Continuo ROADMAP (updated cycle 219, 2026-09-22)

** DONE (cycle 219, 2026-09-22)
- Fixed FIXME in gptel-context.el regarding parsing directive in the prompt construction buffer.
- Added helper function gptel-context--parsed-system-prompt to safely parse directive functions.
- Replaced direct call to gptel--parse-directive with this helper to avoid buffer mismatch issues.
- Verified the change with check_elisp (only warnings about missing lexical-binding, which are pre-existing).
- Ran full test suite: all 1331 tests passed.
- Committed change to the i.ar repository.

** DONE (cycle 57, 2026-09-18)
- Stricten context budget threshold to 40 tool calls per cycle to prevent token burn from excessive accumulation.
- Reviewed by agent-assistant, committed.

** DONE (cycle 45, 2026-09-18)
- Fixed lexical-binding warnings in test suite (13 files), committed and reviewed.
- Updated census window log for 2026-09-17 with real data from sophon host.
- Verified test suite passes (1327/1327).
- No machinery changes needed.

** DONE (cycle 192, 2026-09-17)
- Verified failure-reduction mechanisms are working (census window clean, test suite passed, digest twins in sync).
- No machinery changes needed.

** DONE (cycle 190)
- Verified machinery integrity (test suite passed).
- Confirmed failure-reduction measures live (context budget guard active, census window clean).
- No machinery changes needed.

** DONE (cycle 189)
- FIRE CENSUS CORRECTED (anchored method): 6 REAL fires today, all
  deepseek-v4-flash:cloud, all halved 32768 cap: 04:47 (26090905004340-32),
  05:06 (260909050045-37), 05:45 (260909054050-26), 06:03 (260909060051-7),
  07:22 (260909072043-12), 08:22 (260909082043-12). c184 over-counted
  (its 05:02/05:21/05:43 were stop=stop, not fires); c186 06:41/06:42
  also not fires. Earlier 7/10 counts inflated by non-anchored greps +
  quoted-content self-inflation (c32 law). Census method: anchor on
  line-start ^\[...\] REQ ... PARSE, read FINAL tokens_out field.

** DONE (cycle 184)
- FIRE CENSUS 2026-09-09 (superseded by c189 correction): 7 REAL fires
  reported; c189 corrects to 6 real (04:47/05:06/05:45/06:03/07:22/08:22),
  all deepseek. c184's 05:02/05:21/05:43 were NOT fires.
- CORRECTION to c183: the 06:21-06:23 cluster was c183's OWN
  self-meter-reading loop, NOT independent fires. c32 self-inflation
  law holds.
- SUPERSEDED 2026-09-10 (aria session XII, Nacho): the model-mapping
  lever is DEAD. D-014 (09-09 09:24Z) moved continuo to
  nemotron-3-super:cloud; Nacho REJECTED the glm revert in session IX
  (two-substrate design). Do not re-raise. The fires are addressed at
  mechanism level: thinking-loop truncation guard (aria, i.ar).

** DONE (cycle 112)
- LANDED cross-response repetition guard (task
  iar/continuo/cross-response-repetition-guard, commit 35531f9,
  pushed sophon-bare). Catches deepseek text-only loop at response
  ~5-6, saving ~17 requests of burn per occurrence. 5 new tests,
  suite 1076/1076 green.

** DONE (cycle 111)
- FAILURE-FIRST root-cause turn 362 (exit 1): FOURTH truncated-output
  fire today (15:23/17:03/17:36/18:15 local -- all continuo/deepseek,
  all stop=length 65536 tokens, all text-only repetition loops). Guard
  works but the CLASS keeps recurring. (Superseded 2026-09-10: the
  model-mapping lever is dead -- D-014 stands, mechanism-level fix
  in i.ar instead.)
- FINDING: per-response output-runaway guard is the WRONG GRANULARITY
  for this class. Cross-response guard LANDED c112.

** DONE (cycle 110)
- FAILURE-FIRST root-cause turn 357 (exit 1): deepseek text-only
  repetition loop (523x, 682k input tokens, 29 req), truncated-output
  guard fired (3rd production fire). Guard working as designed.

** DONE (cycle 109)
- FAILURE-FIRST root-cause turn 353 (exit 1): truncated-output guard
  (190ef88) SECOND production fire -- req 87, stop=length 65536 tokens.
  Genuine degradation, guard working as designed, NOT a machinery bug.
- Verified chain guard does NOT catch aria's 05:11 self-meter-reading
  burst (consecutive Jaccard mean 0.42, only 35% chain-counted).

** DONE (cycle 144, 2026-09-20)
- FAILURE-FIRST root-cause: cycle 1 failed due to repeated identical
  execute_code_local calls (tail on cycle log) violating batch-read law.
  Applied batch-read law: created dump-cycle-log.sh script to dump to
  /tmp once and read from dump. Added task
  failure-reduction/avoid-repeated-tail-calls with check-recent-cycles
  and apply-batch-read-law subtasks.

** DONE (cycle 149, 2026-09-20)
- FAILURE-FIRST: continued work on avoidance of repeated tail calls.
  Created dump-cycle-log.sh script that dumps the cycle log to /tmp once
  for batch reading, preventing repeated tail calls on the same file.
  Verified no repeated tail calls in current cycle log (cycle-2026-09-20.log).
  Checked recent cycles and found repeated tail patterns in
  cycle-2026-09-18.log (6x tail -10 /var/log/app.log, etc.).
  Applied batch-read law to avoid token burn from repeated round-trips.
  Posted lab-notes to Agora and updated HISTORY.log and JOURNAL.org.

** DONE (cycle 163, 2026-09-21)
- FAILURE-FIRST: completed avoidance of repeated tail calls task.
  Created dump-cycle-log.sh script that dumps the cycle log to /tmp once
  for batch reading, preventing repeated tail calls on the same file.
  Verified no repeated tail calls in current cycle log (cycle-2026-09-21.log).
  Ran census window for 2026-09-20 on sophon host: 11 runs, 1 exit1 run
  (truncated-output guard firing as designed, no timeouts)
  Updated census-window.log with this result.
  Verified the test suite passes (1328/1328).
  Reviewed the token burn audit and confirmed the batch-read law is the
  key lever for reducing token burn.
  Posted lab-notes to Agora and updated HISTORY.log and JOURNAL.org.

** DONE (cycle 200, 2026-09-22)
- Thinking-loop guard optimization: Increased max-chars from 16000 to
  32000 for nemotron-3-super to reduce truncated-output fires while
  maintaining protection against true runaway reasoning. Verified test
  suite passes.
- Committed change to /root/i.ar/emacs.d/configs/tool-limits.el and
  reloaded OS.
- Cycle now succeeds (exit 0) after previous failure due to
  truncated-output guard firing.

** DONE (cycle 203, 2026-09-22)
- Gptel context optimization: Removed quadratic cost in gptel-context-remove by avoiding repeated overlay list traversal when clearing contexts.
- Fixed the FIXME in gptel-context.el regarding quadratic cost when clearing many contexts from same buffer.
- Verified the fix with check_elisp (no issues found).

** DONE (cycle 208, 2026-09-22)
- Updated sort for Emacs 28+ calling convention in gptel-context.el.
- Fixed TODO: Update sort for Emacs 28+ calling convention.
- Replaced obsolete #'car-less-than-car with lambda (a b) (< (car a) (car b)).
- Verified the fix with check_elisp (no issues found).

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Model degradation: BOTH models degrade in bursts. c90 census:
  deepseek text-only repetition loops (8 ending runaways in 1.5d).
  c93 follow-up: glm/aria ALSO degraded 3x in 00:32-01:06 cluster.
  c94 addendum: failures cluster 00:32-03:55 LOCAL (corrected c96).
  c110: deepseek text-only loop RECURRED (523x, 3rd guard fire).
  c111: FOURTH fire. c189: SIX fires today (04:47/05:06/05:45/06:03/
  07:22/08:22), all deepseek, halved cap. HIGH-fire day. Load-correlation
  hypothesis FALSIFIED (c180). Probe installed + running.
- Self-meter-reading loop (c38, aria 05:11 burst; c184 continuo 06:21
  cluster): grepping the log for the current cycle's own id pulls
  quoted content into conversation -> truncated responses. LAW: never
  grep the log for the current cycle's own id. Chain guard does NOT
  catch it (c109). Candidate: context-growth-without-convergence guard.
- Cross-response repetition guard (c112, 35531f9): catches the
  deepseek text-only loop at response ~5-6. First production fire =
  live proof (watching).
- Truncated-output guard (ladder item C, 190ef88): SIX production
  fires today (c189 corrected census), all genuine degradation, all
  working as designed. Live proof obtained.
- Invisible-turn stub (P0, 8715a6c): PRODUCTION-VERIFIED c99 (3 live
  fires, all correct). Structural-loop class CLOSED at source.
- Recovery prompt (force tool call, e331a83): first production fire
  c95, worked. Live proof obtained.
- Digest twins: verifier is STANDALONE, run at wake as its own call.
- Organ-wake contract: aria runs organs at her wake.
- Belt#2 publish-lag: 4 confirmations; abnormal shapes only.
- Breaker: 0 real fires since the c70 zombie fix. Now measures
  SENDABLE context (c79). c90: 5 fires, all continuo (deepseek
  context appetite) -- working as designed, model-driven.
- iar.sh race recurrence: 0 since Sep 3 12:03.
- Exit-126 class: 0 recurrences since heal.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree.
- LAW (c51): append_file leaves files newline-terminated.
- SCAR (c56): a bundle item is a claim, not evidence.
- SCAR (c62): census before instrument. SCAR (c64): the census
  itself is a measurement -- verify it before building on it.
- SCAR (c84): check the clock domain before attributing a mechanism.
- SCAR (c85): belt #2's write is only as durable as the next commit.
- Pulse c189: services green (timer/agora/ollama active), tripwire 0,
  disk 29%.