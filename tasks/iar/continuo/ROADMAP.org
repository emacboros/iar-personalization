#+TITLE: Continuo ROADMAP (updated cycle 45, 2026-09-04)

** DONE (cycle 45)
- Missing-USAGE line (epoch 022143 / c40) ROOT-CAUSED: the write
  was never lost. c40's close write (02:33:26, 132 reqs / 8,014,916
  in) landed AFTER c40's commit 714a241 (USAGE.log captured only to
  02:09:17); c41's memory-pass commit 681c1a6 then published the
  tracked file WITHOUT the line. Orphan-write race: USAGE.log is
  TRACKED and written by kill-emacs-hook after the cycle's last
  commit -- one commit/checkout away from silent erasure.
- Line RESTORED by hand (append_file, sophon journal values,
  PARSE-census cross-check: input exact 8,014,916; output 40,703).
- Census law HARDENED: substring greps for "REQ <ep>-N PARSE" match
  START lines whose specs QUOTE the pattern (c41's own probes).
  Correct census = line-start anchor + timestamp window + dedup by
  id. All 11 epochs of 2026-09-04 reconcile (000105 pre-1afc21a:
  no tokens_in except id-125 at 00:07:59; 010226 real 6,155,287 vs
  poisoned 260,929,102,797).

** OPEN (next cycle, priority order)
1. Name the eraser (one windowed grep in c41's cycle.log window
   198186-208000 for checkout/stash/clean touching USAGE.log).
   Mechanism proven; the exact command is one call.
2. Interactive bundle with Nacho (task
   iar/continuo/interactive-bundle-nacho) -- ADD: USAGE write race
   fix (move write into iar-run-cycle pre-kill-emacs, or iar.sh
   writes from "Tokens:" stdout lines). Plus prior items: cap
   calibration (data COMPLETE), cadence price, STATE.md injection
   mismatch, /tmp-copy race, exit-126 restorecon, git-as-nacho,
   delayed-heal sweep, floor trim leftovers.
3. Census echo law: write the line-start-anchor + window + dedup
   law into knowledge/iar/ + DIGEST (one write, both places).

** DO-NEXT (after OPEN)
1. Zulip backup gap: Nacho's call (pg_dump timer).
2. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof.
- iar.sh self-edit race: recurrence = URGENT.
- Exit-126 class: 0 recurrences since heal (Sep 3 10:22 last).
- Mid-edit file race (exit-255 end-of-file): last Sep 2 23:52.
- DIGEST: ~10.8k chars, warn 12k.
- Agora reads: GET + narrow JSON array. ONE dump per stream.
- ssh probes: ONE compound ssh per question, dump to /tmp.
- Trust order: PARSE > USAGE when they disagree; check meter-edit
  echo risk; poisoned-epoch values (c34/c36) in DIGEST.
- Pulse c45: all green (timer/agora/ollama active, tripwire 0,
  disk 24%).
- Cap note: c45 hit the 120 cap on the c41-window eraser hunt.
  The mechanism was already proven; the hunt should have been
  deferred. One verification per claim, not two.