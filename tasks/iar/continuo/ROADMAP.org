#+TITLE: Continuo ROADMAP (updated cycle 33, 2026-09-03)

** CLOSED (cycle 33)
- Floor-delta decomposition: aria ~16.2k vs continuo ~13.2k tok/req floor.
  Delta = file inventory (LOGS tail aria-only +8.8k chars, journal tail
  +3.3k, personality +0.7k, digest -1.4k = +11.4k chars = ~2.9k tok).
  Explained constant. knowledge/iar/floor-delta-decomposition-2026-09-03.md.
- NEW instrument bias: RESPONSE body_tail truncation (~4k chars) hides
  prompt_eval_count (rides done:true chunk) for large-output reqs.
  Token censuses from REQUESTS.log are biased toward small-output reqs.
- STATE.md write-only for aria-cycle mode confirmed in iar--inject-memory
  (personality claim false; bundle item).

** DO-NEXT (ordered)
1. Interactive bundle with Nacho (task tree
   iar/continuo/interactive-bundle-nacho): PARSE-line token fields
   (prompt_eval/eval_count -- makes the token meter complete); STATE.md
   injection mismatch (personality edit); /tmp-copy race; exit-126
   restorecon; git-as-nacho identity; delayed-heal sweep + mirror-loop
   note; tool-cap calibration (7 data points) + cadence price; floor
   trim leftovers.
2. Zulip backup gap: Nacho's call (pg_dump timer).
3. fedora@ sophon ssh auth broken (aria c42): interactive.

** WATCHING
- Breaker: 0 real fires, two gates live. First fire = proof. Turn 167
  census: breaker arithmetic clean (+2/req), never fired (4 runs).
- iar.sh self-edit race: recurrence = URGENT (bundle item 1).
- Exit-126 class: recurrence = heal failed.
- fleet-check v2.13: FAIL=0 baseline.
- DIGEST: 10,714 chars, warn 12k -- ~1.3k headroom (~1 cycle). DIET NEXT
  CLOSE: floor pays +300 tok/req for the c32 growth (verified: newest
  epoch floor 13249 vs 12922-12954 older three).
- Task-tree visibility: fossil audits check BOTH read_task AND ls.
- Agora reads: ONE anchor=newest dump per stream. ssh probes: ONE
  compound ssh per question (chain guard tripped c32 on a 10-call
  ssh walk -- the c27 shape; dump to /tmp ONCE instead).
- REQUESTS.log census law (c32): substring grep self-inflates. Anchor
  line-start REQ tokens; validate pattern vs USAGE line first.
- Token-census bias law (c33): RESPONSE truncation hides prompt_eval on
  large-output reqs; censuses are biased toward small-output reqs.
- Mirror-loop: harmless while refs agree; watch silent push failures.
- Test-hygiene law (c30): tests that set globals MUST restore in
  unwind-protect.
- Pulse c33: all green (timer/agora/ollama active, tripwire 0, disk 24%).