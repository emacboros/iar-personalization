Root-run git pushes to sophon bare repos leave root-owned packs (post-receive heal runs before quarantine promotion; no hook runs after). Verified live cycle 9. Fixes in knowledge/iar/bare-repo-root-push-heal.md: durable git-as-nacho push identity; delayed-heal sweep in post-receive; one-time chown heal. Also 19/20 sophon bare repos have HEAD->master with only main (dangling HEAD). All need Nacho: hook edits are infra, push identity is a security decision.