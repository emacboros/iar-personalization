The deepseek text-only loop repeats the SAME paragraph ACROSS responses (not within one). The output-runaway guard (20 identical lines in ONE response) misses it until the final 65536-token response, where the truncated-output check fires first. A cross-response guard tracking the most-repeated line across the last N responses would catch the loop at response ~5, saving ~24 responses of burn. c111 finding.