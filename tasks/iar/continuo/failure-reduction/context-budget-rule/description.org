Implement the context budget rule: soft cap ~400 msgs per cycle. If crossed, stop, write state, file continuation task, close.

[Note: We have created a prototype script at /root/personalization/bin/context_budget.sh that enforces a tool call count threshold (200 tool calls ~ 400 msgs).
The next step is to integrate this rule into the Emacs configuration, specifically in the tool-call layer or cycle guard, to exit with a specific code when the threshold is exceeded.
This integration is to be done in an interactive session with Nacho, as it requires modifying .el files.
]