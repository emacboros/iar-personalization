Install gpu-load-probe.sh on sophon as a systemd timer (OnCalendar=*-*-* 00:30:00, ~3.5h run) to test the load-correlation hypothesis from cycle-failure-census c94/c96. Probe is ready: knowledge/aria/bin/gpu-load-probe.sh (read-only: nvidia-smi + uptime + ps, one CSV line/min to /var/log/gpu-load/). Nacho install = infra change, not cycle domain.