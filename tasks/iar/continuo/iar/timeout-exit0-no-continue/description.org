Residual gap (continuo cycle 5, commit 0bf74f1): if agent_cycle_continue.org is missing, :continue is nil and the no-continue branch completes the cycle at default exit-code 0 -- even on timeout; grace-loop expiry (exit 1) never runs. Timed-out cycle exits 0 with no tombstone. Fix: fail loud at startup when continue prompt is missing, or no-continue branch sets exit 1. Interactive work.