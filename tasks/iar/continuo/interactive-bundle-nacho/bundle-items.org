* Belt #3: USAGE publish-lag exposure (c60/c61, QUEUED for bundle)

Belt #2 (pre-exit write) fixed the WRITE race; PUBLISH is
one-cycle-deferred by design: the session line lands on disk
uncommitted, and the NEXT cycle's memory-pass add -A publishes it.
Pause windows extend the lag to the pause duration (proven c60:
aria c25 + continuo c59 lines uncommitted 2 days). Belt #3 =
iar.sh writes the session line itself from the "Tokens:" stdout
after emacs exits -- moves the write outside the Emacs lifetime
entirely, publish lag becomes zero. Optional; bundle item.

* iar.sh self-edit race [CORRECTED c56 2026-09-04]

The old "copies itself to /tmp" premise was FALSE -- rotate.sh
line 12 execs iar.sh IN PLACE from the repo path; no copy step
exists in the chain. The Sep 3 12:03 exit-127 was the documented
incremental-read race (knowledge/iar/iarsh-self-edit-race.md):
commit 30c5385 inserted a line upstream of the running reader's
buffered fd offset; the next read chunk garbled mid-line; bash
executed the bare string tail -> 127.

Real fix unchanged in kind, corrected in location: rotate.sh
should copy iar.sh to /tmp/iar-$HASH.sh and exec THE COPY (one
line in rotate.sh, interactive). Full analysis:
knowledge/iar/iarsh-self-edit-race-rotation-copy.md.

Recurrence watch: 0 since Sep 3 12:03. URGENT-on-recurrence
trigger unchanged.

* exit-126 durable fix

Container start death: lsetxattr EPERM when :z relabel hits root-owned
files in a mount path. ExecStartPre auto-heal chowns but does not
relabel. Durable fix: restorecon in ExecStartPre (interactive: needs
systemd unit edit + policy decision).

* git-as-nacho push identity

Cycle containers push as root via file-path; receive-pack leaves
root-owned files AFTER the hook heal runs. Durable fix at source: push
as nacho/git user with preauth key. Kills pollution class entirely.

* delayed-heal sweep in post-receive (proposal 2)

Only remaining pollution fix if push identity stays root. ALSO note:
mirror loop is real both directions (sophon hook pushes to rammstein,
rammstein hook pushes back to sophon) -- harmless when refs agree, but
by construction a loop; consider a loop-prevention env var or
one-directional mirror config while editing the hook.

* tool-cap calibration + cadence price

Cap 120 pressed 3x across hemispheres (aria c43/c44, continuo c26),
always in landing phase. Either raise cap (burn ceiling rises) or
pre-trim walks. Cadence price ~360M input tok/day (c38 cap-price doc; supersedes
the ~250M estimate in knowledge/iar/burn-decomposition-2026-09-03.md
-- newer number includes capped-cycle premium + both hemispheres).
Nacho's tradeoff.

* floor trim leftovers

check_elisp vacuous-OK; check_ollama model probe (host-only preflight
lets cloud-model 403 pass); sidecar podman socket bridge (real fix =
security decision). Each small, each interactive-adjacent.

* STATE.md injection mismatch

STATE.md is write-only for aria-cycle mode (personality edit
needed) -- the injection mismatch fix is a personality-file
change, interactive. Detail in aria's cycle notes (c33 era).

* Mirror push silent failure (git@10.66.0.1 preauth)

Bare post-receive mirror push to rammstein fails: git user on
sophon lacks a key accepted by rammstein (publickey preauth
fail). Mirror is silently dead -- same silent-failure class as
the old tripwire. Fix: preauth key for git@sophon ->
rammstein, interactive (key generation + authorized_keys).