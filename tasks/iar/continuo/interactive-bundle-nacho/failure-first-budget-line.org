* FAILURE-FIRST budget line (aria's flag, for-nacho 466) -- Nacho to land

** The problem (verified this cycle from primary evidence)

The FAILURE-FIRST clause in the cycle prompt (prompts/cycles/
continuo_daily.org + aria_daily.org) gives total license:
"root-causing and fixing THAT failure IS this cycle's work."
glm-5.3-flash reads that as "read everything."

The night of 2026-09-07 (local 00:32-01:06 = UTC 03:32-04:06):
three consecutive aria exit-1s, 20.3M input tokens, all
failure-first corpse-reads. Each dead cycle left a bigger corpse
for the next cycle to read, and the reading tripped the caps and
made a new corpse.

Verified from the rotate journal + REQUESTS.log.1:
- 00:32 (epoch 260907031023): census-heavy cycle, 193 real
  execute_code_local calls, hit the 120 soft cap, 5 ignored
  blocks, hard cap ended it. Legitimate work (invisible-turn
  census) but probe-heavy style.
- 00:44 (260907034029): loop-guard HARD STOP -- 6 identical
  execute_code_local calls (same census greps repeated).
- 01:06 (260907040054): invisible-turn reproduction cycle, 257
  requests, died inside the mechanism it was reading.

** The fix (aria's proposal, verified working tool exists)

Add a budget line to the FAILURE-FIRST clause:
"one triage call (failure-triage.sh), then fix, verify, converge
-- the corpse is data, not a place to live."

The tool already exists and is verified: knowledge/aria/bin/
failure-triage.sh (commit 60bc991). One call, ~2k tokens, gates
on status:failed and refuses to triage OK cycles. Verified this
cycle: refuses to triage both aria and continuo (both last-cycle
ok). The budget line is the missing piece -- the prompt does not
currently tell the model to use the tool.

** Why this is interactive-session work

The budget line is a cycle-prompt edit (prompts/cycles/*.org).
Per the personality law (no prompt edits, no archetype edits, no
cycle edits -- changes to own architecture go through interactive
sessions with Nacho), continuo cannot land it from a cycle.

** Status

Filed 2026-09-07 (continuo cycle 104). Nacho to land in an
interactive session. The failure-triage.sh tool is ready and
verified; the budget line is the one-line prompt change that
activates it.
