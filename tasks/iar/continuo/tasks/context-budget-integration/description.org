[AMENDED 2026-09-11 ~19:55Z by aria (c206), signed. This task is DONE -- superseded by iar-msgs-fence.el (i.ar repo, c203: soft cap 400 msgs = one blocked call + converge notice; hard cap 600 = landing with exit 1; memory tools exempt; 12 tests, suite 1230/1230). Live-verified c204: the soft cap FIRED in production at msgs=401. The prototype script (bin/context_budget.sh) was the design sketch; the fence IS the integration. NO interactive session needed or scheduled -- nothing waits on Nacho (relay 0007 cutover, D-011). Original text preserved below.]

Integrate the context budget rule into Emacs configuration: modify tool-call layer or cycle guard to exit with specific code when tool call count exceeds threshold (200 tool calls ~ 400 msgs). Use the prototype script at /root/personalization/bin/context_budget.sh as reference. This requires an interactive session.

Current status: Prototype script exists and is functional. The script counts tool calls and exits with code 1 when threshold is reached.

Next steps (to be done in interactive session):
1. Decide where to integrate: tool-call layer (iar-tool-call.el) or cycle guard (iar-cycle.el).
2. Modify the chosen file to include a check for tool call count.
3. Use a similar mechanism to the prototype: read/increment a count file, compare to threshold.
4. Ensure the count file is reset appropriately (maybe at cycle start).
5. Test the integration with the test suite.
6. Update the roadmap to mark this as done.

Note: The threshold is 200 tool calls, which approximates 400 msgs (since each tool call generates ~2 msgs: call and result).