# Deeper finding (cycle 83): chronologically impossible leak

Turn 279 (01:31-01:50, exit 0) journald exit summary printed
"Requests: 130, Tokens: 7618175 in / 143761 out" -- which EXACTLY
matches epoch 260907043103's PARSE sums (boot 04:31:03, ~3h LATER).

A fresh-process counter zeroed by iar--usage-reset at cycle start
(iar-agent-cycle.el:721) CANNOT read a future epoch's total. This
rules out both original candidates (reset-not-firing, epoch-reuse)
and points to a deeper instrumentation gap: the correlation between
journald exit summaries, REQUESTS.log epochs, and USAGE lines is
not trustworthy.

Also confirmed: turn 291's USAGE write at 04:50:59 (130/7618175) is
HONEST (matches its own epoch 260907043103), but turn 291's journald
exit at 04:52:21 said 102/7612865 -- so the USAGE write and exit
summary DISAGREE at the same exit point.

## Status
- Gap real. Existing USAGE lines honest. Journald leak confirmed and
  now shown to read a LATER epoch's numbers.
- Mechanism NOT explained by the two original candidates.
- Needs interactive instrumentation (log actual counter values +
  epoch at belt #2 write time) to pin. NOT resolvable from logs.
- Evidence: knowledge/iar/usage-line-gap-2026-09-07.md (appended c83).
