# LAB-PATH addendum (2026-09-03, later same session -- changes the verdict logic)

Nacho's articulation of what actually sold him: the dog is a
LAB BENCH THAT IS ALWAYS SET UP. His hardware ideas die in
drawers from SETUP TAX (get board, find cables, connect
programmer, boot environment); software ideas land because the
environment is always already there (Emacs, repos, sophon).
He already killed setup tax once by putting FPGAs on sophon
for remote programming. The dog generalizes it: drawer space
becomes rack space. Payload modules stay connected forever.
Hardware that behaves like software: no end state, unbounded
payload space, setup tax paid once per module, never per idea.

## Verdict logic change (cycle-me: read this before writing
the BUY/WAIT verdict)

The project now has TWO independent justifications:

1. GUT PATH (FPGA spine, MITM ladder) -- depends on the
   motor-protocol answer. Hostile/encrypted protocol does NOT
   kill the project; it defers the gut path indefinitely.
2. LAB PATH (recon-independent) -- stock dog + strapped
   RPi/SDR/FPGA + walking eye + RF patrol + remote FPGA bench
   works on stock firmware, no gut needed. Nacho's SDR gets
   its use here. The lab argument alone can justify BUY if
   the basic facts check out (LiDAR present on Air, import
   path sane, price as listed).

So: "motor protocol hostile" no longer means WAIT/NO-BUY. It
means "gut path deferred, lab path proceeds." Only a compound
failure (hostile protocol AND Air lacks LiDAR AND import path
broken) justifies ALTERNATIVES.

## First real project (design consequence, for the record)

PAYLOAD CHASSIS before sensors: power distribution board +
network (USB hub or ethernet) + standard mounting + small
always-on supervisor. Every payload after it is plug-in, not
custom wiring -- or the setup tax returns and the lab is a
lie. Chassis is electronics (his zone), not mechanics.
The lab's "ease of use" tooling (remote flashing, module
inventory, power daemons, bus dashboard) is SOFTWARE = mind
work, cycle-able. The dog feeds i.ar; i.ar feeds the dog.

## Standing context

Nacho's survival checks (from his own i.ar post-mortem):
low idle cost, entertainment value, practical utility, sprint
compatibility, NO END STATE. The dog passed all five in
conversation. Excitement survived the second night; the
purchase decision waits for this recon's verdict.