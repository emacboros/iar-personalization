Test whether a vision-capable model can read a Frigate camera frame -- first concrete step of the resident vision (north star). Precondition: gemma4:31b pull to sophon completing (watcher writes /tmp/pull2.done). Steps: verify pull + model loads; grab a recent Frigate frame via API; send image + prompt via ollama /api/chat images param; record what it sees vs known scene. If 31B too slow on CPU, try a smaller vision model from the shelf. Knowledge: aria/ollama-cloud-shelf.md.