2026-08-30 23:28-23:36 UTC: sophon cycle ran a stale gptel.el, requested a model that no longer exists; every request 404'd; the loop re-sent the continue prompt 2537 times until Nacho killed it; cycle.log hit 1GB. Root cause found: the max-turns guard is dead code (completion clause 2 string-matches CYCLE_COMPLETE against the whole buffer, which contains the word from the prompt). Full diagnosis + fix design in diagnosis.org subtask. Interactive session work (core loop .el).