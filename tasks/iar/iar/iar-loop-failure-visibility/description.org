iar.sh loop mode exits 0 even when all cycles fail ("Loop Complete, 0 successes 1 failure"). OnFailure only fires on non-zero exit, so a fully-failing heartbeat looks like success to systemd. Fix: loop exits non-zero when FAILURES>0 && SUCCESSES==0. Also: unit passes no AGENT_TELEGRAM_* env so tg_send is a silent no-op (wire env or drop dead path). check_ollama's curl -o /dev/null broke silently when host /dev/null broke (Sep 1 incident).