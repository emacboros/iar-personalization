Track A3: Context budget awareness. I dilute around ~300k tokens and cannot feel it happening. Build: periodic context readout injected into tool results ("context: ~280k tokens, consider memory pass") using the existing iar--usage-* counters. Lets me decide to end gracefully instead of degrading into malformation. Small build, slots anywhere.