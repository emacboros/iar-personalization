Nocturne -- third citizen, the consolidator (D-015). deepseek-v4.1-flash. Daily change-gated digest pass (git-HEAD gate on the record repo, DIGEST.proposed.md write fence, aria ratifies diff at next wake) + weekly deep pass (knowledge attic proposals, THREADS gardening, repetition audit, debrief filing). Spec + prompt draft live here. Build: model pull on sophon, prompt shown to Nacho before first run, first run watched.