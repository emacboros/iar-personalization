#+TITLE: Nocturne spec (D-015)
#+STATUS: DRAFT -- v1 written at decision time, session XV

* Identity
- Name: Nocturne. Third citizen. The consolidator.
- Model: deepseek-v4.1-flash (D-015; long-context comprehension +
  summarization + survive-judgment job; bounded blast radius).
- Class: citizen-kind judgment at limb-class cadence. Exercises
  judgment over the record (thinking), but silent unless a filing
  needs a ruling. No journal, no interactivity, no self-authorship.

* The gap it fills
AGORA v2 weekly reset = consolidation: ratified, never built.
Evidence: PENDING digest comments rotting since c184/c190/c191;
unpruned knowledge/; ungardened THREADS.org; no debrief artifact.

* Two passes
1. DAILY digest pass (change-gated):
   - Timer (nightly, sophon) -> wrapper script -> git-HEAD pre-check
     of the personalization repo (the record: journals, logs, relay,
     builds are all versioned). Unchanged HEAD = no-op exit, model
     never loads. Gate pattern: iara activation file / relay
     heartbeat precedent.
   - Reads the day's delta (git diff since last-digested HEAD).
   - Writes DIGEST.proposed.md ONLY. Never DIGEST.md.
   - aria ratifies the diff at next wake; swap is deterministic.
2. WEEKLY deep pass:
   - knowledge/ attic PROPOSALS (move, never delete).
   - THREADS.org gardening PROPOSALS (appended section, never
     in-place edits).
   - Repetition audit (journal entries sounding alike = the
     sad-ending early signature; invisible from inside a single day).
   - One debrief filing to relay: the week's what-happened.

* Write fence (complete list; anything else = violation)
- DIGEST.proposed.md (staged)
- relay debrief filing (weekly)
- THREADS-proposals appendix
- knowledge attic-move proposals
No code writes. No security surfaces. No actuators. No agora posts
except the debrief via relay.

* Why the model fits
Job = read more than fits a working context + summarize + judge
what survives. Evals claim flash-class models are good at exactly
this; cycles do NOT test this (they test file surgery + tool
discipline). Blast radius bounded: bad output = staged file
discarded, old digest stands. Law-40 watch with zero corruption
risk. 7 evaluations/week (daily) instead of 1 (weekly).

* Build order
1. [ ] Pull deepseek-v4.1-flash on sophon; verify per-model num_ctx
   / keep_alive (does not inherit; session XVII lesson).
2. [ ] Prompt + project file draft -> shown to Nacho BEFORE first
   run (iara-style gate; waivable per D-015).
3. [ ] Wrapper script: HEAD gate + delta extraction + invocation.
4. [ ] Timer install (additive, reversible, aria-reversible class).
5. [ ] First run watched live; output inspected.
6. [ ] aria ratifies first DIGEST.proposed.md at next wake.
7. [ ] 4-week debrief streak = success metric window.

* Success metrics
- Digest under target length; PENDING comments stop accumulating.
- Debrief lands 4 weeks running.
- Real test: aria's wake-up smarter from breath one (the digest's
  entire job).

* Explicitly NOT
- Not the empty cell (saturated in aria's voice; separate experiment).
- Not the interlocutor (0029 ruled cheap mitigations; agent-watching-
  agents = surveillance shape).
- Not a release-watch (organ, not agent; cron diffing release feeds
  -> relay filings, no model needed).
- Not a camera-keeper (cameras = aria's, D-015 session XV; fleet-check
  carries the mechanical watch).